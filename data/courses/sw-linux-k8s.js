export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '🐧',
    title: 'Overview: Linux & Kubernetes อย่างมืออาชีพ',
    content: '<h2>ทำไม Developer ทุกคนต้องรู้ Linux และ Kubernetes?</h2>\n<p>ในโลกของการพัฒนาซอฟต์แวร์ยุคปัจจุบัน ไม่ว่าคุณจะเป็น Backend Developer, Data Engineer, DevOps Engineer หรือแม้แต่ Frontend Developer — ความรู้เรื่อง <strong>Linux</strong> และ <strong>Kubernetes</strong> กลายเป็นทักษะที่แทบขาดไม่ได้ในอุตสาหกรรม Server กว่า 96% ที่รันบน Internet ล้วนใช้ระบบปฏิบัติการตระกูล Linux และแอปพลิเคชันขนาดใหญ่เกือบทั้งหมดในองค์กร Fortune 500 ต่างย้ายมาบน Kubernetes แล้ว</p>\n<h2>ผลกระทบต่ออาชีพของคุณ</h2>\n<p>ข้อมูลจาก Stack Overflow Developer Survey และ LinkedIn Jobs ชี้ให้เห็นว่า ตำแหน่งงานที่ระบุ Kubernetes เป็น Required Skill มีอัตราเงินเดือนสูงกว่าค่าเฉลี่ยถึง <strong>30–50%</strong> ทักษะ Linux + Container + Kubernetes ถือเป็น Trifecta ที่ทำให้คุณเป็นที่ต้องการในตลาดแรงงาน ทั้งในประเทศและระดับสากล</p>\n<ul>\n<li>🏢 <strong>บริษัทเทคโนโลยี</strong> — Google, Meta, Grab, Agoda ล้วนใช้ Kubernetes ใน Production</li>\n<li>💰 <strong>Salary Premium</strong> — DevOps/SRE ที่เชี่ยวชาญ K8s รับเงินเดือนเฉลี่ย 80,000–150,000 บาท/เดือน ในไทย</li>\n<li>🌍 <strong>Remote Work</strong> — ทักษะ Infrastructure เปิดโอกาสทำงาน Remote กับบริษัทต่างประเทศ</li>\n<li>🚀 <strong>Career Growth</strong> — เส้นทางสู่ Cloud Architect, SRE, Platform Engineer</li>\n</ul>\n<h2>คุณจะได้เรียนรู้อะไรในคอร์สนี้?</h2>\n<p>คอร์สนี้ออกแบบมาเพื่อพาคุณจาก 0 ไปถึง Production-ready ในเวลาที่สั้นที่สุด ด้วย Hands-on Labs ที่ทำได้จริงทุกบท</p>\n<ol>\n<li><strong>Linux Fundamentals</strong> — พื้นฐานที่แข็งแกร่ง: File system, Permissions, Package Management</li>\n<li><strong>Shell Scripting</strong> — Automate งานซ้ำๆ ด้วย Bash Scripts อย่างมืออาชีพ</li>\n<li><strong>Linux Networking</strong> — เข้าใจ TCP/IP, DNS, SSH, SSL ระดับ Engineer</li>\n<li><strong>Server Management</strong> — ดูแล Nginx, systemd, Logs บน Production Server</li>\n<li><strong>Docker</strong> — Containerization ตั้งแต่พื้นฐานจนถึง Multi-stage Builds</li>\n<li><strong>Kubernetes Architecture</strong> — เข้าใจ Control Plane, Pods, Services อย่างลึกซึ้ง</li>\n<li><strong>Kubernetes Workloads</strong> — ConfigMaps, Secrets, PersistentVolumes, Ingress</li>\n<li><strong>Kubernetes Operations</strong> — kubectl, Helm, RBAC, Monitoring ด้วย Prometheus + Grafana</li>\n<li><strong>Production Kubernetes</strong> — HPA, Resource Limits, Disaster Recovery</li>\n</ol>\n<h2>Prerequisites</h2>\n<p>คอร์สนี้เหมาะสำหรับผู้ที่มีพื้นฐาน Programming อยู่บ้างแล้ว ไม่จำเป็นต้องเคยใช้ Linux มาก่อน แต่ควรเข้าใจแนวคิดพื้นฐานของ Software Development เช่น การทำงานของ Web Server, Database, และ Network เบื้องต้น</p>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> เตรียม Environment ด้วยการติดตั้ง VirtualBox + Ubuntu 22.04 LTS หรือใช้ WSL2 บน Windows เพื่อทำ Labs ตามได้ทันที นอกจากนี้ยังสามารถใช้ Play with Docker และ Killercoda เป็น Free Online Labs ได้ด้วย</div>',
  },
  {
    number: 1,
    slug: 'linux-basics',
    emoji: '🖥️',
    title: 'Linux Fundamentals: รากฐานที่แข็งแกร่ง',
    content: '<h2>Linux File System Hierarchy</h2>\n<p>Linux ใช้ Filesystem Hierarchy Standard (FHS) ในการจัดระเบียบไฟล์และโฟลเดอร์ ต่างจาก Windows ที่มี Drive Letter (C:, D:) Linux มี <strong>Root Directory</strong> เพียงตัวเดียวคือ <code>/</code> และทุกอย่างอยู่ภายใต้นั้น</p>\n<pre>/ (Root)\n├── /bin       — Essential user commands (ls, cp, mv)\n├── /etc       — System configuration files\n├── /home      — User home directories\n├── /var       — Variable data (logs, databases)\n├── /tmp       — Temporary files\n├── /usr       — User programs and libraries\n├── /opt       — Optional/third-party software\n└── /proc      — Virtual filesystem (process info)</pre>\n<h2>คำสั่งพื้นฐานที่ต้องรู้</h2>\n<p>คำสั่งต่อไปนี้เป็น Building Blocks ของทุกการทำงานบน Linux:</p>\n<pre>ls -la              # แสดงไฟล์ทั้งหมดรวม Hidden files\npwd                 # แสดง Current directory\ncd /var/log         # เปลี่ยน Directory\nmkdir -p /opt/app   # สร้าง Directory (รวม Parent)\ncp -r src/ dst/     # Copy Directory แบบ Recursive\nmv oldname newname  # ย้ายหรือเปลี่ยนชื่อไฟล์\nrm -rf /tmp/test    # ลบ Directory และเนื้อหาทั้งหมด\nfind / -name *.log  # ค้นหาไฟล์ในระบบ\ngrep -r "error" /var/log/  # ค้นหา Pattern ในไฟล์</pre>\n<h2>File Permissions ระบบสิทธิ์ที่ต้องเข้าใจ</h2>\n<p>Linux ใช้ระบบ Permission แบบ <strong>Owner / Group / Others</strong> ทุกไฟล์มีสิทธิ์ 3 ระดับ ได้แก่ Read (r=4), Write (w=2), Execute (x=1)</p>\n<pre>ls -la /etc/passwd\n-rw-r--r-- 1 root root 2847 Jul 13 07:00 /etc/passwd\n# Format: [type][owner][group][others]\n# -       = regular file (d = directory)\n# rw-     = owner can read+write\n# r--     = group can read only\n# r--     = others can read only\n\nchmod 755 script.sh   # rwxr-xr-x\nchmod 600 secret.key  # rw-------\nchown user:group file # เปลี่ยน Owner\nchown -R www-data:www-data /var/www/  # Recursive</pre>\n<h2>User Management</h2>\n<p>การจัดการ Users และ Groups เป็นส่วนสำคัญของ System Administration:</p>\n<pre>useradd -m -s /bin/bash john    # สร้าง User พร้อม Home directory\npasswd john                      # ตั้ง Password\nusermod -aG sudo john            # เพิ่ม User เข้า sudo group\nuserdel -r john                  # ลบ User พร้อม Home directory\ngroupadd developers              # สร้าง Group\nid john                          # ดู User ID และ Groups\nwhoami                           # ดู Current user\nsudo su -                        # Switch เป็น Root</pre>\n<h2>Package Management</h2>\n<p>แต่ละ Linux Distribution มี Package Manager ต่างกัน:</p>\n<pre># Debian/Ubuntu — APT\nsudo apt update                    # อัปเดต Package list\nsudo apt upgrade -y                # อัปเกรด Package ทั้งหมด\nsudo apt install nginx             # ติดตั้ง Package\nsudo apt remove nginx              # ถอนการติดตั้ง\nsudo apt autoremove               # ลบ Package ที่ไม่ใช้\napt search "web server"           # ค้นหา Package\n\n# RedHat/CentOS — YUM/DNF\nsudo yum install httpd\nsudo dnf install nginx\n\n# macOS — Homebrew\nbrew install nginx\nbrew update &amp;&amp; brew upgrade</pre>\n<h2>Process Management</h2>\n<pre>ps aux                  # แสดง Process ทั้งหมด\nps aux | grep nginx     # Filter Process\ntop                     # Real-time Process monitor\nhtop                    # Enhanced Process monitor\nkill -9 1234            # Force kill Process by PID\npkill nginx             # Kill Process by name\nnohup ./server &amp;        # รัน Process ใน Background</pre>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ <code>man &lt;command&gt;</code> เพื่ออ่าน Manual ของทุกคำสั่ง เช่น <code>man chmod</code> และใช้ <code>tldr chmod</code> (ต้องติดตั้งก่อน) สำหรับ Summary ที่กระชับกว่า เป็น Habit ที่ Engineer มืออาชีพทำเป็นประจำ</div>',
  },
  {
    number: 2,
    slug: 'shell',
    emoji: '💻',
    title: 'Shell Scripting Mastery: Automate ทุกอย่างด้วย Bash',
    content: '<h2>ทำไมต้อง Shell Scripting?</h2>\n<p>Shell Script คือการรวม Linux Commands หลายๆ คำสั่งเข้าด้วยกันเพื่อทำงานอัตโนมัติ ตั้งแต่การ Backup ข้อมูล, Deploy แอปพลิเคชัน, ไปจนถึงการ Monitor Server — Shell Script ช่วยลดงานซ้ำๆ และลด Human Error ได้อย่างมาก</p>\n<h2>โครงสร้างพื้นฐานของ Bash Script</h2>\n<pre>#!/bin/bash\n# Shebang line — บอก OS ว่าใช้ Interpreter ตัวไหน\n\nset -e          # หยุดทันทีหาก Command ใด Error\nset -u          # Error ถ้าใช้ตัวแปรที่ไม่ได้ define\nset -o pipefail # Detect error ใน pipe\n\necho "Script เริ่มทำงาน: $(date)"</pre>\n<h2>Variables และ String Operations</h2>\n<pre>#!/bin/bash\n\nAPP_NAME="my-app"\nVERSION="1.0.0"\nPORT=8080\n\necho "Starting $APP_NAME version $VERSION on port $PORT"\n\nCURRENT_DATE=$(date +%Y-%m-%d)\nHOSTNAME=$(hostname)\n\n# Array\nSERVERS=("web01" "web02" "web03")\necho "First server: ${SERVERS[0]}"\necho "All servers: ${SERVERS[@]}"\necho "Total servers: ${#SERVERS[@]}"</pre>\n<h2>Conditionals และ Loops</h2>\n<pre>#!/bin/bash\n\n# If-Else\nFILE="/etc/nginx/nginx.conf"\nif [ -f "$FILE" ]; then\n    echo "Nginx config พบแล้ว"\nelif [ -d "/etc/nginx" ]; then\n    echo "Directory มีอยู่ แต่ไม่มี config file"\nelse\n    echo "Nginx ยังไม่ได้ติดตั้ง"\nfi\n\n# For Loop\nfor SERVER in web01 web02 web03; do\n    echo "Checking $SERVER..."\n    ping -c 1 "$SERVER" &gt;/dev/null 2&gt;&amp;1 &amp;&amp; echo "  UP" || echo "  DOWN"\ndone\n\n# While Loop\nCOUNT=0\nwhile [ $COUNT -lt 5 ]; do\n    echo "Attempt $COUNT"\n    COUNT=$((COUNT + 1))\n    sleep 1\ndone</pre>\n<h2>Functions</h2>\n<pre>#!/bin/bash\n\nlog_info() {\n    echo "[INFO] $(date +%H:%M:%S) — $1"\n}\n\nlog_error() {\n    echo "[ERROR] $(date +%H:%M:%S) — $1" &gt;&amp;2\n}\n\ncheck_service() {\n    local SERVICE=$1\n    if systemctl is-active --quiet "$SERVICE"; then\n        log_info "$SERVICE is running"\n        return 0\n    else\n        log_error "$SERVICE is NOT running"\n        return 1\n    fi\n}\n\ncheck_service "nginx"\ncheck_service "postgresql"\n\nget_disk_usage() {\n    df -h / | tail -1 | awk \'{print $5}\' | tr -d \'%\'\n}\n\nUSAGE=$(get_disk_usage)\nif [ "$USAGE" -gt 85 ]; then\n    log_error "Disk usage critical: ${USAGE}%"\nfi</pre>\n<h2>Cron Jobs: ตั้งเวลาทำงานอัตโนมัติ</h2>\n<pre># เปิด crontab editor\ncrontab -e\n\n# Format: นาที ชั่วโมง วันที่ เดือน วันในสัปดาห์ คำสั่ง\n# ทุกวัน เวลา 02:00 น. — Backup database\n0 2 * * * /opt/scripts/backup.sh &gt;&gt; /var/log/backup.log 2&gt;&amp;1\n\n# ทุก 5 นาที — Health check\n*/5 * * * * /opt/scripts/healthcheck.sh\n\n# ทุกวันจันทร์ เวลา 09:00 — Weekly report\n0 9 * * 1 /opt/scripts/weekly-report.sh\n\n# ดู Cron jobs ปัจจุบัน\ncrontab -l</pre>\n<h2>Automation Script ตัวอย่างจริง</h2>\n<pre>#!/bin/bash\n# deploy.sh — Automated deployment script\n\nset -euo pipefail\n\nAPP_DIR="/opt/myapp"\nBACKUP_DIR="/opt/backups"\n\nlog_info() { echo "[$(date +\'%Y-%m-%d %H:%M:%S\')] INFO: $1"; }\nlog_error() { echo "[$(date +\'%Y-%m-%d %H:%M:%S\')] ERROR: $1" &gt;&amp;2; }\n\n# Step 1: Backup\nlog_info "Creating backup..."\nmkdir -p "$BACKUP_DIR"\ntar -czf "$BACKUP_DIR/app-$(date +%Y%m%d-%H%M%S).tar.gz" "$APP_DIR" 2&gt;/dev/null || true\n\n# Step 2: Pull latest code\nlog_info "Pulling latest code..."\ncd "$APP_DIR" &amp;&amp; git pull origin main\n\n# Step 3: Install dependencies\nlog_info "Installing dependencies..."\nnpm ci --production\n\n# Step 4: Restart service\nlog_info "Restarting service..."\nsystemctl restart myapp\nsleep 3\n\n# Step 5: Verify\nif systemctl is-active --quiet myapp; then\n    log_info "Deployment successful!"\nelse\n    log_error "Deployment failed!"\n    exit 1\nfi</pre>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ <code>shellcheck</code> เป็น Linter ตรวจสอบ Script ก่อน Run เสมอ ติดตั้งด้วย <code>apt install shellcheck</code> แล้วรัน <code>shellcheck script.sh</code> เพื่อหา Bug และ Best Practice violations ก่อน Deploy จริง</div>',
  },
  {
    number: 3,
    slug: 'networking',
    emoji: '🌐',
    title: 'Networking in Linux: เข้าใจ Network ระดับ Engineer',
    content: '<h2>พื้นฐาน Network ที่ Linux Admin ต้องรู้</h2>\n<p>การเข้าใจ Networking ใน Linux เป็นทักษะสำคัญสำหรับการ Debug ปัญหา Production, ตั้งค่า Security, และทำ Infrastructure Engineering การรู้ว่า Traffic ไหลอย่างไร, Port ไหนเปิดอยู่, และ Service ไหนกำลัง Listen อยู่ทำให้แก้ปัญหาได้เร็วขึ้นมาก</p>\n<h2>IP Address และ Network Configuration</h2>\n<pre># ดู Network Interfaces ทั้งหมด\nip addr show\nip a                    # Short form\nifconfig               # Legacy command\n\n# ดู Routing Table\nip route show\n\n# ตั้ง Static IP (ชั่วคราว)\nip addr add 192.168.1.100/24 dev eth0\nip route add default via 192.168.1.1\n\n# Ubuntu 20.04+ ใช้ Netplan\ncat /etc/netplan/00-installer-config.yaml\nsudo netplan apply</pre>\n<h2>DNS Troubleshooting</h2>\n<pre># ตรวจสอบ DNS resolution\nnslookup google.com\ndig google.com\ndig google.com A         # Query A record\ndig google.com MX        # Query MX record\ndig @8.8.8.8 google.com  # ใช้ DNS Server เฉพาะ\n\n# ดู DNS Config\ncat /etc/resolv.conf\ncat /etc/hosts\n\n# Flush DNS Cache\nsudo systemd-resolve --flush-caches\nsudo resolvectl flush-caches</pre>\n<h2>Port Scanning และ Network Diagnostics</h2>\n<pre># ดู Open Ports และ Listening Services\nss -tlnp              # TCP listening ports\nss -ulnp              # UDP listening ports\nnetstat -tlnp          # Legacy (ต้องติดตั้ง net-tools)\n\n# ตรวจสอบ Port เฉพาะ\nss -tlnp | grep :80\nlsof -i :8080\n\n# Port Connectivity Test\nnc -zv hostname 80     # TCP check\nnc -zvu hostname 53    # UDP check\ntelnet hostname 3306   # Database port check\n\n# Packet Analysis\ntcpdump -i eth0 port 80\ntcpdump -i any host 192.168.1.1</pre>\n<h2>iptables: Linux Firewall</h2>\n<pre># ดู Rules ปัจจุบัน\nsudo iptables -L -n -v\n\n# อนุญาต Incoming HTTP/HTTPS\nsudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT\nsudo iptables -A INPUT -p tcp --dport 443 -j ACCEPT\n\n# อนุญาต SSH จาก IP เฉพาะเท่านั้น\nsudo iptables -A INPUT -p tcp -s 10.0.0.0/24 --dport 22 -j ACCEPT\nsudo iptables -A INPUT -p tcp --dport 22 -j DROP\n\n# Block IP Address\nsudo iptables -A INPUT -s 1.2.3.4 -j DROP\n\n# บันทึก Rules\nsudo iptables-save &gt; /etc/iptables/rules.v4\n\n# UFW (Uncomplicated Firewall)\nsudo ufw enable\nsudo ufw allow 80/tcp\nsudo ufw allow 443/tcp\nsudo ufw allow from 10.0.0.0/24 to any port 22\nsudo ufw status verbose</pre>\n<h2>curl และ wget สำหรับ HTTP Testing</h2>\n<pre># Basic HTTP requests\ncurl https://api.example.com/health\ncurl -X POST https://api.example.com/data \\\n     -H "Content-Type: application/json" \\\n     -H "Authorization: Bearer TOKEN" \\\n     -d \'{"key": "value"}\'\n\n# Download File\ncurl -L -o file.tar.gz https://example.com/file.tar.gz\nwget -O file.tar.gz https://example.com/file.tar.gz\n\n# Verbose output สำหรับ Debug\ncurl -v https://example.com\ncurl -I https://example.com   # Header only</pre>\n<h2>SSH Tunneling และ Port Forwarding</h2>\n<pre># Local Port Forwarding: เข้า Remote DB ผ่าน SSH\nssh -L 5432:db.internal:5432 user@jump-server\n# จากนั้น connect localhost:5432 เพื่อเข้าถึง db.internal:5432\n\n# Remote Port Forwarding: Expose Local service\nssh -R 8080:localhost:3000 user@public-server\n\n# SOCKS Proxy\nssh -D 1080 user@server\n\n# SSH Config file\n# ~/.ssh/config\n# Host prod\n#     HostName prod.example.com\n#     User ubuntu\n#     IdentityFile ~/.ssh/prod-key.pem\n#     ServerAliveInterval 60\nssh prod  # ใช้แทน ssh ยาวๆ</pre>\n<h2>SSL/TLS Certificates</h2>\n<pre># ตรวจสอบ SSL Certificate\nopenssl s_client -connect example.com:443 &lt; /dev/null\n\n# สร้าง Self-signed Certificate สำหรับ Testing\nopenssl req -x509 -nodes -days 365 -newkey rsa:2048 \\\n    -keyout server.key -out server.crt \\\n    -subj "/CN=localhost/O=Dev/C=TH"\n\n# ตรวจสอบ Certificate Expiry\necho | openssl s_client -connect yourdomain.com:443 2&gt;/dev/null \\\n      | openssl x509 -noout -enddate</pre>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ <code>mtr</code> (combination ของ ping + traceroute) สำหรับ Network troubleshooting ที่ดีกว่า <code>traceroute</code> ทั่วไป ติดตั้งด้วย <code>apt install mtr</code> และรันด้วย <code>mtr google.com</code> เพื่อดู Real-time packet loss ในแต่ละ Hop</div>',
  },
  {
    number: 4,
    slug: 'server',
    emoji: '🖥️',
    title: 'Server Management: ดูแล Production Server อย่างมืออาชีพ',
    content: '<h2>Nginx: Web Server และ Reverse Proxy</h2>\n<p>Nginx (อ่านว่า "Engine-X") เป็น Web Server ที่ทรงพลังและใช้กันอย่างแพร่หลายในโลก Production ด้วยสถาปัตยกรรม Event-driven ที่จัดการ Concurrent Connections ได้หลายหมื่นต่อวินาที บน Resource น้อยกว่า Apache มาก</p>\n<h2>ติดตั้งและตั้งค่า Nginx</h2>\n<pre># ติดตั้ง Nginx\nsudo apt update &amp;&amp; sudo apt install nginx -y\n\n# Start, Enable, Status\nsudo systemctl start nginx\nsudo systemctl enable nginx\nsudo systemctl status nginx\n\n# ตรวจสอบ Config Syntax\nsudo nginx -t\n\n# Reload Config โดยไม่ Restart (Zero Downtime)\nsudo systemctl reload nginx</pre>\n<h2>Nginx Virtual Host Configuration</h2>\n<pre># /etc/nginx/sites-available/myapp.conf\nserver {\n    listen 80;\n    server_name myapp.example.com;\n    return 301 https://$server_name$request_uri;\n}\n\nserver {\n    listen 443 ssl http2;\n    server_name myapp.example.com;\n\n    ssl_certificate /etc/letsencrypt/live/myapp.example.com/fullchain.pem;\n    ssl_certificate_key /etc/letsencrypt/live/myapp.example.com/privkey.pem;\n    ssl_protocols TLSv1.2 TLSv1.3;\n\n    # Security Headers\n    add_header X-Frame-Options DENY;\n    add_header X-Content-Type-Options nosniff;\n    add_header Strict-Transport-Security "max-age=31536000" always;\n\n    location / {\n        proxy_pass http://localhost:3000;\n        proxy_http_version 1.1;\n        proxy_set_header Upgrade $http_upgrade;\n        proxy_set_header Connection "upgrade";\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n    }\n}\n\n# Enable site\nsudo ln -s /etc/nginx/sites-available/myapp.conf /etc/nginx/sites-enabled/\nsudo nginx -t &amp;&amp; sudo systemctl reload nginx</pre>\n<h2>SSL Certificate ด้วย Certbot (Let\'s Encrypt)</h2>\n<pre># ติดตั้ง Certbot\nsudo apt install certbot python3-certbot-nginx -y\n\n# ขอ Certificate\nsudo certbot --nginx -d myapp.example.com -d www.myapp.example.com\n\n# Test Auto-renewal\nsudo certbot renew --dry-run\n\n# ดู Certificates ที่มีอยู่\nsudo certbot certificates</pre>\n<h2>systemd Service Management</h2>\n<pre># สร้าง Custom systemd Service\n# /etc/systemd/system/myapp.service\n\n[Unit]\nDescription=My Node.js Application\nAfter=network.target\n\n[Service]\nType=simple\nUser=www-data\nWorkingDirectory=/opt/myapp\nExecStart=/usr/bin/node /opt/myapp/server.js\nRestart=always\nRestartSec=10\nEnvironment=NODE_ENV=production\nStandardOutput=journal\nStandardError=journal\n\n[Install]\nWantedBy=multi-user.target\n\n# โหลด และ Enable Service\nsudo systemctl daemon-reload\nsudo systemctl enable myapp\nsudo systemctl start myapp\nsudo journalctl -u myapp -f  # ดู Logs แบบ Real-time</pre>\n<h2>Log Management</h2>\n<pre># ดู System Logs\njournalctl -n 100             # ล่าสุด 100 บรรทัด\njournalctl -u nginx           # Logs ของ nginx\njournalctl --since "1 hour ago"\njournalctl -p err             # Error level เท่านั้น\n\n# Log Rotation config ตัวอย่าง\n# /etc/logrotate.d/nginx\n# /var/log/nginx/*.log {\n#     daily\n#     rotate 14\n#     compress\n#     delaycompress\n#     notifempty\n# }\n\nsudo logrotate -d /etc/logrotate.d/nginx  # Test</pre>\n<h2>Performance Tuning</h2>\n<pre># ดู System Resources\nfree -h                  # Memory usage\ndf -h                    # Disk usage\niostat -x 1              # Disk I/O\nvmstat 1                 # Virtual Memory stats\n\n# Optimize Nginx\n# /etc/nginx/nginx.conf\nworker_processes auto;\nworker_connections 1024;\nkeepalive_timeout 65;\ngzip on;\ngzip_types text/plain application/json application/javascript text/css;\nclient_max_body_size 10M;\n\n# Kernel Parameter Tuning\nsudo sysctl -w net.core.somaxconn=65535\nsudo sysctl -w vm.swappiness=10\necho "net.core.somaxconn=65535" | sudo tee -a /etc/sysctl.conf\nsudo sysctl -p</pre>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ติดตั้ง <code>fail2ban</code> เพื่อ Auto-block IP ที่ Brute force SSH: <code>sudo apt install fail2ban</code> จากนั้น Copy <code>/etc/fail2ban/jail.conf</code> ไปเป็น <code>/etc/fail2ban/jail.local</code> และเปิดใช้ sshd jail เป็นชั้นป้องกันแรกสำหรับทุก Production Server</div>',
  },
  {
    number: 5,
    slug: 'docker',
    emoji: '📦',
    title: 'Docker Fundamentals: Containerization ตั้งแต่ Zero ถึง Pro',
    content: '<h2>Containers vs Virtual Machines</h2>\n<p>ก่อนที่จะเรียน Docker ต้องเข้าใจว่า <strong>Container</strong> แตกต่างจาก <strong>Virtual Machine (VM)</strong> อย่างไร VM รัน Guest OS เต็มรูปแบบพร้อม Kernel แยกต่างหาก ใช้ทรัพยากรมาก แต่ Container ใช้ Kernel ของ Host OS ร่วมกัน Isolate เฉพาะ Process, Network, และ Filesystem ทำให้เบากว่า เร็วกว่า และ Portable กว่ามาก</p>\n<ul>\n<li>🐋 <strong>Docker Engine</strong> — Container Runtime ที่จัดการ Build, Run, Push Images</li>\n<li>📦 <strong>Image</strong> — Blueprint ของ Container เปรียบเหมือน Class ใน OOP</li>\n<li>🏃 <strong>Container</strong> — Instance ที่รันอยู่จริง เปรียบเหมือน Object</li>\n<li>🗄️ <strong>Registry</strong> — ที่เก็บ Images เช่น Docker Hub, ECR, GCR</li>\n</ul>\n<h2>Docker Commands พื้นฐาน</h2>\n<pre># Pull Image จาก Registry\ndocker pull nginx:alpine\ndocker pull node:20-slim\n\n# รัน Container\ndocker run nginx                          # Foreground\ndocker run -d nginx                       # Detached (Background)\ndocker run -d -p 8080:80 --name web nginx # Map Port + Name\ndocker run -it ubuntu:22.04 bash          # Interactive Shell\n\n# จัดการ Containers\ndocker ps                   # ดู Running containers\ndocker ps -a                # ดูทั้งหมดรวม Stopped\ndocker stop web\ndocker start web\ndocker rm web\ndocker rm -f web            # Force remove\ndocker logs web\ndocker logs -f web          # Follow Logs\ndocker exec -it web bash    # เข้าไปใน Running container</pre>\n<h2>เขียน Dockerfile</h2>\n<pre># Dockerfile สำหรับ Node.js Application\nFROM node:20-slim\n\nWORKDIR /app\n\n# Copy package files ก่อน (Leverage Docker Cache)\nCOPY package*.json ./\nRUN npm ci --only=production\n\nCOPY . .\n\n# สร้าง non-root user\nRUN addgroup --system appgroup &amp;&amp; \\\n    adduser --system --ingroup appgroup appuser\nUSER appuser\n\nEXPOSE 3000\n\nHEALTHCHECK --interval=30s --timeout=3s \\\n    CMD node -e "require(\'http\').get(\'http://localhost:3000/health\', r =&gt; process.exit(r.statusCode === 200 ? 0 : 1))"\n\nCMD ["node", "server.js"]\n\n# Build Image\ndocker build -t myapp:1.0.0 .\ndocker build -t myapp:latest -f Dockerfile.prod .</pre>\n<h2>Multi-stage Builds</h2>\n<pre># Stage 1: Build\nFROM node:20 AS builder\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\nRUN npm run build\n\n# Stage 2: Production\nFROM node:20-slim AS production\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci --only=production\nCOPY --from=builder /app/dist ./dist\nEXPOSE 3000\nCMD ["node", "dist/server.js"]\n\n# ผลลัพธ์: Image เล็กลงจาก ~1GB เหลือแค่ ~150MB</pre>\n<h2>Docker Compose</h2>\n<pre># docker-compose.yml\nversion: "3.9"\n\nservices:\n  app:\n    build: .\n    ports:\n      - "3000:3000"\n    environment:\n      - NODE_ENV=production\n      - DATABASE_URL=postgresql://postgres:secret@db:5432/mydb\n    depends_on:\n      db:\n        condition: service_healthy\n    volumes:\n      - ./uploads:/app/uploads\n    restart: unless-stopped\n    networks:\n      - app-network\n\n  db:\n    image: postgres:16-alpine\n    environment:\n      POSTGRES_DB: mydb\n      POSTGRES_USER: postgres\n      POSTGRES_PASSWORD: secret\n    volumes:\n      - postgres-data:/var/lib/postgresql/data\n    healthcheck:\n      test: ["CMD-SHELL", "pg_isready -U postgres"]\n      interval: 10s\n      timeout: 5s\n      retries: 5\n    networks:\n      - app-network\n\nvolumes:\n  postgres-data:\n\nnetworks:\n  app-network:\n    driver: bridge\n\n# Commands\ndocker compose up -d\ndocker compose down\ndocker compose logs -f app\ndocker compose ps</pre>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ <code>.dockerignore</code> เพื่อลดขนาด Build Context ป้องกัน node_modules, .git, .env จากการ Copy เข้าไปใน Image ตัวอย่าง: เพิ่ม <code>node_modules</code>, <code>.git</code>, <code>*.log</code>, <code>.env</code> ใน .dockerignore จะทำให้ Build เร็วขึ้นและ Image ปลอดภัยขึ้น</div>',
  },
  {
    number: 6,
    slug: 'k8s-arch',
    emoji: '⎈',
    title: 'Kubernetes Architecture: เข้าใจ K8s จากภายใน',
    content: '<h2>Kubernetes คืออะไรและทำไมต้องใช้?</h2>\n<p>Kubernetes (K8s) คือ Open-source Container Orchestration Platform ที่พัฒนาโดย Google และนำมา Donate ให้ CNCF ในปี 2014 มันแก้ปัญหาที่เกิดเมื่อมี Container หลายร้อยหรือหลายพันตัว: ทำอย่างไรให้ Deploy, Scale, Self-heal, และ Load Balance ได้โดยอัตโนมัติ?</p>\n<h2>Control Plane Components</h2>\n<p>Control Plane คือ "สมอง" ของ Kubernetes Cluster ทำงานบน Master Node:</p>\n<ul>\n<li>🧠 <strong>kube-apiserver</strong> — Frontend ของ Control Plane รับ API Requests ทั้งหมด ทั้งจาก kubectl, Dashboard, และ Internal Components</li>\n<li>💾 <strong>etcd</strong> — Distributed Key-Value Store เก็บ State ทั้งหมดของ Cluster ต้อง Backup เสมอ!</li>\n<li>📅 <strong>kube-scheduler</strong> — ตัดสินใจว่า Pod ควรรันบน Node ไหน โดยพิจารณา Resources, Affinity, Taints</li>\n<li>⚙️ <strong>kube-controller-manager</strong> — รัน Controller Loops ต่างๆ เช่น ReplicaSet Controller, Node Controller</li>\n<li>☁️ <strong>cloud-controller-manager</strong> — จัดการ Integration กับ Cloud Provider (AWS, GCP, Azure)</li>\n</ul>\n<h2>Worker Node Components</h2>\n<ul>\n<li>🤖 <strong>kubelet</strong> — Agent ที่รันบนทุก Node รับ PodSpec จาก API Server และดูแลให้ Container รันตาม Spec</li>\n<li>🔀 <strong>kube-proxy</strong> — จัดการ Network Rules บน Node ใช้ iptables/ipvs เพื่อ Route Traffic ไปยัง Pod ที่ถูกต้อง</li>\n<li>🐳 <strong>Container Runtime</strong> — รัน Container จริงๆ เช่น containerd, CRI-O</li>\n</ul>\n<h2>Core Objects: Pods</h2>\n<pre># pod.yaml — Unit เล็กที่สุดใน Kubernetes\napiVersion: v1\nkind: Pod\nmetadata:\n  name: myapp-pod\n  labels:\n    app: myapp\n    env: production\nspec:\n  containers:\n  - name: myapp\n    image: myapp:1.0.0\n    ports:\n    - containerPort: 3000\n    resources:\n      requests:\n        memory: "128Mi"\n        cpu: "100m"\n      limits:\n        memory: "256Mi"\n        cpu: "500m"\n    livenessProbe:\n      httpGet:\n        path: /health\n        port: 3000\n      initialDelaySeconds: 30\n      periodSeconds: 10\n    readinessProbe:\n      httpGet:\n        path: /ready\n        port: 3000\n      initialDelaySeconds: 5\n      periodSeconds: 5\n\nkubectl apply -f pod.yaml\nkubectl get pods\nkubectl describe pod myapp-pod\nkubectl logs myapp-pod</pre>\n<h2>Deployments: การ Deploy ที่ยืดหยุ่น</h2>\n<pre># deployment.yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: myapp-deployment\n  namespace: production\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: myapp\n  strategy:\n    type: RollingUpdate\n    rollingUpdate:\n      maxSurge: 1\n      maxUnavailable: 0   # Zero Downtime Deployment\n  template:\n    metadata:\n      labels:\n        app: myapp\n    spec:\n      containers:\n      - name: myapp\n        image: myapp:1.0.0\n        ports:\n        - containerPort: 3000\n\n# Commands\nkubectl apply -f deployment.yaml\nkubectl rollout status deployment/myapp-deployment\nkubectl rollout history deployment/myapp-deployment\nkubectl rollout undo deployment/myapp-deployment\nkubectl set image deployment/myapp-deployment myapp=myapp:1.1.0</pre>\n<h2>Services: Network Access to Pods</h2>\n<pre># service.yaml\napiVersion: v1\nkind: Service\nmetadata:\n  name: myapp-service\nspec:\n  selector:\n    app: myapp\n  type: ClusterIP\n  ports:\n  - protocol: TCP\n    port: 80               # Service port\n    targetPort: 3000       # Pod port\n\n# Service Types:\n# ClusterIP   — เข้าได้เฉพาะภายใน Cluster (Default)\n# NodePort    — Expose port บน Node IP (30000-32767)\n# LoadBalancer — สร้าง Cloud Load Balancer อัตโนมัติ\n# ExternalName — Map ไปยัง External DNS name</pre>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> เรียนรู้ Kubernetes ด้วย <code>minikube</code> หรือ <code>kind</code> (Kubernetes in Docker) บนเครื่อง Local ก่อน Killercoda และ Play with Kubernetes มี Free Lab Environment แบบ Browser-based ที่ไม่ต้องติดตั้งอะไรเลย เหมาะมากสำหรับการฝึก</div>',
  },
  {
    number: 7,
    slug: 'k8s-workloads',
    emoji: '⚙️',
    title: 'Kubernetes Workloads: จัดการ Config, Storage และ Traffic',
    content: '<h2>ConfigMaps: จัดการ Configuration</h2>\n<p>ConfigMap ใช้แยก Configuration ออกจาก Container Image ทำให้ Deploy ใน Environment ต่างๆ (Dev, Staging, Prod) โดยไม่ต้อง Build Image ใหม่</p>\n<pre># สร้าง ConfigMap\nkubectl create configmap app-config \\\n    --from-literal=APP_ENV=production \\\n    --from-literal=LOG_LEVEL=info\n\n# ConfigMap YAML\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: app-config\ndata:\n  APP_ENV: "production"\n  LOG_LEVEL: "info"\n\n# ใช้ ConfigMap ใน Pod (Environment Variables)\nspec:\n  containers:\n  - name: app\n    envFrom:\n    - configMapRef:\n        name: app-config\n\n# ใช้เป็น Volume Mount\n    volumeMounts:\n    - name: config-volume\n      mountPath: /app/config\n  volumes:\n  - name: config-volume\n    configMap:\n      name: app-config</pre>\n<h2>Secrets: จัดการ Sensitive Data</h2>\n<pre># สร้าง Secret\nkubectl create secret generic db-secret \\\n    --from-literal=DB_PASSWORD=supersecret \\\n    --from-literal=DB_USER=admin\n\n# Secret YAML (value ต้อง Base64 encode)\napiVersion: v1\nkind: Secret\nmetadata:\n  name: db-secret\ntype: Opaque\ndata:\n  DB_PASSWORD: c3VwZXJzZWNyZXQ=\n  DB_USER: YWRtaW4=\n\n# ใช้ Secret ใน Pod\nspec:\n  containers:\n  - name: app\n    env:\n    - name: DB_PASSWORD\n      valueFrom:\n        secretKeyRef:\n          name: db-secret\n          key: DB_PASSWORD</pre>\n<h2>Persistent Volumes และ Storage</h2>\n<pre># PersistentVolume (PV)\napiVersion: v1\nkind: PersistentVolume\nmetadata:\n  name: postgres-pv\nspec:\n  capacity:\n    storage: 10Gi\n  accessModes:\n    - ReadWriteOnce\n  persistentVolumeReclaimPolicy: Retain\n  storageClassName: fast-ssd\n  hostPath:\n    path: /data/postgres\n\n# PersistentVolumeClaim (PVC)\napiVersion: v1\nkind: PersistentVolumeClaim\nmetadata:\n  name: postgres-pvc\nspec:\n  accessModes:\n    - ReadWriteOnce\n  storageClassName: fast-ssd\n  resources:\n    requests:\n      storage: 10Gi\n\n# ใช้ PVC ใน Pod\nspec:\n  containers:\n  - name: postgres\n    image: postgres:16\n    volumeMounts:\n    - mountPath: /var/lib/postgresql/data\n      name: postgres-storage\n  volumes:\n  - name: postgres-storage\n    persistentVolumeClaim:\n      claimName: postgres-pvc</pre>\n<h2>StatefulSets: สำหรับ Stateful Applications</h2>\n<pre># StatefulSet สำหรับ Database Cluster\napiVersion: apps/v1\nkind: StatefulSet\nmetadata:\n  name: postgres\nspec:\n  serviceName: "postgres"\n  replicas: 3\n  selector:\n    matchLabels:\n      app: postgres\n  template:\n    metadata:\n      labels:\n        app: postgres\n    spec:\n      containers:\n      - name: postgres\n        image: postgres:16\n        ports:\n        - containerPort: 5432\n        volumeMounts:\n        - name: data\n          mountPath: /var/lib/postgresql/data\n  volumeClaimTemplates:\n  - metadata:\n      name: data\n    spec:\n      accessModes: ["ReadWriteOnce"]\n      resources:\n        requests:\n          storage: 10Gi\n# StatefulSet ให้ Stable Network Identity: postgres-0, postgres-1, postgres-2</pre>\n<h2>DaemonSets: รัน Pod บนทุก Node</h2>\n<pre># DaemonSet สำหรับ Log Collector (Fluentd)\napiVersion: apps/v1\nkind: DaemonSet\nmetadata:\n  name: fluentd\nspec:\n  selector:\n    matchLabels:\n      name: fluentd\n  template:\n    metadata:\n      labels:\n        name: fluentd\n    spec:\n      tolerations:\n      - key: node-role.kubernetes.io/control-plane\n        effect: NoSchedule\n      containers:\n      - name: fluentd\n        image: fluent/fluentd-kubernetes-daemonset:v1\n        resources:\n          limits:\n            memory: 200Mi\n        volumeMounts:\n        - name: varlog\n          mountPath: /var/log\n      volumes:\n      - name: varlog\n        hostPath:\n          path: /var/log</pre>\n<h2>Ingress: HTTP Routing</h2>\n<pre># ติดตั้ง Nginx Ingress Controller\nkubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/main/deploy/static/provider/cloud/deploy.yaml\n\n# Ingress Resource\napiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata:\n  name: app-ingress\n  annotations:\n    nginx.ingress.kubernetes.io/rewrite-target: /\n    cert-manager.io/cluster-issuer: "letsencrypt-prod"\nspec:\n  ingressClassName: nginx\n  tls:\n  - hosts:\n    - myapp.example.com\n    secretName: myapp-tls\n  rules:\n  - host: myapp.example.com\n    http:\n      paths:\n      - path: /api\n        pathType: Prefix\n        backend:\n          service:\n            name: api-service\n            port:\n              number: 80\n      - path: /\n        pathType: Prefix\n        backend:\n          service:\n            name: frontend-service\n            port:\n              number: 80</pre>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ <strong>cert-manager</strong> ใน Kubernetes เพื่อจัดการ SSL Certificates อัตโนมัติ ติดตั้งด้วย Helm แล้วกำหนด ClusterIssuer สำหรับ Let\'s Encrypt จากนั้น Annotation เดียวบน Ingress ก็จัดการ Certificate ได้ครบวงจรโดยไม่ต้องทำ Manual อีกเลย</div>',
  },
  {
    number: 8,
    slug: 'k8s-ops',
    emoji: '🔧',
    title: 'Kubernetes Operations: ควบคุม Cluster อย่างเชี่ยวชาญ',
    content: '<h2>kubectl: Swiss Army Knife ของ K8s</h2>\n<p>kubectl คือ CLI หลักสำหรับการ Interact กับ Kubernetes Cluster การเรียนรู้ kubectl Commands อย่างลึกซึ้งทำให้คุณ Debug และจัดการ Cluster ได้เร็วกว่าการใช้ Dashboard มาก</p>\n<pre># Context Management\nkubectl config get-contexts\nkubectl config use-context production\nkubectl config set-context --current --namespace=myapp\n\n# Get Resources\nkubectl get all -n production\nkubectl get pods -A                    # ทุก Namespace\nkubectl get pods -w                    # Watch แบบ Real-time\nkubectl get pods -o wide\nkubectl get pods -o yaml\n\n# Debugging\nkubectl describe pod myapp-pod-xyz\nkubectl logs myapp-pod-xyz\nkubectl logs myapp-pod-xyz -c sidecar\nkubectl logs --previous myapp-pod-xyz\nkubectl exec -it myapp-pod-xyz -- bash\n\n# Port Forward สำหรับ Local Debug\nkubectl port-forward pod/myapp-pod-xyz 8080:3000\nkubectl port-forward svc/myapp-service 8080:80</pre>\n<h2>Namespaces: การแบ่ง Resource</h2>\n<pre># สร้างและจัดการ Namespace\nkubectl create namespace production\nkubectl create namespace staging\nkubectl create namespace monitoring\n\n# ดู Resources ใน Namespace\nkubectl get pods -n production\nkubectl get all -n staging\n\n# Resource Quotas\napiVersion: v1\nkind: ResourceQuota\nmetadata:\n  name: production-quota\n  namespace: production\nspec:\n  hard:\n    requests.cpu: "10"\n    requests.memory: 20Gi\n    limits.cpu: "20"\n    limits.memory: 40Gi\n    pods: "50"</pre>\n<h2>RBAC: Role-Based Access Control</h2>\n<pre># ServiceAccount\napiVersion: v1\nkind: ServiceAccount\nmetadata:\n  name: deploy-sa\n  namespace: production\n\n# Role\napiVersion: rbac.authorization.k8s.io/v1\nkind: Role\nmetadata:\n  name: deploy-role\n  namespace: production\nrules:\n- apiGroups: ["apps"]\n  resources: ["deployments"]\n  verbs: ["get", "list", "update", "patch"]\n- apiGroups: [""]\n  resources: ["pods", "services"]\n  verbs: ["get", "list"]\n\n# RoleBinding\napiVersion: rbac.authorization.k8s.io/v1\nkind: RoleBinding\nmetadata:\n  name: deploy-rb\n  namespace: production\nsubjects:\n- kind: ServiceAccount\n  name: deploy-sa\n  namespace: production\nroleRef:\n  kind: Role\n  name: deploy-role\n  apiGroup: rbac.authorization.k8s.io\n\n# ทดสอบสิทธิ์\nkubectl auth can-i update deployments \\\n  --as=system:serviceaccount:production:deploy-sa -n production</pre>\n<h2>Helm: Kubernetes Package Manager</h2>\n<pre># ติดตั้ง Helm\ncurl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash\n\n# เพิ่ม Repositories\nhelm repo add bitnami https://charts.bitnami.com/bitnami\nhelm repo update\n\n# ค้นหาและติดตั้ง Chart\nhelm search repo nginx\nhelm install my-nginx bitnami/nginx\nhelm install postgres bitnami/postgresql \\\n    --set auth.postgresPassword=secret\n\n# Helm Chart ของตัวเอง\nhelm create myapp\nhelm lint myapp/\nhelm upgrade --install myapp ./myapp \\\n    --namespace production \\\n    --values prod-values.yaml\n\n# ดูและจัดการ Releases\nhelm list -A\nhelm history myapp -n production\nhelm rollback myapp 2 -n production\nhelm uninstall myapp -n production</pre>\n<h2>Monitoring ด้วย Prometheus และ Grafana</h2>\n<pre># ติดตั้ง Prometheus Stack ด้วย Helm\nhelm repo add prometheus-community \\\n    https://prometheus-community.github.io/helm-charts\nhelm install prometheus prometheus-community/kube-prometheus-stack \\\n    --namespace monitoring \\\n    --create-namespace \\\n    --set grafana.adminPassword=admin123\n\n# Port forward Grafana\nkubectl port-forward svc/prometheus-grafana 3000:80 -n monitoring\n\n# ServiceMonitor สำหรับ Custom Metrics\napiVersion: monitoring.coreos.com/v1\nkind: ServiceMonitor\nmetadata:\n  name: myapp-monitor\n  labels:\n    release: prometheus\nspec:\n  selector:\n    matchLabels:\n      app: myapp\n  endpoints:\n  - port: metrics\n    path: /metrics\n    interval: 30s\n\n# PromQL Queries\n# CPU Usage:\nrate(container_cpu_usage_seconds_total{namespace="production"}[5m])\n# Memory:\ncontainer_memory_working_set_bytes{namespace="production"}</pre>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ติดตั้ง <code>k9s</code> ซึ่งเป็น Terminal UI สำหรับ Kubernetes ที่ทำให้การ Navigate และ Debug Cluster ง่ายขึ้นมาก คุณสามารถดู Pods, Logs, Exec เข้า Container, และจัดการ Resources ทั้งหมดด้วยคีย์บอร์ดโดยไม่ต้องพิมพ์ kubectl ยาวๆ ทุกครั้ง</div>',
  },
  {
    number: 9,
    slug: 'k8s-prod',
    emoji: '🏆',
    title: 'Production Kubernetes: ทำให้ Cluster พร้อมรับ Traffic จริง',
    content: '<h2>Horizontal Pod Autoscaler (HPA)</h2>\n<p>HPA คือ Feature ที่ Scale จำนวน Pod อัตโนมัติตาม Metrics เช่น CPU Utilization, Memory, หรือ Custom Metrics จาก Prometheus ทำให้แอปรองรับ Traffic Spike ได้โดยไม่ต้อง Manual Scale</p>\n<pre># Enable Metrics Server\nkubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml\n\n# สร้าง HPA\napiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nmetadata:\n  name: myapp-hpa\n  namespace: production\nspec:\n  scaleTargetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: myapp-deployment\n  minReplicas: 3\n  maxReplicas: 20\n  metrics:\n  - type: Resource\n    resource:\n      name: cpu\n      target:\n        type: Utilization\n        averageUtilization: 70\n  - type: Resource\n    resource:\n      name: memory\n      target:\n        type: Utilization\n        averageUtilization: 80\n  behavior:\n    scaleDown:\n      stabilizationWindowSeconds: 300\n\nkubectl get hpa -n production\nkubectl describe hpa myapp-hpa -n production</pre>\n<h2>Resource Requests และ Limits</h2>\n<pre># ตั้งค่า Resources อย่างเหมาะสม\nspec:\n  containers:\n  - name: myapp\n    resources:\n      requests:\n        memory: "256Mi"\n        cpu: "250m"       # 250m = 0.25 CPU Core\n      limits:\n        memory: "512Mi"\n        cpu: "1000m"      # 1000m = 1 CPU Core\n\n# LimitRange — ตั้ง Default Resource สำหรับทั้ง Namespace\napiVersion: v1\nkind: LimitRange\nmetadata:\n  name: default-limits\n  namespace: production\nspec:\n  limits:\n  - type: Container\n    default:\n      memory: "256Mi"\n      cpu: "500m"\n    defaultRequest:\n      memory: "128Mi"\n      cpu: "100m"\n    max:\n      memory: "2Gi"\n      cpu: "2000m"</pre>\n<h2>Node Affinity และ Taints/Tolerations</h2>\n<pre># Node Affinity — กำหนดว่า Pod ควรรันบน Node ไหน\nspec:\n  affinity:\n    nodeAffinity:\n      requiredDuringSchedulingIgnoredDuringExecution:\n        nodeSelectorTerms:\n        - matchExpressions:\n          - key: node-type\n            operator: In\n            values:\n            - high-memory\n    podAntiAffinity:    # กระจาย Pods ไปหลาย Nodes\n      preferredDuringSchedulingIgnoredDuringExecution:\n      - weight: 100\n        podAffinityTerm:\n          labelSelector:\n            matchLabels:\n              app: myapp\n          topologyKey: kubernetes.io/hostname\n\n# Taint Node\nkubectl taint nodes node1 dedicated=gpu:NoSchedule\n\n# Toleration ใน Pod\nspec:\n  tolerations:\n  - key: "dedicated"\n    operator: "Equal"\n    value: "gpu"\n    effect: "NoSchedule"</pre>\n<h2>PodDisruptionBudget (PDB)</h2>\n<pre># PDB ป้องกันไม่ให้มี Pod ถูก Evict พร้อมกันเกินกว่าที่กำหนด\napiVersion: policy/v1\nkind: PodDisruptionBudget\nmetadata:\n  name: myapp-pdb\n  namespace: production\nspec:\n  minAvailable: 2\n  selector:\n    matchLabels:\n      app: myapp\n\nkubectl get pdb -n production\nkubectl describe pdb myapp-pdb -n production</pre>\n<h2>Cluster Upgrade Strategies</h2>\n<pre># Blue-Green Deployment\n# 1. Deploy Version ใหม่เป็น Deployment ใหม่ (green)\n# 2. Test ว่า Green ทำงานถูกต้อง\n# 3. สลับ Service Selector ไปที่ Green\nkubectl patch service myapp-service \\\n  -p \'{"spec":{"selector":{"version":"v2"}}}\'\n\n# Node Upgrade — Drain Node ก่อน Upgrade\nkubectl cordon node1\nkubectl drain node1 --ignore-daemonsets --delete-emptydir-data\n# Upgrade kubeadm, kubelet, kubectl บน Node\nkubectl uncordon node1</pre>\n<h2>Disaster Recovery</h2>\n<pre># Backup etcd\nETCDCTL_API=3 etcdctl snapshot save /backup/etcd-$(date +%Y%m%d).db \\\n  --endpoints=https://127.0.0.1:2379 \\\n  --cacert=/etc/kubernetes/pki/etcd/ca.crt \\\n  --cert=/etc/kubernetes/pki/etcd/server.crt \\\n  --key=/etc/kubernetes/pki/etcd/server.key\n\n# ตรวจสอบ Snapshot\nETCDCTL_API=3 etcdctl snapshot status /backup/etcd-20260713.db\n\n# Velero — Backup K8s Resources + PersistentVolumes\nvelero install --provider aws --bucket my-backup-bucket \\\n    --backup-location-config region=ap-southeast-1\n\n# สร้าง Backup\nvelero backup create production-backup --include-namespaces production\n\n# ดู Backup\nvelero backup get\n\n# Restore\nvelero restore create --from-backup production-backup\n\n# ตั้ง Schedule Backup\nvelero schedule create daily-backup --schedule="0 2 * * *" \\\n    --include-namespaces production \\\n    --ttl 720h</pre>\n<h2>Production Checklist</h2>\n<ul>\n<li>✅ <strong>Resource Requests/Limits</strong> ทุก Container ต้องมีกำหนด</li>\n<li>✅ <strong>Liveness และ Readiness Probes</strong> ทุก Pod</li>\n<li>✅ <strong>HPA</strong> สำหรับ Stateless Services</li>\n<li>✅ <strong>PodDisruptionBudget</strong> สำหรับ Critical Services</li>\n<li>✅ <strong>RBAC</strong> ใช้ Least Privilege Principle</li>\n<li>✅ <strong>Network Policies</strong> จำกัด Pod-to-Pod Communication</li>\n<li>✅ <strong>Secrets Management</strong> ใช้ External Secrets หรือ Vault</li>\n<li>✅ <strong>Monitoring</strong> Prometheus + Grafana + Alerts</li>\n<li>✅ <strong>Logging</strong> Centralized Logs (ELK Stack หรือ Loki)</li>\n<li>✅ <strong>Backup</strong> etcd และ PersistentVolumes สม่ำเสมอ</li>\n</ul>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ <strong>Kubecost</strong> เพื่อ Monitor ค่าใช้จ่ายของแต่ละ Namespace และ Workload ใน Cloud K8s Cluster การ Right-size Resource Requests ตาม Actual Usage สามารถลดค่า Cloud ได้ถึง 30-50% โดยเฉพาะ Development และ Staging Environments ที่มักมี Resource Over-provisioning สูง</div>',
  },
];