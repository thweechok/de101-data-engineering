export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '🖥️',
    title: 'บทนำ: ทำไม GUI ถึงไม่พอ? พลังของ CLI และ Version Control',
    content: '<h2>ยินดีต้อนรับสู่โลกของ Terminal และ Git</h2><p>ในยุคที่ทุกอย่างมี User Interface (GUI) ที่สวยงามและใช้งานง่าย ทำไม Developer ถึงยังต้องกลับไปใช้ Command Line Interface (CLI) ที่มีแต่ตัวหนังสือสีขาวบนพื้นดำ? คำตอบคือ "พลังแห่งการควบคุมและความเร็ว"</p><h2>ข้อจำกัดของ GUI (Graphical User Interface)</h2><p>GUI ถูกออกแบบมาเพื่อให้ผู้ใช้ทั่วไปสามารถโต้ตอบกับคอมพิวเตอร์ได้ง่าย แต่ในมุมมองของการพัฒนาซอฟต์แวร์ GUI มีข้อจำกัดหลายอย่าง:</p><ul><li><strong>ความเร็วในการทำงาน:</strong> การคลิกเมาส์หาเมนูย่อยต้องใช้เวลา ในขณะที่ CLI สามารถพิมพ์คำสั่งและกด Enter ได้ทันที</li><li><strong>การทำซ้ำ (Reproducibility):</strong> คุณไม่สามารถเขียนสคริปต์เพื่อให้เมาส์คลิกตามขั้นตอนเดิมได้ง่ายๆ แต่ใน CLI คุณสามารถบันทึกคำสั่งหลายบรรทัดเป็น Shell Script เพื่อรันกี่ครั้งก็ได้ผลลัพธ์เหมือนเดิม</li><li><strong>การเข้าถึงเซิร์ฟเวอร์ (Remote Access):</strong> เมื่อคุณต้องจัดการ Server หรือ Cloud (เช่น AWS, DigitalOcean) การเชื่อมต่อผ่าน SSH จะมีแค่ CLI เท่านั้น ไม่มีหน้าจอให้คลิก</li><li><strong>การใช้ทรัพยากร (Resource Usage):</strong> GUI กินแรมและซีพียูมากกว่า CLI มาก</li></ul><h2>พลังของ CLI (Command Line Interface)</h2><p>CLI ไม่ใช่เทคโนโลยีที่ล้าสมัย แต่เป็นเครื่องมือขั้นสูงที่ให้คุณคุยกับระบบปฏิบัติการ (OS) ได้โดยตรง คุณสามารถค้นหาไฟล์ที่ซ่อนอยู่, จัดการ Permission, ตรวจสอบการทำงานของ Process หรือแม้แต่แก้ไขไฟล์ผ่าน Terminal ได้โดยไม่ต้องเปิดโปรแกรมใดๆ เพิ่มเติม เมื่อคุณเชี่ยวชาญ CLI คุณจะทำงานได้เร็วขึ้นอย่างก้าวกระโดด</p><div class="tip-box"><strong>💡 Pro Tip</strong>: อย่าพยายามจำทุกคำสั่ง ให้จำเฉพาะคำสั่งที่ใช้บ่อย และจำวิธีอ่าน Manual (คำสั่ง man) หรือใช้ --help เพื่อดูวิธีใช้เมื่อต้องการ</div><h2>ทำไมต้องมี Version Control System (VCS)?</h2><p>ในอดีตเวลาเราแก้ไขไฟล์งาน เรามักจะเซฟไฟล์ในชื่อ <em>project_final.zip</em>, <em>project_final_v2.zip</em>, <em>project_final_really.zip</em> ซึ่งทำให้เกิดปัญหา:</p><ol style="padding-left:20px"><li>ไม่รู้ว่าแต่ละเวอร์ชันต่างกันอย่างไร</li><li>ถ้าแก้ไขโค้ดแล้วพัง ไม่รู้จะย้อนกลับไปจุดที่ยังใช้งานได้ยังไง</li><li>ทำงานร่วมกับคนอื่นไม่ได้ (ถ้าสองคนแก้ไฟล์เดียวกันพร้อมกัน ใครจะเซฟทับใคร?)</li></ol><h2>Git: ราชาแห่ง Version Control</h2><p>Git คือ Distributed Version Control System (DVCS) ที่เกิดมาเพื่อแก้ปัญหาเหล่านั้น ทุกการเปลี่ยนแปลง (Commit) จะถูกบันทึกไว้ในประวัติ คุณสามารถย้อนเวลากลับไปดูโค้ดเก่า, แตกสาขา (Branch) เพื่อทดลองเขียนฟีเจอร์ใหม่โดยไม่กระทบโค้ดหลัก, และสามารถทำงานร่วมกับทีม (Collaboration) ได้อย่างไร้รอยต่อผ่าน Platform อย่าง GitHub หรือ GitLab</p><p>ในคอร์สนี้ เราจะปูพื้นฐานตั้งแต่การใช้ Terminal เบื้องต้น ไปจนถึงการใช้งาน Git ระดับสูงเพื่อเพิ่มศักยภาพในการเป็น Developer ของคุณ!</p>'
  },
  {
    number: 1,
    slug: 'terminal',
    emoji: '💻',
    title: 'Terminal Basics: พื้นฐานการเอาตัวรอดใน Command Line',
    content: '<h2>ทำความรู้จักกับ Terminal</h2><p>Terminal คือหน้าต่างที่เราใช้พิมพ์คำสั่ง (Command) เพื่อสั่งให้ระบบปฏิบัติการทำงาน ใน Windows เราอาจใช้ PowerShell, Command Prompt, หรือ Git Bash ส่วนใน macOS และ Linux จะมีโปรแกรม Terminal ติดตั้งมาให้อยู่แล้ว โดย Shell ยอดนิยมที่ใช้รันคำสั่งคือ Bash หรือ Zsh</p><h2>การนำทางในระบบไฟล์ (Navigation)</h2><p>การเข้าใจว่าเราอยู่ที่ไหนในระบบไฟล์ (File System) เป็นเรื่องสำคัญที่สุด 3 คำสั่งหลักที่คุณต้องใช้ให้คล่องคือ:</p><ul><li><strong>pwd (Print Working Directory):</strong> ใช้ดูว่าตอนนี้เราอยู่ในโฟลเดอร์ไหน ผลลัพธ์จะเป็น Path เต็ม เช่น <code>/Users/username/Projects</code></li><li><strong>ls (List):</strong> ใช้ดูว่าในโฟลเดอร์ปัจจุบันมีไฟล์หรือโฟลเดอร์อะไรบ้าง<ul><li><code>ls -l</code> แสดงแบบยาว (มีรายละเอียด Permission, เจ้าของ, ขนาดไฟล์)</li><li><code>ls -a</code> แสดงไฟล์ที่ซ่อนอยู่ (ไฟล์ที่ชื่อขึ้นต้นด้วยจุด เช่น .env หรือ .gitignore)</li><li><code>ls -la</code> รวมทั้งสองคำสั่งเข้าด้วยกัน</li></ul></li><li><strong>cd (Change Directory):</strong> ใช้เปลี่ยนโฟลเดอร์ เช่น <code>cd Documents</code> เพื่อเข้าไปในโฟลเดอร์ Documents, <code>cd ..</code> เพื่อถอยกลับไปหนึ่งระดับ, หรือ <code>cd ~</code> เพื่อกลับไปยัง Home Directory</li></ul><h2>การจัดการไฟล์และโฟลเดอร์ (File Operations)</h2><p>แทนที่จะใช้คลิกขวาแล้วเลือกเมนู เราสามารถจัดการไฟล์ได้รวดเร็วผ่านคำสั่ง:</p><ol style="padding-left:20px"><li><strong>mkdir (Make Directory):</strong> สร้างโฟลเดอร์ใหม่ เช่น <code>mkdir my-project</code></li><li><strong>touch:</strong> สร้างไฟล์ใหม่ที่ว่างเปล่า หรืออัปเดตเวลาล่าสุดของไฟล์ เช่น <code>touch index.html</code></li><li><strong>cp (Copy):</strong> คัดลอกไฟล์ เช่น <code>cp file.txt copy.txt</code> ถ้าต้องการคัดลอกโฟลเดอร์ต้องใช้ flag -r (recursive) เช่น <code>cp -r my-folder backup-folder</code></li><li><strong>mv (Move):</strong> ย้ายไฟล์ หรือเปลี่ยนชื่อไฟล์ เช่น <code>mv oldname.txt newname.txt</code> (เป็นการเปลี่ยนชื่อ) หรือ <code>mv file.txt /path/to/destination/</code> (เป็นการย้าย)</li><li><strong>rm (Remove):</strong> ลบไฟล์ เช่น <code>rm file.txt</code> หากต้องการลบโฟลเดอร์ต้องใช้ <code>rm -r folder-name</code> (ระวัง! คำสั่งนี้ลบแล้วลบเลย ไม่ไปอยู่ใน Recycle Bin)</li></ol><div class="tip-box"><strong>💡 ข้อควรระวัง</strong>: คำสั่ง <code>rm -rf /</code> คือคำสั่งลบทุกอย่างในเครื่อง ห้ามรันเด็ดขาดถ้าคุณไม่รู้ว่ากำลังทำอะไรอยู่!</div><h2>สิทธิ์การเข้าถึงไฟล์ (Permissions)</h2><p>ในระบบ Unix/Linux ไฟล์และโฟลเดอร์จะมีเรื่อง Permission หรือสิทธิ์ในการ Read (r), Write (w), และ Execute (x) โดยแบ่งออกเป็น 3 กลุ่มคือ Owner (เจ้าของ), Group (กลุ่ม), และ Others (คนอื่นๆ)</p><ul><li><strong>chmod (Change Mode):</strong> ใช้เปลี่ยนสิทธิ์การเข้าถึงของไฟล์ เช่น <code>chmod +x script.sh</code> เพื่อทำให้ไฟล์ script.sh สามารถ Execute หรือรันได้ หรือใช้แบบตัวเลขเช่น <code>chmod 755 index.html</code></li><li><strong>chown (Change Owner):</strong> ใช้เปลี่ยนเจ้าของไฟล์ มักใช้เมื่อเราต้องการโอนสิทธิ์ให้ User อื่น เช่น <code>chown root:root config.conf</code> (มักต้องใช้ sudo นำหน้า)</li></ul><p>ความเข้าใจใน Terminal เบื้องต้นนี้จะทำให้คุณสามารถจัดการระบบไฟล์ได้อย่างรวดเร็ว และเป็นพื้นฐานสำคัญก่อนที่เราจะเรียนรู้คำสั่งที่ซับซ้อนขึ้นในบทต่อไป</p>'
  },
  {
    number: 2,
    slug: 'advanced-cli',
    emoji: '🔧',
    title: 'Advanced Command Line: ยกระดับการใช้ Terminal ให้เหมือน Hacker',
    content: '<h2>Pipes และ Redirects (ท่อและทางเบี่ยงข้อมูล)</h2><p>พลังที่แท้จริงของ Command Line ไม่ใช่แค่คำสั่งเดี่ยวๆ แต่คือการนำคำสั่งหลายๆ อันมาต่อกัน (Chain) เพื่อสร้างกระบวนการทำงานที่ซับซ้อน</p><ul><li><strong>Redirect Output (&gt; และ &gt;&gt;):</strong> ใช้ส่งผลลัพธ์จากคำสั่งไปเก็บในไฟล์ แทนที่จะแสดงบนหน้าจอ<ul><li><code>echo "Hello World" &gt; file.txt</code> : จะเขียนทับ (Overwrite) ไฟล์เดิม หรือสร้างใหม่ถ้ายังไม่มี</li><li><code>echo "More text" &gt;&gt; file.txt</code> : จะต่อท้าย (Append) ไฟล์เดิม</li></ul></li><li><strong>Redirect Input (&lt;):</strong> ใช้ส่งข้อมูลจากไฟล์เข้าไปเป็น Input ของคำสั่ง เช่น <code>sort &lt; list.txt</code></li><li><strong>Pipes (|):</strong> เครื่องหมายไปป์ใช้ส่ง Output ของคำสั่งแรก ไปเป็น Input ของคำสั่งที่สอง เช่น <code>ls -l | grep "Aug"</code> (ลิสต์ไฟล์ทั้งหมด แล้วกรองเอาเฉพาะไฟล์ที่แก้ไขในเดือนสิงหาคม)</li></ul><h2>คำสั่งค้นหาข้อความและไฟล์</h2><ol style="padding-left:20px"><li><strong>grep (Global Regular Expression Print):</strong> ใช้ค้นหาข้อความภายในไฟล์ เช่น <code>grep "ERROR" app.log</code> เพื่อหาบรรทัดที่มีคำว่า ERROR ในไฟล์ Log หากต้องการหาทุกไฟล์ในโฟลเดอร์ให้ใช้ <code>grep -r "API_KEY" .</code></li><li><strong>find:</strong> คำสั่งทรงพลังในการค้นหาไฟล์จากชื่อ ขนาด หรือเวลา เช่น <code>find . -name "*.js"</code> หาไฟล์ JavaScript ทั้งหมด หรือ <code>find . -type d</code> เพื่อหาโฟลเดอร์ทั้งหมด</li></ol><div class="tip-box"><strong>💡 Pro Tip</strong>: คุณสามารถใช้ find ร่วมกับคำสั่งอื่นได้ เช่น <code>find . -name "*.log" -delete</code> เพื่อลบไฟล์ Log ทั้งหมดทันที</div><h2>Stream Editors: sed และ awk</h2><p>เมื่อคุณต้องการแก้ไขข้อความจำนวนมากผ่าน CLI คุณจะต้องพึ่งสองคำสั่งนี้:</p><ul><li><strong>sed (Stream Editor):</strong> เก่งเรื่องการค้นหาและแทนที่ข้อความ (Find and Replace) เช่น <code>sed "s/localhost/127.0.0.1/g" config.js</code> เพื่อเปลี่ยนคำว่า localhost เป็น 127.0.0.1 ทั้งไฟล์</li><li><strong>awk:</strong> เป็นภาษาโปรแกรมย่อยๆ ที่เก่งเรื่องการจัดการข้อมูลที่มีโครงสร้างเป็นคอลัมน์ เช่น <code>ls -l | awk \'{print $9}\'</code> เพื่อดึงเอาเฉพาะคอลัมน์ที่ 9 (ชื่อไฟล์) ออกมาแสดงผล</li></ul><h2>Environment Variables (ตัวแปรสภาพแวดล้อม)</h2><p>Environment Variables คือตัวแปรที่ระบบปฏิบัติการหรือโปรแกรมสามารถเรียกใช้ได้ นิยมใช้เก็บข้อมูลที่ไม่อยาก Hardcode ไว้ในโปรแกรม เช่น รหัสผ่าน หรือ API Key</p><ul><li><strong>ดูตัวแปรทั้งหมด:</strong> ใช้คำสั่ง <code>printenv</code> หรือ <code>env</code></li><li><strong>การสร้างตัวแปร:</strong> ใช้ <code>export API_URL="https://api.example.com"</code> (ใช้ได้เฉพาะ session นั้น)</li><li><strong>การเรียกใช้ตัวแปร:</strong> ใช้เครื่องหมาย $ นำหน้า เช่น <code>echo $API_URL</code></li></ul><p>การใช้งานเครื่องมือเหล่านี้ได้อย่างคล่องแคล่ว จะทำให้คุณสามารถเขียน Shell Script เพื่อ Automate งานที่น่าเบื่อได้อย่างมีประสิทธิภาพ</p>'
  },
  {
    number: 3,
    slug: 'git-intro',
    emoji: '📦',
    title: 'Introduction to Git: ก้าวแรกสู่ระบบควบคุมเวอร์ชันระดับโลก',
    content: '<h2>Git คืออะไร?</h2><p>Git คือ Decentralized Version Control System (ระบบควบคุมเวอร์ชันแบบกระจายศูนย์) ที่ถูกสร้างขึ้นโดย Linus Torvalds (ผู้สร้าง Linux) ระบบนี้ช่วยให้คุณสามารถติดตามทุกการเปลี่ยนแปลงของไฟล์ในโปรเจกต์ (Tracking Changes), ย้อนกลับไปยังเวอร์ชันเก่า (Revert), และทำงานร่วมกับผู้อื่นโดยที่ประวัติการทำงาน (History) ของแต่ละคนสามารถนำมารวมกันได้อย่างสมบูรณ์แบบ "Decentralized" หมายความว่า Developer ทุกคนจะมีประวัติการทำงานทั้งหมด (Full Repository) อยู่ในเครื่องตัวเอง ไม่จำเป็นต้องต่อเน็ตตลอดเวลาเพื่อใช้งาน Git</p><h2>วงจรชีวิตของไฟล์ใน Git</h2><p>การเข้าใจสถานะ (State) ของไฟล์ใน Git เป็นเรื่องที่สำคัญมาก ไฟล์ใน Git จะมี 3 สถานะหลักๆ คือ:</p><ol style="padding-left:20px"><li><strong>Modified (Working Directory):</strong> คุณได้แก้ไขไฟล์ แต่ยังไม่ได้บันทึกลงฐานข้อมูล Git</li><li><strong>Staged (Staging Area):</strong> คุณได้ทำเครื่องหมาย (Mark) ไฟล์ที่ถูกแก้ไข เพื่อเตรียมพร้อมสำหรับการ Commit ในขั้นตอนต่อไป</li><li><strong>Committed (Git Directory/Repository):</strong> ไฟล์ถูกบันทึกลงในฐานข้อมูลของ Git บนเครื่องของคุณเรียบร้อยแล้ว</li></ol><h2>คำสั่ง Git พื้นฐานที่ใช้ทุกวัน</h2><ul><li><strong>git init:</strong> ใช้เปลี่ยนโฟลเดอร์ธรรมดาให้กลายเป็น Git Repository (สร้างโฟลเดอร์ซ่อนชื่อ .git ขึ้นมา)</li><li><strong>git status:</strong> คำสั่งที่ใช้บ่อยที่สุด! เพื่อดูว่ามีไฟล์ไหนถูกแก้ไข ไฟล์ไหนอยู่ใน Staging Area และอยู่ใน Branch อะไร</li><li><strong>git add:</strong> นำไฟล์จาก Working Directory เข้าสู่ Staging Area เช่น <code>git add index.html</code> หรือ <code>git add .</code> เพื่อเพิ่มไฟล์ทั้งหมดที่มีการเปลี่ยนแปลง</li><li><strong>git commit:</strong> บันทึก Snapshot ของ Staging Area ลงใน Repository โดยต้องมีข้อความอธิบาย (Commit Message) เสมอ เช่น <code>git commit -m "Add login functionality"</code></li><li><strong>git log:</strong> ดูประวัติการ Commit ทั้งหมด เพื่อดูว่าใครเป็นคนทำ แก้ไขเมื่อไหร่ และข้อความอธิบายคืออะไร (สามารถใช้ <code>git log --oneline</code> เพื่อดูแบบย่อ)</li><li><strong>git diff:</strong> ดูความแตกต่างของโค้ดว่ามีบรรทัดไหนถูกเพิ่มหรือลบออกไปบ้าง โดยเทียบระหว่าง Working Directory กับจุดที่ Commit ไปล่าสุด</li></ul><h2>การใช้ .gitignore</h2><p>ไม่ใช่ทุกไฟล์ที่คุณควรบันทึกลงใน Git เช่น ไฟล์รหัสผ่าน, โฟลเดอร์ node_modules ที่ดาวน์โหลดมา, หรือไฟล์ชั่วคราวของ OS (เช่น .DS_Store) เราสามารถสร้างไฟล์ชื่อ <code>.gitignore</code> และใส่ชื่อไฟล์หรือแพทเทิร์น (เช่น <code>*.log</code>) ลงไป เพื่อบอกให้ Git เพิกเฉยต่อไฟล์เหล่านี้ ไม่ต้องนำไป Tracking อีกต่อไป</p><div class="tip-box"><strong>💡 Pro Tip</strong>: ควรสื่อสารผ่าน Commit Message ให้ชัดเจน ใช้คำกริยาขึ้นต้น (Imperative mood) เช่น "Fix typo in header" ดีกว่า "fixed a bug" เพราะจะช่วยให้คนในทีม (รวมถึงตัวคุณเองในอนาคต) เข้าใจบริบทได้ง่ายขึ้น</div>'
  },
  {
    number: 4,
    slug: 'branching',
    emoji: '🌿',
    title: 'Branching and Merging: ทำงานขนานกันแบบไร้รอยต่อ',
    content: '<h2>ทำไมต้อง Branch?</h2><p>ลองจินตนาการว่าคุณกำลังพัฒนาฟีเจอร์ใหม่ แต่จู่ๆ ก็มีบั๊กในระบบหลัก (Production) ที่ต้องรีบแก้ทันที หากคุณไม่มี Branch โค้ดที่ครึ่งๆ กลางๆ ของคุณจะปะปนกับโค้ดที่ต้องแก้บั๊ก ทำให้คุณไม่สามารถ Deploy โค้ดไปใช้งานได้</p><p>Branch ใน Git เปรียบเสมือนการ "คัดลอกโฟลเดอร์โปรเจกต์ (แบบเสมือนจริงและใช้พื้นที่น้อยมาก)" เพื่อไปทดลองทำงานแบบแยกส่วน (Isolated) เมื่อทำฟีเจอร์เสร็จ ค่อยนำผลลัพธ์นั้นมาผนวกรวม (Merge) กลับเข้าสู่ Branch หลัก (มักจะชื่อ <code>main</code> หรือ <code>master</code>)</p><h2>คำสั่งที่เกี่ยวกับการจัดการ Branch</h2><ul><li><strong>git branch:</strong> ใช้ดูรายการ Branch ทั้งหมดในโปรเจกต์ ถ้าตามด้วยชื่อ เช่น <code>git branch feature-login</code> จะเป็นการสร้าง Branch ใหม่</li><li><strong>git checkout หรือ git switch:</strong> ใช้เปลี่ยน Branch เพื่อย้ายไปทำงานที่ Branch อื่น เช่น <code>git switch feature-login</code> (git switch เป็นคำสั่งใหม่ที่ทำให้เข้าใจง่ายกว่า checkout)</li><li><strong>git checkout -b [branch-name] หรือ git switch -c [branch-name]:</strong> สร้าง Branch ใหม่และย้ายเข้าไปที่ Branch นั้นทันที (เป็น Shorthand ที่สะดวกมาก)</li></ul><h2>การรวม Branch (Merging)</h2><p>เมื่อฟีเจอร์ใน Branch ของคุณเสร็จสมบูรณ์ คุณจะต้องรวมมันเข้ากับ Branch หลัก โดยก่อนอื่นต้อง switch ไปที่ Branch ปลายทาง (เช่น main) แล้วใช้คำสั่ง <code>git merge [branch-ต้นทาง]</code></p><p>Git มีวิธีการ Merge สองรูปแบบหลักที่มักพบเจอ:</p><ol style="padding-left:20px"><li><strong>Fast-forward Merge:</strong> เกิดขึ้นเมื่อ Branch หลักไม่ได้มีการเปลี่ยนแปลงใหม่ใดๆ เลยตั้งแต่คุณแยก Branch ออกไป Git จะแค่เลื่อน Pointer ของ Branch หลักมาชี้ที่ Commit ล่าสุดของคุณ ไม่มีประวัติการแยกสายให้เห็นชัดเจน</li><li><strong>Recursive/3-Way Merge:</strong> เกิดขึ้นเมื่อ Branch หลักมีคนอื่น Commit ไปข้างหน้าแล้ว และคุณก็มี Commit ใหม่ใน Branch ตัวเอง Git จะสร้าง Commit ใหม่อีก 1 อันเรียกว่า "Merge Commit" เพื่อนำประวัติทั้งสองสายมารวมกัน</li></ol><h2>การแก้ปัญหา Merge Conflicts (ความขัดแย้งของโค้ด)</h2><p>โลกความจริงไม่ได้สวยงามเสมอไป ถ้าคุณและเพื่อนร่วมทีมแก้ไขไฟล์เดียวกันในบรรทัดเดียวกัน Git จะไม่รู้ว่าควรเลือกของใคร และจะแจ้งเตือนว่าเกิด <strong>Merge Conflict</strong></p><p>เมื่อเกิด Conflict คุณต้องเข้าไปในไฟล์นั้น (มักจะใช้ Text Editor หรือ VS Code จะแสดงไฮไลต์ให้เห็นง่ายๆ) โค้ดจะมีหน้าตาประมาณนี้:</p><pre>&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD\nconsole.log("Hello from main");\n=======\nconsole.log("Hello from feature");\n&gt;&gt;&gt;&gt;&gt;&gt;&gt; feature-branch</pre><p>สิ่งที่คุณต้องทำคือ <strong>ลบเครื่องหมายที่ Git สร้างขึ้น และตัดสินใจเก็บโค้ดส่วนที่ถูกต้องไว้</strong> (หรือเก็บทั้งสองอย่าง) จากนั้นทำการบันทึกไฟล์ ใช้ <code>git add</code> และ <code>git commit</code> อีกครั้งเพื่อยืนยันการแก้ Conflict เพียงเท่านี้การ Merge ก็เสร็จสมบูรณ์</p><div class="tip-box"><strong>💡 Pro Tip</strong>: เคล็ดลับการลด Conflict คือการสื่อสารกับทีม พยายามอัปเดต (Pull) โค้ดจาก main บ่อยๆ เพื่อให้ Branch ของคุณมีโค้ดล่าสุดเสมอ และแยกไฟล์การทำงานกันให้ชัดเจน</div>'
  },
  {
    number: 5,
    slug: 'github',
    emoji: '☁️',
    title: 'GitHub and Remotes: ทำงานร่วมกับทีมบน Cloud',
    content: '<h2>Git Remote คืออะไร?</h2><p>จนถึงตอนนี้ เราใช้งาน Git อยู่แค่บนเครื่องของเราคนเดียว (Local) แต่ถ้าต้องการให้คนอื่นเข้าถึงโปรเจกต์ หรือต้องการเก็บ Backup เราต้องมี "Remote Repository" ซึ่งคือเซิร์ฟเวอร์ที่เก็บโค้ดของเรา แพลตฟอร์มที่เป็นที่นิยมที่สุดคือ <strong>GitHub</strong>, <strong>GitLab</strong>, และ <strong>Bitbucket</strong></p><h2>การเชื่อมต่อกับ Remote</h2><ul><li><strong>git clone [URL]:</strong> ใช้ดาวน์โหลด Repository ทั้งหมดจาก Remote มายังเครื่องของเรา (มักจะเป็นการเริ่มโปรเจกต์ใหม่) Git จะเซ็ตชื่อ Remote ให้เป็น <code>origin</code> โดยอัตโนมัติ</li><li><strong>git remote add [name] [URL]:</strong> ใช้เชื่อมโปรเจกต์บนเครื่องเรา (ที่รัน git init ไว้แล้ว) ไปยัง Remote บน Cloud เช่น <code>git remote add origin https://github.com/user/repo.git</code></li><li><strong>git remote -v:</strong> ตรวจสอบว่าเชื่อมต่อกับ Remote ตัวไหนบ้าง (จะแสดงทั้ง Fetch และ Push URL)</li></ul><h2>Push, Pull และ Fetch</h2><ol style="padding-left:20px"><li><strong>git push:</strong> ส่ง Commit ที่อยู่ในเครื่องของเราขึ้นไปยัง Remote Repository เช่น <code>git push origin main</code> ถ้าคุณเพิ่งสร้าง Branch ใหม่ในเครื่อง และต้องการดันขึ้น Remote ครั้งแรก ต้องใช้ <code>git push -u origin [branch-name]</code> เพื่อเซ็ต Upstream</li><li><strong>git fetch:</strong> ดึงข้อมูลอัปเดตจาก Remote มายังเครื่องของเรา "แต่ยังไม่รวม (Merge) เข้ากับโค้ดที่เรากำลังทำงานอยู่" ทำให้เราสามารถตรวจสอบก่อนได้ว่ามีใครเปลี่ยนอะไรไปบ้าง</li><li><strong>git pull:</strong> เป็นการรวมคำสั่ง <code>git fetch</code> และ <code>git merge</code> เข้าด้วยกัน คือดึงข้อมูลใหม่มาและรวมเข้ากับ Branch ปัจจุบันทันที เป็นคำสั่งที่ใช้บ่อยที่สุดเพื่ออัปเดตโค้ด</li></ol><h2>Forks และ Pull Requests สไตล์ Open Source</h2><p>ในโลกของ Open Source เรามักจะไม่มีสิทธิ์ Push โค้ดเข้าสู่ Repository หลักของคนอื่นโดยตรง เราจึงต้องใช้กระบวนการที่เรียกว่า <strong>Fork</strong> (คัดลอก Repo ของคนอื่นมาเป็นของเราบน GitHub) จากนั้นเราแก้โค้ดบน Repo ของเราแล้วเปิดสิ่งที่เรียกว่า <strong>Pull Request (PR)</strong> เพื่อขอให้เจ้าของโปรเจกต์ดึง (Pull) โค้ดของเรากลับเข้าไปรวมกับโปรเจกต์หลัก</p><h2>การยืนยันตัวตนด้วย SSH Keys (Authentication)</h2><p>การ Push/Pull โค้ดบ่อยๆ โดยต้องใส่ Username และ Password ทุกครั้งเป็นเรื่องน่ารำคาญ (และปัจจุบัน GitHub ไม่อนุญาตให้ใช้ Password ตรงๆ แล้ว ต้องใช้ Personal Access Token) วิธีที่เป็นมาตรฐานและปลอดภัยกว่าคือการใช้ <strong>SSH Keys</strong></p><p>SSH Key เป็นเหมือนกุญแจสองดอก (Public Key และ Private Key) คุณเก็บ Private Key ไว้ในเครื่องอย่างปลอดภัย (ห้ามให้ใครเห็น) และเอา Public Key ไปวางไว้บน GitHub เมื่อคุณทำคำสั่ง Git เครื่องจะทำการตรวจสอบกุญแจกันเอง ทำให้คุณทำงานได้อย่างปลอดภัยและไม่ต้องกรอกรหัสผ่านอีกต่อไป</p><div class="tip-box"><strong>💡 Pro Tip</strong>: หากต้องทำงานกับหลาย GitHub Account (เช่น ของบริษัทและของส่วนตัว) คุณสามารถสร้าง SSH Key หลายคู่ และตั้งค่าในไฟล์ <code>~/.ssh/config</code> เพื่อให้ Git สลับ Account อัตโนมัติตาม URL ของ Repo ได้</div>'
  },
  {
    number: 6,
    slug: 'workflow',
    emoji: '🔄',
    title: 'Git Workflow และ PRs: มาตรฐานการทำงานของทีมระดับโลก',
    content: '<h2>ทำไมต้องมี Git Workflow?</h2><p>ถ้าทุกคนในทีมต่างคนต่าง Push โค้ดเข้า Branch <code>main</code> โดยตรง (Direct Commit) หายนะย่อมเกิดแน่นอน โค้ดจะพังบ่อย ไม่มีการตรวจทาน (Code Review) และยากต่อการหาคนรับผิดชอบ Git Workflow คือข้อตกลงและกระบวนการทำงานร่วมกันของทีม เพื่อให้โค้ดมีคุณภาพและป้องกันข้อผิดพลาด</p><h2>Workflow ยอดฮิตในวงการ</h2><ol style="padding-left:20px"><li><strong>Feature Branch Workflow:</strong> เป็นพื้นฐานที่สุด กฎมีข้อเดียวคือ "ห้าม Commit ลง main เด็ดขาด" งานทุกชิ้น (ฟีเจอร์, แก้บั๊ก) ต้องสร้าง Branch ใหม่เสมอ เมื่อเสร็จค่อยนำมารวมเข้า main เหมาะกับทีมขนาดเล็ก-กลาง</li><li><strong>GitFlow Workflow:</strong> เป็นระบบที่มีกฎเกณฑ์เข้มงวด มี Branch หลักคือ <code>main</code> (เฉพาะโค้ดพร้อม Deploy), <code>develop</code> (รวมฟีเจอร์เพื่อเทสต์), และ Branch ชั่วคราวอย่าง <code>feature</code>, <code>release</code>, <code>hotfix</code> เหมาะกับโปรเจกต์ Enterprise ที่มีการแบ่ง Version Release (เช่น 1.0.0, 1.1.0) อย่างชัดเจน</li><li><strong>GitHub Flow:</strong> แนวทางที่ GitHub และองค์กรสาย Agile นิยมใช้ เป็นการลดความซับซ้อนของ GitFlow เหลือแค่ <code>main</code> กับ <code>feature branches</code> เท่านั้น เน้นทำเสร็จแล้ว Deploy ทันที (Continuous Deployment)</li></ol><h2>Pull Requests (PR) / Merge Requests (MR)</h2><p>ไม่ว่าคุณจะใช้ Workflow ไหน หัวใจสำคัญของการรวม Branch ในปัจจุบันคือ Pull Request (GitHub) หรือ Merge Request (GitLab) มันไม่ใช่คำสั่งของ Git แต่เป็นฟีเจอร์บนแพลตฟอร์ม</p><p>เมื่อคุณ Push ฟีเจอร์ขึ้น Remote แทนที่จะใช้ Git merge ในเครื่อง คุณจะไปที่หน้าเว็บแล้วกดสร้าง PR ขั้นตอนนี้คือพื้นที่สำหรับ <strong>Code Review</strong></p><ul><li><strong>ประโยชน์ของ PR:</strong> ทีมสามารถเข้ามาดูการเปลี่ยนแปลงทีละบรรทัด (Diff), ถกเถียงและคอมเมนต์เพื่อขอให้แก้โค้ด, และ CI/CD (ระบบทดสอบอัตโนมัติ) สามารถรันเทสต์ก่อนล่วงหน้าเพื่อคอนเฟิร์มว่าโค้ดใหม่จะไม่ทำให้ระบบพัง</li><li><strong>Approval:</strong> มักจะมีกฎว่า PR จะ Merge ได้ก็ต่อเมื่อมีเพื่อนในทีม (Reviewer) อย่างน้อย 1-2 คนเข้ามากด Approve</li></ul><h2>Best Practices สำหรับการเปิด PR</h2><ul><li><strong>ทำ PR ให้เล็ก (Keep PRs Small):</strong> PR ที่แก้ไขไฟล์เป็นสิบๆ ไฟล์ หรือหลักพันบรรทัด จะไม่มีใครอยากตรวจ และมักจะโดนตรวจแบบผ่านๆ (LGTM - Looks Good To Me) ให้แบ่งฟีเจอร์ใหญ่ออกเป็น PR เล็กๆ</li><li><strong>เขียน Description ให้ชัดเจน:</strong> อธิบายว่า PR นี้แก้ปัญหาอะไร ทำไปทำไม (Why) และระบุวิธีเทสต์ (How to test) มีรูปภาพหรือวิดีโอ (Screen record) ประกอบจะยอดเยี่ยมมาก</li><li><strong>เชื่อมโยงกับ Issue Tracking:</strong> ใส่หมายเลข Ticket เช่น "Fixes #123" (หรือ Jira ticket) เพื่อให้ระบบปิด Issue นั้นอัตโนมัติเมื่อ PR ถูก Merge</li></ul><div class="tip-box"><strong>💡 Pro Tip</strong>: การเป็น Reviewer ที่ดี ไม่ใช่การจับผิด แต่คือการรักษามาตรฐาน แนะนำทางออกที่ขึ้น และควรสื่อสารอย่างสร้างสรรค์ (Constructive feedback) เสมอ</div>'
  },
  {
    number: 7,
    slug: 'advanced-git',
    emoji: '⚡',
    title: 'Advanced Git: รีทัชประวัติศาสตร์และแก้ไขข้อผิดพลาด',
    content: '<h2>Rebase vs Merge: สองแนวทางการรวมสาขา</h2><p>ถ้าคุณเบื่อกับประวัติ Git ที่ยุ่งเหยิงเต็มไปด้วย Merge Commits สานกันไปมาเหมือนเส้นบะหมี่ (Spaghetti History) คุณควรทำความรู้จักกับ <strong>Rebase</strong></p><p><code>git rebase [branch]</code> มีจุดประสงค์เหมือนการ Merge คือเอาโค้ดมารวมกัน แต่วิธีการทำงานต่างกัน Rebase จะทำการ "ถอด" Commit ของคุณออก แล้วเลื่อนฐาน (Base) ของ Branch คุณไปต่อท้ายสุดของ Branch หลัก จากนั้นค่อย "สวม" Commit ของคุณกลับเข้าไปใหม่</p><ul><li><strong>ข้อดี:</strong> ประวัติ Git จะเป็นเส้นตรง (Linear History) อ่านง่าย ดูสะอาดตา</li><li><strong>ข้อควรระวัง (Golden Rule of Rebase):</strong> ห้ามทำ Rebase กับ Branch ที่ Push ขึ้น Remote ไปแล้วและมีคนอื่นกำลังใช้งานอยู่เด็ดขาด! เพราะ Commit ID จะเปลี่ยนไปทั้งหมด ทำให้เพื่อนร่วมทีมที่ดึงโค้ดไปเจอปัญหาใหญ่หลวง</li></ul><h2>การรวม Commit (Squash)</h2><p>ระหว่างทำงาน คุณอาจมี Commit ยิบย่อยเช่น "wip", "fix typo", "try fixing bug", "really fix bug" การนำประวัติเหล่านี้เข้า Branch หลักอาจจะรกเกินไป คุณสามารถทำ <strong>Interactive Rebase</strong> (<code>git rebase -i</code>) เพื่อเลือกยุบ (Squash) Commit หลายๆ อันรวมกันเป็น Commit เดียวที่มีความหมายและทรงพลังก่อนทำการ Merge หรือเปิด PR</p><h2>กู้คืนความพินาศ: Reset และ Reflog</h2><p>ทำ Commit ผิดไปแล้ว ทำอย่างไรดี? คำสั่ง <code>git reset</code> คือพระเอกในสถานการณ์นี้ มันมีระดับความรุนแรง 3 ระดับ:</p><ol style="padding-left:20px"><li><strong>--soft:</strong> ย้อน Commit แต่เก็บไฟล์ที่แก้ไว้ใน Staging Area (พร้อม Commit ใหม่)</li><li><strong>--mixed (ค่าเริ่มต้น):</strong> ย้อน Commit และดึงไฟล์ออกจาก Staging (Unstage) แต่ยังเก็บการแก้ไขไว้ในไฟล์บนเครื่อง</li><li><strong>--hard:</strong> โหดสุด ย้อน Commit และ "ลบการแก้ไขทิ้งทั้งหมด" ให้ไฟล์กลับไปเหมือนใน Commit นั้นเป๊ะ (ระวังข้อมูลหาย!)</li></ol><p>แล้วถ้าเผลอ <code>reset --hard</code> ผิดล่ะ? ข้อมูลหายหมดแล้วหรือยัง? ไม่! Git มีเครื่องมือเทพที่เรียกว่า <strong>Reflog</strong> (Reference logs) ใช้คำสั่ง <code>git reflog</code> คุณจะเห็นประวัติการขยับ Pointer ของ Git ทุกฝีก้าวแม้กระทั่งการลบ Branch หรือ Reset คุณสามารถดู Commit ID เก่าและสั่ง Reset กลับไปหาจุดก่อนที่คุณจะทำผิดพลาดได้</p><h2>Cherry-pick และ Stash</h2><ul><li><strong>git cherry-pick [commit-hash]:</strong> สมมติว่ามี 1 Commit ใน Branch ของเพื่อนที่ดีมากๆ คุณไม่อยาก Merge มาทั้ง Branch แต่อยากหยิบมาแค่ Commit เดียว คำสั่งนี้จะช่วยดึง Commit นั้นมาแปะใน Branch ของคุณทันที</li><li><strong>git stash:</strong> กำลังแก้โค้ดอยู่ดีๆ แต่ยังไม่เสร็จพอจะ Commit แล้วหัวหน้าสั่งให้สลับ Branch ไปแก้บั๊กฉุกเฉิน คุณสามารถพิมพ์ <code>git stash</code> เพื่อ "พับเก็บ" โค้ดที่แก้ค้างไว้เข้าลิ้นชักชั่วคราว ทำให้ Working Directory สะอาด พอแก้บั๊กเสร็จกลับมา Branch เดิม ก็ใช้ <code>git stash pop</code> เพื่อดึงงานที่พับไว้ออกมาทำต่อ</li></ul><div class="tip-box"><strong>💡 Pro Tip</strong>: คำสั่ง Git ชั้นสูงเหล่านี้เป็นเครื่องมือประทังชีวิต ยิ่งเข้าใจว่า Git ทำงานคล้ายคลึงกับกราฟแบบ Tree (DAG) มากเท่าไหร่ คุณก็จะใช้คำสั่งเหล่านี้ได้อย่างไม่ลังเล</div>'
  },
  {
    number: 8,
    slug: 'hooks',
    emoji: '🪝',
    title: 'Git Hooks: ระบบอัตโนมัติป้องกันโค้ดขยะก่อนเข้าโปรเจกต์',
    content: '<h2>Git Hooks คืออะไร?</h2><p>เคยไหมที่แก้โค้ดเสร็จแล้วลืมจัด Format โค้ด (Prettier) หรือลืมรัน Linter (ESLint) แล้วดัน Push โค้ดที่มี Error ขึ้นไปให้คนในทีมบ่น? เราสามารถป้องกันปัญหานี้ได้ด้วย <strong>Git Hooks</strong></p><p>Git Hooks คือสคริปต์ (Script) ที่จะทำงานโดยอัตโนมัติเมื่อเกิดเหตุการณ์ (Events) สำคัญๆ ใน Git เช่น ก่อนการ Commit (pre-commit), หรือก่อนการ Push (pre-push) ไฟล์ Hooks เหล่านี้จะถูกซ่อนอยู่ในโฟลเดอร์ <code>.git/hooks/</code> ของทุก Repository โดยค่าเริ่มต้น</p><h2>ประเภทของ Hooks ที่ใช้บ่อย</h2><ol style="padding-left:20px"><li><strong>pre-commit:</strong> ทำงาน "ก่อน" ที่หน้าต่างให้พิมพ์ข้อความ Commit จะเด้งขึ้นมา มักใช้เพื่อตรวจเช็ค Syntax ของโค้ด จัดฟอร์แมต หรือสแกนหา Secret Keys ที่เผลอหลุดไป ถ้าสคริปต์รันแล้วพัง (Return code ไม่ใช่ 0) การ Commit จะถูกยกเลิกทันที</li><li><strong>commit-msg:</strong> ทำงานขณะกำลังอ่านข้อความ Commit นิยมใช้เพื่อตรวจสอบว่าข้อความที่พิมพ์มาตรงตามรูปแบบมาตรฐานที่กำหนดไว้หรือไม่ (เช่น ต้องขึ้นต้นด้วย feat: หรือ fix: ตามมาตรฐาน Conventional Commits)</li><li><strong>pre-push:</strong> ทำงานก่อนที่จะเริ่มอัปโหลดข้อมูลไปยัง Remote ใช้สำหรับรัน Unit Test เต็มรูปแบบที่อาจจะใช้เวลานาน ไม่เหมาะจะรันทุกครั้งที่ Commit แต่อยากให้ชัวร์ก่อน Push ขึ้น Cloud</li></ol><h2>ปัญหาของ Native Git Hooks</h2><p>แม้ Git จะมี Hooks มาให้ในตัว แต่ปัญหาคือ โฟลเดอร์ <code>.git/</code> ไม่สามารถนำขึ้น Remote (GitHub) เพื่อแชร์ให้คนในทีมได้ นั่นหมายความว่าทีมต้องก๊อปปี้สคริปต์ไปลงเครื่องใครเครื่องมันเอง ซึ่งจัดการยากมาก ในโลกของ Node.js และ JavaScript จึงเกิดเครื่องมือเพื่อแก้ปัญหานี้</p><h2>Husky และ Lint-Staged</h2><ul><li><strong>Husky:</strong> เป็นแพ็กเกจ npm ที่ช่วยจัดการ Git Hooks ให้ง่ายขึ้นมาก คุณแค่ลง Husky ไว้ในโปรเจกต์ (package.json) เมื่อใครก็ตามที่ clone โปรเจกต์ไปแล้วรัน <code>npm install</code> Husky จะติดตั้ง Hooks ทุกตัวที่ทีมเตรียมไว้ให้โดยอัตโนมัติ</li><li><strong>Lint-Staged:</strong> ถ้าโปรเจกต์ใหญ่มาก การรัน Linter ทั้งโปรเจกต์ทุกครั้งที่ Commit จะกินเวลามาก Lint-Staged ออกแบบมาให้ทำงานร่วมกับ Husky โดยมันจะดึงเอามาตรวจสอบเฉพาะ "ไฟล์ที่เพิ่งถูก git add (Staged files)" ในรอบนั้นเท่านั้น ช่วยลดเวลาการทำงานจากหลายนาทีเหลือเพียงเสี้ยววินาที</li></ul><div class="tip-box"><strong>💡 Pro Tip</strong>: ตัวอย่างการทำงานร่วมกัน: เมื่อกด git commit Husky จะจับ Event pre-commit แล้วไปเรียก Lint-Staged สั่งให้ Prettier จัดฟอร์แมตโค้ดในไฟล์ที่แก้และ ESLint ตรวจบั๊ก หากผ่านฉลุย โค้ดถึงจะถูก Commit ลง Git อย่างสวยงาม ระบบนี้เรียกว่าเป็นการสร้าง "คุณภาพโค้ด" ตั้งแต่ต้นทาง</div>'
  },
  {
    number: 9,
    slug: 'shell',
    emoji: '📜',
    title: 'Bash/Shell Scripting Basics: เขียนสคริปต์ให้ทำงานแทนคุณ',
    content: '<h2>ทำไมต้องเขียน Shell Script?</h2><p>ตลอดหลักสูตรนี้เราได้เรียนรู้การพิมพ์คำสั่งบน Terminal ไปแล้ว แต่ถ้าคุณมีชุดคำสั่งยาวๆ 10 บรรทัดที่ต้องพิมพ์ทุกเช้าเพื่อรันเซิร์ฟเวอร์ (เช่น ต้องเปิด Docker, เคลียร์ Cache, ดึงโค้ดล่าสุด, แล้วค่อย Start โพรเซส) การมานั่งพิมพ์ทีละบรรทัดทุกวันคงเสียเวลาชีวิต เราสามารถเอาคำสั่งทั้งหมดมารวมไว้ในไฟล์เดียวที่เรียกว่า <strong>Shell Script</strong> เพื่อสั่งให้ทำงานทีเดียวแบบอัตโนมัติ</p><h2>พื้นฐานการสร้าง Shell Script</h2><ol style="padding-left:20px"><li><strong>สร้างไฟล์:</strong> เริ่มด้วยการสร้างไฟล์ที่มีนามสกุล <code>.sh</code> เช่น <code>deploy.sh</code></li><li><strong>Shebang (บรรทัดแรกของไฟล์):</strong> ต้องขึ้นต้นไฟล์ด้วย <code>#!/bin/bash</code> เพื่อบอกระบบให้รู้ว่าสคริปต์นี้ต้องถูกรันด้วยโปรแกรมแปลภาษาอะไร (ในที่นี้คือ bash)</li><li><strong>กำหนดสิทธิ์ (Permission):</strong> ตามค่าเริ่มต้นไฟล์จะไม่สามารถรันได้ ต้องใช้คำสั่ง <code>chmod +x deploy.sh</code> เพื่อทำให้ไฟล์เป็น Executable</li><li><strong>วิธีรันสคริปต์:</strong> รันด้วย <code>./deploy.sh</code></li></ol><h2>โครงสร้างภาษา Bash</h2><p>Bash ก็เหมือนภาษาโปรแกรมมิ่งทั่วไปที่มีตัวแปร เงื่อนไข และการวนลูป:</p><ul><li><strong>ตัวแปร (Variables):</strong> การประกาศตัวแปรห้ามเว้นวรรครอบเครื่องหมายเท่ากับ (<code>=</code>) เด็ดขาด เช่น <code>NAME="John"</code> ส่วนการเรียกใช้ให้ใส่ <code>$</code> นำหน้า เช่น <code>echo "Hello $NAME"</code></li><li><strong>รับค่าพารามิเตอร์ (Arguments):</strong> สามารถรับค่าที่พิมพ์ต่อท้ายตอนรันสคริปต์ได้ โดยใช้ <code>$1</code>, <code>$2</code> เช่น <code>./greet.sh John</code> (ในสคริปต์ <code>$1</code> จะมีค่าเท่ากับ "John")</li><li><strong>เงื่อนไข (Conditionals):</strong> ใช้สร้างตรรกะการตัดสินใจ โครงสร้างจะเป็น <code>if [ เงื่อนไข ]; then ... else ... fi</code><pre>if [ "$USER" == "root" ]; then\n  echo "You are admin!"\nelse\n  echo "Please run as root"\nfi</pre></li><li><strong>การวนลูป (Loops):</strong> ใช้ทำงานซ้ำๆ โครงสร้าง for loop พื้นฐาน เช่น<pre>for file in *.txt; do\n  echo "Processing $file"\n  mv "$file" "processed_$file"\ndone</pre></li></ul><h2>นำไปประยุกต์ใช้อะไรได้บ้าง?</h2><p>คุณสามารถใช้ Shell Script ร่วมกับคำสั่ง Git และ Terminal ที่เรียนมาทั้งหมดเพื่อสร้างเครื่องมือสุดเจ๋งได้มากมาย เช่น:</p><ul><li>สคริปต์ <strong>Backup</strong> โฟลเดอร์ที่สำคัญ บีบอัดไฟล์ (zip) และประทับเวลา (timestamp) ไว้ที่ชื่อไฟล์</li><li>สคริปต์รวบยอดคำสั่ง <strong>Git</strong> เช่น ดึงโค้ด, ลบ Branch เก่าที่ไม่ได้ใช้ แล้วโชว์ผลลัพธ์ว่าอัปเดตเรียบร้อย</li><li>สคริปต์ตั้งค่าเครื่องคอมพิวเตอร์ใหม่ (<strong>Dotfiles</strong>) ที่โหลดโปรแกรมและตั้งค่าต่างๆ อัตโนมัติในคลิกเดียว</li></ul><div class="tip-box"><strong>💡 บทสรุป</strong>: ยินดีด้วย! ตอนนี้คุณได้ติดปีกด้วยทักษะ Command Line และ Git เรียบร้อยแล้ว สองเครื่องมือนี้คือกระดูกสันหลังของวงการ Software Development ขอให้สนุกกับการนำความรู้ไปประยุกต์ใช้เพื่อทำให้งานของคุณง่ายและเร็วขึ้นนะครับ!</div>'
  }
];
