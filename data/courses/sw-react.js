export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '⚛️',
    title: 'บทนำ: ทำไมต้อง React?',
    content: '<h2>ยินดีต้อนรับสู่โลกของ React</h2><p>ในยุคปัจจุบันของการพัฒนาเว็บแอปพลิเคชัน (Web Application Development) ความคาดหวังของผู้ใช้งานต่อความเร็ว การตอบสนอง (Responsiveness) และประสบการณ์การใช้งาน (User Experience - UX) นั้นสูงขึ้นอย่างมาก การสร้างเว็บไซต์แบบดั้งเดิมที่ต้องโหลดหน้าใหม่ทุกครั้งเมื่อมีการเปลี่ยนหน้าหรือส่งข้อมูลเริ่มไม่ตอบโจทย์อีกต่อไป นี่คือจุดที่ <strong>Single Page Applications (SPA)</strong> ก้าวเข้ามามีบทบาทสำคัญ และ <strong>React</strong> คือหนึ่งในไลบรารีที่ทรงอิทธิพลที่สุดในการสร้าง SPA ในปัจจุบัน</p><h2>React คืออะไร?</h2><p>React เป็น JavaScript Library แบบ Open-source ที่ถูกสร้างและดูแลโดย Facebook (หรือ Meta ในปัจจุบัน) ถูกเปิดตัวครั้งแรกในปี 2013 โดยมีเป้าหมายหลักในการสร้าง <strong>User Interfaces (UI)</strong> หรือส่วนติดต่อผู้ใช้งานที่ซับซ้อนให้มีประสิทธิภาพและจัดการได้ง่าย React ไม่ใช่ Framework แบบเต็มรูปแบบ (Full-fledged Framework) เหมือน Angular หรือ Vue (แม้ Vue จะเป็นลักษณะผสม) แต่ React โฟกัสเฉพาะส่วนที่เป็น "View" ในสถาปัตยกรรม Model-View-Controller (MVC) เท่านั้น ซึ่งทำให้ React มีความยืดหยุ่นสูงมากในการเลือกใช้เครื่องมือหรือไลบรารีอื่นๆ มาประกอบกัน</p><h2>สถาปัตยกรรมแบบ Component-based (Component-based Architecture)</h2><p>แนวคิดที่เป็นหัวใจสำคัญของ React คือการแบ่งหน้าจอ UI ที่ซับซ้อนออกเป็นส่วนย่อยๆ ที่เรียกว่า <strong>Component</strong> ลองจินตนาการถึงหน้าเว็บ Facebook ที่มีทั้งแถบนำทาง (Navbar), แถบสถานะ (Status Bar), รายการโพสต์ (News Feed), และแชท (Chat Box) แทนที่เราจะเขียนโค้ด HTML/JS รวมกันทั้งหมดในไฟล์เดียว React สนับสนุนให้เราสร้าง Component แยกกันสำหรับแต่ละส่วน</p><p>ข้อดีของ Component-based architecture มีหลายประการ:</p><ul><li><strong>Reusability (การนำกลับมาใช้ใหม่):</strong> Component ที่สร้างขึ้นสามารถนำไปใช้ในหน้าอื่นๆ หรือแม้แต่โปรเจกต์อื่นๆ ได้ เช่น Button Component, Card Component</li><li><strong>Maintainability (การบำรุงรักษา):</strong> เมื่อเกิดบั๊กหรือต้องการแก้ไข UI เราสามารถพุ่งเป้าไปที่ Component นั้นๆ ได้ทันทีโดยไม่กระทบกับส่วนอื่น</li><li><strong>Separation of Concerns:</strong> แต่ละ Component รับผิดชอบเฉพาะการแสดงผลและการทำงานของตัวเอง ทำให้โค้ดอ่านง่ายและเป็นระเบียบ</li></ul><h2>Virtual DOM: ความลับแห่งความเร็ว</h2><p>หนึ่งในปัญหาหลักของการทำเว็บแบบดั้งเดิมคือการแก้ไข DOM (Document Object Model) ดั้งเดิมของเบราว์เซอร์นั้นใช้ทรัพยากรสูงและช้ามาก ทุกครั้งที่มีการเปลี่ยนแปลง DOM เบราว์เซอร์จะต้องคำนวณ Layout ใหม่และ Paint หน้าจอใหม่ (Reflow and Repaint) ซึ่งหากมีการอัปเดตบ่อยๆ จะทำให้เว็บกระตุก</p><p>React แก้ปัญหานี้ด้วยนวัตกรรมที่เรียกว่า <strong>Virtual DOM</strong> (DOM เสมือน)</p><ol style="padding-left:20px"><li>เมื่อ State หรือข้อมูลใน React เปลี่ยนแปลง React จะสร้าง Virtual DOM Tree ขึ้นมาใหม่ในหน่วยความจำ (Memory) ซึ่งเป็นเพียง JavaScript Object ธรรมดา ทำให้การสร้างรวดเร็วมาก</li><li>React จะนำ Virtual DOM ตัวใหม่นี้ไปเปรียบเทียบ (Diffing) กับ Virtual DOM ตัวเก่าที่บันทึกไว้</li><li>React จะคำนวณหาความแตกต่าง (Differences หรือ Patches) ว่ามี Node ไหนบ้างที่เปลี่ยนไปจริงๆ ขั้นตอนนี้เรียกว่า <strong>Reconciliation</strong></li><li>สุดท้าย React จะทำการอัปเดต <strong>เฉพาะส่วนที่มีการเปลี่ยนแปลงจริงๆ</strong> ลงใน Real DOM ของเบราว์เซอร์ในครั้งเดียว (Batch Update) ทำให้ลดการ Reflow และ Repaint ลงได้อย่างมหาศาล</li></ol><div class="tip-box"><strong>💡 Pro Tip:</strong> การทำงานของ Virtual DOM เป็นเหตุผลสำคัญว่าทำไม React ถึงสามารถจัดการกับหน้าจอที่มีการอัปเดตข้อมูลแบบ Real-time (เช่น กราฟหุ้น, แชท, หรือกระดานคะแนน) ได้อย่างลื่นไหล หากคุณเข้าใจว่า React อัปเดต DOM อย่างไร คุณจะสามารถเขียนโค้ดเพื่อ Optimize Performance ได้ดียิ่งขึ้นในบทหลังๆ</div><h2>ทำความรู้จักกับ JSX (JavaScript XML)</h2><p>หากคุณเคยเขียน HTML และ JavaScript แยกไฟล์กัน คุณอาจจะรู้สึกแปลกๆ เมื่อเห็นโค้ด React ครั้งแรก เพราะ React ใช้ <strong>JSX</strong> ซึ่งเป็น Syntax Extension สำหรับ JavaScript ที่อนุญาตให้คุณเขียนโค้ดที่หน้าตาเหมือน HTML ลงไปในไฟล์ JavaScript ได้เลย!</p><p>ตัวอย่างโค้ด JSX เบื้องต้น:</p><pre><code>const element = &lt;h1&gt;Hello, React!&lt;/h1&gt;;</code></pre><p>แม้จะดูเหมือน HTML แต่มันคือ JavaScript ภายใต้เบื้องหลัง JSX จะถูกแปลง (Compile) ผ่านเครื่องมืออย่าง Babel ให้กลายเป็นฟังก์ชัน `React.createElement()` ทำให้เราสามารถใช้พลังทั้งหมดของ JavaScript ภายใน HTML ได้ เช่น การใส่ตัวแปร, การใช้เงื่อนไข (if-else), หรือการวนลูป (map) ผ่านวงเล็บปีกกา `{}`</p><h2>ภาพรวมของ Ecosystem</h2><p>การเรียน React ไม่ใช่แค่การเรียนตัวไลบรารี React เท่านั้น แต่ยังรวมถึง Ecosystem ที่กว้างใหญ่ไพศาล ในคอร์สนี้เราจะค่อยๆ เจาะลึกไปถึง:</p><ul><li><strong>State Management:</strong> การจัดการข้อมูลภายในแอป เช่น Redux, Context API, หรือ Zustand</li><li><strong>Routing:</strong> การเปลี่ยนหน้าเว็บโดยไม่โหลดหน้าใหม่ด้วย React Router</li><li><strong>Data Fetching:</strong> การดึงข้อมูลจาก API อย่างมีประสิทธิภาพด้วยเครื่องมืออย่าง Axios หรือ React Query</li><li><strong>Styling:</strong> การจัดการ CSS ใน React เช่น Tailwind CSS, Styled-components, หรือ CSS Modules</li></ul><p>ในบทต่อไป เราจะมาเริ่มติดตั้งสภาพแวดล้อมสำหรับการพัฒนา (Development Environment) และสร้างโปรเจกต์ React แรกของเราไปด้วยกัน เตรียมตัวให้พร้อมสำหรับการเดินทางเข้าสู่โลกของ Modern Web Development!</p>'
  },
  {
    number: 1,
    slug: 'setup',
    emoji: '🛠️',
    title: 'React Setup & JSX',
    content: '<h2>การตั้งค่าโปรเจกต์ React (React Setup)</h2><p>ก่อนที่เราจะเริ่มเขียนโค้ด React ได้ เราจำเป็นต้องเตรียมเครื่องมือให้พร้อม การตั้งค่าโปรเจกต์ React ในอดีตอาจจะยุ่งยากเพราะต้องตั้งค่า Webpack และ Babel เอง แต่ปัจจุบันเรามีเครื่องมือที่ช่วยสร้างโปรเจกต์ (Boilerplate) ให้พร้อมใช้งานได้ภายในไม่กี่วินาที</p><h3>Create React App (CRA) vs Vite</h3><p>หลายปีที่ผ่านมา <strong>Create React App (CRA)</strong> คือมาตรฐานที่ทุกคนแนะนำสำหรับผู้เริ่มต้น CRA ถูกพัฒนาโดยทีม React เอง มันครอบคลุมการตั้งค่าทุกอย่างแบบ Out-of-the-box ทำให้ผู้พัฒนาไม่ต้องปวดหัวกับการ Config แต่เมื่อโปรเจกต์มีขนาดใหญ่ขึ้น CRA กลับมีปัญหาเรื่องความเร็วในการ Build และการเริ่ม Development Server ที่ช้ามาก (บางโปรเจกต์อาจใช้เวลาหลายนาที)</p><p>นี่คือสาเหตุที่ <strong>Vite</strong> (อ่านว่า วีท) ก้าวเข้ามาแทนที่ Vite เป็น Build Tool รุ่นใหม่ที่ใช้ Native ES Modules (ESM) ในบราวเซอร์ระหว่างการ Development ทำให้มันสามารถ Start Server ได้แทบจะในทันที (หลักมิลลิวินาที) และ Hot Module Replacement (HMR) - การอัปเดตหน้าจอเมื่อแก้โค้ด - ก็ทำงานได้รวดเร็วมาก ไม่ว่าโปรเจกต์จะใหญ่แค่ไหนก็ตาม ปัจจุบันแม้แต่ Document ออฟฟิเชียลของ React เองก็แนะนำให้ใช้ Framework หรือ Tool อย่าง Vite แทน CRA แล้ว</p><h3>การสร้างโปรเจกต์ด้วย Vite</h3><p>เพื่อให้ทุกคนได้ใช้เครื่องมือที่ทันสมัยที่สุด ในคอร์สนี้เราจะใช้ Vite ในการสร้างโปรเจกต์ เปิด Terminal ของคุณขึ้นมาแล้วพิมพ์คำสั่งต่อไปนี้ (ต้องติดตั้ง Node.js ก่อนนะ!):</p><pre><code>npm create vite@latest my-react-app -- --template react\ncd my-react-app\nnpm install\nnpm run dev</code></pre><p>เมื่อคำสั่งทำงานเสร็จสิ้น คุณจะได้ URL (มักจะเป็น `http://localhost:5173`) ให้เปิดในเบราว์เซอร์ คุณจะพบกับหน้าจอเริ่มต้นของ React ที่รันด้วย Vite ยินดีด้วย! คุณได้สร้างโปรเจกต์ React แรกสำเร็จแล้ว</p><h2>โครงสร้างโปรเจกต์เบื้องต้น</h2><p>เมื่อคุณเปิดโปรเจกต์ใน Code Editor (เช่น VS Code) คุณจะพบกับโครงสร้างไฟล์ดังนี้:</p><ul><li><strong>node_modules/:</strong> โฟลเดอร์เก็บไลบรารีทั้งหมด (ห้ามแก้ไขและไม่ควรนำขึ้น Git)</li><li><strong>public/:</strong> โฟลเดอร์สำหรับเก็บไฟล์ Static (เช่น Favicon หรือรูปภาพที่ไม่ต้องการให้ Vite นำไป Process)</li><li><strong>src/:</strong> หัวใจของโปรเจกต์ โค้ด React ที่เราเขียนเกือบทั้งหมดจะอยู่ที่นี่<ul><li><code>main.jsx</code>: จุดเริ่มต้น (Entry Point) ของแอปพลิเคชัน ทำหน้าที่นำ Component หลัก (App) ไป render ลงใน HTML</li><li><code>App.jsx</code>: Component หลัก (Root Component) ที่ครอบแอปของเราไว้ทั้งหมด</li></ul></li><li><strong>index.html:</strong> ไฟล์ HTML หลักที่มี <code>&lt;div id="root"&gt;&lt;/div&gt;</code> ซึ่งเป็นจุดที่ React จะแทรกตัวเข้าไป</li><li><strong>package.json:</strong> ไฟล์จัดการ Dependencies และ Scripts ต่างๆ</li></ul><h2>เจาะลึก JSX (JavaScript XML)</h2><p>ในไฟล์ `App.jsx` คุณจะเห็นไวยากรณ์แปลกๆ ที่เหมือน HTML ไปผสมกับ JavaScript นี่คือ <strong>JSX</strong> อย่างที่เราได้เกริ่นไปในบทนำ JSX ทำให้การเขียน UI ใน React เป็นเรื่องง่ายและเป็นธรรมชาติ แต่ JSX มี <strong>กฎเหล็ก (Rules of JSX)</strong> ที่คุณต้องปฏิบัติตามอย่างเคร่งครัด:</p><ol style="padding-left:20px"><li><strong>ต้องมี Root Element เดียว (Return a single root element):</strong> Component ของ React สามารถคืนค่า (return) Element ได้เพียงก้อนเดียวเท่านั้น หากคุณมีหลาย Elements คุณต้องนำ Tag อื่นมาครอบมันไว้ เช่น <code>&lt;div&gt;</code> หรือ <strong>Fragment</strong> (<code>&lt;&gt; ... &lt;/&gt;</code>)</li><li><strong>ปิด Tag ให้ครบทุกครั้ง (Close all the tags):</strong> ใน HTML คุณอาจจะเขียน <code>&lt;img src="..."&gt;</code> หรือ <code>&lt;br&gt;</code> ทิ้งไว้ได้ แต่ใน JSX ทุก Tag ต้องมี Tag ปิด หรือใช้ Self-closing tag เสมอ เช่น <code>&lt;img src="..." /&gt;</code> หรือ <code>&lt;br /&gt;</code></li><li><strong>CamelCase สำหรับ Attributes (camelCase all most of the things):</strong> เนื่องจาก JSX คือ JavaScript และใน JS มีคำสงวน (Reserved words) อยู่ เช่น `class` หรือ `for` ดังนั้นเราจึงต้องใช้ <code>className</code> แทน <code>class</code> สำหรับ CSS และ <code>htmlFor</code> แทน <code>for</code> ใน label นอกจากนี้ Attributes แบบสองคำขึ้นไปต้องใช้ camelCase เช่น <code>onClick</code>, <code>tabIndex</code>, <code>style</code></li></ol><div class="tip-box"><strong>💡 Pro Tip:</strong> ทำไมถึงต้องมี Root Element เดียว? เพราะเบื้องหลังการทำงาน JSX จะถูกแปลงเป็นฟังก์ชัน `React.createElement(component, props, ...children)` ใน JavaScript ฟังก์ชันไม่สามารถ Return ค่าหลายๆ ตัวพร้อมกันโดยไม่จับมัดรวมกันใน Array หรือ Object ได้ ดังนั้นการมี Root Element เดียวจึงเป็นสิ่งจำเป็นเพื่อให้การ Return ค่าถูกต้องตามหลักภาษา JS</div><h2>การแทรก Expressions (JavaScript Expressions) ใน JSX</h2><p>ความสามารถที่ทรงพลังที่สุดของ JSX คือการดึงพลังของ JavaScript มาใช้ในการแสดงผล คุณสามารถแทรกตัวแปร, ฟังก์ชัน, หรือนิพจน์ใดๆ ของ JavaScript ลงใน JSX ได้โดยการใช้ <strong>วงเล็บปีกกา `{}` (Curly Braces)</strong></p><pre><code>function Welcome() {\n  const name = "John Doe";\n  const isProMember = true;\n\n  return (\n    &lt;div className="welcome-card"&gt;\n      &lt;h1&gt;Hello, {name.toUpperCase()}!&lt;/h1&gt;\n      &lt;p&gt;Status: {isProMember ? "Pro" : "Free"} User&lt;/p&gt;\n      &lt;p&gt;2 + 2 equals {2 + 2}&lt;/p&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p>ข้อควรระวังคือ สิ่งที่อยู่ใน `{}` ต้องเป็น <strong>Expression (นิพจน์ที่ให้ค่าออกมา)</strong> เท่านั้น คุณไม่สามารถใส่ Statement อย่าง `if-else` หรือ `for-loop` ไว้ใน `{}` โดยตรงได้ (นั่นเป็นเหตุผลที่เรามักใช้ Ternary Operator `condition ? true : false` แทน if-else ภายใน JSX)</p><h2>การแสดงรายการข้อมูล (Rendering Lists ด้วย map)</h2><p>การแสดงข้อมูลเป็นรายการ (List) เช่น รายการสินค้า หรือ รายการผู้ใช้งาน เป็นสิ่งที่พบได้บ่อยที่สุดในการทำเว็บ ใน React เราจะไม่ใช้ `for-loop` แบบดั้งเดิม แต่เราจะใช้ Method ของ Array ที่ชื่อว่า <strong><code>.map()</code></strong> เพื่อแปลง Array ของข้อมูล (Data) ให้กลายเป็น Array ของ JSX Elements</p><pre><code>function UserList() {\n  const users = [\n    { id: 1, name: "Alice" },\n    { id: 2, name: "Bob" },\n    { id: 3, name: "Charlie" }\n  ];\n\n  return (\n    &lt;ul&gt;\n      {users.map((user) =&gt; (\n        &lt;li key={user.id}&gt;{user.name}&lt;/li&gt;\n      ))}\n    &lt;/ul&gt;\n  );\n}</code></pre><p>สิ่งสำคัญอย่างหนึ่งที่คุณต้องไม่ลืมเมื่อใช้ `map` คือการกำหนด Prop ที่ชื่อว่า <strong><code>key</code></strong> ให้กับ Element ชั้นนอกสุดภายใน loop เสมอ `key` ต้องเป็นค่าที่ Unique (ไม่ซ้ำกัน) เช่น ID จากฐานข้อมูล React ใช้ `key` เพื่อติดตามว่ามี Element ไหนที่ถูกเพิ่ม ลบ หรือแก้ไขบ้าง เพื่อนำไปใช้กับกระบวนการ Virtual DOM Reconciliation ให้ทำงานได้อย่างมีประสิทธิภาพสูงสุด</p><p>ในบทเรียนนี้ เราได้เรียนรู้วิธีการ Set up โปรเจกต์ที่ทันสมัยด้วย Vite ทำความเข้าใจโครงสร้างเบื้องต้น และกฎเหล็กของ JSX ที่เป็นหัวใจของการแสดงผล ในบทต่อไป เราจะดำดิ่งลงไปในเรื่องของ Components และการส่งต่อข้อมูล (Props) เพื่อประกอบร่าง UI ของเราให้ซับซ้อนยิ่งขึ้น!</p>'
  },
  {
    number: 2,
    slug: 'components',
    emoji: '🧩',
    title: 'Components and Props: การประกอบร่าง UI',
    content: '<h2>ทำความรู้จักกับ Components</h2><p>ในบทก่อนหน้าเราได้พูดถึงสถาปัตยกรรม Component-based อย่างคร่าวๆ ไปแล้ว ในบทนี้เราจะมาเจาะลึกว่า Component ใน React สร้างขึ้นมาได้อย่างไร และทำงานอย่างไร ในอดีต React เคยมีการสร้าง Component ด้วย Class (Class Components) ซึ่งมีความซับซ้อนและเขียนยาก แต่ในปัจจุบัน (ตั้งแต่ React 16.8 เป็นต้นมา) มาตรฐานใหม่ของการเขียน React คือการใช้ <strong>Functional Components</strong> ร่วมกับ Hooks ซึ่งทั้งอ่านง่ายกว่า เขียนสั้นกว่า และมีประสิทธิภาพที่ดีเยี่ยม</p><h2>Functional Components คืออะไร?</h2><p>Functional Component อธิบายง่ายๆ คือ ฟังก์ชัน JavaScript ธรรมดาที่รับข้อมูลเข้ามา (เรียกว่า Props) และคืนค่า (return) กลับมาเป็น JSX Element เสมอ ลองดูตัวอย่างที่เรียบง่ายที่สุด:</p><pre><code>function Button() {\n  return &lt;button className="btn-primary"&gt;Click Me!&lt;/button&gt;;\n}</code></pre><p>ข้อควรจำ: <strong>ชื่อของ Component ต้องขึ้นต้นด้วยตัวพิมพ์ใหญ่เสมอ (PascalCase)</strong> เช่น `Button`, `UserProfile`, `Header` นี่เป็นกฎที่ React ใช้ในการแยกแยะว่าอะไรคือ HTML Tag ธรรมดา (เช่น `&lt;button&gt;`) และอะไรคือ React Component (เช่น `&lt;Button /&gt;`) หากคุณตั้งชื่อขึ้นต้นด้วยตัวพิมพ์เล็ก React จะมองว่ามันคือ HTML Tag และทำให้เกิด Error ทันที</p><h2>การใช้งาน Props (Properties)</h2><p>Component จะไม่มีประโยชน์มากนักหากมันแสดงผลเหมือนเดิมทุกครั้ง การทำให้ Component ยืดหยุ่นและนำไปใช้ซ้ำได้ (Reusable) เราต้องพึ่งพา <strong>Props</strong></p><p>Props ย่อมาจาก Properties ทำหน้าที่เสมือนพารามิเตอร์ (Parameters) ของฟังก์ชัน มันเป็นวิธีที่ Component แม่ (Parent Component) ใช้ในการส่งข้อมูลลงไปยัง Component ลูก (Child Component) การส่งข้อมูลนี้จะเป็นการส่งแบบ <strong>One-way Data Binding</strong> คือส่งจากบนลงล่างเท่านั้น (Top-down) ลูกไม่สามารถแก้ไข Props ที่แม่ส่งมาได้โดยตรง (Read-only)</p><pre><code>function Greeting(props) {\n  return &lt;h1&gt;Hello, {props.name}! Welcome to {props.country}.&lt;/h1&gt;;\n}\n\nfunction App() {\n  return (\n    &lt;div&gt;\n      &lt;Greeting name="Somchai" country="Thailand" /&gt;\n      &lt;Greeting name="John" country="USA" /&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p>ในตัวอย่างด้านบน `App` ส่ง `name` และ `country` ไปให้ `Greeting` ผ่านทาง HTML-like Attributes ภายใน `Greeting` เราสามารถรับข้อมูลนั้นผ่านตัวแปร `props` ซึ่งเป็น Object (ในที่นี้คือ `{ name: "...", country: "..." }`)</p><h2>Destructuring Props เพื่อความสวยงาม</h2><p>การเขียน `props.name`, `props.country` ไปเรื่อยๆ อาจจะทำให้โค้ดดูรกรุงรัง ใน JavaScript สมัยใหม่ (ES6) เรามีฟีเจอร์ที่เรียกว่า <strong>Destructuring Assignment</strong> ซึ่งนำมาใช้ร่วมกับ Props ได้อย่างลงตัว โดยการแกะค่า (Extract) ออกมาตั้งแต่ตอนรับ Parameter เลย:</p><pre><code>// แทนที่จะเขียน function Greeting(props)\nfunction Greeting({ name, country }) {\n  return &lt;h1&gt;Hello, {name}! Welcome to {country}.&lt;/h1&gt;;\n}</code></pre><p>การเขียนแบบนี้ทำให้เรารู้ได้ทันทีตั้งแต่บรรทัดแรกว่า Component นี้ต้องการรับข้อมูลอะไรบ้าง และทำให้โค้ดด้านในกระชับขึ้นอย่างเห็นได้ชัด</p><h2>Default Props (ค่าเริ่มต้น)</h2><p>บางครั้ง Component ลูกอาจจะไม่ได้รับ Props บางตัวจากแม่ หากเราต้องการป้องกันไม่ให้เกิดค่า `undefined` และแสดงผลผิดพลาด เราสามารถกำหนดค่าเริ่มต้น (Default Value) ให้กับ Props ได้เลยผ่าน Destructuring:</p><pre><code>function UserBadge({ username = "Guest", role = "Visitor" }) {\n  return (\n    &lt;div className="badge"&gt;\n      &lt;strong&gt;{username}&lt;/strong&gt; ({role})\n    &lt;/div&gt;\n  );\n}</code></pre><p>หาก `App` เรียกใช้ `&lt;UserBadge /&gt;` โดยไม่ส่ง Props อะไรมาเลย หน้าจอจะแสดงคำว่า "Guest (Visitor)" แทนที่จะว่างเปล่า</p><h2>ความลับของ Children Prop</h2><p>ใน HTML เรามักจะเขียน Tag ซ้อนกัน เช่น <code>&lt;div&gt;&lt;p&gt;Text&lt;/p&gt;&lt;/div&gt;</code> ใน React เราก็ทำแบบนั้นกับ Component ได้เช่นกัน! หากเราต้องการส่ง Element ใดๆ เข้าไปอยู่ตรงกลางของเปิด/ปิด Component (เช่น <code>&lt;Card&gt;...&lt;/Card&gt;</code>) สิ่งที่อยู่ตรงกลางนั้นจะถูกส่งไปเป็น Prop พิเศษที่ชื่อว่า <strong><code>children</code></strong></p><pre><code>function Card({ title, children }) {\n  return (\n    &lt;div className="card-container"&gt;\n      &lt;h2 className="card-title"&gt;{title}&lt;/h2&gt;\n      &lt;div className="card-content"&gt;\n        {children}\n      &lt;/div&gt;\n    &lt;/div&gt;\n  );\n}\n\nfunction App() {\n  return (\n    &lt;Card title="User Information"&gt;\n      &lt;img src="avatar.png" alt="Avatar" /&gt;\n      &lt;p&gt;Name: Somchai&lt;/p&gt;\n      &lt;button&gt;Edit Profile&lt;/button&gt;\n    &lt;/Card&gt;\n  );\n}</code></pre><div class="tip-box"><strong>💡 Pro Tip:</strong> `children` prop คือกุญแจสำคัญในการสร้าง Layout Components หรือ Wrapper Components (เช่น Modal, Layout, Container) มันช่วยให้คุณสร้างกรอบ (Frame) ที่มีพฤติกรรมหรือสไตล์เหมือนกัน โดยไม่จำเป็นต้องรู้ล่วงหน้าว่าเนื้อหาข้างใน (Content) จะเป็นอะไร ทำให้ Component ของคุณมีความยืดหยุ่นในระดับสูงสุด</div><p>ในบทนี้เราได้เรียนรู้วิธีการประกอบ UI ด้วย Functional Components และการส่งผ่านข้อมูลด้วย Props ซึ่งเป็นพื้นฐานสำคัญที่สุดของ React แต่ Props มีข้อจำกัดคือเป็นข้อมูลที่ Read-only และเปลี่ยนค่าไม่ได้ ในบทต่อไปเราจะมาทำความรู้จักกับ <strong>State</strong> สิ่งที่จะทำให้แอปของเรามีชีวิตและตอบสนองโต้ตอบกับผู้ใช้งานได้!</p>'
  },
  {
    number: 3,
    slug: 'usestate',
    emoji: '📦',
    title: 'State ด้วย useState Hook',
    content: '<h2>ทำไมเราถึงต้องมี State?</h2><p>หาก Props เปรียบเสมือน DNA ที่พ่อแม่ส่งมาให้ลูก (เปลี่ยนไม่ได้) <strong>State (สถานะ)</strong> ก็เปรียบเสมือนความทรงจำหรืออารมณ์ของตัว Component เอง ที่สามารถเปลี่ยนแปลงได้ตามกาลเวลาหรือตามการกระทำ (Action) ของผู้ใช้งาน Props ใช้สื่อสารระหว่าง Component ในขณะที่ State ใช้จัดการข้อมูลภายในตัวเอง (Local Data)</p><p>ทำไมเราถึงไม่ใช้ตัวแปรธรรมดาๆ ใน JavaScript (เช่น `let count = 0`) เพื่อเก็บค่าที่เปลี่ยนไป? คำตอบคือ React จะไม่ทราบเลยว่าตัวแปรนั้นเปลี่ยนค่าไปตอนไหน และ <strong>React จะไม่ทำการ Render หน้าจอใหม่ (Re-render)</strong> หากเราใช้ตัวแปรธรรมดา เราจึงต้องใช้เครื่องมือพิเศษของ React ที่เรียกว่า <strong>Hook</strong></p><h2>แนะนำ useState Hook</h2><p><strong>`useState`</strong> เป็น Hook ที่พื้นฐานที่สุดและถูกใช้บ่อยที่สุดใน React ทำหน้าที่ประกาศ State Variable (ตัวแปรสถานะ) ให้กับ Functional Component เมื่อใดก็ตามที่ State ตัวนี้ถูกอัปเดตค่า React จะทำการเรียกใช้ Component นั้นใหม่ (Re-render) และอัปเดตหน้าจอให้แสดงผลข้อมูลล่าสุดทันที</p><p>วิธีใช้งาน `useState` เริ่มจากการ import เข้ามาจาก react:</p><pre><code>import { useState } from "react";\n\nfunction Counter() {\n  const [count, setCount] = useState(0);\n\n  return (\n    &lt;div&gt;\n      &lt;p&gt;คุณคลิกไปแล้ว {count} ครั้ง&lt;/p&gt;\n      &lt;button onClick={() =&gt; setCount(count + 1)}&gt;\n        คลิกฉันสิ!\n      &lt;/button&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><h3>แกะรอยการทำงานของ useState</h3><ol style="padding-left:20px"><li>เราเรียก <code>useState(0)</code> โดย <code>0</code> คือ <strong>ค่าเริ่มต้น (Initial Value)</strong></li><li><code>useState</code> จะคืนค่ากลับมาเป็น Array ที่มีสมาชิก 2 ตัวเสมอ</li><li>เราใช้ Destructuring Array เพื่อแยกมันออกเป็น <code>[count, setCount]</code><ul><li><code>count</code>: คือตัวแปร State ตัวปัจจุบัน (ตอนนี้คือ 0)</li><li><code>setCount</code>: คือ <strong>ฟังก์ชัน (Setter Function)</strong> ที่ใช้สำหรับสั่งเปลี่ยนค่าของ <code>count</code> (เช่น setCount(1))</li></ul></li></ol><p>เมื่อผู้ใช้คลิกปุ่ม ฟังก์ชัน `setCount(count + 1)` จะถูกเรียก React จะนำค่าใหม่ (1) ไปอัปเดต และสั่ง Render Component `Counter` ใหม่ทั้งหมด ทันใดนั้น `{count}` บนหน้าจอก็จะเปลี่ยนจาก 0 เป็น 1</p><h2>กฎเหล็กของ State: Immutability (ห้ามแก้ค่าตรงๆ!)</h2><p>กฎที่สำคัญที่สุดในการจัดการ State ของ React คือ <strong>ห้ามเปลี่ยนค่าตัวแปร State โดยตรง (Do not mutate state directly)</strong></p><pre><code>// ❌ ผิดวิธี! React จะไม่รู้ว่าค่าถูกเปลี่ยน และจะไม่ Re-render\ncount = count + 1;\n\n// ✅ ถูกวิธี! ต้องเรียกผ่าน Setter Function เสมอ\nsetCount(count + 1);</code></pre><p>ปัญหานี้มักจะเกิดขึ้นบ่อยเมื่อ State ของเราเป็นข้อมูลประเภท Reference Type อย่าง <strong>Object</strong> หรือ <strong>Array</strong></p><h3>การอัปเดต State ที่เป็น Object</h3><p>สมมติว่าคุณมีฟอร์มสำหรับเก็บข้อมูลผู้ใช้งาน:</p><pre><code>const [user, setUser] = useState({ name: "John", age: 25 });</code></pre><p>หากคุณต้องการเปลี่ยนแค่ชื่อเป็น "Jane" คุณไม่สามารถทำแบบนี้ได้: <code>user.name = "Jane"; setUser(user);</code> เพราะ Memory Address ของ Object ไม่ได้เปลี่ยน React จะมองว่ามันคือ Object เดิม! วิธีที่ถูกต้องคือการ <strong>สร้าง Object ใหม่ขึ้นมา (Copy)</strong> โดยใช้ Spread Operator (<code>...</code>):</p><pre><code>setUser({ \n  ...user, \n  name: "Jane" \n});</code></pre><p>นี่หมายถึง "ก๊อปปี้ทุกอย่างจาก `user` ตัวเดิม (...user) แล้วเขียนทับเฉพาะ Property `name` ให้เป็น Jane"</p><h3>การอัปเดต State ที่เป็น Array</h3><p>เช่นเดียวกันกับ Array คุณไม่สามารถใช้ Method ที่แก้ไข Array เดิม (Mutative Methods) อย่าง <code>.push()</code> หรือ <code>.pop()</code> ได้ ให้ใช้ Method ที่คืนค่าเป็น Array ตัวใหม่แทน เช่น Spread, `map()`, หรือ `filter()`</p><pre><code>const [todos, setTodos] = useState(["Learn HTML", "Learn CSS"]);\n\n// การเพิ่ม Item ใหม่ (แทนการใช้ .push)\nsetTodos([...todos, "Learn React"]);\n\n// การลบ Item (ใช้ .filter)\nsetTodos(todos.filter(todo =&gt; todo !== "Learn CSS"));</code></pre><h2>Previous State Pattern (การอ้างอิงค่าเดิม)</h2><p>มีกรณีหนึ่งที่คุณต้องระวังเป็นพิเศษ คือเมื่อ State ใหม่ต้องคำนวณมาจาก State เดิม สมมติว่าคุณเรียก `setCount(count + 1)` ติดกัน 3 ครั้ง ภายในฟังก์ชันเดียว ผลลัพธ์อาจจะไม่ได้บวก 3 อย่างที่คุณคิด เนื่องจาก React มีกลไกการรวมการอัปเดต (Batch Updates) เพื่อประสิทธิภาพ</p><p>หาก State ใหม่ขึ้นอยู่กับค่าเดิม <strong>เสมอ</strong> ให้ใช้ Pattern การส่ง Callback Function เข้าไปใน Setter แทน:</p><pre><code>setCount((prevCount) =&gt; prevCount + 1);\nsetCount((prevCount) =&gt; prevCount + 1);\n// ผลลัพธ์ที่ได้จะอัปเดตถูกต้องแน่นอน เพราะ prevCount จะเป็นค่าล่าสุดในคิวเสมอ</code></pre><div class="tip-box"><strong>💡 Pro Tip:</strong> State เปรียบเสมือน Snapshot ของ UI ในแต่ละเฟรม หากหน้าจอคุณซับซ้อน ให้พยายามแยก State ออกเป็นหลายๆ ตัว (เช่น useState แยกกันสำหรับ name, age, email) มากกว่าจะรวมทุกอย่างเป็น Object ก้อนใหญ่ก้อนเดียว จะทำให้โค้ดจัดการง่ายกว่าและลดการ Re-render ในส่วนที่ไม่จำเป็นได้</div><p>ตอนนี้คุณมีพลังที่จะทำให้ Component เก็บข้อมูลที่เปลี่ยนแปลงได้แล้ว! ในบทถัดไป เราจะมาดูว่าผู้ใช้งานจะมีปฏิสัมพันธ์ (Interact) กับแอปพลิเคชันของเราได้อย่างไร ผ่านการจัดการเหตุการณ์ต่างๆ (Handling Events) เช่น การคลิกเมาส์ การพิมพ์คีย์บอร์ด หรือการส่งฟอร์ม</p>'
  },
  {
    number: 4,
    slug: 'events',
    emoji: '🖱️',
    title: 'Handling Events: ตอบสนองต่อผู้ใช้งาน',
    content: '<h2>Event Handling ใน React คืออะไร?</h2><p>แอปพลิเคชันจะไม่มีชีวิตชีวาเลยหากผู้ใช้ไม่สามารถโต้ตอบกับมันได้ ใน HTML ดั้งเดิมเราคุ้นเคยกับการดักจับเหตุการณ์ (Events) ด้วย attribute อย่าง <code>onclick</code> หรือ <code>onchange</code> ใน React ก็มีแนวคิดที่คล้ายกัน แต่มีข้อแตกต่างและกฎเกณฑ์บางอย่างที่ถูกออกแบบมาเพื่อให้ทำงานร่วมกับ JSX และ State ได้อย่างราบรื่นยิ่งขึ้น</p><p>ข้อแตกต่างหลักๆ มีอยู่ 2 ประการ:</p><ol style="padding-left:20px"><li><strong>CamelCase Naming:</strong> ชื่อ Event ใน React จะต้องใช้รูปแบบ camelCase เสมอ เช่น <code>onClick</code> แทนที่จะเป็น <code>onclick</code> หรือ <code>onChange</code> แทนที่จะเป็น <code>onchange</code></li><li><strong>Passing a Function (Not a String):</strong> ใน HTML เดิมเราจะส่งคำสั่งเป็น String เช่น <code>onclick="handleClick()"</code> แต่ใน React เราต้องส่งฟังก์ชัน (Function Reference) เข้าไปโดยใช้ปีกกา <code>{}</code> เช่น <code>onClick={handleClick}</code></li></ol><h2>การเพิ่ม Event Handler เบื้องต้น</h2><p>วิธีที่ง่ายที่สุดในการดักจับ Event คือการสร้างฟังก์ชัน (มักจะตั้งชื่อขึ้นต้นด้วย `handle` เช่น `handleClick`) และส่งฟังก์ชันนั้นไปให้กับ Event Prop ของ Element</p><pre><code>function SubmitButton() {\n  function handleClick() {\n    alert("Button was clicked!");\n  }\n\n  return (\n    // สังเกตว่าเราไม่ใส่ () หลัง handleClick\n    &lt;button onClick={handleClick}&gt;\n      Submit Form\n    &lt;/button&gt;\n  );\n}</code></pre><p><strong>ข้อควรระวังขั้นเด็ดขาด:</strong> การส่งฟังก์ชันไปยัง Event Handler <strong>ห้ามใส่เครื่องหมายวงเล็บ () เด็ดขาด</strong> (เขียน <code>onClick={handleClick}</code> ไม่ใช่ <code>onClick={handleClick()}</code>) เพราะการใส่ `()` จะทำให้ฟังก์ชันนั้น <strong>ถูกเรียกใช้งานทันที</strong> ในขณะที่ React กำลัง Render หน้าจอ ไม่ใช่ตอนที่ผู้ใช้คลิกจริงๆ!</p><h2>Event Object (e)</h2><p>เมื่อมีเหตุการณ์เกิดขึ้น (เช่น การคลิก หรือการพิมพ์) React จะสร้าง Event Object ขึ้นมา (เรียกว่า SyntheticEvent ซึ่งคล้ายกับ Event ธรรมดาของ Browser แต่ถูกปรับปรุงให้ทำงานเหมือนกันในทุก Browser) และส่งมันเป็นพารามิเตอร์ตัวแรกให้กับฟังก์ชัน Handler ของเรา เรามักจะใช้ตัวย่อว่า <code>e</code> หรือ <code>event</code></p><pre><code>function SearchInput() {\n  function handleChange(e) {\n    // e.target คือ Element ที่เกิด Event (ในที่นี้คือ &lt;input&gt;)\n    // e.target.value คือ ค่าที่ผู้ใช้พิมพ์เข้ามา\n    console.log("คุณพิมพ์ว่า: " + e.target.value);\n  }\n\n  return &lt;input type="text" onChange={handleChange} placeholder="Search..." /&gt;;\n}</code></pre><p>การใช้ `onChange` และดึงค่าจาก `e.target.value` เป็นหัวใจสำคัญในการทำ Form และ Controlled Components ซึ่งเราจะเจาะลึกกันในบทต่อๆ ไป</p><h2>Prevent Default Behaviour (ป้องกันพฤติกรรมดั้งเดิม)</h2><p>ใน Browser บาง HTML Elements จะมีพฤติกรรมดั้งเดิม (Default Behaviour) ฝังมาด้วย เช่น เมื่อเราคลิกปุ่ม `<button>` ภายใน `<form>` Browser จะพยายามรีเฟรชหน้าเว็บเพื่อส่งข้อมูลไปที่ Server เสมอ ซึ่งนี่ขัดกับหลักการของ Single Page Application (SPA) ที่เราไม่ต้องการให้หน้ารีเฟรช</p><p>เราสามารถหยุดพฤติกรรมนี้ได้โดยการเรียกใช้ method <strong><code>e.preventDefault()</code></strong></p><pre><code>function LoginForm() {\n  function handleSubmit(e) {\n    e.preventDefault(); // หยุดการรีเฟรชหน้าจอ\n    console.log("Form submitted via React!");\n    // หลังจากนี้ค่อยเขียนโค้ดส่งข้อมูลไป Backend ด้วย Axios หรือ Fetch\n  }\n\n  return (\n    &lt;form onSubmit={handleSubmit}&gt;\n      &lt;input type="text" name="username" /&gt;\n      &lt;button type="submit"&gt;Login&lt;/button&gt;\n    &lt;/form&gt;\n  );\n}</code></pre><h2>การส่ง Arguments (ค่าพารามิเตอร์) ไปยัง Event Handler</h2><p>บ่อยครั้งที่เรามีปุ่มหลายๆ ปุ่มที่ใช้ Handler ตัวเดียวกัน (เช่น รายการสินค้าที่มีปุ่มลบ (Delete) ทุกบรรทัด) เราจำเป็นต้องรู้ว่าผู้ใช้คลิกลบที่รายการไหน เราจะส่ง ID (Argument) ไปให้ `handleDelete` ได้อย่างไร? ในเมื่อถ้าเราใส่ `handleDelete(id)` มันจะถูกเรียกทันที?</p><p>วิธีแก้ปัญหาคือการใช้ <strong>Inline Arrow Function</strong>:</p><pre><code>function ShoppingList({ items }) {\n  function handleDelete(itemId) {\n    console.log("ต้องการลบ Item ID:", itemId);\n  }\n\n  return (\n    &lt;ul&gt;\n      {items.map((item) =&gt; (\n        &lt;li key={item.id}&gt;\n          {item.name} \n          {/* สร้างฟังก์ชันใหม่มารับ Event ก่อน แล้วค่อยเรียก handleDelete พร้อมส่ง ID */}\n          &lt;button onClick={() =&gt; handleDelete(item.id)}&gt;Delete&lt;/button&gt;\n        &lt;/li&gt;\n      ))}\n    &lt;/ul&gt;\n  );\n}</code></pre><div class="tip-box"><strong>💡 Pro Tip:</strong> แม้การใช้ Inline Arrow Function <code>() =&gt; handler(id)</code> จะสะดวกและนิยมมาก แต่ในบางกรณีที่มี Component จำนวนมหาศาล (เป็นหมื่นๆ ชิ้น) การสร้างฟังก์ชันใหม่ในทุกๆ ครั้งที่ Render อาจส่งผลกระทบต่อ Performance ได้ (แม้จะน้อยนิดมากในเครื่องสมัยใหม่) ในระดับสูง เราอาจจะพิจารณาใช้ Data Attributes (เช่น <code>data-id={item.id}</code>) แล้วดึงค่าจาก <code>e.currentTarget.dataset.id</code> แทน</div><p>ในบทนี้เราได้รู้วิธีดักจับพฤติกรรมของผู้ใช้แล้ว ซึ่งเมื่อนำไปรวมกับ <strong>State</strong> (จากบทที่ 3) คุณก็สามารถสร้าง Interactive UI ได้อย่างสมบูรณ์แบบ เช่น กดปุ่มเพื่อเปลี่ยนค่า State และอัปเดตหน้าจอ ในบทต่อไป เราจะพูดถึง Hook อีกตัวหนึ่งที่สำคัญไม่แพ้ useState นั่นคือ <strong>useEffect</strong> ซึ่งจะช่วยให้เราจัดการกับเหตุการณ์ที่ไม่เกี่ยวข้องกับผู้ใช้ (Side Effects) ได้</p>'
  },
  {
    number: 5,
    slug: 'useeffect',
    emoji: '🔄',
    title: 'useEffect & Lifecycle: วงจรชีวิตและผลข้างเคียง',
    content: '<h2>Component Lifecycle (วงจรชีวิตของ Component)</h2><p>เช่นเดียวกับสิ่งมีชีวิต React Component มี "วงจรชีวิต" ของมันเอง ตั้งแต่เกิดจนดับ ลองจินตนาการถึงหน้า Profile ผู้ใช้งาน:</p><ol style="padding-left:20px"><li><strong>Mounting (เกิด):</strong> เมื่อ Component ถูกนำมาแสดงบนหน้าจอครั้งแรก (ตัวอย่าง: เข้าหน้า Profile เป็นครั้งแรก)</li><li><strong>Updating (เติบโต/เปลี่ยนแปลง):</strong> เมื่อข้อมูล (State/Props)ภายใน Component เปลี่ยนไป ทำให้ต้อง Render ใหม่ (ตัวอย่าง: กดปุ่ม Edit ชื่อ แล้วบันทึก)</li><li><strong>Unmounting (ดับ):</strong> เมื่อ Component ถูกถอดออกจากหน้าจอ (ตัวอย่าง: ผู้ใช้คลิกเมนูเพื่อเปลี่ยนไปหน้าอื่น)</li></ol><p>ในอดีต (ยุค Class Component) เราต้องแยกเขียนโค้ดสำหรับแต่ละช่วงชีวิต (เช่น componentDidMount, componentWillUnmount) แต่ในยุคปัจจุบันของ Functional Component เรามีพระเอกเพียงคนเดียวที่จัดการได้ทุกช่วงชีวิต นั่นคือ <strong>`useEffect`</strong></p><h2>Side Effects คืออะไร?</h2><p>React ถูกออกแบบมาให้เป็น "Pure" ในเรื่องของการแสดงผล หมายความว่า ถ้ารับ Props หรือ State เดิมเข้ามา ผลลัพธ์ (JSX) ที่ Return ออกไปก็ต้องหน้าตาเหมือนเดิมเป๊ะ และต้องไม่มีผลกระทบต่อโลกภายนอก (Side Effects)</p><p>แต่ในโลกความเป็นจริง แอปของเราต้องติดต่อกับโลกภายนอกเสมอ เช่น:<br/>- การดึงข้อมูล (Fetch API) จาก Server หรือ Database<br/>- การเชื่อมต่อ WebSockets หรือ Chat<br/>- การจับเวลา (setTimeout / setInterval)<br/>- การแก้ไข DOM โดยตรง (เช่น การเปลี่ยน Title บน Tab Browser)</p><p>พฤติกรรมเหล่านี้แหละที่เรียกว่า <strong>Side Effects</strong> และสถานที่เดียวที่ React อนุญาตให้เราทำเรื่องเหล่านี้ได้คือ ภายใน <strong>`useEffect` Hook</strong> เท่านั้น</p><h2>โครงสร้างและ Syntax ของ useEffect</h2><pre><code>import { useEffect } from "react";\n\nuseEffect(() =&gt; {\n  // โค้ดที่จะให้ทำงาน (The Effect)\n  \n  return () =&gt; {\n    // (Optional) โค้ดสำหรับทำความสะอาด (The Cleanup)\n  };\n}, [dependencies]); // (Optional) Dependency Array</code></pre><p>`useEffect` รับพารามิเตอร์ 2 ตัว:<br/>1. <strong>Effect Function:</strong> ฟังก์ชันที่จะทำงานหลังจากการ Render เสร็จสิ้น<br/>2. <strong>Dependency Array (อาร์เรย์ของตัวแปรที่พึ่งพา):</strong> สิ่งนี้คือหัวใจสำคัญที่สุดในการควบคุม <em>"เมื่อไหร่"</em> ที่ Effect นี้จะทำงาน</p><h2>เวทมนตร์ของ Dependency Array ( [ ] )</h2><p>การใส่ (หรือไม่ใส่) Dependency Array จะเปลี่ยนพฤติกรรมของ `useEffect` ไปอย่างสิ้นเชิง! มาดูกรณีทั้ง 3 แบบกัน:</p><h3>1. ไม่ใส่ Dependency Array เลย (ทำงานทุกครั้งที่ Re-render)</h3><pre><code>useEffect(() =&gt; {\n  console.log("ฉันทำงานทุกครั้งที่มีการ Render หรืออัปเดต State ใดๆ");\n});</code></pre><p>แบบนี้ไม่ค่อยนิยมใช้ เพราะมันอาจทำให้เกิด Infinite Loop ได้ง่ายหากมีการ Update State ภายใน Effect</p><h3>2. ใส่ Array ว่าง <code>[]</code> (ทำงานครั้งเดียวตอน Mounting)</h3><pre><code>useEffect(() =&gt; {\n  console.log("ฉันจะทำงานแค่ \'ครั้งแรก\' ที่ Component โผล่มาบนหน้าจอเท่านั้น!");\n  // เหมาะสำหรับการ Fetch ข้อมูลเริ่มต้น (Initial Data Fetching)\n}, []);</code></pre><p>แบบนี้เปรียบได้กับ <code>componentDidMount</code> นิยมใช้มากที่สุดสำหรับการดึงข้อมูล (API Fetch) เมื่อเข้าหน้าเว็บครั้งแรก</p><h3>3. ใส่ตัวแปรใน Array <code>[state1, prop2]</code> (ทำงานเมื่อค่าตัวแปรเหล่านี้เปลี่ยน)</h3><pre><code>const [searchTerm, setSearchTerm] = useState("");\n\nuseEffect(() =&gt; {\n  console.log("ค่า Search เปลี่ยนไปแล้ว! เริ่มดึงข้อมูลใหม่สำหรับคำว่า:", searchTerm);\n  // โค้ดนี้จะรันครั้งแรก และรันอีกครั้งเมื่อตัวแปร searchTerm ถูกเปลี่ยนค่า\n}, [searchTerm]);</code></pre><p>แบบนี้ React จะเปรียบเทียบค่า `searchTerm` รอบก่อนหน้า กับรอบปัจจุบัน ถ้าค่าต่างกัน มันถึงจะยอมสั่งให้ Effect ข้างในทำงาน (เป็นวิธีที่ฉลาดมากในการประหยัดทรัพยากร!)</p><h2>Cleanup Function (การทำความสะอาดก่อนดับ)</h2><p>บาง Side Effects เมื่อเราเริ่มมันแล้ว มันจะทำงานค้างไว้ในระบบ เช่น <code>setInterval</code> หรือ Event Listener ถ้าเราไม่ทำความสะอาดมันตอนที่ Component ดับ (Unmount) มันจะสร้างภาระให้ Memory (Memory Leak) และทำให้แอปพังได้</p><p>เราสามารถทำความสะอาดได้โดยการ <strong>`return` ฟังก์ชัน</strong> ออกมาจาก Effect ฟังก์ชันที่ Return ออกมานี้แหละที่เรียกว่า <strong>Cleanup Function</strong></p><pre><code>useEffect(() =&gt; {\n  console.log("Component Mounted - ติดตั้ง Event Listener");\n  const handleScroll = () =&gt; console.log("กำลังเลื่อนหน้าจอ...");\n  window.addEventListener("scroll", handleScroll);\n\n  // นี่คือ Cleanup Function\n  return () =&gt; {\n    console.log("Component กำลังจะ Unmount - ถอด Event Listener ออก!");\n    window.removeEventListener("scroll", handleScroll);\n  };\n}, []);</code></pre><p>React จะเรียก Cleanup Function นี้ 2 กรณี:<br/>1. ก่อนที่ Component จะถูกทำลาย (Unmount) อย่างถาวร<br/>2. ก่อนที่จะรัน Effect ในรอบถัดไป (กรณีที่มี Dependency) เพื่อเคลียร์ของเก่าก่อนรันของใหม่</p><div class="tip-box"><strong>💡 Pro Tip:</strong> กฎทองของการใช้ useEffect (The Golden Rule) คือ: <strong>ตัวแปรใดๆ (State หรือ Props) ที่ถูกใช้งาน "ภายใน" Effect จะต้องถูกนำมาใส่ใน Dependency Array เสมอ</strong> หากคุณละเมิดกฎนี้ คุณอาจเจอบั๊กแปลกๆ ที่เรียกว่า "Stale State" หรือ Effect อ่านค่าเก่าที่ไม่อัปเดตได้ (Linter ของ React จะแจ้งเตือนคุณเสมอหากคุณลืมใส่)</div><p>การเข้าใจ useEffect อย่างถ่องแท้เป็นสิ่งที่แบ่งแยกระหว่าง React Developer ระดับเริ่มต้นและระดับกลาง (Mid-level) ในบทต่อไป เราจะนำ useEffect มาประยุกต์ใช้กับงานที่เจอบ่อยที่สุดในการทำเว็บ นั่นคือการ "ดึงข้อมูลจาก Server" (Data Fetching)</p>'
  },
  {
    number: 6,
    slug: 'fetching',
    emoji: '🌐',
    title: 'Data Fetching: การดึงข้อมูลจาก API',
    content: '<h2>ชีวิตที่ขาดข้อมูลไม่ได้</h2><p>เว็บแอปพลิเคชันส่วนใหญ่บนโลกใบนี้ ล้วนแต่ต้องดึงข้อมูลมาจากแหล่งอื่น (Database หรือ API Server) เพราะการเก็บข้อมูลแบบ Hardcode ไว้ในโค้ดนั้นไม่มีทางสเกลได้ ใน React เรามักจะทำการดึงข้อมูลเหล่านี้ทันทีที่ Component โหลดขึ้นมา (Mount) หรือเมื่อผู้ใช้กระทำการบางอย่าง (เช่น กดปุ่มค้นหา) และอย่างที่เราเรียนไปในบทก่อนหน้า สถานที่ที่ถูกต้องที่สุดสำหรับการทำ Data Fetching ตอนเริ่มโหลดหน้าเว็บก็คือ <strong>`useEffect`</strong></p><h2>โครงสร้างพื้นฐานของการ Fetch ข้อมูล</h2><p>เมื่อพูดถึงการดึงข้อมูล เรามักจะมี <strong>State อย่างน้อย 3 ตัว</strong> ที่ต้องจัดการควบคู่กันไปเสมอ (Pattern นี้สำคัญมากและคุณจะได้ใช้ตลอดชีวิตการเป็น Developer):</p><ol style="padding-left:20px"><li><strong><code>data</code>:</strong> สำหรับเก็บข้อมูลจริงๆ ที่ได้รับมาจาก API</li><li><strong><code>isLoading</code>:</strong> (Boolean) สำหรับบอกว่า ตอนนี้กำลังโหลดข้อมูลอยู่หรือเปล่า (เพื่อนำไปโชว์ Loading Spinner ⏳)</li><li><strong><code>error</code>:</strong> สำหรับเก็บข้อความแจ้งเตือน เผื่อในกรณีที่ระบบล่ม เน็ตหลุด หรือ API ตอบกลับมาเป็น Error</li></ol><h3>ตัวอย่างการใช้ `fetch` API ดั้งเดิม (Native Fetch)</h3><pre><code>import { useState, useEffect } from "react";\n\nfunction UserList() {\n  const [users, setUsers] = useState([]);\n  const [isLoading, setIsLoading] = useState(true);\n  const [error, setError] = useState(null);\n\n  useEffect(() =&gt; {\n    // 1. เริ่มโหลดข้อมูล (แสดง Loading)\n    setIsLoading(true);\n\n    fetch("https://jsonplaceholder.typicode.com/users")\n      .then((response) =&gt; {\n        // เช็คว่า response ตอบกลับมารหัส 2xx (Success) หรือไม่\n        if (!response.ok) {\n          throw new Error("Network response was not ok");\n        }\n        return response.json(); // แปลงเป็น JSON\n      })\n      .then((data) =&gt; {\n        // 2. ได้ข้อมูลแล้ว บันทึกลง State และปิด Loading\n        setUsers(data);\n        setIsLoading(false);\n      })\n      .catch((err) =&gt; {\n        // 3. เกิดข้อผิดพลาด บันทึก Error และปิด Loading\n        setError(err.message);\n        setIsLoading(false);\n      });\n  }, []); // [] = ทำงานแค่ตอนโหลดหน้าครั้งแรก\n\n  // ส่วนของการ Render (UI)\n  if (isLoading) return &lt;div&gt;⏳ กำลังโหลดข้อมูล...&lt;/div&gt;;\n  if (error) return &lt;div&gt;❌ เกิดข้อผิดพลาด: {error}&lt;/div&gt;;\n\n  return (\n    &lt;ul&gt;\n      {users.map(user =&gt; &lt;li key={user.id}&gt;{user.name}&lt;/li&gt;)}\n    &lt;/ul&gt;\n  );\n}</code></pre><h2>Fetch vs Axios: เลือกใช้อะไรดี?</h2><p>ในตัวอย่างด้านบน เราใช้ `fetch` ซึ่งมีมาให้ใน Browser ทุกตัว (Native) แต่คุณจะสังเกตเห็นว่ามันมีข้อเสียคือ: 1) ต้องเรียก `.json()` ก่อน 1 รอบ 2) เราต้องเช็ค `response.ok` เอง เพราะ `fetch` จะไม่เข้าบล็อก `.catch()` แม้ว่า Server จะตอบกลับมาเป็น 404 (Not Found) หรือ 500 (Server Error) ก็ตาม (มันจะเข้า catch ต่อเมื่อเน็ตพังเท่านั้น)</p><p>ด้วยเหตุนี้ บริษัทและโปรเจกต์ส่วนใหญ่จึงนิยมใช้ Library อย่าง <strong>Axios</strong> มากกว่า</p><h3>มาลองเขียนด้วย Axios และ Async/Await กัน</h3><p>คุณต้องติดตั้งก่อน: <code>npm install axios</code> จากนั้นเราสามารถใช้ <code>async / await</code> (ซึ่งอ่านง่ายกว่า <code>.then()</code> มาก) ได้ดังนี้:</p><pre><code>import { useState, useEffect } from "react";\nimport axios from "axios";\n\nfunction UserListAxios() {\n  // State 3 ทหารเสือเหมือนเดิม\n  const [users, setUsers] = useState([]);\n  const [isLoading, setIsLoading] = useState(true);\n  const [error, setError] = useState(null);\n\n  useEffect(() =&gt; {\n    // สร้าง Async Function ภายใน useEffect\n    const fetchUsers = async () =&gt; {\n      try {\n        // เริ่มโหลด\n        setIsLoading(true);\n        \n        // เรียก API (Axios จัดการแปลง JSON และเช็ค Error HTTP ให้เรียบร้อย)\n        const response = await axios.get("https://jsonplaceholder.typicode.com/users");\n        \n        // Axios เอาข้อมูลจริงใส่ไว้ใน response.data เสมอ\n        setUsers(response.data);\n      } catch (err) {\n        // จัดการ Error\n        setError(err.message);\n      } finally {\n        // ไม่ว่าจะพังหรือผ่าน ต้องปิด Loading เสมอ!\n        setIsLoading(false);\n      }\n    };\n\n    // เรียกใช้\n    fetchUsers();\n  }, []);\n\n  // Render ย่อแบบสั้นๆ (Early Return)\n  if (isLoading) return &lt;h2&gt;Loading...&lt;/h2&gt;;\n  if (error) return &lt;h2&gt;Error: {error}&lt;/h2&gt;;\n  return &lt;ul&gt;{users.map(u =&gt; &lt;li key={u.id}&gt;{u.name}&lt;/li&gt;)}&lt;/ul&gt;;\n}</code></pre><p>สังเกตว่าเรา <strong>ไม่สามารถใส่ `async` ไปที่ Arrow Function ของ `useEffect` ตรงๆ ได้</strong> (<code>useEffect(async () =&gt; {...})</code> ❌) เพราะ useEffect คาดหวังให้คุณ Return <em>"Cleanup Function" (หรือไม่มีเลย)</em> แต่ `async function` จะ Return ออกมาเป็น `Promise` เสมอ ทำให้ผิดกฎของ React! วิธีที่ถูกต้องคือการ <strong>สร้าง Async Function ไว้ข้างใน แล้วเรียกใช้ทันที</strong> อย่างในตัวอย่างด้านบนนั่นเอง</p><div class="tip-box"><strong>💡 Pro Tip:</strong> ในการทำงานสเกลใหญ่ (Production) การเขียน useEffect + Axios เองอาจจะเหนื่อยเพราะต้องจัดการ State ถึง 3 ตัวทุกครั้ง ปัจจุบันจึงมี Library เกิดมาเพื่อแก้ปัญหานี้โดยเฉพาะอย่าง <strong>React Query (TanStack Query)</strong> หรือ <strong>SWR</strong> ซึ่งมันจะจัดการเรื่อง Loading, Error, Caching (เก็บข้อมูลสำรองไว้ในเครื่อง), และ Refetching ให้คุณโดยอัตโนมัติ! แนะนำอย่างยิ่งให้ศึกษา React Query หลังจากจบคอร์สนี้</div><h2>สรุป</h2><p>การดึงข้อมูลเป็นศาสตร์ที่คุณต้องใช้บ่อยที่สุด จำ Pattern `data, isLoading, error` เอาไว้ให้ขึ้นใจ ในบทต่อไป เราจะเปลี่ยนจากการรับข้อมูล (Get) มาเป็นการส่งข้อมูลกลับไปยังระบบ (Post) ด้วยการจัดการ <strong>ฟอร์ม (Forms)</strong> ใน React กันบ้าง!</p>'
  },
  {
    number: 7,
    slug: 'forms',
    emoji: '📝',
    title: 'Forms in React: ควบคุมแบบฟอร์มให้อยู่หมัด',
    content: '<h2>โลกสองใบของแบบฟอร์ม</h2><p>ฟอร์ม (Forms) เป็นช่องทางหลักที่ผู้ใช้ใช้ป้อนข้อมูลให้กับแอปพลิเคชัน (เช่น ล็อกอิน, สมัครสมาชิก, กรอกที่อยู่) ใน HTML ทั่วไป ฟอร์มและช่องกรอกข้อมูลต่างๆ (เช่น <code>&lt;input&gt;</code>, <code>&lt;textarea&gt;</code>, <code>&lt;select&gt;</code>) จะเก็บ "สถานะของตัวมันเอง" ไว้ใน DOM ของ Browser โดยตรง (เช่น สิ่งที่เราพิมพ์ลงไปในช่อง text)</p><p>แต่ใน React เรามีแนวคิดเรื่อง <strong>State</strong> (จากบทที่ 3) เป็นหัวใจสำคัญของข้อมูลทั้งหมด ซึ่งมันขัดแย้งกัน! React บอกว่า "ฉันต้องเป็นคนคุมข้อมูลทั้งหมดสิ" ในขณะที่ HTML Input บอกว่า "ฉันก็เก็บข้อมูลของฉันเองได้นะ" ด้วยเหตุนี้ ใน React จึงแบ่งวิธีจัดการฟอร์มออกเป็น 2 สำนักหลักๆ คือ <strong>Controlled Components</strong> และ <strong>Uncontrolled Components</strong></p><h2>1. Controlled Components (ฟอร์มที่ถูกควบคุม 100%)</h2><p>นี่คือ <strong>"วิธีที่เป็นมาตรฐาน (React Way)"</strong> ในวิธีนี้ React State จะเป็นแหล่งความจริงเพียงหนึ่งเดียว (Single Source of Truth) ของฟอร์ม ข้อมูลใน `&lt;input&gt;` จะมาจาก State และเมื่อผู้ใช้พิมพ์ ค่าใน `&lt;input&gt;` จะไม่เปลี่ยนจนกว่า State จะถูกอัปเดต (แม้จะเร็วระดับมิลลิวินาทีก็ตาม)</p><h3>การสร้าง Controlled Input</h3><pre><code>import { useState } from "react";\n\nfunction SimpleForm() {\n  const [username, setUsername] = useState("");\n\n  const handleSubmit = (e) =&gt; {\n    e.preventDefault(); // กันหน้ารีเฟรช\n    console.log("ส่งข้อมูล:", username);\n  };\n\n  return (\n    &lt;form onSubmit={handleSubmit}&gt;\n      &lt;label&gt;ชื่อผู้ใช้:&lt;/label&gt;\n      &lt;input \n        type="text" \n        // 1. ควบคุมค่าที่จะแสดงด้วย State\n        value={username} \n        // 2. เมื่อผู้ใช้พิมพ์ ให้อัปเดต State (e.target.value คือตัวอักษรล่าสุด)\n        onChange={(e) =&gt; setUsername(e.target.value)} \n      /&gt;\n      &lt;button type="submit"&gt;ส่งข้อมูล&lt;/button&gt;\n    &lt;/form&gt;\n  );\n}</code></pre><p><strong>ข้อดีสุดยอดของ Controlled Form:</strong><br/>- <strong>Real-time Validation (ตรวจสอบทันที):</strong> คุณสามารถเช็คได้ทันทีขณะที่ผู้ใช้กำลังพิมพ์ เช่น ถ้า `username.length &lt; 5` ให้ขึ้นกรอบแดงทันที!<br/>- <strong>Format on the fly (จัดรูปแบบขณะพิมพ์):</strong> เช่น เปลี่ยนตัวพิมพ์เล็กเป็นพิมพ์ใหญ่ทั้งหมด (<code>setUsername(e.target.value.toUpperCase())</code>) หรือการใส่ขีดกลาง (Dash) อัตโนมัติในเบอร์โทรศัพท์</p><h2>การจัดการฟอร์มที่มีหลาย Input (Multiple Inputs)</h2><p>ถ้าฟอร์มของคุณมีช่องกรอกข้อมูล 10 ช่อง การสร้าง useState 10 ตัวคงไม่สนุกแน่ (<code>setName</code>, <code>setEmail</code>, <code>setPassword</code>...) วิธีที่ดีกว่าคือการเก็บ State รวมกันเป็น <strong>Object ก้อนเดียว</strong></p><pre><code>function RegisterForm() {\n  const [formData, setFormData] = useState({\n    firstName: "",\n    lastName: "",\n    email: ""\n  });\n\n  const handleChange = (e) =&gt; {\n    // เทคนิคสำคัญ: ดึง name (ชื่อฟิลด์) และ value (ค่าที่พิมพ์) จาก input\n    const { name, value } = e.target;\n\n    // อัปเดต State แบบ Object (ทบทวนบทที่ 3)\n    setFormData(prevData =&gt; ({\n      ...prevData, // ก๊อปปี้ข้อมูลเก่า\n      [name]: value // อัปเดตเฉพาะฟิลด์ที่มีชื่อตรงกับ attribute name ของ input\n    }));\n  };\n\n  return (\n    &lt;form&gt;\n      &lt;input \n        name="firstName" // ชื่อนี้ต้องตรงกับ Key ใน Object formData!\n        value={formData.firstName}\n        onChange={handleChange}\n        placeholder="ชื่อจริง"\n      /&gt;\n      &lt;input \n        name="lastName"\n        value={formData.lastName}\n        onChange={handleChange}\n        placeholder="นามสกุล"\n      /&gt;\n      &lt;input \n        name="email"\n        value={formData.email}\n        onChange={handleChange}\n        placeholder="อีเมล"\n      /&gt;\n    &lt;/form&gt;\n  );\n}</code></pre><p>เทคนิค <code>[name]: value</code> (Computed Property Names ของ JavaScript ES6) เป็นไม้ตายที่ช่วยให้คุณจัดการฟอร์มได้เป็นสิบๆ ช่องด้วยฟังก์ชัน <code>handleChange</code> เพียงแค่ตัวเดียว!</p><h2>2. Uncontrolled Components (ปล่อยให้ DOM จัดการ)</h2><p>ถ้าคุณคิดว่าการสร้าง State และ onChange มันเสียเวลา หรือคุณมีฟอร์มมหาศาลที่คุณไม่ได้แคร์เรื่อง Validation แบบ Real-time คุณสามารถปล่อยให้ Browser จัดการข้อมูลเองเหมือน HTML ปกติ แล้วใช้ <strong>`useRef` Hook</strong> เพื่อดึงข้อมูลออกมาเฉพาะ "ตอนที่จะส่ง (Submit)" เท่านั้น</p><pre><code>import { useRef } from "react";\n\nfunction UncontrolledForm() {\n  // สร้าง Ref (ตัวแปรอ้างอิงไปถึงกล่อง input ในหน้าจอ)\n  const emailRef = useRef(null);\n  const passwordRef = useRef(null);\n\n  const handleSubmit = (e) =&gt; {\n    e.preventDefault();\n    // ดึงค่าโดยตรงจาก DOM (DOM Element อยู่ใน .current)\n    console.log("Email:", emailRef.current.value);\n    console.log("Password:", passwordRef.current.value);\n  };\n\n  return (\n    &lt;form onSubmit={handleSubmit}&gt;\n      {/* ไม่ต้องมี value หรือ onChange! */}\n      &lt;input type="email" ref={emailRef} placeholder="Email" /&gt;\n      &lt;input type="password" ref={passwordRef} placeholder="Password" /&gt;\n      &lt;button type="submit"&gt;Login&lt;/button&gt;\n    &lt;/form&gt;\n  );\n}</code></pre><div class="tip-box"><strong>💡 Pro Tip:</strong> แม้ว่าการเขียนแบบ Controlled (วิธีที่ 1) จะเป็นวิธีมาตรฐานและทรงพลังที่สุด แต่การจัดการฟอร์มใน React ด้วยตัวเองมักจะนำไปสู่โค้ดที่รกรุงรัง (Boilerplate) มหาศาลเมื่อแอปใหญ่ขึ้น (ไหนจะต้องดัก Error Message ไหนจะต้องเช็คว่าผู้ใช้จิ้มช่องนี้หรือยัง (Touched) ไหนจะเรื่อง Performance หากฟอร์มใหญ่มากๆ) ในการทำงานจริงเกือบทุกโปรเจกต์ <strong>เราจะไม่เขียนฟอร์มเองทั้งหมด</strong> แต่จะใช้ Library ตัวท็อปของวงการอย่าง <strong>React Hook Form</strong> หรือ <strong>Formik</strong> ร่วมกับ <strong>Yup/Zod</strong> สำหรับ Validation ซึ่งทำงานแบบ Uncontrolled เป็นหลัก ทำให้เร็วปรู๊ดปร๊าดและโค้ดสะอาดสุดๆ</div><p>ในบทนี้เราสามารถจัดการข้อมูลเข้าสู่ระบบได้แล้ว ตอนนี้แอปของคุณเริ่มมีความซับซ้อนขึ้น ในบทต่อไปเราจะมาแก้ปัญหา "การส่งต่อข้อมูลแบบมาราธอน" ข้ามผ่าน Component นับสิบๆ ชั้น ด้วยระบบศูนย์กลางที่เรียกว่า <strong>Context API</strong></p>'
  },
  {
    number: 8,
    slug: 'context',
    emoji: '🌐',
    title: 'Context API: จัดการ State ระดับสากล (Global State)',
    content: '<h2>ปัญหาของการส่งต่อ Prop (Prop Drilling Problem)</h2><p>ในบทที่ 2 เราได้เรียนรู้ว่าข้อมูลใน React จะไหลจากบนลงล่าง (Top-down) ผ่านทาง <strong>Props</strong> เสมอ วิธีนี้ใช้งานได้ดีมากสำหรับแอปเล็กๆ แต่เมื่อแอปของคุณเริ่มใหญ่ขึ้น (เช่น ระบบ E-Commerce) โครงสร้าง Component Tree ก็จะลึกและซับซ้อนขึ้นตามไปด้วย</p><p>ลองนึกภาพว่าคุณมีข้อมูล "User ที่ล็อกอินอยู่" เก็บไว้ใน <code>App</code> (Component บนสุด) แต่คนที่ต้องการใช้ข้อมูลนี้คือ <code>Avatar</code> (Component ย่อยสุดที่อยู่ใน <code>Header</code> -> <code>Navigation</code> -> <code>UserMenu</code> -> <code>Avatar</code>) คุณจะต้องส่ง Prop <code>user</code> ผ่าน Component ตรงกลางทั้งหมดที่ไม่ได้ใช้งานมันเลย! ปัญหานี้ในวงการเรียกว่า <strong>"Prop Drilling"</strong> (การเจาะทะลวง Prop) ซึ่งทำให้โค้ดบำรุงรักษายากมาก</p><p>วิธีแก้ปัญหาคือ <strong>Global State (สถานะส่วนกลาง)</strong> และ React ได้เตรียมเครื่องมือในตัว (Built-in) ไว้ให้เราแล้ว นั่นคือ <strong>Context API</strong></p><h2>Context API คืออะไร?</h2><p>Context API เป็นฟีเจอร์ที่เปรียบเสมือน "อุโมงค์มิติ (Teleport)" ที่ช่วยให้ Component ต้นทางสามารถส่งผ่านข้อมูลไปยัง Component ปลายทางที่อยู่ลึกแค่ไหนก็ได้ โดยไม่ต้องโยนผ่าน Component ชั้นกลางๆ (Middlemen)</p><p>การทำงานของ Context ประกอบด้วย 3 ขั้นตอนหลัก:</p><ol style="padding-left:20px"><li><strong>สร้าง Context (Create):</strong> ประกาศว่าจะมีอุโมงค์มิติชื่ออะไร</li><li><strong>สร้างจุดจ่าย (Provider):</strong> กำหนดจุดเริ่มต้นของอุโมงค์และข้อมูลที่จะส่งเข้าไป</li><li><strong>จุดรับ/ดูดข้อมูล (Consumer / useContext):</strong> Component ปลายทางที่ต้องการดึงข้อมูลออกจากอุโมงค์</li></ol><h3>ขั้นตอนที่ 1: สร้าง Context (createContext)</h3><p>เรามักจะสร้างแยกไว้ในไฟล์ใหม่ (เช่น <code>ThemeContext.js</code>) เพื่อความเป็นระเบียบ</p><pre><code>import { createContext } from "react";\n\n// 1. สร้างและ Export Context \n// (ค่าเริ่มต้น "light" จะถูกใช้ถ้าไม่มี Provider ครอบอยู่)\nexport const ThemeContext = createContext("light");</code></pre><h3>ขั้นตอนที่ 2: สร้าง Provider (จุดจ่ายข้อมูล)</h3><p>กลับมาที่ <code>App.jsx</code> (หรือ Component ชั้นบนสุด) เราจะใช้ <code>Context.Provider</code> มาครอบ Component ลูกทั้งหมด และจ่ายข้อมูลผ่าน Prop พิเศษที่ชื่อว่า <code>value</code></p><pre><code>import { useState } from "react";\nimport { ThemeContext } from "./ThemeContext";\nimport Header from "./Header";\n\nfunction App() {\n  const [theme, setTheme] = useState("dark");\n\n  const toggleTheme = () =&gt; {\n    setTheme(prev =&gt; prev === "dark" ? "light" : "dark");\n  };\n\n  return (\n    // 2. ใช้ Provider ครอบแอป ส่ง State (และ/หรือ ฟังก์ชัน) เข้าไปใน value\n    &ltThemeContext.Provider value={{ theme, toggleTheme }}&gt;\n      &lt;div className={`app-container ${theme}`}&gt;\n        &lt;Header /&gt;\n        {/* ... คอมโพเนนต์อื่นๆ ภายใต้ App จะเข้าถึง value นี้ได้ทั้งหมด */}\n      &lt;/div&gt;\n    &lt;/ThemeContext.Provider&gt;\n  );\n}</code></pre><h3>ขั้นตอนที่ 3: ดึงข้อมูลไปใช้ (useContext)</h3><p>ที่ Component ปลายทาง (เช่น ใน <code>Header</code> -> <code>ThemeToggleButton</code>) ไม่ว่ามันจะอยู่ลึกแค่ไหน เราสามารถดึงข้อมูลออกจากท่อได้ทันทีด้วย Hook <strong><code>useContext</code></strong></p><pre><code>import { useContext } from "react";\nimport { ThemeContext } from "./ThemeContext";\n\nfunction ThemeToggleButton() {\n  // 3. ดูดข้อมูลออกจากท่อ! \n  // ค่าที่ได้จะเป็น Object { theme, toggleTheme } ที่เราส่งมาจากข้อ 2\n  const { theme, toggleTheme } = useContext(ThemeContext);\n\n  return (\n    &lt;button onClick={toggleTheme}&gt;\n      โหมดปัจจุบัน: {theme === "dark" ? "🌙 กลางคืน" : "☀️ กลางวัน"}\n    &lt;/button&gt;\n  );\n}</code></pre><h2>เมื่อไหร่ควรใช้ Context API?</h2><p>Context API เหมาะสำหรับข้อมูลที่เป็น Global อย่างแท้จริง (ข้อมูลที่ถูกแชร์และใช้งานทั่วทั้งแอป) ตัวอย่างสุดคลาสสิก ได้แก่:</p><ul><li><strong>Theme / UI Mode:</strong> Dark/Light Mode</li><li><strong>Authentication:</strong> ข้อมูล User Profile ของผู้ใช้งานที่เข้าสู่ระบบ</li><li><strong>Language/Locale:</strong> การตั้งค่าภาษาของแอป (ไทย/อังกฤษ)</li></ul><div class="tip-box"><strong>💡 Pro Tip:</strong> อย่าใช้ Context API แทน State ธรรมดาทุกเรื่อง! ข้อเสียที่ใหญ่ที่สุดของ Context คือ <strong>"เมื่อค่า value ใน Provider เปลี่ยน Component \'ทุกตัว\' ที่เรียก useContext (ใน Context นั้น) จะถูกบังคับให้ Re-render ทันที"</strong> ไม่ว่าพวกมันจะใช้ค่านั้นหรือไม่ก็ตาม! ดังนั้น สำหรับแอปขนาดใหญ่ที่มี State ซับซ้อนและเปลี่ยนบ่อยมาก (เช่น ตะกร้าสินค้าที่มีรายการเยอะๆ) การใช้ Library เฉพาะทางอย่าง <strong>Redux Toolkit (RTK)</strong> หรือ <strong>Zustand</strong> จะมีประสิทธิภาพ (Performance) ที่ดีกว่า Context ล้วนๆ อย่างเทียบไม่ติด</div><p>ตอนนี้คุณมีเครื่องมือครบครันทั้ง Local State และ Global State ในบทถัดไป เราจะมาดูวิธีการรวมพลังของ Hooks พื้นฐานอย่าง useState และ useEffect มาประดิษฐ์เป็น "อาวุธคู่กาย" ของคุณเอง (Custom Hooks)!</p>'
  },
  {
    number: 9,
    slug: 'custom-hooks',
    emoji: '🎣',
    title: 'Custom Hooks: สกัดและซ่อนความซับซ้อน',
    content: '<h2>ทำไมต้องสร้าง Custom Hooks?</h2><p>ในบทก่อนๆ เราได้รู้จักกับ Hooks พื้นฐาน (Built-in Hooks) ที่มาพร้อมกับ React อย่าง <code>useState</code>, <code>useEffect</code> และ <code>useContext</code> ไปแล้ว Hooks เหล่านี้ทรงพลังมาก แต่มักจะมีปัญหาอย่างหนึ่งเมื่อแอปใหญ่ขึ้น นั่นคือ "การซ้ำซ้อนของโค้ด (Code Duplication)"</p><p>สมมติว่าในบทที่ 6 (Data Fetching) เราต้องเขียนโค้ดเพื่อสร้าง State `data, isLoading, error` และ `useEffect` พร้อมกับ Axios ถ้าในแอปของคุณมีการเรียก API หน้าละ 3 ตัว คุณก็ต้องก๊อปปี้โค้ดชุดนี้ไปวางซ้ำๆ ทุกหน้า Component ของคุณจะอ้วนและอ่านยากมาก (Fat Components)</p><p>นี่คือจุดที่ <strong>Custom Hooks (ฮุกทำเอง)</strong> เข้ามาช่วยชีวิต! Custom Hooks อนุญาตให้เราดึง (Extract) <strong>"ลอจิกที่มีการใช้งาน State (Stateful Logic)"</strong> ออกมาแยกเป็นฟังก์ชันต่างหาก เพื่อนำไปใช้ซ้ำใน Component อื่นๆ ได้ (เสมือนกับการสร้างฟังก์ชัน Helper ใน JavaScript ปกติ แต่พิเศษกว่าตรงที่มันสามารถเรียกใช้ React Hooks ตัวอื่นๆ ไว้ข้างในตัวมันเองได้!)</p><h2>กฎของการสร้าง Custom Hooks</h2><p>การสร้าง Custom Hook นั้นง่ายมาก มันเป็นเพียงฟังก์ชัน JavaScript ธรรมดา แต่มีกฎเหล็ก (Conventions) 2 ข้อ:</p><ol style="padding-left:20px"><li><strong>ชื่อต้องขึ้นต้นด้วยคำว่า <code>use</code> เสมอ:</strong> เช่น <code>useFetch</code>, <code>useWindowSize</code>, <code>useLocalStorage</code> (การขึ้นต้นด้วย use เป็นการส่งสัญญาณให้ React Linter ทราบว่านี่คือ Hook และมันจะช่วยตรวจสอบกฎการใช้งานให้คุณ)</li><li><strong>สามารถเรียกใช้ Hooks ตัวอื่นได้:</strong> นี่คือสิ่งที่แยก Custom Hook ออกจากฟังก์ชันทั่วไป (ฟังก์ชันธรรมดาไม่สามารถเรียกใช้ useState หรือ useEffect ข้างในได้)</li></ol><h2>ตัวอย่างที่ 1: <code>useFetch</code> (พระเอกของเรา)</h2><p>มาลองสกัดลอจิกการดึงข้อมูลจากบทที่ 6 ให้ออกมาเป็น Custom Hook กัน เราจะสร้างไฟล์ใหม่ชื่อ `useFetch.js`</p><pre><code>// useFetch.js\nimport { useState, useEffect } from "react";\nimport axios from "axios";\n\n// รับพารามิเตอร์เป็น URL ที่ต้องการยิง API\nexport function useFetch(url) {\n  const [data, setData] = useState(null);\n  const [isLoading, setIsLoading] = useState(true);\n  const [error, setError] = useState(null);\n\n  useEffect(() =&gt; {\n    const fetchData = async () =&gt; {\n      setIsLoading(true);\n      try {\n        const response = await axios.get(url);\n        setData(response.data);\n        setError(null);\n      } catch (err) {\n        setError(err.message);\n      } finally {\n        setIsLoading(false);\n      }\n    };\n\n    // หาก URL เปลี่ยน ให้ทำการดึงข้อมูลใหม่!\n    if (url) fetchData();\n    \n  }, [url]);\n\n  // Return สิ่งที่ Component ควรรู้ (มักจะ Return เป็น Array หรือ Object)\n  return { data, isLoading, error };\n}</code></pre><p><strong>วิธีนำไปใช้งาน:</strong> ทันใดนั้น Component <code>UserList</code> ของเราจะสะอาดตาสุดๆ!</p><pre><code>import { useFetch } from "./useFetch";\n\nfunction UserList() {\n  // เรียกใช้ Custom Hook เหมือนใช้ Hook ปกติเลย!\n  const { data: users, isLoading, error } = useFetch("https://api.example.com/users");\n\n  if (isLoading) return &lt;div&gt;Loading...&lt;/div&gt;;\n  if (error) return &lt;div&gt;Error: {error}&lt;/div&gt;;\n\n  return (\n    &lt;ul&gt;\n      {users.map(user =&gt; &lt;li key={user.id}&gt;{user.name}&lt;/li&gt;)}\n    &lt;/ul&gt;\n  );\n}</code></pre><h2>ตัวอย่างที่ 2: <code>useLocalStorage</code> (บันทึกข้อมูลติดเครื่อง)</h2><p>อีกหนึ่ง Use Case ที่เจอบ่อยคือการเชื่อมต่อ <code>useState</code> เข้ากับ <code>localStorage</code> ของ Browser เพื่อให้ข้อมูลไม่หายไปตอนที่ผู้ใช้กดรีเฟรชหน้าเว็บ (เช่น โหมด Dark/Light หรือ ของในตะกร้า)</p><pre><code>import { useState, useEffect } from "react";\n\nexport function useLocalStorage(key, initialValue) {\n  // 1. อ่านค่าตอนเริ่มต้น (Lazy Initialization)\n  const [value, setValue] = useState(() =&gt; {\n    const storedValue = localStorage.getItem(key);\n    return storedValue ? JSON.parse(storedValue) : initialValue;\n  });\n\n  // 2. เมื่อ State ตัวนี้เปลี่ยน สั่งให้บันทึกลง LocalStorage อัตโนมัติ\n  useEffect(() =&gt; {\n    localStorage.setItem(key, JSON.stringify(value));\n  }, [key, value]); // ทำงานเมื่อ key หรือ value เปลี่ยน\n\n  // Return หน้าตาให้เหมือน useState เปี๊ยบ [ค่า, Setter]\n  return [value, setValue];\n}</code></pre><p><strong>วิธีนำไปใช้งาน:</strong></p><pre><code>function SettingsForm() {\n  // ใช้เหมือน useState ทุกประการ แต่ข้อมูลนี้จะไม่มีวันหายเมื่อรีเฟรช!\n  const [theme, setTheme] = useLocalStorage("app-theme", "light");\n  const [volume, setVolume] = useLocalStorage("app-volume", 50);\n\n  return (\n    // ... UI ของคุณ\n  );\n}</code></pre><div class="tip-box"><strong>💡 Pro Tip:</strong> Custom Hook ไม่ใช่การ "แชร์ State (Global State)" ระหว่าง Component (ถ้าต้องการแชร์ State ต้องใช้ Context API) แต่ละ Component ที่เรียก <code>useFetch</code> หรือ <code>useLocalStorage</code> จะได้ State (กล่อง) ที่เป็น <strong>อิสระแยกจากกันโดยสิ้นเชิง</strong> Custom Hook เป็นเพียงการ "แชร์ลอจิก (Share Logic)" หรือแชร์กระบวนการในการสร้าง State เท่านั้น!</div><p>การเข้าใจการสร้าง Custom Hooks คือก้าวสำคัญที่ยกระดับคุณจาก "ผู้ใช้งาน React" เป็น "วิศวกร React (React Engineer)" อย่างเต็มตัว ในบทต่อไป เราจะก้าวออกจาก Component เดี่ยวๆ แล้วมาสร้างเว็บไซต์แบบเต็มรูปแบบที่มีหลายหน้า (Multi-page experience) ด้วย <strong>React Router v6</strong></p>'
  },
  {
    number: 10,
    slug: 'router',
    emoji: '🚦',
    title: 'React Router v6: นำทางระหว่างหน้าเว็บ',
    content: '<h2>ทำไม React ต้องมี Router?</h2><p>อย่างที่เราทราบกันตั้งแต่บทนำ React ถูกสร้างมาเพื่อทำ <strong>Single Page Application (SPA)</strong> หมายความว่าแอปพลิเคชันของคุณจะมีไฟล์ <code>index.html</code> เพียงไฟล์เดียวเท่านั้น แต่ผู้ใช้งานเว็บไซต์คาดหวังว่าพวกเขาจะสามารถกดลิงก์ เปลี่ยนหน้าไปที่หน้า /about, /products หรือ /contact ได้ และสามารถกดปุ่ม Back/Forward ของเบราว์เซอร์ได้ตามปกติ</p><p>เราจะจำลอง (Simulate) การเปลี่ยนหน้าจอโดยไม่โหลดหน้า HTML ใหม่ได้อย่างไร? คำตอบคือเราต้องใช้ไลบรารีจัดการ Routing ซึ่ง <strong>React Router (ปัจจุบันคือเวอร์ชัน 6)</strong> คือมาตรฐานอันดับหนึ่งที่ทุกคนใช้</p><p>ติดตั้งแพ็กเกจด้วยคำสั่ง: <code>npm install react-router-dom</code></p><h2>โครงสร้างพื้นฐาน (BrowserRouter และ Routes)</h2><p>การจะใช้ React Router เราต้องกำหนดโครงสร้างที่จุดเริ่มต้นของแอป (มักจะเป็นในไฟล์ <code>main.jsx</code> หรือ <code>App.jsx</code>)</p><pre><code>import { BrowserRouter, Routes, Route } from "react-router-dom";\nimport Home from "./pages/Home";\nimport About from "./pages/About";\nimport NotFound from "./pages/NotFound";\n\nfunction App() {\n  return (\n    // 1. BrowserRouter ควบคุม URL ของ History API ในบราวเซอร์\n    &lt;BrowserRouter&gt;\n      &lt;div className="app-container"&gt;\n        &lt;Navbar /&gt; {/* Navbar จะแสดงในทุกๆ หน้า เพราะอยู่นอก Routes */}\n\n        {/* 2. Routes เป็นตัวครอบพื้นที่ที่จะมีการเปลี่ยน Component ตาม URL */}\n        &lt;Routes&gt;\n          {/* 3. Route จับคู่ URL path กับ Component */}\n          &lt;Route path="/" element={&lt;Home /&gt;} /&gt;\n          &lt;Route path="/about" element={&lt;About /&gt;} /&gt;\n          {/* กรณีผู้ใช้พิมพ์ URL มั่วๆ (Catch-all route) */}\n          &lt;Route path="*" element={&lt;NotFound /&gt;} /&gt;\n        &lt;/Routes&gt;\n      &lt;/div&gt;\n    &lt;/BrowserRouter&gt;\n  );\n}</code></pre><h2>การเปลี่ยนหน้า (Link และ NavLink)</h2><p>กฎที่สำคัญที่สุดของการทำ SPA คือ: <strong>ห้ามใช้แท็ก <code>&lt;a href="..."&gt;</code> ในการเปลี่ยนหน้าเด็ดขาด!</strong> เพราะมันจะทำให้เบราว์เซอร์โหลดหน้าเว็บใหม่และตัดการเชื่อมต่อ State ทั้งหมด คุณต้องใช้ Component จาก React Router แทน:</p><ul><li><strong><code>&lt;Link to="/about"&gt;</code>:</strong> ทำหน้าที่เหมือน `&lt;a&gt;` แต่จะไม่รีเฟรชหน้าเว็บ เปลี่ยนหน้าได้ไวเหมือนสายฟ้า</li><li><strong><code>&lt;NavLink to="/about"&gt;</code>:</strong> เหมือน Link ทุกประการ แต่จะ <em>ฉลาด</em> กว่าตรงที่มันรู้ว่าตัวเอง "กำลังแอกทีฟ (Active)" หรือไม่ เหมาะสำหรับทำเมนูนำทาง (Navbar) เพื่อไฮไลต์หน้าปัจจุบัน</li></ul><pre><code>import { NavLink } from "react-router-dom";\n\nfunction Navbar() {\n  return (\n    &lt;nav&gt;\n      {/* className สามารถรับฟังก์ชันเพื่อดูสถานะ isActive ได้ */}\n      &lt;NavLink \n        to="/" \n        className={({ isActive }) =&gt; isActive ? "text-red-500 font-bold" : "text-gray-500"}\n      &gt;\n        หน้าแรก\n      &lt;/NavLink&gt;\n      \n      &lt;NavLink \n        to="/about"\n        className={({ isActive }) =&gt; isActive ? "text-red-500 font-bold" : "text-gray-500"}\n      &gt;\n        เกี่ยวกับเรา\n      &lt;/NavLink&gt;\n    &lt;/nav&gt;\n  );\n}</code></pre><h2>Dynamic Routes และ useParams</h2><p>ในแอปจริง URL มักจะไม่ได้ตายตัว เช่น หน้ารายละเอียดสินค้า อาจจะเป็น <code>/products/123</code> หรือ <code>/products/abc</code> เราเรียกว่า Dynamic Routing เราสามารถกำหนดได้โดยใช้ <strong>เครื่องหมายโคลอน <code>:</code></strong> ใน path:</p><pre><code>// ใน App.jsx\n&lt;Route path="/products/:productId" element={&lt;ProductDetail /&gt;} /&gt;</code></pre><p>และภายใน Component <code>ProductDetail</code> เราจะใช้ Hook <strong><code>useParams</code></strong> เพื่อดึงค่า <code>productId</code> ออกมาใช้งาน (เช่น นำไปยิง API ต่อ)</p><pre><code>import { useParams } from "react-router-dom";\n\nfunction ProductDetail() {\n  // ชื่อตัวแปร productId ต้องตรงกับที่ตั้งไว้ข้างหลังเครื่องหมาย : ใน Route\n  const { productId } = useParams();\n\n  // ... นำ productId ไป useFetch()\n\n  return &lt;h1&gt;กำลังแสดงรายละเอียดของสินค้า รหัส: {productId}&lt;/h1&gt;;\n}</code></pre><h2>Programmatic Navigation (การเปลี่ยนหน้าด้วยโค้ด)</h2><p>บางครั้งเราไม่ได้เปลี่ยนหน้าเพราะผู้ใช้คลิก Link แต่เปลี่ยนหน้าหลังจากที่ "โค้ดทำงานเสร็จ" (เช่น ผู้ใช้กดปุ่ม Login โค้ดยิง API เช็ครหัสผ่าน ถ้าผ่านแล้วถึงจะเปลี่ยนหน้าไป Dashboard) กรณีนี้เราจะใช้ Hook <strong><code>useNavigate</code></strong></p><pre><code>import { useNavigate } from "react-router-dom";\n\nfunction LoginForm() {\n  const navigate = useNavigate();\n\n  const handleLogin = async (e) =&gt; {\n    e.preventDefault();\n    // ... สมมติว่าส่งข้อมูลตรวจสอบผ่านแล้ว\n    const isSuccess = true;\n    \n    if (isSuccess) {\n      // ส่งผู้ใช้ไปที่หน้า Dashboard\n      navigate("/dashboard");\n    } else {\n      alert("รหัสผ่านไม่ถูกต้อง");\n    }\n  };\n\n  return &lt;button onClick={handleLogin}&gt;เข้าสู่ระบบ&lt;/button&gt;;\n}</code></pre><div class="tip-box"><strong>💡 Pro Tip:</strong> `useNavigate` สามารถรับ Argument แบบตัวเลขได้ด้วย เช่น `navigate(-1)` คือการสั่งให้ Browser กลับไปหน้าก่อนหน้า (Go Back) เสมือนผู้ใช้กดปุ่ม Back บน Browser!</div><p>ในบทนี้ คุณได้แปลง Component ธรรมดาให้กลายเป็นแอปพลิเคชันที่มีโครงสร้างเหมือนเว็บไซต์จริงแล้ว React Router v6 ยังมีฟีเจอร์ขั้นสูงอย่าง Nested Routes (เส้นทางซ้อนกัน) และ Protected Routes (ล็อคหน้าเว็บ ต้องเข้าสู่ระบบก่อน) ซึ่งคุณจะได้ใช้งานจริงในโปรเจกต์ท้ายคอร์ส ในบทถัดไปเราจะกลับไปสำรวจ Hooks ขั้นสูงที่เหลืออยู่ของ React กัน</p>'
  },
  {
    number: 11,
    slug: 'advanced-hooks',
    emoji: '🚀',
    title: 'Advanced Hooks: ยกระดับพลังการจัดการ React',
    content: '<h2>ก้าวข้ามพื้นฐานสู่ระดับโปร</h2><p>หากคุณใช้แค่ `useState`, `useEffect` และ Custom Hooks คุณก็สามารถสร้างแอป React ได้ทุกประเภทแล้ว อย่างไรก็ตาม React ยังมี Hooks ขั้นสูง (Advanced Hooks) อีกหลายตัวที่ออกแบบมาเพื่อแก้ปัญหาเฉพาะทาง โดยเฉพาะเรื่องของ "ความซับซ้อนของ State" และ "การจัดการ DOM โดยตรง" ในบทนี้เราจะมาโฟกัสที่สองตัวที่เจอบ่อยที่สุด คือ <strong>`useReducer`</strong> และ <strong>`useRef`</strong></p><h2>1. useReducer (พี่ใหญ่ของ useState)</h2><p>ในบทที่ 3 เราใช้ `useState` จัดการ State ง่ายๆ แต่เมื่อ Component ของคุณซับซ้อนขึ้น มี State หลายตัวที่ต้องอัปเดตไปพร้อมๆ กัน (เช่น ระบบตะกร้าสินค้าที่มีรายการของเยอะๆ, ยอดรวม, สถานะส่วนลด, สถานะจัดส่ง) การใช้ `useState` หลายๆ ครั้งอาจทำให้เกิดบั๊กได้ง่ายและโค้ดอ่านยาก</p><p><strong>`useReducer`</strong> คือ Hook ที่รวมศูนย์การจัดการ State ไว้ในที่เดียว โดยใช้ลอจิกที่เรียกว่า <strong>"Reducer Function"</strong> (ซึ่งเป็นคอนเซปต์หลักที่มาจากไลบรารีชื่อดังอย่าง Redux)</p><h3>ส่วนประกอบของ useReducer</h3><ol style="padding-left:20px"><li><strong>State:</strong> ข้อมูลปัจจุบัน</li><li><strong>Action:</strong> วัตถุ (Object) ที่อธิบายว่า "เกิดเหตุการณ์อะไรขึ้น" (มักมี <code>type</code> และ <code>payload</code>)</li><li><strong>Reducer Function:</strong> ฟังก์ชันที่รับ State เดิม กับ Action เข้ามา แล้วตัดสินใจคืนค่า State "ก้อนใหม่" กลับไป</li><li><strong>Dispatch:</strong> ฟังก์ชันสำหรับยิง Action ไปหา Reducer</li></ol><h3>ตัวอย่าง: ระบบตะกร้าสินค้าแบบมินิ</h3><pre><code>import { useReducer } from "react";\n\n// 1. สร้าง Reducer Function (มักจะวางไว้นอก Component เพื่อความสะอาด)\nfunction cartReducer(state, action) {\n  switch (action.type) {\n    case "ADD_ITEM":\n      // รับจำนวนเก่า + จำนวนใหม่ (ที่ส่งมาทาง action.payload)\n      return { count: state.count + action.payload };\n    case "REMOVE_ITEM":\n      // ห้ามจำนวนติดลบ\n      return { count: Math.max(0, state.count - action.payload) };\n    case "RESET":\n      return { count: 0 };\n    default:\n      return state;\n  }\n}\n\nfunction ShoppingCart() {\n  // 2. ประกาศ useReducer(ฟังก์ชัน, ค่าเริ่มต้น)\n  const [state, dispatch] = useReducer(cartReducer, { count: 0 });\n\n  return (\n    &lt;div&gt;\n      &lt;h3&gt;สินค้าในตะกร้า: {state.count} ชิ้น&lt;/h3&gt;\n      {/* 3. ใช้ dispatch ส่ง Action { type: "...", payload: ... } */}\n      &lt;button onClick={() =&gt; dispatch({ type: "ADD_ITEM", payload: 1 })}&gt;\n        เพิ่ม 1 ชิ้น\n      &lt;/button&gt;\n      &lt;button onClick={() =&gt; dispatch({ type: "REMOVE_ITEM", payload: 1 })}&gt;\n        ลบ 1 ชิ้น\n      &lt;/button&gt;\n      &lt;button onClick={() =&gt; dispatch({ type: "RESET" })}&gt;\n        ล้างตะกร้า\n      &lt;/button&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p>ข้อดีสุดยอดของ <code>useReducer</code> คือ Component ของคุณจะไม่ต้องรู้เลยว่า "ตะกร้ามันคำนวณบวกลบยังไง" Component แค่มีหน้าที่ "ตะโกน (Dispatch)" บอก Reducer ว่า "เห้ย ผู้ใช้กดเพิ่มของนะ!" ส่วนวิธีคำนวณถูกซ่อนไว้ใน Reducer อย่างเป็นระเบียบ</p><h2>2. useRef (ช่องทางลัดเข้าสู่ DOM)</h2><p>ในบทที่ 7 (Forms) เราได้เห็น `useRef` แวบๆ ไปแล้ว กฎหลักของ React คือ "ห้ามไปยุ่งกับ DOM โดยตรง" ให้ใช้ State ควบคุมเอา แต่บางครั้งเราจำเป็นต้องหลีกเลี่ยงกฎนี้ (Escape Hatch) เช่น:<br/>- ต้องการสั่งโฟกัส (Focus) ที่ช่อง Input อัตโนมัติ<br/>- ต้องการดึงขนาดกว้าง/ยาวของ Element ก้อนนึง<br/>- ต้องการนำไลบรารีที่ไม่ใช่ React (เช่น กราฟ 3D หรือแผนที่ Google Maps) มายัดใส่ใน `&lt;div&gt;`</p><p><strong>`useRef`</strong> อนุญาตให้เราสร้าง "ตัวแปรอ้างอิง" ที่จะพุ่งตรงไปจับ Element ใน HTML โดยตรงได้</p><h3>ตัวอย่าง: ระบบ Auto Focus</h3><pre><code>import { useRef } from "react";\n\nfunction FocusInput() {\n  // 1. สร้างตัวแปร Ref (ค่าเริ่มต้น null)\n  const inputElement = useRef(null);\n\n  const handleFocus = () =&gt; {\n    // 3. เข้าถึง Element จริงๆ ผ่าน .current\n    // สังเกตว่าเราเรียก .focus() ซึ่งเป็น Method ดั้งเดิมของ DOM ได้เลย\n    inputElement.current.focus();\n  };\n\n  return (\n    &lt;div&gt;\n      {/* 2. ผูกตัวแปรเข้ากับ attribute ref ของแท็กที่ต้องการ */}\n      &lt;input type="text" ref={inputElement} placeholder="พิมพ์ที่นี่..." /&gt;\n      &lt;button onClick={handleFocus}&gt;บังคับโฟกัสช่องกรอก&lt;/button&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><h2>useRef กับการเก็บค่าคงที่ (Mutable Values)</h2><p>ความลับอีกอย่างของ `useRef` คือมันไม่ได้ใช้จับ DOM ได้อย่างเดียว! มันสามารถเก็บ "ค่าอะไรก็ได้" คล้ายๆ กับ <code>useState</code> <strong>แต่ข้อแตกต่างสำคัญคือ การเปลี่ยนค่า `.current` ของ `useRef` จะไม่ทำให้ Component เกิดการ Re-render (หน้าจอไม่อัปเดต)</strong></p><p>เรามักจะใช้ `useRef` เก็บค่าที่ใช้ประมวลผลอยู่เบื้องหลัง เช่น เก็บ ID ของ <code>setInterval</code> เพื่อนำไป <code>clearInterval</code> ตอนลบทิ้ง โดยที่ผู้ใช้บนหน้าจอไม่จำเป็นต้องรู้ว่า ID คืออะไร (ถ้าใช้ State หน้าจอจะกระตุกเสียเปล่า)</p><div class="tip-box"><strong>💡 Pro Tip:</strong> กฎการจำง่ายๆ:<br/>- ถ้าค่าเปลี่ยน ➡️ หน้าจอต้องเปลี่ยนตาม ➡️ ใช้ <strong><code>useState</code></strong> หรือ <strong><code>useReducer</code></strong><br/>- ถ้าค่าเปลี่ยน ➡️ เก็บไว้รู้คนเดียวเบื้องหลัง (หน้าจอห้ามเปลี่ยน) ➡️ ใช้ <strong><code>useRef</code></strong></div><p>ในบทนี้เราได้เรียนรู้เครื่องมือระดับสูงกันแล้ว ในบทต่อไปเราจะมาดู Hooks ขั้นสูงอีกกลุ่มหนึ่งที่ไม่ได้ออกแบบมาจัดการ State แต่เกิดมาเพื่อ "เร่งความเร็ว (Performance)" อย่าง <code>useMemo</code> และ <code>useCallback</code></p>'
  },
  {
    number: 12,
    slug: 'performance',
    emoji: '⚡',
    title: 'Performance Optimization: รีดประสิทธิภาพ React ให้สุด',
    content: '<h2>เมื่อ React เริ่มทำงานช้าลง</h2><p>React ขึ้นชื่อเรื่องความเร็วด้วย Virtual DOM แต่เมื่อแอปพลิเคชันของคุณใหญ่ขึ้น มี Component ซ้อนกันเป็นร้อยเป็นพันตัว คุณอาจเริ่มสังเกตเห็นอาการ "กระตุก" (Lag) ตอนพิมพ์ฟอร์ม หรือตอนกดปุ่ม</p><p>สาเหตุหลักมาจากธรรมชาติของ React ที่ว่า: <strong>"เมื่อ Component แม่ (Parent) เกิดการ Re-render Component ลูก (Children) ทั้งหมดจะถูกบังคับให้ Re-render ตามไปด้วยเสมอ"</strong> ไม่ว่า Props ของลูกจะเปลี่ยนหรือไม่ก็ตาม!</p><p>ในบทนี้เราจะมาเรียนรู้อาวุธลับ 3 ชิ้น ที่ใช้ในการ "หยุด" การ Re-render ที่ไม่จำเป็น ได้แก่ <strong><code>React.memo</code></strong>, <strong><code>useMemo</code></strong>, และ <strong><code>useCallback</code></strong></p><h2>1. React.memo (โล่ป้องกัน Component ลูก)</h2><p>สมมติว่าคุณมี `App` ที่มี State `count` และมีลูกคือ `HeavyComponent` (สมมติว่าเป็นกราฟที่กินซีพียูสูง) ทุกครั้งที่คุณกดเพิ่ม `count` เจ้า `HeavyComponent` ก็จะถูก Render ใหม่ด้วย ทั้งๆ ที่มันไม่ได้ใช้ `count` เลย!</p><pre><code>import { useState, memo } from "react";\n\n// 1. ใช้ memo() ครอบ Component ที่ต้องการปกป้อง\nconst HeavyGraph = memo(function HeavyGraph({ data }) {\n  console.log("วาดกราฟใหม่..."); // จะไม่แสดงถ้า data ไม่เปลี่ยน!\n  return &lt;div&gt;กราฟซับซ้อน: {data}&lt;/div&gt;;\n});\n\nfunction App() {\n  const [count, setCount] = useState(0);\n  const [graphData, setGraphData] = useState("คงที่");\n\n  return (\n    &lt;div&gt;\n      &lt;button onClick={() =&gt; setCount(c =&gt; c + 1)}&gt;Count: {count}&lt;/button&gt;\n      {/* เมื่อกดปุ่ม HeavyGraph จะไม่ถูก Render ใหม่แล้ว! */}\n      &lt;HeavyGraph data={graphData} /&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p><code>memo</code> จะสั่งให้ React "จำ" ผลลัพธ์เดิมไว้ และจะยอม Render ใหม่ก็ต่อเมื่อ <strong>Props (ในที่นี้คือ data) มีการเปลี่ยนแปลงค่าเท่านั้น</strong></p><h2>ปัญหาของ Reference Equality (เมื่อ โล่ memo แตก)</h2><p><code>memo</code> ฟังดูดี แต่มันมีจุดอ่อนร้ายแรงเกี่ยวกับการอ้างอิง (Reference) ของ JavaScript หาก Prop ที่คุณส่งไปเป็น <strong>Object, Array, หรือ Function</strong> โล่ของ `memo` จะแตกทันที!</p><pre><code>// ใน App.jsx\nconst config = { theme: "dark" }; // เป็น Object\nconst handleClick = () =&gt; alert("คลิก!"); // เป็น Function\n\n&lt;HeavyGraph data={graphData} config={config} onClick={handleClick} /&gt;</code></pre><p>เพราะทุกครั้งที่ <code>App</code> Re-render มันจะ <strong>สร้าง Object และ Function ตัวใหม่ขึ้นมาในหน่วยความจำเสมอ (คนละ Memory Address)</strong> ทำให้ <code>memo</code> เข้าใจผิดคิดว่า Props เปลี่ยนไปแล้ว (เพราะ Object เก่า !== Object ใหม่) จึงเกิดการ Re-render อยู่ดี</p><h2>2. useMemo (จำค่าตัวแปร)</h2><p>เพื่อแก้ปัญหาโล่แตกจาก Object/Array หรือการคำนวณสูตรคณิตศาสตร์ที่หนักหน่วง เราจะใช้ <strong><code>useMemo</code></strong> เพื่อ "จำค่า (Memoize)" ไว้ จนกว่าตัวแปรต้นทางจะเปลี่ยน</p><pre><code>import { useMemo } from "react";\n\nfunction App() {\n  const [count, setCount] = useState(0);\n\n  // 1. ถ้าไม่ใช้ useMemo ลูป 1 ล้านรอบนี้จะทำงานทุกครั้งที่กดปุ่ม count!\n  // 2. พอใช้ useMemo มันจะทำงานแค่ตอนแรก และจำค่า 500000500000 ไว้ตลอดไป\n  const heavyCalculation = useMemo(() =&gt; {\n    console.log("กำลังคำนวณ 1 ล้านรอบ...");\n    let sum = 0;\n    for (let i = 0; i &lt;= 1000000; i++) sum += i;\n    return sum;\n  }, []); // &lt;- Dependency Array คล้าย useEffect\n\n  // 3. ใช้กับ Object เพื่อไม่ให้ Reference เปลี่ยน\n  const config = useMemo(() =&gt; ({ theme: "dark" }), []);\n\n  return (\n    &lt;div&gt;\n      &lt;HeavyGraph data={heavyCalculation} config={config} /&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><h2>3. useCallback (จำฟังก์ชัน)</h2><p>เหมือนกับ `useMemo` เป๊ะๆ แต่ <strong>`useCallback` เกิดมาเพื่อจำ "ฟังก์ชัน" โดยเฉพาะ</strong> (เพราะการเขียน <code>useMemo(() =&gt; () =&gt; {...})</code> มันดูซับซ้อนเกินไป)</p><pre><code>import { useCallback } from "react";\n\nfunction App() {\n  const [count, setCount] = useState(0);\n\n  // ฟังก์ชันนี้จะถูกสร้างแค่ "ครั้งเดียว" และใช้ Reference เดิมตลอดไป\n  const handleClick = useCallback(() =&gt; {\n    console.log("ส่งข้อมูลไป Server");\n  }, []); // &lt;- ใส่ dependency ได้เหมือนกัน\n\n  return (\n    &lt;div&gt;\n      &lt;HeavyGraph onClick={handleClick} /&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><div class="tip-box"><strong>💡 Pro Tip:</strong> อย่าใช้ <code>memo</code>, <code>useMemo</code>, <code>useCallback</code> มั่วซั่วไปหมดทุกที่! การสั่งให้ React "จำ" ของพวกนี้ก็ต้อง <strong>เสียหน่วยความจำ (RAM)</strong> และ <strong>เสีย CPU</strong> ในการคำนวณเปรียบเทียบ Dependency Array ในแอปทั่วไปที่ไม่ได้กระตุก การปล่อยให้ React Re-render มักจะ "เร็วกว่า" การใช้ Hook เหล่านี้ขวางไว้เสมอ จงใช้ต่อเมื่อ 1) ส่ง Prop ลงไปยัง Component ที่หนักมาก หรือ 2) มีการคำนวณที่ใช้เวลามากกว่า 10-20ms จริงๆ เท่านั้น (Premature optimization is the root of all evil)</div><p>ในบทนี้เราได้ติดปีกให้แอป React ของเราเร็วขึ้นแล้ว ในบทต่อไป เราจะก้าวเข้าสู่เรื่องสไตล์ (Styling) ซึ่งเป็นสิ่งที่ขาดไม่ได้ในการทำให้แอปของเราสวยงามน่าใช้!</p>'
  },
  {
    number: 13,
    slug: 'styling',
    emoji: '🎨',
    title: 'Styling in React: ศิลปะแห่งการตกแต่ง UI',
    content: '<h2>ทำไมเรื่องสไตล์ถึงเป็นเรื่องใหญ่ใน React?</h2><p>ในโลก HTML/CSS ดั้งเดิม เรามักจะเขียนไฟล์ <code>style.css</code> ก้อนใหญ่ๆ หนึ่งไฟล์แล้วดึงไปใช้ทุกหน้า ปัญหาที่นักพัฒนาเจอประจำคือ <strong>"CSS Class Name ชนกัน (Global Scope Problem)"</strong> คุณอาจจะสร้างคลาส `.button` ในหน้า Home แต่ปรากฏว่ามันไปทำปุ่มในหน้า About พังพินาศ เพราะ CSS มันแชร์กันหมด</p><p>ใน React ซึ่งเน้นความเป็น <strong>Component (ชิ้นส่วนที่แยกขาดจากกัน)</strong> เราจึงต้องการวิธีเขียนสไตล์ที่ "ผูกติด (Scoped)" อยู่กับ Component นั้นๆ โดยเฉพาะ และนี่คือ 4 แนวทางยอดฮิตในวงการ React ปัจจุบัน</p><h2>1. CSS ปกติ (Global CSS)</h2><p>วิธีที่ง่ายที่สุด (แต่ปัญหาสะสมเยอะสุด) คือการ <code>import "./App.css"</code> เข้ามาดื้อๆ</p><pre><code>// Button.css\n.btn-primary {\n  background-color: blue;\n  color: white;\n}\n\n// Button.jsx\nimport "./Button.css";\nfunction Button() { \n  return &lt;button className="btn-primary"&gt;Click&lt;/button&gt;; \n}</code></pre><p><strong>ข้อควรระวัง:</strong> แม้คุณจะ import ไว้ใน `Button.jsx` แต่คลาส <code>.btn-primary</code> จะหลุดออกไปมีผล <strong>ทั่วทั้งแอปพลิเคชัน</strong> ทันทีที่ปุ่มนี้โผล่ขึ้นมา!</p><h2>2. CSS Modules (มาตรฐานที่ปลอดภัย)</h2><p>React (และ Vite/Next.js) รองรับฟีเจอร์ที่เรียกว่า <strong>CSS Modules</strong> แบบแกะกล่อง เพียงแค่คุณเปลี่ยนชื่อไฟล์เป็น <code>*.module.css</code></p><pre><code>// Button.module.css\n.btnPrimary {\n  background-color: blue;\n  color: white;\n}\n\n// Button.jsx\n// 1. สังเกตการ import ที่เปลี่ยนไป เป็นการ import object\nimport styles from "./Button.module.css";\n\nfunction Button() { \n  // 2. เรียกใช้ผ่าน styles.className\n  return &lt;button className={styles.btnPrimary}&gt;Click&lt;/button&gt;; \n}</code></pre><p><strong>เวทมนตร์ของมัน:</strong> ตอน React บิลด์ (Build) โค้ด มันจะเปลี่ยนคลาส <code>.btnPrimary</code> ให้กลายเป็นชื่อแปลกๆ เช่น <code>.Button_btnPrimary__3F_09</code> ทำให้รับประกันได้ 100% ว่าชื่อคลาสจะไม่ชนกับ Component อื่นแน่นอน!</p><h2>3. CSS-in-JS (Styled Components)</h2><p>นี่คือยุคตื่นทองของ React แนวคิดคือ "ทำไมเราไม่เขียน CSS ลงไปใน JavaScript ซะเลยล่ะ?" ไลบรารีที่ดังที่สุดคือ <code>styled-components</code> (ต้อง <code>npm install styled-components</code>)</p><pre><code>import styled from "styled-components";\n\n// 1. สร้าง Component ใหม่ที่ครอบ CSS ไว้ข้างใน\nconst StyledButton = styled.button`\n  background-color: ${(props) =&gt; props.primary ? "blue" : "gray"};\n  color: white;\n  padding: 10px 20px;\n  border-radius: 5px;\n  border: none;\n  cursor: pointer;\n\n  &amp;:hover {\n    opacity: 0.8;\n  }\n`;\n\nfunction App() {\n  return (\n    &lt;div&gt;\n      {/* 2. นำมาใช้เหมือน Component ปกติ และส่ง Props ไปควบคุม CSS ได้ด้วย! */}\n      &lt;StyledButton primary&gt;Primary Button&lt;/StyledButton&gt;\n      &lt;StyledButton&gt;Default Button&lt;/StyledButton&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p><strong>ข้อดี:</strong> คุณสามารถใช้ JavaScript ลอจิก (if/else, Props, State) เข้ามาคำนวณ CSS ได้โดยตรงอย่างทรงพลังสุดๆ</p><h2>4. Utility-first Framework (Tailwind CSS 👑)</h2><p>นี่คือ <strong>"ราชาองค์ปัจจุบัน"</strong> ของวงการ Frontend (และเป็นสิ่งที่แนะนำให้ใช้ที่สุดในการเริ่มโปรเจกต์ใหม่) Tailwind CSS เปลี่ยนวิธีคิดใหม่หมด แทนที่จะเขียน CSS เอง เราจะเอาคลาสสั้นๆ ที่เขากำหนดไว้ให้ (Utility Classes) มาประกอบกันใน HTML โดยตรง</p><p>สมมติเราติดตั้ง Tailwind เสร็จแล้ว:</p><pre><code>function Card() {\n  return (\n    // ไม่ต้องเขียน CSS เองเลยสักบรรทัด!\n    &lt;div className="max-w-sm rounded-xl overflow-hidden shadow-lg bg-white p-6 hover:shadow-xl transition-shadow"&gt;\n      &lt;h2 className="text-2xl font-bold text-gray-800 mb-2"&gt;\n        Tailwind คือเวทมนตร์\n      &lt;/h2&gt;\n      &lt;p className="text-gray-600 text-base"&gt;\n        ตอนแรกอาจจะดูรกรุงรัง แต่พอเขียนจนชินแล้ว คุณจะไม่อยากกลับไปเขียน CSS แบบเดิมอีกเลย\n      &lt;/p&gt;\n      &lt;button className="mt-4 bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"&gt;\n        อ่านเพิ่มเติม\n      &lt;/button&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><div class="tip-box"><strong>💡 Pro Tip:</strong> เลือกใช้แบบไหนดี?<br/>- <strong>โปรเจกต์เล็ก/เพิ่งเริ่มเรียน:</strong> ใช้ CSS ปกติ หรือ CSS Modules<br/>- <strong>โปรเจกต์ที่ต้องดีไซน์อิสระและไดนามิกจัดๆ:</strong> ใช้ Styled Components<br/>- <strong>โปรเจกต์ทั่วไป/ทำงานเป็นทีม/ต้องการความเร็วขั้นสุด:</strong> ใช้ <strong>Tailwind CSS</strong> (ปัจจุบันวงการเทไปทาง Tailwind เกินกว่า 70% แล้ว)</div><p>เมื่อแอปสวยงามแล้ว สิ่งต่อไปที่จะมองข้ามไม่ได้คือ "ความน่าเชื่อถือ" ในบทต่อไปเราจะมาเรียนรู้วิธี "ทดสอบ" ว่าโค้ดที่เราเขียนมานั้นทำงานได้ถูกต้องจริงหรือไม่ ก่อนที่จะปล่อยให้ผู้ใช้งานจริง (Testing)</p>'
  },
  {
    number: 14,
    slug: 'testing',
    emoji: '🧪',
    title: 'Testing in React: เขียนโค้ดอย่างมั่นใจ',
    content: '<h2>ทำไม Developer ต้องเขียน Test?</h2><p>ลองจินตนาการว่าคุณเขียนระบบ "ตะกร้าสินค้า" เสร็จแล้ว คุณทดสอบด้วยการคลิกเองบนเบราว์เซอร์ ทุกอย่างทำงานเพอร์เฟกต์! สัปดาห์ต่อมา คุณเพิ่มฟีเจอร์ "คูปองส่วนลด" โดยแก้ไขโค้ดนิดหน่อย คุณคิดว่าตะกร้าสินค้ายังบวกเลขถูกอยู่ไหม? คุณจะต้องกลับไปนั่งคลิกตะกร้าสินค้าใหม่หมดทุกหน้าเพื่อเช็คหรือเปล่า?</p><p>นี่คือจุดที่ <strong>Automated Testing (การทดสอบอัตโนมัติ)</strong> เข้ามามีบทบาท แทนที่เราจะใช้คนกด (Manual Test) เราจะ "เขียนโค้ดเพื่อไปทดสอบโค้ดของเราเอง" การเขียน Test ช่วยให้เรากล้าแก้ไขโค้ดเก่า (Refactoring) อย่างมั่นใจ โดยไม่ต้องกลัวว่าจะทำแอปพัง</p><p>ในโลกของ React เครื่องมือมาตรฐานที่ใช้คู่กันเสมอคือ <strong>Vitest (หรือ Jest)</strong> เป็นตัวรันเทส และ <strong>React Testing Library (RTL)</strong> เป็นตัวจำลองการเรนเดอร์และจำลองผู้ใช้งาน</p><h2>ปรัชญาของ React Testing Library (RTL)</h2><p>กฎทองของ RTL คือ <strong>"ทดสอบในมุมมองของผู้ใช้งาน (Test exactly as the user interacts)"</strong></p><p>ผู้ใช้งานไม่รู้หรอกว่าคุณตั้งชื่อ State ว่า <code>count</code> ผู้ใช้งานไม่รู้หรอกว่าคุณใช้ <code>useMemo</code> หรือเปล่า สิ่งที่ผู้ใช้รู้คือ <em>"ถ้าฉันคลิกปุ่มที่มีคำว่า \'เพิ่ม\' ตัวเลขบนหน้าจอต้องเปลี่ยนจาก 0 เป็น 1"</em> ดังนั้นเราจะเขียน Test โดยอิงจากสิ่งที่มองเห็น (DOM) ไม่ใช่การล้วงเข้าไปดูตัวแปรเบื้องหลัง</p><h2>ตัวอย่างที่ 1: การเขียนเทสพื้นฐาน</h2><p>เรามี <code>Counter.jsx</code> ธรรมดาๆ มาเขียนเทสกันในไฟล์ <code>Counter.test.jsx</code></p><pre><code>// Counter.jsx (โค้ดต้นทาง)\nexport function Counter() {\n  const [count, setCount] = useState(0);\n  return (\n    &lt;div&gt;\n      &lt;h1&gt;Count: {count}&lt;/h1&gt;\n      &lt;button onClick={() =&gt; setCount(c =&gt; c + 1)}&gt;เพิ่ม&lt;/button&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><pre><code>// Counter.test.jsx (ไฟล์ทดสอบ)\nimport { render, screen } from "@testing-library/react";\nimport userEvent from "@testing-library/user-event";\nimport { Counter } from "./Counter";\n\n// คำอธิบายกลุ่มของการเทส (Test Suite)\ndescribe("Counter Component", () =&gt; {\n\n  // เทสเคสที่ 1 (Test Case)\n  it("แสดงผลค่าเริ่มต้นที่ 0 ได้ถูกต้อง", () =&gt; {\n    // 1. จำลองการ Render Component\n    render(&lt;Counter /&gt;);\n    \n    // 2. ค้นหาข้อความ (Query)\n    // getByText จะหา Element ที่มีข้อความตรงเป๊ะ (ถ้าไม่เจอ จะโยน Error ทันที โค้ดเทสพัง)\n    const heading = screen.getByText("Count: 0");\n    \n    // 3. ตรวจสอบเงื่อนไข (Assert)\n    expect(heading).toBeInTheDocument();\n  });\n\n  // เทสเคสที่ 2\n  it("เพิ่มค่าขึ้น 1 เมื่อผู้ใช้คลิกปุ่ม", async () =&gt; {\n    // เตรียม user (จำลองผู้ใช้)\n    const user = userEvent.setup();\n    \n    render(&lt;Counter /&gt;);\n    \n    // หาปุ่มที่มีคำว่า "เพิ่ม"\n    const button = screen.getByRole("button", { name: "เพิ่ม" });\n    \n    // สั่งให้ผู้ใช้จำลอง "คลิก" (ต้องใช้ await เพราะการคลิกเป็นเรื่องของเวลา/Event loop)\n    await user.click(button);\n    \n    // ตรวจสอบว่าข้อความเปลี่ยนเป็น 1 แล้วหรือยัง\n    expect(screen.getByText("Count: 1")).toBeInTheDocument();\n  });\n\n});</code></pre><h2>ตัวอย่างที่ 2: การเทส Data Fetching (API Mocking)</h2><p>เมื่อ Component เรามีการยิง API ไปที่ Server จริงๆ (เช่น ใน `useEffect`) เราจะ <strong>ไม่ยิง API จริงตอนทำเทสเด็ดขาด!</strong> เพราะมันอาจจะช้า เน็ตพัง หรือทำข้อมูลฝั่ง Server เสียหาย เราจะใช้เทคนิคการ "ดักจับและปลอมข้อมูล (Mocking)" ซึ่งเรานิยมใช้ <strong>MSW (Mock Service Worker)</strong> หรือใช้ <code>vi.mock()</code> (ใน Vitest)</p><pre><code>// ตัวอย่างแบบคอนเซปต์\nit("แสดงรายการผู้ใช้หลังจากโหลดข้อมูลเสร็จ", async () =&gt; {\n  // 1. สั่งปลอม API ก่อน\n  mockApiToReturn([{ id: 1, name: "Somchai" }]);\n  \n  render(&lt;UserList /&gt;);\n  \n  // 2. ตอนแรกต้องขึ้น Loading\n  expect(screen.getByText("Loading...")).toBeInTheDocument();\n  \n  // 3. รอจนกว่าคำว่า Somchai จะโผล่ขึ้นมา (findBy เป็น Async ใช้สำหรับรอของที่ยังไม่มา)\n  const userItem = await screen.findByText("Somchai");\n  \n  // 4. ตรวจสอบว่าโผล่มาจริงๆ\n  expect(userItem).toBeInTheDocument();\n});</code></pre><div class="tip-box"><strong>💡 Pro Tip:</strong> มือใหม่มักจะพยายาม <em>"เขียนเทสให้ครอบคลุม 100% (100% Coverage)"</em> ซึ่งเป็นกับดักที่กินเวลาชีวิตมาก ในความจริง จงโฟกัสที่การเทส "เส้นทางหลักที่สำคัญที่สุดของแอป (Happy Path)" เช่น ระบบล็อกอิน ระบบจ่ายเงิน หรือส่วนที่มีการคำนวณซับซ้อน ส่วน Component นิ่งๆ ที่มีแค่ UI สวยๆ อาจไม่ต้องเขียนเทสเลยก็ได้</div><p>การเขียนเทสเป็นทักษะที่บริษัทระดับโลก (Tech Giants) ต้องการอย่างมาก ในบทสุดท้ายของคอร์สนี้ เราจะรวบรวมความรู้ทั้งหมด และพูดถึง "สถาปัตยกรรม" ในการจัดวางไฟล์ (Folder Structure) และการเตรียมแอปพลิเคชันขึ้นสู่ระบบจริง (Deployment)</p>'
  },
  {
    number: 15,
    slug: 'architecture',
    emoji: '🏗️',
    title: 'Architecture & Deployment: สร้างบ้านให้แข็งแรงและพรีเซนต์ให้โลกเห็น',
    content: '<h2>ปัญหาการจัดเรียงโฟลเดอร์ (Folder Structure)</h2><p>เมื่อคุณเรียนรู้เครื่องมือทุกอย่างของ React แล้ว ปัญหาคลาสสิกที่สุดที่ทุกคนต้องเจอเมื่อเริ่มโปรเจกต์จริงคือ: <em>"แล้วฉันควรเอาไฟล์เหล่านี้ไปวางไว้ตรงไหนดี?"</em> React ไม่เคยวางกฎเกณฑ์เรื่องนี้ (เป็น Unopinionated) ปล่อยให้คุณอิสระเต็มที่ ซึ่งมักจะนำไปสู่ความหายนะเมื่อโปรเจกต์มีขนาดใหญ่ขึ้น</p><p>โครงสร้างโฟลเดอร์แบบ "Feature-driven (จัดตามฟีเจอร์)" เป็นโครงสร้างที่ได้รับการยอมรับว่าดูแลรักษาง่ายที่สุดในระยะยาว (ดีกว่าการจับแยกประเภทไฟล์เช่น แยก components ไว้โฟลเดอร์นึง แยก hooks ไว้อีกโฟลเดอร์นึง)</p><h3>โครงสร้างแนะนำ (Best Practice สำหรับแอปขนาดกลาง-ใหญ่)</h3><pre><code>src/\n├── assets/        # รูปภาพ, ฟอนต์, ไอคอน\n├── components/    # Component ส่วนกลางที่ใช้หลายๆ ที่ (เช่น Button, Modal, Input)\n├── config/        # ตัวแปรระบบ (เช่น Axios instance, ค่า Constants)\n├── context/       # Global State Contexts (เช่น AuthContext, ThemeContext)\n├── hooks/         # Custom Hooks ส่วนกลาง (เช่น useWindowSize, useLocalStorage)\n├── layouts/       # โครงร่างของเพจ (เช่น MainLayout, AuthLayout)\n├── pages/         # Component ที่เป็น "หน้าเว็บ" (ผูกกับ React Router)\n│   ├── Home.jsx\n│   └── NotFound.jsx\n├── features/      # ✨ พระเอกของเรา! แบ่งโค้ดตาม "ฟีเจอร์"\n│   ├── auth/      # ฟีเจอร์ล็อกอิน\n│   │   ├── components/  # Component ที่ใช้เฉพาะในระบบล็อกอิน\n│   │   ├── hooks/       # Hooks เฉพาะล็อกอิน (เช่น useLogin)\n│   │   ├── utils/       # ฟังก์ชันช่วยเฉพาะล็อกอิน (เช่น validateToken)\n│   │   └── api/         # โค้ดเรียก API ส่วนล็อกอิน\n│   ├── products/  # ฟีเจอร์สินค้า\n│   │   ├── components/  # (เช่น ProductCard, ProductList)\n│   │   └── ...\n│   └── cart/      # ฟีเจอร์ตระกร้า\n├── utils/         # ฟังก์ชันช่วยเหลือส่วนกลาง (เช่น formatCurrency, formatDate)\n├── App.jsx\n└── main.jsx</code></pre><p>ด้วยโครงสร้างนี้ เมื่อมีคนใหม่เข้ามาร่วมทีม และได้รับมอบหมายให้แก้บักเรื่องตะกร้าสินค้า เขาแค่เดินเข้าไปที่โฟลเดอร์ <code>src/features/cart</code> เขาก็จะเจอทุกสิ่งที่เกี่ยวข้องกับตะกร้าอยู่รวมกัน โดยไม่ต้องวิ่งหาไฟล์ทั่วทั้งโปรเจกต์!</p><h2>การบิลด์โค้ดเตรียมขึ้นโปรดักชัน (Build for Production)</h2><p>ขณะที่คุณกำลังรัน <code>npm run dev</code> (หรือ start) โค้ดของ React จะอยู่ในหมวด "Development" ซึ่งเต็มไปด้วยเครื่องมือช่วยดีบัก ข้อความเตือน (Warnings) และไฟล์ขนาดใหญ่มาก</p><p>ก่อนที่เราจะเอาเว็บขึ้นโฮสต์จริง เราต้อง "บีบอัด" และ "แปลง" โค้ดให้อยู่ในเวอร์ชันที่ดีที่สุด เร็วที่สุด โดยใช้คำสั่ง:</p><pre><code>npm run build</code></pre><p>คำสั่งนี้ (ไม่ว่าคุณจะใช้ Vite หรือ CRA) จะทำการ:<br/>1. ลบโค้ดช่วยดีบักทิ้งทั้งหมด<br/>2. ย่อขนาดไฟล์ JavaScript และ CSS ให้เล็กที่สุด (Minification)<br/>3. นำโค้ดทั้งหมดไปแพ็กเป็นไฟล์ไม่กี่ไฟล์ แล้วไปใส่ไว้ในโฟลเดอร์ที่ชื่อว่า <strong><code>dist/</code> (หรือ <code>build/</code>)</strong></p><p>โฟลเดอร์ <code>dist/</code> นี้แหละ คือสิ่ง <strong>เดียว</strong> ที่คุณจะนำไปโยนใส่ Server (ไม่ต้องเอาโฟลเดอร์ src, node_modules หรือไฟล์อื่นๆ ไปเลย)</p><h2>การอัปโหลดขึ้นเซิร์ฟเวอร์จริง (Deployment)</h2><p>เนื่องจาก React ที่เราบิลด์เสร็จแล้ว (ในโฟลเดอร์ dist) เป็นเพียงไฟล์ <strong>Static HTML/CSS/JS ธรรมดา</strong> คุณจึงสามารถนำมันไปฝากไว้ที่ Hosting ที่รองรับ Static Files ที่ไหนก็ได้ในโลก! (ซึ่งส่วนใหญ่ ฟรี! สำหรับโปรเจกต์ขนาดเล็ก)</p><h3>ทางเลือกยอดนิยมในการ Deploy:</h3><ol style="padding-left:20px"><li><strong>Vercel (👑 แนะนำที่สุด):</strong> เป็น Hosting ที่เกิดมาเพื่อ React และ Next.js แค่ผูก GitHub ของคุณเข้ากับ Vercel มันจะทำการดึงโค้ดไป Build และ Deploy ให้อัตโนมัติทุกครั้งที่คุณ <code>git push</code> (CI/CD)</li><li><strong>Netlify:</strong> คู่แข่งหลักของ Vercel ใช้งานง่ายพอกันและได้รับความนิยมสูงมากในกลุ่มคนทำ SPA</li><li><strong>GitHub Pages / Cloudflare Pages:</strong> ฟรีและเร็วมาก เหมาะกับเว็บไซต์แบบนำเสนอประวัติ (Portfolio)</li></ol><div class="tip-box"><strong>💡 Pro Tip: การแก้ปัญหา 404 (Not Found) บน Production:</strong><br/>เมื่อคุณ Deploy React Router SPA ขึ้น Server จริง คุณมักจะเจอปัญหาที่ว่า: ถ้าผู้ใช้เข้าหน้า <code>yourweb.com/about</code> โดยตรง พิมพ์ URL แล้วกด Enter บราวเซอร์จะแสดง Error 404 <br/>สาเหตุเพราะ Server พยายามควานหาไฟล์ <code>about.html</code> ซึ่งมันไม่มี! (เรามีแต่ <code>index.html</code>) วิธีแก้คือ คุณต้องไปตั้งค่าที่ Hosting ของคุณว่า <strong>"ถ้าหาไฟล์ใดๆ ไม่เจอ ให้ชี้กลับมาที่ <code>index.html</code> เสมอ"</strong> (เช่น บน Vercel ให้สร้างไฟล์ <code>vercel.json</code> ตั้งค่า "rewrites") แล้ว React Router จะรับช่วงต่อในการพาไปยัง Component ที่ถูกต้องเอง</div><h2>บทสรุป</h2><p>ขอแสดงความยินดีด้วย! คุณได้เดินทางมาถึงตอนจบของคอร์ส "React จากศูนย์" แล้ว จากคนที่อาจจะเคยสับสนว่า JSX คืออะไร วันนี้คุณได้เข้าใจทั้ง Components, State, Hooks ชั้นสูง, การดึงข้อมูล, การทำ Routing ไปจนถึงสถาปัตยกรรมระดับโปรเจกต์จริง</p><p>แต่อย่างที่ทราบกันดี วงการ Frontend ไม่เคยหยุดนิ่ง เส้นทางต่อไปที่คุณควรศึกษาต่อยอด ได้แก่ <strong>React Query (สำหรับการจัดการ Server State)</strong>, <strong>Zustand/Redux (สำหรับ Global State)</strong> และแน่นอน <strong>Next.js (Framework ที่ครอบ React อีกทีเพื่อทำ SSR/SEO)</strong></p><p>ขอให้สนุกกับการสร้างสรรค์แอปพลิเคชันด้วย React! Happy Coding! 🚀</p>'
  }
];
