export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '🧱',
    title: 'Databricks คืออะไร และ Lakehouse Architecture',
    content: '<h2>Databricks คืออะไร?</h2><p>Databricks คือ cloud data platform ที่ founded โดยผู้สร้าง Apache Spark เอง เป็น unified analytics platform ที่รวม Data Engineering, Data Science, และ Machine Learning ไว้ด้วยกัน Databricks ทำงานบน AWS, Azure, และ GCP ทำให้ใช้งานได้กับ cloud ที่องค์กรมีอยู่แล้ว</p><p>Databricks แก้ปัญหาหลักของ Big Data ecosystem ที่ซับซ้อนมาก: แทนที่จะ manage Spark cluster เอง, configure Hadoop, ติดตั้ง libraries ต่างๆ Databricks จัดการ infrastructure ทั้งหมดให้ ให้ทีม Data focus ที่ business value แทนที่จะเสียเวลากับ infrastructure</p><h2>Lakehouse Architecture</h2><p>Lakehouse คือ architecture ใหม่ที่รวมข้อดีของ Data Lake (cheap storage, flexible schema, supports all data types) กับ Data Warehouse (ACID transactions, performance optimization, BI-ready) ไว้ด้วยกัน แก้ปัญหา data quality และ governance ของ pure data lakes</p><p>Databricks implement Lakehouse ด้วย Delta Lake: open-source storage layer ที่เพิ่ม ACID transactions, schema enforcement, และ time travel บน Parquet files บน cloud storage (S3, ADLS, GCS) ทำให้ใช้ cloud storage ราคาถูกได้พร้อม Data Warehouse features</p><h2>ส่วนประกอบหลักของ Databricks</h2><p>Databricks Workspace: IDE สำหรับ notebooks, repos, และ workflows, Delta Lake: storage layer ที่ enable Lakehouse, Unity Catalog: data governance platform, MLflow: ML experiment tracking, Model Registry, Model Serving, Databricks SQL: BI-focused query engine สำหรับ analysts</p><p>Databricks Runtime: optimized Spark distribution ที่เร็วกว่า open-source Spark ถึง 5x บน benchmark tests รวม Photon Engine ที่ rewrite query execution ใน C++ สำหรับ SQL workloads และมี Delta Lake, MLflow, optimized libraries pre-installed</p><h2>ทำไมองค์กรเลือก Databricks</h2><p>Cost efficiency: ไม่มี license fee สำหรับ compute (pay ตาม cloud compute) managed service ลด operational overhead ของ data team Photon engine ลด compute time ทำให้ cost ต่ำลง unified platform ลด complexity ของ tech stack</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ Databricks Community Edition ฟรีสำหรับ learning และ personal projects ได้ cluster ขนาดเล็กสำหรับ exploration พอสำหรับ dataset ขนาดกลาง ไม่มี time limit และไม่ expire ทำให้ practice ได้ต่อเนื่อง</div><p>Databricks ในตลาด: ได้รับความไว้วางใจจาก 10,000+ organizations รวม Comcast, Shell, Regeneron, NBCUniversal Databricks raise $1.6B ใน 2021 ด้วย valuation $38B ทำให้เป็น VC-backed company ที่ stable และ well-funded สำหรับ long-term investment</p>'
  },
  {
    number: 1,
    slug: 'workspace',
    emoji: '💻',
    title: 'Databricks Workspace และ Notebooks',
    content: '<h2>Databricks Workspace</h2><p>Workspace คือ environment ที่ทีม Data ทำงานร่วมกัน มี folder structure สำหรับ organize notebooks, libraries, experiments, และ workflows การ share notebooks ง่ายมาก สามารถ comment บน cells, view revision history, และ work simultaneously กับ team members</p><p>Workspace hierarchy: Workspace root → Users folder (personal space ของแต่ละคน) → Shared folder (team resources) → Repos (Git-connected notebooks) ควร organize project code ใน Repos แทน Workspace folders เพราะ version controlled และ traceable</p><h2>Notebooks ใน Databricks</h2><p>Notebooks รองรับ Python, Scala, SQL, R และสามารถ mix languages ใน notebook เดียวกัน ด้วย %python, %sql, %scala magic commands ทำให้ analyst เขียน SQL ใน notebook เดียวกับที่ engineer เขียน PySpark ได้โดยไม่ต้อง switch tools</p><p>Notebook features: auto-complete, variable inspector, revision history, comment threads ต่อ cell, realtime collaboration (multiple users edit พร้อมกัน), built-in data visualization, และ widgets สำหรับ parameterized notebooks</p><h2>Databricks Widgets</h2><p>Widgets สร้าง interactive parameters สำหรับ notebooks: dbutils.widgets.text(\'date\', \'2024-01-01\') สร้าง text input, dbutils.widgets.dropdown(\'env\', \'prod\', [\'dev\', \'staging\', \'prod\']) สร้าง dropdown ทำให้ notebooks reusable ข้าม dates/environments</p><p>ดึง widget values: dbutils.widgets.get(\'date\') ใช้ใน notebook code หรือ SQL queries ด้วย ${date} syntax Widgets ใช้งานได้ทั้งใน interactive notebooks และ scheduled jobs ทำให้ parameterize execution ได้ง่าย</p><h2>Revision History และ Collaboration</h2><p>Databricks เก็บ revision history ของทุก notebooks สามารถ compare versions ต่างๆ และ restore เวอร์ชันเก่าได้ Comments บน cells ช่วย code review รับ notifications เมื่อมี replies ทำให้ async collaboration ทำงานได้ดีสำหรับ distributed teams</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ Databricks Repos เพื่อ connect workspace กับ GitHub/GitLab ทำให้ version control notebooks เหมือน code files สามารถ create branches, open PRs, และ collaborate ด้วย standard Git workflow ดีกว่า workspace folders มากสำหรับ production code</div><p>Notebook ที่ดีควรมี: documentation cell อธิบาย purpose, required libraries, expected inputs/outputs, ไม่ควร hardcode credentials หรือ sensitive values ใช้ dbutils.secrets แทน ทำให้ notebook safe ที่จะ share กับ team members</p>'
  },
  {
    number: 2,
    slug: 'clusters',
    emoji: '⚡',
    title: 'Clusters: All-Purpose vs Job Clusters',
    content: '<h2>Cluster Types ใน Databricks</h2><p>Databricks มี cluster types หลัก 2 ประเภท: All-Purpose Clusters (interactive clusters สำหรับ development) และ Job Clusters (สร้างอัตโนมัติสำหรับ job แล้วหยุดเมื่อ job เสร็จ) ทำให้ balance ระหว่าง developer experience และ cost optimization</p><p>All-Purpose Cluster: ใช้สำหรับ interactive development บน notebooks หลายคนสามารถ attach notebooks หลายอันไปยัง cluster เดียวกันได้ ไม่ควร terminate บ่อยเพราะ startup นานกว่า 5 นาที มีค่าใช้จ่ายสูงกว่า job clusters</p><h2>Job Clusters</h2><p>Job Clusters สร้างขึ้นเฉพาะเมื่อ job run และหยุดอัตโนมัติเมื่อเสร็จ ประหยัด cost มากเพราะ pay เฉพาะตอนทำงาน recommended สำหรับ production scheduled jobs ทุกตัว ข้อเสียคือ startup time ทำให้ job ช้ากว่า 5-10 นาที</p><p>Job Cluster configuration: กำหนด Databricks Runtime version, worker type, number of workers, auto-scaling settings, spark config, init scripts ควร pin Databricks Runtime version ใน job config เพื่อ reproducibility</p><h2>Auto-scaling Clusters</h2><p>Auto-scaling ปรับ number of workers ตาม workload อัตโนมัติ ตั้ง min และ max workers Databricks จะ scale up เมื่อ tasks queue และ scale down เมื่อ cluster idle ประหยัด cost เมื่อ workload variable</p><p>Enhanced Auto-scaling: Databricks-specific feature ที่ดูแลทั้ง scale up/down อย่าง aggressive ยิ่งขึ้น ดีกว่า standard Spark auto-scaling เพราะ Databricks มี insight เรื่อง workload pattern ที่ standard Spark ไม่มี</p><h2>Cluster Configuration Best Practices</h2><p>เลือก instance type ตาม workload: Memory-optimized สำหรับ joins/aggregations ขนาดใหญ่, Compute-optimized สำหรับ CPU-intensive ML training, Storage-optimized สำหรับ HDFS workloads ใน single-node mode ใช้ node เดียวสำหรับ development ด้วย datasets เล็กๆ ประหยัด cost</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ตั้ง auto-termination บน All-Purpose clusters เช่น 2 ชั่วโมง ป้องกัน cluster รันทิ้งไว้เมื่อไม่ได้ใช้ งาน cluster costs เป็นค่าใช้จ่ายหลักใน Databricks ควร monitor และ alert เมื่อ monthly cost เกิน budget</div><p>Spot instances: ใช้ spot/preemptible instances สำหรับ worker nodes ลด compute cost ได้ 60-80% ตั้งให้ driver node เป็น on-demand และ workers เป็น spot ถ้า spot instances ถูก reclaim Databricks จะ reschedule tasks อัตโนมัติ</p>'
  },
  {
    number: 3,
    slug: 'delta-lake',
    emoji: '🏞️',
    title: 'Delta Lake: ACID Transactions บน Data Lake',
    content: '<h2>Delta Lake คืออะไร?</h2><p>Delta Lake คือ open-source storage layer ที่ add ACID transactions บน Parquet files ที่เก็บบน cloud storage Delta tables ดูเหมือน Parquet files ธรรมดาแต่มี transaction log (_delta_log) ที่ track ทุก changes Delta Lake เป็น core component ของ Databricks Lakehouse</p><p>ปัญหาของ plain Parquet ที่ Delta Lake แก้: ไม่มี ACID transactions ทำให้ concurrent reads/writes อาจได้ data ที่ inconsistent, ไม่มี schema enforcement ทำให้ bad data แอบ sneak in, ไม่มี history ทำให้ undo mistakes ยาก</p><h2>ACID ใน Delta Lake</h2><p>Atomicity: write ทั้งหมดหรือไม่ทำเลย ถ้า job fail ระหว่าง write data จะไม่ partial corrupt Consistency: schema validation enforce ก่อน write Isolation: concurrent operations ไม่ conflict กัน Durability: committed data persist แม้ระบบ fail transaction log ทำให้ guarantee ทั้งหมดนี้</p><p>Transaction Log: เก็บใน _delta_log/ directory มี JSON files แต่ละ file แทน transaction หนึ่งครั้ง ทำให้ query ว่า table state เป็นอะไรในแต่ละ version ได้ เป็น foundation ของ time travel feature</p><h2>Writing Delta Tables</h2><p>df.write.format(\'delta\').mode(\'overwrite\').save(\'/path/to/table\') หรือ df.write.format(\'delta\').saveAsTable(\'my_database.my_table\') สร้าง managed table ใน metastore Delta tables รองรับ CREATE TABLE ด้วย SQL syntax เหมือน traditional database</p><p>Merge (UPSERT): DeltaTable.forPath(spark, path).alias(\'target\').merge(source.alias(\'source\'), \'target.id = source.id\').whenMatchedUpdateAll().whenNotMatchedInsertAll().execute() ทำ upsert atomic ไม่ต้องกังวลเรื่อง duplicates</p><h2>Time Travel</h2><p>อ่าน Delta table ณ version ใดก็ได้: spark.read.format(\'delta\').option(\'versionAsOf\', 5).load(path) หรือ option(\'timestampAsOf\', \'2024-01-15\') ใช้สำหรับ audit, reproduce results, rollback data errors ทำให้ data recovery ง่ายมาก</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> รัน OPTIMIZE บน Delta tables สม่ำเสมอเพื่อ compact small files: OPTIMIZE my_table ZORDER BY (date, customer_id) Z-ORDER clustering ช่วยให้ queries ที่ filter บน date และ customer_id เร็วขึ้น 10x+ โดยเฉพาะบน large tables</div><p>Delta table maintenance: รัน VACUUM เพื่อลบ old file versions ที่ไม่ต้องการ: VACUUM my_table RETAIN 168 HOURS ระวัง: หลัง VACUUM จะ time travel ย้อนกลับไปไม่ได้ ควร retain อย่างน้อย 7 วันสำหรับ production tables</p>'
  },
  {
    number: 4,
    slug: 'unity-catalog',
    emoji: '📚',
    title: 'Unity Catalog: Data Governance',
    content: '<h2>Unity Catalog คืออะไร?</h2><p>Unity Catalog คือ centralized governance solution สำหรับ data, AI assets, และ compute ใน Databricks ทำให้จัดการ access control, audit, lineage, และ discovery ใน single place ข้าม workspaces และ clouds แทนที่จะมี access control กระจัดกระจายใน multiple tools</p><p>ก่อน Unity Catalog: access control ทำผ่าน workspace-level table ACLs ที่ manage แยกแต่ละ workspace, ไม่มี cross-workspace sharing, ไม่มี fine-grained column-level security, audit trail ไม่ครอบคลุม Unity Catalog แก้ปัญหาทั้งหมดนี้ใน single governance layer</p><h2>3-Level Namespace</h2><p>Unity Catalog ใช้ 3-level namespace: Catalog.Schema.Table เช่น prod.sales.orders Catalog คือ top-level container (เหมือน database instance), Schema คือ namespace ใน catalog (เหมือน schema ใน Postgres), Table คือ table หรือ view ทำให้ organize data เป็น hierarchy ได้</p><p>สร้าง catalog: CREATE CATALOG finance สร้าง schema: CREATE SCHEMA finance.reporting สร้าง table: CREATE TABLE finance.reporting.daily_summary ทำให้ separate concerns ระหว่าง teams (finance team ดู finance catalog เท่านั้น)</p><h2>Access Control</h2><p>GRANT และ REVOKE ใน SQL: GRANT SELECT ON TABLE catalog.schema.table TO principal ทำ fine-grained control ได้ถึง column level: GRANT SELECT (customer_name, order_total) ON TABLE orders TO analyst_role ซ่อน sensitive columns เช่น email, phone จาก users ที่ไม่มีสิทธิ์</p><p>Row-level security: ใช้ Row Filters ที่ define function สำหรับ filter rows ตาม user attributes เช่น ทุกคนเห็นเฉพาะ rows ที่ country ตรงกับ country ของตัวเอง ทำให้ multi-tenant data sharing ปลอดภัย</p><h2>Data Lineage และ Discovery</h2><p>Unity Catalog track lineage อัตโนมัติ ทำให้เห็น data flow จาก source table ไปยัง downstream tables และ dashboards column-level lineage สำหรับ SQL transformations Data explorer ใน Workspace ใช้ browse tables, ดู schema, sample data, และ lineage โดยไม่ต้องเขียน code</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ Tags ใน Unity Catalog เพื่อ label tables ด้วย business metadata เช่น contains_pii, domain=marketing, data_quality=certified ทำให้ data governance automation ทำงานตาม tags เช่น auto-apply masking rules บน tables ที่ tagged contains_pii</div><p>Shares and Recipients ใน Delta Sharing: share data กับ external organizations หรือ partners โดยไม่ต้อง copy data Recipients ได้รับ credential สำหรับ read data ผ่าน Delta Sharing protocol ที่ open ทำให้ data sharing ปลอดภัยและ auditable</p>'
  },
  {
    number: 5,
    slug: 'workflows',
    emoji: '🔄',
    title: 'Databricks Workflows และ Jobs',
    content: '<h2>Databricks Workflows คืออะไร?</h2><p>Databricks Workflows คือ orchestration engine ในตัวสำหรับ schedule และ run notebooks, Python scripts, Delta Live Tables, SQL queries, และ external services ทำให้สร้าง complex multi-step pipelines โดยไม่ต้องใช้ Airflow แยกต่างหาก เหมาะสำหรับ simpler orchestration needs</p><p>Job คือ collection ของ tasks ที่ run บน schedule หรือ trigger Workflows UI ให้ configure dependencies ระหว่าง tasks, กำหนด retry policies, ตั้ง email/Slack notifications, ดู run history, และ rerun failed tasks ทำให้ pipeline management ง่ายขึ้นมาก</p><h2>สร้าง Job และ Tasks</h2><p>สร้าง Job ผ่าน UI หรือ Databricks CLI/REST API แต่ละ task กำหนด: task type (Notebook, Python script, Delta Live Tables, SQL, dbt), cluster (new job cluster หรือ existing cluster), parameters, retry policy ตั้ง task dependencies ด้วย Depends on ทำให้ task B รอ task A complete ก่อน</p><p>Job parameters: ส่ง parameters ไปยัง notebooks ผ่าน widgets, ส่งไปยัง Python scripts ผ่าน command-line arguments ใช้ dynamic values เช่น {{job.start_time}} หรือ {{job.run_id}} ที่ Databricks inject ให้อัตโนมัติ</p><h2>Retry Policies</h2><p>ตั้ง max_retries และ retry_on_timeout สำหรับแต่ละ task ระบุ min_retry_interval_millis เพื่อ implement exponential backoff เหมาะสำหรับ tasks ที่ depend on external services ที่อาจ unavailable ชั่วคราว ป้องกัน thundering herd</p><p>Task timeout: ตั้ง timeout_seconds สำหรับแต่ละ task ป้องกัน task ค้างนานจนเกินไปและ waste resources ถ้า task ใช้เวลานานกว่า expected ควรตรวจสอบ data quality issues หรือ performance regressions</p><h2>Monitoring Jobs</h2><p>Job run history แสดง: duration ของแต่ละ run, success/failure status, start/end time, task-level details ดู trends เพื่อ identify performance degradation สัญญาณเตือนภัย: run time เพิ่มขึ้น consistent อาจหมายถึง data volume เพิ่มหรือ query regression</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ Databricks CLI กับ YAML config files เพื่อ manage jobs as code: databricks jobs create --json @job_config.json store config ใน Git เพื่อ version control และ deploy ผ่าน CI/CD pipeline แทนที่จะ configure ผ่าน UI ที่ไม่ traceable</div><p>Multi-task Workflows: สร้าง complex DAG ของ tasks ด้วย for-each tasks ที่ iterate ข้าม list ของ values, if-else task branching ตาม conditions, และ repair run ที่ rerun เฉพาะ failed tasks ไม่ต้อง rerun ทั้งหมดประหยัด time และ cost</p>'
  },
  {
    number: 6,
    slug: 'delta-live-tables',
    emoji: '🏭',
    title: 'Delta Live Tables: Declarative Pipelines',
    content: '<h2>Delta Live Tables (DLT) คืออะไร?</h2><p>Delta Live Tables เป็น declarative framework สำหรับ building reliable data pipelines บน Databricks แทนที่จะเขียน imperative code (รัน step 1, step 2, step 3) คุณ declare ว่าแต่ละ table ควรมี data อะไร DLT จัดการ execution order, error handling, data quality, และ monitoring ให้</p><p>DLT ทำให้ data engineering ง่ายขึ้นมาก: ไม่ต้อง manage dependencies manually, ไม่ต้อง handle schema changes, ไม่ต้อง implement retry logic, มี built-in data quality monitoring ด้วย Expectations ทำให้ engineer focus ที่ business logic</p><h2>DLT Syntax</h2><p>ใช้ @dlt.table decorator บน Python functions หรือ CREATE LIVE TABLE ใน SQL function return DataFrame ที่ represent table content DLT infer dependencies จาก dlt.read() calls และสร้าง execution order ที่ถูกต้องอัตโนมัติ</p><p>Live tables (complete recomputation) vs Streaming Live Tables (incremental processing) ตัวอย่าง: @dlt.table def silver_orders(): return dlt.read(\'bronze_orders\').filter(col(\'status\') != \'cancelled\') DLT จัดการ materialization และ scheduling ให้ทั้งหมด</p><h2>Data Quality ด้วย Expectations</h2><p>DLT Expectations กำหนด data quality rules: @dlt.expect(\'valid_order_total\', \'order_total > 0\') expect_or_drop ลบ rows ที่ fail, expect_or_fail หยุด pipeline ถ้า fail, expect รายงาน violations แต่ยังดำเนินการต่อ ทำให้ data quality monitoring เป็นส่วนหนึ่งของ pipeline โดยอัตโนมัติ</p><p>Expectations metrics ถูก track ใน pipeline run history ทำให้เห็น trends ของ data quality ได้ เมื่อ violation rate เพิ่มขึ้น อาจหมายถึง upstream data quality มีปัญหาที่ต้อง investigate</p><h2>Bronze-Silver-Gold Architecture</h2><p>DLT เหมาะมากสำหรับ implement medallion architecture: Bronze (raw ingest ไม่แก้ไข), Silver (clean, validated, enriched), Gold (business-ready aggregated) แต่ละ layer เป็น DLT table DLT manage data flow และ dependencies ระหว่าง layers อัตโนมัติ</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ DLT Enhanced Autoscaling เพื่อ optimize cluster size ตาม workload DLT รู้ว่า pipeline มี stages ที่ parallelism สูงและต่ำ ทำให้ scale appropriately ลด cost เมื่อเทียบกับ fixed-size cluster ได้มากในระยะยาว</div><p>DLT Pipeline Monitoring: Databricks UI แสดง pipeline graph, data flow, row counts, error counts สำหรับแต่ละ table ตาม run ทำให้ debug data issues ง่าย ดูได้ว่า records หายไปที่ stage ไหนหรือ expectation violations เกิดขึ้นที่ table ไหน</p>'
  },
  {
    number: 7,
    slug: 'mlflow',
    emoji: '🧪',
    title: 'MLflow: Experiment Tracking และ Model Registry',
    content: '<h2>MLflow คืออะไร?</h2><p>MLflow คือ open-source platform สำหรับ manage ML lifecycle ประกอบด้วย: Tracking (log experiments, parameters, metrics, artifacts), Models (package models ในรูปแบบ standard), Registry (manage model versions และ deployment), Projects (reproducible ML code) Databricks integrate MLflow อย่าง seamless</p><p>ปัญหาที่ MLflow แก้: นักวิทยาศาสตร์ข้อมูลมักไม่รู้ว่า model version ไหนใช้ parameters อะไร ได้ metrics อะไร ทำให้ reproduce results ยาก MLflow บันทึกทุกอย่างอัตโนมัติทำให้ experiments reproducible และ comparable</p><h2>MLflow Tracking</h2><p>เริ่ม experiment: mlflow.start_run() log parameters: mlflow.log_param(\'learning_rate\', 0.01) log metrics: mlflow.log_metric(\'accuracy\', 0.95) log artifacts: mlflow.log_artifact(\'model.pkl\') ใช้ context manager ด้วย with mlflow.start_run() เพื่อ auto-close run</p><p>Auto-logging: mlflow.autolog() หรือ mlflow.sklearn.autolog() log parameters, metrics, models อัตโนมัติโดยไม่ต้องเขียน explicit log calls รองรับ scikit-learn, XGBoost, LightGBM, TensorFlow, PyTorch ลด boilerplate code มาก</p><h2>Model Registry</h2><p>Register model: mlflow.register_model(\'runs:/run_id/model_name\', \'ProductionModel\') Model Registry มี stages: Staging (testing), Production (serving), Archived (deprecated) ทีม ML ops manage transitions ระหว่าง stages ผ่าน approval workflow</p><p>Model versioning: ทุก registered model สร้าง version ใหม่อัตโนมัติ ทำให้ rollback ง่ายถ้า new version มีปัญหา Alias ทำให้ point ไปยัง version ที่ต้องการโดยไม่ต้อง hardcode version number ใน code</p><h2>Model Serving บน Databricks</h2><p>Deploy registered model เป็น REST API ได้ทันทีด้วย Databricks Model Serving (Mosaic AI Model Serving) serverless, auto-scales จาก 0, ใช้ Databricks compute เพื่อ serve predictions เหมาะสำหรับ real-time inference</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ตั้ง Experiment name อย่างเป็นระบบ เช่น /Users/username/project/experiment_name หรือ /Shared/team/project/ ใน Databricks workspace ทำให้ organize experiments ต่อ project และ find experiments เก่าได้ง่ายแม้ผ่านไปหลายเดือน</div><p>MLflow Projects: package ML code ด้วย MLproject file ระบุ entry points, parameters, environment (conda, docker) ทำให้ run project ด้วย mlflow run . พร้อม parameter overrides เหมาะสำหรับ reproducible training pipelines ที่ทีม reuse ได้</p>'
  },
  {
    number: 8,
    slug: 'databricks-sql',
    emoji: '📊',
    title: 'Databricks SQL สำหรับ Analytics',
    content: '<h2>Databricks SQL คืออะไร?</h2><p>Databricks SQL (DBSQL) คือ SQL Analytics layer ใน Databricks ที่ออกแบบสำหรับ data analysts และ BI users ใช้ SQL Warehouses (Serverless หรือ Classic) แทน Spark Clusters ทำให้ query เร็วขึ้นและ cost-effective สำหรับ SQL workloads Photon engine ทำงานอยู่เบื้องหลัง</p><p>DBSQL รองรับ standard SQL syntax เหมือน Snowflake/Redshift ทำให้ analysts ย้ายมาจาก traditional DWH ได้ง่าย มี Query Editor ที่สะดวก, สร้าง Dashboards ในตัว, และ integrate กับ BI tools เช่น Tableau, Power BI, Looker</p><h2>SQL Warehouses</h2><p>SQL Warehouses แทน Spark Clusters สำหรับ SQL workloads มี serverless option ที่ start ใน วินาที (ไม่ต้องรอ cluster boot) auto-suspend เมื่อ idle auto-scale ตาม concurrent queries Classic Warehouses ใช้ customer cloud account จึง flexible กว่า</p><p>Warehouse sizing: X-Small สำหรับ light development, Medium สำหรับ production queries ทั่วไป, Large สำหรับ heavy concurrent workloads Databricks แนะนำ Serverless สำหรับ cost optimization เพราะ pay-per-query ไม่มี minimum uptime</p><h2>Query Editor และ History</h2><p>Query Editor มี: syntax highlighting, schema browser ดู tables/columns, execution time, data preview, download results Query History เก็บ queries ที่รันไปทุกครั้งพร้อม execution metrics ใช้ debug slow queries ดู execution plan โดยไม่ต้อง explain ในหน้า Editor</p><p>Saved Queries และ Dashboards: save queries เพื่อ share กับ team ทำ Dashboard รวม visualizations จากหลาย queries ตั้ง refresh schedule สำหรับ dashboards ส่ง snapshot ทาง email ตาม schedule เหมาะสำหรับ daily reports</p><h2>Query Optimization</h2><p>ตรวจสอบ query profile: ดู stages ที่ใช้เวลานาน, data scanned, cache hits ใช้ CACHE TABLE สำหรับ tables ที่ query บ่อย, ใช้ partitioned tables ที่มี filter ที่ partition key ใช้ liquid clustering (ฟีเจอร์ใหม่) แทน ZORDER สำหรับ better performance</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ Predictive I/O ที่ enable โดย default บน Serverless SQL Warehouses ทำให้ Databricks predict ว่า query จะอ่าน files ไหน และ pre-fetch ล่วงหน้า ลด query time ได้ 2-4x สำหรับ repeated query patterns บน Delta tables</div><p>SQL Alerts: ตั้ง alerts บน queries ที่ return threshold ที่กำหนด เช่น แจ้งเมื่อ daily revenue ต่ำกว่า expected, หรือ error rate สูงกว่า 1% send alerts ไปยัง email, Slack, PagerDuty ทำให้ business stakeholders ได้รับแจ้งทันทีเมื่อ KPIs ผิดปกติ</div>'
  },
  {
    number: 9,
    slug: 'streaming',
    emoji: '🌊',
    title: 'Auto Loader: Incremental Ingestion',
    content: '<h2>Auto Loader คืออะไร?</h2><p>Auto Loader คือ Databricks-optimized feature สำหรับ incrementally ingest new files จาก cloud storage (S3, ADLS, GCS) เข้า Delta Lake ทำงาน Structured Streaming ที่ optimize สำหรับ file ingestion โดยเฉพาะ ตรวจสอบ files ใหม่ด้วย cloud notifications แทน directory listing ทำให้ scale ได้ดีกว่ามาก</p><p>ปัญหาของ traditional file ingestion: ต้อง track ว่า files ไหนถูก processed แล้ว ถ้า job fail กลางคัน ต้อง handle duplicates manually ต้อง list ทุก files ใน directory (ช้ามากเมื่อมีหลายล้านไฟล์) Auto Loader แก้ปัญหาเหล่านี้ทั้งหมด</p><h2>ใช้งาน Auto Loader</h2><p>spark.readStream.format(\'cloudFiles\').option(\'cloudFiles.format\', \'json\').option(\'cloudFiles.schemaLocation\', \'/path/to/schema\').load(\'/path/to/source/\') ต่อด้วย writeStream ไปยัง Delta table Auto Loader track ว่า files ไหน processed แล้วและจะไม่ process ซ้ำ</p><p>Schema inference และ evolution: Auto Loader infer schema จากไฟล์ใหม่และ store ใน schemaLocation เมื่อ schema เปลี่ยน (column เพิ่ม) Auto Loader handle การ merge schema อัตโนมัติ ไม่ต้อง stop pipeline และ update schema manually</p><h2>File Notification Mode</h2><p>Auto Loader มี 2 modes: Directory Listing (default สำหรับ testing) และ File Notification (production) File Notification ใช้ cloud event notifications (SQS บน AWS, Event Grid บน Azure) แทน list files จาก directory เร็วกว่าและ scale ได้ดีกว่ามาก</p><p>Setup File Notification: ต้อง grant Databricks permissions เพื่อ setup cloud notifications อัตโนมัติผ่าน Databricks admin configuration หลังจาก setup แล้ว Auto Loader ทำงานได้โดยไม่ต้อง configure เพิ่ม ทำงาน near real-time เมื่อ files ถูก upload</p><h2>Auto Loader ใน DLT</h2><p>รวม Auto Loader กับ Delta Live Tables สำหรับ complete ingestion pipeline: DLT Bronze table ใช้ Auto Loader ingest raw files, Silver table clean และ validate, Gold table aggregate ทุกอย่าง managed โดย DLT ทำให้ end-to-end pipeline ง่ายมาก</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ตั้ง maxFilesPerTrigger option เพื่อ control จำนวนไฟล์ที่ process ต่อ micro-batch ทำให้ batch size predictable: .option(\'maxFilesPerTrigger\', 1000) เหมาะสำหรับ catch-up scenarios ที่มี files backlog และต้องการ process อย่างเป็น systematic</div><p>Rescue data: ใช้ cloudFiles.schemaEvolutionMode=\'rescue\' เพื่อ route columns ที่ไม่ match schema ปัจจุบันไปยัง special _rescued_data column แทนที่จะ fail ทำให้ pipeline ยืดหยุ่นมากขึ้นเมื่อ upstream schema เปลี่ยนบ่อยๆ</p>'
  },
  {
    number: 10,
    slug: 'cost-optimization',
    emoji: '💰',
    title: 'Cost Optimization บน Databricks',
    content: '<h2>ทำความเข้าใจ Databricks Pricing</h2><p>Databricks คิดเงินตาม Databricks Units (DBUs) ซึ่งแตกต่างกันตาม cluster type และ runtime: All-Purpose Compute, Jobs Compute, DLT, SQL Warehouses แต่ละอย่าง DBU rate ต่างกัน บวกกับ cloud compute cost (EC2, GCE) ที่ Databricks ใช้ รวมเป็น total cost</p><p>ตัวอย่าง: All-Purpose clusters แพงกว่า Jobs clusters 2-3 เท่า สำหรับ DBUs เดียวกัน Serverless SQL Warehouses คิดแพงกว่า Classic แต่ startup เร็วกว่ามาก ไม่มี idle cost ทำให้ overall cost ต่ำกว่าสำหรับ sporadic workloads</p><h2>Spot Instances</h2><p>ใช้ Spot/Preemptible instances สำหรับ worker nodes ลด compute cost 60-80%: ตั้ง workers เป็น spot, driver เป็น on-demand เพื่อ stability ถ้า spot instances ถูก terminate Databricks reschedule tasks ไปยัง available spot instances หรือ on-demand</p><p>Databricks Cost Optimization: Autoscaling ลด worker count เมื่อ workload ต่ำ, Auto-termination ปิด clusters ที่ idle, ใช้ Job Clusters แทน All-Purpose สำหรับ scheduled jobs ทุก 3 best practices นี้สามารถลด cost ได้ 40-60%</p><h2>Right-sizing Clusters</h2><p>ทดสอบ workload ด้วย different cluster sizes เพื่อหา sweet spot: เล็กเกินไป → OOM, ช้า, ใหญ่เกินไป → waste resources Ganglia Metrics ใน Spark UI ช่วย monitor CPU, memory utilization จริง ถ้า average CPU utilization ต่ำกว่า 50% cluster ใหญ่เกินไป</p><p>Instance type selection: memory-optimized สำหรับ joins/caching, compute-optimized สำหรับ ML training, storage-optimized ถ้าต้อง cache data มากบน disk ไม่ควรใช้ general-purpose สำหรับ production workloads ที่ optimize ได้</p><h2>Data Caching</h2><p>Databricks Delta Caching: cache Parquet data บน local SSD ของ worker nodes ทำให้ queries เร็วขึ้นมากสำหรับ repeated access โดยไม่ต้อง read จาก remote S3/GCS ทุกครั้ง Enable โดย default บน Storage Optimized instances</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ Databricks Cost Management dashboard เพื่อ track spending ต่อ cluster, user, และ job ตั้ง budget alerts เมื่อ spending เกิน threshold สร้าง chargeback reports ต่อ team เพื่อ incentivize cost-conscious behavior</div><p>Serverless Compute: ใช้ Serverless SQL Warehouses สำหรับ interactive queries ที่ไม่ consistent Serverless ไม่มี startup cost หรือ idle cost จ่ายเฉพาะ query time ประหยัดมากสำหรับ sporadic usage patterns เช่น ad-hoc analysis</p>'
  }
];
