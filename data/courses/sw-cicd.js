export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '🔄',
    title: 'Overview',
    content: '<h2>เริ่มต้นทำความรู้จักกับ CI/CD</h2><p>ในยุคที่การพัฒนาซอฟต์แวร์ต้องแข่งขันกับเวลา การส่งมอบฟีเจอร์ใหม่ๆ ให้ถึงมือผู้ใช้งานอย่างรวดเร็วและมีข้อผิดพลาดน้อยที่สุดจึงเป็นเรื่องสำคัญมาก นี่คือจุดที่ <strong>CI/CD</strong> ก้าวเข้ามามีบทบาทสำคัญในการเปลี่ยนแปลงวิธีการทำงานของทีมพัฒนาซอฟต์แวร์ไปตลอดกาล</p><h2>1. วัฒนธรรม DevOps (DevOps Culture)</h2><p>ก่อนที่เราจะลงลึกถึงเครื่องมือ เราต้องเข้าใจก่อนว่า CI/CD เป็นส่วนหนึ่งของ <strong>DevOps Culture</strong> หรือวัฒนธรรมองค์กรที่มุ่งเน้นการทลายกำแพงระหว่างทีม Development (ผู้เขียนโค้ด) และทีม Operations (ผู้ดูแลระบบ) ในอดีตทั้งสองทีมมักทำงานแยกกัน ทำให้เกิดปัญหาเวลา Deploy งานขึ้น Production เช่น "โค้ดรันได้บนเครื่องฉัน แต่พังบน Server" DevOps เข้ามาแก้ปัญหานี้โดยการสร้างความร่วมมือ การสื่อสาร และการใช้ Automation Tools เพื่อให้กระบวนการตั้งแต่เขียนโค้ดจนถึง Deploy เป็นไปอย่างราบรื่นและคาดเดาได้</p><div class="tip-box"><strong>💡 Pro Tip</strong>: DevOps ไม่ใช่แค่ชื่อตำแหน่งหรือเครื่องมือ แต่คือ "วัฒนธรรม" ที่ทีมต้องมีร่วมกัน การใช้ GitHub Actions เป็นเพียงการนำเครื่องมือมาช่วยให้วัฒนธรรมนี้เกิดขึ้นจริงได้ง่ายขึ้น</div><h2>2. ทำไมถึงต้องทำ Automation? (Why Automation)</h2><p>มนุษย์มักจะสร้างข้อผิดพลาด (Human Error) เมื่อต้องทำกระบวนการเดิมซ้ำๆ ลองจินตนาการว่าคุณต้อง Deploy ระบบด้วยตัวเองทุกวัน คุณต้อง 1. รัน Test 2. Build โค้ด 3. SSH เข้า Server 4. อัปโหลดไฟล์ 5. Restart Service หากคุณลืมขั้นตอนใดขั้นตอนหนึ่ง ระบบก็อาจจะพังได้</p><p>การทำ Automation ช่วยแก้ปัญหาเหล่านี้:</p><ul><li><strong>ลด Human Error:</strong> คอมพิวเตอร์ไม่เคยลืมขั้นตอนที่ถูกเขียนไว้</li><li><strong>ประหยัดเวลา:</strong> นักพัฒนาไม่ต้องเสียเวลากับงานซ้ำซาก สามารถเอาเวลาไปโฟกัสกับการเขียนฟีเจอร์ใหม่ๆ ได้เต็มที่</li><li><strong>ความสม่ำเสมอ (Consistency):</strong> ทุกๆ การ Deploy จะผ่านกระบวนการเดียวกันเสมอ ไม่ว่าจะ Deploy วันละ 1 ครั้งหรือ 100 ครั้ง</li><li><strong>Feedback Loop ที่รวดเร็ว:</strong> ถ้าระบบมีปัญหาหรือโค้ดบั๊ก เราจะรู้ตัวทันที ไม่ต้องรอจนถึงวัน Deploy จริง</li></ul><h2>3. ความแตกต่างระหว่าง CI vs CD vs CD</h2><p>คำว่า CI/CD มักถูกใช้รวมกัน แต่จริงๆ แล้วมันประกอบด้วย 3 คอนเซปต์หลักๆ ที่มีความแตกต่างกันอย่างชัดเจน ดังนี้:</p><h3>Continuous Integration (CI)</h3><p>CI คือกระบวนการ <strong>"รวมโค้ด"</strong> ของนักพัฒนาทุกคนเข้าด้วยกันอย่างสม่ำเสมอ (มักจะทำหลายครั้งต่อวัน) เมื่อมีการ Push โค้ดใหม่ขึ้น Repository ระบบ CI จะทำหน้าที่ตรวจสอบโค้ดนั้นโดยอัตโนมัติ เช่น การทำ Linting, Type Checking และที่สำคัญที่สุดคือการรัน Automated Tests</p><p>เป้าหมายของ CI คือการตรวจสอบว่าโค้ดใหม่ที่เขียนมานั้น ไม่ทำให้ระบบเดิมพัง และสามารถทำงานร่วมกับโค้ดของคนอื่นในทีมได้ หาก CI พัง (Red build) นักพัฒนาต้องรีบแก้ไขทันทีก่อนที่โค้ดนั้นจะถูกนำไปใช้งานต่อ</p><h3>Continuous Delivery (CD)</h3><p>CD (Delivery) คือขั้นตอนต่อจาก CI หลังจากที่โค้ดผ่านการ Test เรียบร้อยแล้ว ระบบจะทำการ Build โค้ดให้อยู่ในรูปแบบที่พร้อมนำไปติดตั้ง (Deployable Artifact) เช่น Docker Image หรือ Build Folder และเตรียมพร้อมสำหรับนำขึ้น Environment ต่างๆ (Staging, UAT, Production)</p><p>จุดเด่นของ Continuous Delivery คือ โค้ด <strong>"พร้อม"</strong> ที่จะขึ้น Production เสมอ แต่การกดปุ่ม Deploy ขึ้น Production จริงๆ ยังคงต้องใช้ <strong>"มนุษย์"</strong> ในการตัดสินใจกดปุ่ม (Manual Approval) เพื่อควบคุมจังหวะการ Release ให้ตรงกับแผนการตลาดหรือความพร้อมของธุรกิจ</p><h3>Continuous Deployment (CD)</h3><p>CD (Deployment) คือขั้นสุดของ Automation มันต่อยอดจาก Continuous Delivery โดยการตัด "มนุษย์" ออกจากกระบวนการตัดสินใจ นั่นหมายความว่า หากโค้ดผ่านการ Test และ Build สำเร็จ ระบบจะ <strong>Deploy ขึ้น Production โดยอัตโนมัติทันที!</strong></p><p>การทำ Continuous Deployment ต้องอาศัยความมั่นใจในระบบ Automated Testing อย่างมหาศาล เพราะถ้า Test หลุด บั๊กก็จะถูกส่งถึงมือผู้ใช้ทันที บริษัทระดับโลกอย่าง Amazon, Netflix หรือ Facebook ใช้แนวทางนี้ ทำให้เขาสามารถ Deploy โค้ดได้หลายพันครั้งต่อวัน</p><h2>สรุปภาพรวม</h2><p>ในคอร์สนี้ เราจะใช้ <strong>GitHub Actions</strong> ซึ่งเป็นเครื่องมือ CI/CD ยอดนิยมที่ผนวกรวมมากับ GitHub อย่างสมบูรณ์ เพื่อสร้าง Pipeline ตั้งแต่การทำ CI (Testing & Code Quality), CD (Building Artifacts) ไปจนถึง CD (Automated Deployment) ในบทถัดไปเราจะมาเริ่มลงมือเขียน Workflow YAML กัน!</p>'
  },
  {
    number: 1,
    slug: 'basics',
    emoji: '🐈',
    title: 'GitHub Actions Basics',
    content: '<h2>ทำความรู้จักกับ GitHub Actions (GitHub Actions Basics)</h2><p>GitHub Actions เป็นเครื่องมือสำหรับทำ CI/CD และ Automation ที่ทรงพลังมาก ซึ่งถูกฝังมาใน GitHub เลย ทำให้เราไม่ต้องไปตั้งค่า Server หรือเชื่อมต่อกับบริการภายนอกให้ยุ่งยาก เพียงแค่สร้างไฟล์ Configuration เราก็สามารถสั่งให้ GitHub ทำงานตามที่เราต้องการได้ทันที</p><h2>1. โครงสร้างของ Workflow YAML (Workflow YAML Structure)</h2><p>หัวใจสำคัญของ GitHub Actions คือ <strong>Workflow</strong> ซึ่งเขียนด้วยภาษา <strong>YAML</strong> (อ่านว่า ยามึล) โดยไฟล์เหล่านี้จะต้องถูกเก็บไว้ในโฟลเดอร์ <code>.github/workflows/</code> เสมอ ตัวอย่างโครงสร้างพื้นฐานมีดังนี้:</p><pre>name: CI Pipeline\n\non: [push]\n\njobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout code\n        uses: actions/checkout@v3\n      - name: Run script\n        run: echo "Hello GitHub Actions"\n</pre><p>จากโค้ดด้านบน เราสามารถแบ่งส่วนประกอบหลักๆ ออกได้เป็น <strong>Events (on)</strong>, <strong>Jobs</strong>, และ <strong>Steps</strong> ซึ่งเราจะมาเจาะลึกกันทีละส่วน</p><h2>2. Events พื้นฐาน (Triggers)</h2><p><strong>Events</strong> คือตัวจุดชนวนที่บอกว่า Workflow นี้จะเริ่มทำงานเมื่อไหร่ (กำหนดด้วยคีย์เวิร์ด <code>on:</code>) ในโลกของการพัฒนาซอฟต์แวร์ Events ที่ใช้บ่อยมีดังนี้:</p><ul><li><code>push</code>: ทำงานเมื่อมีการ Push โค้ดขึ้น Repository สามารถระบุ Branch ได้ เช่น เฉพาะ <code>main</code> หรือ <code>develop</code></li><li><code>pull_request</code>: ทำงานเมื่อมีการเปิด PR หรืออัปเดต PR เหมาะมากสำหรับการทำ Automated Testing เพื่อตรวจสอบโค้ดก่อน Merge</li><li><code>schedule</code>: ทำงานตามเวลาที่กำหนด (Cron Job) เช่น รัน Test ทุกๆ เที่ยงคืน หรือทำ Backup Database ทุกสัปดาห์</li><li><code>workflow_dispatch</code>: ทำให้เราสามารถกดปุ่มรัน Workflow ได้แบบ Manual ผ่านหน้าเว็บ GitHub โดยตรง</li></ul><div class="tip-box"><strong>💡 Pro Tip</strong>: การใช้ <code>workflow_dispatch</code> มีประโยชน์มากในการสร้าง Pipeline สำหรับ Deploy งานขึ้น Production เพราะมันช่วยให้เราควบคุมจังหวะการ Deploy ได้ด้วยตัวเอง (Continuous Delivery)</div><h2>3. องค์ประกอบสำคัญ: Jobs, Steps, Runners</h2><p>เมื่อ Event ถูก Trigger แล้ว สิ่งที่จะทำงานต่อไปก็คือ <strong>Jobs</strong></p><h3>Jobs</h3><p>Job คือกลุ่มของคำสั่งที่เราต้องการให้รัน Workflow หนึ่งตัวสามารถมีได้หลาย Jobs และโดยปกติแล้ว Jobs ต่างๆ จะทำงาน <strong>ขนานกัน (Parallel)</strong> เพื่อความรวดเร็ว ยกเว้นว่าเราจะกำหนดความสัมพันธ์ (Dependencies) ด้วยคีย์เวิร์ด <code>needs:</code> เช่น Job Deploy ต้องรอให้ Job Build เสร็จก่อน</p><h3>Runners</h3><p>แต่ละ Job จะต้องระบุว่าจะไปทำงานอยู่บนเครื่อง Server แบบไหน ซึ่งเราเรียกว่า <strong>Runner</strong> (กำหนดด้วย <code>runs-on:</code>) ทาง GitHub มี Runner มาตรฐานให้ใช้ฟรี เช่น <code>ubuntu-latest</code>, <code>windows-latest</code>, <code>macos-latest</code> หรือเราจะตั้งเครื่อง Server ของเราเองเป็น <strong>Self-hosted runner</strong> ก็ได้ (ซึ่งเหมาะกับงานที่ต้องการเข้าถึง Network ภายในบริษัท หรือต้องการสเปคเครื่องแรงๆ)</p><h3>Steps</h3><p>ภายใน Job จะประกอบด้วย <strong>Steps</strong> ซึ่งจะทำงานเรียงตามลำดับจากบนลงล่าง แต่ละ Step สามารถเป็นได้ 2 รูปแบบ:</p><ol style="padding-left:20px"><li><strong>รันคำสั่ง Command Line (Shell script):</strong> ใช้คีย์เวิร์ด <code>run:</code> เช่น <code>npm install</code> หรือ <code>docker build</code></li><li><strong>เรียกใช้ Action สำเร็จรูป:</strong> ใช้คีย์เวิร์ด <code>uses:</code> ซึ่งเป็นการนำโค้ดที่คนอื่นเขียนไว้แล้วมาใช้งานต่อ</li></ol><h2>4. การใช้ Actions Marketplace</h2><p>คุณไม่จำเป็นต้องเขียน Script สำหรับทุกอย่างด้วยตัวเอง GitHub มี <strong>Actions Marketplace</strong> ซึ่งเปรียบเสมือน App Store สำหรับ CI/CD ที่มีคนเขียน Action สำเร็จรูปไว้ให้เราใช้ฟรีๆ กว่าหมื่นตัว!</p><p>ตัวอย่าง Actions ยอดฮิตที่คุณต้องเจอ:</p><ul><li><code>actions/checkout</code>: สำหรับดึงโค้ดจาก Repository ของเรามาไว้ที่ Runner (ต้องมีแทบทุก Workflow)</li><li><code>actions/setup-node</code>: สำหรับติดตั้ง Node.js พร้อมตั้งค่า Caching ให้รวดเร็ว</li><li><code>docker/build-push-action</code>: สำหรับ Build และ Push Docker Image แบบครบวงจร</li></ul><p>การเลือกใช้ Action จาก Marketplace ช่วยลดเวลาการเขียน Workflow YAML ลงไปได้อย่างมหาศาล แต่ข้อควรระวังคือ ควรเลือกใช้ Action จากผู้พัฒนาที่น่าเชื่อถือ (Verified Creator) หรือมียอดดาวสูงๆ เพื่อความปลอดภัยของระบบ</p>'
  },
  {
    number: 2,
    slug: 'testing-pipeline',
    emoji: '🧪',
    title: 'ทำ Automated Testing',
    content: '<h2>ทำ Automated Testing ให้เป็นเรื่องอัตโนมัติ (Automated Testing Pipeline)</h2><p>การเขียน Automated Test (Unit Test, Integration Test) เป็นสิ่งที่ดี แต่ถ้าเขียนแล้วไม่ค่อยมีคนรัน มันก็ไม่มีประโยชน์ การนำระบบ Test เข้ามาผูกกับ CI Pipeline จึงเป็นสิ่งที่จำเป็นที่สุด เพื่อให้มั่นใจว่าโค้ดทุกบรรทัดที่จะถูกนำเข้า Branch หลักนั้นผ่านการทดสอบแล้วเสมอ</p><h2>1. การ Run Tests บน Pull Request (PR)</h2><p>แนวทางปฏิบัติที่ดีที่สุด (Best Practice) คือการเซ็ตอัพ Workflow ให้รัน Test ทุกครั้งที่มีคนเปิด <strong>Pull Request (PR)</strong> หรือมีการอัปเดตโค้ดใน PR นั้นๆ เพราะเป็นจุดสุดท้ายที่เราสามารถดักจับบั๊กได้ก่อนที่โค้ดจะถูกนำไปรวมกับงานของเพื่อนร่วมทีม</p><pre>name: PR Checks\non:\n  pull_request:\n    branches: [ main ]\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v3\n      - name: Install Dependencies\n        run: npm ci\n      - name: Run Tests\n        run: npm test\n</pre><p>เมื่อ Workflow นี้ถูกนำไปใช้ หากมี Test ใดที่ Fail (เกิด Red build) ตัว GitHub จะแสดงเครื่องหมายกากบาทสีแดงบน PR ทันที ทำให้นักพัฒนาทราบว่าต้องแก้ไขโค้ดก่อน และผู้ Review ก็จะรู้ว่ายังไม่ควร Merge โค้ดชุดนี้</p><h2>2. การใช้งาน Test Matrix สำหรับหลากหลาย Environment</h2><p>บางครั้งโปรเจกต์ของคุณอาจเป็น Open Source Library หรือแอปพลิเคชันที่ต้องรองรับผู้ใช้งานหลายแพลตฟอร์ม คุณจึงอยากรัน Test บน Node.js หลายเวอร์ชัน หรือบน OS ที่แตกต่างกัน การใช้ <strong>Matrix Strategy</strong> คือคำตอบ</p><pre>jobs:\n  test:\n    runs-on: ubuntu-latest\n    strategy:\n      matrix:\n        node-version: [14.x, 16.x, 18.x]\n    steps:\n      - uses: actions/checkout@v3\n      - name: Use Node.js ${{ matrix.node-version }}\n        uses: actions/setup-node@v3\n        with:\n          node-version: ${{ matrix.node-version }}\n      - run: npm test\n</pre><p>ด้วยโค้ดเพียงแค่นี้ GitHub Actions จะทำการแตก Job ออกเป็น 3 Jobs (รันขนานกัน) โดยใช้ Node.js เวอร์ชัน 14, 16 และ 18 ตามลำดับ ทำให้เราประหยัดเวลาและมั่นใจได้ว่าโค้ดของเราครอบคลุมการใช้งานจริงในทุก Environment</p><h2>3. การรัน Jest ใน CI และ Coverage Reports</h2><p>สำหรับโปรเจกต์ JavaScript/TypeScript เครื่องมือ Testing ที่ได้รับความนิยมสูงสุดคือ <strong>Jest</strong> การรัน Jest บน CI มีเทคนิคเล็กน้อยที่คุณควรทราบ:</p><ul><li><strong>ใช้คำสั่ง <code>--ci</code>:</strong> ควรสั่ง <code>npx jest --ci</code> เพื่อบอก Jest ว่ากำลังทำงานบนเซิร์ฟเวอร์ CI มันจะเปลี่ยนรูปแบบการแสดงผลให้เหมาะสมและไม่ใช้โหมด Interactive</li><li><strong>ใช้คำสั่ง <code>--runInBand</code>:</strong> หาก Runner มีสเปคจำกัด (เช่นมีแค่ 1-2 vCPU) การสั่ง <code>--runInBand</code> เพื่อให้ Jest รัน Test แบบเรียงลำดับ (Serial) อาจจะเร็วกว่าการรันขนานกัน (Parallel) ซึ่งกิน Memory สูงและอาจทำให้ CI ล่มได้</li></ul><p>นอกจากนี้ การตรวจสอบ <strong>Test Coverage</strong> (สัดส่วนของโค้ดที่ถูกทดสอบ) ก็สำคัญ เราสามารถตั้งค่าให้ Jest สรุป Coverage Report และใช้ Action อย่าง <code>codecov/codecov-action</code> เพื่อส่ง Report ไปแสดงผลอย่างสวยงาม และตั้งเกณฑ์ (Threshold) ได้ว่าถ้า Coverage ต่ำกว่า 80% ให้ถือว่า PR นั้นไม่ผ่าน!</p><div class="tip-box"><strong>💡 Pro Tip</strong>: หากใช้คำสั่ง <code>npm ci</code> แทน <code>npm install</code> บนเครื่อง CI จะช่วยให้การติดตั้ง Packages เร็วกว่าและปลอดภัยกว่า เพราะมันจะอ้างอิงจาก <code>package-lock.json</code> อย่างเคร่งครัด</div><h2>4. กลยุทธ์ Fail Fast (Fail Fast Strategy)</h2><p>เวลาของ CI มีค่า (บางแพลตฟอร์มคิดเงินตามนาทีที่ใช้งาน) เราจึงไม่ควรปล่อยให้ Job รันต่อไปถ้าเรารู้ว่ามันมีข้อผิดพลาดเกิดขึ้นแล้ว <strong>Fail Fast Strategy</strong> คือการออกแบบ Pipeline ให้พังเร็วที่สุดเมื่อมีอะไรผิดปกติ</p><p>ตัวอย่างเช่น:</p><ul><li>รัน Linting (ตรวจสอบ Syntax และสไตล์โค้ด) ก่อนรัน Tests เสมอ เพราะ Linting ใช้เวลาไม่กี่วินาที ถ้า Lint ไม่ผ่าน ก็ไม่ต้องเสียเวลารัน Tests ที่อาจใช้เวลาเป็นนาที</li><li>หากใช้งาน Matrix Strategy ตัวเลือก <code>fail-fast: true</code> (ค่าเริ่มต้น) จะทำให้ถ้ารันบน Node 14 แล้วพัง GitHub Actions จะทำการยกเลิก Job ที่กำลังรัน Node 16 และ 18 ทันทีเพื่อประหยัดทรัพยากร (สามารถตั้งค่าเป็น false ได้หากต้องการดูผลลัพธ์ของทุกเวอร์ชัน)</li></ul><p>การเข้าใจแนวคิดเหล่านี้จะช่วยให้คุณสร้าง Testing Pipeline ที่ไม่เพียงแต่เสถียร แต่ยังรวดเร็วและประหยัดค่าใช้จ่ายอีกด้วย</p>'
  },
  {
    number: 3,
    slug: 'build',
    emoji: '📦',
    title: 'Build และ Artifacts',
    content: '<h2>การ Build และจัดการ Artifacts ใน CI Pipeline</h2><p>หลังจากที่โค้ดผ่านการทดสอบ (Testing) เรียบร้อยแล้ว ขั้นตอนต่อไปคือการ "Build" เพื่อแปลงซอร์สโค้ดให้อยู่ในรูปแบบที่พร้อมนำไปใช้งานจริง (Production-ready) ไม่ว่าจะเป็นแอปพลิเคชันรูปแบบไหนก็ตาม การ Build มักจะตามมาด้วยการสร้าง <strong>Artifacts</strong> ซึ่งเป็นไฟล์หรือชุดไฟล์ผลลัพธ์ที่เราจะนำไป Deploy ต่อไป</p><h2>1. การ Build สำหรับโปรเจกต์ต่างๆ</h2><p>แต่ละภาษาหรือเฟรมเวิร์กมีวิธีการ Build ที่แตกต่างกัน แต่หลักการใน GitHub Actions นั้นเหมือนกันคือการกำหนด Environment, ติดตั้ง Dependencies, และรันคำสั่ง Build:</p><h3>Next.js / React</h3><p>สำหรับโปรเจกต์ Web Frontend การ Build มักจะเป็นการแปลงโค้ด React (JSX/TSX) ให้เป็น Static HTML/CSS/JS หรือเพื่อเตรียมสำหรับ Server-side Rendering</p><pre>- name: Install Dependencies\n  run: npm ci\n- name: Build Next.js\n  run: npm run build\n</pre><h3>Flutter (Mobile App)</h3><p>การ Build Mobile App จะซับซ้อนกว่าเล็กน้อย เพราะต้องใช้ SDK ของ Android หรือ iOS เราสามารถใช้ Action เช่น <code>subosito/flutter-action</code> เพื่อเซ็ตอัพ Environment ให้พร้อม</p><pre>- uses: subosito/flutter-action@v2\n- run: flutter pub get\n- run: flutter build apk --release\n</pre><h3>Python</h3><p>โปรเจกต์ Python อาจจะไม่มีการ "Build" โค้ดตรงๆ เหมือนภาษาที่เป็น Compiled Language แต่มักจะเป็นการเตรียม Package (เช่น Wheel) หรือเตรียม <code>requirements.txt</code> สำหรับ Environment ปลายทาง</p><h2>2. การ Caching Dependencies เพื่อความรวดเร็ว</h2><p>หนึ่งในปัญหาหลักที่ทำให้ CI/CD ทำงานช้าคือ การดาวน์โหลด Dependencies (เช่น <code>node_modules</code>) ใหม่ทุกครั้งที่รัน Workflow โชคดีที่ GitHub Actions มีระบบ <strong>Caching</strong> ที่ช่วยจำไฟล์เหล่านี้ไว้ใช้งานในรอบถัดไป</p><p>เราสามารถใช้ <code>actions/cache</code> หรือใช้ฟีเจอร์ Cache ที่มาพร้อมกับ <code>actions/setup-node</code> ได้เลย:</p><pre>- uses: actions/setup-node@v3\n  with:\n    node-version: 18\n    cache: "npm"\n</pre><p>เพียงแค่เพิ่ม <code>cache: "npm"</code> ระบบจะทำการ Cache โฟลเดอร์ที่จำเป็นตาม <code>package-lock.json</code> ให้โดยอัตโนมัติ ช่วยลดเวลาการทำงานจากหลายนาทีเหลือเพียงไม่กี่วินาที!</p><h2>3. การใช้งาน GitHub Artifacts (Upload / Download)</h2><p>เมื่อเรา Build เสร็จแล้ว (เช่น ได้โฟลเดอร์ <code>out/</code> หรือไฟล์ <code>app-release.apk</code>) ไฟล์เหล่านี้จะหายไปเมื่อ Job นั้นทำงานเสร็จ (เพราะ Runner เป็นเครื่องแบบใช้แล้วทิ้ง) ดังนั้นเราต้อง "อัปโหลด" มันเก็บไว้ก่อนด้วย <code>actions/upload-artifact</code></p><pre>- name: Upload Build Artifact\n  uses: actions/upload-artifact@v3\n  with:\n    name: nextjs-build\n    path: .next/\n</pre><p>หลังจากอัปโหลดเสร็จ เราสามารถเปิดดูและดาวน์โหลดไฟล์นี้ได้จากหน้า Workflow Summary บน GitHub และใน Job ถัดไป (เช่น Job Deploy) เราสามารถใช้ <code>actions/download-artifact</code> เพื่อดึงไฟล์นี้ไปใช้งานต่อได้</p><h2>4. การ Build Docker Image ใน CI</h2><p>ในยุคของ Cloud-native ซอฟต์แวร์ส่วนใหญ่มักจะถูก Deploy ในรูปแบบของ Docker Container การ Build Docker Image จึงเป็น Artifact รูปแบบหนึ่งที่นิยมมากที่สุด</p><p>GitHub Actions มี Action พิเศษจาก Docker โดยตรงที่ชื่อว่า <code>docker/build-push-action</code> ซึ่งรองรับการ Build ได้อย่างมีประสิทธิภาพ:</p><pre>- name: Set up Docker Buildx\n  uses: docker/setup-buildx-action@v2\n- name: Build Docker Image\n  uses: docker/build-push-action@v4\n  with:\n    context: .\n    push: false\n    tags: my-app:latest\n    cache-from: type=gha\n    cache-to: type=gha,mode=max\n</pre><div class="tip-box"><strong>💡 Pro Tip</strong>: การใช้ <code>cache-from</code> และ <code>cache-to</code> แบบ <code>type=gha</code> คือการใช้ระบบ GitHub Actions Cache สำหรับ Docker Layer Caching ซึ่งจะทำให้การ Build Docker Image เร็วขึ้นอย่างมหาศาลหากมีการแก้ไขโค้ดเพียงเล็กน้อย!</div>'
  },
  {
    number: 4,
    slug: 'deploy',
    emoji: '🚀',
    title: 'Automated Deploy',
    content: '<h2>เริ่มต้นการทำ Automated Deploy ไปยังเซิร์ฟเวอร์ (Automated Deployment)</h2><p>มาถึงขั้นตอนสำคัญคือ Continuous Deployment (CD) นั่นคือการนำ Artifact ที่ Build เสร็จแล้ว หรือโค้ดที่ผ่านการทดสอบแล้ว ไปติดตั้งบน Server หรือ Cloud Provider เพื่อให้ผู้ใช้งานสามารถเข้าถึงแอปพลิเคชันของเราได้ การทำ Automated Deploy จะช่วยลดความผิดพลาดจากการก็อปปี้ไฟล์ด้วยมือ และทำให้เราสามารถ Rollback ได้ง่ายหากเกิดปัญหา</p><h2>1. การ Deploy ไปยัง PaaS (Vercel, Railway, Heroku)</h2><p>Platform as a Service (PaaS) อย่าง Vercel หรือ Railway ออกแบบมาให้เข้ากับ CI/CD ได้อย่างแนบเนียนที่สุด ปกติแพลตฟอร์มเหล่านี้มักจะมีระบบ CI ของตัวเองอยู่แล้ว (แค่ผูก GitHub Repository ก็ทำงานได้เลย) แต่บางครั้งเราต้องการรัน Testing ใน GitHub Actions ก่อน แล้วค่อยสั่ง Deploy</p><p>สำหรับ Vercel เราสามารถใช้ Vercel CLI ควบคู่กับ GitHub Actions เพื่อสั่ง Deploy ได้:</p><pre>- name: Install Vercel CLI\n  run: npm i -g vercel\n- name: Deploy to Vercel\n  run: vercel pull --yes --environment=production && vercel build --prod && vercel deploy --prebuilt --prod\n  env:\n    VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}\n    VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}\n    VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}\n</pre><h2>2. การ Deploy ขึ้น AWS (Amazon Web Services)</h2><p>องค์กรขนาดใหญ่มักใช้ Cloud Provider อย่าง AWS การ Deploy จะซับซ้อนกว่า PaaS เพราะมีบริการให้เลือกหลากหลาย เช่น Amazon EC2, ECS, หรือ S3</p><p>สมมติว่าเราต้องการ Deploy โค้ด Frontend (Static Website) ไปยัง AWS S3 เราจะต้องตั้งค่า AWS Credentials ก่อน (โดยใช้ GitHub Secrets) จากนั้นจึงใช้คำสั่ง AWS CLI:</p><pre>- name: Configure AWS Credentials\n  uses: aws-actions/configure-aws-credentials@v2\n  with:\n    aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}\n    aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}\n    aws-region: ap-southeast-1\n- name: Deploy to S3\n  run: aws s3 sync ./build s3://my-frontend-bucket --delete\n</pre><h2>3. การ Deploy แบบดั้งเดิมผ่าน SSH (Self-hosted Server)</h2><p>หากคุณใช้ VPS (เช่น DigitalOcean, Linode) หรือมี Server ของตัวเอง การ Deploy มักจะทำผ่าน <strong>SSH</strong> โดยหลักการคือให้ GitHub Actions ทำการ Remote เข้าไปยัง Server แล้วรันคำสั่ง (เช่น <code>git pull</code>, <code>npm run build</code>, <code>pm2 restart</code>)</p><p>เราสามารถใช้ Action ชื่อ <code>appleboy/ssh-action</code> ซึ่งเป็นเครื่องมือยอดนิยมสำหรับการรันคำสั่งผ่าน SSH:</p><pre>- name: Deploy via SSH\n  uses: appleboy/ssh-action@v0.1.10\n  with:\n    host: ${{ secrets.SERVER_HOST }}\n    username: ${{ secrets.SERVER_USER }}\n    key: ${{ secrets.SSH_PRIVATE_KEY }}\n    script: |\n      cd /var/www/my-app\n      git pull origin main\n      npm install\n      npm run build\n      pm2 restart my-app\n</pre><div class="tip-box"><strong>💡 Pro Tip</strong>: เพื่อความปลอดภัย คุณควรสร้าง SSH Key Pair แยกต่างหากสำหรับ GitHub Actions โดยเฉพาะ (Deploy Key) และไม่ใช้ Root User ในการ Deploy เด็ดขาด เพื่อจำกัดสิทธิ์การเข้าถึงไฟล์ในเซิร์ฟเวอร์</div><h2>4. การจัดการ Environment-specific Deployments</h2><p>ในการทำงานจริง เรามักจะไม่ได้มีแค่เซิร์ฟเวอร์เดียว แต่มีหลาย Environment เช่น Staging, UAT, และ Production เราสามารถสร้าง Workflow ที่แยกว่าควร Deploy ไปที่ไหนโดยเช็คจาก <strong>Branch</strong> ที่ถูก Push เข้ามา</p><pre>jobs:\n  deploy:\n    runs-on: ubuntu-latest\n    environment: \n      name: ${{ github.ref == \'refs/heads/main\' && \'production\' || \'staging\' }}\n    steps:\n      - run: echo "Deploying to ${{ github.ref == \'refs/heads/main\' && \'Production\' || \'Staging\' }}"\n</pre><p>GitHub Actions มีฟีเจอร์ <strong>Environments</strong> (อยู่ในแท็บ Settings ของ Repository) ที่ให้เราสามารถกำหนด Secrets แยกกันสำหรับ Staging และ Production ได้ ทำให้ Workflow เดียวกันสามารถ Deploy ไปคนละเซิร์ฟเวอร์ได้อย่างปลอดภัยและจัดการง่ายขึ้น</p>'
  },
  {
    number: 5,
    slug: 'secrets',
    emoji: '🔒',
    title: 'Secrets และ Environment Variables',
    content: '<h2>การจัดการความปลอดภัยด้วย Secrets และ Environment Variables</h2><p>ในการทำ CI/CD เราหลีกเลี่ยงไม่ได้ที่จะต้องจัดการกับข้อมูลสำคัญ (Sensitive Data) เช่น API Keys, Database Passwords, AWS Credentials หรือ SSH Keys การนำข้อมูลเหล่านี้เขียนลงไปใน Source Code ตรงๆ (Hardcoding) เป็นเรื่องที่ <strong>อันตรายถึงขั้นวิกฤต</strong> เพราะใครก็ตามที่เข้าถึง Repository ได้ ก็จะเห็นความลับเหล่านี้ทั้งหมด</p><h2>1. รู้จักกับ GitHub Secrets และ Variables</h2><p>GitHub เตรียมระบบสำหรับการเก็บรักษาข้อมูลเหล่านี้ไว้อย่างปลอดภัย โดยแบ่งออกเป็น 2 ประเภท:</p><ul><li><strong>Secrets:</strong> สำหรับเก็บข้อมูลที่เป็นความลับ เช่น Password หรือ Token ข้อมูลนี้จะถูกเข้ารหัส (Encrypted) ตั้งแต่ตอนที่เรากรอก และไม่สามารถกดดูค่ามันได้อีกเลยในหน้าเว็บ และหากเราเผลอสั่ง <code>echo</code> ค่านี้ใน Workflow ตัว GitHub จะเซ็นเซอร์ให้เป็น <code>***</code> โดยอัตโนมัติ</li><li><strong>Variables:</strong> สำหรับเก็บข้อมูล Configuration ที่ไม่ใช่ความลับ เช่น ชื่อเซิร์ฟเวอร์ (<code>SERVER_PORT=8080</code>) หรือ URL ของ API ข้อมูลนี้สามารถกดดูแก้ไขได้ปกติ</li></ul><p>วิธีการเรียกใช้งานใน Workflow YAML จะใช้รูปแบบ <code>${{ secrets.SECRET_NAME }}</code> และ <code>${{ vars.VARIABLE_NAME }}</code></p><h2>2. การปกป้อง Environment ด้วย Environment Protection Rules</h2><p>ใน GitHub Enterprise หรือ GitHub Public Repositories จะมีฟีเจอร์ที่เรียกว่า <strong>Environment</strong> ซึ่งเราสามารถผูก Secrets ไว้กับ Environment นั้นๆ ได้ (เช่น Environment <code>production</code> จะมี Token ที่ต่างจาก <code>staging</code>)</p><p>ความสามารถพิเศษของฟีเจอร์นี้คือ <strong>Protection Rules</strong> เราสามารถตั้งค่า <strong>Required Reviewers</strong> ได้ นั่นหมายความว่า ต่อให้โค้ดจะถูก Merge เข้า <code>main</code> Branch และ Workflow เริ่มทำงานแล้ว แต่ก่อนที่ Job Deploy จะถูกรัน ระบบจะหยุดรอ (Pause) ให้ผู้ที่ได้รับมอบหมาย (เช่น หัวหน้าทีม หรือ DevOps) มากด <strong>Approve</strong> เสียก่อน จึงจะนำโค้ดขึ้น Production ได้จริงๆ ฟีเจอร์นี้คือหัวใจสำคัญของการทำ Continuous Delivery อย่างปลอดภัย</p><h2>3. การเชื่อมต่อ Cloud แบบไร้รหัสผ่านด้วย OIDC (OpenID Connect)</h2><p>วิธีการเดิมในการต่อ AWS หรือ GCP จาก GitHub Actions คือการสร้าง Long-lived Credentials (เช่น Access Key) แล้วเอาไปใส่ใน Secrets ซึ่งมีข้อเสียคือ Key เหล่านี้ไม่มีวันหมดอายุ หากหลุดไปจะอันตรายมาก</p><p>ปัจจุบัน Best Practice คือการใช้ <strong>OIDC (OpenID Connect)</strong> ซึ่งเป็นการทำ Trust Relationship ระหว่าง GitHub และ Cloud Provider (เช่น AWS) โดย GitHub จะส่ง Temporary Token ไปให้ AWS เพื่อขอสิทธิ์ชั่วคราว (Short-lived Token) ในการ Deploy</p><pre>permissions:\n  id-token: write # จำเป็นสำหรับการขอ OIDC Token\n  contents: read\n\nsteps:\n  - name: Configure AWS Credentials using OIDC\n    uses: aws-actions/configure-aws-credentials@v2\n    with:\n      role-to-assume: arn:aws:iam::1234567890:role/GitHubActionsDeployRole\n      aws-region: ap-southeast-1\n</pre><p>ด้วยวิธีนี้ เราไม่ต้องเก็บ AWS Key ลงใน GitHub Secrets อีกต่อไป! ทำให้ระบบปลอดภัยขึ้นอย่างมหาศาล เพราะ Token จะหมดอายุทันทีที่ Job รันเสร็จ</p><div class="tip-box"><strong>💡 Pro Tip</strong>: ระบบ OIDC สามารถตั้งเงื่อนไข (Condition) ในฝั่ง AWS ได้ด้วยว่า ให้แจกสิทธิ์นี้เฉพาะ Repository ชื่อนี้ และเฉพาะ <code>main</code> branch เท่านั้น ป้องกันไม่ให้คนอื่นในบริษัทเอา Role นี้ไปใช้มั่วซั่ว</div><h2>4. การ Integrate ร่วมกับ HashiCorp Vault</h2><p>สำหรับองค์กรที่มีระดับความปลอดภัยขั้นสูงสุด มักจะใช้เครื่องมืออย่าง <strong>HashiCorp Vault</strong> ในการเก็บ Secrets แบบรวมศูนย์ (Centralized Secrets Management) GitHub Actions ก็มี Action อย่าง <code>hashicorp/vault-action</code> ที่สามารถดึงข้อมูลความลับจาก Vault Server ในระหว่างที่รัน Pipeline ได้ทันที ทำให้ทีม Security สามารถจัดการสิทธิ์และ Rotate Keys จากศูนย์กลางได้โดยไม่ต้องเข้ามาแก้ใน GitHub</p>'
  },
  {
    number: 6,
    slug: 'docker-ci',
    emoji: '🐳',
    title: 'Docker ใน CI/CD',
    content: '<h2>การจัดการ Docker ใน CI/CD Pipeline</h2><p>Docker ได้กลายเป็นมาตรฐานอุตสาหกรรมสำหรับการแพ็กเกจและรันแอปพลิเคชัน การนำ Docker เข้ามาใช้งานร่วมกับ GitHub Actions จึงเป็นสิ่งที่ขาดไม่ได้สำหรับทีมพัฒนาซอฟต์แวร์สมัยใหม่ เพราะมันช่วยการันตีว่า "รันผ่านบน CI ก็ต้องรันผ่านบน Production" ได้อย่างแท้จริง</p><h2>1. การ Build และ Push Docker Image</h2><p>กระบวนการพื้นฐานที่สุดคือการนำ <code>Dockerfile</code> มา Build เป็น Image และ Push ขึ้นไปเก็บบน Registry (เช่น Docker Hub) เพื่อเตรียมให้ Server ปลายทางดึงไป Deploy</p><pre>- name: Login to Docker Hub\n  uses: docker/login-action@v2\n  with:\n    username: ${{ secrets.DOCKER_USERNAME }}\n    password: ${{ secrets.DOCKER_PASSWORD }}\n- name: Build and push\n  uses: docker/build-push-action@v4\n  with:\n    context: .\n    push: true\n    tags: myusername/myapp:latest\n</pre><h2>2. การใช้งาน GitHub Container Registry (GHCR)</h2><p>แทนที่จะเสียเงินเช่า Private Registry ที่อื่น เราสามารถใช้ <strong>GitHub Container Registry (ghcr.io)</strong> ซึ่งให้พื้นที่ใช้งานฟรีสำหรับโปรเจกต์ทั่วไป และเชื่อมต่อกับ Repository ของเราโดยตรง</p><p>การ Login เข้า GHCR ไม่ต้องไปสร้าง Password ใหม่ แต่สามารถใช้ <code>${{ secrets.GITHUB_TOKEN }}</code> ที่ GitHub Actions มีให้ทุก Workflow ได้เลย</p><pre>- name: Login to GHCR\n  uses: docker/login-action@v2\n  with:\n    registry: ghcr.io\n    username: ${{ github.repository_owner }}\n    password: ${{ secrets.GITHUB_TOKEN }}\n</pre><h2>3. การทำ Multi-architecture Builds</h2><p>ในยุคที่มีทั้ง CPU แบบ x86 (เช่น Intel/AMD) และ ARM (เช่น Apple Silicon M1/M2, AWS Graviton) การสร้าง Docker Image ให้รองรับหลายสถาปัตยกรรมจึงสำคัญมาก โชคดีที่ GitHub Actions สามารถทำเรื่องยากนี้ให้ง่ายได้ด้วย Docker Buildx</p><pre>- name: Set up QEMU\n  uses: docker/setup-qemu-action@v2\n- name: Set up Docker Buildx\n  uses: docker/setup-buildx-action@v2\n- name: Build and push multi-arch\n  uses: docker/build-push-action@v4\n  with:\n    platforms: linux/amd64,linux/arm64\n    push: true\n    tags: ghcr.io/myorg/myapp:latest\n</pre><div class="tip-box"><strong>💡 Pro Tip</strong>: การทำ Multi-arch build อาจจะใช้เวลานานขึ้น 2-3 เท่า เพราะต้องใช้ QEMU ในการจำลอง CPU Architecture ที่ Runner ไม่ได้ซัพพอร์ตโดยตรง การใช้ Caching จึงยิ่งมีความสำคัญ</div><h2>4. การใช้ Layer Caching ใน CI</h2><p>การ Build Docker จากศูนย์ทุกครั้งจะเสียเวลามาก โดยเฉพาะขั้นตอนการติดตั้งแพ็กเกจ (เช่น <code>npm install</code>, <code>pip install</code>) เราสามารถเปิดใช้งาน Docker Layer Caching แบบ GitHub Actions (GHA) ได้</p><pre>- name: Build and push with cache\n  uses: docker/build-push-action@v4\n  with:\n    context: .\n    push: true\n    tags: myapp:latest\n    cache-from: type=gha\n    cache-to: type=gha,mode=max\n</pre><p><code>mode=max</code> หมายความว่าเราจะ Cache ทุกๆ Layer ที่เกิดขึ้นระหว่างการ Build ไม่ใช่เฉพาะผลลัพธ์สุดท้าย ทำให้ครั้งต่อไปที่โค้ดไม่มีการเปลี่ยน Dependencies การ Build จะใช้เวลาเพียงไม่กี่วินาที!</p>'
  },
  {
    number: 7,
    slug: 'quality',
    emoji: '✅',
    title: 'Code Quality Gates',
    content: '<h2>ยกระดับคุณภาพโค้ดด้วย Code Quality Gates</h2><p>ก่อนที่โค้ดจะถูกนำไปรัน Test หรือ Build เราควรมีการ "ตรวจสุขภาพ" ของโค้ดเบื้องต้นก่อน กระบวนการนี้เรียกว่า <strong>Code Quality Gates</strong> เพื่อกรองโค้ดที่ไม่ได้มาตรฐาน มีช่องโหว่ หรือมีโอกาสเกิดบั๊ก ออกไปตั้งแต่เนิ่นๆ (Shift-Left Testing)</p><h2>1. การทำ ESLint และ Prettier Checks</h2><p>การรักษา Style Code ให้ตรงกันทั้งทีมเป็นเรื่องสำคัญ เราสามารถใช้ Workflow เพื่อรัน Linter และ Formatter ได้ ถ้าใครลืม Format โค้ดมา PR นั้นก็จะถูกตีตกทันที (Fail)</p><pre>- name: Run Linter\n  run: npm run lint\n- name: Check Formatting\n  run: npx prettier --check .\n</pre><p>แทนที่จะแค่รายงานว่า Fail เราสามารถใช้เครื่องมืออย่าง <strong>Reviewdog</strong> ซึ่งจะแปลงผลลัพธ์ของ ESLint ให้กลายเป็นคอมเมนต์บน Pull Request แบบอัตโนมัติ ชี้เป้าให้เลยว่าโค้ดบรรทัดไหนที่ผิด Style ทำให้การแก้ไขง่ายขึ้นมาก</p><h2>2. การทำ Type Checking</h2><p>สำหรับภาษาอย่าง TypeScript บางครั้งโค้ดอาจจะผ่าน Linter แต่มี Type ผิดพลาดอยู่ การรัน Type Checker ใน CI จึงเป็นด่านสำคัญด่านที่สอง</p><pre>- name: Type Check\n  run: npx tsc --noEmit\n</pre><p>คำสั่ง <code>--noEmit</code> คือการบอก TypeScript ว่าให้เช็คความถูกต้องเฉยๆ ไม่ต้องแปลงร่างเป็น JavaScript ซึ่งจะทำงานได้เร็วกว่ามาก</p><h2>3. การทำ Security Scanning ด้วย CodeQL และ Snyk</h2><p>ความปลอดภัยของโค้ด (Code Security) ไม่ควรรอให้ถึงช่วงทำ Pentest แต่ควรทำทุกครั้งที่มี PR</p><ul><li><strong>CodeQL:</strong> เครื่องมือฟรีจาก GitHub ที่จะสแกนหาช่องโหว่ใน Source Code ของคุณ (เช่น SQL Injection, XSS) สามารถเปิดใช้งานได้ฟรีสำหรับ Public Repositories (ส่วน Private ต้องใช้ GitHub Advanced Security)</li><li><strong>Snyk:</strong> บริการยอดนิยมสำหรับการทำ SAST (Static Application Security Testing) มี Action ให้เราใช้งานได้ง่ายๆ เพื่อตรวจจับช่องโหว่</li></ul><pre>- name: Run Snyk to check for vulnerabilities\n  uses: snyk/actions/node@master\n  env:\n    SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}\n  with:\n    command: test\n</pre><div class="tip-box"><strong>💡 Pro Tip</strong>: อย่าลืมตั้งค่าให้ Workflow เหล่านี้พัง (Fail) หากพบช่องโหว่ระดับ High หรือ Critical เท่านั้น ไม่เช่นนั้นทีมอาจจะหงุดหงิดที่โค้ดไม่ผ่านเพราะช่องโหว่ระดับ Low ที่ไม่ได้ส่งผลกระทบอะไร</div><h2>4. การตรวจสอบ Dependency Audit</h2><p>บ่อยครั้งที่ช่องโหว่ไม่ได้มาจากโค้ดที่เราเขียนเอง แต่มาจาก Libraries ภายนอก (Supply Chain Attack) การสั่งรันคำสั่ง Audit ใน Pipeline จะช่วยเตือนเราได้ทันท่วงที</p><pre>- name: Audit Dependencies\n  run: npm audit --audit-level=high\n</pre><p>หรือถ้าคุณอยากให้ระบบอัปเดตช่องโหว่ให้โดยอัตโนมัติ ฟีเจอร์ <strong>Dependabot</strong> ของ GitHub ก็เป็นสิ่งที่ควรเปิดใช้งานควบคู่ไปกับ CI Pipeline ของคุณ</p>'
  },
  {
    number: 8,
    slug: 'pr-workflow',
    emoji: '🤝',
    title: 'จัดการ Pull Request Workflow',
    content: '<h2>การจัดการและควบคุม Pull Request Workflow</h2><p>Pull Request (PR) คือด่านหน้าสำคัญของการทำงานร่วมกันในทีม การใช้ GitHub Actions มาช่วยจัดการ PR ไม่เพียงแต่ช่วยลดภาระของคนทำ Code Review แต่ยังทำให้กระบวนการทำงานมีประสิทธิภาพมากขึ้น และป้องกัน "ความมักง่าย" ในการนำโค้ดขึ้นระบบ</p><h2>1. การตั้งค่า Branch Protection Rules</h2><p>หัวใจของการทำ CI/CD ที่ดีคือการควบคุมไม่ให้ใครก็ตามสามารถ Push โค้ดเข้า <code>main</code> branch ได้โดยตรง เราต้องเข้าไปที่ Settings > Branches ใน GitHub แล้วเปิดใช้งาน <strong>Branch Protection Rules</strong></p><ul><li><strong>Require a pull request before merging:</strong> บังคับว่าทุกการเปลี่ยนแปลงต้องผ่าน PR เท่านั้น</li><li><strong>Require approvals:</strong> บังคับว่าต้องมีคนกด Approve อย่างน้อย 1-2 คน (ช่วยป้องกันตาบอดสีตอนเช็คโค้ดตัวเอง)</li></ul><h2>2. การบังคับ Required Status Checks</h2><p>นี่คือจุดที่ GitHub Actions เข้ามาเชื่อมต่อกับ Branch Protection Rule อย่างสมบูรณ์! คุณสามารถตั้งค่า <strong>Require status checks to pass before merging</strong> แล้วระบุชื่อ Job ใน Workflow ของคุณ (เช่น <code>test</code>, <code>lint</code>, <code>build</code>) ลงไป</p><p>ผลลัพธ์คือ: ปุ่ม "Merge pull request" จะถูกล็อคเป็นสีเทา (กดไม่ได้) จนกว่า Job เหล่านั้นจะวิ่งเสร็จและผลออกมาเป็นสีเขียว (Pass) ทั้งหมด นี่คือสุดยอดเกราะป้องกันไม่ให้บั๊กหลุดเข้า <code>main</code></p><h2>3. การใช้งาน Auto-merge</h2><p>บางครั้งทีมอาจจะเจอสถานการณ์ที่ว่า "โค้ดรีวิวเสร็จแล้วนะ แต่รอ CI รันอีก 10 นาทีถึงจะเขียว ค่อยกลับมากด Merge ละกัน" สุดท้ายก็ลืมกด ปัญหานี้แก้ได้ด้วยฟีเจอร์ <strong>Auto-merge</strong></p><p>เมื่อเปิดใช้งานฟีเจอร์นี้ในหน้า Settings ของ Repository ปุ่ม Merge บน PR จะมีตัวเลือก "Enable auto-merge" เพิ่มเข้ามา เมื่อเรากดปุ่มนี้ ระบบจะรอจนกว่า CI ทุกตัวจะวิ่งผ่าน (Required Status Checks) แล้วทำการ Merge PR นั้นเข้า <code>main</code> ให้โดยอัตโนมัติ ประหยัดเวลาที่ต้องมานั่งเฝ้าหน้าจอ</p><h2>4. การใช้ PR Templates และการตรวจสอบข้อมูล</h2><p>เพื่อรักษามาตรฐานการเขียน PR ของทีม เราสามารถสร้างไฟล์ <code>.github/pull_request_template.md</code> เพื่อกำหนดฟอร์มเวลาเปิด PR ใหม่ (เช่น ต้องระบุชื่อ Jira Ticket, ต้องแนบภาพ Screenshot)</p><p>นอกจากนี้เราสามารถใช้ Action เพื่อตรวจสอบว่า PR ทำตามกฎที่วางไว้หรือไม่ เช่น การใช้ <code>amannn/action-semantic-pull-request</code> เพื่อเช็คว่าชื่อ PR (Title) ถูกเขียนในรูปแบบ Semantic Versioning หรือไม่ (เช่น <code>feat: add login</code>, <code>fix: button color</code>)</p><pre>- name: Check PR Title\n  uses: amannn/action-semantic-pull-request@v5\n  env:\n    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}\n</pre><div class="tip-box"><strong>💡 Pro Tip</strong>: หากใช้ร่วมกับเครื่องมืออย่าง Release Please หรือ Semantic Release การมีชื่อ PR ที่เป็นระบบจะช่วยให้เราสามารถ Generate Changelog และทำ Version Tagging ได้แบบอัตโนมัติ 100%!</div>'
  },
  {
    number: 9,
    slug: 'monitoring',
    emoji: '📊',
    title: 'Monitoring Pipeline',
    content: '<h2>การ Monitoring และปรับปรุงประสิทธิภาพ Pipeline</h2><p>หลังจากที่เราสร้าง CI/CD Pipeline จนเสร็จสมบูรณ์แล้ว งานของเรายังไม่จบ การติดตามผลการทำงาน (Monitoring) และการปรับปรุงประสิทธิภาพ (Optimization) คือสิ่งที่จะทำให้ระบบของเรายั่งยืนและรองรับสเกลของทีมที่ใหญ่ขึ้นได้</p><h2>1. การแจ้งเตือนผ่าน Slack หรือ LINE (Workflow Notifications)</h2><p>คุณคงไม่อยากให้นักพัฒนาต้องคอยเปิดหน้าเว็บ GitHub เพื่อดูว่า Deploy เสร็จหรือยัง การส่ง Notification เข้าไปยังช่องทางสื่อสารหลักของทีมจึงเป็นเรื่องจำเป็น</p><p>สำหรับ <strong>Slack</strong> คุณสามารถใช้ Action อย่าง <code>rtCamp/action-slack-notify</code>:</p><pre>- name: Slack Notification\n  uses: rtCamp/action-slack-notify@v2\n  env:\n    SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}\n    SLACK_MESSAGE: "Deploy to Production Successful! 🚀"\n</pre><p>สำหรับ <strong>LINE Notify</strong> ก็สามารถทำได้ง่ายๆ โดยการยิง HTTP Request ผ่าน cURL:</p><pre>- name: LINE Notify\n  run: |\n    curl -X POST -H "Authorization: Bearer ${{ secrets.LINE_TOKEN }}" \\\n    -F "message=Deploy Completed" https://notify-api.line.me/api/notify\n</pre><h2>2. การลดความซ้ำซ้อนด้วย Reusable Workflows</h2><p>เมื่อโปรเจกต์คุณใหญ่ขึ้น คุณอาจจะมี Repository หลายสิบตัวที่ใช้กระบวนการ Build เหมือนกันเป๊ะ การก็อปปี้โค้ด YAML ไปวางทุกๆ Repositories จะทำให้การดูแลรักษา (Maintenance) กลายเป็นฝันร้าย</p><p>GitHub Actions มีฟีเจอร์ <strong>Reusable Workflows</strong> ที่ให้คุณเขียน Workflow กลางไว้ที่เดียว แล้วให้ Repository อื่นๆ "เรียกใช้" ได้ โดยใช้คำสั่ง <code>uses:</code> ตามด้วย Path ของ Workflow นั้น</p><pre># ฝั่ง Repository ที่ต้องการเรียกใช้ (Caller)\njobs:\n  call-reusable-workflow:\n    uses: myorg/central-repo/.github/workflows/build.yml@main\n    with:\n      node_version: 18\n    secrets: inherit\n</pre><h2>3. การวิเคราะห์ Pipeline Analytics</h2><p>การวัดผลว่า Pipeline ของเราทำงานได้ดีแค่ไหน สามารถดูได้จากเครื่องมือเสริมหรือตัว GitHub เอง สิ่งที่ควรให้ความสนใจคือ:</p><ul><li><strong>Build Time:</strong> ระยะเวลาเฉลี่ยที่ใช้ตั้งแต่ Push โค้ดจนถึง Deploy เสร็จ หากใช้เวลานานเกิน 15-20 นาที ทีมอาจจะเริ่มรู้สึกหงุดหงิด (Context Switching)</li><li><strong>Success Rate:</strong> เปอร์เซ็นต์ที่ CI รันผ่านเทียบกับที่รันพัง ถ้าพังบ่อยอาจหมายถึง Test Flaky (Test ที่บางทีก็ผ่าน บางทีก็พังโดยไม่มีการแก้โค้ด) ซึ่งต้องรีบแก้ไข</li></ul><h2>4. การลดต้นทุน (Cost Optimization)</h2><p>GitHub Actions ให้โควตานาทีฟรีสำหรับ Public Repo แบบไม่จำกัด แต่สำหรับ Private Repo จะมีจำกัด (เช่น 2,000 นาที/เดือน สำหรับ Free plan) การปรับแต่งให้ประหยัดจึงสำคัญ:</p><ol style="padding-left:20px"><li><strong>ใช้ Linux Runner:</strong> การใช้ <code>ubuntu-latest</code> คิดค่าใช้จ่ายเป็น 1 เท่า แต่ถ้าใช้ <code>windows-latest</code> คิด 2 เท่า และ <code>macos-latest</code> คิดถึง 10 เท่า! พยายามใช้ Linux ให้มากที่สุด</li><li><strong>ใช้ Concurrency Limit:</strong> ถ้านักพัฒนา Push โค้ดรัวๆ 5 ครั้งติดกัน แทนที่จะปล่อยให้ CI รันพร้อมกัน 5 รอบ เราสามารถตั้งค่า <code>concurrency</code> เพื่อให้ระบบยกเลิกคิวเก่าๆ ที่ยังทำไม่เสร็จ แล้วรันเฉพาะคิวล่าสุดเท่านั้น</li></ol><pre>concurrency:\n  group: ${{ github.workflow }}-${{ github.ref }}\n  cancel-in-progress: true\n</pre><div class="tip-box"><strong>💡 Pro Tip</strong>: CI/CD ที่ดีไม่ใช่แค่ระบบที่ทำทุกอย่างอัตโนมัติ แต่คือระบบที่รวดเร็ว เชื่อถือได้ และทำให้ชีวิตของนักพัฒนาในทีมมีความสุขขึ้น!</div>'
  }
];
