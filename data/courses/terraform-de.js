export const chapters = [
  { number: 0, slug: 'overview', emoji: '🏗️', title: 'Terraform สำหรับ Data Engineer', content: '<h2>Terraform สำหรับ Data Engineer คืออะไร?</h2><p><strong>Infrastructure as Code (IaC)</strong> คือแนวคิดการจัดการโครงสร้างพื้นฐานคลาวด์ผ่านโค้ด แทนที่จะคลิกผ่าน Console ด้วยมือ ซึ่งทำให้ทีม Data Engineer สามารถสร้าง, แก้ไข, และทำลาย Cloud Resources ได้อย่างรวดเร็ว ทำซ้ำได้ และตรวจสอบได้ผ่าน Version Control</p><h2>ทำไม Data Engineer ต้องใช้ Terraform?</h2><p>งานของ Data Engineer เกี่ยวข้องกับการจัดการ Cloud Resources จำนวนมาก ไม่ว่าจะเป็น Data Lake บน S3 หรือ GCS, Data Warehouse อย่าง BigQuery หรือ Redshift, Orchestration Tools อย่าง Cloud Composer หรือ MWAA, รวมถึง IAM Policies และ Networking ต่าง ๆ การจัดการทั้งหมดนี้ด้วยมือนั้นช้า เกิดข้อผิดพลาดง่าย และไม่สามารถ reproduce ได้ Terraform แก้ปัญหาทั้งหมดนี้</p><h3>เปรียบเทียบเครื่องมือ IaC</h3><ul><li><strong>Terraform</strong>: Cloud-agnostic, HCL syntax, Provider ecosystem ใหญ่มาก, Community แข็งแกร่ง</li><li><strong>AWS CloudFormation</strong>: เฉพาะ AWS เท่านั้น, YAML/JSON syntax, Integration กับ AWS ดีมาก แต่ใช้กับ GCP/Azure ไม่ได้</li><li><strong>Pulumi</strong>: ใช้ภาษาโปรแกรมจริง (Python, TypeScript), เหมาะกับนักพัฒนา แต่ Learning curve สูงกว่า</li><li><strong>CDK</strong>: AWS เฉพาะ, ใช้ Python/TypeScript, เหมาะกับทีมที่ถนัด OOP</li></ul><h2>Write-Plan-Apply Workflow</h2><p>Terraform ทำงานผ่าน 3 ขั้นตอนหลัก คือ <code>terraform write</code> (เขียน HCL code), <code>terraform plan</code> (ดู Preview ว่าจะเกิดอะไรขึ้น), และ <code>terraform apply</code> (ลง Action จริง) กระบวนการนี้ทำให้ทีมสามารถ Review การเปลี่ยนแปลงก่อน Apply จริง ลดความเสี่ยงของ Human Error ได้อย่างมาก</p><h3>ประโยชน์สำหรับ Data Engineering Team</h3><ul><li><strong>Consistency</strong>: สภาพแวดล้อม Dev, Staging, Production เหมือนกันทุกประการ</li><li><strong>Version Control</strong>: ติดตามการเปลี่ยนแปลง Infrastructure ผ่าน Git ได้เหมือนโค้ดปกติ</li><li><strong>Collaboration</strong>: ทีมสามารถ Review Infrastructure Changes ผ่าน Pull Request</li><li><strong>Disaster Recovery</strong>: สร้าง Infrastructure ใหม่ทั้งหมดได้ภายในไม่กี่นาที</li><li><strong>Cost Management</strong>: เห็นและควบคุม Resources ที่มีอยู่ได้ชัดเจน</li><li><strong>Documentation</strong>: โค้ด HCL คือ Documentation ของ Infrastructure โดยตัวมันเอง</li></ul><p>สำหรับ Data Engineer การใช้ Terraform หมายถึงการสามารถ Spin up Data Pipeline Infrastructure ทั้งชุดได้ใน 1 คำสั่ง ตั้งแต่ S3 Bucket ไปจนถึง Glue Job, Redshift Cluster และ IAM Role ทั้งหมดนี้ใน <code>terraform apply</code> เดียว</p>' },
  { number: 1, slug: 'hcl-basics', emoji: '📝', title: 'HCL: Providers, Resources, Variables', content: '<h2>HCL Syntax พื้นฐาน</h2><p><strong>HashiCorp Configuration Language (HCL)</strong> คือภาษาที่ Terraform ใช้ในการอธิบาย Infrastructure ถูกออกแบบมาให้อ่านง่ายโดยมนุษย์และเครื่องจักร มีโครงสร้างหลักคือ Blocks, Arguments, และ Expressions</p><pre><code>block_type "label" "name" { argument = value }</code></pre><h2>Providers</h2><p><strong>Provider</strong> คือ Plugin ที่ทำให้ Terraform สื่อสารกับ Cloud Platform หรือ Service ต่าง ๆ ได้ เช่น <code>hashicorp/google</code> สำหรับ GCP หรือ <code>hashicorp/aws</code> สำหรับ AWS การ Configure Provider ทำในบล็อก <code>terraform</code> และ <code>provider</code></p><ul><li><code>required_providers</code>: ระบุ Provider ที่ต้องการและ Version ที่ต้องการ</li><li><code>provider "google" {}</code>: กำหนดค่า Project, Region, Credentials</li><li><code>provider "aws" {}</code>: กำหนดค่า Region, Profile, Access Key</li></ul><h2>Resources</h2><p><strong>Resource</strong> คือส่วนที่สำคัญที่สุดใน Terraform แต่ละ Resource Block อธิบาย Infrastructure Object 1 ชิ้น เช่น S3 Bucket, BigQuery Dataset หรือ IAM Role</p><pre><code>resource "google_bigquery_dataset" "raw" { dataset_id = "raw_data" location = "asia-southeast1" }</code></pre><h2>Variables และ Locals</h2><p><strong>Input Variables</strong> ทำให้ Configuration ยืดหยุ่น สามารถ Reuse ได้หลาย Environment โดยไม่ต้องแก้โค้ดหลัก ส่วน <strong>Local Values</strong> ใช้สำหรับ Expression ที่ซับซ้อนหรือใช้ซ้ำหลายครั้งภายใน Module</p><ul><li><code>variable "project_id" { type = string }</code>: ประกาศ Input Variable</li><li><code>locals { env = "prod" }</code>: ประกาศ Local Value</li><li><code>var.project_id</code>: อ้างอิง Variable</li><li><code>local.env</code>: อ้างอิง Local</li></ul><h2>Outputs</h2><p><strong>Output Values</strong> ใช้เพื่อ Export ข้อมูลจาก Terraform Configuration ออกมาให้ผู้ใช้หรือ Module อื่นนำไปใช้ต่อ เช่น ARN ของ S3 Bucket หรือ Connection String ของ Database</p><h2>Data Sources</h2><p><strong>Data Sources</strong> ช่วยให้ Terraform ดึงข้อมูลจาก Resources ที่มีอยู่แล้ว (ไม่ได้ถูก Manage โดย Terraform) มาใช้ใน Configuration ได้ เหมาะมากสำหรับการอ้างอิง Existing VPC, IAM Role หรือ Secret จาก Secret Manager</p><h2>terraform.tfvars</h2><p>ไฟล์ <code>terraform.tfvars</code> ใช้สำหรับกำหนดค่า Variable ตาม Environment โดยอัตโนมัติ Terraform จะโหลดไฟล์นี้ทันที หรือจะสร้างไฟล์ <code>prod.tfvars</code> และ <code>dev.tfvars</code> แยกกันก็ได้ แล้วใช้ <code>-var-file=prod.tfvars</code> ตอน Plan/Apply</p><h3>โครงสร้างโปรเจกต์ที่ดี</h3><ul><li><strong>main.tf</strong>: Resource declarations หลัก</li><li><strong>variables.tf</strong>: Input variable definitions</li><li><strong>outputs.tf</strong>: Output value definitions</li><li><strong>providers.tf</strong>: Provider configuration</li><li><strong>terraform.tfvars</strong>: Variable values</li></ul>' },
  { number: 2, slug: 'gcp-terraform', emoji: '☁️', title: 'Terraform บน GCP สำหรับ DE', content: '<h2>Google Provider Setup</h2><p>การเริ่มต้นใช้ Terraform กับ GCP ต้องตั้งค่า <strong>google provider</strong> ก่อน โดยระบุ Project ID, Region, และ Credentials Service Account ที่มีสิทธิ์เพียงพอ วิธีที่ดีที่สุดคือใช้ Application Default Credentials (ADC) หรือ Service Account Key ที่จัดการผ่าน Secret Manager</p><pre><code>provider "google" { project = var.project_id region = "asia-southeast1" }</code></pre><h2>BigQuery Datasets และ Tables</h2><p><strong>BigQuery</strong> คือ Data Warehouse หลักของ GCP และเป็นหัวใจของ Data Stack ส่วนใหญ่ การสร้าง Dataset และ Table ผ่าน Terraform ช่วยให้มั่นใจว่า Schema, Access Controls, และ Labels ถูกต้องในทุก Environment</p><ul><li><code>google_bigquery_dataset</code>: สร้าง Dataset พร้อม Location, Default Table Expiration, Access Policy</li><li><code>google_bigquery_table</code>: สร้าง Table พร้อม Schema JSON, Partitioning, Clustering</li><li><code>google_bigquery_dataset_iam_member</code>: กำหนด IAM สำหรับ Dataset</li></ul><pre><code>resource "google_bigquery_table" "events" { dataset_id = google_bigquery_dataset.raw.dataset_id table_id = "events" time_partitioning { type = "DAY" field = "event_date" } }</code></pre><h2>GCS Buckets</h2><p><strong>Google Cloud Storage</strong> คือ Data Lake Layer ของ GCP ที่ใช้เก็บ Raw Data, Processed Data, และ Terraform State File การสร้าง Bucket ผ่าน Terraform ช่วยกำหนด Lifecycle Rules, Versioning, CORS, และ IAM ได้อย่างครบถ้วน</p><ul><li><code>google_storage_bucket</code>: สร้าง Bucket พร้อม Location, Storage Class, Uniform Access</li><li><code>lifecycle_rule</code>: กำหนดการย้าย/ลบ Object อัตโนมัติ เช่น ย้ายไป Coldline หลัง 90 วัน</li><li><code>google_storage_bucket_iam_member</code>: กำหนดสิทธิ์การเข้าถึง Bucket</li></ul><h2>Cloud Composer Environments</h2><p><strong>Cloud Composer</strong> คือ Managed Apache Airflow บน GCP เป็น Orchestration Layer หลักของ Data Pipeline การสร้างผ่าน Terraform ช่วยกำหนด Machine Type, Node Count, Python Packages, Environment Variables, และ Airflow Config ได้ทั้งหมด</p><h2>IAM Bindings</h2><p>การจัดการ IAM ผ่าน Terraform เป็นสิ่งสำคัญมากสำหรับ Data Engineering เพื่อให้แต่ละ Service Account มีสิทธิ์เฉพาะที่จำเป็น (Least Privilege)</p><ul><li><code>google_project_iam_member</code>: กำหนด Role ระดับ Project</li><li><code>google_service_account</code>: สร้าง Service Account</li><li><code>google_service_account_iam_binding</code>: กำหนดว่าใครใช้ SA นี้ได้</li></ul><h3>ตัวอย่าง DE Stack บน GCP</h3><p>ด้วย Terraform คุณสามารถสร้าง GCP DE Stack ทั้งชุดใน <code>terraform apply</code> เดียว รวมถึง Raw/Processed/Curated GCS Buckets, BigQuery Datasets สำหรับแต่ละ Layer, Cloud Composer Environment, Service Accounts สำหรับแต่ละ Component, และ IAM Bindings ทั้งหมด</p>' },
  { number: 3, slug: 'aws-terraform', emoji: '🌩️', title: 'Terraform บน AWS สำหรับ DE', content: '<h2>AWS Provider Configuration</h2><p>การตั้งค่า <strong>AWS Provider</strong> ใน Terraform ต้องระบุ Region และ Authentication Method ที่ใช้ วิธีที่ปลอดภัยที่สุดสำหรับ Production คือการใช้ IAM Role ผ่าน Instance Profile หรือ OIDC (สำหรับ CI/CD) แทนการใช้ Access Key โดยตรง</p><pre><code>provider "aws" { region = var.aws_region default_tags { tags = { Project = var.project_name Environment = var.environment } } }</code></pre><h2>S3 Buckets และ Policies</h2><p><strong>Amazon S3</strong> คือ Data Lake Layer หลักของ AWS DE Stack Terraform ช่วยสร้าง S3 Bucket พร้อมการตั้งค่าด้านความปลอดภัยและการจัดการข้อมูลอย่างครบถ้วน</p><ul><li><code>aws_s3_bucket</code>: สร้าง Bucket พร้อมชื่อและ Tags</li><li><code>aws_s3_bucket_versioning</code>: เปิด Versioning สำหรับ Audit Trail</li><li><code>aws_s3_bucket_server_side_encryption_configuration</code>: เข้ารหัสด้วย SSE-S3 หรือ SSE-KMS</li><li><code>aws_s3_bucket_lifecycle_configuration</code>: กำหนด Lifecycle ย้ายไป Glacier หลัง 90 วัน</li><li><code>aws_s3_bucket_policy</code>: กำหนด Bucket Policy ควบคุมการเข้าถึง</li></ul><h2>AWS Glue: Databases และ Crawlers</h2><p><strong>AWS Glue</strong> คือ Managed ETL Service ที่ใช้สร้าง Data Catalog และรัน ETL Jobs บน AWS การจัดการผ่าน Terraform ทำให้ Environment สอดคล้องกันระหว่าง Dev และ Production</p><ul><li><code>aws_glue_catalog_database</code>: สร้าง Glue Database สำหรับ Data Catalog</li><li><code>aws_glue_crawler</code>: สร้าง Crawler สแกน S3 และสร้าง Table Schema อัตโนมัติ</li><li><code>aws_glue_job</code>: สร้าง Glue ETL Job พร้อม Script Location และ Worker Configuration</li><li><code>aws_iam_role</code>: สร้าง IAM Role สำหรับ Glue Service</li></ul><h2>Redshift Clusters</h2><p><strong>Amazon Redshift</strong> คือ Data Warehouse ของ AWS ที่นิยมใช้กับ DE Stack การสร้างผ่าน Terraform ช่วยกำหนด Cluster Configuration, Subnet Group, Security Group, และ Parameter Group ได้อย่างสม่ำเสมอ</p><ul><li><code>aws_redshift_cluster</code>: กำหนด Node Type, Number of Nodes, Database Name, Credentials</li><li><code>aws_redshift_subnet_group</code>: กำหนด VPC Subnets สำหรับ Cluster</li><li><code>aws_redshift_parameter_group</code>: ปรับแต่ง Cluster Parameters</li></ul><h2>Terraform Modules สำหรับ AWS DE Stack</h2><p>การใช้ Modules ทำให้สามารถ Package AWS DE Infrastructure ไว้เป็น Reusable Unit ได้ เช่น สร้าง Module <em>data-lake</em> ที่รวม S3 Buckets, Bucket Policies, KMS Keys, และ IAM Roles ไว้ด้วยกัน แล้ว Call Module เดียวก็ได้ทั้ง Data Lake Infrastructure</p><h3>ตัวอย่างโครงสร้าง AWS DE Stack</h3><ul><li><strong>Storage Layer</strong>: S3 Buckets (raw, processed, curated, athena-results)</li><li><strong>Catalog Layer</strong>: Glue Data Catalog Databases + Crawlers</li><li><strong>Compute Layer</strong>: Glue Jobs, EMR Clusters หรือ Lambda Functions</li><li><strong>Warehouse Layer</strong>: Redshift Cluster หรือ Redshift Serverless</li><li><strong>Orchestration</strong>: MWAA (Managed Airflow) หรือ Step Functions</li></ul>' },
  { number: 4, slug: 'state', emoji: '💾', title: 'Remote State และ State Locking', content: '<h2>terraform.tfstate คืออะไร?</h2><p><strong>Terraform State</strong> คือไฟล์ <code>terraform.tfstate</code> ที่ Terraform ใช้เก็บข้อมูลว่า Infrastructure ในโลกจริงมีหน้าตาอย่างไร ประกอบด้วย Resource IDs, Properties, และ Dependencies ทั้งหมด State File นี้สำคัญมาก ถ้าหาย Terraform จะไม่รู้ว่า Resources ที่มีอยู่คืออะไร</p><h3>ปัญหาของ Local State</h3><ul><li>เก็บ State บนเครื่องตัวเอง ทีมอื่นเข้าถึงไม่ได้</li><li>ถ้าเครื่องพัง State หาย</li><li>ถ้า 2 คน Apply พร้อมกัน State Conflict เกิดขึ้นได้</li><li>State มักมี Sensitive Data (Passwords, Keys) ที่ไม่ควรอยู่ในเครื่อง Local</li></ul><h2>Remote Backend</h2><p><strong>Remote Backend</strong> คือการเก็บ State File ไว้บน Cloud Storage แทนเครื่อง Local ทำให้ทีมทุกคนใช้ State เดียวกัน และ State ถูก Backup โดยอัตโนมัติ</p><h3>GCS Backend (สำหรับ GCP)</h3><pre><code>terraform { backend "gcs" { bucket = "my-terraform-state" prefix = "terraform/state" } }</code></pre><ul><li>สร้าง GCS Bucket ก่อนด้วย <code>gsutil mb</code> หรือ Terraform เอง (แต่ต้องระวัง Chicken-and-egg)</li><li>เปิด Versioning บน Bucket เสมอ เพื่อ Rollback State ได้</li><li>เปิด Uniform Bucket Access และ Encryption</li></ul><h3>S3 Backend (สำหรับ AWS)</h3><pre><code>terraform { backend "s3" { bucket = "my-terraform-state" key = "prod/terraform.tfstate" region = "ap-southeast-1" dynamodb_table = "terraform-lock" encrypt = true } }</code></pre><h2>State Locking</h2><p><strong>State Locking</strong> คือกลไกป้องกันไม่ให้ 2 คนรัน Terraform พร้อมกัน ซึ่งจะทำให้ State Corrupt ได้</p><ul><li><strong>DynamoDB Locking (AWS)</strong>: สร้าง DynamoDB Table ที่มี Primary Key <code>LockID</code> ชนิด String แล้วระบุใน Backend Config</li><li><strong>GCS Locking (GCP)</strong>: GCS Backend รองรับ Locking ผ่าน Object Metadata โดยอัตโนมัติ ไม่ต้องสร้าง Resource เพิ่ม</li></ul><h2>State Commands สำคัญ</h2><ul><li><code>terraform state list</code>: แสดง Resources ทั้งหมดใน State</li><li><code>terraform state show resource_type.name</code>: แสดงรายละเอียด Resource ใน State</li><li><code>terraform state mv</code>: ย้าย Resource ใน State (เช่นตอน Refactor)</li><li><code>terraform state rm</code>: ลบ Resource ออกจาก State โดยไม่ Destroy</li></ul><h2>terraform import, taint, refresh</h2><ul><li><code>terraform import</code>: นำ Resource ที่มีอยู่แล้วใน Cloud เข้ามาใน State โดยไม่ต้อง Destroy แล้วสร้างใหม่</li><li><code>terraform taint</code> (เก่า) / <code>terraform apply -replace</code> (ใหม่): บังคับให้ Terraform Destroy และ Recreate Resource นั้นในครั้งถัดไป</li><li><code>terraform refresh</code>: ซิงค์ State ให้ตรงกับ Cloud ปัจจุบัน (ใช้ระวัง อาจทำให้ Plan เปลี่ยน)</li></ul><h3>Best Practices สำหรับ State Management</h3><p>ควรแยก State ตาม Environment (dev, staging, prod) และแยก State ตาม Component ใหญ่ ๆ เช่น networking, data-lake, warehouse เพื่อลด Blast Radius ถ้า State มีปัญหา</p>' },
  { number: 5, slug: 'modules', emoji: '📦', title: 'Reusable Terraform Modules', content: '<h2>Terraform Module คืออะไร?</h2><p><strong>Module</strong> ใน Terraform คือชุดของ <code>.tf</code> files ที่รวมกันเป็น Logical Unit ที่สามารถ Reuse ได้ ทุก Terraform Directory คือ Module โดยธรรมชาติ เมื่อเราเรียก Module อื่น มันเรียกว่า Child Module และ Module ที่เรียกคือ Root Module</p><h2>โครงสร้าง Module ที่ดี</h2><pre><code>modules/ └── data-lake/ ├── main.tf ├── variables.tf ├── outputs.tf └── README.md</code></pre><ul><li><strong>main.tf</strong>: Resource definitions ทั้งหมดของ Module</li><li><strong>variables.tf</strong>: Input variables ที่ Module ต้องการ</li><li><strong>outputs.tf</strong>: ค่าที่ Module ส่งออกไปให้ Parent Module</li><li><strong>README.md</strong>: Documentation วิธีใช้ Module, Input/Output ที่รองรับ</li></ul><h2>การเรียกใช้ Module</h2><p>การ Call Module ทำได้ง่ายโดยระบุ <code>source</code> ซึ่งอาจเป็น Local Path, Git URL, หรือ Terraform Registry</p><pre><code>module "raw_data_lake" { source = "./modules/data-lake" bucket_name = "my-project-raw" environment = var.environment location = "asia-southeast1" }</code></pre><h2>Terraform Module Registry</h2><p><strong>Terraform Registry</strong> (registry.terraform.io) คือ Repository สาธารณะที่มี Community Modules สร้างไว้แล้ว สำหรับ DE ที่น่าสนใจ ได้แก่ Modules สำหรับ S3, GCS, BigQuery, VPC, และอีกมากมาย สามารถใช้ได้โดยระบุ Source เป็น <code>hashicorp/s3-bucket/aws</code></p><h2>สร้าง DE-Specific Modules</h2><p>ทีม DE ควรสร้าง Module เฉพาะสำหรับ Pattern ที่ใช้บ่อย เช่น</p><ul><li><strong>data-lake-layer</strong>: สร้าง S3/GCS Bucket พร้อม Lifecycle, Versioning, Encryption, IAM</li><li><strong>glue-job</strong>: สร้าง Glue Job พร้อม IAM Role, S3 Script Location, Connection</li><li><strong>bigquery-dataset</strong>: สร้าง BQ Dataset พร้อม Access Controls, Default Expiration</li><li><strong>airflow-env</strong>: สร้าง Cloud Composer / MWAA Environment พร้อม Dependencies ทั้งหมด</li></ul><h2>Module Versioning</h2><p>เมื่อ Module ถูกเก็บใน Git Repository ควรใช้ Git Tag เป็น Version เพื่อ Pin Version ที่ใช้และไม่ให้การเปลี่ยนแปลง Module กระทบทุก Environment พร้อมกัน</p><pre><code>module "data_lake" { source = "git::https://github.com/myorg/terraform-modules.git//data-lake?ref=v1.2.0" }</code></pre><h3>ข้อดีของการใช้ Modules</h3><ul><li><em>DRY (Don\'t Repeat Yourself)</em>: เขียน Infrastructure Logic ครั้งเดียว ใช้ได้ทุก Project</li><li><em>Consistency</em>: ทุก Team ใช้ Standard เดียวกันในการสร้าง Resources</li><li><em>Testing</em>: Test Module ได้แยกต่างหาก ก่อน Deploy ใน Production</li><li><em>Abstraction</em>: ซ่อน Complexity ไว้ใน Module ให้ผู้ใช้เห็นแค่ Input/Output ที่จำเป็น</li></ul>' },
  { number: 6, slug: 'workspaces', emoji: '🔀', title: 'Terraform Workspaces: dev/staging/prod', content: '<h2>Terraform Workspaces คืออะไร?</h2><p><strong>Terraform Workspace</strong> คือกลไกการจัดการ State หลาย ๆ ชุดใน Configuration เดียวกัน แทนที่จะมี State File เดียว แต่ละ Workspace จะมี State File ของตัวเอง ทำให้สามารถ Deploy Infrastructure ชุดเดียวกันไปหลาย Environment ได้โดยใช้ Code เดียวกัน</p><h2>Workspace Commands</h2><ul><li><code>terraform workspace list</code>: แสดง Workspace ทั้งหมดที่มี</li><li><code>terraform workspace new dev</code>: สร้าง Workspace ใหม่ชื่อ dev</li><li><code>terraform workspace select staging</code>: สลับไป Workspace staging</li><li><code>terraform workspace show</code>: แสดง Workspace ปัจจุบัน</li><li><code>terraform workspace delete old-workspace</code>: ลบ Workspace ที่ไม่ใช้แล้ว</li></ul><h2>การใช้ Workspace สำหรับ Environment Separation</h2><p>ใน HCL สามารถอ้างอิง Workspace ปัจจุบันได้ผ่าน <code>terraform.workspace</code> เพื่อปรับ Configuration ตาม Environment</p><pre><code>locals { env_config = { dev = { instance_type = "db.t3.micro" node_count = 1 } prod = { instance_type = "db.r5.2xlarge" node_count = 3 } } config = local.env_config[terraform.workspace] }</code></pre><h2>Variable Overrides ตาม Workspace</h2><p>วิธีที่นิยมคือสร้างไฟล์ <code>tfvars</code> แยกต่าม Environment และ Pass ตอน Apply</p><ul><li><code>terraform apply -var-file=envs/dev.tfvars</code>: Apply ด้วยค่า dev</li><li><code>terraform apply -var-file=envs/prod.tfvars</code>: Apply ด้วยค่า prod</li><li>ตั้งชื่อ Resource ให้รวม Workspace: <code>name = "data-lake-${terraform.workspace}"</code></li></ul><h2>Workspace Best Practices สำหรับ DE Teams</h2><ul><li><strong>ใช้ Workspace สำหรับ Lightweight Isolation</strong>: เหมาะสำหรับ Infrastructure ที่คล้ายกันมาก แต่ต่าง Scale</li><li><strong>ไม่ใช้ Workspace สำหรับ Critical Production</strong>: สำหรับ Production ที่แตกต่างมาก ควรใช้ Separate State + Separate Directory แทน</li><li><strong>กำหนดสิทธิ์ตาม Workspace</strong>: ใน CI/CD ให้ Developer Apply ได้แค่ dev Workspace ส่วน prod ต้องผ่าน Approval</li><li><strong>ตั้ง Resource Naming Convention</strong>: ชื่อ Resource ทุกตัวควรรวม Environment ไว้เพื่อไม่ให้สับสน เช่น <code>raw-data-dev</code> vs <code>raw-data-prod</code></li></ul><h3>เปรียบเทียบ Workspace vs Directory Structure</h3><ul><li><em>Workspace</em>: Simple, Code เหมือนกัน, ต่าง State เท่านั้น, เหมาะกับ Small Team</li><li><em>Separate Directories</em>: ซับซ้อนกว่า แต่ Isolate ได้ดีกว่า รองรับ Configuration ที่แตกต่างกันมาก เหมาะกับ Enterprise</li></ul><p>สำหรับทีม DE ขนาดกลาง แนะนำให้ใช้ Workspaces สำหรับ dev/staging และใช้ Separate State File สำหรับ Production เพื่อความปลอดภัยสูงสุด</p>' },
  { number: 7, slug: 'cicd-terraform', emoji: '⚙️', title: 'CI/CD กับ Terraform: GitHub Actions', content: '<h2>ทำไมต้องมี CI/CD สำหรับ Terraform?</h2><p>การรัน Terraform ด้วยมือในทุก Environment เปิดโอกาสให้เกิด Human Error สูง ทีมหนึ่งอาจ Apply โดยไม่มีคนอื่น Review การนำ Terraform เข้า <strong>CI/CD Pipeline</strong> ช่วยบังคับให้ทุก Infrastructure Change ผ่านกระบวนการ Review + Approval และมี Audit Trail ที่ตรวจสอบได้</p><h2>GitHub Actions Workflow สำหรับ Terraform</h2><p>Pattern ที่นิยมที่สุดคือ <strong>Plan on Pull Request, Apply on Merge</strong> กล่าวคือ ทุกครั้งที่เปิด PR จะมีการรัน <code>terraform plan</code> และแสดงผลใน PR Comment เพื่อให้ทีม Review ว่าจะเกิดอะไรขึ้น และเมื่อ Merge เข้า main แล้ว จะรัน <code>terraform apply</code> อัตโนมัติ</p><pre><code>name: Terraform CI on: pull_request: push: branches: [main] jobs: terraform: runs-on: ubuntu-latest steps: - uses: actions/checkout@v3 - uses: hashicorp/setup-terraform@v2 - run: terraform init - run: terraform plan -out=tfplan - if: github.ref == \'refs/heads/main\' run: terraform apply tfplan</code></pre><h2>Secrets Management ใน GitHub Actions</h2><p>ไม่ควร Hard-code Credentials ลงใน Workflow File เด็ดขาด วิธีที่แนะนำ ได้แก่</p><ul><li><strong>GitHub Secrets</strong>: เก็บ AWS Access Key, GCP Service Account JSON ใน Repository/Organization Secrets แล้วอ้างอิงด้วย <code>${{ secrets.AWS_ACCESS_KEY_ID }}</code></li><li><strong>OIDC (OpenID Connect)</strong>: วิธีที่ดีที่สุดสำหรับ AWS และ GCP ทำให้ GitHub Actions สามารถ Assume IAM Role โดยไม่ต้องมี Long-lived Credentials เลย</li><li><strong>Vault / Secret Manager</strong>: ดึง Secrets จาก HashiCorp Vault หรือ AWS Secrets Manager ใน Runtime</li></ul><h2>Workflow ที่สมบูรณ์สำหรับ DE Team</h2><ul><li><strong>Feature Branch</strong>: Developer แก้ Terraform Code แล้วเปิด PR</li><li><strong>Plan Stage</strong>: CI รัน <code>fmt check</code>, <code>validate</code>, <code>plan</code> และโพสต์ผลใน PR</li><li><strong>Review</strong>: Senior DE หรือ Platform Team Approve การเปลี่ยนแปลง</li><li><strong>Apply Stage</strong>: หลัง Merge CI รัน <code>apply</code> อัตโนมัติและส่ง Notification ไปยัง Slack</li></ul><h2>Atlantis: Alternative สำหรับทีมใหญ่</h2><p><strong>Atlantis</strong> คือ Open-source Tool ที่ทำงานเป็น Self-hosted Server รับ Webhook จาก GitHub/GitLab/Bitbucket และรัน Terraform Plan/Apply ตาม Comment ใน PR เช่น พิมพ์ <code>atlantis plan</code> หรือ <code>atlantis apply</code> ใน PR Comment ข้อดีคือ State Lock จัดการดีกว่า และมี Approval Workflow ที่ยืดหยุ่นกว่า GitHub Actions</p><h3>เครื่องมืออื่นที่น่าสนใจ</h3><ul><li><strong>Terraform Cloud</strong>: Managed Service จาก HashiCorp รองรับ Remote Execution, State Management, Policy as Code</li><li><strong>Spacelift</strong>: Enterprise-grade Terraform Automation Platform</li><li><strong>env0</strong>: IaC Management Platform ที่เน้น Cost Visibility</li></ul>' },
  { number: 8, slug: 'de-stack', emoji: '🏛️', title: 'End-to-end DE Stack ด้วย Terraform', content: '<h2>Complete DE Infrastructure ด้วย Terraform</h2><p>บทนี้จะรวมทุกอย่างที่เรียนมาเพื่อสร้าง <strong>End-to-end Data Engineering Stack</strong> ที่สมบูรณ์ด้วย Terraform ครอบคลุม Data Lake, Data Warehouse, Orchestration, Monitoring, และ Security ทั้งหมดในโครงสร้างโปรเจกต์ที่ Production-ready</p><h2>โครงสร้างโปรเจกต์ Multi-environment</h2><pre><code>de-infrastructure/ ├── modules/ │ ├── data-lake/ │ ├── data-warehouse/ │ ├── orchestration/ │ └── monitoring/ ├── environments/ │ ├── dev/ │ │ ├── main.tf │ │ └── terraform.tfvars │ ├── staging/ │ └── prod/ └── global/ └── iam/</code></pre><h2>Data Lake Layer</h2><p>Data Lake Layer ประกอบด้วย Storage Buckets หลายชั้น ตาม Medallion Architecture</p><ul><li><strong>Bronze/Raw Layer</strong>: เก็บ Raw Data จาก Source Systems ไม่แปลงข้อมูลเลย เปิด Versioning ไว้</li><li><strong>Silver/Processed Layer</strong>: Cleaned และ Validated Data พร้อม Partitioning ที่เหมาะสม</li><li><strong>Gold/Curated Layer</strong>: Business-ready Data ที่ผ่าน Aggregation และ Business Logic แล้ว</li><li><strong>Athena/BQ Results</strong>: Bucket สำหรับ Query Results เพื่อควบคุม Cost</li></ul><h2>Data Warehouse Layer</h2><p>สำหรับ GCP Stack ใช้ <strong>BigQuery</strong> เป็น Warehouse หลัก โดยสร้าง Datasets แยกตาม Zone และ Access Level สำหรับ AWS Stack ใช้ <strong>Redshift</strong> หรือ <strong>Redshift Serverless</strong> พร้อม Workgroup Configuration</p><h2>Orchestration Layer</h2><p>Orchestration คือส่วนที่ซับซ้อนที่สุดในการ Provision ผ่าน Terraform เนื่องจากต้องการ Dependencies หลายอย่าง</p><ul><li><strong>GCP</strong>: Cloud Composer Environment พร้อม Service Account, GKE Node Pool Config, Python Packages</li><li><strong>AWS</strong>: MWAA (Amazon Managed Workflows for Apache Airflow) พร้อม S3 DAG Bucket, Security Group, Execution Role</li></ul><h2>Monitoring Stack</h2><ul><li><code>google_monitoring_alert_policy</code>: Alert สำหรับ Pipeline Failures, High Cost, Latency</li><li><code>aws_cloudwatch_metric_alarm</code>: Monitor Glue Job Failures, Redshift Query Duration</li><li><strong>Grafana/Datadog</strong>: สร้าง Dashboards ผ่าน Terraform Providers ของ Grafana/Datadog</li></ul><h2>Multi-environment Best Practices</h2><ul><li><strong>Shared Modules, Separate State</strong>: Module เดิม แต่ State แยกต่างหากทุก Environment</li><li><strong>Resource Naming Convention</strong>: <code>{project}-{env}-{component}</code> เช่น <code>myco-prod-data-lake</code></li><li><strong>Tagging Strategy</strong>: Tag ทุก Resource ด้วย Environment, Team, CostCenter, Project</li><li><strong>Gradual Rollout</strong>: Deploy ไป dev ก่อน ทดสอบ แล้วค่อย Promote ไป staging และ prod</li></ul>' },
  { number: 9, slug: 'best-practices', emoji: '🛡️', title: 'Security และ Best Practices', content: '<h2>Security Best Practices สำหรับ Terraform</h2><p>การใช้ Terraform ใน Production ต้องคำนึงถึง Security อย่างจริงจัง เนื่องจาก Terraform มีสิทธิ์ในการสร้าง แก้ไข และลบ Resources ใน Cloud Account ถ้าถูก Compromise อาจส่งผลร้ายแรงมาก บทนี้รวบรวม Best Practices ด้านความปลอดภัยที่ DE Team ควรทำ</p><h2>State File Encryption</h2><p><strong>State File</strong> มักมีข้อมูล Sensitive เช่น Database Passwords, API Keys, Connection Strings ดังนั้นต้องให้ความสำคัญกับการเข้ารหัส</p><ul><li><strong>GCS Backend</strong>: เปิด Customer-managed Encryption Keys (CMEK) ผ่าน Cloud KMS</li><li><strong>S3 Backend</strong>: ใช้ <code>encrypt = true</code> และระบุ KMS Key ARN ใน Backend Config</li><li><strong>Terraform Cloud</strong>: State ถูกเข้ารหัสโดยอัตโนมัติและมี Access Control ในตัว</li><li><strong>ไม่ควร Commit State File ลง Git</strong> เด็ดขาด ต้องเพิ่ม <code>*.tfstate</code> ใน <code>.gitignore</code></li></ul><h2>Least Privilege IAM</h2><p>Service Account หรือ IAM Role ที่ Terraform ใช้ควรมีสิทธิ์เฉพาะที่จำเป็นสำหรับ Resources ที่ต้อง Manage เท่านั้น ไม่ควรให้ Admin Access เด็ดขาด</p><ul><li>สร้าง Dedicated Service Account สำหรับ Terraform แต่ละ Module</li><li>ใช้ <strong>OIDC Federation</strong> แทน Long-lived Credentials ใน CI/CD</li><li>Review และ Audit IAM Permissions เป็นประจำด้วย Policy Analyzer</li><li>ใช้ <code>terraform plan</code> เพื่อ Review ว่า IAM Change อะไรบ้างก่อน Apply</li></ul><h2>Secret Management</h2><p>ไม่ควร Hard-code Secrets ใน <code>.tf</code> Files หรือ <code>terraform.tfvars</code> เด็ดขาด วิธีที่ถูกต้อง</p><ul><li><strong>AWS Secrets Manager / GCP Secret Manager</strong>: ดึง Secrets ผ่าน Data Source ใน Terraform</li><li><strong>HashiCorp Vault</strong>: Centralized Secret Management สำหรับ Multi-cloud</li><li><strong>Environment Variables</strong>: Pass Sensitive Values ผ่าน <code>TF_VAR_*</code> Environment Variables ใน CI/CD</li></ul><h2>Code Review และ Policy as Code</h2><ul><li><strong>tflint</strong>: Linter สำหรับ Terraform Code ตรวจหา Error และ Best Practice Violations</li><li><strong>tfsec / Checkov</strong>: Static Security Analysis ตรวจหา Security Misconfigurations เช่น S3 Bucket ที่เปิด Public Access</li><li><strong>Sentinel (Terraform Cloud)</strong>: Policy as Code Framework บังคับ Governance Rules ก่อน Apply</li><li><strong>OPA (Open Policy Agent)</strong>: Alternative Open-source Policy Engine</li></ul><h2>Tagging Strategy</h2><p>การ Tag Resources ทุกชิ้นอย่างสม่ำเสมอช่วยใน Cost Management, Security Audit, และ Operational Excellence</p><pre><code>default_tags = { Environment = terraform.workspace Project = var.project_name Team = "data-engineering" CostCenter = var.cost_center ManagedBy = "terraform" }</code></pre><h2>Cost Management</h2><ul><li>ใช้ <strong>Infracost</strong> ใน CI/CD เพื่อประมาณ Cost ก่อน Apply</li><li>ตั้ง <strong>Budget Alerts</strong> ผ่าน Terraform (<code>google_billing_budget</code>, <code>aws_budgets_budget</code>)</li><li>ตั้ง <strong>Lifecycle Rules</strong> บน Storage เพื่อลบหรือ Archive Data เก่าอัตโนมัติ</li><li>ใช้ <strong>terraform destroy</strong> บน dev Environment ตอนไม่ใช้เพื่อประหยัด Cost</li><li>Review <code>terraform plan</code> สำหรับการเปลี่ยนแปลงที่อาจเพิ่ม Cost เช่น การ Resize Cluster</li></ul>' },
];
