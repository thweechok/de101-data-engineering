export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '🗺️',
    title: 'คอร์สนี้จะสอนอะไร? DE คืออะไร? เส้นทางสู่ DE',
    content: '<h2>🗺️ ยินดีต้อนรับสู่ DE101 — จุดเริ่มต้นของ Data Engineer</h2>' +
      '<p>ถ้าคุณเปิดคอร์สนี้ขึ้นมา แสดงว่าคุณสนใจที่จะเป็น <strong>Data Engineer (DE)</strong> — หนึ่งในอาชีพที่ร้อนแรงที่สุดในวงการ Tech ของไทยและทั่วโลก ในช่วงปี 2024-2025 ความต้องการ DE เพิ่มขึ้นกว่า 40% ต่อปี ขณะที่เงินเดือนเฉลี่ยในไทยอยู่ที่ 60,000-150,000 บาท/เดือน</p>' +
      '<h3>📌 คอร์สนี้สอนอะไร?</h3>' +
      '<p>DE101 เป็น <strong>คอร์ส Beginner ที่สมบูรณ์ที่สุด</strong> สำหรับคนที่ต้องการเริ่มต้นเป็น Data Engineer จากศูนย์ ไม่จำเป็นต้องมีพื้นฐาน Programming แต่ถ้ามีจะเรียนได้เร็วขึ้น</p>' +
      '<pre><code class="language-text">DE101 — Learning Roadmap\n========================\n\nPhase 1: Foundation (บทที่ 1-4)\n  → เข้าใจบทบาท DE\n  → Linux & Terminal พื้นฐาน\n  → Python สำหรับ DE\n  → SQL ที่ใช้ทุกวัน\n\nPhase 2: Data Concepts (บทที่ 5-6)\n  → Data Warehouse, Lake, Lakehouse\n  → ETL/ELT Pipeline\n\nPhase 3: Core Tools (บทที่ 7-12)\n  → Apache Airflow\n  → Docker\n  → Cloud Computing\n  → BigQuery\n  → dbt\n  → Apache Spark\n\nPhase 4: Professional Skills (บทที่ 13-15)\n  → Data Quality\n  → Monitoring & Alerting\n  → Portfolio Project</code></pre>' +
      '<h3>🎯 ใครเหมาะกับคอร์สนี้?</h3>' +
      '<ul><li>👨‍💻 นักเรียน/นักศึกษาที่อยากทำงานสาย Data</li><li>💼 คนทำงาน IT ที่อยากเปลี่ยนสายมาเป็น DE</li><li>📊 Data Analyst ที่อยาก Upskill เป็น DE</li><li>🔄 Backend Developer ที่สนใจ Data Pipeline</li><li>🆕 คนที่ยังไม่มีพื้นฐาน Programming เลย</li></ul>' +
      '<h3>🗺️ เส้นทางสู่การเป็น Data Engineer</h3>' +
      '<pre><code class="language-text">เส้นทาง DE ใน 12-18 เดือน\n===========================\n\nเดือนที่ 1-3:  Foundation\n  ✓ Python, SQL, Linux\n  ✓ เข้าใจ Data Concepts\n\nเดือนที่ 4-6:  Core Tools\n  ✓ Airflow, Docker, Cloud\n  ✓ BigQuery, dbt\n\nเดือนที่ 7-9:  Advanced\n  ✓ Spark, Kafka\n  ✓ Data Quality, Monitoring\n\nเดือนที่ 10-12: Job Ready\n  ✓ Portfolio Project\n  ✓ สมัครงาน + Interview\n  ✓ ได้งาน DE แรก! 🎉</code></pre>' +
      '<div class="tip-box"><strong>💡 Tips:</strong> อย่ากดดันตัวเองมากเกินไป การเรียน DE ไม่ใช่การท่องจำเครื่องมือ แต่คือการเข้าใจว่า <em>ทำไมเราถึงต้องใช้เครื่องมือนั้น</em> เรียนช้าแต่เข้าใจลึกดีกว่าเรียนเร็วแต่ไม่รู้จริง</div>' +
      '<h3>⚙️ Setup ที่ต้องเตรียม</h3>' +
      '<p>ตลอดคอร์สเราจะใช้เครื่องมือเหล่านี้ ทุกอย่างฟรีหมด:</p>' +
      '<ul><li><strong>VS Code</strong> — Code Editor หลัก</li><li><strong>Python 3.10+</strong> — ภาษาหลักของ DE</li><li><strong>Git</strong> — Version Control</li><li><strong>Docker Desktop</strong> — สำหรับ Run Services</li><li><strong>Google Cloud Free Tier</strong> — สำหรับ BigQuery</li><li><strong>DBeaver</strong> — SQL Client</li></ul>' +
      '<p>ไม่ต้องกังวล ในบทที่ 2 เราจะ Setup ทุกอย่างด้วยกัน ทีละขั้นตอน</p>' +
      '<h3>🏆 เป้าหมายปลายทาง</h3>' +
      '<p>เมื่อเรียนจบ DE101 คุณจะสามารถ:</p>' +
      '<ul><li>✅ ออกแบบและสร้าง Data Pipeline แบบ End-to-End ได้</li><li>✅ ใช้ Airflow, dbt, Spark, BigQuery ได้จริง</li><li>✅ เข้าใจ Cloud Architecture สำหรับ Data</li><li>✅ มี Portfolio Project ที่ใส่ใน Resume ได้</li><li>✅ ผ่าน Technical Interview ระดับ Junior DE ได้</li></ul>' +
      '<div class="tip-box"><strong>🚀 Let\'s go!</strong> เริ่มจากบทที่ 1 กันเลย — เราจะไปรู้จัก Data Engineer ให้ชัดขึ้นก่อนว่าจริงๆ แล้วทำงานอะไร ได้เงินเดือนเท่าไหร่ และต่างจากอาชีพอื่นยังไง</div>'
  },
  {
    number: 1,
    slug: 'what-is-de',
    emoji: '👷',
    title: 'Data Engineer คือใคร? เงินเดือนเท่าไหร่? ต่างจาก DS ยังไง?',
    content: '<h2>👷 Data Engineer คือใคร?</h2>' +
      '<p><strong>Data Engineer (DE)</strong> คือคนที่สร้างและดูแล "ท่อส่งน้ำ" ของข้อมูลในองค์กร — ถ้าเปรียบข้อมูลเป็นน้ำ DE คือช่างที่วางท่อ ติดตั้งปั๊ม และตรวจสอบให้น้ำไหลถึงปลายทางอย่างถูกต้องและครบถ้วน</p>' +
      '<p>ในชีวิตจริง DE ทำงานเกี่ยวกับ:</p>' +
      '<ul><li>📥 <strong>Ingestion</strong> — ดึงข้อมูลจาก Source ต่างๆ (APIs, Databases, Files, Streams)</li><li>🔄 <strong>Transformation</strong> — แปลงข้อมูลดิบให้เป็นรูปแบบที่ใช้งานได้</li><li>💾 <strong>Storage</strong> — จัดเก็บข้อมูลใน Data Warehouse / Data Lake</li><li>⚙️ <strong>Orchestration</strong> — ควบคุมลำดับการทำงานของ Pipeline</li><li>🔍 <strong>Quality</strong> — ตรวจสอบว่าข้อมูลถูกต้อง ครบถ้วน ทันเวลา</li><li>📊 <strong>Serving</strong> — จัดข้อมูลให้ DS, Analyst, Product ใช้งานได้</li></ul>' +
      '<h3>💰 เงินเดือน Data Engineer ในไทย</h3>' +
      '<pre><code class="language-text">ระดับ         ประสบการณ์    เงินเดือน (บาท/เดือน)\n============================================\nJunior DE     0-2 ปี        40,000 - 70,000\nMid-Level DE  2-4 ปี        70,000 - 120,000\nSenior DE     4-7 ปี        120,000 - 180,000\nStaff/Lead DE 7+ ปี         180,000 - 300,000+\n\nNote: บริษัท Tech ต่างชาติมักจ่ายสูงกว่า 20-40%\nNote: Freelance DE ต่างประเทศ = $50-150/ชั่วโมง</code></pre>' +
      '<h3>🆚 DE vs DS vs DA — ต่างกันยังไง?</h3>' +
      '<p>นี่คือคำถามที่คนถามบ่อยมากที่สุด ลองดูตารางเปรียบเทียบ:</p>' +
      '<table border="1" cellpadding="8" cellspacing="0" style="border-collapse:collapse;width:100%"><tr style="background:#1a1a2e;color:#fff"><th>หัวข้อ</th><th>Data Analyst (DA)</th><th>Data Scientist (DS)</th><th>Data Engineer (DE)</th></tr><tr><td><strong>โฟกัส</strong></td><td>วิเคราะห์ข้อมูลที่มีอยู่</td><td>สร้าง Model/AI</td><td>สร้างระบบข้อมูล</td></tr><tr><td><strong>ทักษะหลัก</strong></td><td>SQL, Excel, BI Tools</td><td>Python, ML, Statistics</td><td>Python, SQL, Cloud, Pipeline</td></tr><tr><td><strong>ผลลัพธ์</strong></td><td>Dashboard, Report</td><td>ML Model, Prediction</td><td>Data Pipeline, Warehouse</td></tr><tr><td><strong>คำถามที่ตอบ</strong></td><td>เกิดอะไรขึ้น?</td><td>จะเกิดอะไรขึ้น?</td><td>ข้อมูลไหลได้ยังไง?</td></tr><tr><td><strong>เงินเดือน (Mid)</strong></td><td>50-90K</td><td>80-150K</td><td>70-120K</td></tr></table>' +
      '<h3>🏢 DE ทำงานที่ไหนบ้าง?</h3>' +
      '<p>DE หางานได้ในหลายประเภทองค์กร:</p>' +
      '<ul><li>🏦 <strong>Fintech/Bank</strong> — Kasikorn X, SCB Tech, K Bank (เงินเดือนสูง, ข้อมูลเยอะ)</li><li>🛒 <strong>E-Commerce</strong> — Lazada, Shopee, Central Group</li><li>📱 <strong>Tech Startup</strong> — Grab, Agoda, LINE, True Digital</li><li>☁️ <strong>Cloud Consulting</strong> — Accenture, Deloitte, KPMG Tech</li><li>🏭 <strong>Enterprise</strong> — PTT, SCG, Betagro (Data Transformation)</li></ul>' +
      '<h3>📋 Job Description ทั่วไปของ Junior DE</h3>' +
      '<pre><code class="language-text">ตัวอย่าง JD: Junior Data Engineer\n===================================\nRequirements:\n  ✓ Python (pandas, requests, SQLAlchemy)\n  ✓ SQL (JOIN, GROUP BY, Window Functions)\n  ✓ Basic knowledge of ETL processes\n  ✓ Experience with at least one Cloud (GCP/AWS/Azure)\n  ✓ Git version control\n\nNice to have:\n  ◦ Apache Airflow\n  ◦ dbt\n  ◦ Docker\n  ◦ Spark\n\nงานที่ทำ:\n  → สร้างและดูแล Data Pipeline\n  → Load ข้อมูลเข้า Data Warehouse\n  → เขียน SQL Transform ข้อมูล\n  → Monitor Pipeline ให้ทำงานได้\n  → ทำงานร่วมกับ DA/DS</code></pre>' +
      '<div class="tip-box"><strong>💡 Insight จากคนในวงการ:</strong> DE ที่ดีไม่ใช่แค่รู้เครื่องมือ แต่ต้องเข้าใจ <em>ธุรกิจ</em> ด้วย — รู้ว่าทำไมข้อมูลชุดนี้ถึงสำคัญ ใครจะเอาไปใช้ และถ้า Pipeline พัง ผลกระทบคืออะไร DE ที่คิดแบบนี้จะเลื่อนขั้นได้เร็วกว่ามาก</div>' +
      '<h3>🧭 Career Path ของ Data Engineer</h3>' +
      '<pre><code class="language-text">Junior DE → Mid DE → Senior DE → Staff/Principal DE → DE Manager\n                                                    ↘ Data Architect\n                                                    ↘ Platform Engineer\n                                                    ↘ Analytics Engineer</code></pre>' +
      '<p>บางคนเลือกไปทาง <strong>Technical Track</strong> (Staff/Principal) บางคนไปทาง <strong>Management Track</strong> หรือ Specialize ใน Analytics Engineering / Platform Engineering ทั้งหมดนี้มีอนาคตที่ดีมาก</p>'
  },
  {
    number: 2,
    slug: 'computer-basics',
    emoji: '🖥️',
    title: 'Linux, Terminal, SSH, Environment Setup',
    content: '<h2>🖥️ Linux & Terminal — พื้นฐานที่ DE ต้องรู้</h2>' +
      '<p>ไม่ว่าคุณจะทำงานกับ Cloud, Docker, หรือ Server จริงๆ — คุณจะต้องใช้ <strong>Terminal และ Linux</strong> ทุกวัน นี่คือทักษะพื้นฐานที่ขาดไม่ได้สำหรับ DE</p>' +
      '<h3>🐧 ทำไม DE ต้องรู้ Linux?</h3>' +
      '<ul><li>Server ส่วนใหญ่ในโลกรัน Linux (95%+)</li><li>Docker Containers รัน Linux Kernel</li><li>Cloud Services (GCP, AWS) ใช้ Linux</li><li>Airflow, Spark ทำงานบน Linux</li><li>Script automation ง่ายกว่าใน Linux/Mac</li></ul>' +
      '<h3>⌨️ คำสั่ง Terminal ที่ DE ใช้ทุกวัน</h3>' +
      '<pre><code class="language-bash"># Navigation\npwd                    # บอกว่าอยู่ที่ไหน\nls -la                 # แสดง files ทั้งหมด (รวม hidden)\ncd /path/to/folder     # เปลี่ยน directory\ncd ..                  # กลับ directory บน\ncd ~                   # กลับ home directory\n\n# File Operations\ncp file.txt backup.txt # copy ไฟล์\nmv old.txt new.txt     # rename/move ไฟล์\nrm file.txt            # ลบไฟล์\nrm -rf folder/         # ลบ folder (ระวัง!)\nmkdir my_project       # สร้าง folder\ntouch .env             # สร้างไฟล์เปล่า\n\n# View Files\ncat config.yml         # แสดงไฟล์ทั้งหมด\nhead -n 20 data.csv    # แสดง 20 บรรทัดแรก\ntail -f app.log        # ดู log แบบ real-time\nless big_file.txt      # เลื่อนอ่านไฟล์ใหญ่\n\n# Search\ngrep "ERROR" app.log   # หาคำใน file\nfind . -name "*.py"    # หาไฟล์ทั้งหมด\ngrep -r "import" src/  # หาใน folder ทั้งหมด\n\n# Process\nps aux | grep python   # ดู process ที่รันอยู่\nkill -9 12345          # ยุติ process\ntop                    # ดู resource usage</code></pre>' +
      '<h3>🔗 SSH — เชื่อมต่อ Server ระยะไกล</h3>' +
      '<p>DE มักต้อง SSH เข้า Server เพื่อ Deploy Pipeline หรือ Debug</p>' +
      '<pre><code class="language-bash"># เชื่อมต่อ Server\nssh username@server-ip\nssh -i ~/.ssh/my_key.pem ubuntu@ec2-1-2-3-4.compute.amazonaws.com\n\n# สร้าง SSH Key (ทำครั้งเดียว)\nssh-keygen -t rsa -b 4096 -C "your@email.com"\n# จะได้ไฟล์ 2 ไฟล์:\n# ~/.ssh/id_rsa       (private key — ห้ามแชร์!)\n# ~/.ssh/id_rsa.pub   (public key — ส่งให้ server)\n\n# Copy ไฟล์ระหว่าง Local และ Server\nscp file.txt user@server:/home/user/\nscp -r ./project/ user@server:/var/www/\n\n# สร้าง SSH Config ให้สะดวก\n# ~/.ssh/config\nHost myserver\n    HostName 1.2.3.4\n    User ubuntu\n    IdentityFile ~/.ssh/my_key.pem\n\n# แล้วเชื่อมแค่:\nssh myserver</code></pre>' +
      '<h3>🛠️ Environment Setup สำหรับ DE</h3>' +
      '<p>ตั้งค่า Development Environment ที่สมบูรณ์แบบ:</p>' +
      '<pre><code class="language-bash"># 1. ติดตั้ง Python (แนะนำ pyenv)\ncurl https://pyenv.run | bash\npyenv install 3.11.7\npyenv global 3.11.7\npython --version  # Python 3.11.7\n\n# 2. Virtual Environment — สำคัญมาก!\npython -m venv .venv\nsource .venv/bin/activate      # Linux/Mac\n.venv\\Scripts\\activate         # Windows\n\n# ตรวจสอบว่า activate แล้ว\nwhich python  # ควรชี้ไปที่ .venv/bin/python\n\n# 3. Project Structure ที่ดี\nmy-de-project/\n├── .venv/           # Virtual Environment\n├── .env             # Environment Variables (ห้าม commit!)\n├── .gitignore       # บอก git ให้ ignore อะไรบ้าง\n├── requirements.txt # รายชื่อ packages\n├── src/             # Source code\n│   ├── __init__.py\n│   ├── pipeline/\n│   └── utils/\n├── tests/           # Unit Tests\n├── dags/            # Airflow DAGs\n└── README.md        # อธิบาย project</code></pre>' +
      '<h3>📝 .env File — จัดการ Secrets อย่างปลอดภัย</h3>' +
      '<pre><code class="language-bash"># .env file\nDB_HOST=localhost\nDB_PORT=5432\nDB_USER=myuser\nDB_PASSWORD=supersecretpassword\nGCP_PROJECT_ID=my-gcp-project\nAPI_KEY=abc123xyz</code></pre>' +
      '<pre><code class="language-python"># ใช้ python-dotenv อ่าน .env\nfrom dotenv import load_dotenv\nimport os\n\nload_dotenv()  # อ่าน .env\n\ndb_host = os.getenv("DB_HOST")\ndb_password = os.getenv("DB_PASSWORD")\nprint(f"Connecting to {db_host}...")</code></pre>' +
      '<div class="tip-box"><strong>🔒 Security Rule #1:</strong> ห้าม Commit ไฟล์ .env เด็ดขาด! ใส่ชื่อในไฟล์ .gitignore ทุกครั้ง ถ้าผิดพลาด commit ไปแล้ว ให้รีบเปลี่ยน credentials ทันที — ใน GitHub มีบอทที่คอย scan หา API keys ตลอดเวลา</div>' +
      '<h3>🔧 Vim/Nano — Edit ไฟล์บน Terminal</h3>' +
      '<pre><code class="language-bash"># Nano — ง่ายกว่า (แนะนำสำหรับ Beginner)\nnano config.yml\n# Ctrl+O = Save, Ctrl+X = Exit\n\n# Vim — ทรงพลังกว่า (ต้องฝึก)\nvim config.yml\n# i = เข้า insert mode (พิมพ์ได้)\n# Esc = ออก insert mode\n# :w = save\n# :q = quit\n# :wq = save + quit\n# :q! = quit โดยไม่ save</code></pre>'
  },
  {
    number: 3,
    slug: 'python-basics',
    emoji: '🐍',
    title: 'Python พื้นฐาน สำหรับ DE (ไม่ใช่ทั่วไป)',
    content: '<h2>🐍 Python สำหรับ Data Engineer</h2>' +
      '<p>Python ที่ DE ใช้ไม่เหมือน Python ของ DS หรือ Web Developer — เราเน้น <strong>การจัดการข้อมูล, การเชื่อมต่อ Database, การเขียน Pipeline ที่ reliable</strong> มากกว่าการทำ Machine Learning หรือสร้าง Web App</p>' +
      '<h3>🔑 Python Concepts ที่ DE ต้องแม่น</h3>' +
      '<pre><code class="language-python"># 1. Functions ที่ reusable\ndef extract_data(source_url: str, timeout: int = 30) -> dict:\n    """ดึงข้อมูลจาก API\n    \n    Args:\n        source_url: URL ของ API\n        timeout: Timeout ในหน่วย seconds\n    Returns:\n        dict: ข้อมูลที่ดึงมาได้\n    """\n    import requests\n    response = requests.get(source_url, timeout=timeout)\n    response.raise_for_status()  # Raise error ถ้า status != 200\n    return response.json()\n\n# 2. Error Handling — สำคัญมากสำหรับ Pipeline\ndef safe_extract(url: str) -> dict | None:\n    try:\n        data = extract_data(url)\n        return data\n    except requests.exceptions.Timeout:\n        print(f"Timeout: {url}")\n        return None\n    except requests.exceptions.HTTPError as e:\n        print(f"HTTP Error {e.response.status_code}: {url}")\n        return None\n    except Exception as e:\n        print(f"Unexpected error: {e}")\n        raise  # Re-raise ถ้าไม่รู้จะจัดการยังไง</code></pre>' +
      '<h3>📊 Pandas — ห้องทดลองข้อมูลของ DE</h3>' +
      '<pre><code class="language-python">import pandas as pd\n\n# อ่านข้อมูลจากหลาย format\ndf_csv = pd.read_csv("sales.csv")\ndf_json = pd.read_json("users.json")\ndf_excel = pd.read_excel("report.xlsx", sheet_name="Sheet1")\n\n# Basic Exploration — ทำก่อนเสมอ\nprint(df_csv.shape)          # (rows, columns)\nprint(df_csv.dtypes)         # ชนิดข้อมูลของแต่ละ column\nprint(df_csv.isnull().sum()) # นับ missing values\nprint(df_csv.describe())     # statistics พื้นฐาน\nprint(df_csv.head(5))        # 5 แถวแรก\n\n# Cleaning — งานหลักของ DE\ndf = df_csv.copy()\ndf = df.dropna(subset=["user_id", "amount"])  # ลบ rows ที่ null ใน col สำคัญ\ndf["amount"] = pd.to_numeric(df["amount"], errors="coerce")  # แปลง type\ndf["created_at"] = pd.to_datetime(df["created_at"])  # แปลงวันที่\ndf = df.drop_duplicates(subset=["transaction_id"])  # ลบ duplicates\ndf.columns = [c.lower().strip().replace(" ", "_") for c in df.columns]  # normalize column names\n\n# Transformation\ndf["year_month"] = df["created_at"].dt.to_period("M")\ndf["amount_thb"] = df["amount_usd"] * 35.5\ndf["is_large"] = df["amount"] > 10000\n\n# Aggregation\ndaily_sales = df.groupby("date").agg(\n    total_amount=("amount", "sum"),\n    transaction_count=("transaction_id", "count"),\n    avg_amount=("amount", "mean")\n).reset_index()\n\n# Save\ndf.to_csv("cleaned_sales.csv", index=False)\ndf.to_parquet("cleaned_sales.parquet")  # แนะนำ format นี้สำหรับ DE</code></pre>' +
      '<div class="tip-box"><strong>💡 Parquet vs CSV:</strong> Parquet คือ columnar format ที่ compress ข้อมูลได้ดีกว่า CSV 5-10 เท่า และ query ได้เร็วกว่าในระบบ Big Data เช่น Spark, BigQuery — ใน Production ให้ใช้ Parquet เสมอ</div>' +
      '<h3>🔌 เชื่อมต่อ Database ด้วย Python</h3>' +
      '<pre><code class="language-python">import sqlalchemy as sa\nimport pandas as pd\nfrom dotenv import load_dotenv\nimport os\n\nload_dotenv()\n\n# สร้าง Database Connection\nengine = sa.create_engine(\n    f"postgresql://{os.getenv(\'DB_USER\')}:{os.getenv(\'DB_PASSWORD\')}"\n    f"@{os.getenv(\'DB_HOST\')}:{os.getenv(\'DB_PORT\')}/{os.getenv(\'DB_NAME\')}"\n)\n\n# อ่านข้อมูลจาก Database\ndf = pd.read_sql("SELECT * FROM orders WHERE date >= \'2024-01-01\'", engine)\n\n# เขียนข้อมูลเข้า Database\ndf_clean.to_sql(\n    name="orders_clean",\n    con=engine,\n    if_exists="replace",  # หรือ "append"\n    index=False,\n    chunksize=1000  # Insert ทีละ 1000 rows\n)\n\nprint(f"Loaded {len(df_clean)} rows to database")</code></pre>' +
      '<h3>📡 เรียก API และจัดการ JSON</h3>' +
      '<pre><code class="language-python">import requests\nimport json\nfrom datetime import datetime\n\ndef fetch_exchange_rates(base_currency: str = "USD") -> dict:\n    """ดึงอัตราแลกเปลี่ยนจาก API"""\n    url = f"https://api.exchangerate-api.com/v4/latest/{base_currency}"\n    \n    response = requests.get(url, timeout=10)\n    response.raise_for_status()\n    data = response.json()\n    \n    # Flatten JSON ให้เหมาะสำหรับใส่ Database\n    records = []\n    for currency, rate in data["rates"].items():\n        records.append({\n            "base_currency": base_currency,\n            "target_currency": currency,\n            "rate": rate,\n            "fetched_at": datetime.now().isoformat()\n        })\n    \n    return records\n\nrates = fetch_exchange_rates("USD")\ndf_rates = pd.DataFrame(rates)\nprint(df_rates.head())</code></pre>' +
      '<h3>⏰ Logging — ทำให้ Pipeline Debug ได้</h3>' +
      '<pre><code class="language-python">import logging\n\n# Setup Logging — ทำ 1 ครั้งในแต่ละ script\nlogging.basicConfig(\n    level=logging.INFO,\n    format="%(asctime)s - %(name)s - %(levelname)s - %(message)s",\n    handlers=[\n        logging.FileHandler("pipeline.log"),\n        logging.StreamHandler()  # แสดงบน Terminal ด้วย\n    ]\n)\n\nlogger = logging.getLogger(__name__)\n\ndef run_pipeline():\n    logger.info("Pipeline started")\n    try:\n        data = extract_data("https://api.example.com/data")\n        logger.info(f"Extracted {len(data)} records")\n        # ... transform ...\n        logger.info("Pipeline completed successfully")\n    except Exception as e:\n        logger.error(f"Pipeline failed: {e}", exc_info=True)\n        raise</code></pre>'
  },
  {
    number: 4,
    slug: 'sql-basics',
    emoji: '🗃️',
    title: 'SQL ที่ DE ต้องรู้ทุกวัน',
    content: '<h2>🗃️ SQL สำหรับ Data Engineer</h2>' +
      '<p>SQL คือภาษาที่ DE ใช้มากที่สุด — มากกว่า Python ด้วยซ้ำในงานบางส่วน เพราะ Data Warehouse ทุกตัวใช้ SQL เป็นภาษาหลัก ต้องแม่นทั้ง Query, Design, และ Optimization</p>' +
      '<h3>📐 SQL พื้นฐานที่ต้องรู้จนหลับตาทำได้</h3>' +
      '<pre><code class="language-sql">-- SELECT พื้นฐาน\nSELECT\n    order_id,\n    customer_id,\n    amount,\n    created_at,\n    UPPER(status) AS status_upper,\n    DATE(created_at) AS order_date\nFROM orders\nWHERE\n    amount > 100\n    AND status IN (\'completed\', \'shipped\')\n    AND created_at >= \'2024-01-01\'\nORDER BY amount DESC\nLIMIT 100;\n\n-- JOIN ทุกประเภท\nSELECT\n    o.order_id,\n    c.customer_name,\n    c.email,\n    p.product_name,\n    o.quantity,\n    o.amount\nFROM orders o\nINNER JOIN customers c ON o.customer_id = c.customer_id\nLEFT JOIN products p ON o.product_id = p.product_id  -- ดึงมาแม้ไม่มี product\nWHERE o.created_at >= \'2024-01-01\';\n\n-- GROUP BY + Aggregation\nSELECT\n    DATE_TRUNC(\'month\', created_at) AS month,\n    status,\n    COUNT(*) AS order_count,\n    SUM(amount) AS total_revenue,\n    AVG(amount) AS avg_order_value,\n    MIN(amount) AS min_order,\n    MAX(amount) AS max_order\nFROM orders\nGROUP BY 1, 2\nHAVING SUM(amount) > 10000  -- Filter หลัง GROUP BY\nORDER BY 1 DESC;</code></pre>' +
      '<h3>🔮 Window Functions — ทักษะที่แยก Junior ออกจาก Mid</h3>' +
      '<p>Window Functions คือ SQL ที่ทรงพลังมาก — ทำ Calculation โดยไม่ต้อง GROUP BY</p>' +
      '<pre><code class="language-sql">-- ROW_NUMBER: ให้เลขลำดับในแต่ละ partition\nSELECT\n    customer_id,\n    order_id,\n    amount,\n    created_at,\n    ROW_NUMBER() OVER (\n        PARTITION BY customer_id\n        ORDER BY created_at\n    ) AS order_sequence  -- ออเดอร์ที่เท่าไหร่ของ customer คนนี้\nFROM orders;\n\n-- RANK vs DENSE_RANK\nSELECT\n    product_name,\n    total_sales,\n    RANK() OVER (ORDER BY total_sales DESC) AS rank,         -- ข้าม rank ถ้า tie\n    DENSE_RANK() OVER (ORDER BY total_sales DESC) AS dense_rank  -- ไม่ข้าม\nFROM product_sales;\n\n-- LAG/LEAD: เปรียบเทียบกับ row ก่อน/หลัง\nSELECT\n    month,\n    revenue,\n    LAG(revenue, 1) OVER (ORDER BY month) AS prev_month_revenue,\n    revenue - LAG(revenue, 1) OVER (ORDER BY month) AS revenue_change,\n    ROUND(\n        100.0 * (revenue - LAG(revenue, 1) OVER (ORDER BY month))\n        / LAG(revenue, 1) OVER (ORDER BY month),\n        2\n    ) AS pct_change\nFROM monthly_revenue\nORDER BY month;\n\n-- Running Total (Cumulative Sum)\nSELECT\n    date,\n    daily_revenue,\n    SUM(daily_revenue) OVER (\n        ORDER BY date\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS running_total\nFROM daily_revenue;</code></pre>' +
      '<h3>📝 CTE (Common Table Expression) — เขียน SQL ที่อ่านได้</h3>' +
      '<pre><code class="language-sql">-- CTE ช่วยแบ่ง Query ซับซ้อนเป็น step ย่อยๆ\nWITH\n-- Step 1: หา customer แต่ละคนมียอดซื้อเท่าไหร่\ncustomer_totals AS (\n    SELECT\n        customer_id,\n        COUNT(*) AS order_count,\n        SUM(amount) AS lifetime_value\n    FROM orders\n    WHERE status = \'completed\'\n    GROUP BY customer_id\n),\n\n-- Step 2: แบ่ง Segment\ncustomer_segments AS (\n    SELECT\n        customer_id,\n        lifetime_value,\n        CASE\n            WHEN lifetime_value >= 100000 THEN \'Platinum\'\n            WHEN lifetime_value >= 50000  THEN \'Gold\'\n            WHEN lifetime_value >= 10000  THEN \'Silver\'\n            ELSE \'Bronze\'\n        END AS segment\n    FROM customer_totals\n),\n\n-- Step 3: Join กับข้อมูล customer\nfinal AS (\n    SELECT\n        c.customer_name,\n        c.email,\n        cs.segment,\n        cs.lifetime_value\n    FROM customers c\n    JOIN customer_segments cs USING (customer_id)\n)\n\nSELECT * FROM final\nORDER BY lifetime_value DESC;</code></pre>' +
      '<div class="tip-box"><strong>💡 Pro Tip:</strong> ใน dbt คุณจะเขียน SQL แบบ CTE ตลอดเวลา เพราะมันทำให้ Code อ่านง่าย Test ได้ และ Reuse ได้ ฝึกเขียน CTE ตอนนี้จะได้เปรียบมากตอนใช้ dbt</div>' +
      '<h3>🏗️ DDL — สร้างและจัดการ Table</h3>' +
      '<pre><code class="language-sql">-- สร้าง Table\nCREATE TABLE IF NOT EXISTS orders (\n    order_id     BIGINT PRIMARY KEY,\n    customer_id  BIGINT NOT NULL,\n    product_id   BIGINT,\n    amount       DECIMAL(10, 2) NOT NULL,\n    status       VARCHAR(50) DEFAULT \'pending\',\n    created_at   TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n    updated_at   TIMESTAMP\n);\n\n-- Index เพื่อเพิ่มความเร็วในการ Query\nCREATE INDEX idx_orders_customer ON orders(customer_id);\nCREATE INDEX idx_orders_created ON orders(created_at);\n\n-- เพิ่ม Column ใน Table ที่มีอยู่\nALTER TABLE orders ADD COLUMN discount_amount DECIMAL(10, 2) DEFAULT 0;\n\n-- Insert ข้อมูล\nINSERT INTO orders (order_id, customer_id, amount, status)\nVALUES (1001, 555, 1500.00, \'completed\');\n\n-- Upsert (Insert or Update)\nINSERT INTO orders (order_id, customer_id, amount, status)\nVALUES (1001, 555, 1600.00, \'shipped\')\nON CONFLICT (order_id)\nDO UPDATE SET\n    amount = EXCLUDED.amount,\n    status = EXCLUDED.status,\n    updated_at = CURRENT_TIMESTAMP;</code></pre>'
  },
  {
    number: 5,
    slug: 'data-concepts',
    emoji: '🏛️',
    title: 'Data Warehouse, Data Lake, Lakehouse คืออะไร',
    content: '<h2>🏛️ Architecture ของ Data — ต้องเข้าใจก่อนสร้าง Pipeline</h2>' +
      '<p>ก่อนจะสร้าง Pipeline DE ต้องเข้าใจ "ปลายทาง" ของข้อมูลก่อน — ข้อมูลจะถูก Store อยู่ที่ไหน และในรูปแบบอะไร นี่คือพื้นฐาน Architecture ที่ทุก DE ต้องรู้</p>' +
      '<h3>🏭 Data Warehouse คืออะไร?</h3>' +
      '<p><strong>Data Warehouse (DWH)</strong> คือระบบจัดเก็บข้อมูลที่ถูก Structure ไว้แล้ว เหมาะสำหรับ Analytics Query โดยเฉพาะ</p>' +
      '<ul><li>✅ <strong>Structured Data</strong> — ข้อมูลเป็น table ชัดเจน</li><li>✅ <strong>Fast Analytics</strong> — Query ได้เร็วมาก</li><li>✅ <strong>ACID Compliant</strong> — ข้อมูลถูกต้อง consistent</li><li>❌ <strong>ราคาแพง</strong> — Compute + Storage แยกไม่ได้ในบางระบบ</li><li>❌ <strong>Semi/Unstructured</strong> — ไม่เหมาะกับ JSON, Image, Video</li></ul>' +
      '<p>ตัวอย่าง: <strong>BigQuery, Snowflake, Redshift, Synapse</strong></p>' +
      '<h3>🌊 Data Lake คืออะไร?</h3>' +
      '<p><strong>Data Lake</strong> คือระบบจัดเก็บข้อมูลทุกรูปแบบ ทั้ง Structured, Semi-structured, Unstructured — เก็บแบบ raw ก่อน แล้วค่อย process ทีหลัง</p>' +
      '<ul><li>✅ <strong>Any Format</strong> — CSV, JSON, Parquet, Images, Logs</li><li>✅ <strong>Cheap Storage</strong> — ถูกมาก (GCS, S3, ADLS)</li><li>✅ <strong>Scalable</strong> — เก็บได้ไม่จำกัด</li><li>❌ <strong>No Schema</strong> — อาจกลายเป็น "Data Swamp" ถ้าไม่จัดการดี</li><li>❌ <strong>Query ช้ากว่า</strong> DWH สำหรับ Analytics</li></ul>' +
      '<p>ตัวอย่าง: <strong>Google Cloud Storage, AWS S3, Azure Data Lake</strong></p>' +
      '<h3>🦢 Data Lakehouse — Best of Both Worlds</h3>' +
      '<p><strong>Data Lakehouse</strong> คือ Architecture ใหม่ที่รวมข้อดีของ DWH และ Data Lake เข้าด้วยกัน — จัดเก็บแบบ Lake แต่ query ได้เร็วแบบ Warehouse</p>' +
      '<pre><code class="language-text">Data Lakehouse Architecture\n============================\n\n┌─────────────────────────────────────┐\n│         STORAGE LAYER               │\n│  (Cloud Object Storage: GCS / S3)   │\n│  ข้อมูลจัดเก็บแบบ Open Format:      │\n│  Delta Lake / Apache Iceberg / Hudi  │\n└───────────────┬─────────────────────┘\n                │\n┌───────────────▼─────────────────────┐\n│         METADATA LAYER              │\n│  (Schema, Versioning, ACID)         │\n│  เพิ่มความสามารถแบบ DWH บน Lake    │\n└───────────────┬─────────────────────┘\n                │\n┌───────────────▼─────────────────────┐\n│         QUERY ENGINE                │\n│  Spark / Trino / BigQuery / Athena  │\n└─────────────────────────────────────┘</code></pre>' +
      '<p>ตัวอย่าง: <strong>Databricks Delta Lake, Apache Iceberg on BigQuery, AWS Lake Formation</strong></p>' +
      '<h3>📊 เปรียบเทียบ 3 Architecture</h3>' +
      '<table border="1" cellpadding="8" cellspacing="0" style="border-collapse:collapse;width:100%"><tr style="background:#1a1a2e;color:#fff"><th>หัวข้อ</th><th>Data Warehouse</th><th>Data Lake</th><th>Data Lakehouse</th></tr><tr><td>ประเภทข้อมูล</td><td>Structured เท่านั้น</td><td>ทุกรูปแบบ</td><td>ทุกรูปแบบ</td></tr><tr><td>ราคา Storage</td><td>แพง</td><td>ถูกมาก</td><td>ถูก</td></tr><tr><td>Query Speed</td><td>เร็ว</td><td>ช้า</td><td>เร็ว</td></tr><tr><td>ACID</td><td>✅</td><td>❌</td><td>✅</td></tr><tr><td>ML/AI Ready</td><td>❌</td><td>✅</td><td>✅</td></tr><tr><td>ตัวอย่าง</td><td>Snowflake, BigQuery</td><td>S3, GCS</td><td>Delta Lake, Iceberg</td></tr></table>' +
      '<h3>🏗️ Kimball Dimensional Modeling — วิธีออกแบบ DWH</h3>' +
      '<p>Ralph Kimball เป็นผู้คิดค้น concept ที่ DE ทุกคนต้องรู้ — <strong>Star Schema</strong></p>' +
      '<pre><code class="language-sql">-- Fact Table: เก็บ Event/Transaction\nCREATE TABLE fact_orders (\n    order_sk        BIGINT PRIMARY KEY,  -- Surrogate Key\n    customer_sk     BIGINT,  -- Foreign Key to dim_customer\n    product_sk      BIGINT,  -- Foreign Key to dim_product\n    date_sk         INT,     -- Foreign Key to dim_date\n    quantity        INT,\n    amount          DECIMAL(10,2),\n    discount        DECIMAL(10,2)\n);\n\n-- Dimension Table: เก็บ Context/Attributes\nCREATE TABLE dim_customer (\n    customer_sk     BIGINT PRIMARY KEY,\n    customer_id     VARCHAR(50),  -- Natural Key\n    customer_name   VARCHAR(200),\n    email           VARCHAR(200),\n    city            VARCHAR(100),\n    country         VARCHAR(100),\n    segment         VARCHAR(50),\n    -- SCD Type 2 fields\n    valid_from      DATE,\n    valid_to        DATE,\n    is_current      BOOLEAN\n);\n\n-- Query บน Star Schema ง่าย + เร็วมาก\nSELECT\n    dc.segment,\n    dp.category,\n    dd.year_month,\n    SUM(fo.amount) AS revenue\nFROM fact_orders fo\nJOIN dim_customer dc ON fo.customer_sk = dc.customer_sk\nJOIN dim_product  dp ON fo.product_sk  = dp.product_sk\nJOIN dim_date     dd ON fo.date_sk     = dd.date_sk\nWHERE dc.is_current = TRUE\nGROUP BY 1, 2, 3;</code></pre>' +
      '<div class="tip-box"><strong>💡 เริ่มต้นแบบไหนดี?</strong> สำหรับ Startup เล็ก → เริ่มด้วย BigQuery + dbt + GCS ก็พอ บริษัทใหญ่ที่มีข้อมูลหลาย TB → พิจารณา Lakehouse Architecture ด้วย Databricks หรือ BigQuery + Parquet บน GCS</div>'
  },
  {
    number: 6,
    slug: 'etl-pipeline',
    emoji: '🔄',
    title: 'ETL vs ELT, สร้าง Pipeline แรก',
    content: '<h2>🔄 ETL vs ELT — หัวใจของ Data Engineering</h2>' +
      '<p><strong>Pipeline</strong> คือการนำข้อมูลจาก Source → ผ่านกระบวนการ → ไปถึง Destination อัตโนมัติ นี่คืองานหลักของ DE ทุกคน</p>' +
      '<h3>📦 ETL: Extract → Transform → Load</h3>' +
      '<p>แบบดั้งเดิม — แปลงข้อมูลก่อนแล้วค่อย Load เข้า Warehouse</p>' +
      '<pre><code class="language-text">ETL Flow:\nSource DB → [Extract] → Staging Area → [Transform] → [Load] → Data Warehouse\n\nข้อดี:\n  + Transform ก่อน Load → ประหยัด Storage ใน Warehouse\n  + ข้อมูลใน Warehouse สะอาดแล้ว\nข้อเสีย:\n  - Transform logic อยู่ใน ETL tool (ดูแลยาก)\n  - ถ้า Logic เปลี่ยน ต้อง Re-run ข้อมูลเก่า\n  - Compute แยกจาก Warehouse</code></pre>' +
      '<h3>🚀 ELT: Extract → Load → Transform</h3>' +
      '<p>แบบ Modern — Load raw data เข้า Warehouse ก่อน แล้วค่อย Transform ด้วย SQL</p>' +
      '<pre><code class="language-text">ELT Flow:\nSource DB → [Extract] → [Load] → Data Warehouse → [Transform with SQL/dbt]\n\nข้อดี:\n  + Transform ด้วย SQL ใน Warehouse — ง่ายกว่ามาก\n  + ข้อมูล raw ยังอยู่ → ย้อนกลับได้เสมอ\n  + Warehouse ทันสมัย (BigQuery, Snowflake) compute ได้เร็ว\nข้อเสีย:\n  - ต้องการ Warehouse ที่ scalable\n  - Storage ใน Warehouse แพงกว่า (เก็บ raw)\n\n→ Modern DE ใช้ ELT เป็นส่วนใหญ่ในปัจจุบัน!</code></pre>' +
      '<h3>🛠️ สร้าง ETL Pipeline แรก (Python)</h3>' +
      '<p>ตัวอย่างการสร้าง Pipeline ดึงข้อมูล Exchange Rate → แปลง → โหลดเข้า Database</p>' +
      '<pre><code class="language-python">import requests\nimport pandas as pd\nimport sqlalchemy as sa\nfrom datetime import datetime\nimport logging\n\n# Setup\nlogging.basicConfig(level=logging.INFO)\nlogger = logging.getLogger(__name__)\n\n# ============================================================\n# EXTRACT — ดึงข้อมูลจาก Source\n# ============================================================\ndef extract(date: str) -> list[dict]:\n    """ดึงอัตราแลกเปลี่ยนจาก API"""\n    logger.info(f"Extracting data for {date}")\n    url = f"https://api.exchangerate-api.com/v4/latest/THB"\n    resp = requests.get(url, timeout=30)\n    resp.raise_for_status()\n    data = resp.json()\n    \n    records = []\n    for currency, rate in data["rates"].items():\n        records.append({\n            "base": "THB",\n            "target": currency,\n            "rate": rate,\n            "date": date\n        })\n    logger.info(f"Extracted {len(records)} records")\n    return records\n\n# ============================================================\n# TRANSFORM — แปลงและทำความสะอาดข้อมูล\n# ============================================================\ndef transform(raw_records: list[dict]) -> pd.DataFrame:\n    """แปลงข้อมูล raw เป็น DataFrame ที่พร้อม Load"""\n    df = pd.DataFrame(raw_records)\n    \n    # กรองเฉพาะสกุลเงินหลัก\n    main_currencies = [\'USD\', \'EUR\', \'GBP\', \'JPY\', \'SGD\', \'CNY\']\n    df = df[df[\'target\'].isin(main_currencies)].copy()\n    \n    # แปลง type\n    df[\'rate\'] = pd.to_numeric(df[\'rate\'], errors=\'coerce\')\n    df[\'date\'] = pd.to_datetime(df[\'date\'])\n    df[\'loaded_at\'] = datetime.now()\n    \n    logger.info(f"Transformed to {len(df)} records")\n    return df\n\n# ============================================================\n# LOAD — โหลดเข้า Database\n# ============================================================\ndef load(df: pd.DataFrame, engine: sa.Engine) -> int:\n    """โหลดข้อมูลเข้า Database"""\n    rows = df.to_sql(\n        name=\'exchange_rates\',\n        con=engine,\n        if_exists=\'append\',\n        index=False\n    )\n    logger.info(f"Loaded {rows} rows")\n    return rows\n\n# ============================================================\n# PIPELINE RUNNER\n# ============================================================\ndef run_pipeline(date: str = None):\n    if date is None:\n        date = datetime.today().strftime(\'%Y-%m-%d\')\n    \n    engine = sa.create_engine("postgresql://user:pass@localhost/mydb")\n    \n    try:\n        raw = extract(date)\n        clean = transform(raw)\n        loaded = load(clean, engine)\n        logger.info(f"Pipeline SUCCESS: {loaded} rows for {date}")\n    except Exception as e:\n        logger.error(f"Pipeline FAILED: {e}")\n        raise\n\nif __name__ == "__main__":\n    run_pipeline()</code></pre>' +
      '<div class="tip-box"><strong>✅ Best Practice:</strong> แยก Extract, Transform, Load เป็น functions แยกกันเสมอ นี่คือ <strong>Separation of Concerns</strong> — ทำให้ Test ได้แยกกัน, Debug ง่ายขึ้น และ Reuse Code ได้</div>' +
      '<h3>📋 Pipeline Design Checklist</h3>' +
      '<pre><code class="language-text">Pipeline ที่ดีต้องมี:\n✓ Idempotent — รัน 2 ครั้งได้ผลเหมือนกัน\n✓ Error Handling — จัดการ error อย่างสุภาพ\n✓ Logging — บันทึกทุก step\n✓ Retry Logic — ลองใหม่ถ้า API หลุด\n✓ Data Validation — ตรวจก่อน Load\n✓ Alerting — แจ้งเตือนถ้า fail\n✓ Backfill — ย้อน run historical ได้</code></pre>'
  },
  {
    number: 7,
    slug: 'airflow-intro',
    emoji: '🌊',
    title: 'Apache Airflow คืออะไร, สร้าง DAG แรก',
    content: '<h2>🌊 Apache Airflow — ระบบ Orchestration ของ DE</h2>' +
      '<p><strong>Apache Airflow</strong> คือ Platform สำหรับ Orchestrate Data Pipelines — มันไม่ได้ "รัน" Pipeline แต่คือ "กรรมการ" ที่บอกว่า Pipeline ไหนควรรันตอนไหน ลำดับอะไร และถ้าพังต้องทำอะไร</p>' +
      '<p>ใช้โดย: Airbnb (คนสร้าง), LinkedIn, Twitter, Lyft, NASA</p>' +
      '<h3>🔑 Concept หลักใน Airflow</h3>' +
      '<ul><li><strong>DAG (Directed Acyclic Graph)</strong> — คือ Pipeline หนึ่งชิ้น ประกอบด้วย Tasks ที่มีความสัมพันธ์กัน</li><li><strong>Task</strong> — หน่วยงานย่อยที่เล็กที่สุดใน DAG</li><li><strong>Operator</strong> — Template สำหรับสร้าง Task (PythonOperator, BashOperator, etc.)</li><li><strong>Schedule</strong> — กำหนดว่า DAG จะรันเมื่อไหร่ (cron syntax)</li><li><strong>Task Instance</strong> — การรัน Task หนึ่งครั้งจริงๆ</li><li><strong>DAG Run</strong> — การรัน DAG ทั้งหมดหนึ่งครั้ง</li></ul>' +
      '<h3>🐳 ติดตั้ง Airflow ด้วย Docker (แนะนำ)</h3>' +
      '<pre><code class="language-bash"># สร้าง Project folder\nmkdir airflow-project && cd airflow-project\n\n# ดาวน์โหลด docker-compose.yaml ของ Airflow\ncurl -LfO \'https://airflow.apache.org/docs/apache-airflow/stable/docker-compose.yaml\'\n\n# สร้าง Folders ที่ต้องการ\nmkdir -p ./dags ./logs ./plugins ./config\necho -e "AIRFLOW_UID=$(id -u)" > .env\n\n# Initialize Database\ndocker compose up airflow-init\n\n# Start Airflow\ndocker compose up -d\n\n# เปิด Browser: http://localhost:8080\n# Username: airflow / Password: airflow</code></pre>' +
      '<h3>✍️ สร้าง DAG แรก</h3>' +
      '<pre><code class="language-python"># dags/my_first_dag.py\nfrom airflow import DAG\nfrom airflow.operators.python import PythonOperator\nfrom airflow.operators.bash import BashOperator\nfrom datetime import datetime, timedelta\nimport logging\n\nlogger = logging.getLogger(__name__)\n\n# Default arguments สำหรับทุก Task ใน DAG\ndefault_args = {\n    \'owner\': \'data-team\',\n    \'depends_on_past\': False,\n    \'email_on_failure\': True,\n    \'email\': [\'team@company.com\'],\n    \'retries\': 3,\n    \'retry_delay\': timedelta(minutes=5),\n}\n\n# ฟังก์ชันที่ Task จะรัน\ndef extract_data(**context):\n    run_date = context[\'ds\']  # \'2024-01-15\'\n    logger.info(f"Extracting data for {run_date}")\n    # ... logic ดึงข้อมูล ...\n    return {\'records_extracted\': 1000}\n\ndef transform_data(**context):\n    # รับ output จาก extract task ผ่าน XCom\n    ti = context[\'ti\']\n    result = ti.xcom_pull(task_ids=\'extract\')\n    count = result[\'records_extracted\']\n    logger.info(f"Transforming {count} records")\n    # ... logic แปลงข้อมูล ...\n    return {\'records_transformed\': count}\n\ndef load_data(**context):\n    logger.info("Loading data to warehouse")\n    # ... logic โหลดข้อมูล ...\n\n# สร้าง DAG\nwith DAG(\n    dag_id=\'exchange_rate_pipeline\',\n    description=\'Daily exchange rate ETL pipeline\',\n    default_args=default_args,\n    start_date=datetime(2024, 1, 1),\n    schedule=\'0 8 * * *\',  # ทุกวัน 8:00 AM\n    catchup=False,  # ไม่ backfill อัตโนมัติ\n    tags=[\'etl\', \'finance\'],\n) as dag:\n\n    # Tasks\n    extract = PythonOperator(\n        task_id=\'extract\',\n        python_callable=extract_data,\n    )\n\n    transform = PythonOperator(\n        task_id=\'transform\',\n        python_callable=transform_data,\n    )\n\n    load = PythonOperator(\n        task_id=\'load\',\n        python_callable=load_data,\n    )\n\n    notify = BashOperator(\n        task_id=\'notify\',\n        bash_command=\'echo "Pipeline completed at $(date)"\',\n    )\n\n    # กำหนด Dependencies\n    extract >> transform >> load >> notify</code></pre>' +
      '<h3>📅 Cron Schedule ที่ใช้บ่อย</h3>' +
      '<pre><code class="language-text">\'@daily\'         → ทุกวัน เที่ยงคืน\n\'@hourly\'        → ทุกชั่วโมง\n\'@weekly\'        → ทุกวันอาทิตย์\n\'0 8 * * *\'      → ทุกวัน 8:00 AM\n\'0 */6 * * *\'    → ทุก 6 ชั่วโมง\n\'0 9 * * 1-5\'    → วันจันทร์-ศุกร์ 9:00 AM\n\'0 0 1 * *\'      → วันที่ 1 ของทุกเดือน</code></pre>' +
      '<div class="tip-box"><strong>💡 catchup=False คืออะไร?</strong> ถ้า DAG มี start_date = 1 ม.ค. แต่คุณเพิ่ง Deploy วันนี้ Airflow จะ "backfill" รันย้อนหลังทุกวันตั้งแต่ 1 ม.ค. ถ้าไม่ต้องการ ให้ตั้ง catchup=False เสมอสำหรับ Production</div>' +
      '<h3>🔍 Monitoring DAG</h3>' +
      '<p>Airflow UI ที่ http://localhost:8080 มีเครื่องมือ:</p>' +
      '<ul><li><strong>Tree View</strong> — เห็นทุก Task Run ในอดีต</li><li><strong>Graph View</strong> — เห็น dependency ของ Tasks</li><li><strong>Gantt Chart</strong> — เห็น performance ของแต่ละ Task</li><li><strong>Logs</strong> — ดู output/error ของแต่ละ Task Run</li></ul>'
  },
  {
    number: 8,
    slug: 'docker-basics',
    emoji: '🐳',
    title: 'Docker สำหรับ DE, containerize pipeline',
    content: '<h2>🐳 Docker สำหรับ Data Engineer</h2>' +
      '<p><strong>Docker</strong> คือ Platform สำหรับ Containerize Applications — แพ็ค Code + Dependencies ทั้งหมดไว้ใน Container เดียว ทำให้รันได้ทุกที่เหมือนกันหมด ไม่มี "มันรันได้บนเครื่องฉัน แต่ไม่รันบน Server"</p>' +
      '<h3>🎯 ทำไม DE ต้องรู้ Docker?</h3>' +
      '<ul><li>📦 <strong>Reproduce Environment</strong> — Airflow, Spark, Kafka รันด้วย Docker</li><li>🚢 <strong>Deploy Pipeline</strong> — บน Kubernetes/Cloud Run</li><li>🧪 <strong>Testing</strong> — Test กับ Database จริงใน Container</li><li>📚 <strong>Development</strong> — ทุกคนในทีม Environment เหมือนกัน</li></ul>' +
      '<h3>🔑 Concept หลัก</h3>' +
      '<ul><li><strong>Image</strong> — Blueprint ของ Container (เหมือน Class)</li><li><strong>Container</strong> — Instance ที่รันจริงๆ (เหมือน Object)</li><li><strong>Dockerfile</strong> — ไฟล์ Script สำหรับสร้าง Image</li><li><strong>Docker Hub</strong> — Registry เก็บ Images สาธารณะ</li><li><strong>docker-compose</strong> — รัน หลาย Services พร้อมกัน</li></ul>' +
      '<h3>⚡ คำสั่ง Docker ที่ใช้ทุกวัน</h3>' +
      '<pre><code class="language-bash"># Images\ndocker pull python:3.11-slim         # ดาวน์โหลด image\ndocker images                         # ดู images ทั้งหมด\ndocker rmi python:3.11-slim          # ลบ image\n\n# Containers\ndocker run python:3.11-slim python --version  # รัน command ใน container\ndocker run -it python:3.11-slim bash          # รัน interactive shell\ndocker run -d -p 5432:5432 -e POSTGRES_PASSWORD=secret postgres:15  # Background\ndocker ps                              # ดู containers ที่รัน\ndocker ps -a                           # รวม stopped containers\ndocker stop <container_id>\ndocker rm <container_id>\n\n# Build จาก Dockerfile\ndocker build -t my-pipeline:1.0 .     # Build image ชื่อ my-pipeline\ndocker run my-pipeline:1.0            # รัน image นั้น\n\n# Logs & Debugging\ndocker logs <container_id>\ndocker logs -f <container_id>          # ดู real-time\ndocker exec -it <container_id> bash   # เข้าไปใน container ที่รันอยู่</code></pre>' +
      '<h3>📝 Dockerfile สำหรับ Python Pipeline</h3>' +
      '<pre><code class="language-dockerfile"># Dockerfile\n# เริ่มจาก Python slim image (เล็กกว่า python:3.11)\nFROM python:3.11-slim\n\n# ตั้ง working directory\nWORKDIR /app\n\n# Copy requirements ก่อน (ใช้ Docker cache ได้)\nCOPY requirements.txt .\n\n# ติดตั้ง dependencies\nRUN pip install --no-cache-dir -r requirements.txt\n\n# Copy source code\nCOPY src/ ./src/\n\n# Set environment variables\nENV PYTHONPATH=/app\nENV LOG_LEVEL=INFO\n\n# รัน pipeline\nCMD [\"python\", \"src/pipeline/run.py\"]</code></pre>' +
      '<pre><code class="language-text"># requirements.txt\npandas==2.1.0\nrequests==2.31.0\nsqlalchemy==2.0.23\npsycopg2-binary==2.9.9\npython-dotenv==1.0.0</code></pre>' +
      '<h3>🎼 Docker Compose — รัน Multiple Services</h3>' +
      '<p>ตัวอย่าง docker-compose.yml สำหรับ DE Development:</p>' +
      '<pre><code class="language-yaml">version: \'3.8\'\n\nservices:\n  # PostgreSQL Database\n  postgres:\n    image: postgres:15\n    environment:\n      POSTGRES_DB: datawarehouse\n      POSTGRES_USER: deuser\n      POSTGRES_PASSWORD: depassword\n    ports:\n      - "5432:5432"\n    volumes:\n      - postgres_data:/var/lib/postgresql/data\n    healthcheck:\n      test: ["CMD", "pg_isready", "-U", "deuser"]\n      interval: 10s\n\n  # pgAdmin — Web UI สำหรับ PostgreSQL\n  pgadmin:\n    image: dpage/pgadmin4\n    environment:\n      PGADMIN_DEFAULT_EMAIL: admin@admin.com\n      PGADMIN_DEFAULT_PASSWORD: admin\n    ports:\n      - "5050:80"\n    depends_on:\n      - postgres\n\n  # Python Pipeline\n  pipeline:\n    build: .\n    environment:\n      DB_HOST: postgres\n      DB_PORT: 5432\n      DB_USER: deuser\n      DB_PASSWORD: depassword\n      DB_NAME: datawarehouse\n    depends_on:\n      postgres:\n        condition: service_healthy\n    volumes:\n      - ./logs:/app/logs\n\nvolumes:\n  postgres_data:</code></pre>' +
      '<pre><code class="language-bash"># รัน ทุก Services\ndocker compose up -d\n\n# ดู logs\ndocker compose logs -f pipeline\n\n# Stop\ndocker compose down\n\n# Stop และลบ volumes (ระวัง!)\ndocker compose down -v</code></pre>' +
      '<div class="tip-box"><strong>💡 Best Practice:</strong> ใช้ <code>.dockerignore</code> เหมือน <code>.gitignore</code> เพื่อไม่ให้ COPY ไฟล์ที่ไม่ต้องการเข้า Image เช่น .venv, .git, *.log ทำให้ Image เล็กลงและ Build เร็วขึ้น</div>'
  },
  {
    number: 9,
    slug: 'cloud-intro',
    emoji: '☁️',
    title: 'Cloud Computing, GCP/AWS/Azure เลือกอะไรดี',
    content: '<h2>☁️ Cloud Computing สำหรับ Data Engineer</h2>' +
      '<p>ยุคนี้ Data Engineering ทำงานบน Cloud เป็นส่วนใหญ่ — ไม่มี Physical Server อีกแล้ว DE ต้องเข้าใจ Cloud เพื่อ Deploy Pipeline, จัดเก็บข้อมูล และใช้ Managed Services ต่างๆ</p>' +
      '<h3>🌐 ทำไมต้องใช้ Cloud?</h3>' +
      '<ul><li>💰 <strong>Pay-as-you-go</strong> — จ่ายตามที่ใช้จริง ไม่ต้องซื้อ Server</li><li>📈 <strong>Scalability</strong> — Scale ขึ้น/ลงตามความต้องการ</li><li>🛡️ <strong>Reliability</strong> — SLA 99.9%+ ไม่ต้องดูแล Hardware</li><li>🔧 <strong>Managed Services</strong> — ใช้ BigQuery, Dataflow โดยไม่ต้อง Setup เอง</li><li>🌍 <strong>Global</strong> — Deploy ใกล้ผู้ใช้งานทั่วโลก</li></ul>' +
      '<h3>🏆 3 Cloud Providers หลัก</h3>' +
      '<table border="1" cellpadding="8" cellspacing="0" style="border-collapse:collapse;width:100%"><tr style="background:#1a1a2e;color:#fff"><th>หัวข้อ</th><th>GCP (Google)</th><th>AWS (Amazon)</th><th>Azure (Microsoft)</th></tr><tr><td>Market Share</td><td>~12%</td><td>~33%</td><td>~23%</td></tr><tr><td>Data Warehouse</td><td>BigQuery ⭐</td><td>Redshift</td><td>Synapse</td></tr><tr><td>Object Storage</td><td>Cloud Storage</td><td>S3</td><td>Blob Storage</td></tr><tr><td>Stream Processing</td><td>Pub/Sub + Dataflow</td><td>Kinesis</td><td>Event Hub</td></tr><tr><td>Free Tier</td><td>$300 credit + Free tier ✅</td><td>12 months free tier ✅</td><td>$200 credit ✅</td></tr><tr><td>DE Learning</td><td>ดีที่สุดสำหรับ Beginner</td><td>Market leader</td><td>ถ้าทำงานกับ Enterprise</td></tr></table>' +
      '<h3>🎯 แนะนำ: เริ่มด้วย GCP</h3>' +
      '<p>สำหรับ DE Beginner ในไทย <strong>GCP คือตัวเลือกที่ดีที่สุด</strong> เพราะ:</p>' +
      '<ul><li>BigQuery มี Free Tier 10 GB/เดือน + 1 TB query/เดือน</li><li>Documentation ดีและอ่านง่าย</li><li>Tools เชื่อมต่อกันได้ง่าย (BigQuery + Dataflow + Pub/Sub + dbt)</li><li>Certification ที่ได้รับการยอมรับในตลาด</li></ul>' +
      '<h3>🔧 GCP Services ที่ DE ใช้บ่อย</h3>' +
      '<pre><code class="language-text">Data Storage:\n  Cloud Storage (GCS)     → Object Storage (เก็บ Parquet, CSV, JSON)\n  BigQuery                → Data Warehouse หลัก\n  Cloud SQL               → Managed PostgreSQL/MySQL\n  Firestore               → NoSQL Document DB\n\nData Processing:\n  Dataflow                → Managed Apache Beam (Stream + Batch)\n  Dataproc                → Managed Spark & Hadoop\n  Cloud Functions         → Serverless Python/Node functions\n  Cloud Run               → Containerized apps + pipelines\n\nOrchestration:\n  Cloud Composer          → Managed Apache Airflow\n  Cloud Scheduler         → Cron Jobs บน Cloud\n\nMessaging:\n  Pub/Sub                 → Message Queue (เหมือน Kafka แต่ Managed)\n\nML/AI:\n  Vertex AI               → ML Platform\n  BigQuery ML             → ML ใน SQL</code></pre>' +
      '<h3>⚙️ ตั้งค่า GCP ครั้งแรก</h3>' +
      '<pre><code class="language-bash"># 1. ติดตั้ง Google Cloud CLI\n# ดาวน์โหลดจาก: https://cloud.google.com/sdk/docs/install\n\n# 2. Login\ngcloud auth login\ngcloud auth application-default login  # สำหรับ Python code\n\n# 3. ตั้ง Project\ngcloud config set project YOUR_PROJECT_ID\n\n# 4. Enable APIs ที่ต้องใช้\ngcloud services enable bigquery.googleapis.com\ngcloud services enable storage.googleapis.com\ngcloud services enable run.googleapis.com\n\n# 5. สร้าง Service Account สำหรับ Pipeline\ngcloud iam service-accounts create de-pipeline-sa \\\n    --display-name="DE Pipeline Service Account"\n\ngcloud projects add-iam-policy-binding YOUR_PROJECT_ID \\\n    --member="serviceAccount:de-pipeline-sa@YOUR_PROJECT.iam.gserviceaccount.com" \\\n    --role="roles/bigquery.dataEditor"\n\n# สร้าง Key File\ngcloud iam service-accounts keys create ./sa-key.json \\\n    --iam-account=de-pipeline-sa@YOUR_PROJECT.iam.gserviceaccount.com\n\n# ตั้ง Environment Variable\nexport GOOGLE_APPLICATION_CREDENTIALS="./sa-key.json"</code></pre>' +
      '<h3>📤 Upload ข้อมูลเข้า GCS และ BigQuery</h3>' +
      '<pre><code class="language-python">from google.cloud import storage, bigquery\nimport pandas as pd\n\n# Upload ไฟล์เข้า GCS\ndef upload_to_gcs(local_path: str, bucket: str, blob_name: str):\n    client = storage.Client()\n    bucket_obj = client.bucket(bucket)\n    blob = bucket_obj.blob(blob_name)\n    blob.upload_from_filename(local_path)\n    print(f"Uploaded to gs://{bucket}/{blob_name}")\n\n# Load DataFrame เข้า BigQuery\ndef load_to_bigquery(df: pd.DataFrame, table_id: str):\n    client = bigquery.Client()\n    job_config = bigquery.LoadJobConfig(\n        write_disposition="WRITE_APPEND",  # หรือ WRITE_TRUNCATE\n        autodetect=True,  # อ่าน Schema อัตโนมัติ\n    )\n    job = client.load_table_from_dataframe(df, table_id, job_config=job_config)\n    job.result()  # รอให้เสร็จ\n    print(f"Loaded {job.output_rows} rows to {table_id}")</code></pre>' +
      '<div class="tip-box"><strong>🔒 Security Tip:</strong> อย่าใช้ Service Account Key File ใน Production — ใช้ Workload Identity Federation แทน และให้ IAM Role แบบ least privilege เสมอ (ให้สิทธิ์แค่ที่จำเป็นจริงๆ)</div>'
  },
  {
    number: 10,
    slug: 'bigquery-basics',
    emoji: '🔍',
    title: 'BigQuery: load data, query, partition, cluster',
    content: '<h2>🔍 BigQuery — Data Warehouse ระดับ Enterprise ของ Google</h2>' +
      '<p><strong>BigQuery</strong> คือ Serverless Data Warehouse ของ Google ที่สามารถ Query ข้อมูล Petabyte ได้ภายในไม่กี่วินาที — ไม่ต้องตั้งค่า Server ไม่ต้อง Manage Infrastructure แค่ Upload ข้อมูลแล้ว Query เลย</p>' +
      '<h3>💡 ทำไม BigQuery ถึงเร็วมาก?</h3>' +
      '<ul><li><strong>Columnar Storage</strong> — เก็บข้อมูลแบบ Column ไม่ใช่ Row ทำให้ Query เฉพาะ Column ที่ต้องการ</li><li><strong>Distributed Computing</strong> — ใช้ CPU หลาย Thousands ในการ Query พร้อมกัน</li><li><strong>Dremel Engine</strong> — Query Engine ที่ Google พัฒนาเอง</li><li><strong>Automatic Caching</strong> — Cache ผลลัพธ์ Query ไว้ 24 ชั่วโมง</li></ul>' +
      '<h3>📤 วิธี Load ข้อมูลเข้า BigQuery</h3>' +
      '<pre><code class="language-python">from google.cloud import bigquery\nimport pandas as pd\n\nclient = bigquery.Client(project="my-project")\n\n# Method 1: Load จาก DataFrame\ndef load_df_to_bq(df: pd.DataFrame, table_id: str, write_mode: str = "WRITE_APPEND"):\n    job_config = bigquery.LoadJobConfig(\n        write_disposition=write_mode,\n        schema=[\n            bigquery.SchemaField("order_id", "INTEGER", mode="REQUIRED"),\n            bigquery.SchemaField("customer_id", "INTEGER"),\n            bigquery.SchemaField("amount", "FLOAT"),\n            bigquery.SchemaField("created_at", "TIMESTAMP"),\n        ]\n    )\n    job = client.load_table_from_dataframe(df, table_id, job_config=job_config)\n    job.result()\n    table = client.get_table(table_id)\n    print(f"Loaded {table.num_rows} rows to {table_id}")\n\n# Method 2: Load จาก GCS\ndef load_gcs_to_bq(gcs_uri: str, table_id: str):\n    job_config = bigquery.LoadJobConfig(\n        source_format=bigquery.SourceFormat.PARQUET,  # หรือ CSV, NEWLINE_DELIMITED_JSON\n        write_disposition="WRITE_TRUNCATE",\n        autodetect=True\n    )\n    job = client.load_table_from_uri(gcs_uri, table_id, job_config=job_config)\n    job.result()\n    print("Loaded from GCS")\n\n# Method 3: Read BigQuery → DataFrame\ndf = client.query("SELECT * FROM `my-project.dataset.table` LIMIT 1000").to_dataframe()</code></pre>' +
      '<h3>🗂️ Partitioning — ทำให้ Query ถูกลงและเร็วขึ้น</h3>' +
      '<p>Partition คือการแบ่ง Table เป็นส่วนๆ ตาม Column ทำให้ BigQuery scan เฉพาะ Partition ที่ต้องการ</p>' +
      '<pre><code class="language-sql">-- สร้าง Partitioned Table\nCREATE OR REPLACE TABLE `my_project.analytics.orders`\nPARTITION BY DATE(created_at)  -- แบ่งตามวันที่\nOPTIONS (\n    partition_expiration_days = 365  -- ลบ Partition เก่ากว่า 1 ปีอัตโนมัติ\n)\nAS SELECT * FROM `staging.raw_orders`;\n\n-- Query ที่ดีใช้ Partition Filter\n-- ✅ ดี: ระบุวันที่ — BigQuery scan แค่ Partition นั้น\nSELECT *\nFROM `my_project.analytics.orders`\nWHERE DATE(created_at) = \'2024-01-15\';\n\n-- ❌ แย่: ไม่มี Partition Filter — Scan ทั้ง Table!\nSELECT * FROM `my_project.analytics.orders`\nWHERE customer_id = 123;\n\n-- ดูขนาดของแต่ละ Partition\nSELECT\n    partition_id,\n    total_rows,\n    total_logical_bytes / POW(1024, 3) AS size_gb\nFROM `my_project.analytics.INFORMATION_SCHEMA.PARTITIONS`\nWHERE table_name = \'orders\'\nORDER BY partition_id DESC;</code></pre>' +
      '<h3>🎯 Clustering — Sort ข้อมูลภายใน Partition</h3>' +
      '<pre><code class="language-sql">-- สร้าง Table ที่ทั้ง Partition และ Cluster\nCREATE OR REPLACE TABLE `my_project.analytics.orders`\nPARTITION BY DATE(created_at)\nCLUSTER BY customer_id, status  -- Sort ภายใน partition ด้วย columns นี้\nAS SELECT * FROM `staging.raw_orders`;\n\n-- Query ที่ได้ประโยชน์จาก Clustering\nSELECT *\nFROM `my_project.analytics.orders`\nWHERE\n    DATE(created_at) = \'2024-01-15\'  -- Partition filter\n    AND customer_id = 555             -- Clustering benefit\n    AND status = \'completed\';          -- Clustering benefit</code></pre>' +
      '<h3>💸 BigQuery Pricing — ประหยัดเงิน</h3>' +
      '<pre><code class="language-text">BigQuery Pricing Model:\n========================\nStorage: $0.02/GB/month (active), $0.01/GB/month (long-term)\nQuery:   $5/TB scanned (first 1TB/month ฟรี!)\n\nTips ประหยัดเงิน:\n  1. ใช้ SELECT column1, column2 แทน SELECT *\n  2. ใช้ Partition Filter ทุกครั้ง\n  3. Preview ด้วย LIMIT ก่อน (แต่ Query cost คิดทั้งหมด!)\n  4. ใช้ Materialized Views\n  5. ตั้ง project-level quota เพื่อกัน overrun</code></pre>' +
      '<div class="tip-box"><strong>💡 Pro Tip:</strong> ก่อน Run Query ใหญ่ ดูที่มุมขวาบนของ BigQuery Editor จะเห็น "This query will process X GB" — ถ้าใหญ่มากให้เพิ่ม WHERE clause ก่อน</div>' +
      '<h3>🧮 BigQuery ฟีเจอร์ที่ DE ต้องรู้</h3>' +
      '<pre><code class="language-sql">-- MERGE (Upsert) — สำคัญมากสำหรับ incremental load\nMERGE `analytics.customers` T\nUSING `staging.customers_delta` S\nON T.customer_id = S.customer_id\nWHEN MATCHED THEN\n    UPDATE SET\n        T.customer_name = S.customer_name,\n        T.updated_at = CURRENT_TIMESTAMP()\nWHEN NOT MATCHED THEN\n    INSERT (customer_id, customer_name, created_at)\n    VALUES (S.customer_id, S.customer_name, CURRENT_TIMESTAMP());\n\n-- Scheduled Query\n-- ตั้งใน BQ Console: Scheduled Queries → Create\n-- หรือใช้ bq CLI:\nbq query --use_legacy_sql=false --schedule="every 24 hours" \\\n    "INSERT INTO analytics.daily_summary SELECT ..."</code></pre>'
  },
  {
    number: 11,
    slug: 'dbt-intro',
    emoji: '🔨',
    title: 'dbt: transform data อย่าง engineer ตัวจริง',
    content: '<h2>🔨 dbt — Transform Data อย่าง Software Engineer</h2>' +
      '<p><strong>dbt (data build tool)</strong> คือ Tool ที่เปลี่ยนวิธีทำ Data Transformation — แทนที่จะเขียน Python Script ยาวๆ คุณเขียนแค่ <strong>SQL + dbt จัดการส่วนที่เหลือ</strong> ทั้ง Dependency, Testing, Documentation, และ Deployment</p>' +
      '<p>dbt ทำให้ SQL เขียนแบบ Software Engineer — มี Version Control, Testing, และ CI/CD</p>' +
      '<h3>🎯 dbt ทำอะไรบ้าง?</h3>' +
      '<ul><li>🔄 <strong>Transform</strong> — แปลงข้อมูลใน Warehouse ด้วย SQL</li><li>📦 <strong>Dependency</strong> — จัดการ Model Dependencies อัตโนมัติ</li><li>✅ <strong>Testing</strong> — Test Data Quality ด้วย YAML</li><li>📚 <strong>Documentation</strong> — Generate Docs อัตโนมัติ</li><li>🔧 <strong>Macros</strong> — Reusable SQL Functions</li><li>🏗️ <strong>Lineage</strong> — เห็น Data Flow ทั้งหมด</li></ul>' +
      '<h3>⚙️ ติดตั้งและ Setup dbt</h3>' +
      '<pre><code class="language-bash"># ติดตั้ง dbt สำหรับ BigQuery\npip install dbt-bigquery\n\n# สร้าง Project ใหม่\ndbt init my_analytics\n\n# Structure ที่ได้\nmy_analytics/\n├── dbt_project.yml        # Config หลัก\n├── profiles.yml           # Connection settings\n├── models/                # SQL Models\n│   ├── staging/           # Raw → Cleaned\n│   ├── intermediate/      # Business Logic\n│   └── marts/             # Final Analytics Tables\n├── tests/                 # Custom Tests\n├── macros/                # Reusable SQL Functions\n└── seeds/                 # Static CSV Data</code></pre>' +
      '<pre><code class="language-yaml"># profiles.yml\nmy_analytics:\n  target: dev\n  outputs:\n    dev:\n      type: bigquery\n      method: service-account\n      project: my-gcp-project\n      dataset: dbt_dev\n      keyfile: /path/to/service-account.json\n      threads: 4\n    prod:\n      type: bigquery\n      method: service-account\n      project: my-gcp-project\n      dataset: analytics\n      keyfile: /path/to/service-account.json\n      threads: 8</code></pre>' +
      '<h3>📝 สร้าง dbt Model แรก</h3>' +
      '<pre><code class="language-sql">-- models/staging/stg_orders.sql\n-- Staging Model: Clean raw data\n\n{{config(\n    materialized=\'view\',\n    schema=\'staging\'\n)}}\n\nSELECT\n    CAST(order_id AS INT64) AS order_id,\n    CAST(customer_id AS INT64) AS customer_id,\n    CAST(amount AS FLOAT64) AS amount,\n    LOWER(TRIM(status)) AS status,\n    TIMESTAMP(created_at) AS created_at,\n    CURRENT_TIMESTAMP() AS _loaded_at\nFROM {{ source(\'raw\', \'orders\') }}\nWHERE order_id IS NOT NULL\n    AND amount > 0</code></pre>' +
      '<pre><code class="language-sql">-- models/marts/fct_orders.sql\n-- Fact Table: Business-ready\n\n{{config(\n    materialized=\'incremental\',\n    unique_key=\'order_id\',\n    partition_by={\'field\': \'order_date\', \'data_type\': \'date\'}\n)}}\n\nWITH orders AS (\n    SELECT * FROM {{ ref(\'stg_orders\') }}\n    {% if is_incremental() %}\n        WHERE created_at > (SELECT MAX(created_at) FROM {{ this }})\n    {% endif %}\n),\n\ncustomers AS (\n    SELECT * FROM {{ ref(\'stg_customers\') }}\n),\n\nfinal AS (\n    SELECT\n        o.order_id,\n        o.customer_id,\n        c.customer_name,\n        c.segment,\n        o.amount,\n        o.status,\n        DATE(o.created_at) AS order_date,\n        o.created_at\n    FROM orders o\n    LEFT JOIN customers c USING (customer_id)\n)\n\nSELECT * FROM final</code></pre>' +
      '<h3>✅ dbt Tests — Data Quality ใน YAML</h3>' +
      '<pre><code class="language-yaml"># models/staging/schema.yml\nversion: 2\n\nmodels:\n  - name: stg_orders\n    description: "Cleaned orders from raw source"\n    columns:\n      - name: order_id\n        description: "Unique order identifier"\n        tests:\n          - unique\n          - not_null\n      - name: status\n        tests:\n          - not_null\n          - accepted_values:\n              values: [\'completed\', \'pending\', \'cancelled\', \'shipped\']\n      - name: amount\n        tests:\n          - not_null\n          - dbt_utils.expression_is_true:\n              expression: ">= 0"</code></pre>' +
      '<h3>🚀 คำสั่ง dbt ที่ใช้บ่อย</h3>' +
      '<pre><code class="language-bash"># Build ทุก models\ndbt run\n\n# Build เฉพาะ model\ndbt run --select stg_orders\n\n# Build model + ทุก dependencies\ndbt run --select +fct_orders\n\n# รัน Tests\ndbt test\ndbt test --select stg_orders\n\n# Generate และดู Documentation\ndbt docs generate\ndbt docs serve  # เปิด Browser ที่ localhost:8080\n\n# Debug connection\ndbt debug\n\n# Seed CSV files\ndbt seed</code></pre>' +
      '<div class="tip-box"><strong>💡 dbt Materialization:</strong> เลือกตามการใช้งาน — <code>view</code>: ไม่เก็บ Storage (ดีสำหรับ Staging), <code>table</code>: rebuild ทุกครั้ง (ดีสำหรับ Marts), <code>incremental</code>: เพิ่มเฉพาะ data ใหม่ (ดีสำหรับ Fact Tables ใหญ่)</div>'
  },
  {
    number: 12,
    slug: 'spark-intro',
    emoji: '⚡',
    title: 'Apache Spark: big data processing basics',
    content: '<h2>⚡ Apache Spark — Big Data Processing Engine</h2>' +
      '<p><strong>Apache Spark</strong> คือ Distributed Computing Framework สำหรับประมวลผลข้อมูลขนาดใหญ่ — ถ้าข้อมูลใหญ่เกินกว่าจะใส่ใน RAM เครื่องเดียวได้ Spark คือคำตอบ</p>' +
      '<p>Spark เร็วกว่า Hadoop MapReduce ถึง 100x เพราะประมวลผลใน Memory แทนการเขียน Disk</p>' +
      '<h3>🔑 Spark Concepts ที่ต้องรู้</h3>' +
      '<ul><li><strong>RDD (Resilient Distributed Dataset)</strong> — Data structure พื้นฐาน (ไม่ค่อยใช้ตรงๆ แล้ว)</li><li><strong>DataFrame</strong> — เหมือน Pandas DataFrame แต่ Distributed</li><li><strong>Transformation</strong> — Operations แบบ Lazy (filter, select, join)</li><li><strong>Action</strong> — Operations ที่ Trigger การประมวลผลจริง (show, count, write)</li><li><strong>Partition</strong> — ส่วนย่อยของข้อมูลที่กระจายไปตาม Nodes</li><li><strong>Driver</strong> — โปรแกรมหลักที่ orchestrate การทำงาน</li><li><strong>Executor</strong> — Workers ที่ประมวลผลจริง</li></ul>' +
      '<h3>🐍 PySpark — Spark สำหรับ Python</h3>' +
      '<pre><code class="language-bash"># ติดตั้ง PySpark\npip install pyspark\n\n# หรือ Run ด้วย Docker\ndocker run -it --rm \\\n    -p 4040:4040 \\\n    jupyter/pyspark-notebook \\\n    /bin/bash</code></pre>' +
      '<pre><code class="language-python">from pyspark.sql import SparkSession\nfrom pyspark.sql import functions as F\nfrom pyspark.sql.types import *\n\n# สร้าง SparkSession\nspark = SparkSession.builder \\\n    .appName("DE101 Spark Intro") \\\n    .master("local[*]") \\\n    .config("spark.sql.adaptive.enabled", "true") \\\n    .getOrCreate()\n\nspark.sparkContext.setLogLevel("WARN")\nprint(f"Spark Version: {spark.version}")\n\n# อ่านข้อมูล\ndf = spark.read.parquet("gs://my-bucket/sales/*.parquet")\n\n# Schema\ndf.printSchema()\ndf.show(5)\nprint(f"Rows: {df.count()}, Columns: {len(df.columns)}")</code></pre>' +
      '<h3>🔄 Spark Transformations</h3>' +
      '<pre><code class="language-python"># Filter และ Select\ndf_filtered = df \\\n    .filter(F.col("amount") > 1000) \\\n    .filter(F.col("status") == "completed") \\\n    .select("order_id", "customer_id", "amount", "created_at")\n\n# Add Columns\ndf_enriched = df_filtered \\\n    .withColumn("year", F.year("created_at")) \\\n    .withColumn("month", F.month("created_at")) \\\n    .withColumn("amount_thb", F.col("amount") * 35.5) \\\n    .withColumn("is_large", F.when(F.col("amount") > 50000, True).otherwise(False))\n\n# Aggregation\ndaily_summary = df_enriched \\\n    .groupBy("year", "month", F.date_trunc("day", "created_at").alias("date")) \\\n    .agg(\n        F.count("*").alias("total_orders"),\n        F.sum("amount").alias("total_revenue"),\n        F.avg("amount").alias("avg_order_value"),\n        F.countDistinct("customer_id").alias("unique_customers")\n    ) \\\n    .orderBy("date")\n\ndaily_summary.show()\n\n# JOIN\ncustomers = spark.read.parquet("gs://my-bucket/customers/*.parquet")\n\ndf_joined = df_enriched \\\n    .join(customers, on="customer_id", how="left") \\\n    .select(\n        "order_id", "customer_id", "customer_name",\n        "segment", "amount", "created_at"\n    )</code></pre>' +
      '<h3>💾 อ่านและเขียนข้อมูล</h3>' +
      '<pre><code class="language-python"># อ่าน\ndf_csv = spark.read.csv("data/sales.csv", header=True, inferSchema=True)\ndf_json = spark.read.json("data/events/*.json")\ndf_parquet = spark.read.parquet("data/orders/*.parquet")\n\n# เขียน (แนะนำ Parquet เสมอ)\ndf_result.write \\\n    .mode("overwrite") \\\n    .partitionBy("year", "month") \\\n    .parquet("gs://my-bucket/output/orders/")\n\n# เขียนเข้า BigQuery\ndf_result.write \\\n    .format("bigquery") \\\n    .option("table", "my-project:analytics.orders") \\\n    .option("temporaryGcsBucket", "my-temp-bucket") \\\n    .mode("append") \\\n    .save()\n\n# Spark SQL — เขียน SQL แทน DataFrame API\ndf.createOrReplaceTempView("orders")\nresult = spark.sql("""\n    SELECT\n        DATE_TRUNC(\'month\', created_at) AS month,\n        SUM(amount) AS revenue\n    FROM orders\n    WHERE status = \'completed\'\n    GROUP BY 1\n    ORDER BY 1\n""")\nresult.show()</code></pre>' +
      '<h3>⚡ Performance Tips</h3>' +
      '<pre><code class="language-python"># 1. Cache ถ้าจะใช้ DataFrame หลายครั้ง\ndf_joined.cache()  # หรือ .persist(StorageLevel.MEMORY_AND_DISK)\n\n# 2. Repartition ก่อน Write\ndf_result.repartition(10).write.parquet("output/")\n\n# 3. Broadcast Join สำหรับ Table เล็ก\nfrom pyspark.sql.functions import broadcast\ndf_big.join(broadcast(df_small), on="customer_id")\n\n# 4. ดู Execution Plan\ndf_result.explain(True)\n\n# 5. Spark UI ที่ http://localhost:4040\nprint("Spark UI: http://localhost:4040")</code></pre>' +
      '<div class="tip-box"><strong>💡 เมื่อไหร่ควรใช้ Spark?</strong> ถ้าข้อมูล < 10 GB → ใช้ Pandas หรือ BigQuery ก็พอ ถ้าข้อมูล > 100 GB หรือต้องประมวลผลแบบ Distributed → นั่นคือเวลาของ Spark Spark เหมาะกับ Batch Processing ใหญ่ๆ เช่น รัน ETL สำหรับข้อมูล 1 ปีทุกสัปดาห์</div>'
  },
  {
    number: 13,
    slug: 'data-quality',
    emoji: '✅',
    title: 'Data Quality, Testing, Great Expectations',
    content: '<h2>✅ Data Quality — ข้อมูลที่ไม่ถูกต้องคือภัยพิบัติ</h2>' +
      '<p>Data Pipeline ที่ดีไม่ใช่แค่ที่ "รันได้" — แต่ต้องรับประกันว่า <strong>ข้อมูลที่ได้ถูกต้อง สมบูรณ์ และทันเวลา</strong> ข้อมูลที่ผิดพลาดเข้าไปใน Dashboard ทำให้ Business ตัดสินใจผิดพลาด ซึ่งอาจทำให้บริษัทเสียหายได้มหาศาล</p>' +
      '<h3>🔍 มิติของ Data Quality</h3>' +
      '<table border="1" cellpadding="8" cellspacing="0" style="border-collapse:collapse;width:100%"><tr style="background:#1a1a2e;color:#fff"><th>มิติ</th><th>คำถาม</th><th>ตัวอย่าง</th></tr><tr><td><strong>Completeness</strong></td><td>ข้อมูลครบหรือไม่?</td><td>customer_name ไม่มี NULL</td></tr><tr><td><strong>Accuracy</strong></td><td>ข้อมูลถูกต้องหรือไม่?</td><td>amount ต้องไม่ติดลบ</td></tr><tr><td><strong>Consistency</strong></td><td>ข้อมูลสอดคล้องกันหรือไม่?</td><td>status อยู่ใน allowed values</td></tr><tr><td><strong>Uniqueness</strong></td><td>ไม่มี Duplicate หรือไม่?</td><td>order_id ไม่ซ้ำกัน</td></tr><tr><td><strong>Timeliness</strong></td><td>ข้อมูลทันเวลาหรือไม่?</td><td>ข้อมูลเมื่อวานพร้อมใช้เช้าวันนี้</td></tr><tr><td><strong>Validity</strong></td><td>Format ถูกต้องหรือไม่?</td><td>email format ถูก, date อยู่ในช่วงที่สมเหตุสมผล</td></tr></table>' +
      '<h3>🧪 เขียน Data Quality Checks ด้วย Python</h3>' +
      '<pre><code class="language-python">import pandas as pd\nfrom dataclasses import dataclass\nfrom typing import List\n\n@dataclass\nclass QualityCheckResult:\n    check_name: str\n    passed: bool\n    message: str\n    failed_rows: int = 0\n\nclass DataQualityChecker:\n    def __init__(self, df: pd.DataFrame, table_name: str):\n        self.df = df\n        self.table_name = table_name\n        self.results: List[QualityCheckResult] = []\n    \n    def check_not_null(self, column: str) -> "DataQualityChecker":\n        null_count = self.df[column].isnull().sum()\n        self.results.append(QualityCheckResult(\n            check_name=f"{column}_not_null",\n            passed=null_count == 0,\n            message=f"{null_count} nulls in {column}",\n            failed_rows=null_count\n        ))\n        return self\n    \n    def check_unique(self, column: str) -> "DataQualityChecker":\n        dup_count = self.df[column].duplicated().sum()\n        self.results.append(QualityCheckResult(\n            check_name=f"{column}_unique",\n            passed=dup_count == 0,\n            message=f"{dup_count} duplicates in {column}",\n            failed_rows=dup_count\n        ))\n        return self\n    \n    def check_accepted_values(self, column: str, values: list) -> "DataQualityChecker":\n        invalid = ~self.df[column].isin(values)\n        invalid_count = invalid.sum()\n        self.results.append(QualityCheckResult(\n            check_name=f"{column}_accepted_values",\n            passed=invalid_count == 0,\n            message=f"{invalid_count} rows with invalid {column}",\n            failed_rows=invalid_count\n        ))\n        return self\n    \n    def check_row_count(self, min_rows: int) -> "DataQualityChecker":\n        count = len(self.df)\n        self.results.append(QualityCheckResult(\n            check_name="min_row_count",\n            passed=count >= min_rows,\n            message=f"{count} rows (min: {min_rows})"\n        ))\n        return self\n    \n    def run(self, fail_on_error: bool = True) -> bool:\n        failed = [r for r in self.results if not r.passed]\n        \n        print(f"\\n=== Data Quality Report: {self.table_name} ===")\n        for r in self.results:\n            status = "✅ PASS" if r.passed else "❌ FAIL"\n            print(f"  {status} | {r.check_name}: {r.message}")\n        \n        if failed:\n            print(f"\\n{len(failed)}/{len(self.results)} checks FAILED")\n            if fail_on_error:\n                raise ValueError(f"Data Quality checks failed: {[f.check_name for f in failed]}")\n            return False\n        \n        print(f"\\nAll {len(self.results)} checks PASSED ✅")\n        return True\n\n# ใช้งาน\ndf = pd.read_parquet("orders.parquet")\n\nchecker = DataQualityChecker(df, "orders")\nchecker \\\n    .check_not_null("order_id") \\\n    .check_not_null("customer_id") \\\n    .check_unique("order_id") \\\n    .check_accepted_values("status", ["completed", "pending", "cancelled", "shipped"]) \\\n    .check_row_count(100) \\\n    .run(fail_on_error=True)</code></pre>' +
      '<h3>🎯 Great Expectations — Framework มาตรฐาน</h3>' +
      '<pre><code class="language-bash">pip install great-expectations\ngx init  # สร้าง project structure</code></pre>' +
      '<pre><code class="language-python">import great_expectations as gx\n\n# สร้าง Context\ncontext = gx.get_context()\n\n# สร้าง Expectation Suite\nsuite = context.add_expectation_suite("orders_suite")\n\n# สร้าง Validator\nvalidator = context.get_validator(\n    batch_request=...,\n    expectation_suite_name="orders_suite"\n)\n\n# กำหนด Expectations\nvalidator.expect_column_values_to_not_be_null("order_id")\nvalidator.expect_column_values_to_be_unique("order_id")\nvalidator.expect_column_values_to_be_in_set("status", ["completed", "pending", "cancelled"])\nvalidator.expect_column_values_to_be_between("amount", min_value=0, max_value=1000000)\nvalidator.expect_column_values_to_match_regex("email", r".+@.+\\..+")\nvalidator.expect_table_row_count_to_be_between(min_value=100)\n\n# Save\nvalidator.save_expectation_suite()\n\n# Validate\nresult = context.run_checkpoint(checkpoint_name="orders_checkpoint")\nprint(f"Success: {result.success}")</code></pre>' +
      '<div class="tip-box"><strong>💡 Data Quality ใน Pipeline:</strong> ใส่ Quality Checks ไว้ใน Pipeline เสมอ — ทำทั้งก่อน Load (input validation) และหลัง Load (output validation) ถ้า Check fail ให้ Pipeline หยุดและแจ้งเตือนทันที อย่าปล่อยข้อมูลเสียเข้า Warehouse</div>'
  },
  {
    number: 14,
    slug: 'monitoring',
    emoji: '📡',
    title: 'Pipeline Monitoring, Alerting, SLAs',
    content: '<h2>📡 Monitoring & Alerting — Pipeline ต้องเชื่อถือได้</h2>' +
      '<p>Pipeline ที่ดีคือ Pipeline ที่ <strong>เมื่อพังแล้ว คนรู้ทันที</strong> ไม่ใช่รู้เมื่อ Business มาบอกว่า "ทำไม Dashboard ข้อมูลเมื่อวานหายไป?" — Monitoring และ Alerting คือความแตกต่างระหว่าง DE มืออาชีพกับ DE มือใหม่</p>' +
      '<h3>🎯 SLA, SLO, SLI คืออะไร?</h3>' +
      '<ul><li><strong>SLI (Service Level Indicator)</strong> — วัดอะไร? เช่น Pipeline Success Rate, Data Freshness</li><li><strong>SLO (Service Level Objective)</strong> — ต้องได้เท่าไหร่? เช่น Success Rate >= 99.5%</li><li><strong>SLA (Service Level Agreement)</strong> — ข้อตกลงกับ Business เช่น ข้อมูลพร้อมก่อน 8:00 AM ทุกวัน</li></ul>' +
      '<pre><code class="language-text">ตัวอย่าง SLA สำหรับ Daily Sales Pipeline:\n==========================================\nSLI: Pipeline Success Rate\nSLO: >= 99% (ล้มเหลวได้ไม่เกิน 3-4 ครั้ง/ปี)\nSLA: ข้อมูล Sales ของเมื่อวานพร้อมใน BigQuery ก่อน 7:00 AM ทุกวัน\n     ถ้าไม่พร้อม → ต้องแจ้ง Business ภายใน 15 นาที\n     ถ้าล่าช้า > 2 ชั่วโมง → Root Cause Analysis report ภายใน 24 ชั่วโมง</code></pre>' +
      '<h3>📊 Metrics ที่ DE ต้อง Monitor</h3>' +
      '<pre><code class="language-python">import time\nfrom datetime import datetime\nfrom dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass PipelineMetrics:\n    pipeline_name: str\n    run_date: str\n    start_time: float = field(default_factory=time.time)\n    end_time: Optional[float] = None\n    records_extracted: int = 0\n    records_loaded: int = 0\n    records_failed: int = 0\n    status: str = "running"\n    error_message: Optional[str] = None\n    \n    @property\n    def duration_seconds(self) -> float:\n        if self.end_time:\n            return self.end_time - self.start_time\n        return time.time() - self.start_time\n    \n    def complete(self, records_loaded: int):\n        self.end_time = time.time()\n        self.records_loaded = records_loaded\n        self.status = "success"\n    \n    def fail(self, error: str):\n        self.end_time = time.time()\n        self.status = "failed"\n        self.error_message = error\n    \n    def to_dict(self) -> dict:\n        return {\n            "pipeline_name": self.pipeline_name,\n            "run_date": self.run_date,\n            "status": self.status,\n            "duration_seconds": round(self.duration_seconds, 2),\n            "records_extracted": self.records_extracted,\n            "records_loaded": self.records_loaded,\n            "records_failed": self.records_failed,\n            "error_message": self.error_message,\n            "logged_at": datetime.now().isoformat()\n        }\n\n# ใช้งานใน Pipeline\ndef run_pipeline_with_metrics(run_date: str):\n    metrics = PipelineMetrics("sales_pipeline", run_date)\n    \n    try:\n        data = extract(run_date)\n        metrics.records_extracted = len(data)\n        \n        clean = transform(data)\n        loaded = load(clean)\n        metrics.complete(loaded)\n        \n    except Exception as e:\n        metrics.fail(str(e))\n        send_alert(f"Pipeline FAILED: {e}", severity="critical")\n        raise\n    finally:\n        # บันทึก metrics ทุกครั้งไม่ว่าจะสำเร็จหรือไม่\n        save_metrics_to_db(metrics.to_dict())\n        print(metrics.to_dict())</code></pre>' +
      '<h3>🔔 ระบบ Alerting</h3>' +
      '<pre><code class="language-python">import requests\nfrom enum import Enum\n\nclass AlertSeverity(Enum):\n    INFO = "info"\n    WARNING = "warning"\n    CRITICAL = "critical"\n\ndef send_slack_alert(message: str, severity: AlertSeverity, channel: str = "#data-alerts"):\n    """ส่ง Alert ไปยัง Slack"""\n    color_map = {\n        AlertSeverity.INFO: "#36a64f",       # เขียว\n        AlertSeverity.WARNING: "#ff9500",    # ส้ม\n        AlertSeverity.CRITICAL: "#ff0000",   # แดง\n    }\n    \n    payload = {\n        "channel": channel,\n        "attachments": [{\n            "color": color_map[severity],\n            "text": message,\n            "footer": f"DE Monitoring System | {datetime.now().strftime(\'%Y-%m-%d %H:%M\')}",\n            "title": f"[{severity.value.upper()}] Data Pipeline Alert"\n        }]\n    }\n    \n    webhook_url = os.getenv("SLACK_WEBHOOK_URL")\n    resp = requests.post(webhook_url, json=payload, timeout=10)\n    resp.raise_for_status()\n\n# ใน Airflow — ตั้ง on_failure_callback\ndef alert_on_failure(context):\n    dag_id = context[\'dag\'].dag_id\n    task_id = context[\'task\'].task_id\n    run_id = context[\'run_id\']\n    \n    message = (\n        f"*Pipeline Failed!* 🚨\\n"\n        f"DAG: `{dag_id}`\\n"\n        f"Task: `{task_id}`\\n"\n        f"Run ID: `{run_id}`\\n"\n        f"<http://localhost:8080/dags/{dag_id}|View in Airflow>"\n    )\n    send_slack_alert(message, AlertSeverity.CRITICAL)\n\n# ใน DAG\ndefault_args = {\n    "on_failure_callback": alert_on_failure,\n    "retries": 3,\n    "retry_delay": timedelta(minutes=5),\n}</code></pre>' +
      '<h3>📈 Monitoring Dashboard ด้วย SQL</h3>' +
      '<pre><code class="language-sql">-- Pipeline Health Dashboard\nSELECT\n    pipeline_name,\n    DATE(run_date) AS date,\n    COUNT(*) AS total_runs,\n    COUNTIF(status = \'success\') AS successful_runs,\n    COUNTIF(status = \'failed\') AS failed_runs,\n    ROUND(100.0 * COUNTIF(status = \'success\') / COUNT(*), 2) AS success_rate,\n    ROUND(AVG(duration_seconds) / 60, 2) AS avg_duration_minutes,\n    MAX(records_loaded) AS max_records\nFROM pipeline_metrics\nWHERE DATE(run_date) >= DATE_SUB(CURRENT_DATE(), INTERVAL 30 DAY)\nGROUP BY 1, 2\nORDER BY 1, 2 DESC;</code></pre>' +
      '<div class="tip-box"><strong>💡 Golden Rules ของ Monitoring:</strong> (1) Alert ต้อง Actionable — ถ้า alert ไปแล้วไม่รู้ต้องทำอะไร แสดงว่า Alert ไม่ดี (2) ลด Noise — Alert เยอะเกินไปทำให้คนชิน จนไม่สนใจ Alert สำคัญ (3) ทุก Alert ต้องมี Runbook — เอกสารที่บอกขั้นตอนการแก้ไขปัญหา</div>'
  },
  {
    number: 15,
    slug: 'portfolio',
    emoji: '🏆',
    title: 'สร้าง Portfolio Project ให้ได้งาน DE',
    content: '<h2>🏆 Portfolio Project — ประตูสู่งาน Data Engineer</h2>' +
      '<p>การสมัครงาน DE โดยไม่มี Portfolio เหมือนการบอกว่า "ฉันขับรถได้" โดยไม่เคยขับ Portfolio คือหลักฐานที่พูดแทนคุณ — บอก Hiring Manager ว่าคุณทำได้จริง ไม่ใช่แค่ผ่านคอร์สมา</p>' +
      '<h3>🎯 Portfolio ที่ดีต้องมีอะไรบ้าง?</h3>' +
      '<ul><li>✅ <strong>End-to-End Pipeline</strong> — ครบทุกขั้นตอนตั้งแต่ Source ถึง Dashboard</li><li>✅ <strong>Real Data</strong> — ใช้ข้อมูลจริงจาก Public API หรือ Public Dataset</li><li>✅ <strong>Production-like</strong> — มี Error Handling, Logging, Testing</li><li>✅ <strong>Modern Stack</strong> — Airflow + dbt + BigQuery/Spark</li><li>✅ <strong>Documentation</strong> — README ดี, Architecture Diagram ชัดเจน</li><li>✅ <strong>GitHub Repository</strong> — Code สะอาด, Commit history ดี</li></ul>' +
      '<h3>🚀 Portfolio Project: Real-time Stock Market Pipeline</h3>' +
      '<p>โปรเจกต์ที่แนะนำสำหรับ DE Beginner — สร้าง Data Pipeline สำหรับข้อมูลหุ้น</p>' +
      '<pre><code class="language-text">Project Architecture:\n========================\n\n[Yahoo Finance API]\n        ↓\n[Python Extractor (Daily)]\n        ↓\n[Google Cloud Storage]\n   (Raw Parquet files)\n        ↓\n[BigQuery (Staging)]\n        ↓\n[dbt Transform]\n   - stg_stock_prices\n   - fct_daily_returns\n   - fct_portfolio_performance\n        ↓\n[BigQuery (Analytics)]\n        ↓\n[Looker Studio Dashboard]\n\nOrchestration: Apache Airflow\nMonitoring: Slack Alerts + Pipeline Metrics Table\nTesting: dbt tests + Great Expectations\nIaC: Terraform (optional)</code></pre>' +
      '<h3>📁 Project Structure</h3>' +
      '<pre><code class="language-text">stock-market-pipeline/\n├── README.md              # อธิบาย project ชัดเจน\n├── architecture.png       # Diagram\n├── .env.example           # Template สำหรับ .env\n├── requirements.txt\n├── docker-compose.yml     # Airflow + Postgres\n├── dags/\n│   └── stock_pipeline.py  # Airflow DAG\n├── src/\n│   ├── extractors/\n│   │   └── yahoo_finance.py\n│   ├── loaders/\n│   │   ├── gcs_loader.py\n│   │   └── bigquery_loader.py\n│   └── utils/\n│       ├── logging.py\n│       └── alerts.py\n├── dbt/\n│   ├── dbt_project.yml\n│   ├── models/\n│   │   ├── staging/\n│   │   │   └── stg_stock_prices.sql\n│   │   └── marts/\n│   │       ├── fct_daily_returns.sql\n│   │       └── fct_portfolio.sql\n│   └── tests/\n├── tests/\n│   ├── test_extractor.py\n│   └── test_transformer.py\n└── terraform/             # IaC (bonus)\n    ├── main.tf\n    └── variables.tf</code></pre>' +
      '<h3>💻 ตัวอย่าง Code หลัก</h3>' +
      '<pre><code class="language-python"># src/extractors/yahoo_finance.py\nimport yfinance as yf\nimport pandas as pd\nfrom datetime import datetime, timedelta\nimport logging\n\nlogger = logging.getLogger(__name__)\n\nTICKERS = [\'AAPL\', \'GOOGL\', \'MSFT\', \'AMZN\', \'META\']\n\ndef extract_stock_prices(date: str) -> pd.DataFrame:\n    """ดึงราคาหุ้นจาก Yahoo Finance"""\n    logger.info(f"Extracting stock data for {date}")\n    \n    end_date = datetime.strptime(date, \'%Y-%m-%d\') + timedelta(days=1)\n    \n    all_data = []\n    for ticker in TICKERS:\n        try:\n            stock = yf.Ticker(ticker)\n            hist = stock.history(start=date, end=end_date.strftime(\'%Y-%m-%d\'))\n            \n            if hist.empty:\n                logger.warning(f"No data for {ticker} on {date}")\n                continue\n            \n            hist = hist.reset_index()\n            hist[\'ticker\'] = ticker\n            all_data.append(hist)\n            \n        except Exception as e:\n            logger.error(f"Failed to extract {ticker}: {e}")\n    \n    if not all_data:\n        raise ValueError(f"No data extracted for {date}")\n    \n    df = pd.concat(all_data, ignore_index=True)\n    df.columns = [c.lower().replace(\' \', \'_\') for c in df.columns]\n    \n    logger.info(f"Extracted {len(df)} records for {len(TICKERS)} tickers")\n    return df</code></pre>' +
      '<pre><code class="language-sql">-- dbt/models/marts/fct_daily_returns.sql\n{{config(materialized=\'incremental\', unique_key=[\'ticker\', \'date\'])}}\n\nWITH prices AS (\n    SELECT * FROM {{ ref(\'stg_stock_prices\') }}\n    {% if is_incremental() %}\n        WHERE date > (SELECT MAX(date) FROM {{ this }})\n    {% endif %}\n),\n\nwith_prev AS (\n    SELECT\n        ticker,\n        date,\n        close_price,\n        LAG(close_price) OVER (PARTITION BY ticker ORDER BY date) AS prev_close\n    FROM prices\n)\n\nSELECT\n    ticker,\n    date,\n    close_price,\n    prev_close,\n    ROUND(close_price - prev_close, 4) AS price_change,\n    ROUND(100.0 * (close_price - prev_close) / prev_close, 2) AS pct_return\nFROM with_prev\nWHERE prev_close IS NOT NULL</code></pre>' +
      '<h3>📝 README ที่ดึงดูด Recruiter</h3>' +
      '<pre><code class="language-markdown"># 📈 Stock Market Data Pipeline\n\nEnd-to-end Data Pipeline สำหรับวิเคราะห์ราคาหุ้น US Market\n\n## Architecture\n[ใส่รูป Architecture Diagram]\n\n## Tech Stack\n- **Orchestration**: Apache Airflow 2.7\n- **Storage**: Google Cloud Storage\n- **Warehouse**: BigQuery\n- **Transform**: dbt 1.7\n- **Processing**: Python 3.11 + Pandas\n- **Containerization**: Docker\n- **Dashboard**: Looker Studio\n\n## Features\n- ✅ Daily automated ingestion of 5 major stocks\n- ✅ Data quality checks with Great Expectations\n- ✅ dbt incremental models for efficiency\n- ✅ Slack alerting on pipeline failures\n- ✅ Pipeline metrics dashboard\n\n## Setup\n...\n\n## Dashboard\n[ใส่ลิงก์ Looker Studio Dashboard]</code></pre>' +
      '<div class="tip-box"><strong>🏆 Tips สำหรับการหางาน:</strong> (1) ทำ Portfolio 1-2 โปรเจกต์ที่ดีมากๆ ดีกว่า 10 โปรเจกต์ที่ทำไม่เสร็จ (2) เขียน Blog อธิบาย technical challenges ที่เจอใน Portfolio (3) LinkedIn ต้องมี Skills ครบ: Airflow, dbt, BigQuery, Python, SQL (4) สมัครงานเยอะๆ DE entry-level ในไทยยังขาดแคลนมาก (5) อย่ารอให้ Portfolio "สมบูรณ์แบบ" 100% — เริ่มสมัครได้เลยตั้งแต่มีโปรเจกต์ที่ทำงานได้</div>' +
      '<h3>🎓 ขั้นตอนต่อไปหลังจาก DE101</h3>' +
      '<pre><code class="language-text">เส้นทางต่อจาก DE101:\n====================\n\n1. DE201 — Intermediate Data Engineering\n   → Advanced Airflow, Advanced dbt, Kafka, CI/CD\n\n2. SQL Mastery\n   → Window Functions, Query Optimization, BigQuery Advanced\n\n3. GCP Certification\n   → Professional Data Engineer Exam\n\n4. Kafka 101\n   → Stream Processing, Real-time Pipeline\n\n5. Portfolio Interview Prep\n   → Technical Interview, System Design\n\nคุณพร้อมแล้ว — ขอให้โชคดีในการหางาน DE! 🚀</code></pre>'
  }
];
