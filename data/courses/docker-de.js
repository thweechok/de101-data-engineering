export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '🐳',
    title: 'Docker สำหรับ Data Engineer',
    content: '<h2>Docker คืออะไรและทำไม DE ต้องรู้?</h2><p>Docker คือ containerization platform ที่ช่วยให้คุณ package application พร้อม dependencies ทั้งหมดไว้ใน container เดียว แล้ว run ได้ทุกที่ ไม่ว่าจะเป็น laptop ของนักพัฒนา, staging server, หรือ production cloud การเข้าใจ Docker เป็นพื้นฐานสำคัญของ modern Data Engineering ที่ไม่สามารถข้ามได้</p><p>ปัญหา classic ที่ Docker แก้คือ "It works on my machine" เมื่อก่อน DE ต้องใช้เวลามากในการ setup environment ให้ตรงกันระหว่าง dev, staging, production ด้วย Docker ทุก environment รัน container เดียวกันทำให้ behavior สม่ำเสมอและ reproducible</p><h2>Docker ในโลก Data Engineering</h2><p>DE ใช้ Docker ในหลายบริบท: รัน Apache Airflow บน Docker, package Spark jobs เป็น containers, deploy API services สำหรับ data access, รัน databases สำหรับ development (Postgres, MySQL), สร้าง reproducible data processing environments ที่ทีมทุกคนใช้ได้เหมือนกัน</p><p>Docker กลายเป็น prerequisite สำหรับ Kubernetes, Apache Airflow, dbt Cloud, และ tools อื่นๆ ใน modern data stack การเข้าใจ Docker concepts ทำให้คุณทำงานกับ tools เหล่านี้ได้ดีขึ้นมาก และ debug ปัญหาได้เร็วขึ้น</p><h2>Container Ecosystem</h2><p>Docker ecosystem ประกอบด้วย: Docker Engine (runtime ที่รัน containers), Docker Hub (public registry เก็บ images), Docker Compose (จัดการ multi-container applications), Docker Desktop (GUI สำหรับ Mac/Windows) นอกจาก Docker ยังมี alternatives เช่น Podman, containerd แต่ Docker ยังเป็น standard</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ติดตั้ง Docker Desktop บน local machine เพื่อเริ่มต้น มี GUI ที่ใช้งานง่าย แสดง running containers, resource usage, และ logs ได้ สามารถ toggle ไป Docker Engine สำหรับ production servers ที่ไม่มี GUI ภายหลัง</div><p>ในบทถัดไปเราจะลงลึกเรื่อง Container vs VM เพื่อเข้าใจว่าทำไม containers ถึงเป็นที่นิยมมากกว่า virtual machines ในยุคปัจจุบัน และ trade-offs ที่ต้องพิจารณาเมื่อเลือก technology</p>'
  },
  {
    number: 1,
    slug: 'concepts',
    emoji: '💡',
    title: 'Container vs Virtual Machine',
    content: '<h2>Virtual Machine คืออะไร?</h2><p>Virtual Machine (VM) คือการ emulate hardware เต็มรูปแบบ แต่ละ VM มี OS ของตัวเอง ต้องใช้ hypervisor (VMware, VirtualBox, KVM) ในการจัดการ VM แต่ละตัวใช้ RAM หลาย GB เพราะต้องรัน full OS stack boot time ใช้เวลาหลายนาทีซึ่งช้าสำหรับ modern workflows</p><p>ข้อดีของ VM: isolation สมบูรณ์แบบ, สามารถรัน OS ต่างชนิดบน host เดียวกัน, security boundary แข็งแกร่งกว่า Container ข้อเสีย: หนักมาก, ใช้ resource มาก, boot ช้า, port ยากกว่าเพราะ image ขนาดใหญ่หลาย GB</p><h2>Container คืออะไร?</h2><p>Container share kernel กับ host OS ทำให้เบากว่า VM มาก Container ไม่ได้ emulate hardware แต่ใช้ Linux features (namespaces, cgroups) เพื่อ isolate processes Container start ใน milliseconds ใช้ RAM เพียงสำหรับ application เอง ไม่ต้องมี OS overhead</p><p>Docker Container ประกอบด้วย: Application code, Runtime (Python, Java, Node.js), Libraries และ Dependencies, Configuration files ทุกอย่าง package ใน Docker Image ซึ่งเป็น immutable template สำหรับสร้าง containers เหมือน blueprint</p><h2>เปรียบเทียบโดยตรง</h2><p>เปรียบเทียบ: VM ใช้ RAM 1-4GB ต่อ instance, Container ใช้ RAM 50-200MB, VM boot 1-5 นาที, Container start ใน 1-10 วินาที, VM portable แต่ image ใหญ่หลาย GB, Container image มักแค่ 100-500MB ทำให้ distribute และ deploy เร็วกว่ามาก</p><p>สำหรับ Data Engineering: ใช้ Container สำหรับ application workloads (Airflow, APIs, dbt), ใช้ VM สำหรับ long-running compute workloads หรือเมื่อต้องการ stronger isolation ระหว่าง tenants ใน multi-tenant environments</p><h2>Docker Architecture</h2><p>Docker ทำงานแบบ client-server: Docker Client (CLI) ส่ง commands ไปยัง Docker Daemon (dockerd) ที่รันเป็น background service Docker Daemon จัดการ images, containers, networks, volumes Docker Registry เก็บ images ให้ download และ upload</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ docker stats command เพื่อ monitor resource usage ของ containers ที่กำลังรัน ช่วย identify containers ที่กิน CPU/Memory มากเกินไปก่อนที่จะเป็นปัญหา production ทำให้ capacity planning แม่นยำขึ้น</div><p>Container networking: แต่ละ container มี virtual network interface ของตัวเอง containers communicate กันผ่าน Docker networks การเข้าใจ networking เป็นสิ่งสำคัญสำหรับ multi-container data applications เช่น Airflow + Postgres + Redis</p>'
  },
  {
    number: 2,
    slug: 'dockerfile',
    emoji: '📝',
    title: 'เขียน Dockerfile สำหรับ Python และ Airflow',
    content: '<h2>Dockerfile คืออะไร?</h2><p>Dockerfile คือ text file ที่ contain instructions สำหรับ build Docker image แต่ละ instruction สร้าง layer ใน image Docker อ่าน Dockerfile จากบนลงล่างและ execute คำสั่งทีละบรรทัด การเขียน Dockerfile ที่ดีทำให้ image มีขนาดเล็กและ build เร็ว ลด deployment time</p><p>Dockerfile instructions หลัก: FROM กำหนด base image, RUN execute command ระหว่าง build, COPY/ADD copy files เข้า image, ENV ตั้งค่า environment variables, WORKDIR ตั้ง working directory, EXPOSE ระบุ port, CMD/ENTRYPOINT กำหนด default command เมื่อ container start</p><h2>Python Dockerfile ขั้นพื้นฐาน</h2><p>ตัวอย่าง Python Dockerfile: FROM python:3.11-slim ใช้ slim variant เพื่อลดขนาด image ลง 60-70%, WORKDIR /app, COPY requirements.txt . เพื่อ leverage layer caching, RUN pip install --no-cache-dir -r requirements.txt, COPY . ., CMD ["python", "main.py"]</p><p>การใช้ .dockerignore ลดขนาด image: สร้างไฟล์ .dockerignore ที่ list files ที่ไม่ต้องการ copy เช่น .git, __pycache__, *.pyc, .env, tests/, docs/ ทำให้ build context เล็กลง build เร็วขึ้น และ image ไม่มี sensitive files</p><h2>Airflow Dockerfile</h2><p>สำหรับ Airflow ขยาย official image: FROM apache/airflow:2.8.0-python3.11, USER root สำหรับ system packages, RUN apt-get update && apt-get install -y build-essential สำหรับ native dependencies, USER airflow กลับมา, RUN pip install providers ที่ต้องการ, COPY dags/ และ plugins/ เข้า image</p><p>Best practice สำหรับ Airflow Docker image: pin exact Airflow version เสมอเพื่อ reproducibility, ติดตั้ง providers ที่ต้องการ (Google, AWS, Snowflake) เพิ่มเติม, แยก requirements.txt เป็น airflow-requirements.txt และ dbt-requirements.txt ตาม concerns</p><h2>Multi-stage Build</h2><p>Multi-stage build ช่วยลดขนาด final image: stage 1 (builder) ติดตั้ง dependencies ทั้งหมดรวม build tools เช่น gcc, stage 2 (final) copy เฉพาะ artifacts ที่ต้องการจาก builder stage ทำให้ production image ไม่มี unnecessary tools ลด attack surface และ size</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ docker build --no-cache เมื่อต้องการ force rebuild ทุก layer เช่น เมื่อ base image อัปเดต security patches ปกติ Docker cache layers ทำให้ rebuild เร็ว แต่บางครั้งต้องการ fresh build เพื่อได้ latest security fixes</div><p>Layer caching optimization: วาง instructions ที่เปลี่ยนบ่อยไว้ท้าย Dockerfile เพราะเมื่อ layer หนึ่งเปลี่ยน layers ถัดจากนั้นทั้งหมดต้อง rebuild การ copy requirements.txt ก่อน source code ทำให้ pip install ถูก cache ไว้ เร็วขึ้นมากในการ iterate</p>'
  },
  {
    number: 3,
    slug: 'docker-compose',
    emoji: '🎼',
    title: 'Docker Compose สำหรับหลาย Services',
    content: '<h2>Docker Compose คืออะไร?</h2><p>Docker Compose คือ tool สำหรับ define และ run multi-container Docker applications ใช้ YAML file (docker-compose.yml) ในการกำหนด services, networks, และ volumes ของ application รัน docker compose up เพื่อ start ทุก services พร้อมกันด้วย command เดียว</p><p>ตัวอย่าง data stack ที่ใช้ Compose: Airflow (webserver + scheduler + worker), Postgres (metadata database), Redis (Celery broker), MinIO (S3-compatible storage) ทั้งหมด define ใน docker-compose.yml เดียว ทำให้ทีมทุกคน setup environment ได้เหมือนกัน</p><h2>โครงสร้าง docker-compose.yml</h2><p>version กำหนด Compose file version, services section define แต่ละ service ด้วย: image หรือ build context, ports mapping, environment variables, volumes, depends_on สำหรับ service dependencies, healthcheck สำหรับ readiness, networks สำหรับ networking isolation</p><p>ตัวอย่าง Postgres service: image: postgres:15, environment กำหนด POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, volumes mount สำหรับ persistent storage, healthcheck ด้วย pg_isready command เพื่อ ensure Postgres ready ก่อน dependent services start</p><h2>Environment Variables ใน Compose</h2><p>ใช้ .env file กับ Compose: สร้างไฟล์ .env ใน folder เดียวกับ docker-compose.yml Compose จะ auto-load environment variables ใช้ dollar brace syntax ใน yaml file ป้องกัน hardcode credentials ใน yaml ที่อาจถูก commit ขึ้น Git</p><p>env_file directive: สามารถ specify env files เฉพาะสำหรับแต่ละ service เช่น env_file: - airflow.env - secrets.env ช่วยจัดการ variables ที่แตกต่างกันระหว่าง services และ environments ได้อย่าง clean</p><h2>Compose Commands</h2><p>docker compose up -d: start ใน detached background mode, docker compose down: stop และ remove containers แต่เก็บ volumes, docker compose down -v: remove volumes ด้วย (ข้อมูลหาย), docker compose logs -f service_name: ดู logs แบบ realtime, docker compose exec service_name bash: เข้า container shell</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ profiles ใน Compose เพื่อ group services ที่ optional เช่น monitoring profile สำหรับ Prometheus/Grafana รัน docker compose --profile monitoring up เมื่อต้องการ monitoring tools เท่านั้น ประหยัด resources ใน development</div><p>Compose override: สร้าง docker-compose.override.yml สำหรับ local development overrides เช่น mount source code เป็น volume เพื่อ hot reload Compose จะ auto-merge กับ docker-compose.yml หลัก ทำให้ production config สะอาดและ dev config flexible</p>'
  },
  {
    number: 4,
    slug: 'airflow-docker',
    emoji: '✈️',
    title: 'รัน Apache Airflow บน Docker',
    content: '<h2>Official Airflow Docker Setup</h2><p>Apache Airflow มี official docker-compose.yml ที่ดาวน์โหลดได้จาก documentation ด้วย curl command แล้ว customize ตามต้องการ setup นี้รวม webserver, scheduler, worker, triggerer, postgres, redis ครบ พร้อมตั้งค่า CeleryExecutor สำหรับ parallel task execution</p><p>ก่อนรัน Airflow บน Docker ต้องสร้าง directories สำหรับ dags, logs, plugins, config และตั้ง AIRFLOW_UID environment variable ใน .env file จากนั้นรัน docker compose up airflow-init เพื่อ initialize database และสร้าง admin user ครั้งแรก</p><h2>Custom DAGs บน Docker</h2><p>Mount DAGs folder เป็น volume ใน Compose เมื่อ save DAG file ใน ./dags บน host machine Airflow จะ detect ใน container อัตโนมัติ สะดวกมากสำหรับ development เพราะไม่ต้อง rebuild image ทุกครั้งที่แก้ DAG code</p><p>Airflow environment variables สำคัญ: AIRFLOW__CORE__EXECUTOR กำหนด executor type, AIRFLOW__DATABASE__SQL_ALCHEMY_CONN กำหนด database connection, AIRFLOW__CELERY__BROKER_URL สำหรับ Celery executor, AIRFLOW__WEBSERVER__SECRET_KEY สำหรับ session security</p><h2>Production Airflow Docker</h2><p>สำหรับ production ควร: สร้าง custom image แทน official เพื่อ pre-install providers, ใช้ external managed Postgres (RDS, Cloud SQL) แทน Postgres container, ใช้ external Redis (ElastiCache, Memorystore), implement health checks สำหรับทุก service, ใช้ Docker secrets หรือ Vault สำหรับ credentials</p><p>Resource limits: กำหนด memory และ CPU limits สำหรับแต่ละ service ป้องกัน OOM kills และ CPU starvation ที่จะทำให้ Airflow scheduler หยุดทำงาน โดยเฉพาะ worker containers ที่รัน heavy data processing tasks</p><h2>Logging สำหรับ Airflow Docker</h2><p>Default Airflow logs เก็บใน container filesystem ซึ่งหายเมื่อ container หยุด ต้อง configure remote logging เช่น GCS หรือ S3 เพื่อ persist logs ข้าม container restarts ทำให้ debug ปัญหาใน production ได้แม้ว่า container จะถูก restart ไปแล้ว</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ Flower UI สำหรับ monitor Celery workers บน Docker รัน docker compose --profile flower up เพื่อ enable Flower ดู task queue, worker status, และ task history ได้แบบ real-time ช่วย debug พอ tasks ค้างหรือ fail</div><p>Scaling workers: เพิ่ม worker instances ด้วย docker compose up --scale airflow-worker=3 ใช้สำหรับ handle peak loads เช่น เวลา backfill หลาย dates พร้อมกัน สำหรับ production จริงควร migrate ไป Kubernetes ที่ auto-scale ได้ตาม demand</p>'
  },
  {
    number: 5,
    slug: 'networking',
    emoji: '🌐',
    title: 'Docker Networking สำหรับ DE',
    content: '<h2>Docker Networks พื้นฐาน</h2><p>Docker มี network drivers หลายแบบ: bridge (default สำหรับ standalone containers บน host เดียว), host (share network กับ host โดยตรง), overlay (สำหรับ Docker Swarm/K8s multi-host), none (no networking สำหรับ isolated tasks) สำหรับ DE workloads มักใช้ bridge ใน development</p><p>Default bridge network: containers สื่อสารกันผ่าน IP addresses แต่ไม่ resolve ด้วย container names Custom bridge network: containers สื่อสารกันด้วย container names ได้โดยตรง ซึ่งสะดวกมากกว่า แนะนำให้สร้าง custom network เสมอใน production setups</p><h2>Compose Networking</h2><p>Docker Compose สร้าง default network ให้ทุก services อัตโนมัติ ทำให้ services communicate กันด้วย service name เช่น Airflow สามารถ connect ไป Postgres ด้วย hostname postgres ตรงกับ service name ใน Compose ทำให้ config portable ข้าม environments</p><p>สร้าง custom networks ใน Compose เพื่อ isolate services กลุ่มต่างๆ เช่น frontend network กับ backend network แยกกัน การกำหนด static subnets ช่วยเมื่อต้องการ predictable IP ranges สำหรับ firewall rules หรือ VPN configurations</p><h2>Port Mapping</h2><p>Format: HOST_PORT:CONTAINER_PORT เช่น 8080:8080 map port 8080 บน host ไปยัง port 8080 ใน container ระวัง port conflicts เมื่อรัน services หลายตัว เช่น Airflow UI: 8080, Flower: 5555, MinIO: 9000, Grafana: 3000 ต้องใช้ host ports ที่ไม่ซ้ำกัน</p><p>ใช้ 127.0.0.1 prefix สำหรับ local-only access: 127.0.0.1:5432:5432 ทำให้ Postgres accessible เฉพาะ localhost ไม่ expose สู่ network ภายนอก เพิ่ม security สำหรับ development environment ที่ไม่ควร expose database สู่ internet</p><h2>Service Discovery</h2><p>ใน Compose environment: Airflow webserver connect ไป postgres ด้วย connection string postgresql://airflow:airflow@postgres:5432/airflow ชื่อ postgres คือ service name ใน Compose ไม่ใช่ IP address ทำให้ config portable และไม่ต้องแก้ IP เมื่อ restart</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ docker network inspect network_name เพื่อดู IP addresses และ connected containers ช่วย debug networking issues ได้ เมื่อ containers connect กันไม่ได้ ตรวจสอบว่าอยู่ใน network เดียวกันหรือไม่</div><p>Network troubleshooting: รัน docker exec -it container_name ping other_container เพื่อ test connectivity, ใช้ nslookup service_name เพื่อ test DNS resolution, ตรวจสอบว่า containers อยู่ใน network เดียวกัน และ firewall rules ไม่ block traffic</p>'
  },
  {
    number: 6,
    slug: 'volumes',
    emoji: '💾',
    title: 'Volumes และ Persistent Data',
    content: '<h2>ทำไม Volumes ถึงสำคัญ?</h2><p>Container filesystem เป็น ephemeral หมายความว่าเมื่อ container ถูก remove ข้อมูลทั้งหมดหายไป สำหรับ DE workloads ที่มี stateful services เช่น databases, message queues, object storage ต้องใช้ Volumes เพื่อ persist data ข้าม container restarts</p><p>Docker มี 3 วิธี mount data: Volumes (managed โดย Docker เอง), Bind mounts (map path บน host ตรงๆ), tmpfs mounts (in-memory ชั่วคราว) แต่ละแบบมี use case ต่างกัน Volumes เหมาะสำหรับ production data, Bind mounts เหมาะสำหรับ development</p><h2>Named Volumes</h2><p>สร้าง named volume: docker volume create my_data_volume ใช้ใน Compose โดย declare ใน volumes section ระดับบนสุดและ reference ใน service volumes Named volumes จัดการโดย Docker เก็บใน /var/lib/docker/volumes/ บน Linux ทำให้ data survive container removal</p><p>ดู volumes: docker volume ls, ดูรายละเอียด: docker volume inspect volume_name, ลบ unused volumes: docker volume prune ระวัง: docker volume rm ลบถาวร ข้อมูลหายไม่ได้คืน backup volumes เสมอก่อนลบใน production environment</p><h2>Bind Mounts สำหรับ Development</h2><p>Bind mount map folder บน host ไปยัง path ใน container เมื่อ edit files บน host, container เห็น changes ทันที เหมาะมากสำหรับ development workflow ที่ต้องการ hot reload เช่น mount DAGs folder ใน Airflow development</p><p>ข้อควรระวัง bind mounts: file permissions อาจ mismatch ระหว่าง host user และ container user ทำให้ permission denied errors, performance บน Docker Desktop (Mac/Windows) ช้ากว่า Linux เพราะ file system translation layer</p><h2>Volume Backup และ Restore</h2><p>Backup volume โดย run temporary container ที่ mount volume และ tar ออกมา: สร้าง backup.tar.gz จาก /data directory ใน volume Restore โดย untar กลับเข้าไป สำหรับ Postgres ใช้ pg_dump/pg_restore แทนที่จะ backup volume โดยตรง เพราะ consistent กว่า</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> สำหรับ production ใช้ external managed storage แทน Docker volumes เช่น AWS EBS สำหรับ Postgres, GCS/S3 สำหรับ Airflow logs, NFS สำหรับ shared DAGs ระหว่าง multiple workers ทำให้ HA และ disaster recovery ง่ายขึ้นมาก</div><p>Volume plugins: Docker รองรับ third-party volume plugins สำหรับ enterprise storage สามารถ use shared storage ข้าม multiple Docker hosts ได้ เหมาะสำหรับ clustered deployments ที่ต้องการ data sharing ระหว่าง nodes</p>'
  },
  {
    number: 7,
    slug: 'registry',
    emoji: '📋',
    title: 'Docker Registry และ Docker Hub',
    content: '<h2>Docker Registry คืออะไร?</h2><p>Docker Registry คือ service สำหรับ store และ distribute Docker images Docker Hub เป็น public registry หลัก มี images สำหรับ software ยอดนิยมเกือบทุกตัว organization สามารถมี private registries ของตัวเองเช่น AWS ECR, Google Artifact Registry, GitHub Container Registry</p><p>Image naming convention: registry/username/repository:tag เช่น docker.io/apache/airflow:2.8.0 หรือ gcr.io/myproject/my-image:v1.2.3 ถ้าไม่ระบุ registry จะ default เป็น Docker Hub ถ้าไม่ระบุ tag จะ default เป็น latest ซึ่งไม่แนะนำ สำหรับ production</p><h2>Push Image ไป Registry</h2><p>Process: Build image, Tag สำหรับ registry โดยใช้ docker tag, Login ด้วย docker login, Push ด้วย docker push สำหรับ private registry เปลี่ยน username เป็น registry URL เช่น 123456789.dkr.ecr.ap-southeast-1.amazonaws.com สำหรับ AWS ECR</p><p>Semantic versioning สำหรับ images: ใช้ version tags แบบ v1.2.3 แทน latest สำหรับ production, tag ด้วย git commit hash เพื่อ traceability เช่น myimage:abc1234, maintain latest tag สำหรับ convenience แต่ deployment ต้องใช้ specific version เสมอ</p><h2>Private Registry สำหรับ DE Teams</h2><p>AWS ECR: integrate กับ IAM ทำให้ manage permissions ผ่าน IAM policies, scan images หา vulnerabilities อัตโนมัติ, replicate ข้าม regions สำหรับ multi-region deployments Google Artifact Registry: ใช้กับ GKE ง่าย, multi-format รองรับ Docker, npm, Maven GitHub Container Registry (ghcr.io): integrate กับ GitHub Actions ได้ seamless</p><p>Harbor: self-hosted registry ที่ feature-rich มี vulnerability scanning, role-based access control, replication policies เหมาะสำหรับองค์กรที่ต้องการ full control และ compliance requirements ที่ไม่อนุญาตให้ใช้ cloud services</p><h2>Image Security</h2><p>Scan images ก่อน deploy: ใช้ docker scout, Snyk, หรือ Trivy เพื่อ scan vulnerabilities ใน base images และ dependencies ตั้ง CI pipeline ให้ block deployment ถ้ามี critical vulnerabilities ป้องกัน known security issues ไม่ให้เข้า production</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ Docker Content Trust (DCT) สำหรับ sign images โดย export DOCKER_CONTENT_TRUST=1 ก่อน pull/push ทำให้ verify ได้ว่า image ไม่ถูก tamper ระหว่าง push กับ pull เหมาะสำหรับ security-conscious organizations</div><p>Image layering strategy: organize Dockerfile layers จาก stable ไป unstable (base OS → system packages → app dependencies → app code) ทำให้ layers ที่เปลี่ยนบ่อยอยู่บนสุดและ cache hit rate สูงขึ้น ลด build time และ registry storage significantly</p>'
  },
  {
    number: 8,
    slug: 'k8s-intro',
    emoji: '☸️',
    title: 'Kubernetes เบื้องต้นสำหรับ DE',
    content: '<h2>ทำไมต้องเรียน Kubernetes?</h2><p>Kubernetes (K8s) คือ container orchestration platform ที่ manage containers ใน scale ถ้า Docker Compose เหมาะสำหรับ development และ small deployments K8s เหมาะสำหรับ production workloads ที่ต้องการ high availability, auto-scaling, และ self-healing capabilities</p><p>DE ใช้ K8s สำหรับ: รัน Airflow KubernetesExecutor ที่ spawn worker pods dynamically, รัน Spark on K8s แทน YARN, deploy data APIs แบบ scalable, manage ML model serving, สร้าง data processing pipelines ที่ scale อัตโนมัติตาม demand</p><h2>K8s Concepts พื้นฐาน</h2><p>Pod: unit เล็กที่สุดใน K8s contain 1+ containers ที่ share network และ storage, Deployment: manage Pod replicas และ rolling updates, Service: expose Pods ผ่าน stable network endpoint, ConfigMap/Secret: manage configuration และ sensitive data, PersistentVolumeClaim: request storage สำหรับ Pods</p><p>Namespace: virtual cluster ใน K8s cluster ใช้แบ่ง environment (dev, staging, prod) หรือ teams ใน cluster เดียว Resource limits ใน K8s: กำหนด CPU requests/limits และ memory requests/limits สำหรับแต่ละ container เพื่อ fair resource sharing</p><h2>Airflow บน K8s</h2><p>Airflow KubernetesExecutor: แต่ละ task รันเป็น independent Pod ใน K8s cluster ข้อดีคือ isolation สมบูรณ์, scale to zero ไม่มี idle workers, resource isolation ต่อ task ข้อเสียคือ overhead ของการสร้าง Pod ทุกครั้ง (20-30 วินาที) ทำให้ไม่เหมาะกับ short tasks</p><p>ติดตั้ง Airflow บน K8s ด้วย Helm chart: เพิ่ม apache-airflow helm repo, install chart ใน namespace airflow พร้อม custom values.yaml ที่กำหนด executor, database connection, และ resource limits ตาม workload requirements</p><h2>K8s สำหรับ Data Workloads</h2><p>Spark on K8s: ใช้ K8s เป็น resource manager แทน YARN driver submit jobs ไปยัง K8s API ซึ่ง spawn executor pods อัตโนมัติ integrate กับ managed services เช่น AWS EMR on EKS, Google Dataproc on GKE ได้ ทำให้ mix managed และ self-managed Spark ได้</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> เริ่มต้น K8s ด้วย minikube หรือ kind (K8s in Docker) บน local machine ก่อน ทั้งสองตัว setup ง่ายในไม่กี่นาที เหมาะสำหรับ learning และ testing โดยไม่ต้องใช้ cloud resources และไม่มี cost</div><p>K8s resource monitoring: ใช้ kubectl top pods และ kubectl top nodes เพื่อดู resource usage, ติดตั้ง Metrics Server สำหรับ HPA (Horizontal Pod Autoscaler), ใช้ Prometheus + Grafana สำหรับ comprehensive monitoring และ alerting</p>'
  },
  {
    number: 9,
    slug: 'de-stack',
    emoji: '🏗️',
    title: 'Complete DE Stack บน Docker',
    content: '<h2>Modern DE Stack คืออะไร?</h2><p>Modern DE Stack บน Docker ประกอบด้วย components หลายตัวที่ทำงานร่วมกัน: Airflow สำหรับ orchestration, dbt สำหรับ transformation, Postgres สำหรับ metadata, MinIO สำหรับ object storage, Spark สำหรับ processing, Trino สำหรับ query engine รวมกันเป็น end-to-end data platform</p><p>การ setup ทุก components พร้อมกันใน docker-compose.yml เดียวช่วยให้ทีมมี consistent development environment ทุกคนรัน stack เดียวกันและ reproduce bugs ได้ง่าย ลด "it works on my machine" problems ที่เกิดจาก environment differences</p><h2>Architecture ของ DE Stack</h2><p>Layer 1 - Storage: MinIO เป็น S3-compatible object storage สำหรับ raw data, Postgres สำหรับ relational data Layer 2 - Processing: Spark cluster สำหรับ distributed processing, dbt สำหรับ SQL transforms Layer 3 - Orchestration: Airflow orchestrate ทุกอย่าง Layer 4 - Query: Trino หรือ DuckDB สำหรับ ad-hoc queries Layer 5 - Monitoring: Prometheus + Grafana</p><p>ทั้งหมดนี้รันได้บน laptop ที่มี RAM 16GB+ สำหรับ development และ testing ก่อน deploy ขึ้น cloud ช่วยประหยัด cloud cost ระหว่าง development phase และทำให้ development loop เร็วขึ้นมาก</p><h2>Data Flow ใน DE Stack</h2><p>Typical data flow: External API/Database → Airflow DAG trigger → Python script extract data → save ไปยัง MinIO raw bucket → Spark job read จาก MinIO → transform และ save ไปยัง MinIO processed bucket → dbt read จาก processed → create analytics tables ใน Postgres → Trino query สำหรับ BI tools</p><p>Airflow orchestrate ทุก steps ตาม schedule มี retry logic, SLA monitoring, และ alerting เมื่อ jobs fail integration ระหว่าง Airflow กับ Spark ผ่าน SparkSubmitOperator หรือ HTTP calls ไปยัง Spark REST API</p><h2>Configuration Management</h2><p>สร้าง .env files แยกตาม environment: .env.local, .env.staging, .env.prod ใช้ env_file directive ใน Compose เพื่อ load ตาม environment Variable ที่สำคัญ: database passwords, API keys, bucket names, external service endpoints ทั้งหมดนี้ไม่ควร hardcode</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ Makefile เพื่อ wrap docker compose commands ทำให้ทีม run stack ได้ง่ายด้วย make up, make down, make logs, make shell แทนที่จะจำ docker compose commands ยาวๆ ลด onboarding time ของ new team members</div><p>Health checks สำคัญมากใน DE stack เพราะ services มี dependencies เช่น Airflow ต้องรอ Postgres ready ก่อน ตั้ง healthcheck และ depends_on condition service_healthy ใน Compose เพื่อ ensure startup order ถูกต้องและไม่เกิด race conditions</p>'
  },
  {
    number: 10,
    slug: 'best-practices',
    emoji: '⭐',
    title: 'Docker Best Practices สำหรับ Production',
    content: '<h2>Security Best Practices</h2><p>ไม่รัน containers เป็น root: เพิ่ม USER directive ใน Dockerfile เพื่อ switch ไป non-root user ก่อน CMD ลด attack surface ถ้า container ถูก compromise Scan images สม่ำเสมอ: ใช้ Trivy, Snyk, หรือ AWS ECR scanning ตั้ง CI pipeline ให้ fail เมื่อพบ critical CVEs</p><p>ใช้ read-only filesystem: --read-only flag หรือ readOnlyRootFilesystem ใน K8s ทำให้ container เขียน filesystem ไม่ได้ ยกเว้น tmpfs หรือ volumes ที่ระบุไว้เท่านั้น ป้องกัน malware ที่พยายาม modify container filesystem เพื่อ persistence</p><h2>Performance Optimization</h2><p>ลดขนาด images: ใช้ alpine หรือ slim base images, multi-stage builds, .dockerignore ที่ครอบคลุม image เล็กลง deploy เร็วขึ้น pull เร็วขึ้น ประหยัด storage ใน registry และลด attack surface เพราะมี packages น้อยกว่า</p><p>Resource limits: กำหนด CPU และ memory limits ทุก container ใน production ป้องกัน noisy neighbor problem ที่ container หนึ่งกิน resources จน impact containers อื่น ใช้ memory limit และ cpus option ใน Compose หรือ resources spec ใน K8s</p><h2>Logging และ Observability</h2><p>Centralized logging: ส่ง container logs ไปยัง centralized system เช่น ELK Stack, Loki, หรือ CloudWatch ใช้ log drivers ใน Docker เช่น json-file, fluentd, awslogs หลีกเลี่ยง log rotation ที่ไม่ตั้งค่า เพิ่ม max-size และ max-file options</p><p>Metrics: รัน cAdvisor เพื่อ export container metrics ไปยัง Prometheus ดู CPU, memory, network, disk I/O ต่อ container ตั้ง alerts ใน Grafana สำหรับ containers ที่ใช้ memory เกิน threshold ป้องกัน OOM kills ที่ไม่คาดคิด</p><h2>Immutable Infrastructure</h2><p>ไม่ SSH เข้า production containers เพื่อแก้ไขปัญหา ให้ rebuild image ใหม่แทน ใช้ GitOps: ทุก change ต้องผ่าน Git → CI/CD → deploy ใหม่ ไม่มี manual changes บน production ทำให้ environment reproducible และ auditable สำหรับ compliance</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ Docker Buildx สำหรับ multi-platform builds: docker buildx build --platform linux/amd64,linux/arm64 เพื่อ support ทั้ง Intel/AMD servers และ ARM (AWS Graviton, Apple Silicon) ลด cost บน Graviton ได้ 20-40%</div><p>Container lifecycle management: ตั้ง restart policies ที่เหมาะสม: restart: unless-stopped สำหรับ long-running services, restart: on-failure:3 สำหรับ jobs ที่ควร retry เมื่อ fail แต่ไม่ควร restart ตลอดเวลา cleanup containers และ images เก่าสม่ำเสมอด้วย docker system prune</p>'
  }
];
