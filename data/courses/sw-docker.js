export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '🐳',
    title: 'Overview - ทำความรู้จักกับ Docker',
    content: '<h2>ทำความรู้จักกับ Containerization</h2><p>ก่อนที่เราจะเริ่มใช้งาน Docker เราจำเป็นต้องเข้าใจแนวคิดของ <strong>Containerization</strong> กันก่อน ในยุคอดีต การนำแอปพลิเคชันไปรันบน Server มักจะพบปัญหาคลาสสิกที่ว่า "มันรันได้บนเครื่องฉัน แต่มันพังบนเครื่อง Production!" (It works on my machine) ซึ่งปัญหานี้เกิดจาก Environment หรือสภาพแวดล้อมของเครื่องที่แตกต่างกัน เช่น เวอร์ชันของ OS, Library, หรือ Dependencies ต่างๆ ไม่ตรงกัน</p><p>Containerization คือเทคโนโลยีที่เข้ามาแก้ปัญหานี้ โดยการบรรจุ (Package) แอปพลิเคชันและสิ่งที่จำเป็นต้องใช้ทั้งหมด (Dependencies, Libraries, Configuration files) ไว้ในกล่องจำลองที่เรียกว่า <strong>Container</strong> ซึ่ง Container นี้สามารถนำไปรันที่ไหนก็ได้ ไม่ว่าจะเป็นเครื่อง Local ของนักพัฒนา, Test Server, หรือ Production Server โดยรับประกันได้ว่ามันจะทำงานได้ผลลัพธ์เหมือนกันทุกที่ เหมือนกับการแพ็คสินค้าใส่ตู้คอนเทนเนอร์ที่สามารถยกไปวางบนเรือ รถบรรทุก หรือรถไฟได้โดยไม่ต้องจัดเรียงใหม่</p><h2>Docker vs Virtual Machine (VM)</h2><p>หลายคนอาจจะสงสัยว่า Container มันต่างจาก Virtual Machine (VM) อย่างไร ในเมื่อมันเป็นการจำลองสภาพแวดล้อมเหมือนกัน? คำตอบคือความแตกต่างในระดับสถาปัตยกรรม (Architecture) ของทั้งสองเทคโนโลยี</p><ul><li><strong>Virtual Machine (VM):</strong> การทำ VM คือการจำลองคอมพิวเตอร์ทั้งเครื่อง โดยใช้ซอฟต์แวร์ที่เรียกว่า Hypervisor มาแบ่งทรัพยากร (CPU, RAM) จากเครื่อง Host แต่ละ VM จะต้องมีระบบปฏิบัติการ (Guest OS) เป็นของตัวเองทั้งหมด (เช่น Windows, Ubuntu) ซึ่งทำให้ VM มีขนาดใหญ่ กินทรัพยากรมาก และใช้เวลาบูตระบบนาน (เป็นนาที)</li><li><strong>Docker (Container):</strong> Container ใช้เทคโนโลยี OS-level virtualization โดยแชร์ Kernel (แกนกลางของระบบปฏิบัติการ) ร่วมกับเครื่อง Host (เครื่องหลัก) ไม่ต้องลง OS ใหม่ซ้ำซ้อน ทำให้ Container มีขนาดเล็กมาก (ระดับ Megabytes) กินทรัพยากรน้อย และที่สำคัญคือสามารถเปิดใช้งานได้ภายในไม่กี่วินาที (หรือเสี้ยววินาที)</li></ul><p>ด้วยความที่ Docker เบาและรวดเร็วกว่า VM มาก ทำให้ใน Server 1 เครื่อง เราอาจจะรัน VM ได้แค่ 5-10 ตัว แต่สามารถรัน Container ได้เป็นร้อยหรือพันตัว!</p><h2>ทำไม Docker ถึงเปลี่ยนโลกของ DevOps?</h2><p>การมาถึงของ Docker ทำให้วงการ Software Development และ DevOps เปลี่ยนแปลงไปอย่างสิ้นเชิง โดยมีเหตุผลหลักดังนี้:</p><ol style="padding-left:20px"><li><strong>Consistency (ความสม่ำเสมอ):</strong> หมดปัญหา "It works on my machine" เพราะ Developer, QA, และ System Admin ใช้ Container Image ตัวเดียวกันตั้งแต่ขั้นตอน Dev จนถึง Production</li><li><strong>Microservices Architecture:</strong> Docker ทำให้แนวคิด Microservices เกิดขึ้นได้จริงและบริหารจัดการง่ายขึ้น การแบ่งระบบใหญ่ๆ ออกเป็น Service ย่อยๆ แล้วรันแต่ละ Service ใน Container ของตัวเอง ทำให้ระบบยืดหยุ่น (Scalable) และอัปเดตหรือแก้ไขได้โดยไม่ต้องหยุดการทำงานของระบบทั้งหมด</li><li><strong>CI/CD Integration:</strong> Docker เข้ากันได้ดีเยี่ยมกับระบบ Continuous Integration / Continuous Deployment การสร้าง (Build), ทดสอบ (Test), และนำขึ้นระบบ (Deploy) สามารถทำเป็นอัตโนมัติได้อย่างรวดเร็วและปลอดภัย</li><li><strong>Resource Efficiency:</strong> การใช้ทรัพยากรคุ้มค่ามากขึ้น ลดต้นทุนทางด้าน Infrastructure ลงอย่างมหาศาล เพราะไม่ต้องเสีย Overhead ไปกับ Guest OS หลายๆ ตัว</li></ol><div class="tip-box"><strong>💡 Pro Tip:</strong> ถึงแม้ว่า Docker จะแชร์ Kernel กับ Host แต่ก็ยังมีกลไกเรื่อง Isolation (Namespace และ Cgroups) ที่แยก Process, Network, และ File System ออกจากกัน เพื่อความปลอดภัยและการบริหารทรัพยากร ทำให้แอปพลิเคชันที่รันใน Container A จะมองไม่เห็น หรือกวนการทำงานของแอปพลิเคชันใน Container B</div><h2>ระบบนิเวศของ Docker (Docker Ecosystem)</h2><p>Docker ไม่ใช่แค่โปรแกรมเดียวโดดๆ แต่มีองค์ประกอบที่ทำงานร่วมกันหลายส่วน:</p><ul><li><strong>Docker Daemon (dockerd):</strong> เป็นโปรแกรมพื้นหลัง (Background Service) ที่ทำงานบน Host คอยจัดการสร้าง รัน และดูแล Containers</li><li><strong>Docker Client (CLI):</strong> คือคำสั่ง (Command-line) ที่เราใช้พิมพ์เพื่อสั่งงาน Docker Daemon เช่น <pre>docker run</pre>, <pre>docker build</pre> โดย Client และ Daemon สื่อสารกันผ่าน REST API</li><li><strong>Docker Image:</strong> เป็นเหมือนพิมพ์เขียว (Blueprint) ที่อ่านได้อย่างเดียว (Read-only) เป็นตัวกำหนดว่าแอปพลิเคชันของเรามีหน้าตาเป็นอย่างไร มี Library อะไรบ้าง</li><li><strong>Docker Container:</strong> คือ Image ที่ถูกนำมา "รัน" หรือทำงานจริง เป็น Instance ของ Image</li><li><strong>Docker Registry (Docker Hub):</strong> เป็นเหมือน App Store หรือคลังเก็บ Docker Images ที่เราสามารถดาวน์โหลด Image มาตรฐาน (เช่น Ubuntu, Nginx, Node.js) มาใช้งาน หรืออัปโหลด Image ของเราเองไปเก็บไว้</li></ul><p>ในบทถัดๆ ไป เราจะมาเรียนรู้วิธีการติดตั้งและลงมือใช้งาน Docker อย่างจริงจังกันครับ</p>'
  },
  {
    number: 1,
    slug: 'basics',
    emoji: '📚',
    title: 'Docker Basics',
    content: '<h2>การติดตั้ง Docker Desktop</h2><p>ก่อนที่เราจะเริ่มใช้งานคำสั่งต่างๆ ได้ เราต้องติดตั้งเครื่องมือลงบนคอมพิวเตอร์ของเราก่อน วิธีที่ง่ายและนิยมที่สุดสำหรับฝั่ง Development คือการใช้ <strong>Docker Desktop</strong> ซึ่งเป็นโปรแกรมสำเร็จรูปที่รวมเอาเครื่องมือที่จำเป็นทั้งหมด (Docker Engine, CLI, Docker Compose และ Kubernetes) ไว้ในตัวเดียว</p><ol style="padding-left:20px"><li>ไปที่เว็บไซต์ <pre>docs.docker.com/get-docker</pre></li><li>เลือกดาวน์โหลดเวอร์ชันตามระบบปฏิบัติการที่คุณใช้งาน (Windows, macOS, หรือ Linux)</li><li>สำหรับ Windows: แนะนำให้เปิดใช้งาน <strong>WSL 2</strong> (Windows Subsystem for Linux 2) เป็น Backend เพื่อให้ Docker ทำงานได้เร็วและกินทรัพยากรน้อยลง</li><li>สำหรับ macOS: รองรับทั้งชิป Intel และ Apple Silicon (M1/M2/M3)</li><li>หลังจากติดตั้งและรีสตาร์ทเครื่อง ให้เปิดโปรแกรม Docker Desktop ขึ้นมา สังเกตที่ไอคอนรูปปลาวาฬที่ System Tray จะต้องเป็นสีเขียว หรือขึ้นสถานะ "Engine running"</li></ol><p>ตรวจสอบการติดตั้งโดยเปิด Terminal (หรือ Command Prompt) แล้วพิมพ์คำสั่ง:</p><pre>docker --version</pre><p>หากแสดงเวอร์ชันของ Docker ออกมา แปลว่าพร้อมลุยแล้ว!</p><h2>ความแตกต่างระหว่าง Images และ Containers</h2><p>แนวคิดที่สำคัญที่สุดสองอย่างใน Docker คือ <strong>Image</strong> และ <strong>Container</strong> หากไม่เข้าใจสองสิ่งนี้ จะทำให้การเรียนรู้ Docker เป็นเรื่องยากมาก ลองเปรียบเทียบง่ายๆ แบบนี้ครับ:</p><ul><li><strong>Image</strong> เปรียบเสมือน "สูตรทำเค้ก" หรือ "แม่พิมพ์" (Class ใน OOP) เป็นสิ่งที่สร้างขึ้นมาแล้วไม่สามารถแก้ไขได้ (Read-only template) ภายในจะประกอบด้วยโค้ดของแอปพลิเคชัน, Runtime (เช่น Node, Python), Libraries, และ Configuration</li><li><strong>Container</strong> เปรียบเสมือน "ก้อนเค้ก" ที่อบเสร็จแล้ว (Object หรือ Instance) เป็นหน่วยที่ทำงานจริง (Running instance) ซึ่งถูกสร้างมาจาก Image เราสามารถสร้าง Container หลายๆ ตัวจาก Image ตัวเดียวกันได้ และแต่ละ Container สามารถเขียนไฟล์ชั่วคราว มีการทำงาน (Process) เป็นเอกเทศ</li></ul><h2>คำสั่งพื้นฐาน (Docker CLI Basics)</h2><p>คำสั่งของ Docker มีเยอะมาก แต่ในชีวิตประจำวัน เรามักจะใช้เพียงไม่กี่คำสั่งหลักๆ ดังต่อไปนี้</p><h3>1. การดึง Image มาไว้ในเครื่อง (docker pull)</h3><p>หากเราต้องการใช้ Nginx Web Server เราสามารถดาวน์โหลด Image มาจาก Docker Hub ได้ด้วยคำสั่ง:</p><pre>docker pull nginx:latest</pre><p>คำว่า <pre>latest</pre> คือ Tag ที่ระบุเวอร์ชัน หากไม่ระบุ Docker จะถือว่าดึงเวอร์ชันล่าสุดเสมอ</p><h3>2. การดูรายการ Image ในเครื่อง (docker images)</h3><p>อยากรู้ว่าตอนนี้เรามี Image อะไรเก็บไว้บ้าง พิมพ์:</p><pre>docker images</pre><p>คุณจะเห็นตารางแสดง REPOSITORY, TAG, IMAGE ID, CREATED (เวลาที่ Image นี้ถูกสร้างขึ้นบน Docker Hub ไม่ใช่เวลาที่เราโหลดมา), และ SIZE (ขนาดไฟล์)</p><h3>3. การรัน Container (docker run)</h3><p>นี่คือคำสั่งพระเอกของ Docker ใช้สำหรับสร้างและรัน Container จาก Image</p><pre>docker run -d -p 8080:80 --name my-web nginx</pre><p>อธิบาย Option ทีละตัว:</p><ul><li><pre>-d</pre> (Detach): ให้ Container รันเป็น Background ทำให้เราใช้ Terminal ต่อได้</li><li><pre>-p 8080:80</pre> (Publish): ทำ Port Mapping หรือส่งต่อทราฟฟิกจาก Port 8080 ของเครื่องคอมพิวเตอร์เรา (Host) ไปยัง Port 80 ของ Container</li><li><pre>--name my-web</pre>: ตั้งชื่อ Container ว่า my-web (หากไม่ใส่ Docker จะสุ่มชื่อตลกๆ มาให้ เช่น <pre>laughing_lovelace</pre>)</li><li><pre>nginx</pre>: ชื่อ Image ที่ต้องการใช้</li></ul><div class="tip-box"><strong>💡 Pro Tip:</strong> หลังจากรันคำสั่งนี้ ลองเปิด Browser แล้วเข้า <pre>http://localhost:8080</pre> จะพบกับหน้า Welcome to nginx! นี่คือพลังของ Docker ที่ทำให้เราเซ็ตอัป Web Server ได้ใน 1 วินาที โดยไม่ต้องติดตั้ง Nginx ลงในเครื่องคอมพิวเตอร์จริงๆ เลย</div><h3>4. การดู Container ที่กำลังทำงานอยู่ (docker ps)</h3><pre>docker ps</pre><p>จะแสดงตาราง Container ที่สถานะเป็น UP หากต้องการดู Container ทั้งหมด (รวมถึงตัวที่หยุดทำงานไปแล้ว) ให้เพิ่มธง <pre>-a</pre> (All) เข้าไป:</p><pre>docker ps -a</pre><h3>5. การหยุดและการลบ Container (docker stop & docker rm)</h3><p>หากต้องการหยุด Container ที่รันอยู่ ให้ใช้ ID (เอาตัวอักษร 3-4 ตัวแรกพอ) หรือชื่อของ Container:</p><pre>docker stop my-web</pre><p>และเมื่อหยุดแล้ว Container จะอยู่ในสถานะ Exited มันยังคงกินพื้นที่ในดิสก์อยู่ หากต้องการลบทิ้งให้ใช้:</p><pre>docker rm my-web</pre><p>หาก Container กำลังรันอยู่แล้วเราอยากลบทิ้งเลยแบบบังคับ (Force) สามารถใช้ <pre>docker rm -f my-web</pre></p><h3>6. การลบ Image (docker rmi)</h3><p>หากต้องการลบ Image ที่ไม่ได้ใช้แล้ว เพื่อคืนพื้นที่ดิสก์ ใช้คำสั่ง (i ย่อมาจาก Image):</p><pre>docker rmi nginx:latest</pre><p>ระวัง: คุณไม่สามารถลบ Image ได้ หากยังมี Container (แม้จะหยุดทำงานไปแล้ว) ที่สร้างมาจาก Image นี้อยู่ ต้องลบ Container ก่อนครับ</p>'
  },
  {
    number: 2,
    slug: 'dockerfile',
    emoji: '📝',
    title: 'Dockerfile',
    content: '<h2>Dockerfile คืออะไร?</h2><p>ในบทที่แล้ว เราใช้ Image สำเร็จรูปที่คนอื่นทำไว้ แต่ในการทำงานจริง เราต้องแพ็ค Source Code แอปพลิเคชันของเราเองให้กลายเป็น Image สิ่งที่จะช่วยเราในขั้นตอนนี้คือ <strong>Dockerfile</strong></p><p>Dockerfile เป็นไฟล์ Text ธรรมดา (ไม่มีนามสกุลไฟล์) ที่เก็บชุดคำสั่งเป็นบรรทัดๆ (Instructions) เพื่อบอก Docker ว่าจะต้องทำอะไรบ้างทีละสเต็ปในการสร้าง Image ขึ้นมา เปรียบเสมือน Recipe หรือสูตรอาหารนั่นเอง</p><h2>คำสั่งหลักใน Dockerfile ที่ต้องรู้</h2><p>Dockerfile มีคำสั่ง (Instruction) พื้นฐานไม่กี่ตัว หากเข้าใจคำสั่งเหล่านี้ คุณก็สามารถเขียน Dockerfile สำหรับแอปพลิเคชัน 90% ในโลกนี้ได้แล้ว</p><ul><li><strong>FROM:</strong> เป็นคำสั่งบรรทัดแรกเสมอ! ใช้บอกว่าเราจะต่อยอด (Base Image) จากระบบอะไร เช่น <pre>FROM node:18-alpine</pre> หรือ <pre>FROM python:3.9</pre></li><li><strong>WORKDIR:</strong> เป็นการกำหนดโฟลเดอร์หรือ Directory เริ่มต้น (Working directory) ภายใน Container คำสั่งอื่นๆ ถัดจากนี้ (เช่น RUN, CMD, COPY) จะทำงานในโฟลเดอร์นี้ทั้งหมด เหมือนเราพิมพ์คำสั่ง <pre>cd</pre> เข้าไปนั่นเอง</li><li><strong>COPY:</strong> ใช้สำหรับคัดลอกไฟล์จากเครื่องเรา (Host) เข้าไปไว้ใน Image เช่น <pre>COPY package.json .</pre> หรือ <pre>COPY . .</pre> เพื่อก๊อปปี้ทุกอย่าง</li><li><strong>ADD:</strong> คล้ายกับ COPY แต่มีความสามารถพิเศษคือ สามารถแตกไฟล์ .tar อัตโนมัติ หรือโหลดไฟล์จาก URL ได้ (แต่ Best Practice แนะนำให้ใช้ COPY มากกว่าเพื่อความชัดเจน ยกเว้นจำเป็นจริงๆ)</li><li><strong>RUN:</strong> เป็นการรันคำสั่งบน Shell ระหว่างกระบวนการ "Build Image" มักใช้สำหรับลง Library หรือติดตั้ง Dependencies เช่น <pre>RUN npm install</pre> หรือ <pre>RUN apt-get update</pre></li><li><strong>ENV:</strong> ใช้สำหรับกำหนด Environment Variables ให้กับ Container เช่น <pre>ENV PORT=8080</pre></li><li><strong>EXPOSE:</strong> เป็นการ Document หรือแจ้งให้ทราบว่า Container นี้จะให้บริการบนพอร์ตไหน (แต่ไม่ได้เป็นการ Publish Port ออกมาจริงๆ การ Publish จะต้องทำตอนรันผ่าน <pre>-p</pre>) เช่น <pre>EXPOSE 8080</pre></li><li><strong>CMD:</strong> เป็นคำสั่งเริ่มต้น (Default command) ที่จะถูกรันตอนที่ Container เริ่มทำงาน (ตอนรัน <pre>docker run</pre>) ใน 1 Dockerfile จะมี CMD ได้แค่บรรทัดเดียว (หากมีหลายอัน อันสุดท้ายจะทับอันอื่น) เช่น <pre>CMD ["npm", "start"]</pre></li><li><strong>ENTRYPOINT:</strong> คล้ายกับ CMD มาก แต่บังคับให้ Container ทำงานเป็นโปรแกรมนั้นๆ เสมอ (ไม่สามารถ Override ผ่าน Command line ได้ง่ายๆ แบบ CMD) มักใช้คู่กับ CMD เพื่อกำหนดพารามิเตอร์เริ่มต้น</li></ul><h2>ลองเขียน Dockerfile สำหรับ Node.js App</h2><p>มาดูตัวอย่างการนำคำสั่งเหล่านี้มาประกอบร่างกัน สมมติว่าเรามีโปรเจค Express.js ง่ายๆ:</p><pre># 1. เลือก Base image ที่เล็กและเบา\nFROM node:18-alpine\n\n# 2. กำหนด Working directory\nWORKDIR /app\n\n# 3. คัดลอกเฉพาะ package.json มาก่อน เพื่อทำ Caching\nCOPY package.json package-lock.json ./\n\n# 4. ติดตั้ง dependencies\nRUN npm install\n\n# 5. คัดลอก Source Code ทั้งหมด\nCOPY . .\n\n# 6. แจ้งว่าแอปนี้รันบนพอร์ต 3000\nEXPOSE 3000\n\n# 7. คำสั่งเมื่อ Container สตาร์ท\nCMD ["npm", "run", "dev"]</pre><h2>การ Build Image (docker build)</h2><p>เมื่อเขียนไฟล์ Dockerfile เสร็จแล้ว (เซฟในชื่อ <pre>Dockerfile</pre> ภายในโปรเจค) เราจะสั่งให้ Docker ทำการอ่านไฟล์นี้แล้วสร้าง Image ออกมาด้วยคำสั่ง:</p><pre>docker build -t my-node-app:v1 .</pre><ul><li><pre>-t</pre> (Tag): เป็นการตั้งชื่อและระบุแท็กให้ Image ถ้าไม่ใส่ <pre>:v1</pre> มันจะเป็น <pre>:latest</pre></li><li><pre>.</pre> (จุด): สำคัญมาก! เป็นการระบุ Build Context หรือตำแหน่งปัจจุบันที่มี Dockerfile และโค้ดของเราอยู่</li></ul><p>Docker จะทำงานทีละบรรทัดจากบนลงล่าง และแต่ละบรรทัดจะถูกแปลงไปเป็นสิ่งที่เรียกว่า <strong>Layer</strong></p><h2>แนวคิดเรื่อง Layers (Image Layers)</h2><p>ความชาญฉลาดของ Docker คือระบบ <strong>Layer Caching</strong> ภาพรวมคือ Image ไม่ได้เป็นไฟล์ก้อนเดียวใหญ่ๆ แต่เกิดจากการซ้อนทับกันของเลเยอร์ต่างๆ แต่ละบรรทัดใน Dockerfile (เช่น RUN, COPY) จะสร้าง Layer ใหม่ 1 ชั้น (Read-only layer)</p><p>หากเราสั่งรัน <pre>docker build</pre> ซ้ำอีกครั้ง Docker จะเช็คว่ามีอะไรเปลี่ยนแปลงในบรรทัดนั้นไหม หากไม่มีการเปลี่ยนแปลง มันจะดึง Layer เดิมจาก Cache มาใช้ทันที ทำให้ Build เร็วขึ้นมาก (ขึ้น <pre>CACHED</pre> บนหน้าจอ)</p><div class="tip-box"><strong>💡 Pro Tip (ทำไมต้อง COPY package.json ก่อน?):</strong> สังเกตในตัวอย่าง เรา <pre>COPY package.json</pre> แยกมาก่อน แล้วสั่ง <pre>RUN npm install</pre> จากนั้นค่อย <pre>COPY . .</pre> (ก๊อปปี้โค้ด) เหตุผลเพราะว่า โค้ด (เช่น server.js) มักจะถูกแก้บ่อยมาก หากเราก๊อปปี้มาทั้งหมดในบรรทัดเดียว Layer นั้นจะหลุด Cache ตลอด ทำให้ต้อง <pre>npm install</pre> ใหม่ทุกครั้งที่แก้โค้ด! การแยกบรรทัดทำให้ Cache ของ <pre>npm install</pre> ถูกใช้งานต่อไป ตราบใดที่ package.json ไม่มีการเปลี่ยนแปลง นี่คือเทคนิคการปรับแต่ง Image ระดับโปรครับ</div><h2>ไฟล์ .dockerignore</h2><p>เวลาเราสั่ง <pre>COPY . .</pre> บางครั้งเราอาจจะเผลอก๊อปปี้โฟลเดอร์ที่ไม่จำเป็นเข้าไปใน Image ด้วย เช่น <pre>node_modules/</pre>, โฟลเดอร์ <pre>.git/</pre>, หรือไฟล์ <pre>.env</pre> ที่เก็บรหัสผ่าน</p><p>เพื่อป้องกันปัญหานี้ เราจะสร้างไฟล์ที่ชื่อว่า <pre>.dockerignore</pre> วางคู่กับ Dockerfile มีการทำงานเหมือน <pre>.gitignore</pre> เลยครับ ตัวอย่างเนื้อหาด้านใน:</p><pre>node_modules\nnpm-debug.log\n.git\n.env\nDockerfile\n.dockerignore</pre><p>วิธีนี้จะช่วยให้ Image มีขนาดเล็กลง Build เร็วขึ้น และปลอดภัยจากการหลุดของข้อมูลสำคัญครับ</p>'
  },
  {
    number: 3,
    slug: 'images',
    emoji: '🖼️',
    title: 'Docker Images',
    content: '<h2>การจัดการ Docker Images อย่างมืออาชีพ</h2><p>ในโลกของการทำ Containerization <strong>Image</strong> คือหัวใจสำคัญ หาก Image มีขนาดใหญ่หรือมีช่องโหว่ จะทำให้ระบบทำงานช้าและเสี่ยงต่อการถูกโจมตี ในบทนี้เราจะมาเจาะลึกวิธีการจัดการ Image ให้มีประสิทธิภาพเทียบเท่ากับระดับองค์กรใช้งานกันครับ</p><h2>Official Images vs Unofficial Images</h2><p>เมื่อคุณเข้าไปค้นหา Image บน <strong>Docker Hub</strong> (hub.docker.com) คุณจะพบกับ Image นับล้านตัว แต่จุดสังเกตสำคัญคือป้ายกำกับ (Badge) ที่เขียนว่า <strong>Docker Official Image</strong> หรือ <strong>Verified Publisher</strong></p><ul><li><strong>Official Images:</strong> คือ Image ที่ดูแลโดยทีมงาน Docker ร่วมกับเจ้าของเทคโนโลยีนั้นๆ (เช่น Node.js, Python, Nginx) ซึ่งผ่านการตรวจสอบความปลอดภัย มีการอัปเดตสม่ำเสมอ และมีการเขียน Document ไว้ชัดเจน แนะนำให้ใช้ตัวนี้เป็น Base Image เสมอ</li><li><strong>Unofficial Images:</strong> คือ Image ที่ผู้ใช้งานทั่วไปอัปโหลดขึ้นไป อาจจะเป็นของใครก็ไม่รู้ที่สร้างไว้ หากนำมาใช้อาจเสี่ยงต่อการถูกฝังมัลแวร์ หรือไม่มีการอัปเดตแพตช์รักษาความปลอดภัย</li></ul><h2>การ Tagging และ Pushing Image</h2><p>หลังจากที่เราสร้าง Image ด้วย <pre>docker build</pre> เสร็จแล้ว เรามักจะต้องส่ง Image นี้ไปเก็บไว้ที่ส่วนกลาง (Registry) เพื่อให้นำไป Deploy ต่อได้</p><p><strong>1. การ Tag Image:</strong> Tag คือการตั้งชื่อและเวอร์ชัน หากเราจะส่งขึ้น Docker Hub เราต้องตั้งชื่อให้ตรงกับรูปแบบ <pre>&lt;username&gt;/&lt;image-name&gt;:&lt;tag&gt;</pre> เช่น:</p><pre>docker tag my-node-app:v1 johndoe/my-node-app:v1</pre><p><strong>2. การ Login:</strong> ก่อนส่ง เราต้องล็อกอินเข้า Docker Hub ก่อน (ต้องสมัครสมาชิกที่เว็บก่อน)</p><pre>docker login</pre><p><strong>3. การ Push Image:</strong> สั่งอัปโหลด Image ขึ้นสู่ Docker Hub</p><pre>docker push johndoe/my-node-app:v1</pre><div class="tip-box"><strong>💡 Pro Tip:</strong> นอกจาก Docker Hub แบบ Public แล้ว องค์กรส่วนใหญ่มักจะใช้ <strong>Private Registry</strong> เพื่อเก็บ Image ที่เป็นความลับของบริษัท เช่น AWS ECR, Google Artifact Registry, หรือ GitHub Container Registry (GHCR) ซึ่งวิธี push ก็จะคล้ายๆ กัน แค่เปลี่ยน username เป็น URL ของ Registry นั้นๆ</div><h2>Multi-stage Builds (ความลับในการลดขนาด Image)</h2><p>ปัญหาที่พบบ่อยในการสร้าง Image คือ ขนาดที่ใหญ่เกินไป (เช่น 1-2 GB) เพราะมันรวมเอาเครื่องมือที่ใช้ตอน Build (เช่น Compiler, Development Libraries) เข้ามาด้วย ทั้งที่ตอนรันจริง (Production) เราต้องการแค่โค้ดที่คอมไพล์เสร็จแล้ว</p><p><strong>Multi-stage builds</strong> คือเทคนิคในการเขียน Dockerfile 1 ไฟล์ให้มีหลาย <pre>FROM</pre> โดยเราสามารถใช้ State แรกในการ Build ข้อมูล และคัดลอกเฉพาะ "ผลลัพธ์สุดท้าย" ไปยัง State ที่สองที่เป็น Base Image ขนาดเล็กได้</p><p>ตัวอย่างสำหรับแอป Golang หรือ React (สมมติว่าเป็น React ที่ต้อง Build ออกมาเป็นไฟล์ Static HTML):</p><pre># Stage 1: Build Stage\nFROM node:18-alpine AS builder\nWORKDIR /app\nCOPY package*.json ./\nRUN npm install\nCOPY . .\nRUN npm run build\n\n# Stage 2: Production Stage (ใช้ Nginx ที่เบาหวิว)\nFROM nginx:alpine\n# ก๊อปปี้เฉพาะโฟลเดอร์ build จาก Stage 1 มาที่ Nginx\nCOPY --from=builder /app/build /usr/share/nginx/html\nEXPOSE 80\nCMD ["nginx", "-g", "daemon off;"]</pre><p>ด้วยเทคนิคนี้ Image ที่ได้จะมีแค่ Nginx และไฟล์ HTML เท่านั้น ลดขนาดจาก 1GB+ ลงมาเหลือไม่ถึง 50MB ได้อย่างมหัศจรรย์!</p><h2>Image Optimization & Best Practices</h2><ol style="padding-left:20px"><li><strong>ใช้ Alpine Linux:</strong> พยายามใช้ Base Image ที่มีแท็ก <pre>-alpine</pre> (เช่น <pre>node:18-alpine</pre>) เพราะเป็นลีนุกซ์เวอร์ชันย่อส่วนที่มีขนาดเพียง ~5MB เทียบกับ <pre>-buster</pre> หรือ <pre>-ubuntu</pre> ที่อาจใหญ่ถึง 100MB+</li><li><strong>รวมคำสั่ง RUN:</strong> การใช้ <pre>RUN</pre> หลายบรรทัด จะสร้าง Layer หลายชั้น หากเป็นคำสั่งที่เกี่ยวข้องกัน ควรเชื่อมด้วย <pre>&&</pre> เพื่อลดจำนวน Layer เช่น <pre>RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*</pre></li><li><strong>ลบ Cache หลังจากติดตั้งแพ็กเกจ:</strong> สังเกตจากข้อที่แล้ว การรันคำสั่ง <pre>rm -rf ...</pre> เพื่อลบไฟล์ temporary ทันที จะช่วยไม่ให้มีขยะติดอยู่ใน Image Layer นั้นๆ</li><li><strong>จัดเรียงคำสั่งให้ถูกต้อง:</strong> เอาคำสั่งที่เปลี่ยนแปลงน้อย (เช่น COPY package.json) ไว้ข้างบน และสิ่งที่แก้บ่อย (เช่น COPY . .) ไว้ข้างล่าง เพื่อใช้ประโยชน์จาก Layer Caching ให้ได้มากที่สุด</li></ol>'
  },
  {
    number: 4,
    slug: 'networking',
    emoji: '🌐',
    title: 'Docker Networking',
    content: '<h2>เข้าใจระบบ Network ของ Docker</h2><p>เมื่อคุณรัน Container ขึ้นมา มันจะไม่ได้เชื่อมต่อกับโลกภายนอกหรือ Container ตัวอื่นๆ โดยอัตโนมัติ Docker มีระบบ Networking ของตัวเองที่ทำหน้าที่เป็น "สวิตช์" (Virtual Switch) คอยจัดการเส้นทางการจราจรของข้อมูล เรามาทำความรู้จักกับ Network Driver หลักๆ กันครับ</p><h2>Network Drivers ที่สำคัญ</h2><ul><li><strong>Bridge (Default):</strong> เมื่อคุณรัน Container โดยไม่ระบุ Network มันจะถูกจับเข้าไปอยู่ใน Network ที่ชื่อว่า <pre>bridge</pre> เป็นค่าเริ่มต้น Container ที่อยู่ใน Bridge เดียวกันสามารถคุยกันได้ผ่าน IP Address (แต่คุยผ่านชื่อไม่ได้) เหมาะสำหรับการใช้งานแบบ Standalone</li><li><strong>Host:</strong> หากคุณระบุ <pre>--network host</pre> Container จะไม่ได้รับ IP ของตัวเอง แต่จะไปแชร์ระบบ Network ของเครื่อง Host แบบ 100% เลย นั่นแปลว่าถ้าแอปใน Container รันที่พอร์ต 80 พอร์ต 80 ของเครื่อง Host จะถูกใช้งานทันที ไม่ต้องทำ Port Mapping (วิธีนี้รองรับเฉพาะบน Linux เท่านั้น)</li><li><strong>None:</strong> ปิดการเชื่อมต่อเครือข่ายทุกชนิดของ Container นั้นๆ (ตัดขาดจากโลกภายนอก) ใช้สำหรับงานประมวลผลที่ต้องการความปลอดภัยสูงสุด</li></ul><h2>การสื่อสารระหว่าง Container (Container Communication)</h2><p>ในระบบที่ใช้งานจริง เรามักจะมี Container มากกว่า 1 ตัว เช่น มี Web Server 1 ตัว และ Database อีก 1 ตัว ปัญหาคือ หากเราใช้ Default Bridge เราจะต้องเรียกหากันผ่าน IP Address ซึ่ง IP ของ Container มักจะเปลี่ยนไปเรื่อยๆ ทุกครั้งที่เราลบและรันใหม่</p><p>วิธีแก้ปัญหาที่ถูกต้องคือ การสร้าง <strong>Custom Bridge Network</strong> (User-defined network) ขึ้นมาเอง</p><h3>การสร้างและใช้งาน Custom Network</h3><p><strong>1. สร้าง Network ใหม่:</strong></p><pre>docker network create my-net</pre><p><strong>2. รัน Database (MongoDB) ให้อยู่ใน Network นี้:</strong></p><pre>docker run -d --name my-db --network my-net mongo</pre><p><strong>3. รัน Web App ให้อยู่ใน Network เดียวกัน:</strong></p><pre>docker run -d --name my-web --network my-net -p 8080:80 my-node-app</pre><div class="tip-box"><strong>💡 Pro Tip: ระบบ DNS ภายใน (Embedded DNS):</strong> ข้อดีที่สุดของการสร้าง Custom Network คือ Docker จะเปิดระบบ DNS ภายในให้โดยอัตโนมัติ นั่นหมายความว่า Web App (my-web) สามารถต่อฐานข้อมูลโดยระบุ Hostname เป็น <pre>my-db</pre> (ซึ่งเป็นชื่อของ Container) ได้เลย! เช่น Connection String จะเป็น <pre>mongodb://my-db:27017</pre> ไม่ต้องจำ IP อีกต่อไป และแม้ IP จะเปลี่ยน ระบบ DNS ก็จะอัปเดตให้เอง</div><h2>Port Mapping (Publishing Ports)</h2><p>ในบทก่อนๆ เราได้เห็นการใช้ <code>-p</code> กันมาบ้างแล้ว การทำ Port mapping คือการเจาะกำแพงให้คนนอก (หรือเครื่อง Host) สามารถเข้ามาใช้งานแอปพลิเคชันที่อยู่ใน Container ได้</p><p>รูปแบบคือ <code>-p &lt;host-port&gt;:&lt;container-port&gt;</code></p><ul><li><pre>-p 8080:80</pre> หมายถึง รับ Request จากพอร์ต 8080 ของคอมพิวเตอร์เรา แล้วโยนเข้าไปที่พอร์ต 80 ของ Container</li><li><pre>-p 3000:3000</pre> หมายถึง พอร์ตตรงกันทั้งในและนอก (มักใช้ตอน Dev)</li></ul><p>ข้อควรระวัง: เราไม่ควร Map Port ของ Database (เช่น MySQL 3306 หรือ Postgres 5432) ออกมายัง Host Port แบบ Public บน Production (เพราะเสี่ยงโดนแฮ็ก) ควรปล่อยให้ Container ของ Database คุยกับ Container ของ Web App ภายใน Docker Network ก็เพียงพอแล้ว</p>'
  },
  {
    number: 5,
    slug: 'volumes',
    emoji: '📁',
    title: 'Docker Volumes',
    content: '<h2>ทำไมต้องมี Docker Volumes?</h2><p>ตามธรรมชาติของ Container นั้น มันถูกออกแบบมาให้เป็นสิ่งชั่วคราว (Ephemeral) หรือ <strong>Stateless</strong> หมายความว่า ไฟล์ใดๆ ก็ตามที่ถูกสร้างหรือแก้ไขในขณะที่ Container กำลังรันอยู่ จะหายวับไปทันทีเมื่อ Container นั้นถูกลบทิ้ง (ถูกทำลายไปพร้อมกับ Container layer)</p><p>ลองจินตนาการว่าคุณรัน Database (เช่น MySQL) ใน Container มีคนสมัครสมาชิกเข้ามา 100 คน ข้อมูลถูกเซฟไว้เรียบร้อย แต่พอคุณอัปเดตเวอร์ชัน Database โดยการลบ Container เก่าแล้วสร้างใหม่... บู้ม! ข้อมูลผู้ใช้งานทั้ง 100 คนหายเกลี้ยง!</p><p>เพื่อแก้ปัญหานี้ เราจึงต้องใช้ <strong>Volumes</strong> ซึ่งเป็นกลไกในการ "นำพื้นที่เก็บข้อมูล (Storage) จากภายนอก (Host) มาเชื่อมต่อ (Mount) เข้ากับโฟลเดอร์ภายใน Container"</p><h2>ประเภทของการ Mount Data</h2><p>Docker มีวิธีกำหนดพื้นที่เก็บข้อมูล 3 รูปแบบหลักๆ ที่ต้องจำให้ดีครับ:</p><h3>1. Volumes (แนะนำที่สุด)</h3><p>เป็นการให้ Docker สร้างโฟลเดอร์ขึ้นมาบริหารจัดการเอง (ปกติจะอยู่ที่ <code>/var/lib/docker/volumes/</code> บน Linux) เราไม่จำเป็นต้องรู้ว่าไฟล์อยู่ตรงไหนบนเครื่องเรา ข้อดีคือมันถูกจัดการโดย Docker 100% ทำให้ปลอดภัย ย้ายเครื่องง่าย และทำงานบน Windows/Mac ได้ลื่นไหลกว่า</p><p><strong>การสร้างและใช้งาน Volume:</strong></p><pre>docker volume create my-db-data</pre><p>เวลารัน Container เราจะใช้ flag <code>-v</code> หรือ <code>--mount</code> เช่น:</p><pre>docker run -d --name mysql-db -e MYSQL_ROOT_PASSWORD=1234 -v my-db-data:/var/lib/mysql mysql</pre><p>คำอธิบาย: <code>my-db-data</code> (ชื่อ Volume บนเครื่องเรา) <code>:</code> (เชื่อมต่อกับ) <code>/var/lib/mysql</code> (โฟลเดอร์เก็บข้อมูลของ MySQL ภายใน Container)</p><h3>2. Bind Mounts</h3><p>ต่างจาก Volumes ตรงที่เราเป็นคนกำหนดเส้นทาง (Path) ของโฟลเดอร์บนเครื่อง Host เองแบบเจาะจงเลย เช่น นำโฟลเดอร์โค้ดบน Desktop ของเรา เข้าไปใส่ใน Container</p><p>วิธินี้ได้รับความนิยมมากใน <strong>Environment สำหรับ Development</strong> เพราะเมื่อเราแก้ไขโค้ด (เช่นเซฟไฟล์ .js หรือ .py ใน VS Code) ไฟล์ใน Container ก็จะเปลี่ยนตามทันที ทำให้เกิดการ Hot-reload โดยไม่ต้อง Build Image ใหม่ทุกครั้ง</p><p><strong>การใช้งาน Bind Mount:</strong></p><pre>docker run -d -p 3000:3000 -v $(pwd)/src:/app/src my-node-app</pre><p>คำอธิบาย: <code>$(pwd)/src</code> (โฟลเดอร์ src ในตำแหน่งปัจจุบัน) เชื่อมต่อกับ <code>/app/src</code> ภายใน Container</p><div class="tip-box"><strong>💡 Pro Tip:</strong> อย่าใช้ Bind Mount บน Production สำหรับเก็บข้อมูล Database (เช่น ชี้ไปที่ /path/to/host/folder:/var/lib/mysql) เพราะการจัดการสิทธิ์การอ่านเขียนไฟล์ (File Permissions) ระหว่าง Host OS กับ Container มักจะสร้างปัญหาชวนปวดหัว ให้ใช้ Volumes (ข้อ 1) เสมอครับ</div><h3>3. tmpfs Mounts</h3><p>เป็นการเขียนไฟล์ลงใน "หน่วยความจำ (RAM)" ของเครื่อง Host แทนที่จะเขียนลง Disk จริงๆ เมื่อ Container หยุดทำงาน ข้อมูลนี้ก็จะหายไป (เหมาะสำหรับเก็บไฟล์ชั่วคราว ข้อมูลที่เป็นความลับ หรือแคชที่ต้องการความเร็วสูงมากๆ)</p><h2>คำสั่งจัดการ Volumes เบื้องต้น</h2><ul><li><code>docker volume ls</code>: แสดงรายชื่อ Volumes ทั้งหมดที่มีในเครื่อง</li><li><code>docker volume inspect &lt;volume_name&gt;</code>: ดูรายละเอียดว่า Volume นี้เก็บไฟล์ไว้ที่พาธไหนบนเครื่อง Host (Mountpoint)</li><li><code>docker volume rm &lt;volume_name&gt;</code>: ลบ Volume ทิ้ง (จะทำได้ก็ต่อเมื่อไม่มี Container ไหนใช้งานอยู่)</li><li><code>docker volume prune</code>: ลบ Volumes ทั้งหมดที่ไม่ได้ถูกใช้งานโดย Container ใดๆ (ระวัง! ใช้คำสั่งนี้ข้อมูลอาจปลิวหายวับแบบกู้ไม่ได้)</li></ul>'
  },
  {
    number: 6,
    slug: 'compose',
    emoji: '📦',
    title: 'Docker Compose',
    content: '<h2>Docker Compose คืออะไร?</h2><p>จากบทที่ผ่านๆ มา เราเรียนรู้วิธีการรัน Container ทีละตัวด้วยคำสั่ง <code>docker run</code> แต่ในแอปพลิเคชันจริงๆ มักจะไม่ได้มีแค่ Container เดียว สมมติว่าระบบของคุณประกอบด้วย Web App (React), API (Node.js), และ Database (PostgreSQL) การมานั่งพิมพ์คำสั่ง <code>docker run</code> ยาวๆ สามรอบ แถมต้องคอยจำว่าต้องสร้าง Network หรือ Mount Volume ตอนไหนบ้าง คงไม่ใช่เรื่องสนุกแน่ๆ</p><p><strong>Docker Compose</strong> คือเครื่องมือที่เกิดมาเพื่อแก้ปัญหานี้ โดยมันจะช่วยให้เราเขียน Configuration ของระบบที่มีหลายๆ Container (Multi-container applications) ให้อยู่ในไฟล์เดียวที่ชื่อว่า <code>docker-compose.yml</code> (ใช้ฟอร์แมต YAML) แล้วสั่งรันทั้งหมดพร้อมกันด้วยคำสั่งเดียว!</p><h2>โครงสร้างของ docker-compose.yml</h2><p>ไฟล์ <code>docker-compose.yml</code> มีส่วนประกอบหลัก (Top-level elements) ดังนี้:</p><ul><li><strong>version:</strong> (ปัจจุบันไม่จำเป็นต้องใส่แล้วใน Compose V2 แต่บางคนยังใส่อยู่เพื่อความเข้ากันได้)</li><li><strong>services:</strong> หัวใจหลักของไฟล์ เป็นการกำหนดว่าแอปพลิเคชันของเรามีส่วนประกอบ (Container) อะไรบ้าง</li><li><strong>networks:</strong> กำหนด Custom network (ถ้าไม่กำหนด Compose จะสร้าง Default network ให้เอง)</li><li><strong>volumes:</strong> กำหนด Named volumes ที่จะใช้ร่วมกันใน services</li></ul><p>ลองดูตัวอย่างไฟล์ Compose สำหรับรัน WordPress คู่กับ MySQL:</p><pre>services:\n  db:\n    image: mysql:8.0\n    restart: always\n    environment:\n      MYSQL_ROOT_PASSWORD: rootpassword\n      MYSQL_DATABASE: wordpress\n    volumes:\n      - db_data:/var/lib/mysql\n\n  wordpress:\n    depends_on:\n      - db\n    image: wordpress:latest\n    ports:\n      - "8000:80"\n    environment:\n      WORDPRESS_DB_HOST: db:3306\n      WORDPRESS_DB_USER: root\n      WORDPRESS_DB_PASSWORD: rootpassword\n\nvolumes:\n  db_data:</pre><h2>เจาะลึกคำสั่งใน Services</h2><ul><li><code>image:</code> ระบุ Image ที่จะใช้</li><li><code>build:</code> หากไม่มี Image ให้บอกพาธที่เก็บ Dockerfile เพื่อสั่ง Build ใหม่</li><li><code>ports:</code> การทำ Port mapping เหมือนกับ <code>-p</code> เช่น <code>"8000:80"</code> (ฝั่งซ้าย Host, ฝั่งขวา Container)</li><li><code>environment:</code> การส่ง Environment Variables เหมือนกับ <code>-e</code></li><li><code>volumes:</code> การเชื่อมต่อ Volume เหมือนกับ <code>-v</code></li><li><code>restart: always</code> สั่งให้ Container สตาร์ทตัวเองอัตโนมัติหากเกิดข้อผิดพลาด หรือเปิดเครื่องใหม่</li><li><code>depends_on:</code> ควบคุมลำดับการสตาร์ท เช่น ให้ <code>wordpress</code> รอให้ <code>db</code> สตาร์ทเสร็จก่อน ค่อยเริ่มทำงาน</li></ul><h2>การจัดการตัวแปรด้วยไฟล์ .env</h2><p>เราไม่ควรเก็บรหัสผ่านไว้ในไฟล์ <code>docker-compose.yml</code> ตรงๆ (เพราะมักจะถูกเผลอ push ขึ้น Git) วิธีที่ถูกต้องคือการสร้างไฟล์ <code>.env</code> ไว้โฟลเดอร์เดียวกัน แล้วอ้างอิงตัวแปรด้วยสัญลักษณ์ <code>${VAR_NAME}</code></p><p><strong>ไฟล์ .env</strong></p><pre>DB_PASS=mysecretpassword</pre><p><strong>ไฟล์ docker-compose.yml</strong></p><pre>environment:\n  MYSQL_ROOT_PASSWORD: ${DB_PASS}</pre><h2>คำสั่งใช้งาน Docker Compose พื้นฐาน</h2><ul><li><code>docker compose up -d</code> : สตาร์ทระบบทั้งหมดแบบ Background (-d) คำสั่งเดียวจบ!</li><li><code>docker compose ps</code> : ดูสถานะของ Container ในโปรเจคนี้</li><li><code>docker compose logs -f</code> : ดู Log รวมของทุก Container แบบเรียลไทม์</li><li><code>docker compose down</code> : ปิดและลบ Container, Network ทิ้ง (แต่ Volume ไม่ถูกลบ ข้อมูลยังอยู่)</li></ul>'
  },
  {
    number: 7,
    slug: 'fullstack',
    emoji: '🏗️',
    title: 'ตั้ง Full Stack ด้วย Docker Compose',
    content: '<h2>โปรเจคจำลอง: Full Stack Web Application</h2><p>เพื่อเห็นภาพรวมทั้งหมด เราจะมาลองเซ็ตอัปโปรเจค Full Stack ที่ประกอบไปด้วยเทคโนโลยีที่ได้รับความนิยมในปัจจุบัน 5 ส่วนด้วยกัน ได้แก่:</p><ol style="padding-left:20px"><li><strong>Frontend:</strong> Next.js (React Framework) รันบนพอร์ต 3000</li><li><strong>Backend API:</strong> FastAPI (Python) รันบนพอร์ต 8000</li><li><strong>Database:</strong> PostgreSQL สำหรับเก็บข้อมูลหลัก</li><li><strong>Cache:</strong> Redis สำหรับทำ Caching ให้ระบบเร็วขึ้น</li><li><strong>Reverse Proxy:</strong> Nginx ทำหน้าที่เป็นยามเฝ้าประตู รับ Request จากพอร์ต 80 แล้วกระจายไปยัง Frontend หรือ Backend</li></ol><h2>โครงสร้างโปรเจค (Directory Structure)</h2><p>การจัดวางไฟล์มักจะแบ่งโฟลเดอร์ตาม Service:</p><pre>my-fullstack-app/\n├── frontend/\n│   ├── Dockerfile\n│   └── (Next.js source code...)\n├── backend/\n│   ├── Dockerfile\n│   └── (FastAPI source code...)\n├── nginx/\n│   └── default.conf\n├── .env\n└── docker-compose.yml</pre><h2>ไฟล์ Nginx Configuration (nginx/default.conf)</h2><p>เราจะตั้งค่าให้ Nginx ส่ง Request ที่ขึ้นต้นด้วย <code>/api</code> ไปที่ Backend และ Request อื่นๆ ไปที่ Frontend</p><pre>server {\n    listen 80;\n\n    location /api/ {\n        proxy_pass http://api:8000/;\n    }\n\n    location / {\n        proxy_pass http://frontend:3000;\n    }\n}</pre><h2>ประกอบร่างด้วย docker-compose.yml</h2><p>นี่คือไฟล์พระเอกของเรา ที่จะทำหน้าที่ผูกทุกอย่างเข้าด้วยกัน:</p><pre>services:\n  # 1. Reverse Proxy\n  nginx:\n    image: nginx:alpine\n    ports:\n      - "80:80"\n    volumes:\n      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf\n    depends_on:\n      - frontend\n      - api\n\n  # 2. Frontend (Next.js)\n  frontend:\n    build: ./frontend\n    environment:\n      - NEXT_PUBLIC_API_URL=/api\n    # ใช้ depends_on ในโหมดพัฒนาอาจจะไม่จำเป็น แต่ใน production ช่วยป้องกัน error\n\n  # 3. Backend (FastAPI)\n  api:\n    build: ./backend\n    environment:\n      - DATABASE_URL=postgresql://${DB_USER}:${DB_PASS}@postgres_db:5432/${DB_NAME}\n      - REDIS_URL=redis://redis_cache:6379\n    depends_on:\n      postgres_db:\n        condition: service_healthy # รอให้ DB รันจนสมบูรณ์ก่อน\n      redis_cache:\n        condition: service_started\n\n  # 4. Database (PostgreSQL)\n  postgres_db:\n    image: postgres:15-alpine\n    environment:\n      POSTGRES_USER: ${DB_USER}\n      POSTGRES_PASSWORD: ${DB_PASS}\n      POSTGRES_DB: ${DB_NAME}\n    volumes:\n      - pg_data:/var/lib/postgresql/data\n    healthcheck:\n      test: ["CMD-SHELL", "pg_isready -U ${DB_USER}"]\n      interval: 10s\n      timeout: 5s\n      retries: 5\n\n  # 5. Cache (Redis)\n  redis_cache:\n    image: redis:alpine\n\nvolumes:\n  pg_data:</pre><h2>จุดที่น่าสนใจในโค้ดนี้ (Best Practices)</h2><ul><li><strong>Healthcheck:</strong> ใน <code>postgres_db</code> เราเพิ่ม <code>healthcheck</code> เพื่อตรวจสอบว่า DB พร้อมรับ Connection หรือยัง จากนั้นใน <code>api</code> เราใช้ <code>depends_on</code> ร่วมกับ <code>condition: service_healthy</code> เพื่อให้มั่นใจว่า API จะสตาร์ทก็ต่อเมื่อ DB พร้อมทำงานจริงๆ เท่านั้น ป้องกันปัญหา API ล่มเพราะต่อ DB ไม่ได้</li><li><strong>Embedded DNS:</strong> สังเกต Connection String ของ DB ในส่วน API จะเห็นว่าเราใช้ชื่อ Host เป็น <code>postgres_db</code> และ <code>redis_cache</code> ได้เลย Docker จะรู้เองว่าต้องส่งข้อมูลไปที่ Container ไหน</li><li><strong>Nginx Proxy:</strong> เรา Publish Port ออกไปที่เครื่อง Host แค่พอร์ต 80 ของ Nginx เท่านั้น ส่วน Frontend, API, DB และ Redis จะคุยกันผ่าน Internal Network ทำให้ระบบมีความปลอดภัยสูง ผู้ใช้ไม่สามารถเข้าถึงฐานข้อมูลได้โดยตรง</li></ul><p>เมื่อพร้อมแล้ว ก็แค่พิมพ์ <code>docker compose up -d</code> เพียงพริบตาเดียว ระบบ Full Stack ของคุณก็พร้อมให้บริการ!</p>'
  },
  {
    number: 8,
    slug: 'production',
    emoji: '🚀',
    title: 'Docker สำหรับ Production',
    content: '<h2>ความแตกต่างระหว่าง Development กับ Production</h2><p>การใช้ Docker ในระหว่างการพัฒนา (Development) เน้นความสะดวกสบายและการแก้ไขโค้ดที่รวดเร็ว (เช่น การใช้ Bind Mounts เพื่อ Hot-reload หรือรันด้วยสิทธิ์ Root เพื่อความง่าย) แต่เมื่อต้องนำระบบขึ้นสู่ <strong>Production</strong> เราจำเป็นต้องเปลี่ยนแนวคิดมาเน้นเรื่อง <strong>"ความปลอดภัย (Security)"</strong> และ <strong>"ประสิทธิภาพ (Performance)"</strong> เป็นอันดับแรก</p><p>บทนี้จะสรุป Best Practices สำคัญที่คุณต้องทำเสมอเมื่อจะนำ Docker ขึ้น Production ครับ</p><h2>1. ใช้ Multi-stage Builds เพื่อลดขนาด Image</h2><p>ดังที่เกริ่นไปในบทที่ 3 ขนาดของ Image มีผลโดยตรงต่อความเร็วในการ Deploy และ Scaling หาก Image คุณมีขนาด 1GB เวลาเครื่องล่มแล้วต้องดึง (Pull) Image ใหม่ อาจใช้เวลาหลายนาที แต่ถ้า Image มีขนาด 50MB ระบบอาจจะฟื้นตัวกลับมาได้ในไม่กี่วินาที นอกจากนี้ Image ที่เล็กลง ยังหมายถึงจำนวนโปรแกรม (และช่องโหว่) ที่น้อยลงด้วย</p><h2>2. ไม่รัน Container ในฐานะ Root User (Non-root user)</h2><p>โดยค่าเริ่มต้น (Default) เวลาเราสร้าง Container ระบบจะทำงานภายใต้สิทธิ์ <code>root</code> ซึ่งอันตรายมาก หากแฮกเกอร์เจาะเข้าแอปพลิเคชันของคุณได้ เขาอาจจะได้สิทธิ์ root ของเครื่อง Host ไปด้วย (เรียกว่า Container Breakout)</p><p>วิธีแก้คือการสร้างและใช้งาน User ธรรมดา (ที่มีสิทธิ์จำกัด) ภายใน Dockerfile:</p><pre>FROM node:18-alpine\nWORKDIR /app\nCOPY package*.json ./\nRUN npm install --production\nCOPY . .\n\n# 1. สร้าง User ใหม่ (ใน alpine ใช้ addgroup/adduser)\nRUN addgroup -S appgroup && adduser -S appuser -G appgroup\n\n# 2. เปลี่ยนเจ้าของไฟล์ทั้งหมดให้เป็นของ User ใหม่\nRUN chown -R appuser:appgroup /app\n\n# 3. บอกให้ Docker รันคำสั่งหลังจากนี้ในฐานะ User ใหม่\nUSER appuser\n\nCMD ["npm", "start"]</pre><h2>3. อ่านได้อย่างเดียว (Read-only Filesystem)</h2><p>แอปพลิเคชันที่ดีบน Production ไม่ควรจะสามารถแก้ไข Source Code หรือไฟล์ระบบของตัวเองได้ เพื่อป้องกันไม่ให้ผู้ไม่หวังดีฝังโค้ดอันตรายลงไป (Web Shell) เราสามารถบังคับให้ Container ทำงานในโหมด Read-only ได้</p><p>หากรันด้วยคำสั่ง: <code>docker run --read-only ...</code></p><p>หรือใน Docker Compose:</p><pre>services:\n  my-api:\n    image: my-api:v1\n    read_only: true\n    # หากแอปจำเป็นต้องเขียนไฟล์ชั่วคราว ให้ mount tmpfs ไปที่จุดนั้น\n    tmpfs:\n      - /tmp</pre><h2>4. การจัดการความลับ (Secret Management)</h2><p><strong>ห้าม!!</strong> นำข้อมูลความลับ (รหัสผ่านฐานข้อมูล, API Keys, SSL Certificates) ใส่ลงไปใน Dockerfile (เช่น ENV PASSWORD=1234) เป็นอันขาด เพราะใครก็ตามที่มี Image นี้จะเห็นรหัสผ่านทั้งหมดผ่านคำสั่ง <code>docker history</code></p><p>ใน Production เราควรใช้เครื่องมือจัดการ Secret โดยเฉพาะ เช่น:</p><ul><li><strong>Docker Swarm Secrets:</strong> ระบบจัดการ Secret ที่มากับ Docker Swarm</li><li><strong>HashiCorp Vault</strong> หรือ <strong>AWS Secrets Manager:</strong> ดึงความลับผ่าน API ในตอนรันไทม์</li><li><strong>.env File แบบระมัดระวัง:</strong> ส่งความลับผ่าน Environment Variables เฉพาะเครื่อง Server ปลายทางเท่านั้น โดยไม่คอมมิต .env เข้า Git เด็ดขาด</li></ul><h2>5. รันแบบไร้สถานะ (Stateless)</h2><p>Container บน Production สามารถตายและเกิดใหม่ได้ตลอดเวลา (เช่น เวลาระบบ Scale Out เพื่อรับโหลด) ดังนั้น <strong>ห้ามเก็บข้อมูลถาวร (เช่น ไฟล์รูปภาพอัปโหลด, Session) ไว้ใน Container เด็ดขาด</strong></p><ul><li>รูปภาพ/ไฟล์อัปโหลด -> ควรโยนขึ้น Object Storage เช่น AWS S3</li><li>User Session -> ควรเก็บไว้ใน Redis</li><li>ข้อมูลระบบ -> ควรเก็บใน Database (ที่รันแบบมี Volume จัดการแยกต่างหาก หรือใช้ DatabaseaaS เช่น Amazon RDS)</li></ul>'
  },
  {
    number: 9,
    slug: 'registry',
    emoji: '📦',
    title: 'Container Registry',
    content: '<h2>Container Registry คืออะไร?</h2><p><strong>Container Registry</strong> คือระบบจัดเก็บและแจกจ่าย Docker Images ซึ่งทำหน้าที่คล้ายกับ Github แต่เปลี่ยนจากการเก็บ Source Code มาเป็นการเก็บ Image ที่ Build เสร็จแล้วแทน เมื่อเราต้องการนำระบบขึ้น Production เซิร์ฟเวอร์ปลายทางก็จะทำการ <code>docker pull</code> Image จาก Registry นี้ลงมารัน</p><h2>ผู้ให้บริการ Registry ยอดนิยม</h2><ol style="padding-left:20px"><li><strong>Docker Hub:</strong> เป็น Registry สาธารณะที่ใหญ่ที่สุดและเป็นค่าเริ่มต้นของ Docker ข้อดีคือใช้งานง่าย ฟรีสำหรับ Public Image แต่ข้อเสียคือมีข้อจำกัดในการ Pull (Rate Limit) สำหรับผู้ใช้ฟรี และแพลนเสียเงินสำหรับ Private Image อาจจะราคาสูงเมื่อเทียบกับคู่แข่ง</li><li><strong>GitHub Container Registry (GHCR):</strong> บริการฟรีที่มาพร้อมกับ GitHub ใช้งานง่ายมากหาก Source Code ของคุณอยู่บน GitHub อยู่แล้ว รองรับ Private Image ฟรี (จำกัดโบนัสแบนด์วิธ) เหมาะกับทีมขนาดเล็กถึงกลาง</li><li><strong>AWS Elastic Container Registry (ECR):</strong> Registry ระดับ Enterprise จาก Amazon Web Services เหมาะมากหากระบบ Production ของคุณรันอยู่บน AWS (เช่น ECS, EKS) เพราะจะทำให้การดึง Image รวดเร็วมากและไม่เสียค่า Data Transfer ออกนอกระบบ</li><li><strong>Google Artifact Registry (GAR) & Azure Container Registry (ACR):</strong> บริการที่คล้ายกับ ECR แต่สำหรับผู้ที่ใช้ Cloud ของ Google และ Microsoft</li></ol><h2>การใช้งาน GitHub Container Registry (GHCR)</h2><p>สมมติว่าเราต้องการ Push Image ขึ้น GHCR นี่คือขั้นตอนแบบรวบรัด:</p><p><strong>1. สร้าง Personal Access Token (PAT):</strong> ไปที่ Github -> Settings -> Developer settings -> Personal access tokens (Classic) แล้วสร้าง Token ที่มีสิทธิ์ <code>write:packages</code> และ <code>read:packages</code></p><p><strong>2. ล็อกอินผ่าน Docker CLI:</strong></p><pre>echo "YOUR_TOKEN" | docker login ghcr.io -u YOUR_GITHUB_USERNAME --password-stdin</pre><p><strong>3. Tag Image ให้ชี้ไปที่ GHCR:</strong> (สำคัญมาก ต้องขึ้นต้นด้วย ghcr.io)</p><pre>docker tag my-app:v1 ghcr.io/yourusername/my-app:v1</pre><p><strong>4. Push Image ขึ้นระบบ:</strong></p><pre>docker push ghcr.io/yourusername/my-app:v1</pre><h2>Automated Builds (CI/CD Pipeline)</h2><p>ในการทำงานจริงระดับมืออาชีพ นักพัฒนาจะไม่มานั่งพิมพ์ <code>docker build</code> และ <code>docker push</code> ด้วยมือตัวเองจากเครื่อง Local เพราะอาจเกิด Human Error และยากต่อการควบคุมเวอร์ชัน</p><p>สิ่งที่ควรทำคือการสร้างระบบ <strong>CI/CD (Continuous Integration/Continuous Deployment)</strong> เช่น ใช้ <strong>GitHub Actions</strong> หรือ <strong>GitLab CI</strong> เพื่อให้เมื่อมีการ Push Code เข้า Git branch <code>main</code> ระบบจะทำการ Build Image, รัน Automated Tests, และ Push ขึ้น Registry ให้อัตโนมัติ</p><p>ตัวอย่างสคริปต์ GitHub Actions (<code>.github/workflows/docker-publish.yml</code>):</p><pre>name: Docker Image CI\n\non:\n  push:\n    branches: [ "main" ]\n\njobs:\n  build-and-push:\n    runs-on: ubuntu-latest\n    steps:\n    - name: Check out code\n      uses: actions/checkout@v3\n\n    - name: Log in to the Container registry\n      uses: docker/login-action@v2\n      with:\n        registry: ghcr.io\n        username: ${{ github.actor }}\n        password: ${{ secrets.GITHUB_TOKEN }}\n\n    - name: Build and push Docker image\n      uses: docker/build-push-action@v4\n      with:\n        context: .\n        push: true\n        tags: ghcr.io/yourusername/my-app:latest</pre>'
  },
  {
    number: 10,
    slug: 'swarm',
    emoji: '🐝',
    title: 'Docker Swarm เบื้องต้น',
    content: '<h2>Docker Swarm คืออะไร?</h2><p>เมื่อโปรเจคของคุณใหญ่ขึ้น เซิร์ฟเวอร์เพียง 1 เครื่องอาจไม่เพียงพอต่อการรับโหลดอีกต่อไป (ต้องทำ Horizontal Scaling) การนำ Server หลายๆ เครื่องมารวมกันเพื่อรัน Container เราเรียกระบบนี้ว่า <strong>Container Orchestration</strong></p><p>เครื่องมือที่โด่งดังที่สุดในด้านนี้คือ <strong>Kubernetes (K8s)</strong> แต่ข้อเสียของ K8s คือมีความซับซ้อนและเรียนรู้ยากมาก <strong>Docker Swarm</strong> จึงเป็นอีกหนึ่งทางเลือกที่ถูกฝังมาพร้อมกับตัว Docker Engine เลย ใช้งานง่ายสุดๆ และรองรับ <code>docker-compose.yml</code> ได้แทบจะ 100%</p><h2>Swarm vs Kubernetes (ใช้ตัวไหนดี?)</h2><ul><li><strong>Docker Swarm:</strong> เหมาะสำหรับทีมขนาดเล็กถึงกลาง, โปรเจคที่ไม่ซับซ้อนมาก, มี Server ไม่เกิน 50 เครื่อง, และต้องการความรวดเร็วในการเซ็ตอัป (ตั้งค่าเสร็จใน 5 นาที)</li><li><strong>Kubernetes:</strong> เหมาะสำหรับระบบระดับ Enterprise, โครงสร้างซับซ้อน, ต้องการฟีเจอร์ระดับสูง (เช่น Auto-scaling, Custom Metrics), มีทีม DevOps โดยเฉพาะ</li></ul><div class="tip-box"><strong>💡 Pro Tip:</strong> สำหรับบริษัททั่วไปที่ระบบไม่ได้ใหญ่ระดับ Facebook หรือ Netflix การใช้ Docker Swarm ถือว่า <strong>"เพียงพอแล้ว (Good enough)"</strong> และประหยัดแรงงานในการดูแลรักษากว่า Kubernetes มหาศาล</div><h2>การสร้าง Cluster เบื้องต้น</h2><p>สมมติว่าเรามีเซิร์ฟเวอร์ 3 เครื่อง (Manager 1 ตัว, Worker 2 ตัว)</p><p><strong>1. เริ่มต้น Swarm บนเครื่อง Manager:</strong></p><pre>docker swarm init --advertise-addr &lt;MANAGER-IP&gt;</pre><p>ระบบจะคืนค่าเป็นคำสั่ง <code>docker swarm join ...</code> พร้อมกับ Token ยาวๆ</p><p><strong>2. นำเครื่อง Worker เข้าร่วม Cluster:</strong> (นำคำสั่งที่ได้ไปรันบนเครื่อง Worker)</p><pre>docker swarm join --token &lt;TOKEN&gt; &lt;MANAGER-IP&gt;:2377</pre><p>ตรวจสอบสถานะ Cluster บนเครื่อง Manager:</p><pre>docker node ls</pre><h2>Services และ Replicas</h2><p>ในโลกของ Swarm เราจะไม่สั่ง <code>docker run</code> เป็นตัวๆ อีกต่อไป แต่เราจะสร้างสิ่งที่เรียกว่า <strong>Service</strong> ซึ่งบอกว่าเราต้องการให้แอปนี้มีกี่ก็อปปี้ (Replicas)</p><pre>docker service create --name my-web --replicas 3 -p 80:80 nginx</pre><p>คำสั่งนี้จะบอกให้ Swarm ไปสร้าง Nginx จำนวน 3 Container แล้วกระจายลงไปรันตามเครื่อง Worker ต่างๆ ใน Cluster หากเครื่องไหนพัง Swarm จะสร้าง Container ใหม่บนเครื่องที่ยังทำงานอยู่ให้อัตโนมัติ (Self-healing)</p><h2>Rolling Updates (อัปเดตแบบระบบไม่ล่ม)</h2><p>ข้อดีสุดยอดของ Swarm คือการทำ <strong>Zero-downtime Deployment</strong> เมื่อเราต้องการอัปเดตเวอร์ชันของ Image เราสามารถสั่ง:</p><pre>docker service update --image my-web:v2 --update-parallelism 1 --update-delay 10s my-web</pre><p>Swarm จะทยอยปิด Container ทีละ 1 ตัว (Parallelism 1), อัปเดต Image เป็น v2, รอ 10 วินาที, แล้วค่อยไปอัปเดตตัวถัดไป ทำให้ระบบยังคงให้บริการผู้ใช้ได้อย่างต่อเนื่อง ไม่มีสะดุด!</p><h2>Docker Secrets ใน Swarm</h2><p>ดังที่กล่าวในบทก่อน Swarm มีระบบจัดการความลับที่ปลอดภัยมาก โดยสร้างผ่านคำสั่ง:</p><pre>echo "my_db_password" | docker secret create db_pass -</pre><p>จากนั้นใน Compose file สำหรับ Swarm เราสามารถอ้างถึง Secret นี้ได้ มันจะถูก Mount เป็นไฟล์เข้าไปที่ <code>/run/secrets/db_pass</code> ในแบบ In-memory (tmpfs) ซึ่งปลอดภัยกว่า Environment Variables ธรรมดามาก</p>'
  },
  {
    number: 11,
    slug: 'project',
    emoji: '🛠️',
    title: 'โปรเจค Dockerize App จริง',
    content: '<h2>บทสรุป: นำความรู้ทั้งหมดมารวมกัน</h2><p>ในบทสุดท้ายนี้ เราจะทบทวนความรู้ตั้งแต่บทที่ 1-10 โดยจำลองสถานการณ์ว่าคุณได้รับมอบหมายให้ทำ Dockerize ระบบ E-Commerce ซึ่งประกอบด้วย <strong>Next.js (Frontend)</strong> และ <strong>Python FastAPI (Backend API)</strong> พร้อมด้วย Database เพื่อเตรียมนำขึ้น Production</p><h2>1. เตรียมไฟล์ Dockerfile (Multi-stage)</h2><p><strong>Frontend (Next.js):</strong></p><pre># Stage 1: Dependencies\nFROM node:18-alpine AS deps\nWORKDIR /app\nCOPY package.json package-lock.json ./\nRUN npm ci\n\n# Stage 2: Builder\nFROM node:18-alpine AS builder\nWORKDIR /app\nCOPY --from=deps /app/node_modules ./node_modules\nCOPY . .\nRUN npm run build\n\n# Stage 3: Runner (Production)\nFROM node:18-alpine AS runner\nWORKDIR /app\nENV NODE_ENV production\n\n# รันด้วย Non-root user\nRUN addgroup -g 1001 -S nodejs\nRUN adduser -S nextjs -u 1001\n\nCOPY --from=builder /app/public ./public\nCOPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./\nCOPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static\n\nUSER nextjs\nEXPOSE 3000\nCMD ["node", "server.js"]</pre><p><strong>Backend (FastAPI):</strong></p><pre>FROM python:3.9-slim AS builder\nWORKDIR /app\n# ติดตั้ง C Compiler สำหรับบาง Library\nRUN apt-get update && apt-get install -y --no-install-recommends build-essential\nCOPY requirements.txt .\nRUN pip install --user -r requirements.txt\n\nFROM python:3.9-slim AS runner\nWORKDIR /app\n\nRUN useradd -m appuser\nCOPY --from=builder /root/.local /home/appuser/.local\nCOPY . .\n\nUSER appuser\nENV PATH=/home/appuser/.local/bin:$PATH\nEXPOSE 8000\nCMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]</pre><h2>2. รวมร่างด้วย docker-compose.yml สำหรับ Production</h2><pre>services:\n  # --- Frontend ---\n  frontend:\n    build:\n      context: ./frontend\n    image: my-company/ecommerce-web:latest\n    ports:\n      - "3000:3000"\n    environment:\n      - NEXT_PUBLIC_API_URL=https://api.mycompany.com\n    restart: always\n    networks:\n      - webnet\n\n  # --- Backend API ---\n  backend:\n    build:\n      context: ./backend\n    image: my-company/ecommerce-api:latest\n    environment:\n      - DATABASE_URL=postgresql://user:${DB_PASSWORD}@db:5432/ecommerce\n    restart: always\n    depends_on:\n      - db\n    networks:\n      - webnet\n      - dbnet\n\n  # --- Database ---\n  db:\n    image: postgres:15-alpine\n    environment:\n      POSTGRES_USER: user\n      POSTGRES_PASSWORD: ${DB_PASSWORD}\n      POSTGRES_DB: ecommerce\n    volumes:\n      - db-data:/var/lib/postgresql/data\n    networks:\n      - dbnet\n    restart: always\n\nnetworks:\n  webnet:\n  dbnet:\n    internal: true # เครือข่ายปิด ห้ามเข้าถึงจากภายนอก\n\nvolumes:\n  db-data:</pre><h2>3. บทสรุปสิ่งที่ควรตรวจสอบก่อน Go Live</h2><ol style="padding-left:20px"><li><strong>Security:</strong> เช็คว่าไม่ได้รันเป็น root (เช็คผ่าน <code>docker exec -it &lt;container&gt; whoami</code>)</li><li><strong>Data Persistence:</strong> ตรวจสอบว่า Volume ของ DB ถูกเมานท์อย่างถูกต้อง (รัน <code>docker volume ls</code> ต้องมี <code>db-data</code> โผล่ขึ้นมา)</li><li><strong>Network Isolation:</strong> สังเกตว่า <code>db</code> อยู่ใน <code>dbnet</code> ที่เป็น <code>internal: true</code> ทำให้ปลอดภัยจากการถูกยิงพอร์ตตรงๆ จากโลกภายนอก</li><li><strong>Secrets:</strong> รหัสผ่านถูกส่งผ่าน Environment Variable <code>DB_PASSWORD</code> ที่เครื่อง Server เท่านั้น ไม่มีการ Hardcode ลงในไฟล์</li></ol><div class="tip-box"><strong>🎉 ยินดีด้วย!</strong> คุณได้เรียนรู้ตั้งแต่พื้นฐาน Container ไปจนถึงการเขียน Dockerfile แบบมืออาชีพ และการตั้งค่าเพื่อนำขึ้น Production ขอให้สนุกกับการนำ Docker ไปใช้งานในโปรเจคของคุณนะครับ!</div>'
  }
];
