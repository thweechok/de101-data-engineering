export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '🔍',
    title: 'Overview: JavaScript Advanced — จาก Junior สู่ Senior',
    content: '<h2>อะไรคือสิ่งที่แยก Junior ออกจาก Senior JavaScript Developer?</h2>\n<p>หลายคนเขียน JavaScript มาหลายปี แต่ยังคงติดกับดักของ Junior Developer อยู่ ไม่ใช่เพราะขาดประสบการณ์ แต่เพราะไม่เคยเข้าใจ <strong>กลไกเบื้องหลัง</strong> ของภาษานี้อย่างแท้จริง คอร์สนี้จะพาคุณลงลึกในส่วนที่ Developer ส่วนใหญ่มองข้าม</p>\n<h2>ความแตกต่างที่แท้จริงระหว่าง Junior และ Senior</h2>\n<ul>\n<li><strong>Junior</strong> เขียน code ให้ทำงานได้ — <strong>Senior</strong> เขียน code ที่เข้าใจได้ บำรุงรักษาได้ และ scale ได้</li>\n<li><strong>Junior</strong> ใช้ <code>async/await</code> โดยไม่รู้ว่า Event Loop ทำงานอย่างไร — <strong>Senior</strong> รู้ว่าทำไม Promise บางอันถึง resolve ก่อน</li>\n<li><strong>Junior</strong> copy Closure มาจาก Stack Overflow — <strong>Senior</strong> ออกแบบ Closure เพื่อแก้ปัญหาเฉพาะหน้า</li>\n<li><strong>Junior</strong> กลัว Prototype Chain — <strong>Senior</strong> ใช้มันสร้าง abstraction ที่ทรงพลัง</li>\n<li><strong>Junior</strong> debug ด้วย <code>console.log</code> — <strong>Senior</strong> ใช้ DevTools Profiler หาคอขวดด้าน performance</li>\n</ul>\n<h2>สิ่งที่คุณจะได้เรียนในคอร์สนี้</h2>\n<p>คอร์ส <strong>JavaScript Advanced</strong> นี้ออกแบบมาสำหรับ Developer ที่รู้พื้นฐาน JavaScript แล้ว และต้องการก้าวไปอีกขั้น ทุก Chapter สร้างต่อยอดจาก Chapter ก่อน ดังนี้:</p>\n<ol>\n<li><strong>Execution Context &amp; Call Stack</strong> — เข้าใจว่า JS Engine ประมวลผล code ของคุณอย่างไร</li>\n<li><strong>Closures ขั้นเทพ</strong> — ใช้ Closure สร้าง private state, memoization, และ module pattern</li>\n<li><strong>Prototype &amp; Inheritance</strong> — เข้าใจ Prototype Chain และความสัมพันธ์กับ class syntax</li>\n<li><strong>Async JS Deep Dive</strong> — ดำดิ่งสู่ Event Loop, microtask queue, และ Promise internals</li>\n<li><strong>Functional Programming</strong> — pure functions, currying, composition, และ Maybe monad</li>\n<li><strong>Advanced Array &amp; Object Methods</strong> — Proxy, Reflect, Optional Chaining, และ pattern ขั้นสูง</li>\n<li><strong>Error Handling &amp; Debugging</strong> — custom errors, error boundaries, และ performance profiling</li>\n<li><strong>Web Workers &amp; Performance</strong> — multithreading ใน browser และการจัดการ memory leaks</li>\n<li><strong>Security &amp; Best Practices</strong> — XSS, CSRF, CSP และ secure coding patterns</li>\n</ol>\n<h2>ใครควรเรียนคอร์สนี้?</h2>\n<ul>\n<li>Developer ที่มีประสบการณ์ JavaScript อย่างน้อย 6 เดือน</li>\n<li>คนที่ต้องการผ่าน Technical Interview ระดับ Senior</li>\n<li>Frontend Developer ที่ต้องการเข้าใจ Framework เช่น React, Vue ในระดับลึก</li>\n<li>Full-stack Developer ที่ต้องการเขียน Node.js ที่มี performance ดีขึ้น</li>\n</ul>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> อย่าข้าม Chapter — แต่ละหัวข้อสร้างต่อยอดกัน การเข้าใจ Execution Context จะทำให้ Closure ชัดเจนขึ้น และการเข้าใจ Closure จะทำให้ Functional Programming สนุกขึ้นอย่างที่คุณคาดไม่ถึง</div>\n<h2>วิธีใช้คอร์สนี้ให้ได้ประโยชน์สูงสุด</h2>\n<p>แต่ละ Chapter มี code ตัวอย่างที่คุณควร <strong>พิมพ์เอง</strong> ไม่ใช่ copy-paste เพราะกระบวนการพิมพ์ช่วยให้สมองประมวลผลและจำได้ดีกว่า หลังจากเรียนแต่ละ Chapter ลองเขียน code ใหม่โดยไม่ดูตัวอย่าง แล้วจึงกลับมาตรวจสอบ</p>\n<p>พร้อมแล้วหรือยัง? มาเริ่มทำความเข้าใจว่า JavaScript จริงๆ ทำงานอย่างไรกันเถอะ!</p>',
  },
  {
    number: 1,
    slug: 'execution-context',
    emoji: '🏗️',
    title: 'Execution Context & Call Stack',
    content: '<h2>JavaScript Engine ทำงานอย่างไร?</h2>\n<p>ก่อนที่ JavaScript Engine จะ execute code แม้แต่บรรทัดเดียว มันต้องสร้าง <strong>Execution Context</strong> ก่อน การเข้าใจสิ่งนี้คือกุญแจสำคัญที่จะทำให้คุณเข้าใจ Hoisting, Closure, และ this keyword อย่างถ่องแท้</p>\n<h2>Global Execution Context (GEC)</h2>\n<p>เมื่อ script เริ่มทำงาน JS Engine สร้าง Global Execution Context ซึ่งมีสองขั้นตอน:</p>\n<ol>\n<li><strong>Creation Phase (Memory Phase)</strong> — Engine สแกน code ทั้งหมดและจัดสรร memory ให้ตัวแปรและ function</li>\n<li><strong>Execution Phase</strong> — Engine execute code ทีละบรรทัดตามลำดับ</li>\n</ol>\n<pre>// ในระหว่าง Creation Phase:\n// - console เข้าถึงได้\n// - greet ถูกเก็บเป็น function ทั้งหมด\n// - name ถูกเก็บเป็น undefined\n\nconsole.log(name);    // undefined (ไม่ใช่ ReferenceError)\nconsole.log(greet()); // "Hello!" (function พร้อมใช้งาน)\n\nvar name = "Somsak";\nfunction greet() {\n  return "Hello!";\n}</pre>\n<h2>Hoisting คืออะไร?</h2>\n<p><strong>Hoisting</strong> ไม่ใช่การ "ยก" code ขึ้นไปข้างบน แต่เป็นพฤติกรรมที่เกิดจาก Creation Phase โดย Engine ประมวลผล declarations ก่อน executions เสมอ</p>\n<ul>\n<li><code>var</code> — hoisted และ initialized เป็น <code>undefined</code></li>\n<li><code>let</code> และ <code>const</code> — hoisted แต่อยู่ใน <strong>Temporal Dead Zone (TDZ)</strong> จนกว่าจะถึงบรรทัดที่ declare</li>\n<li><strong>Function Declaration</strong> — hoisted ทั้ง function</li>\n<li><strong>Function Expression</strong> — hoisted เฉพาะ variable ไม่ใช่ function</li>\n</ul>\n<pre>console.log(a); // undefined\nconsole.log(b); // ReferenceError: Cannot access \'b\' before initialization\nconsole.log(c); // ReferenceError\n\nvar a = 1;\nlet b = 2;\nconst c = 3;</pre>\n<h2>Call Stack</h2>\n<p><strong>Call Stack</strong> คือ data structure แบบ LIFO (Last In, First Out) ที่ JS Engine ใช้ติดตามว่า function ไหนกำลัง execute อยู่ แต่ละครั้งที่เรียก function จะมีการสร้าง Execution Context ใหม่และ push เข้า Stack</p>\n<pre>function multiply(a, b) {\n  return a * b; // EC ของ multiply อยู่บนสุดของ Stack\n}\n\nfunction square(n) {\n  return multiply(n, n); // EC ของ square อยู่กลาง Stack\n}\n\nfunction printSquare(x) {\n  const result = square(x); // EC ของ printSquare อยู่ล่างสุด\n  console.log(result);\n}\n\nprintSquare(5); // เรียกลำดับ: printSquare → square → multiply</pre>\n<h2>Scope Chain &amp; Lexical Environment</h2>\n<p><strong>Lexical Environment</strong> คือ structure ที่เก็บ mapping ระหว่างชื่อตัวแปรกับค่าของมัน แต่ละ Execution Context มี Lexical Environment เป็นของตัวเอง และมี reference ไปยัง Lexical Environment ของ parent (outer) ซึ่งก็คือ <strong>Scope Chain</strong></p>\n<pre>const globalVar = "global";\n\nfunction outer() {\n  const outerVar = "outer";\n\n  function inner() {\n    const innerVar = "inner";\n    // inner เข้าถึงได้: innerVar, outerVar, globalVar\n    console.log(globalVar); // "global"\n    console.log(outerVar);  // "outer"\n    console.log(innerVar);  // "inner"\n  }\n\n  inner();\n}\n\nouter();</pre>\n<p>เมื่อ <code>inner()</code> ค้นหาตัวแปร มันจะตรวจสอบ Lexical Environment ของตัวเองก่อน ถ้าไม่พบจะขยับขึ้นไปใน Scope Chain จนถึง Global Scope</p>\n<h2>this Binding</h2>\n<p><strong>this</strong> ใน JavaScript ไม่ได้ขึ้นอยู่กับว่า function ถูก declare ที่ไหน แต่ขึ้นอยู่กับว่า function ถูก <strong>เรียก</strong> อย่างไร</p>\n<ul>\n<li><strong>Global context</strong> — <code>this</code> คือ <code>window</code> (browser) หรือ <code>global</code> (Node.js)</li>\n<li><strong>Method call</strong> — <code>this</code> คือ object ที่เรียก method นั้น</li>\n<li><strong>Arrow function</strong> — ไม่มี <code>this</code> ของตัวเอง ใช้ <code>this</code> จาก lexical context</li>\n<li><strong>call/apply/bind</strong> — กำหนด <code>this</code> แบบ explicit</li>\n</ul>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> Stack Overflow ส่วนใหญ่เกิดจาก infinite recursion — function เรียกตัวเองโดยไม่มี base case ทำให้ Call Stack เต็ม ลอง debug โดยดูที่ error message "Maximum call stack size exceeded"</div>',
  },
  {
    number: 2,
    slug: 'closures',
    emoji: '🔒',
    title: 'Closures ขั้นเทพ',
    content: '<h2>Closure คืออะไร?</h2>\n<p><strong>Closure</strong> คือ function ที่สามารถ "จำ" และเข้าถึงตัวแปรจาก Lexical Scope ที่มันถูกสร้างขึ้น แม้ว่า Scope นั้นจะ "ปิดไปแล้ว" คือ function ที่สร้าง Scope นั้น execute เสร็จสิ้นไปแล้วก็ตาม</p>\n<p>นี่ไม่ใช่ magic แต่เป็นผลโดยตรงจากการที่ JS ใช้ <strong>Lexical Scoping</strong> ร่วมกับการที่ function เป็น <strong>First-class citizen</strong> (สามารถส่งเป็น argument หรือ return จาก function ได้)</p>\n<pre>function createCounter() {\n  let count = 0; // ตัวแปรใน Scope ของ createCounter\n\n  return function() {\n    count++; // inner function จำ count ได้แม้ createCounter จบแล้ว\n    return count;\n  };\n}\n\nconst counter = createCounter();\nconsole.log(counter()); // 1\nconsole.log(counter()); // 2\nconsole.log(counter()); // 3\n// count ถูกเก็บไว้ใน Closure ไม่สามารถเข้าถึงจากภายนอกได้โดยตรง</pre>\n<h2>Closure ใช้งานอะไรได้บ้าง?</h2>\n<h3>1. Private Variables</h3>\n<p>JavaScript ไม่มี access modifiers เหมือน Java หรือ C# แต่เราสร้าง private state ได้ด้วย Closure</p>\n<pre>function createBankAccount(initialBalance) {\n  let balance = initialBalance; // private — เข้าถึงจากภายนอกไม่ได้\n\n  return {\n    deposit(amount) {\n      if (amount > 0) balance += amount;\n      return balance;\n    },\n    withdraw(amount) {\n      if (amount > 0 && amount <= balance) {\n        balance -= amount;\n        return balance;\n      }\n      return "ยอดไม่เพียงพอ";\n    },\n    getBalance() {\n      return balance;\n    }\n  };\n}\n\nconst account = createBankAccount(1000);\naccount.deposit(500);   // 1500\naccount.withdraw(200);  // 1300\nconsole.log(account.balance); // undefined — เข้าถึงโดยตรงไม่ได้!</pre>\n<h3>2. Memoization</h3>\n<p>Memoization คือการ cache ผลลัพธ์ของ function เพื่อไม่ต้องคำนวณซ้ำเมื่อ input เหมือนกัน Closure เหมาะมากสำหรับ pattern นี้</p>\n<pre>function memoize(fn) {\n  const cache = {}; // cache อยู่ใน Closure\n\n  return function(...args) {\n    const key = JSON.stringify(args);\n    if (key in cache) {\n      console.log("Cache hit!");\n      return cache[key];\n    }\n    cache[key] = fn(...args);\n    return cache[key];\n  };\n}\n\nconst slowSquare = (n) => n * n;\n\nconst fastSquare = memoize(slowSquare);\nfastSquare(10); // คำนวณจริง\nfastSquare(10); // "Cache hit!" — ดึงจาก cache\nfastSquare(5);  // คำนวณจริง</pre>\n<h3>3. Module Pattern</h3>\n<p>ก่อนที่ ES Modules จะมา Closure เป็นวิธีหลักในการสร้าง module ที่มี private state และ public API</p>\n<pre>const ShoppingCart = (function() {\n  // Private\n  let items = [];\n  let discount = 0;\n\n  function calculateTotal() {\n    return items.reduce((sum, item) => sum + item.price, 0) * (1 - discount);\n  }\n\n  // Public API\n  return {\n    addItem(item) { items.push(item); },\n    setDiscount(d) { discount = d; },\n    getTotal() { return calculateTotal(); },\n    getItemCount() { return items.length; }\n  };\n})(); // IIFE — Immediately Invoked Function Expression\n\nShoppingCart.addItem({ name: "Laptop", price: 30000 });\nShoppingCart.setDiscount(0.1);\nconsole.log(ShoppingCart.getTotal()); // 27000</pre>\n<h3>4. Function Factory</h3>\n<p>สร้าง function ที่มีพฤติกรรมต่างกันโดยใช้ Closure เก็บ configuration</p>\n<pre>function createMultiplier(multiplier) {\n  return (number) => number * multiplier;\n}\n\nconst double = createMultiplier(2);\nconst triple = createMultiplier(3);\nconst tenX = createMultiplier(10);\n\nconsole.log(double(5));  // 10\nconsole.log(triple(5));  // 15\nconsole.log(tenX(5));    // 50</pre>\n<h2>Closure Gotcha: Loop Problem</h2>\n<p>ข้อผิดพลาดที่พบบ่อยมากเมื่อใช้ Closure ใน loop:</p>\n<pre>// ❌ ปัญหา: ทุก function แชร์ i เดียวกัน\nconst funcs = [];\nfor (var i = 0; i < 3; i++) {\n  funcs.push(function() { console.log(i); });\n}\nfuncs[0](); // 3 (ไม่ใช่ 0!)\nfuncs[1](); // 3\nfuncs[2](); // 3\n\n// ✅ แก้ด้วย let (block scope)\nconst funcs2 = [];\nfor (let i = 0; i < 3; i++) {\n  funcs2.push(function() { console.log(i); });\n}\nfuncs2[0](); // 0\nfuncs2[1](); // 1\nfuncs2[2](); // 2</pre>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> Closure มี memory cost เพราะ JS Engine ต้องเก็บ reference ไปยัง Scope ที่ถูกปิด ถ้าสร้าง Closure จำนวนมากใน loop ที่วนหลายพันรอบ ควรระวัง memory leak โดยเฉพาะใน long-running applications</div>',
  },
  {
    number: 3,
    slug: 'prototype',
    emoji: '🔗',
    title: 'Prototype & Inheritance',
    content: '<h2>ทำไม JavaScript ถึงต่างจากภาษา OOP อื่น?</h2>\n<p>JavaScript ไม่ได้ใช้ <strong>Classical Inheritance</strong> เหมือน Java หรือ C++ แต่ใช้ <strong>Prototypal Inheritance</strong> ซึ่งหมายความว่า object สืบทอดคุณสมบัติโดยตรงจาก object อื่น ไม่ใช่จาก class ที่เป็น blueprint</p>\n<p>แม้ว่า ES6 จะเพิ่ม <code>class</code> syntax เข้ามา แต่มันเป็นแค่ <strong>syntactic sugar</strong> ที่ยังคงทำงานบน Prototype Chain เหมือนเดิม</p>\n<h2>Prototype Chain คืออะไร?</h2>\n<p>ทุก object ใน JavaScript มี property ที่ซ่อนอยู่ชื่อ <code>[[Prototype]]</code> ซึ่งเป็น reference ไปยัง object อีกอัน เมื่อเข้าถึง property ที่ไม่มีใน object JS Engine จะไต่ขึ้นไปตาม Prototype Chain จนพบหรือถึง <code>null</code></p>\n<pre>const animal = {\n  breathe() {\n    return `${this.name} กำลังหายใจ`;\n  }\n};\n\nconst dog = Object.create(animal); // dog\'s [[Prototype]] = animal\ndog.name = "บัดดี้";\ndog.bark = function() {\n  return "โฮ่ง!";\n};\n\nconsole.log(dog.bark());    // "โฮ่ง!" — พบใน dog เอง\nconsole.log(dog.breathe()); // "บัดดี้ กำลังหายใจ" — พบใน prototype\nconsole.log(dog.toString()); // พบใน Object.prototype</pre>\n<h2>Constructor Function &amp; Prototype</h2>\n<p>ก่อน ES6 เราสร้าง "class" ด้วย Constructor Function และเพิ่ม method ผ่าน <code>.prototype</code></p>\n<pre>function Person(name, age) {\n  this.name = name;\n  this.age = age;\n}\n\n// Method ที่เพิ่มใน prototype จะถูกแชร์ระหว่าง instances ทั้งหมด\nPerson.prototype.greet = function() {\n  return `สวัสดี ผมชื่อ ${this.name} อายุ ${this.age} ปี`;\n};\n\nPerson.prototype.isAdult = function() {\n  return this.age >= 18;\n};\n\nconst p1 = new Person("สมศักดิ์", 25);\nconst p2 = new Person("มานี", 16);\n\nconsole.log(p1.greet());    // "สวัสดี ผมชื่อ สมศักดิ์ อายุ 25 ปี"\nconsole.log(p2.isAdult());  // false\n\n// ทั้งคู่แชร์ method เดียวกัน — ประหยัด memory\nconsole.log(p1.greet === p2.greet); // true</pre>\n<h2>ES6 Class: Syntactic Sugar</h2>\n<pre>class Animal {\n  constructor(name) {\n    this.name = name;\n  }\n\n  speak() {\n    return `${this.name} ส่งเสียง`;\n  }\n\n  // Static method — เรียกได้จาก class โดยตรง ไม่ใช่ instance\n  static create(name) {\n    return new Animal(name);\n  }\n}\n\nclass Dog extends Animal {\n  constructor(name, breed) {\n    super(name); // เรียก constructor ของ parent\n    this.breed = breed;\n  }\n\n  speak() {\n    return `${this.name} เห่า: โฮ่ง!`;\n  }\n\n  fetch() {\n    return `${this.name} วิ่งไปเก็บลูกบอล`;\n  }\n}\n\nconst dog = new Dog("ไทสัน", "German Shepherd");\nconsole.log(dog.speak());  // "ไทสัน เห่า: โฮ่ง!"\nconsole.log(dog.fetch());  // "ไทสัน วิ่งไปเก็บลูกบอล"\nconsole.log(dog instanceof Dog);    // true\nconsole.log(dog instanceof Animal); // true — ตรวจสอบ Prototype Chain</pre>\n<h2>Object.create vs new</h2>\n<p>เข้าใจความแตกต่างระหว่างสองวิธีในการสร้าง object ที่มี prototype:</p>\n<pre>// Object.create — กำหนด prototype โดยตรง ชัดเจนกว่า\nconst vehicleProto = {\n  describe() {\n    return `${this.type} ความเร็ว ${this.speed} km/h`;\n  }\n};\n\nconst car = Object.create(vehicleProto);\ncar.type = "รถยนต์";\ncar.speed = 120;\nconsole.log(car.describe()); // "รถยนต์ ความเร็ว 120 km/h"\n\n// ตรวจสอบ prototype\nconsole.log(Object.getPrototypeOf(car) === vehicleProto); // true</pre>\n<h2>Mixins: การ Compose พฤติกรรม</h2>\n<p>JavaScript ไม่รองรับ multiple inheritance โดยตรง แต่เราใช้ <strong>Mixins</strong> เพื่อ compose พฤติกรรมจากหลายแหล่ง</p>\n<pre>const Serializable = (superclass) => class extends superclass {\n  serialize() {\n    return JSON.stringify(this);\n  }\n};\n\nconst Timestamped = (superclass) => class extends superclass {\n  constructor(...args) {\n    super(...args);\n    this.createdAt = new Date().toISOString();\n  }\n};\n\nclass User extends Serializable(Timestamped(class {})) {\n  constructor(name, email) {\n    super();\n    this.name = name;\n    this.email = email;\n  }\n}\n\nconst user = new User("สมชาย", "somchai@example.com");\nconsole.log(user.serialize());\nconsole.log(user.createdAt);</pre>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ <code>Object.getPrototypeOf(obj)</code> แทน <code>obj.__proto__</code> เสมอ เพราะ <code>__proto__</code> เป็น non-standard และถูก deprecated ใน environments บางอย่าง การเข้าใจ Prototype Chain จะช่วยให้คุณ debug ปัญหา "method not found" ได้รวดเร็วขึ้นมาก</div>',
  },
  {
    number: 4,
    slug: 'async-deep',
    emoji: '⚡',
    title: 'Asynchronous JS Deep Dive',
    content: '<h2>JavaScript เป็น Single-threaded แล้ว Async ทำงานอย่างไร?</h2>\n<p>JavaScript มีเพียง <strong>หนึ่ง Call Stack</strong> นั่นหมายความว่ามันทำได้เพียงอย่างเดียวในเวลาเดียวกัน แล้วทำไมเราถึงสามารถ fetch ข้อมูล, set timer, และรับ user events ได้พร้อมกัน? คำตอบคือ <strong>Event Loop</strong></p>\n<h2>Event Loop ทำงานอย่างไร?</h2>\n<p>ระบบ async ของ JavaScript ประกอบด้วยส่วนต่างๆ ดังนี้:</p>\n<ul>\n<li><strong>Call Stack</strong> — ที่ JS Execute code ตามลำดับ</li>\n<li><strong>Web APIs</strong> — ส่วนที่ Browser จัดการ async operations (setTimeout, fetch, DOM events)</li>\n<li><strong>Callback Queue (Task Queue)</strong> — ที่รอ callbacks จาก Web APIs</li>\n<li><strong>Microtask Queue</strong> — priority queue สำหรับ Promise callbacks และ queueMicrotask</li>\n<li><strong>Event Loop</strong> — ตรวจสอบว่า Call Stack ว่างหรือไม่ และย้าย tasks เข้า Stack</li>\n</ul>\n<h2>Microtask vs Macrotask</h2>\n<p>ความแตกต่างที่สำคัญที่ Developer หลายคนไม่รู้:</p>\n<pre>console.log("1 - Start");\n\nsetTimeout(() => console.log("2 - setTimeout"), 0);\n\nPromise.resolve().then(() => console.log("3 - Promise 1"));\nPromise.resolve().then(() => console.log("4 - Promise 2"));\n\nconsole.log("5 - End");\n\n// Output:\n// 1 - Start\n// 5 - End\n// 3 - Promise 1   ← Microtask ก่อน\n// 4 - Promise 2   ← Microtask ทั้งหมดก่อน\n// 2 - setTimeout  ← Macrotask หลังสุด</pre>\n<p><strong>กฎสำคัญ:</strong> Event Loop จะ clear <em>ทั้งหมด</em> ของ Microtask Queue ก่อน จึงจะดึง task ใหม่จาก Macrotask Queue</p>\n<h2>Promise Internals</h2>\n<p>Promise มีสาม state: <code>pending</code>, <code>fulfilled</code>, <code>rejected</code> เมื่อ Promise settle แล้วจะเปลี่ยน state ไม่ได้อีก</p>\n<pre>function fetchUserData(id) {\n  return new Promise((resolve, reject) => {\n    if (!id) {\n      reject(new Error("ต้องระบุ user ID"));\n      return;\n    }\n    // จำลอง API call\n    setTimeout(() => {\n      resolve({ id, name: "สมศักดิ์", role: "admin" });\n    }, 1000);\n  });\n}\n\n// Promise chaining\nfetchUserData(1)\n  .then(user => {\n    console.log("พบ user:", user.name);\n    return user.role;\n  })\n  .then(role => console.log("Role:", role))\n  .catch(err => console.error("Error:", err.message))\n  .finally(() => console.log("เสร็จสิ้น"));</pre>\n<h2>Promise Combinators</h2>\n<pre>// Promise.all — รอทุกอัน ถ้า reject แม้อันเดียวก็ fail ทั้งหมด\nconst [users, products] = await Promise.all([\n  fetch("/api/users").then(r => r.json()),\n  fetch("/api/products").then(r => r.json())\n]);\n\n// Promise.allSettled — รอทุกอัน ไม่สนใจว่า reject หรือ resolve\nconst results = await Promise.allSettled([p1, p2, p3]);\nresults.forEach(result => {\n  if (result.status === "fulfilled") console.log(result.value);\n  else console.log("Failed:", result.reason);\n});\n\n// Promise.race — เอาอันแรกที่ settle\nconst fastest = await Promise.race([fetchFromServer1(), fetchFromServer2()]);\n\n// Promise.any — เอาอันแรกที่ fulfill\nconst firstSuccess = await Promise.any([tryMethod1(), tryMethod2()]);</pre>\n<h2>async/await Under the Hood</h2>\n<p><code>async/await</code> คือ syntactic sugar บน Promise ทำให้ code อ่านง่ายขึ้นแต่ยังทำงานแบบ asynchronous</p>\n<pre>async function loadDashboard(userId) {\n  try {\n    const user = await fetchUser(userId);\n    const [orders, wishlist] = await Promise.all([\n      fetchOrders(user.id),\n      fetchWishlist(user.id)\n    ]);\n    return { user, orders, wishlist };\n  } catch (err) {\n    console.error("โหลด Dashboard ล้มเหลว:", err);\n    throw err; // re-throw เพื่อให้ caller จัดการต่อ\n  }\n}</pre>\n<h2>ข้อผิดพลาดที่พบบ่อย: await ใน for loop</h2>\n<pre>// ❌ Sequential — ช้า\nfor (const id of userIds) {\n  const user = await fetchUser(id); // รอทีละคน\n}\n\n// ✅ Parallel — เร็วกว่ามาก\nconst users = await Promise.all(userIds.map(id => fetchUser(id)));</pre>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> หลีกเลี่ยง "await ใน loop" เพราะทำให้ requests วิ่งแบบ sequential แทนที่จะเป็น parallel ใช้ <code>Promise.all(array.map(item => asyncFn(item)))</code> แทนเพื่อประสิทธิภาพสูงสุด ความแตกต่างอาจเป็น 100ms vs 1000ms สำหรับ 10 requests</div>',
  },
  {
    number: 5,
    slug: 'functional',
    emoji: '🧩',
    title: 'Functional Programming',
    content: '<h2>ทำไมต้อง Functional Programming?</h2>\n<p><strong>Functional Programming (FP)</strong> เป็น paradigm ที่มองการเขียน program ว่าเป็นการประกอบ <strong>pure functions</strong> เข้าด้วยกัน หลีกเลี่ยง shared state และ mutable data ทำให้ code อ่านง่าย ทดสอบง่าย และ debug ง่ายกว่ามาก</p>\n<p>JavaScript ไม่ใช่ purely functional language แต่รองรับ FP ได้ดีเพราะ function เป็น first-class citizen</p>\n<h2>Pure Functions</h2>\n<p>Pure function มีคุณสมบัติสองอย่าง:</p>\n<ol>\n<li><strong>Deterministic</strong> — input เหมือนกัน output ต้องเหมือนกันเสมอ</li>\n<li><strong>No Side Effects</strong> — ไม่แก้ไขอะไรนอก function scope (ไม่แก้ global state, ไม่เรียก API, ไม่ log)</li>\n</ol>\n<pre>// ✅ Pure Functions\nconst add = (a, b) => a + b;\nconst formatName = (first, last) => `${first} ${last}`;\nconst calculateTax = (price, rate) => price * rate;\n\n// ❌ Impure Functions\nlet total = 0;\nconst addToTotal = (n) => { total += n; }; // แก้ไข external state\nconst getRandomId = () => Math.random();   // ผลลัพธ์ไม่แน่นอน</pre>\n<h2>Immutability</h2>\n<p>ใน FP เราไม่แก้ไข data โดยตรง แต่สร้าง data ใหม่พร้อมการเปลี่ยนแปลง</p>\n<pre>// ❌ Mutable approach\nconst user = { name: "สมชาย", age: 30, role: "user" };\nuser.role = "admin"; // แก้ไข object โดยตรง\n\n// ✅ Immutable approach\nconst updatedUser = { ...user, role: "admin" }; // สร้าง object ใหม่\n\n// ✅ Immutable array operations\nconst numbers = [1, 2, 3, 4, 5];\nconst withSix = [...numbers, 6];\nconst withoutThree = numbers.filter(n => n !== 3);\nconst doubled = numbers.map(n => n * 2);</pre>\n<h2>map, filter, reduce — The Holy Trinity</h2>\n<pre>const orders = [\n  { id: 1, product: "Laptop", price: 35000, status: "completed" },\n  { id: 2, product: "Mouse", price: 800, status: "pending" },\n  { id: 3, product: "Monitor", price: 12000, status: "completed" },\n  { id: 4, product: "Keyboard", price: 2500, status: "cancelled" },\n];\n\n// map — แปลงทุก element\nconst productNames = orders.map(o => o.product);\n// ["Laptop", "Mouse", "Monitor", "Keyboard"]\n\n// filter — กรอง element\nconst completedOrders = orders.filter(o => o.status === "completed");\n\n// reduce — รวมเป็นค่าเดียว\nconst totalRevenue = orders\n  .filter(o => o.status === "completed")\n  .reduce((sum, o) => sum + o.price, 0);\n// 47000</pre>\n<h2>Currying</h2>\n<p><strong>Currying</strong> คือการแปลง function ที่รับหลาย argument เป็น chain ของ functions ที่รับ argument ทีละอัน</p>\n<pre>// Normal function\nconst multiply = (a, b) => a * b;\n\n// Curried version\nconst curriedMultiply = (a) => (b) => a * b;\n\nconst double = curriedMultiply(2);\nconst triple = curriedMultiply(3);\n\nconsole.log(double(5));  // 10\nconsole.log(triple(5));  // 15\n\n// ใช้ร่วมกับ map ได้สวยงาม\n[1, 2, 3, 4, 5].map(curriedMultiply(10)); // [10, 20, 30, 40, 50]</pre>\n<h2>Function Composition</h2>\n<p>การรวม functions เล็กๆ เข้าด้วยกันเพื่อสร้าง function ที่ซับซ้อนกว่า</p>\n<pre>const pipe = (...fns) => (x) => fns.reduce((acc, fn) => fn(acc), x);\n\nconst trim = (str) => str.trim();\nconst toLowerCase = (str) => str.toLowerCase();\nconst removeSpaces = (str) => str.replace(/\\s+/g, "-");\nconst addPrefix = (prefix) => (str) => `${prefix}-${str}`;\n\nconst createSlug = pipe(\n  trim,\n  toLowerCase,\n  removeSpaces,\n  addPrefix("blog")\n);\n\nconsole.log(createSlug("  Hello World  ")); // "blog-hello-world"</pre>\n<h2>Maybe Monad — จัดการ null อย่างปลอดภัย</h2>\n<pre>class Maybe {\n  constructor(value) { this._value = value; }\n  static of(value) { return new Maybe(value); }\n  isNothing() { return this._value == null; }\n  map(fn) {\n    return this.isNothing() ? this : Maybe.of(fn(this._value));\n  }\n  getOrElse(defaultValue) {\n    return this.isNothing() ? defaultValue : this._value;\n  }\n}\n\n// ไม่มี null pointer exception แม้ user หรือ address จะเป็น null\nconst city = Maybe.of(getUser(1))\n  .map(user => user?.address?.city)\n  .map(c => c.toUpperCase())\n  .getOrElse("ไม่ทราบเมือง");</pre>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใน React และ modern JS code base แนวคิด FP ถูกใช้ทุกที่ เช่น Redux ใช้ pure reducers, React Hooks ใช้ composition, และ Array methods ใน data transformation เริ่มต้นด้วยการเขียน pure functions ให้มากขึ้น แล้วคุณจะเห็นว่า code ดีขึ้นอย่างเป็นรูปธรรม</div>',
  },
  {
    number: 6,
    slug: 'array-object',
    emoji: '📦',
    title: 'Advanced Array & Object Methods',
    content: '<h2>Destructuring Patterns ขั้นสูง</h2>\n<p>Destructuring ไม่ใช่แค่การ unpack ค่าพื้นฐาน มันทรงพลังกว่านั้นมาก</p>\n<pre>// Object destructuring พร้อม rename และ default values\nconst { name: userName = "Guest", age: userAge = 0, role = "user" } = apiResponse;\n\n// Nested destructuring\nconst { address: { city, postcode: zip } } = user;\n\n// Array destructuring พร้อม skip\nconst [first, , third, ...rest] = [1, 2, 3, 4, 5];\n// first=1, third=3, rest=[4,5]\n\n// Destructuring ใน function parameters\nfunction displayUser({ name, age, address: { city = "ไม่ระบุ" } = {} }) {\n  console.log(`${name}, ${age} ปี, จาก ${city}`);\n}\n\n// Swap variables ด้วย destructuring\nlet a = 1, b = 2;\n[a, b] = [b, a]; // a=2, b=1</pre>\n<h2>Spread &amp; Rest อย่างเชี่ยวชาญ</h2>\n<pre>// Deep merge objects\nconst defaultConfig = { timeout: 3000, retries: 3, debug: false };\nconst userConfig = { timeout: 5000, cache: true };\nconst finalConfig = { ...defaultConfig, ...userConfig };\n// { timeout: 5000, retries: 3, debug: false, cache: true }\n\n// Rest parameters\nfunction logWithLevel(level, ...messages) {\n  messages.forEach(msg => console.log(`[${level}] ${msg}`));\n}\nlogWithLevel("INFO", "เริ่มต้น server", "พอร์ต 3000", "โหมด production");</pre>\n<h2>Optional Chaining (?.) &amp; Nullish Coalescing (??)</h2>\n<pre>// แทน code แบบเก่า:\nconst city = user && user.address && user.address.city;\n\n// ใช้ Optional Chaining:\nconst city = user?.address?.city;\nconst length = data?.items?.length ?? 0;\nconst name = user?.getName?.() ?? "ไม่มีชื่อ";\nconst first = arr?.[0];\n\n// Nullish Coalescing ต่างจาก || อย่างไร\nconsole.log(0 || "default");  // "default" (0 เป็น falsy)\nconsole.log(0 ?? "default");  // 0 (0 ไม่ใช่ null/undefined)\nconsole.log("" || "default"); // "default"\nconsole.log("" ?? "default"); // "" — เพราะ "" ไม่ใช่ null/undefined</pre>\n<h2>Proxy: Intercept Object Operations</h2>\n<p><strong>Proxy</strong> ช่วยให้เราดัก (intercept) และกำหนดพฤติกรรมสำหรับ fundamental operations บน object เช่น property lookup, assignment, enumeration</p>\n<pre>function createValidatedUser(userData) {\n  return new Proxy(userData, {\n    set(target, prop, value) {\n      if (prop === "age" && typeof value !== "number") {\n        throw new TypeError("age ต้องเป็นตัวเลข");\n      }\n      if (prop === "email" && !value.includes("@")) {\n        throw new TypeError("email ไม่ถูกต้อง");\n      }\n      target[prop] = value;\n      return true;\n    },\n    get(target, prop) {\n      if (prop === "fullInfo") {\n        return `${target.name} (${target.email})`;\n      }\n      return target[prop];\n    }\n  });\n}\n\nconst user = createValidatedUser({ name: "สมชาย", email: "s@example.com", age: 30 });\nconsole.log(user.fullInfo); // "สมชาย (s@example.com)"\nuser.age = "สามสิบ"; // TypeError: age ต้องเป็นตัวเลข</pre>\n<h2>Reflect API</h2>\n<p><strong>Reflect</strong> มี static methods ที่ mirror Proxy traps ทำให้การเขียน Proxy handlers สะอาดขึ้น</p>\n<pre>const handler = {\n  get(target, prop, receiver) {\n    console.log(`กำลังเข้าถึง: ${String(prop)}`);\n    return Reflect.get(target, prop, receiver);\n  },\n  set(target, prop, value, receiver) {\n    console.log(`กำลังตั้งค่า: ${String(prop)} = ${value}`);\n    return Reflect.set(target, prop, value, receiver);\n  }\n};\n\nconst obj = new Proxy({}, handler);\nobj.name = "Test";\nconsole.log(obj.name);</pre>\n<h2>Advanced Array Methods</h2>\n<pre>// flatMap — map แล้ว flatten 1 ระดับ\nconst sentences = ["Hello World", "Foo Bar"];\nconst words = sentences.flatMap(s => s.split(" "));\n// ["Hello", "World", "Foo", "Bar"]\n\n// Array.from พร้อม mapping\nconst squares = Array.from({ length: 10 }, (_, i) => (i + 1) ** 2);\n// [1, 4, 9, 16, 25, 36, 49, 64, 81, 100]\n\n// findLast และ findLastIndex\nconst lastAdmin = users.findLast(u => u.role === "admin");\nconst lastAdminIndex = users.findLastIndex(u => u.role === "admin");\n\n// at() method — รองรับ negative index\nconst last = arr.at(-1);       // element สุดท้าย\nconst secondLast = arr.at(-2); // element รองสุดท้าย\n\n// Object.entries, Object.fromEntries\nconst doubled = Object.fromEntries(\n  Object.entries(prices).map(([key, val]) => [key, val * 2])\n);</pre>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> Proxy มีต้นทุน performance เพราะทุก property access ผ่าน trap handler ใช้มันเฉพาะเมื่อจำเป็นจริงๆ เช่น validation layer, reactive systems (Vue 3 ใช้ Proxy เป็น core ของ reactivity system), หรือ logging/debugging บน object ที่ซับซ้อน</div>',
  },
  {
    number: 7,
    slug: 'error-handling',
    emoji: '🔧',
    title: 'Error Handling & Debugging',
    content: '<h2>ทำไม Error Handling ถึงสำคัญ?</h2>\n<p>Application ที่ดีไม่ใช่ Application ที่ไม่มี error — แต่คือ Application ที่จัดการ error ได้อย่างสง่างาม ผู้ใช้เห็น friendly message แทนที่จะเห็นหน้าขาวหรือ crash โดยไม่มีคำอธิบาย</p>\n<h2>Error Types ใน JavaScript</h2>\n<ul>\n<li><strong>SyntaxError</strong> — code ไม่ถูก syntax: <code>let = 5</code></li>\n<li><strong>ReferenceError</strong> — เรียกตัวแปรที่ไม่มีอยู่: <code>console.log(undeclaredVar)</code></li>\n<li><strong>TypeError</strong> — ใช้ค่าผิดประเภท: <code>null.property</code></li>\n<li><strong>RangeError</strong> — ค่านอก range ที่อนุญาต: <code>new Array(-1)</code></li>\n<li><strong>URIError</strong> — decode URI ที่ผิดรูปแบบ</li>\n<li><strong>EvalError</strong> — เกิดจาก eval() (หายากมาก)</li>\n</ul>\n<h2>Custom Error Classes</h2>\n<p>การสร้าง Custom Error ทำให้เราแยกประเภทของ error และจัดการได้อย่างเหมาะสม</p>\n<pre>class AppError extends Error {\n  constructor(message, statusCode, code) {\n    super(message);\n    this.name = this.constructor.name;\n    this.statusCode = statusCode;\n    this.code = code;\n    Error.captureStackTrace(this, this.constructor);\n  }\n}\n\nclass ValidationError extends AppError {\n  constructor(message, field) {\n    super(message, 400, "VALIDATION_ERROR");\n    this.field = field;\n  }\n}\n\nclass NotFoundError extends AppError {\n  constructor(resource, id) {\n    super(`ไม่พบ ${resource} ID: ${id}`, 404, "NOT_FOUND");\n    this.resource = resource;\n  }\n}\n\n// การใช้งาน\nfunction getUser(id) {\n  if (!id) throw new ValidationError("ต้องระบุ ID", "id");\n  const user = db.find(id);\n  if (!user) throw new NotFoundError("User", id);\n  return user;\n}\n\n// จัดการตามประเภท\ntry {\n  const user = getUser(null);\n} catch (err) {\n  if (err instanceof ValidationError) {\n    console.log(`Validation failed: ${err.field}`);\n  } else if (err instanceof NotFoundError) {\n    console.log(`Not found: ${err.resource}`);\n  } else {\n    throw err; // re-throw errors ที่จัดการไม่ได้\n  }\n}</pre>\n<h2>Error Handling ใน Async Code</h2>\n<pre>// ✅ Async error handling ที่ถูกต้อง\nasync function loadData() {\n  try {\n    const response = await fetch("/api/data");\n    if (!response.ok) {\n      throw new Error(`HTTP ${response.status}: ${response.statusText}`);\n    }\n    return await response.json();\n  } catch (err) {\n    console.error("โหลดข้อมูลล้มเหลว:", err.message);\n    throw err;\n  }\n}\n\n// Utility: wrap async ให้ return [error, data]\nconst safeAsync = (promise) =>\n  promise.then(data => [null, data]).catch(err => [err, null]);\n\n// การใช้งาน — ไม่ต้องใช้ try/catch ทุกที่\nconst [err, user] = await safeAsync(fetchUser(id));\nif (err) { handleError(err); return; }\nconsole.log(user);</pre>\n<h2>Global Error Handling</h2>\n<pre>// จัดการ unhandled Promise rejections\nwindow.addEventListener("unhandledrejection", (event) => {\n  console.error("Unhandled Promise Rejection:", event.reason);\n  event.preventDefault();\n  errorMonitor.track(event.reason);\n});\n\n// จัดการ runtime errors ทั้งหมด\nwindow.addEventListener("error", (event) => {\n  errorMonitor.track({\n    message: event.message,\n    filename: event.filename,\n    line: event.lineno,\n    col: event.colno\n  });\n});</pre>\n<h2>Debugging ด้วย Chrome DevTools</h2>\n<ul>\n<li><strong>Breakpoints</strong> — คลิกที่หมายเลขบรรทัดใน Sources panel เพื่อหยุด execution</li>\n<li><strong>Conditional Breakpoints</strong> — คลิกขวา เลือก "Add conditional breakpoint" เช่น <code>user.id === 5</code></li>\n<li><strong>Logpoints</strong> — เหมือน console.log แต่ไม่ต้องแก้ code</li>\n<li><strong>Call Stack panel</strong> — ดูว่า function ถูกเรียกจากที่ไหน</li>\n<li><strong>Watch expressions</strong> — monitor ค่าตัวแปรแบบ real-time</li>\n<li><strong>Network tab</strong> — ดู request/response และ timing</li>\n</ul>\n<h2>Performance Profiling</h2>\n<pre>// ใช้ console.time วัด performance เบื้องต้น\nconsole.time("fetchUsers");\nconst users = await fetchAllUsers();\nconsole.timeEnd("fetchUsers"); // "fetchUsers: 234.5ms"\n\n// Performance API สำหรับ precision สูง\nconst start = performance.now();\nheavyComputation();\nconst end = performance.now();\nconsole.log(`ใช้เวลา: ${(end - start).toFixed(2)} ms`);\n\n// Mark และ Measure\nperformance.mark("start-render");\nrenderComponent();\nperformance.mark("end-render");\nperformance.measure("render-time", "start-render", "end-render");\n\n// ดูผลใน DevTools Performance tab หรือ:\nconst measures = performance.getEntriesByType("measure");\nconsole.table(measures);</pre>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> อย่า catch error แล้ว silent ด้วยการ log เพียงอย่างเดียวและทำต่อ นี่คือสาเหตุหลักที่ bugs หลุดไป production ถ้า catch แล้วจัดการได้จริงให้ทำ ถ้าจัดการไม่ได้ให้ re-throw เสมอ และมี global error handler คอยรับไว้เป็น safety net</div>',
  },
  {
    number: 8,
    slug: 'workers',
    emoji: '🚀',
    title: 'Web Workers & Performance',
    content: '<h2>ทำไม JavaScript ถึง Block UI?</h2>\n<p>เพราะ JavaScript ทำงาน <strong>บน main thread เดียวกันกับ UI rendering</strong> เมื่อคุณมี computation ที่หนัก เช่น การประมวลผลภาพ หรือการคำนวณ machine learning ใน browser main thread จะ block ทำให้ UI กระตุก (janky) หรือไม่ตอบสนอง</p>\n<p><strong>Web Workers</strong> แก้ปัญหานี้โดยให้ JavaScript ทำงานบน background thread แยกต่างหาก</p>\n<h2>Web Worker พื้นฐาน</h2>\n<pre>// worker.js — ไฟล์แยกต่างหาก\nself.addEventListener("message", (event) => {\n  const { type, data } = event.data;\n\n  if (type === "COMPUTE") {\n    const result = heavyComputation(data);\n    self.postMessage({ type: "RESULT", result });\n  }\n});\n\nfunction heavyComputation(n) {\n  let result = 0;\n  for (let i = 0; i < n; i++) {\n    result += Math.sqrt(i) * Math.cos(i);\n  }\n  return result;\n}\n\n// main.js — ใน main thread\nconst worker = new Worker("worker.js");\n\nworker.postMessage({ type: "COMPUTE", data: 10_000_000 });\n\nworker.addEventListener("message", (event) => {\n  if (event.data.type === "RESULT") {\n    console.log("ผลลัพธ์:", event.data.result);\n    worker.terminate(); // หยุด worker เมื่อเสร็จ\n  }\n});\n\nworker.addEventListener("error", (err) => {\n  console.error("Worker error:", err.message);\n});</pre>\n<h2>SharedArrayBuffer: Shared Memory</h2>\n<p><strong>SharedArrayBuffer</strong> ช่วยให้ Workers หลายตัวเข้าถึง memory เดียวกันได้โดยไม่ต้องส่ง data ไปมา ทำให้เร็วขึ้นมาก แต่ต้องมี COOP/COEP headers ก่อน</p>\n<pre>// main.js\nconst sharedBuffer = new SharedArrayBuffer(4); // 4 bytes\nconst sharedArray = new Int32Array(sharedBuffer);\nsharedArray[0] = 42;\nworker.postMessage({ buffer: sharedBuffer });\n\n// worker.js\nself.addEventListener("message", ({ data }) => {\n  const arr = new Int32Array(data.buffer);\n  // ใช้ Atomics สำหรับ thread-safe operations\n  Atomics.add(arr, 0, 8); // arr[0] += 8 แบบ atomic\n  console.log(Atomics.load(arr, 0)); // 50\n});</pre>\n<h2>Debouncing &amp; Throttling</h2>\n<pre>// Debounce — รอให้ผู้ใช้หยุดพิมพ์ก่อนค้นหา\nfunction debounce(fn, delay) {\n  let timer;\n  return function(...args) {\n    clearTimeout(timer);\n    timer = setTimeout(() => fn.apply(this, args), delay);\n  };\n}\n\n// Throttle — จำกัดความถี่ในการเรียก function\nfunction throttle(fn, limit) {\n  let lastRun = 0;\n  return function(...args) {\n    const now = Date.now();\n    if (now - lastRun >= limit) {\n      lastRun = now;\n      return fn.apply(this, args);\n    }\n  };\n}\n\nconst debouncedSearch = debounce(searchAPI, 300);\nconst throttledScroll = throttle(handleScroll, 100);</pre>\n<h2>Memory Leak ที่พบบ่อย</h2>\n<pre>// ❌ Event listener ที่ไม่ถูก remove\nfunction setup() {\n  document.addEventListener("click", handleClick); // leak!\n}\n\n// ✅ เก็บ reference ไว้ remove\nfunction setup() {\n  document.addEventListener("click", handleClick);\n  return () => document.removeEventListener("click", handleClick);\n}\n\n// ❌ Closure ที่ถือ reference ขนาดใหญ่\nfunction process(data) {\n  const huge = data; // เก็บทั้ง array\n  return () => huge.length; // Closure ทำให้ไม่ถูก GC\n}\n\n// ✅ เก็บเฉพาะที่จำเป็น\nfunction process(data) {\n  const count = data.length;\n  return () => count; // GC เก็บ array ได้\n}</pre>\n<h2>Lazy Loading &amp; requestAnimationFrame</h2>\n<pre>// Dynamic import — โหลด module เมื่อต้องการจริงๆ\nbutton.addEventListener("click", async () => {\n  const { openModal } = await import("./modal.js");\n  openModal({ title: "รายละเอียด" });\n});\n\n// Intersection Observer สำหรับ lazy load images\nconst observer = new IntersectionObserver((entries) => {\n  entries.forEach(entry => {\n    if (entry.isIntersecting) {\n      const img = entry.target;\n      img.src = img.dataset.src;\n      observer.unobserve(img);\n    }\n  });\n});\ndocument.querySelectorAll("img[data-src]").forEach(img => observer.observe(img));\n\n// ✅ ใช้ requestAnimationFrame สำหรับ animation\nfunction animate(timestamp) {\n  updateState(timestamp);\n  render();\n  requestAnimationFrame(animate);\n}\nrequestAnimationFrame(animate);</pre>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ Chrome DevTools Memory tab ในการตรวจหา memory leaks: ถ่าย Heap Snapshot สองครั้ง (ก่อนและหลังการใช้งาน) แล้วเปรียบเทียบ object ที่เพิ่มขึ้น ถ้า HeapSize เพิ่มขึ้นเรื่อยๆ โดยไม่ลดลง นั่นคือสัญญาณของ memory leak</div>',
  },
  {
    number: 9,
    slug: 'security',
    emoji: '🔐',
    title: 'Security & Best Practices',
    content: '<h2>Security ใน JavaScript: ทำไมต้องสนใจ?</h2>\n<p>ช่องโหว่ด้าน security ส่วนใหญ่ใน web applications ไม่ได้เกิดจากการโจมตีที่ซับซ้อน แต่เกิดจากการเขียน code โดยไม่คำนึงถึง security ตั้งแต่ต้น Developer ที่ดีต้องเขียน code ที่ปลอดภัยเป็น default ไม่ใช่นึกถึงทีหลัง</p>\n<h2>Cross-Site Scripting (XSS)</h2>\n<p>XSS คือการที่ attacker inject JavaScript ที่เป็นอันตรายเข้าไปใน page ของเรา ทำให้ code นั้น execute ใน browser ของผู้ใช้คนอื่น</p>\n<pre>// ❌ อันตราย: innerHTML ด้วยข้อมูลจาก user\nconst userInput = "&lt;img src=x onerror=\'alert(document.cookie)\'&gt;";\ndocument.getElementById("output").innerHTML = userInput; // XSS!\n\n// ✅ ปลอดภัย: ใช้ textContent\ndocument.getElementById("output").textContent = userInput;\n\n// ✅ ปลอดภัย: sanitize ก่อนใส่ HTML\nimport DOMPurify from "dompurify";\nconst clean = DOMPurify.sanitize(userInput);\ndocument.getElementById("output").innerHTML = clean;\n\n// ✅ ปลอดภัย: validate URL scheme\nfunction safeUrl(url) {\n  try {\n    const parsed = new URL(url);\n    return ["http:", "https:"].includes(parsed.protocol) ? url : "#";\n  } catch {\n    return "#";\n  }\n}</pre>\n<h2>Cross-Site Request Forgery (CSRF)</h2>\n<p>CSRF คือการที่ attacker หลอกให้ browser ของผู้ใช้ที่ login อยู่แล้วส่ง request ไปยัง server โดยที่ผู้ใช้ไม่รู้ตัว</p>\n<ul>\n<li><strong>CSRF Token</strong> — server ส่ง unique token มากับ form ทุกครั้ง และ verify เมื่อรับ request</li>\n<li><strong>SameSite Cookie</strong> — ตั้งค่า <code>SameSite=Strict</code> หรือ <code>SameSite=Lax</code> บน cookies</li>\n<li><strong>Double Submit Cookie</strong> — ส่ง token ทั้งใน cookie และใน request header</li>\n</ul>\n<pre>// เพิ่ม CSRF token ใน headers ทุก request\nasync function securePost(url, data) {\n  const csrfToken = document.querySelector(\'meta[name="csrf-token"]\').content;\n\n  return fetch(url, {\n    method: "POST",\n    credentials: "same-origin",\n    headers: {\n      "Content-Type": "application/json",\n      "X-CSRF-Token": csrfToken,\n    },\n    body: JSON.stringify(data),\n  });\n}</pre>\n<h2>Content Security Policy (CSP)</h2>\n<p>CSP เป็น HTTP header ที่บอก browser ว่าจะโหลด resources จากที่ไหนได้บ้าง ช่วยป้องกัน XSS ได้อย่างมีประสิทธิภาพ</p>\n<pre>// ตัวอย่าง CSP Header (ตั้งค่าที่ server)\nContent-Security-Policy:\n  default-src \'self\';\n  script-src \'self\' https://cdn.trusted.com;\n  style-src \'self\' \'unsafe-inline\';\n  img-src \'self\' data: https:;\n  connect-src \'self\' https://api.example.com;\n  frame-ancestors \'none\';\n  upgrade-insecure-requests;</pre>\n<h2>Secure Coding Patterns</h2>\n<h3>1. อย่าเก็บ sensitive data ใน localStorage</h3>\n<pre>// ❌ อันตราย: JWT ใน localStorage\nlocalStorage.setItem("token", jwtToken);\n// เข้าถึงได้ด้วย JavaScript ใดๆ รวมถึง XSS payload\n\n// ✅ ปลอดภัยกว่า: httpOnly cookie\n// ตั้งค่าโดย server:\n// Set-Cookie: token=xxx; HttpOnly; Secure; SameSite=Strict\n// JavaScript เข้าถึงไม่ได้เลย</pre>\n<h3>2. Validate Input ทั้ง Client และ Server</h3>\n<pre>// Client-side: UX เท่านั้น — ห้ามเชื่อถือ\nfunction validateEmail(email) {\n  return /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(email);\n}\n\nfunction sanitizeInput(input) {\n  return String(input).trim().slice(0, 1000);\n}\n\n// ⚠️ Server-side validation คือสิ่งสำคัญที่สุด\n// อย่าเชื่อ input จาก client เลย แม้แต่นิดเดียว</pre>\n<h3>3. อย่า hardcode secrets</h3>\n<pre>// ❌ ห้ามทำเด็ดขาด\nconst apiKey = "sk-1234567890abcdef";\nconst dbPassword = "mypassword123";\n\n// ✅ ใช้ environment variables\nconst apiKey = process.env.API_KEY;\nconst dbPassword = process.env.DB_PASSWORD;</pre>\n<h2>Security Checklist ก่อน Deploy</h2>\n<ul>\n<li>✅ ไม่มีการใช้ <code>innerHTML</code> กับ user input โดยไม่ sanitize</li>\n<li>✅ ไม่มี hardcoded secrets, API keys, หรือ passwords ใน code</li>\n<li>✅ ทุก user input ผ่านการ validate และ sanitize</li>\n<li>✅ Sensitive cookies มี <code>HttpOnly</code>, <code>Secure</code>, <code>SameSite</code></li>\n<li>✅ มี CSP header ที่เข้มงวดพอ</li>\n<li>✅ Dependencies ไม่มี known vulnerabilities (ตรวจด้วย <code>npm audit</code>)</li>\n<li>✅ Error messages ไม่เปิดเผย internal details ให้ผู้ใช้</li>\n<li>✅ HTTPS ทุก endpoint</li>\n<li>✅ Rate limiting บน API</li>\n<li>✅ Dependency versions อัพเดทสม่ำเสมอ</li>\n</ul>\n<pre>// ตรวจสอบ dependencies ที่มีช่องโหว่\nnpm audit\nnpm audit fix\n\n// หรือใช้ snyk สำหรับการตรวจสอบที่ละเอียดกว่า\nnpx snyk test</pre>\n<div class=\'tip-box\'><strong>💡 Pro Tip:</strong> Security ไม่ใช่ feature ที่เพิ่มทีหลัง แต่เป็น mindset ที่ต้องมีตั้งแต่เริ่ม เริ่มจากสิ่งง่ายๆ ก่อน: run <code>npm audit</code> ทุกสัปดาห์, ไม่ใช้ <code>innerHTML</code> กับ user content, และอย่าเก็บ secrets ใน code repository เพียงสามสิ่งนี้ก็ป้องกัน vulnerabilities ส่วนใหญ่ได้แล้ว</div>',
  },
];