export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '✈️',
    title: 'Airflow Advanced: ต่อจาก DE101',
    content: '<h2>ทบทวน Airflow พื้นฐาน</h2><p>ใน DE101 เราเรียนรู้ Airflow basics ได้แก่ DAGs, Tasks, Operators, และ Scheduling ใน course นี้เราจะ deep dive เข้าไปใน advanced features ที่ทำให้ Airflow production-ready ได้แก่ executors ขั้นสูง, custom operators, XCom, sensors, dynamic DAGs, connections management และ monitoring</p><p>Airflow ในปัจจุบัน version 2.x มี TaskFlow API ที่ทำให้เขียน DAGs ด้วย Python functions แบบ decorator ง่ายกว่าเดิมมาก และมี built-in support สำหรับ dataset-based scheduling ที่ elegant กว่า cron-based scheduling แบบ traditional</p><h2>Production Airflow vs Development</h2><p>Development Airflow มักใช้ SequentialExecutor หรือ LocalExecutor รัน tasks ทีละตัวหรือแบบ parallel บน machine เดียวกับ scheduler Production Airflow ต้องการ CeleryExecutor หรือ KubernetesExecutor เพื่อ distribute tasks ข้าม multiple workers สำหรับ scale</p><p>ความแตกต่างที่สำคัญอื่นๆ: Production ต้องมี monitoring, alerting, SLA tracking, และ proper logging Production ใช้ external databases (RDS, Cloud SQL) ไม่ใช่ SQLite, มี load balancer หน้า webserver สำหรับ High Availability</p><h2>Course Overview</h2><p>เนื้อหาใน course นี้ครอบคลุม: Executor types และการเลือกให้เหมาะกับ use case, การเขียน Custom Operators สำหรับ business-specific logic, XCom สำหรับส่งข้อมูลระหว่าง tasks, Sensors สำหรับรอ external events, Dynamic DAG generation, Connections และ Variables management, TaskFlow API สมัยใหม่</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> อ่าน Airflow CHANGELOG ก่อน upgrade version เสมอ Airflow มี breaking changes ระหว่าง major versions และบาง provider versions ต้องการ Airflow version ขั้นต่ำที่แตกต่างกัน ควร test ใน staging ก่อน upgrade production</div><p>Airflow community มีขนาดใหญ่มาก มี providers สำหรับ AWS, GCP, Azure, Databricks, Snowflake, dbt และอื่นๆ อีกมาก ทำให้ integrate กับ services ต่างๆ ได้ง่ายโดยไม่ต้องเขียน code จาก scratch และได้รับ updates จาก community</p>'
  },
  {
    number: 1,
    slug: 'executors',
    emoji: '⚡',
    title: 'Executor Types: Sequential, Local, Celery, K8s',
    content: '<h2>Executor คืออะไร?</h2><p>Executor กำหนดว่า Airflow จะ run tasks อย่างไร SequentialExecutor รัน task ทีละงานไม่ parallel (เหมาะแค่ testing เท่านั้น), LocalExecutor รัน tasks parallel บน machine เดียวกับ scheduler, CeleryExecutor distribute tasks ไปยัง worker machines, KubernetesExecutor spawn Pod ต่อ task</p><p>การเลือก Executor ขึ้นอยู่กับ scale ของ workload: ถ้ามีไม่กี่ DAGs และ tasks ต่อวัน LocalExecutor เพียงพอ ถ้ามีหลาย 100 tasks concurrent ต้องใช้ CeleryExecutor หรือ KubernetesExecutor เพื่อ scale horizontally</p><h2>CeleryExecutor</h2><p>CeleryExecutor ใช้ Celery distributed task queue ร่วมกับ message broker (Redis หรือ RabbitMQ) Scheduler push tasks เข้า queue, Workers pull tasks ออกมา execute สามารถ scale workers ได้ง่ายโดยการเพิ่ม worker machines หรือ containers</p><p>ข้อดี CeleryExecutor: mature technology, horizontal scaling ง่าย, มี Flower UI สำหรับ monitoring workers ข้อเสีย: ต้องดูแล Redis/RabbitMQ เพิ่ม, workers ต้องมี same environment (same packages) ทำให้ manage ยากเมื่อ tasks ต้องการ different environments</p><h2>KubernetesExecutor</h2><p>KubernetesExecutor สร้าง K8s Pod ต่อ task แต่ละ task run ใน isolated environment ไม่ share resources กับ tasks อื่น เหมาะมากเมื่อ tasks ต้องการ different Python packages หรือ resource requirements ที่แตกต่างกันอย่างมาก</p><p>ข้อดี: ไม่มี idle workers ประหยัด cost, isolation สมบูรณ์ per task, resource limits ต่อ task, integrate กับ K8s secrets ข้อเสีย: Pod startup time 20-30 วินาที ทำให้ latency สูง, ต้องมี K8s cluster, complex setup กว่า Celery</p><h2>CeleryKubernetesExecutor</h2><p>Hybrid executor ที่ใช้ทั้ง Celery และ Kubernetes: Light tasks รันบน Celery workers (เร็ว, low overhead), Heavy tasks รันบน K8s pods (isolated, different environments) กำหนดได้ด้วย task-level queue annotation มี flexibility สูงสุดแต่ complex สุดด้วย</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> เริ่มต้นด้วย LocalExecutor สำหรับ teams ขนาดเล็กถึงกลาง (ไม่เกิน 50 DAGs, 1000 tasks/วัน) ง่ายกว่า CeleryExecutor มากและ performance เพียงพอสำหรับ workloads ส่วนใหญ่ ค่อย migrate เมื่อ scale จริงๆ</div><p>Executor tuning: สำหรับ CeleryExecutor ตั้ง worker_concurrency ตามจำนวน CPU cores ของ worker machines สำหรับ LocalExecutor ตั้ง parallelism และ dag_concurrency ให้เหมาะกับ RAM ที่มี ไม่ควร overcommit resources</p>'
  },
  {
    number: 2,
    slug: 'custom-operators',
    emoji: '🔧',
    title: 'เขียน Custom Operators',
    content: '<h2>ทำไมต้องเขียน Custom Operator?</h2><p>Airflow มี built-in operators หลายร้อยตัว แต่บางครั้ง business logic เฉพาะทางต้องการ operator ที่ custom-built เช่น ส่ง data ไป internal API, execute business-specific validation, หรือ integrate กับ legacy systems ที่ไม่มี official provider</p><p>Custom operators ช่วยให้ DAG code สะอาดและ reusable แทนที่จะ copy-paste Python code ทั่วทุก DAG ให้ encapsulate logic ไว้ใน operator ที่ใช้ซ้ำได้ ทำให้ DAGs อ่านง่ายขึ้นและ logic เปลี่ยนที่เดียวส่งผลทุก DAG ที่ใช้</p><h2>โครงสร้าง Custom Operator</h2><p>Inherit จาก BaseOperator: class MyCustomOperator(BaseOperator) ต้องมี __init__ method สำหรับรับ parameters และ execute method ที่ implement logic จริง execute รับ context argument ที่มี runtime information เช่น execution_date, dag_id, task_id ที่ใช้สำหรับ logging และ XCom</p><p>ตัวอย่าง SlackAlertOperator: inherit BaseOperator, รับ webhook_url และ message ใน __init__, ใน execute method ใช้ requests.post เพื่อส่ง message ไปยัง Slack webhook ง่ายและ reusable ทุก DAG ที่ต้องการ Slack alerts</p><h2>Template Fields</h2><p>template_fields tuple กำหนด parameters ที่รองรับ Jinja templating ทำให้ใช้ variables เช่น {{ ds }}, {{ execution_date }}, {{ params.bucket }} ใน parameter values ได้ ตัวอย่าง: template_fields = (\'message\', \'bucket_name\') ทำให้ message dynamic ตาม execution date</p><p>template_ext tuple กำหนด file extensions ที่ render เป็น template เช่น template_ext = (\'.sql\', \'.json\') ทำให้ load SQL file แล้ว render Airflow variables ก่อน execute เหมาะสำหรับ SQL operators ที่ต้องการ dynamic queries</p><h2>Hook ใน Custom Operators</h2><p>Hook คือ abstraction layer สำหรับ connection ไปยัง external systems เช่น PostgresHook, S3Hook, BigQueryHook Custom operators ควร use hooks แทนที่จะ manage connections โดยตรง ทำให้ credentials manage ผ่าน Airflow Connections UI ได้ ไม่ hardcode</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> วาง custom operators ใน plugins/ folder ของ Airflow หรือ install เป็น Python package แยกต่างหาก การ install เป็น package ดีกว่าเพราะ versioned, testable, และ shareable ข้าม Airflow deployments ต่างๆ</div><p>Unit testing custom operators: ใช้ pytest กับ mock objects เพื่อ test execute method โดยไม่ต้องมี Airflow running Mock context object ด้วย dictionary ที่มี keys ที่จำเป็น การมี tests ทำให้มั่นใจได้ว่า operator ทำงานถูกต้องก่อน deploy</p>'
  },
  {
    number: 3,
    slug: 'xcoms',
    emoji: '📨',
    title: 'XCom: ส่งข้อมูลระหว่าง Tasks',
    content: '<h2>XCom คืออะไร?</h2><p>XCom (Cross-Communication) คือ mechanism ของ Airflow สำหรับส่ง small pieces of data ระหว่าง tasks ใน DAG เดียวกัน XCom values ถูกเก็บใน Airflow metadata database และสามารถ retrieve ได้จาก downstream tasks ทำให้ tasks แชร์ข้อมูลกันได้โดยไม่ต้องผ่าน external storage</p><p>Use cases ของ XCom: ส่ง file path ที่ extract ไปยัง transform task, ส่ง API response ไปยัง task ถัดไป, ส่ง row counts สำหรับ validation, ส่ง configuration values ที่คำนวณ dynamically ขณะ runtime</p><h2>Push และ Pull XCom</h2><p>Push XCom: task_instance.xcom_push(key=\'my_key\', value=\'my_value\') หรือ return value จาก execute method จะถูก auto-push ด้วย key=\'return_value\' Pull XCom: task_instance.xcom_pull(task_ids=\'upstream_task\', key=\'my_key\') ดึงค่าที่ task อื่น push ไว้</p><p>ใน TaskFlow API, XCom ทำงาน automatically: return value จาก @task function ถูก push อัตโนมัติ, ส่ง return value เป็น argument ไปยัง downstream task function Airflow handle XCom push/pull ให้โดยอัตโนมัติ ทำให้ code clean และ readable มากขึ้น</p><h2>XCom Limitations</h2><p>XCom ถูกออกแบบสำหรับ small data เท่านั้น (metadata, file paths, counts ไม่เกินไม่กี่ KB) ไม่เหมาะสำหรับ large datasets เพราะเก็บใน metadata database ซึ่งจะ slow และ bloat database ถ้า store data ขนาดใหญ่</p><p>สำหรับ large data ใช้ pattern: Task A เขียนข้อมูลไปยัง S3/GCS, push file path ผ่าน XCom, Task B pull file path จาก XCom แล้วอ่านไฟล์โดยตรง pattern นี้เป็น best practice สำหรับ data pipelines ที่ต้องการ pass large data</p><h2>Custom XCom Backends</h2><p>Airflow 2.x รองรับ custom XCom backends ทำให้ store XCom values ใน S3, GCS, หรือ custom storage แทน metadata database เหมาะสำหรับกรณีที่ต้องการ store medium-sized data ผ่าน XCom โดยไม่ต้องเปลี่ยน pattern การเขียน code</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ระวัง XCom ที่ accumulate ใน metadata database ตั้ง xcom_backend หรือ clean up XCom regularly ด้วย airflow db clean command หรือ DAG ที่ runs nightly เพื่อ prune old XCom entries ป้องกัน database bloat</div><p>XCom serialization: ค่าที่ push ต้องสามารถ serialize เป็น JSON ได้ Python primitives (str, int, list, dict) ทำงานได้ทันที สำหรับ custom objects ต้อง implement serialization เอง หรือ convert เป็น dict ก่อน push</p>'
  },
  {
    number: 4,
    slug: 'sensors',
    emoji: '📡',
    title: 'Sensors: รอ File, API, และ External Events',
    content: '<h2>Sensors ใน Airflow คืออะไร?</h2><p>Sensor เป็น operator พิเศษที่รอจนกระทั่ง condition หนึ่งเป็นจริง แล้วจึง complete Sensors ใช้สำหรับ: รอ file ปรากฏใน S3/GCS, รอ API endpoint return status 200, รอ database query return rows, รอ external DAG finish, รอ time-based conditions ทำให้ pipeline event-driven</p><p>Sensors เป็นพื้นฐานของ event-driven pipelines ทำให้ pipelines รอ upstream data พร้อมก่อนเริ่มประมวลผล แทนที่จะ hardcode delays หรือ schedule ตาม assumption ว่า data จะพร้อมเมื่อไหร่ ทำให้ pipeline robust กว่า</p><h2>Built-in Sensors</h2><p>FileSensor: รอ file ที่ path ที่กำหนด, S3KeySensor: รอ object ใน S3 bucket, HttpSensor: รอ HTTP endpoint return expected response, ExternalTaskSensor: รอ task ใน DAG อื่น complete, TimeDeltaSensor: รอ time interval, DateTimeSensor: รอจน datetime ที่กำหนด</p><p>ตัวอย่าง S3KeySensor: กำหนด task_id, bucket_name, bucket_key ที่ใช้ Jinja template เช่น data/{{ ds }}/file.csv สำหรับ date-based paths, aws_conn_id, poke_interval 60 วินาที, timeout 3600 วินาที รอ file สำหรับวันนั้นๆ อัตโนมัติ</p><h2>Sensor Modes</h2><p>poke mode (default): Sensor occupy worker slot ตลอดเวลาที่รอ ถ้ามี sensors หลายตัวรอพร้อมกันจะ consume workers ทั้งหมด reschedule mode: Sensor release worker slot ระหว่างรอ แล้ว reschedule ตาม poke_interval ประหยัด worker slots มาก เหมาะสำหรับ sensors ที่รอนาน</p><p>ตั้ง mode=\'reschedule\' สำหรับ sensors ที่ timeout นานเกิน 1 ชั่วโมง: ป้องกัน worker starvation เมื่อมีหลาย sensors รอพร้อมกัน โดยเฉพาะใน CeleryExecutor ที่ worker pool มีจำกัด</p><h2>Custom Sensors</h2><p>Inherit จาก BaseSensorOperator แล้ว implement poke method ที่ return True เมื่อ condition met: class MyCustomSensor ตรวจสอบ API endpoint response, database row count, หรือ custom business condition ใดๆ ก็ได้ที่ built-in sensors ไม่รองรับ</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> กำหนด timeout ทุก sensor เสมอ ถ้าไม่กำหนด sensor จะรอไปเรื่อยๆ อาจ block pipeline นานหลายวัน ตั้ง timeout ที่สมเหตุสมผลและ handle timeout ด้วย on_failure_callback เพื่อ notify ทีม</div><p>Sensor deadlock: ถ้า sensors ใช้ poke mode ทั้งหมดและ worker pool หมด DAG runs ใหม่จะ queue ไม่ได้ เพราะ sensors เดิม occupy workers อยู่ ใช้ reschedule mode หรือ เพิ่ม worker pool สำหรับ sensors โดยเฉพาะ</p>'
  },
  {
    number: 5,
    slug: 'dynamic-dags',
    emoji: '🔄',
    title: 'Dynamic DAGs Generation',
    content: '<h2>Dynamic DAGs คืออะไร?</h2><p>Dynamic DAGs คือ DAGs ที่สร้างขึ้นโดย programmatic โดยใช้ Python code แทนที่จะ hard-code ทุก DAG ด้วยมือ เหมาะสำหรับกรณีที่มี pattern เดียวกันสำหรับ multiple datasets, clients, หรือ regions ช่วยลด code duplication อย่างมาก</p><p>Use cases: สร้าง ETL DAG สำหรับแต่ละ country ใน list, สร้าง DAG สำหรับแต่ละ table ใน database, สร้าง DAG ตาม config file ใน S3, generate tasks dynamically ตาม data partitions ทั้งหมดนี้ช่วยให้ scale pipeline โดยไม่ต้องเพิ่ม code</p><h2>Pattern 1: Loop-based DAG Generation</h2><p>วิธีง่ายที่สุดคือ loop ใน Python สร้างหลาย DAGs: for country in list of countries สร้าง DAG object ด้วย dag_id ที่ unique เช่น etl_th, etl_sg, etl_my กำหนด tasks และ dependencies แล้ววาง DAG objects ใน global scope เพื่อให้ Airflow discover</p><p>ข้อควรระวัง: DAGs ทั้งหมดจะปรากฏใน UI อาจ cluttered ถ้ามีมาก performance ของ DAG parser อาจช้าถ้าสร้าง DAGs มากเกินไป consider ใช้ Airflow 2.3+ Dynamic Task Mapping แทนสำหรับ cases ที่เหมาะ</p><h2>Pattern 2: Config-driven DAGs</h2><p>อ่าน config จาก YAML/JSON file แล้วสร้าง DAGs: อ่านไฟล์ config, loop สร้าง DAG ตาม pipeline definitions แต่ละ item ใน config กลายเป็น DAG หนึ่งตัว ทำให้ non-technical team members สามารถ add DAGs ใหม่ผ่าน config file โดยไม่ต้องแก้ Python code</p><p>Config-driven approach ดีกว่า hardcoded เพราะ: เพิ่ม pipeline ใหม่โดยไม่ต้องแก้ Python code, ง่ายต่อการ review changes เพราะเป็น YAML, สามารถ store config ใน database หรือ S3 และ refresh dynamically ทำให้ real-time configuration changes</p><h2>Dynamic Task Mapping (Airflow 2.3+)</h2><p>Dynamic Task Mapping ทำให้สร้าง task instances dynamically ตาม runtime data: process.expand(item=get_items()) โดย get_items() return list ของ values Airflow สร้าง parallel task instances ให้อัตโนมัติ ไม่ต้องรู้จำนวนล่วงหน้าเหมาะสำหรับ variable-length inputs</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ Dynamic Task Mapping แทน dynamic DAG generation เมื่อ pattern คือ "รัน task เดิมกับหลาย inputs" เพราะ cleaner กว่ามาก มี 1 DAG ที่ expandable แทนที่จะมีหลาย DAGs ทำให้ monitoring และ debugging ง่ายกว่า</div><p>DAG factory pattern: สร้าง factory function ที่รับ config และ return DAG object ทำให้ reuse DAG template code ได้ง่าย และ test DAG creation logic ด้วย unit tests ก่อน deploy ป้องกัน logic errors ใน DAG generation</p>'
  },
  {
    number: 6,
    slug: 'connections',
    emoji: '🔌',
    title: 'Connections และ Variables Management',
    content: '<h2>Connections ใน Airflow</h2><p>Connections เก็บ credentials และ connection details สำหรับ external systems เช่น databases, cloud services, APIs Airflow encrypt connections ใน metadata database ด้วย Fernet key สามารถ manage ผ่าน Web UI, CLI, หรือ Environment Variables ได้หลายวิธี</p><p>Connection ประกอบด้วย: Conn Id (unique identifier), Conn Type (postgres, s3, http, etc.), Host, Port, Schema/Database, Login, Password, Extra (JSON สำหรับ additional params) operators ใช้ conn_id เพื่อ lookup connection details ทำให้ credentials ไม่อยู่ใน code</p><h2>Variables ใน Airflow</h2><p>Variables เก็บ configuration values ที่ใช้ทั่ว DAGs เช่น bucket names, API endpoints, feature flags ใช้ Variable.get(\'my_var\') ใน DAG code หรือ Jinja template syntax ใน templates Variables สามารถ JSON-serialize เพื่อเก็บ complex objects</p><p>ระวัง: Variable.get() ทำ database query ทุกครั้งที่เรียก ถ้าเรียกใน task มากๆ อาจกระทบ performance ใช้ Variable.get() ใน execute() ไม่ใช่ top-level DAG code เพราะ top-level ถูก execute ทุกครั้ง Airflow parse DAG</p><h2>Secrets Backend</h2><p>สำหรับ production ใช้ secrets backend แทน Airflow database: AWS Secrets Manager, GCP Secret Manager, HashiCorp Vault ตั้งค่าใน airflow.cfg ระบุ backend class Airflow จะ lookup connections/variables จาก secrets backend อัตโนมัติ</p><p>ข้อดีของ secrets backend: rotation credentials ได้ง่าย, audit log ว่าใครเข้าถึง secrets, ไม่ต้อง re-deploy Airflow เมื่อ rotate passwords, comply กับ security requirements ขององค์กรและ compliance frameworks</p><h2>Environment Variable Connections</h2><p>สามารถ define connections ผ่าน env vars: AIRFLOW_CONN_MY_POSTGRES=postgresql://user:pass@host:5432/dbname Airflow auto-parse เมื่อ start ดีสำหรับ development หรือ container environments ที่ inject env vars ผ่าน K8s secrets หรือ Docker secrets</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ตั้งชื่อ connections และ variables ด้วย naming convention ที่ชัดเจน เช่น gcp_prod_bigquery, aws_staging_s3 ระบุ environment และ service ทำให้รู้ทันทีว่า connection ไหนใช้กับ environment ไหน ป้องกัน connect production กับ staging โดยไม่ตั้งใจ</div><p>Connection testing: ใช้ airflow connections test CONN_ID command หรือ UI เพื่อ test connection ก่อนใช้ใน DAG ช่วย debug connection issues ได้เร็ว ไม่ต้องรอ DAG run fail ซึ่งอาจสายเกินไป</p>'
  },
  {
    number: 7,
    slug: 'taskflow',
    emoji: '🌊',
    title: 'TaskFlow API สมัยใหม่',
    content: '<h2>TaskFlow API คืออะไร?</h2><p>TaskFlow API (เพิ่มใน Airflow 2.0) ช่วยให้เขียน DAGs ด้วย Python decorators แทนที่จะสร้าง Operator objects โดยตรง ทำให้ code สะอาดกว่า, Pythonic กว่า, และ XCom จัดการให้อัตโนมัติ ลด boilerplate code ลงมาก</p><p>ก่อน TaskFlow API ต้องสร้าง PythonOperator ระบุ python_callable, op_kwargs, และจัดการ XCom เอง ด้วย TaskFlow API แค่ใช้ @task decorator บน Python function ธรรมดา Airflow จัดการทุกอย่างให้รวมถึง dependencies</p><h2>ตัวอย่าง TaskFlow DAG</h2><p>ใช้ @dag decorator บน function ที่ return DAG structure, @task decorator บน functions ที่เป็น tasks ผลลัพธ์จาก function หนึ่งส่งเป็น argument ให้ function ถัดไปโดยตรง Airflow แปล return values เป็น XCom push/pull อัตโนมัติ</p><p>ข้อสังเกต: XCom ทำงานผ่าน function arguments โดยอัตโนมัติ, dependencies กำหนดโดย function call order, ไม่ต้องระบุ task_id (ใช้ function name), ไม่ต้องใช้ >> operator สำหรับ simple linear pipelines</p><h2>Type Hints และ XCom</h2><p>TaskFlow API ใช้ type hints สำหรับ XCom serialization ถ้า return type เป็น dict หรือ list ใช้ default JSON serialization ถ้าต้องการ push หลาย values ใช้ multiple_outputs=True ใน @task decorator เพื่อ push each key เป็น separate XCom</p><p>@task(multiple_outputs=True) จะ push XCom ด้วย keys จาก dict keys แยกกัน downstream tasks สามารถ reference แต่ละ key ได้ เหมาะสำหรับ tasks ที่ return complex configuration objects</p><h2>Mixing TaskFlow และ Traditional Operators</h2><p>TaskFlow tasks สามารถ mix กับ traditional operators ใน DAG เดียวกัน ใช้ .output property เพื่อ reference XCom output ของ traditional operator combine ความยืดหยุ่นของทั้งสองรูปแบบ ทำให้ migrate DAGs เดิมมาใช้ TaskFlow ได้ทีละส่วน</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ @task.virtualenv decorator เพื่อรัน task ใน isolated Python virtual environment มีประโยชน์เมื่อ task ต้องการ packages ที่ conflict กับ Airflow environment เช่น different version ของ pandas หรือ tensorflow</div><p>Dataset-based scheduling ใน Airflow 2.4+: ใช้ Dataset objects ใน schedule parameter แทน cron DAG จะ trigger อัตโนมัติเมื่อ dataset ถูก update โดย upstream DAG สร้าง event-driven pipelines ที่ elegant และ decoupled</p>'
  },
  {
    number: 8,
    slug: 'monitoring',
    emoji: '📊',
    title: 'SLA, Monitoring, และ Alerting',
    content: '<h2>Monitoring ใน Airflow</h2><p>Production Airflow ต้องมี monitoring ที่ครอบคลุม เพื่อ detect failures เร็ว, track performance, และปฏิบัติตาม SLAs Airflow มีหลาย mechanisms สำหรับ monitoring: SLA misses, callbacks, metrics integration กับ Prometheus/StatsD</p><p>Metrics ที่สำคัญต้อง monitor: DAG run success/failure rate, task duration trends, scheduler heartbeat lag, worker queue size, XCom table size, metadata database size, executor slot utilization เหล่านี้บ่งบอก health ของ Airflow deployment</p><h2>SLA (Service Level Agreement)</h2><p>SLA ใน Airflow กำหนดว่า task ต้อง complete ภายใน time หลังจาก execution date ตั้ง sla parameter ใน task เป็น timedelta เช่น 2 ชั่วโมง ถ้า task ไม่ complete ภายในเวลาที่กำหนด Airflow จะ trigger SLA miss callback และ send email แจ้งเตือน</p><p>SLA miss callback ที่ DAG level จะถูกเรียกเมื่อมี SLA miss รับ parameters เช่น dag, task_list, blocking_task_list ใช้เพื่อ notify ทีมผ่าน Slack หรือ PagerDuty และ trigger remediation actions</p><h2>Callbacks</h2><p>on_failure_callback: เรียกเมื่อ task fail, on_success_callback: เรียกเมื่อ task สำเร็จ, on_retry_callback: เรียกเมื่อ task retry, on_skipped_callback: เรียกเมื่อ task skip สามารถใช้ callbacks ส่ง notifications ไปยัง Slack, PagerDuty, หรือ ticketing systems</p><p>ตัวอย่าง Slack failure callback: สร้าง function ที่รับ context, ดึง task_instance.task_id และ dag.dag_id จาก context, ส่ง formatted message ไปยัง Slack webhook ทำให้ทีมรู้ทันทีเมื่อมี failure</p><h2>StatsD และ Prometheus Integration</h2><p>Airflow ส่ง metrics ไปยัง StatsD: เปิดใช้ใน airflow.cfg โดยตั้ง statsd_on = True, statsd_host, statsd_port ใช้ statsd_exporter เพื่อ convert StatsD metrics เป็น Prometheus format แล้ว visualize ใน Grafana dashboard</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ตั้ง dagbag_import_error alert เพื่อรับแจ้งเมื่อ DAG มี import error ทำให้รู้ทันทีเมื่อ DAG ใหม่ที่ deploy มีปัญหา syntax หรือ import แทนที่จะรอให้ scheduled run fail ซึ่งอาจเป็นชั่วโมงต่อมา</div><p>Airflow Health Endpoint: /health endpoint return JSON status ของ scheduler และ metadatabase ใช้ health check ใน load balancer หรือ K8s liveness probe เพื่อ auto-restart เมื่อ Airflow scheduler หยุดทำงาน</p>'
  },
  {
    number: 9,
    slug: 'dbt-airflow',
    emoji: '🔗',
    title: 'Airflow + dbt Pipeline',
    content: '<h2>Integration Pattern</h2><p>การ integrate Airflow กับ dbt คือ pattern ที่ใช้กันอย่างแพร่หลายใน modern data stacks Airflow orchestrate timing, dependencies, และ scheduling ขณะที่ dbt handle SQL transformations การ combine ทั้งสองทำให้ได้ complete ELT pipeline ที่ robust และ maintainable</p><p>สองแนวทางหลัก: รัน dbt CLI ผ่าน BashOperator/PythonOperator (ง่าย, flexible), ใช้ official dbt providers (astronomer-cosmos, dbt-airflow) ที่ integrate deeper กับ Airflow แต่ละแนวทางมี trade-offs ที่ต้องพิจารณา</p><h2>รัน dbt ผ่าน BashOperator</h2><p>วิธีง่ายสุด: BashOperator กับ bash_command ที่รัน dbt run พร้อม --profiles-dir และ --project-dir ตั้ง environment variables ผ่าน env parameter ทำงานได้ดีสำหรับ simple cases แต่ไม่มี task-level visibility ของ dbt models</p><p>ปัญหาของ BashOperator approach: ทั้ง dbt run เป็น 1 Airflow task ทำให้ไม่รู้ว่า model ไหน fail, ไม่สามารถ retry specific model ได้, ไม่มี model-level dependencies ใน Airflow graph ทำให้ debug ยากมาก</p><h2>Cosmos: dbt Airflow Provider</h2><p>astronomer-cosmos package สร้าง Airflow tasks จาก dbt models อัตโนมัติ แต่ละ dbt model กลายเป็น Airflow task พร้อม dependencies ที่ถูกต้อง ทำให้เห็น model-level status ใน Airflow UI, retry เฉพาะ model ที่ fail ได้ ทำให้ monitoring และ debugging ง่ายขึ้นมาก</p><p>ติดตั้ง astronomer-cosmos แล้วใช้ DbtDag หรือ DbtTaskGroup พร้อม ProjectConfig และ ProfileConfig ระบุ path ไปยัง dbt project และ profile Cosmos จะ parse dbt manifest แล้วสร้าง task graph อัตโนมัติ</p><h2>dbt + Airflow Best Practices</h2><p>แยก concerns ชัดเจน: Airflow รับผิดชอบ extraction (Fivetran/Airbyte triggers, API calls), dbt รับผิดชอบ transformations ทั้งหมด ไม่ผสม Python transformation ใน Airflow กับ SQL ใน dbt โดยไม่จำเป็น ทำให้ codebase สะอาดขึ้น</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ dbt source freshness ร่วมกับ Airflow ExternalTaskSensor ตรวจสอบว่า upstream data พร้อมก่อนรัน dbt run สร้าง pipeline ที่ event-driven แทนที่จะ hardcode schedule times ที่อาจ miss data</div><p>Error handling: ถ้า dbt model fail ควร notify ทีม data, log error details, และ block downstream tasks ที่ depend on failed models การ integrate กับ dbt Cloud API ทำให้ดู run logs ได้จากทั้ง Airflow และ dbt Cloud UI</p>'
  },
  {
    number: 10,
    slug: 'production-tips',
    emoji: '🚀',
    title: 'Production Tips: Backfill และ Catchup',
    content: '<h2>Backfill ใน Airflow</h2><p>Backfill คือการรัน DAG สำหรับ historical dates ย้อนหลัง ใช้เมื่อ: เพิ่ม DAG ใหม่และต้องการ populate historical data, แก้ bug ใน DAG logic และต้องการ reprocess data, เพิ่ม column ใหม่ใน transformation ที่ต้องการ historical data</p><p>รัน backfill: airflow dags backfill -s 2024-01-01 -e 2024-03-31 my_dag_id Airflow จะสร้าง DAG runs สำหรับทุก schedule interval ระหว่าง start date และ end date ระวัง: backfill ขนาดใหญ่อาจ overwhelm downstream systems และ consume worker resources ทั้งหมด</p><h2>Catchup</h2><p>catchup=True (default): เมื่อ DAG เริ่มทำงานครั้งแรก Airflow จะ create runs สำหรับ intervals ที่ผ่านมาตั้งแต่ start_date จนถึงปัจจุบัน อาจทำให้เกิด run จำนวนมากพร้อมกัน catchup=False: สร้างเฉพาะ run สำหรับ current interval เหมาะสำหรับ DAGs ที่ไม่ต้องการ historical data</p><p>ตั้ง max_active_runs ใน DAG เพื่อ limit จำนวน concurrent DAG runs ป้องกัน resource exhaustion เมื่อ backfill หรือ catchup ทำงาน โดยเฉพาะ DAGs ที่ query external APIs ที่มี rate limits</p><h2>Idempotency</h2><p>DAG tasks ควร idempotent หมายความว่ารัน task เดิมซ้ำๆ จะให้ผลลัพธ์เดิม ไม่สร้าง duplicates ใช้ INSERT OVERWRITE แทน INSERT INTO สำหรับ data writes ใช้ execution_date เป็น partition key เพื่อ overwrite data ของวันนั้นๆ เมื่อ rerun</p><p>Anti-patterns ที่ทำให้ task ไม่ idempotent: ใช้ CURRENT_TIMESTAMP แทน execution_date, append data โดยไม่ dedup, depend on external state ที่เปลี่ยนแปลงระหว่าง runs ทำให้ backfill ให้ผลลัพธ์ผิด</p><h2>Retry Strategies</h2><p>ตั้ง retry parameters: retries=3, retry_delay=timedelta(minutes=5), retry_exponential_backoff=True ทำให้ retry ด้วย exponential backoff ป้องกัน thundering herd เมื่อ external service recovers จาก outage แล้วหลาย tasks retry พร้อมกัน</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ pool ใน Airflow เพื่อ limit concurrent tasks ที่ access resource เดียวกัน เช่น กำหนด database_pool จำกัดไม่เกิน 10 concurrent database queries ป้องกัน database overload จาก parallel tasks</div><p>Clear และ Re-run: ใช้ airflow tasks clear เพื่อ reset task state และ rerun จะ re-execute task ด้วย dependencies เดิม useful สำหรับ fix ข้อมูลที่ผิดพลาดโดยไม่ต้องรัน pipeline ทั้งหมดใหม่ ประหยัดเวลาและ resources</p>'
  },
  {
    number: 11,
    slug: 'best-practices',
    emoji: '⭐',
    title: 'Best Practices สำหรับ Airflow Production',
    content: '<h2>DAG Design Principles</h2><p>DAGs ที่ดีควร: atomic tasks (แต่ละ task ทำสิ่งเดียว), idempotent (รันซ้ำได้ปลอดภัย), stateless (ไม่ depend on local state), ไม่ทำ heavy computation ใน top-level DAG code (เพราะรันทุกครั้งที่ parse), ใช้ operators แทน custom Python เมื่อทำได้</p><p>ไม่ควร: เขียน complex Python ใน top-level DAG code นอก functions, ใช้ Variable.get() ใน top-level, import libraries ที่หนักใน top-level, สร้าง connections ใน DAG code แทนที่จะใช้ Airflow Connections เพราะเพิ่ม parse time</p><h2>Performance Optimization</h2><p>ลด DAG parse time: แต่ละ DAG file ถูก parse บ่อยมากโดย scheduler ควร minimize imports, หลีกเลี่ยง database queries ใน top-level, ใช้ lazy imports ที่ทำงานเฉพาะใน execute() ตั้ง min_file_process_interval ให้นานขึ้นสำหรับ large DAG folders</p><p>Database maintenance: Airflow metadata database ต้องการ maintenance สม่ำเสมอ ใช้ airflow db clean เพื่อ purge old records หรือตั้ง DAG อัตโนมัติลบ XCom เก่า, log entries เก่า, DagRun ที่เสร็จแล้ว ป้องกัน database bloat ที่ทำให้ performance ลดลง</p><h2>Security Best Practices</h2><p>ใช้ RBAC (Role-Based Access Control) ใน Airflow Web UI กำหนด roles: Admin, Op, User, Viewer ให้เหมาะกับ team members ไม่ให้ทุกคน access Admin ใช้ LDAP/OAuth สำหรับ SSO integration ใน enterprise environments</p><p>Audit logging: enable audit logging ใน Airflow เพื่อ track ว่าใคร trigger DAG, clear tasks, แก้ variables มีประโยชน์สำหรับ compliance และ debugging เมื่อมี unauthorized changes หรือ unexpected behavior</p><h2>DAG Documentation</h2><p>เพิ่ม doc_md ใน DAG definition โดยเขียน docstring อธิบาย DAG purpose, expected inputs/outputs, SLA requirements, owner, และ escalation path เมื่อ fail documentation ดีช่วยลด on-call burden เมื่อมี incidents</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ตั้ง owner ในทุก DAG และ task ด้วย email ที่ถูกต้อง เมื่อ SLA miss หรือ failure เกิดขึ้น Airflow จะ email owner โดยตรง ป้องกัน issues ตกหล่นเพราะไม่รู้ว่าใครรับผิดชอบ DAG ไหน</div><p>Testing DAGs: เขียน unit tests สำหรับ DAG structure ตรวจสอบว่าไม่มี import errors, dependencies ถูกต้อง, tasks มีชื่อตาม convention ใช้ pytest กับ airflow.models.DagBag เพื่อ load และ test DAGs ใน CI pipeline ก่อน deploy</p>'
  }
];
