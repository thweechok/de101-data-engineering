export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '⚡',
    title: 'PySpark คืออะไร และโลกของ Big Data',
    content: '<h2>Big Data และปัญหาที่ต้องแก้</h2><p>เมื่อข้อมูลมีขนาดใหญ่จนเกินกว่าที่ single machine จะจัดการได้ เราต้องการ distributed computing framework Apache Spark คือ solution ที่ได้รับความนิยมสูงสุด Spark ประมวลผลข้อมูลแบบ in-memory ทำให้เร็วกว่า Hadoop MapReduce ถึง 100 เท่า</p><p>PySpark คือ Python API สำหรับ Apache Spark ทำให้ Data Engineers และ Data Scientists ที่คุ้นเคยกับ Python สามารถใช้พลังของ Spark ได้โดยไม่ต้องเรียน Scala หรือ Java PySpark เป็น official API ที่ Spark team support อย่างเต็มที่และมี community ขนาดใหญ่</p><h2>Use Cases ของ PySpark</h2><p>PySpark เหมาะสำหรับ: ETL pipelines ที่จัดการ data หลาย TB, Batch processing สำหรับ daily/weekly aggregations, Stream processing ด้วย Spark Streaming, Machine Learning ด้วย MLlib, Interactive analytics ด้วย Spark SQL บน large datasets ที่ไม่ fit ใน single machine</p><p>บริษัทที่ใช้ Spark: Netflix ใช้ Spark สำหรับ recommendation engine, Uber ใช้สำหรับ pricing analytics, Alibaba ใช้สำหรับ e-commerce analytics, LinkedIn ใช้สำหรับ graph processing ของ social connections ล้วนเป็นบริษัทที่มี data scale ใหญ่มาก</p><h2>Spark Architecture</h2><p>Spark ทำงานแบบ master-worker: Driver Program เป็น master ที่รัน user code และ coordinate งาน, Cluster Manager (YARN, Kubernetes, Standalone) จัดสรร resources, Executors เป็น workers ที่รัน tasks จริงๆ ทำให้ process data แบบ parallel ข้าม machines</p><p>SparkContext/SparkSession คือ entry point สำหรับ Spark application ทุกอย่างเริ่มต้นจากที่นี่ SparkSession (Spark 2.0+) รวม SQLContext, HiveContext, SparkContext ไว้ใน object เดียว ใช้ SparkSession.builder.getOrCreate() เพื่อ initialize</p><h2>เมื่อไหรควรใช้ Spark</h2><p>ใช้ Spark เมื่อ: data มากกว่า 1 TB, ต้องการ process เร็วมาก, ต้องการ distributed processing, รัน complex ML jobs ไม่จำเป็นต้องใช้ Spark สำหรับ data ที่ Pandas จัดการได้ Pandas เร็วกว่าสำหรับ small-medium data เพราะไม่มี distributed overhead</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ลองใช้ Databricks Community Edition ฟรีก่อนติดตั้ง Spark เอง มี pre-configured Spark cluster, notebook interface, และ sample datasets พร้อมใช้งาน เหมาะมากสำหรับ learning โดยไม่ต้องกังวลเรื่อง infrastructure setup</div><p>PySpark vs Pandas: ถ้า data fit ใน RAM ใช้ Pandas เพราะง่ายกว่าและ debug ง่ายกว่ามาก ถ้า data ไม่ fit ใน RAM หรือต้องการ parallel processing ใช้ PySpark กฎง่ายๆ: data มากกว่า 10GB เริ่มพิจารณา Spark</p>'
  },
  {
    number: 1,
    slug: 'setup',
    emoji: '⚙️',
    title: 'ติดตั้ง PySpark และ Databricks Community',
    content: '<h2>ติดตั้ง PySpark บน Local Machine</h2><p>PySpark ต้องการ Java (JDK 8 หรือ 11) ก่อน ตรวจสอบ: java -version ติดตั้ง PySpark: pip install pyspark ขนาด package ประมาณ 300MB เพราะรวม Spark binaries ด้วย ตั้ง JAVA_HOME environment variable ให้ถูกต้อง ไม่เช่นนั้น Spark จะ start ไม่ได้</p><p>สร้าง SparkSession แรก: from pyspark.sql import SparkSession, spark = SparkSession.builder.appName(\'MyApp\').master(\'local[*]\').getOrCreate() local[*] หมายความว่าใช้ CPU cores ทั้งหมดบน local machine สำหรับ development ใช้ local[2] เพื่อ limit 2 cores</p><h2>Databricks Community Edition</h2><p>Databricks Community Edition เป็นทางเลือกที่ดีสำหรับ learning ไม่ต้อง install อะไร เข้าที่ community.cloud.databricks.com สมัครฟรี ได้ cluster ขนาดเล็กสำหรับ learning มี notebook interface เหมือน Jupyter พร้อม visualizations ในตัว</p><p>ข้อดีของ Databricks: pre-configured cluster ไม่ต้อง setup Java/Spark versions, มี Delta Lake ในตัว, UI สะดวกสำหรับ notebook sharing ข้อเสีย: Community Edition มี cluster timeout และ limited resources ต้องการ internet connection ตลอด</p><h2>ตั้งค่า PySpark สำหรับ Development</h2><p>ใช้ findspark package เพื่อ locate Spark ใน Python: import findspark; findspark.init() ตั้ง logging level: spark.sparkContext.setLogLevel(\'WARN\') ลด verbose output ตั้ง config สำหรับ local development: .config(\'spark.driver.memory\', \'4g\') และ sql.shuffle.partitions เป็น 4</p><p>Jupyter Notebook + PySpark: ติดตั้ง pyspark และ jupyter ใน virtual environment เดียวกัน ตั้ง PYSPARK_DRIVER_PYTHON=jupyter PYSPARK_DRIVER_PYTHON_OPTS=notebook แล้วรัน pyspark เพื่อ launch Jupyter กับ SparkContext ที่ initialized แล้ว</p><h2>Docker สำหรับ PySpark Development</h2><p>ใช้ official jupyter/pyspark-notebook image: docker run -p 8888:8888 jupyter/pyspark-notebook ได้ environment ที่ reproducible พร้อม PySpark, Jupyter, และ libraries ยอดนิยม เหมาะสำหรับทีมที่ต้องการ consistent environment โดยไม่ต้อง setup Java เอง</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ตั้ง spark.sql.shuffle.partitions=4 สำหรับ local development (default 200 partitions ช้ามากบน local เพราะ overhead การสร้าง tasks) เปลี่ยนเป็น 200-400 บน production cluster ตาม data size</div><p>PySpark versions: ตรวจสอบให้แน่ใจว่า PySpark version ตรงกับ Spark cluster version ที่ใช้ใน production ความไม่ตรงกันทำให้เกิด serialization errors ที่ debug ยากมาก ใช้ pip install pyspark==3.5.0 เพื่อ pin version</p>'
  },
  {
    number: 2,
    slug: 'rdd-basics',
    emoji: '🔢',
    title: 'RDD: Map, Filter, Reduce',
    content: '<h2>RDD คืออะไร?</h2><p>RDD (Resilient Distributed Dataset) คือ core abstraction ของ Spark มันคือ immutable distributed collection ของ objects ที่กระจายอยู่บน cluster Resilient หมายถึง fault-tolerant เมื่อ node fail Spark สามารถ recompute partitions ที่หายไปได้จาก lineage graph</p><p>RDD มี 2 ประเภท operations: Transformations (lazy ไม่ execute ทันที รอจนถึง action) เช่น map, filter, flatMap, groupBy และ Actions (trigger execution จริง) เช่น collect, count, save Lazy evaluation ทำให้ Spark optimize execution plan ก่อน run จริง</p><h2>Map Operation</h2><p>map() apply function ทุก element ใน RDD: sc.parallelize([1, 2, 3, 4, 5]).map(lambda x: x * 2).collect() ได้ [2, 4, 6, 8, 10] map transform แต่ละ element เป็น element ใหม่ จำนวน output elements เท่ากับ input ทำให้ predictable มาก</p><p>flatMap() คล้าย map แต่แต่ละ element สามารถ produce หลาย elements หรือ 0 elements: lines.flatMap(lambda line: line.split(\' \')) ใช้สำหรับ text processing, exploding arrays, หรือ filtering elements บางส่วนออก</p><h2>Filter Operation</h2><p>filter() เลือกเฉพาะ elements ที่ condition เป็น True: numbers.filter(lambda x: x % 2 == 0) เหมาะสำหรับ data cleaning, subset selection, removing nulls ตัวอย่าง: valid_records = rdd.filter(lambda r: r[\'email\'] is not None) ลด dataset ก่อน expensive operations</p><p>Chaining transformations: rdd.filter(lambda x: x > 0).map(lambda x: x ** 2).take(10) Spark สร้าง execution plan สำหรับ chain ทั้งหมดก่อน execute ทำให้ optimize ได้ดีกว่า execute ทีละ step เรียกว่า pipelining</p><h2>Reduce และ Aggregations</h2><p>reduce() aggregate elements ด้วย binary function: numbers.reduce(lambda a, b: a + b) ต่างจาก fold() ตรงที่ไม่มี initial value reduceByKey() สำหรับ key-value RDDs: words.map(lambda w: (w, 1)).reduceByKey(lambda a, b: a + b) ทำ word count แบบ classic MapReduce</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ในปัจจุบันนิยมใช้ DataFrame API มากกว่า RDD API เพราะ optimized กว่า (Catalyst optimizer) และ syntax เป็น declarative ทำให้ readable กว่า ใช้ RDD เฉพาะเมื่อ DataFrame API ไม่รองรับ operation ที่ต้องการ</div><p>Persist RDDs: ถ้ารัน multiple actions บน RDD เดียวกัน ใช้ rdd.cache() หรือ rdd.persist() เพื่อ keep ไว้ใน memory หลังจาก computed ครั้งแรก ป้องกัน recomputation และเพิ่ม performance อย่างมาก เรียก rdd.unpersist() เมื่อใช้เสร็จ</p>'
  },
  {
    number: 3,
    slug: 'dataframes',
    emoji: '📋',
    title: 'DataFrame: อ่าน CSV, Parquet, JSON',
    content: '<h2>PySpark DataFrame คืออะไร?</h2><p>DataFrame คือ distributed collection ของ rows ที่มี named columns คล้ายกับ Pandas DataFrame หรือ SQL table แต่ distributed ข้าม cluster DataFrame ถูก optimize ด้วย Catalyst query optimizer และ Tungsten execution engine ทำให้เร็วกว่า RDD มากสำหรับ structured data</p><p>สร้าง DataFrame จากหลายแหล่ง: อ่านจากไฟล์ (CSV, Parquet, JSON, ORC), อ่านจาก database ผ่าน JDBC, convert จาก Pandas DataFrame ด้วย spark.createDataFrame(pandas_df), สร้างจาก list ด้วย spark.createDataFrame(data, schema) ตาม use case</p><h2>อ่าน CSV Files</h2><p>spark.read.csv(\'path\', header=True, inferSchema=True) inferSchema=True ทำให้ Spark อ่าน data sample เพื่อ detect types อัตโนมัติ แต่ช้ากว่าการ define schema เอง สำหรับ production ควร define schema เพื่อ performance และ type safety</p><p>Define schema manual ด้วย StructType และ StructField ระบุ column names, types, และ nullable flag ทำให้ avoid expensive schema inference pass และ catch type mismatches ทันที ไม่ต้องรอ runtime error</p><h2>อ่าน Parquet Files</h2><p>Parquet เป็น columnar format ที่ recommended สำหรับ Spark workloads เพราะ: columnar storage ทำให้ query เฉพาะ columns ที่ต้องการ, compressed ดีกว่า CSV ถึง 10x, preserve schema อัตโนมัติ spark.read.parquet(\'path\') ไม่ต้องระบุ schema เพราะ Parquet เก็บ schema ไว้แล้ว</p><p>เขียน Parquet: df.write.mode(\'overwrite\').parquet(\'/output/path\') mode options: overwrite (แทนที่ทั้งหมด), append (เพิ่ม), ignore (ข้ามถ้ามีอยู่แล้ว), error (default, error ถ้ามีอยู่แล้ว) เลือก mode ตาม use case</p><h2>DataFrame Operations พื้นฐาน</h2><p>df.show(10): แสดง 10 แถวแรก, df.printSchema(): แสดง schema, df.describe(): statistics ของ numeric columns, df.count(): นับแถว, df.columns: list column names, df.dtypes: list (column, type) tuples เหล่านี้คือ basic exploration commands ใช้เสมอเมื่อ load data ใหม่</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ df.limit(100).toPandas() เพื่อดู sample data ใน Pandas format บน Jupyter notebook แสดงผลสวยกว่า df.show() ระวัง: ไม่ใช้ toPandas() บน full dataset ขนาดใหญ่ เพราะ load ทั้งหมดเข้า driver memory ทำให้ OOM</div><p>Reading multiple files: spark.read.csv(\'/path/to/folder/\') อ่านทุก CSV ใน folder พร้อมกัน ใช้ glob pattern: spark.read.parquet(\'/path/year=2024/month=*/\') อ่าน partitioned data ทุก month ของปี 2024 Spark อ่าน parallel ทำให้เร็ว</p>'
  },
  {
    number: 4,
    slug: 'sql-spark',
    emoji: '🔍',
    title: 'Spark SQL: Query ด้วย SQL บน Big Data',
    content: '<h2>Spark SQL คืออะไร?</h2><p>Spark SQL ช่วยให้รัน SQL queries บน Spark DataFrames ทำให้ Data Analysts ที่คุ้นเคยกับ SQL สามารถทำงานกับ Big Data ได้โดยไม่ต้องเรียน PySpark API Spark SQL ใช้ Catalyst optimizer เดียวกับ DataFrame API ดังนั้น performance เท่ากัน</p><p>Register DataFrame เป็น Temp View: df.createOrReplaceTempView(\'orders\') แล้วรัน SQL: result = spark.sql(\'SELECT customer_id, SUM(amount) as total FROM orders GROUP BY customer_id\') ผลลัพธ์เป็น DataFrame ที่ chain operations ต่อได้ด้วย DataFrame API</p><h2>SQL vs DataFrame API</h2><p>ทั้ง SQL และ DataFrame API produce ผลลัพธ์เดียวกัน เลือกใช้ตาม preference ของทีม: SQL ดีสำหรับทีมที่มี SQL background, DataFrame API ดีสำหรับ complex programmatic logic, สามารถ mix ทั้งสองใน pipeline เดียวกันได้อย่าง seamless</p><p>Complex queries ใน Spark SQL: รองรับ CTEs (WITH clause), window functions, subqueries, LATERAL VIEW explode, และ built-in functions มากมาย ทั้งหมด execute บน distributed cluster ทำให้จัดการ data ขนาดใหญ่ได้โดยไม่ต้อง change SQL syntax</p><h2>Spark SQL Functions</h2><p>Built-in functions: from pyspark.sql.functions import col, lit, when, coalesce, concat, to_date, date_format, year, month, dayofweek ใช้กับ DataFrame API เช่น df.select(year(col(\'order_date\')).alias(\'year\')) ทำงานเร็วกว่า UDFs เพราะ JVM native</p><p>User Defined Functions (UDFs): ถ้า built-in ไม่เพียงพอ สร้าง UDF ด้วย @udf decorator ระบุ return type ระวัง: UDFs ช้ากว่า built-in functions มาก เพราะ serialize/deserialize Python objects ระหว่าง JVM และ Python</p><h2>Global vs Temporary Views</h2><p>Temporary views ใช้ได้เฉพาะใน SparkSession ปัจจุบัน Global Temp Views ใช้ได้ข้าม SparkSessions ใน application เดียวกัน: df.createOrReplaceGlobalTempView(\'global_orders\') ใช้ query ด้วย prefix global_temp schema</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ spark.catalog.cacheTable(\'table_name\') เพื่อ cache table ที่ query บ่อยๆ ไว้ใน memory ทำให้ queries ถัดไปที่ query เหมือนกันเร็วขึ้นมาก uncache ด้วย spark.catalog.uncacheTable() เมื่อไม่ต้องการแล้ว</div><p>Explain query plan: df.explain(True) แสดง logical และ physical plan ช่วย understand ว่า Spark จะ execute query อย่างไร ใช้ debug performance issues เช่น ดูว่ามี full table scan หรือ predicate pushdown ทำงานหรือไม่</p>'
  },
  {
    number: 5,
    slug: 'transformations',
    emoji: '🔄',
    title: 'Transformations: Joins, GroupBy, Window Functions',
    content: '<h2>Join Operations ใน PySpark</h2><p>PySpark รองรับ join types หลักทุกแบบ: inner, left, right, outer, semi, anti join ด้วย DataFrame API: result = orders.join(customers, orders.customer_id == customers.id, how=\'left\') หรือ SQL ด้วย Spark SQL สำหรับทีมที่ถนัด SQL</p><p>Broadcast Join: ถ้า table หนึ่งเล็กมาก (ไม่กี่ร้อย MB) ใช้ broadcast join: from pyspark.sql.functions import broadcast, result = large_df.join(broadcast(small_df), \'key\') Spark ส่ง small table ไปทุก executor ป้องกัน shuffle ขนาดใหญ่ เพิ่ม performance 10x+</p><h2>GroupBy และ Aggregations</h2><p>groupBy().agg() ทำ aggregations: import functions as F, df.groupBy(\'category\').agg(F.sum(\'revenue\').alias(\'total_revenue\'), F.avg(\'revenue\').alias(\'avg_revenue\'), F.count(\'*\').alias(\'order_count\')).show() ได้ aggregated results per category</p><p>Multiple groupBy columns: df.groupBy(\'year\', \'month\', \'category\').agg(...) pivot สำหรับ cross-tabulation: df.groupBy(\'category\').pivot(\'month\').agg(F.sum(\'revenue\')) สร้าง column สำหรับแต่ละ month อัตโนมัติ เหมาะสำหรับ report generation</p><h2>Window Functions</h2><p>Window functions ทำ calculations ข้าม rows ที่ related กัน โดยไม่ reduce เป็น single row: from pyspark.sql.window import Window กำหนด partitionBy และ orderBy แล้วใช้ rank(), row_number(), lag(), lead() หรือ sum() over window</p><p>Running totals: Window.partitionBy(\'user_id\').orderBy(\'order_date\').rowsBetween(Window.unboundedPreceding, Window.currentRow) ใช้กับ sum() ได้ cumulative sum ต่อ user ทรงพลังมากสำหรับ time-series analysis</p><h2>Column Operations</h2><p>withColumn(): เพิ่มหรือแก้ไข column, drop(): ลบ column, select(): เลือก columns, withColumnRenamed(): เปลี่ยนชื่อ, cast(): เปลี่ยน type, when().otherwise(): conditional expressions เหมือน CASE WHEN ใน SQL</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> หลีกเลี่ยง cross joins เด็ดขาด (ไม่มี condition) เพราะ produce N*M rows ซึ่งอาจเป็นพัน ล้าน rows ทำให้ cluster crash ถ้าต้องการ cross join ใช้ broadcast ของ side เล็กและ filter เข้มงวดทันที</div><p>Null handling: isNull(), isNotNull(), fillna(), dropna() ใช้ coalesce() เพื่อหา first non-null value จากหลาย columns เหมาะสำหรับ fallback logic เช่น ใช้ billing_city ถ้ามี ไม่เช่นนั้นใช้ shipping_city</p>'
  },
  {
    number: 6,
    slug: 'optimization',
    emoji: '🚀',
    title: 'Optimization: Partitioning และ Caching',
    content: '<h2>Partitioning ใน Spark</h2><p>Partitioning กำหนดว่า data กระจายอย่างไรข้าม executors แต่ละ partition ถูก process โดย task เดียว ถ้า partitions น้อยเกินไป บาง executors ว่างงาน ถ้าเยอะเกินไป overhead ของการจัดการ tasks สูง rule of thumb คือ 2-4 partitions ต่อ CPU core</p><p>ดู partition count: df.rdd.getNumPartitions() รีพาร์ติชั่น: df.repartition(100) สร้าง 100 partitions ด้วย full shuffle, df.coalesce(10) ลด partitions โดยไม่ shuffle (เร็วกว่า แต่ partitions อาจ uneven) ใช้ repartition() สำหรับเพิ่ม, coalesce() สำหรับลด</p><h2>Partition by Column</h2><p>df.repartition(col(\'country\')) ทำ hash partitioning ตาม country column ทำให้ data ของแต่ละ country อยู่ใน partition เดียวกัน ดีสำหรับ join และ groupBy บน country column เพราะไม่ต้อง shuffle data ระหว่าง executors</p><p>Write partitioned data: df.write.partitionBy(\'year\', \'month\').parquet(\'/output/\') สร้าง folder structure: year=2024/month=01/ ทำให้ queries ที่ filter บน year/month อ่านเฉพาะ partitions ที่เกี่ยวข้อง ลด I/O มาก เรียกว่า partition pruning</p><h2>Caching และ Persistence</h2><p>df.cache() หรือ df.persist() เก็บ DataFrame ไว้ใน memory หลังจาก compute ครั้งแรก มีประโยชน์เมื่อ: ใช้ DataFrame ซ้ำหลายครั้งใน pipeline, iterative algorithms เช่น ML training, debugging ที่รัน actions บ่อยๆ</p><p>Storage levels: MEMORY_ONLY (เร็วสุด แต่ spill ถ้า RAM เต็ม), MEMORY_AND_DISK (spill ไป disk ถ้า RAM เต็ม safer), DISK_ONLY (ช้าแต่ใช้ RAM น้อย) ใช้ df.persist(StorageLevel.MEMORY_AND_DISK) สำหรับ production workloads</p><h2>Catalyst Optimizer</h2><p>Spark Catalyst optimizer analyze query plan และ apply optimizations อัตโนมัติ เช่น predicate pushdown (filter ก่อน join), column pruning (ไม่อ่าน columns ที่ไม่ใช้), constant folding ผู้ใช้ส่วนใหญ่ไม่ต้องทำอะไร Catalyst จัดการให้อัตโนมัติ</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ Spark UI ที่ port 4040 เพื่อ monitor job execution, ดู stage details, partition distribution, และ identify bottlenecks Spark UI เป็นเครื่องมือ debugging ที่ทรงพลังที่สุดสำหรับ Spark developers ควร familiar มากๆ</div><p>Data skew: ถ้า partitions มีขนาดไม่เท่ากันมาก บาง tasks จะช้ามากทำให้ทั้ง job ช้า ตรวจสอบได้ใน Spark UI ใน Stages tab ดู task duration distribution แก้ด้วย salting technique หรือ AQE (Adaptive Query Execution)</p>'
  },
  {
    number: 7,
    slug: 'streaming',
    emoji: '🌊',
    title: 'Structured Streaming กับ PySpark',
    content: '<h2>Spark Structured Streaming คืออะไร?</h2><p>Structured Streaming คือ stream processing engine ที่ built บน Spark SQL engine ทำให้เขียน streaming queries ด้วย DataFrame/SQL API เดียวกับ batch processing Spark treat streaming data เป็น unbounded table ที่มี rows เพิ่มขึ้นเรื่อยๆ</p><p>ความแตกต่างจาก Spark Streaming เดิม (DStream API): Structured Streaming ใช้ DataFrame API, มี exactly-once semantics, รองรับ event time processing, มี watermarking สำหรับ late data handling ทำให้ง่ายกว่า DStream มากในการ implement</p><h2>Sources และ Sinks</h2><p>Streaming sources: Kafka (most common สำหรับ production), File source (อ่านไฟล์ใหม่จาก directory), Rate source (generate synthetic data สำหรับ testing), Socket source (development only) Sinks: Kafka, Files, Memory (testing), Console (debugging), JDBC, Delta Lake</p><p>อ่านจาก Kafka: spark.readStream.format(\'kafka\').option(\'kafka.bootstrap.servers\', \'localhost:9092\').option(\'subscribe\', \'my-topic\').load() Kafka message value เป็น bytes ต้อง cast และ parse JSON ด้วย from_json function</p><h2>Transformations บน Streaming Data</h2><p>ใช้ DataFrame operations เหมือน batch: filter, select, groupBy, join Stateful operations: groupBy กับ window functions สำหรับ aggregations ตาม time window เช่น count events ทุก 5 นาที ด้วย window() function</p><p>Watermarking สำหรับ late data: ตั้ง watermark เพื่อกำหนดว่ายอมรับ late data นานแค่ไหน data ที่ late เกิน threshold จะถูก drop ช่วย bound state size ป้องกัน memory overflow ใน long-running streaming jobs</p><h2>Output Modes</h2><p>Complete: write ทุก aggregate result ทุก trigger เหมาะสำหรับ aggregations Append: write เฉพาะ new rows ที่เพิ่มเข้ามา ไม่มีการ update เหมาะสำหรับ append-only operations Update: write เฉพาะ rows ที่เปลี่ยนแปลง เหมาะสำหรับ aggregations ที่ update ได้</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ checkpointing เสมอสำหรับ production streaming jobs: writeStream.option(\'checkpointLocation\', \'/path/to/checkpoint\') ช่วย recover state เมื่อ job restart และ guarantee exactly-once processing สำคัญมากสำหรับ financial data</div><p>Trigger modes: Trigger.ProcessingTime(\'1 minute\') รัน micro-batch ทุก 1 นาที, Trigger.Once() รัน batch เดียวแล้วหยุด เหมาะสำหรับ scheduled batch-like streaming, Trigger.Continuous สำหรับ true continuous processing (experimental)</p>'
  },
  {
    number: 8,
    slug: 'ml-pipeline',
    emoji: '🤖',
    title: 'MLlib: Machine Learning Pipeline',
    content: '<h2>MLlib ใน PySpark</h2><p>MLlib คือ Spark library สำหรับ machine learning ที่ scale ได้บน distributed cluster มี algorithms สำหรับ classification, regression, clustering, recommendation, และ feature engineering รัน parallel บน data ขนาด TB ได้ ทำให้ train models บน massive datasets</p><p>ML Pipeline ใน Spark: คล้ายกับ scikit-learn Pipeline ประกอบด้วย Transformers (transform data เช่น VectorAssembler, StandardScaler, StringIndexer) และ Estimators (train models เช่น LinearRegression, RandomForestClassifier) Pipeline connect ทุกอย่างเข้าด้วยกัน</p><h2>Feature Engineering</h2><p>StringIndexer: convert categorical strings เป็น numeric index, OneHotEncoder: convert index เป็น one-hot vector, VectorAssembler: รวม feature columns เป็น single vector column (ต้องใช้ก่อน train models), StandardScaler: normalize features ให้ mean=0, std=1</p><p>ตัวอย่าง: VectorAssembler รับ inputCols list ของ feature columns และ outputCol ชื่อ features, StandardScaler รับ inputCol=\'features\' และ outputCol=\'scaled_features\' ส่ง Assembler output ไปยัง Scaler</p><h2>Training Models</h2><p>สร้าง Pipeline จาก list ของ stages: [assembler, scaler, classifier] Pipeline.fit(train_df) return PipelineModel, model.transform(test_df) ได้ predictions column CrossValidator สำหรับ hyperparameter tuning ด้วย ParamGridBuilder สร้าง grid ของ parameters ที่ต้องการ tune</p><p>RandomForestClassifier: featuresCol, labelCol, numTrees, maxDepth เป็น parameters หลัก CrossValidator ทดลองทุก combinations ใน ParamGrid ด้วย k-fold cross-validation เลือก best model โดยอัตโนมัติ</p><h2>Model Evaluation</h2><p>BinaryClassificationEvaluator สำหรับ binary classification: metricName=\'areaUnderROC\' (AUC-ROC) MulticlassClassificationEvaluator สำหรับ multi-class RegressionEvaluator สำหรับ regression: metrics rmse, mae, r2</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> Save และ Load models: model.write().overwrite().save(\'/path/to/model\'), loaded_model = PipelineModel.load(\'/path/to/model\') ทำให้ deploy model เป็น service หรือ reuse ใน batch prediction jobs ได้ง่าย ไม่ต้อง retrain ทุกครั้ง</div><p>Spark MLlib vs scikit-learn: ใช้ scikit-learn สำหรับ data ที่ fit ใน memory เพราะ API สมบูรณ์กว่า รวดเร็วกว่าสำหรับ single machine, algorithms หลากหลายกว่า ใช้ MLlib เฉพาะเมื่อ data ใหญ่จนต้องใช้ distributed training จริงๆ</p>'
  },
  {
    number: 9,
    slug: 'gcs-s3',
    emoji: '☁️',
    title: 'อ่านข้อมูลจาก GCS และ S3',
    content: '<h2>Cloud Storage สำหรับ Big Data</h2><p>Data lake บน cloud storage เป็น standard สำหรับ Big Data workloads ในปัจจุบัน AWS S3 และ Google Cloud Storage (GCS) คือ choices ยอดนิยม Spark สามารถอ่าน-เขียน cloud storage ได้โดยตรงผ่าน connector libraries ทำให้ data engineers ไม่ต้อง download data มา local</p><p>ข้อดีของ cloud storage: infinite scalability, ราคาถูกกว่า local storage มาก, managed service ไม่ต้องดูแล hardware, high availability, integrate กับ analytics services อื่นๆ ได้ง่าย เช่น Athena, BigQuery, Redshift Spectrum</p><h2>อ่านจาก AWS S3</h2><p>ต้องการ hadoop-aws library ใน SparkSession config: org.apache.hadoop:hadoop-aws:3.3.4 ตั้ง credentials ผ่าน Hadoop configuration หรือใช้ IAM role บน EC2/EMR ซึ่งดีกว่าเพราะไม่ต้อง hardcode credentials</p><p>อ่าน S3 ด้วย s3a:// prefix: spark.read.parquet(\'s3a://my-bucket/data/year=2024/\') เขียน: df.write.mode(\'overwrite\').parquet(\'s3a://my-bucket/output/\') ใช้ s3a:// prefix (ไม่ใช่ s3:// หรือ s3n://) เพราะ s3a เร็วที่สุดและ maintained</p><h2>อ่านจาก Google Cloud Storage</h2><p>ต้องการ gcs-connector library ตั้ง path ไปยัง JAR file ใน SparkSession config ตั้ง authentication ด้วย Service Account JSON key file หรือ Application Default Credentials บน GCE/Dataproc</p><p>อ่าน GCS ด้วย gs:// prefix: spark.read.parquet(\'gs://my-bucket/data/\') บน Dataproc GCS connector pre-installed และ config ให้แล้ว ไม่ต้อง setup เอง เป็นอีกเหตุผลที่ Dataproc สะดวกมากสำหรับ GCP users</p><h2>Partitioned Data บน Cloud Storage</h2><p>Hive-style partitioning: data/year=2024/month=01/day=01/*.parquet Spark auto-detect partitions เมื่ออ่าน directory DataFrame จะมี columns year, month, day โดยอัตโนมัติ ทำให้ filter partition pruning ทำงานได้ อ่านเฉพาะ partitions ที่ต้องการ</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ตั้งค่า spark.hadoop.fs.s3a.fast.upload=true และ spark.hadoop.fs.s3a.multipart.size=104857600 (100MB) เพื่อ optimize S3 write performance สำหรับ large files ลด write time ได้อย่างมีนัยสำคัญ</div><p>Delta Lake บน S3/GCS: ใช้ Delta Lake format แทน plain Parquet สำหรับ data lake ได้ ACID transactions, time travel, schema evolution ทำงานบน S3/GCS ได้ดี เป็น best practice สำหรับ production data lakes</p>'
  },
  {
    number: 10,
    slug: 'airflow-spark',
    emoji: '🔗',
    title: 'Submit Spark Jobs ผ่าน Airflow',
    content: '<h2>Airflow + Spark Integration</h2><p>Airflow orchestrate Spark jobs ได้หลายวิธี: SparkSubmitOperator สำหรับ submit ผ่าน spark-submit CLI, BashOperator รัน spark-submit command, DataprocSubmitJobOperator สำหรับ Google Dataproc, EMRAddStepsOperator สำหรับ AWS EMR</p><p>การ integrate ทำให้ Spark jobs เป็นส่วนหนึ่งของ larger pipeline ที่ Airflow orchestrate เช่น Extract ด้วย Python task → Transform ด้วย Spark job → Load ด้วย dbt → Notify ด้วย Slack operator ทำให้เห็น entire pipeline ใน Airflow UI</p><h2>SparkSubmitOperator</h2><p>from airflow.providers.apache.spark.operators.spark_submit import SparkSubmitOperator สร้าง task ด้วย application path, conn_id ที่ point ไปยัง Spark cluster, application_args, executor_cores, num_executors, conf dictionary สำหรับ Spark configurations</p><p>Spark Connection ใน Airflow: ตั้งค่า conn_id \'spark_default\' ใน Connections UI ระบุ master URL เช่น spark://master:7077 หรือ yarn หรือ k8s://https://k8s-api:443 สำหรับ Kubernetes สำหรับ managed services ใช้ provider-specific operators แทน</p><h2>Dataproc Integration</h2><p>สำหรับ GCP users: DataprocSubmitPySparkJobOperator กำหนด main script path บน GCS, arguments, cluster_name, region, project_id ง่ายมากเพราะ Dataproc manage Spark cluster ให้ ไม่ต้องดูแล master/worker nodes</p><p>Ephemeral clusters: สร้าง Dataproc cluster → รัน Spark job → ลบ cluster ประหยัด cost มาก เพราะ pay เฉพาะตอนรัน แต่ startup time นานกว่า 3-5 นาที เหมาะสำหรับ daily batch jobs ที่ไม่ต้องการ low latency</p><h2>Kubernetes Spark Jobs</h2><p>Submit Spark ไปยัง K8s ด้วย KubernetesPodOperator หรือ spark-submit กับ Kubernetes master แต่ละ Spark job สร้าง driver pod และ executor pods ใน K8s cluster Auto-scaling ได้ดีกว่า static clusters เพราะ K8s allocate resources ตาม demand</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ส่ง parameters จาก Airflow ไปยัง Spark script ผ่าน application_args โดยใช้ Jinja templates เช่น [\'--execution-date\', \'{{ ds }}\', \'--env\', \'{{ var.value.environment }}\'] ทำให้ script generic และ reusable ข้าม environments</div><p>Monitoring: ดู Spark UI ผ่าน Airflow โดย expose Spark History Server สำหรับ completed jobs ตั้ง spark.eventLog.enabled=true เพื่อ log events ไปยัง S3/GCS สำหรับ post-mortem analysis เมื่อมี performance issues</p>'
  },
  {
    number: 11,
    slug: 'performance',
    emoji: '📈',
    title: 'Performance: Data Skew และ Spill',
    content: '<h2>Performance Issues ที่พบบ่อย</h2><p>Spark performance problems มักมาจาก 3 สาเหตุหลัก: Data Skew (ข้อมูลกระจายไม่สม่ำเสมอ ทำให้บาง tasks ช้ามาก), Spill (ข้อมูลล้น RAM ไปยัง disk ช้ามาก), และ Shuffle (data movement ข้าม network expensive) การแก้ปัญหาเหล่านี้อาจทำให้ job เร็วขึ้น 10-100 เท่า</p><p>เครื่องมือหลักสำหรับ diagnosis: Spark UI ดู stage details, task duration distribution, data size per partition ใช้ Ganglia/Grafana ดู executor memory/CPU SparkHistory Server ดู completed jobs ที่ fail หรือช้า</p><h2>Data Skew และวิธีแก้</h2><p>Skew เกิดเมื่อ partition บางอันมีข้อมูลมากกว่าอื่นมาก เช่น join บน customer_id ที่ NULL ทุก row จะ join ไปที่ partition เดียว วิธีแก้: Salting technique เพิ่ม random suffix ไปยัง join key เพื่อ distribute skewed keys ออกไปหลาย partitions</p><p>Salting implementation: เพิ่ม salted_key column ด้วย concat ของ key และ random integer (0-9) ทำให้ skewed key แตกออกเป็น 10 partitions ต้อง apply transformation เดียวกับ dimension table ด้วยเพื่อ join ได้ถูกต้อง</p><h2>Spill และการแก้ไข</h2><p>Spill เกิดเมื่อ executor memory ไม่พอ ข้อมูลถูก spill ไปยัง disk ช้ามากเพราะ disk I/O ตรวจสอบใน Spark UI ที่ Stages tab ดู Spill (Memory) column วิธีแก้: เพิ่ม executor memory, ลด partition size, เพิ่มจำนวน partitions</p><p>Memory tuning: spark.executor.memory (total memory ต่อ executor), spark.executor.memoryOverhead (off-heap memory สำหรับ Python UDFs), spark.memory.fraction (fraction สำหรับ execution+storage) ปรับตาม workload characteristics และ cluster specs</p><h2>Shuffle Optimization</h2><p>Shuffle เกิดใน operations: groupBy, join, distinct, repartition ช้าเพราะต้อง serialize, transfer data ข้าม network, deserialize ลดจำนวน shuffles: broadcast join สำหรับ small tables, ลำดับ operations ให้ filter ก่อน join ลด data size</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> เปิด Adaptive Query Execution (AQE): spark.sql.adaptive.enabled=true (default ใน Spark 3.2+) AQE auto-adjust partition count, detect skew, และ optimize join strategies ระหว่าง runtime ลด manual tuning ได้มาก</div><p>Bucketing: สำหรับ tables ที่ join กันบ่อยๆ ใช้ bucketing เพื่อ pre-partition data ตาม join key: df.write.bucketBy(50, \'customer_id\').sortBy(\'customer_id\').saveAsTable(\'orders_bucketed\') ทำให้ join ครั้งต่อไปไม่มี shuffle ลด latency มาก</p>'
  },
  {
    number: 12,
    slug: 'real-world',
    emoji: '🛒',
    title: 'Real-World Case: E-commerce Analytics',
    content: '<h2>E-commerce Data Engineering Case</h2><p>ลงมือทำ end-to-end case จริงสำหรับ e-commerce platform ที่มี transactions 10 ล้านครั้งต่อวัน data source: orders table, order_items table, customers table, products table, events table (clickstream) เป้าหมาย: daily analytics dashboard และ customer segmentation สำหรับ marketing team</p><p>Architecture: Raw data บน S3 → Spark ETL jobs (PySpark) → Processed data บน Delta Lake → dbt transformation → Redshift/BigQuery → Tableau dashboard Airflow orchestrate ทุก steps ตาม daily schedule โดยมี retry logic และ alerting</p><h2>Step 1: Data Ingestion</h2><p>อ่าน raw CSV จาก S3 ด้วย defined schema (ไม่ใช้ inferSchema เพราะ slow), validate data quality ด้วย assertions ว่าไม่มี null primary keys, log metrics เช่น record count เพื่อ detect data volume anomalies ก่อน continue</p><p>Handle bad data: ใช้ badRecordsPath option เพื่อ route malformed records ไปยัง separate error location ไม่ให้ bad records ทำให้ job fail ทั้งหมด สามารถ investigate และ fix error records แยกต่างหาก</p><h2>Step 2: Transformation</h2><p>Join orders กับ order_items และ customers ด้วย broadcast join สำหรับ customers (เล็กกว่า) คำนวณ metrics: daily_summary ด้วย groupBy วัน, หมวดหมู่ aggregation ด้วย sum revenue, count orders, countDistinct customers</p><p>Customer segmentation ด้วย RFM: คำนวณ Recency, Frequency, Monetary ต่อ customer ด้วย window functions จัดกลุ่ม customers เป็น High/Medium/Low value ด้วย ntile(3) function ทำให้ marketing team target แต่ละ segment ได้ถูกต้อง</p><h2>Step 3: Load และ Optimize</h2><p>เขียน result ไปยัง Delta Lake ด้วย partitionBy date ทำให้ queries ที่ filter บน date เร็ว optimize Delta table ด้วย OPTIMIZE command เพื่อ compact small files ที่สะสมจาก streaming writes ลด file count และเพิ่ม read performance</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ใช้ Delta Lake MERGE operation สำหรับ upserts แทน overwrite ทั้งหมด ประหยัด time มากสำหรับ incremental updates: DeltaTable.merge(source, condition).whenMatchedUpdateAll().whenNotMatchedInsertAll().execute()</div><p>Monitoring pipeline: ส่ง metrics ไปยัง CloudWatch/Stackdriver เช่น records processed, processing time, error rate ตั้ง alerts เมื่อ records count ต่ำกว่า threshold (อาจหมายถึง upstream data ปัญหา) ทำให้ detect issues ก่อน dashboard แสดงข้อมูลผิด</p>'
  }
];
