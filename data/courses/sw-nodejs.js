export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '🟩',
    title: 'ภาพรวมของ Node.js (Overview)',
    content: '<h2>1. Node.js คืออะไร?</h2> <p>Node.js เป็น <strong>JavaScript runtime environment</strong> แบบ Open-source ที่สามารถทำงานข้ามแพลตฟอร์มได้ สร้างขึ้นโดยใช้ <strong>V8 JavaScript Engine</strong> ของ Google Chrome ซึ่งทำหน้าที่คอมไพล์โค้ด JavaScript ให้เป็น Machine code ที่ทำงานได้รวดเร็วมาก</p> <p>ในอดีต JavaScript ถูกใช้เฉพาะในฝั่ง <strong>Client-side</strong> (บนเบราว์เซอร์) แต่ด้วย Node.js เราสามารถเขียน JavaScript ในฝั่ง <strong>Server-side</strong> ได้ ทำให้สามารถใช้ภาษาเดียว (JavaScript) พัฒนาทั้ง Frontend และ Backend ช่วยให้ทีมทำงานร่วมกันได้ง่ายขึ้น</p> <h2>2. สถาปัตยกรรม Non-blocking I/O และ Event Loop</h2> <p>สิ่งที่เป็นหัวใจสำคัญของ Node.js คือ <strong>Event Loop</strong> ซึ่งทำหน้าที่จัดการแบบ Non-blocking I/O:</p> <div class="tip-box"><strong>💡 คำอธิบายเชิงเปรียบเทียบ</strong>: ลองนึกภาพร้านอาหารที่มีพนักงานเสิร์ฟ 1 คน (Single-threaded) ถ้าเป็นระบบแบบเก่า (Blocking) พนักงานจะรับออเดอร์โต๊ะ 1 แล้วต้องยืนรอหน้าครัวจนกว่าอาหารจะเสร็จ แต่ในระบบ Node.js (Non-blocking) พนักงานรับออเดอร์โต๊ะ 1 ส่งเข้าครัว (ระบบ I/O) แล้วไปรับออเดอร์โต๊ะ 2 ต่อได้ทันที เมื่อครัวทำอาหารโต๊ะ 1 เสร็จ จะมีกริ่งดัง (Event Callback) พนักงานถึงจะเดินไปหยิบอาหารมาเสิร์ฟ</div> <p>โครงสร้างนี้ทำให้ Node.js สามารถรับ <strong>Concurrent Requests</strong> (คำขอพร้อมกัน) ได้หลักหมื่นถึงแสน requests ต่อวินาที โดยไม่ต้องเพิ่มเซิร์ฟเวอร์ขนาดใหญ่ เหมาะกับระบบอย่าง Real-time Chat และ Streaming API</p> <h2>3. ระบบนิเวศ NPM (NPM Ecosystem)</h2> <p>NPM (Node Package Manager) คือคลัง Open-source library ที่ใหญ่ที่สุดในโลก เป็นทั้ง Online repository และ Command-line utility เพื่อจัดการ Dependencies (แพ็กเกจที่ต้องใช้) ของโปรเจกต์</p> <ul> <li><code>npm init -y</code> - สร้างไฟล์ package.json เพื่อเริ่มโปรเจกต์ใหม่</li> <li><code>npm install express</code> - ติดตั้งแพ็กเกจลงในโปรเจกต์</li> <li><code>npm run dev</code> - รันสคริปต์ที่กำหนดเองใน package.json</li> </ul> <h2>4. เมื่อไหร่ที่ควรและไม่ควรใช้ Node.js</h2> <p><strong>✅ เหมาะกับ:</strong> สร้าง REST API, ระบบ Real-time แบบ Socket.io, Microservices, ระบบที่มีการดึงข้อมูลจาก Database บ่อยๆ (I/O heavy)</p> <p><strong>❌ ไม่เหมาะกับ:</strong> งานประมวลผล CPU หนักๆ (CPU-intensive) เช่น Video Encoding, การบีบอัดไฟล์ขนาดใหญ่ หรือ Machine Learning (เพราะ Single-thread จะถูกบล็อก ทำให้รับ request อื่นไม่ได้)</p>'
  },
  {
    number: 1,
    slug: 'fundamentals',
    emoji: '📚',
    title: 'พื้นฐานการใช้งาน Node.js (Fundamentals)',
    content: '<h2>1. ระบบ Module: CommonJS vs ESM</h2> <p>ใน Node.js การแบ่งโค้ดออกเป็นหลายไฟล์ย่อยๆ เรียกว่า <strong>Module</strong> ปัจจุบันมี 2 มาตรฐานหลักคือ <strong>CommonJS (CJS)</strong> และ <strong>ECMAScript Modules (ESM)</strong></p> <ul> <li><strong>CommonJS:</strong> เป็นมาตรฐานดั้งเดิมของ Node.js ใช้คำสั่ง <code>require()</code> ในการนำเข้า และ <code>module.exports</code> ในการส่งออกฟังก์ชันหรือตัวแปร</li> <li><strong>ESM:</strong> เป็นมาตรฐานใหม่ (รูปแบบเดียวกับที่ใช้ใน Frontend สมัยใหม่) ใช้ <code>import</code> และ <code>export</code> ต้องกำหนด <code>"type": "module"</code> ในไฟล์ package.json ถึงจะสามารถใช้งานได้</li> </ul> <pre><code>// แบบ CommonJS (นิยมใช้ในโปรเจกต์ทั่วไป)\nconst fs = require(\'fs\');\nmodule.exports = { myFunction };\n\n// แบบ ESM\nimport fs from \'fs\';\nexport const myFunction = () => {};</code></pre> <h2>2. Built-in Modules พื้นฐานที่สำคัญ</h2> <p>Node.js มาพร้อมกับโมดูลมาตรฐานที่สามารถเรียกใช้งานได้ทันทีโดยไม่ต้องติดตั้งผ่าน NPM ได้แก่:</p> <ol style="padding-left:20px"> <li><strong>fs (File System):</strong> สำหรับอ่าน/เขียนไฟล์ สร้างไฟล์ ลบไฟล์ (เช่น <code>fs.readFile</code>, <code>fs.writeFile</code>) มีรูปแบบการใช้งานทั้งแบบ Callback, Sync (บล็อกการทำงาน) และ Promise-based</li> <li><strong>path:</strong> สำหรับจัดการเส้นทาง (Path) ของไฟล์ให้ทำงานได้ข้ามระบบปฏิบัติการอย่างปลอดภัย (เช่น <code>path.join(__dirname, \'folder\', \'file.txt\')</code>)</li> <li><strong>os:</strong> ให้ข้อมูลเกี่ยวกับระบบปฏิบัติการ เช่น หน่วยความจำ (<code>os.freemem()</code>), จำนวน CPU cores ซึ่งมีประโยชน์มากเวลานำไปทำ Clustering</li> <li><strong>http:</strong> สำหรับสร้าง Web Server ระดับต่ำสุด (Low-level) ซึ่งเป็นรากฐานที่สำคัญของ Framework อย่าง Express.js</li> </ol> <div class="tip-box"><strong>💡 Best Practice</strong>: แนะนำให้ใช้เวอร์ชัน Promise ของโมดูล <code>fs</code> เสมอ เช่น <code>const fs = require(\'fs/promises\')</code> เพื่อหลีกเลี่ยงปัญหา Callback Hell และสามารถใช้งานร่วมกับคีย์เวิร์ด <code>async/await</code> ได้อย่างเป็นระเบียบและอ่านง่าย</div> <h2>3. การจัดการ NPM Scripts</h2> <p>ในไฟล์ <code>package.json</code> เราสามารถตั้งค่า "scripts" เพื่อตั้งชื่อเล่นให้คำสั่งยาวๆ ได้:</p> <pre><code>{\n  "scripts": {\n    "start": "node index.js",\n    "dev": "nodemon index.js",\n    "test": "jest"\n  }\n}</code></pre> <p>จากนั้นเพียงพิมพ์ <code>npm run dev</code> ใน Terminal เพื่อเริ่มเซิร์ฟเวอร์สำหรับการพัฒนา (Development)</p>'
  },
  {
    number: 2,
    slug: 'express-setup',
    emoji: '🛠️',
    title: 'การติดตั้งและตั้งค่า Express (Express Setup)',
    content: '<h2>1. ทำความรู้จักกับ Express.js</h2> <p>Express.js เป็น <strong>Minimalist Web Framework</strong> สำหรับ Node.js ที่ได้รับความนิยมสูงสุดในโลก ช่วยลดความซับซ้อนของการใช้งาน Core module (อย่าง <code>http</code>) ทำให้การจัดการ HTTP requests, การสร้าง Routes และการเขียน Middleware ทำได้อย่างรวดเร็วและใช้โค้ดน้อยลงมหาศาล</p> <h2>2. การติดตั้ง Express และ Nodemon</h2> <p>เริ่มต้นโปรเจกต์ใหม่ด้วยการติดตั้ง Express พร้อมกับ <strong>Nodemon</strong> (เครื่องมือเสริมที่ช่วย restart เซิร์ฟเวอร์ Node.js ให้อัตโนมัติทุกครั้งที่มีการบันทึกไฟล์):</p> <pre><code>mkdir my-express-app\ncd my-express-app\nnpm init -y\nnpm install express\nnpm install -D nodemon</code></pre> <div class="tip-box"><strong>💡 เคล็ดลับ</strong>: การใช้ธง <code>-D</code> (หรือ <code>--save-dev</code>) เป็นการติดตั้ง Nodemon เฉพาะสำหรับสภาพแวดล้อม Development เท่านั้น เมื่อเรานำแอปพลิเคชันขึ้นสู่ Production จริง แพ็กเกจเหล่านี้จะไม่ถูกนำไปใช้งานให้เปลืองทรัพยากร</div> <h2>3. สร้างเซิร์ฟเวอร์ Express พื้นฐาน</h2> <p>สร้างไฟล์ <code>index.js</code> และเขียนโค้ดตั้งค่าเบื้องต้นดังนี้:</p> <pre><code>const express = require(\'express\');\nconst app = express();\nconst PORT = 3000;\n\n// ตั้งค่าให้ Express สามารถอ่านข้อมูล JSON จาก Request Body ได้\napp.use(express.json());\n\n// สร้าง Basic Route (เส้นทางแรกของแอปพลิเคชัน)\napp.get(\'/\', (req, res) => {\n  res.send(\'Hello, Express!\');\n});\n\n// เริ่มให้เซิร์ฟเวอร์รอรับ Request\napp.listen(PORT, () => {\n  console.log(\`Server is running on http://localhost:\${PORT}\`);\n});</code></pre> <h2>4. เจาะลึก Request และ Response Objects</h2> <p>ในทุกๆ Route handler (ฟังก์ชัน Callback ของ Route) เราจะได้รับ Object ที่เป็นหัวใจหลักของการทำงาน 2 ตัวเสมอ:</p> <ul> <li><strong>Request (req):</strong> บรรจุข้อมูลที่ส่งมาจาก Client (เช่น เบราว์เซอร์, โพสต์แมน, แอปพลิเคชันมือถือ) ข้อมูลสำคัญที่คุณจะได้ใช้บ่อยๆ เช่น <code>req.body</code> (เนื้อหาข้อมูล), <code>req.query</code> (ข้อมูลที่แนบมากับ URL), <code>req.headers</code> (ข้อมูลส่วนหัว เช่น Token), <code>req.params</code> (ตัวแปรใน URL)</li> <li><strong>Response (res):</strong> ออบเจ็กต์ที่ใช้สำหรับกำหนดและส่งข้อมูลกลับไปยัง Client เช่น <code>res.send()</code> (ส่งข้อมูลเป็น Text/HTML), <code>res.json()</code> (ส่งข้อมูลกลับเป็นโครงสร้าง JSON - ใช้บ่อยที่สุดในการทำ API), <code>res.status(404)</code> (ตั้งค่า HTTP Status Code เพื่อบอกสถานะการทำงาน)</li> </ul>'
  },
  {
    number: 3,
    slug: 'routing',
    emoji: '🖥️',
    title: 'การจัดการ Routing และ Controllers',
    content: '<h2>1. แนวคิดพื้นฐานของ Routing ใน Express</h2> <p><strong>Routing</strong> (หรือการกำหนดเส้นทาง) หมายถึง การกำหนดว่าแอปพลิเคชันของเราควรตอบสนองอย่างไรต่อ Client request เมื่อมีการร้องขอไปยัง <strong>URL (Endpoint)</strong> และ <strong>HTTP Method</strong> ที่เฉพาะเจาะจง</p> <p>HTTP Methods หลักที่นิยมใช้สร้าง RESTful API ตามมาตรฐานสากล ได้แก่:</p> <ul> <li><strong>GET:</strong> ใช้สำหรับอ่านหรือดึงข้อมูล (Read) จากเซิร์ฟเวอร์ ห้ามใช้เพื่อเปลี่ยนแปลงข้อมูลเด็ดขาด</li> <li><strong>POST:</strong> ใช้สำหรับส่งข้อมูลขึ้นไปเพื่อสร้างสิ่งใหม่บนเซิร์ฟเวอร์ (Create)</li> <li><strong>PUT / PATCH:</strong> ใช้สำหรับอัปเดตหรือแก้ไขข้อมูล (Update)</li> <li><strong>DELETE:</strong> ใช้สำหรับสั่งลบข้อมูล (Delete)</li> </ul> <h2>2. วิธีการรับข้อมูลจาก URL (Route Params & Query Strings)</h2> <p>การรับข้อมูลแบบไดนามิกจากฝั่งผู้ใช้งานทำได้ 2 รูปแบบหลัก:</p> <ol style="padding-left:20px"> <li><strong>Route Params (req.params):</strong> สำหรับข้อมูลที่เป็นส่วนหนึ่งของ Path มักใช้ระบุ ID ของสิ่งที่ต้องการเข้าถึง<br><code>app.get(\'/users/:id\', (req, res) => { console.log(req.params.id); });</code></li> <li><strong>Query Strings (req.query):</strong> สำหรับข้อมูลที่ต่อท้าย URL ด้วยเครื่องหมาย <code>?</code> (เช่น /search?keyword=node&page=2) มักใช้สำหรับการกรองข้อมูล (Filtering), การค้นหา, หรือการแบ่งหน้า (Pagination)<br><code>app.get(\'/search\', (req, res) => { console.log(req.query.keyword); });</code></li> </ol> <h2>3. การใช้งาน Express Router (แยกไฟล์อย่างเป็นระบบ)</h2> <p>เมื่อแอปพลิเคชันมีขนาดใหญ่ขึ้น การเขียน Routes ทั้งหมดไว้ในไฟล์ <code>index.js</code> จะทำให้ไฟล์บวมและจัดการยากมาก Express มีระบบ <strong>Router</strong> มาให้ใช้ ซึ่งทำหน้าที่เสมือน "mini-application" เพื่อให้เราแยกไฟล์แยกหมวดหมู่ได้อย่างเป็นระเบียบ</p> <pre><code>// สร้างไฟล์: routes/users.route.js\nconst express = require(\'express\');\nconst router = express.Router();\n\nrouter.get(\'/\', (req, res) => { res.json({ msg: \'Get all users\' }) });\nrouter.post(\'/\', (req, res) => { res.json({ msg: \'Create user\' }) });\n\nmodule.exports = router;</code></pre> <p>จากนั้นในไฟล์หลัก <code>index.js</code> ให้นำเข้าและเรียกใช้ผ่านคำสั่ง <code>app.use(\'/users\', userRoutes);</code> (หมายความว่าทุก route ในไฟล์นั้นจะถูกนำหน้าด้วย /users อัตโนมัติ)</p> <h2>4. ทำความรู้จัก Controller Pattern (สถาปัตยกรรม MVC)</h2> <p>เพื่อให้โค้ดดูสะอาดตาและบำรุงรักษาง่ายตามหลักการ MVC (Model-View-Controller) เราควรแยก Logic การทำงาน (เช่น การต่อฐานข้อมูล หรือการคำนวณ) ออกจากไฟล์ Routing ไปเก็บไว้ที่ไฟล์ <strong>Controllers</strong></p> <pre><code>// สร้างไฟล์: controllers/user.controller.js\nexports.getAllUsers = (req, res) => {\n  // Logic ดึงข้อมูลจาก Database สังเคราะห์ไว้ที่นี่\n  res.status(200).json({ success: true, data: [{ name: \'Alice\' }] });\n};\n\n// ไฟล์ routes/users.route.js ก็นำเข้ามาใช้:\nconst { getAllUsers } = require(\'../controllers/user.controller\');\nrouter.get(\'/\', getAllUsers);</code></pre> <div class="tip-box"><strong>💡 ประโยชน์</strong>: โค้ดจะอ่านง่ายขึ้นมาก ลดความซ้ำซ้อน นำกลับมาใช้ซ้ำได้ และทำให้การเขียน Unit Test เพื่อทดสอบการทำงานเฉพาะจุดสะดวกสบายขึ้นอย่างมหาศาล เพราะ Controller ถูกแยกมาเป็นฟังก์ชันอิสระอย่างชัดเจน</div>'
  },
  {
    number: 4,
    slug: 'middleware',
    emoji: '🔄',
    title: 'ทำความเข้าใจ Middleware',
    content: '<h2>1. Middleware คืออะไร?</h2> <p><strong>Middleware</strong> คือ ฟังก์ชันที่เข้ามา "คั่นกลาง" ระหว่างกระบวนการ Request (ขาเข้า) และ Response (ขาออก) ในวงจรการทำงานของ Express.js โดย Middleware จะสามารถเข้าถึง Object <code>req</code>, <code>res</code> และฟังก์ชันพิเศษที่เรียกว่า <code>next()</code> ได้เสมอ</p> <p>ความสามารถหลักของ Middleware ได้แก่:</p> <ul> <li>รันคำสั่งโค้ดต่างๆ เพิ่มเติมก่อนที่ Request จะไปถึง Route จริงๆ</li> <li>ตรวจสอบและแก้ไขเปลี่ยนแปลงข้อมูลใน request / response objects</li> <li>หยุดการทำงานของวงจรและตีกลับ (Reject) ทันที (เช่น หากรหัสผ่านผิด หรือไม่มีสิทธิ์)</li> <li>เรียกใช้งาน Middleware ตัวถัดไปในระบบ ด้วยฟังก์ชัน <code>next()</code></li> </ul> <div class="tip-box"><strong>💡 ข้อควรระวังขั้นสุด</strong>: ถ้า Middleware ไม่ออกจากการทำงานเพื่อส่งผลลัพธ์ผ่านคำสั่ง เช่น <code>res.send()</code> หรือ <code>res.json()</code> จะต้องเรียกคำสั่ง <code>next()</code> เสมอ ไม่เช่นนั้น Request ของ Client จะถูกแขวนทิ้งไว้ (Hanging) หน้าเว็บจะหมุนโหลดไปเรื่อยๆ จนกระทั่งเกิดอาการ Timeout</div> <h2>2. การเขียน Custom Middleware ของตัวเอง</h2> <p>เราสามารถเขียน Middleware ขึ้นเองเพื่อจัดการการทำงานเฉพาะทาง เช่น การสร้างตัวบันทึกประวัติการเข้าชม (Logger):</p> <pre><code>const requestLogger = (req, res, next) => {\n  console.log(\`[\${new Date().toISOString()}] เข้าใช้งานผ่าน: \${req.method} \${req.url}\`);\n  next(); // ต้องส่งต่อให้ Middleware/Route ลำดับถัดไปทำงานต่อเสมอ\n};\n\n// การนำไปใช้งานระดับ Global (ทำงานกับทุกๆ request)\napp.use(requestLogger); \n\n// การใช้งานเฉพาะ Route (คั่นกลางก่อนถึง Controller)\napp.get(\'/secret\', checkAuthToken, (req, res) => { /* ... */ });</code></pre> <h2>3. Built-in Middleware ที่ใช้บ่อย</h2> <p>Express มาพร้อมกับ Middleware ในตัวที่ทรงพลังและจำเป็นต้องใช้เกือบทุกโปรเจกต์ ได้แก่:</p> <ul> <li><code>express.json()</code> - ใช้แปลงข้อมูล request body ที่ถูกแนบมาเป็น application/json ให้กลายเป็น JavaScript Object (ช่วยสร้าง <code>req.body</code> ให้เราเรียกใช้)</li> <li><code>express.urlencoded({ extended: true })</code> - ใช้จัดการข้อมูลที่ส่งมาจาก HTML Form แบบดั้งเดิม</li> <li><code>express.static(\'public\')</code> - ใช้เปิดให้บริการไฟล์ Static (เช่น ไฟล์รูปภาพ, CSS, JavaScript) จากโฟลเดอร์ชื่อ public ให้อ่านได้โดยตรงผ่าน URL</li> </ul> <h2>4. Third-party Middleware ยอดนิยมที่ขาดไม่ได้</h2> <p>การพัฒนาแอปพลิเคชัน Node.js ระดับ Production มักจะต้องอาศัยแพ็กเกจจากภายนอก:</p> <ol style="padding-left:20px"> <li><strong>Morgan:</strong> (<code>npm install morgan</code>) สุดยอดเครื่องมือจัดทำ Logging ที่เก็บรายละเอียด Request/Response time ครบถ้วน เพื่อช่วยในการ Debug มหาศาล</li> <li><strong>Helmet:</strong> (<code>npm install helmet</code>) เพิ่มความปลอดภัยระดับสูงให้ Express App ด้วยการตั้งค่า HTTP Headers พิเศษตามมาตรฐานสากล เพื่อป้องกันช่องโหว่พื้นฐานยอดฮิตอย่าง XSS และ Clickjacking</li> <li><strong>CORS (Cross-Origin Resource Sharing):</strong> (<code>npm install cors</code>) เป็นกลไกอนุญาตให้ Domain อื่นๆ สามารถเรียกใช้งาน API ของเราได้ ซึ่งจำเป็นมากเมื่อคุณมี Frontend (เช่น React, Vue) ที่รันคนละ Port/Server กับฝั่ง Backend API</li> </ol> <pre><code>const cors = require(\'cors\');\napp.use(cors({ origin: \'http://localhost:5173\' })); // อนุญาตเฉพาะ Frontend ที่กำหนดเพื่อความปลอดภัยสูงสุด</code></pre>'
  },
  {
    number: 5,
    slug: 'database',
    emoji: '🗄️',
    title: 'เชื่อมต่อ Database ด้วย Prisma ORM',
    content: '<h2>1. Prisma ORM คืออะไร และทำไมถึงได้รับความนิยมสูงสุด?</h2> <p>การเชื่อมต่อฐานข้อมูลในอดีต มักใช้การเขียนคำสั่ง SQL เพียวๆ หรือใช้ ORM ยุคเก่าอย่าง Sequelize / Mongoose ซึ่งมักจะเจอปัญหาเรื่องการเดา Type ของข้อมูล แต่ <strong>Prisma</strong> เป็น ORM (Object-Relational Mapping) ยุคใหม่ (Next-generation) ที่ช่วยแก้ปัญหาเรื่อง Type Safety และมอบประสบการณ์การเขียนโค้ด (Developer Experience) ที่ยอดเยี่ยมที่สุดในโลกของ Node.js และ TypeScript ปัจจุบัน</p> <p>Prisma รองรับฐานข้อมูลหลากหลาย ทั้ง PostgreSQL, MySQL, SQLite, SQL Server และ MongoDB (แม้แต่ฐานข้อมูล NoSQL ก็ทำได้)</p> <h2>2. การตั้งค่าและติดตั้ง Prisma ครั้งแรก</h2> <p>ติดตั้ง Prisma และเริ่มต้นใช้งาน (ในตัวอย่างหลักสูตรนี้เราจะใช้ SQLite เพื่อความสะดวกรวดเร็วในการเรียนรู้):</p> <pre><code>npm install -D prisma\nnpm install @prisma/client\nnpx prisma init --datasource-provider sqlite</code></pre> <p>คำสั่ง init จะสร้างโฟลเดอร์ชื่อ <code>prisma/</code> ขึ้นมา พร้อมกับไฟล์ตั้งค่าสำคัญชื่อ <code>schema.prisma</code> และไฟล์รหัสผ่าน <code>.env</code></p> <h2>3. การออกแบบ Schema และ Relations (ความสัมพันธ์ของตาราง)</h2> <p>ไฟล์ <code>schema.prisma</code> ใช้สำหรับนิยามโครงสร้างตารางของฐานข้อมูล (Data Modeling) ตัวอย่างการออกแบบความสัมพันธ์แบบ One-to-Many ระหว่างตาราง User และ Post:</p> <pre><code>model User {\n  id        Int      @id @default(autoincrement())\n  email     String   @unique\n  name      String?\n  posts     Post[]   // กำหนดความสัมพันธ์ 1 User มีได้หลาย Post\n}\n\nmodel Post {\n  id        Int      @id @default(autoincrement())\n  title     String\n  content   String?\n  published Boolean  @default(false)\n  authorId  Int\n  author    User     @relation(fields: [authorId], references: [id])\n}</code></pre> <p>หลังจากนิยาม Schema เสร็จเรียบร้อย ให้ใช้คำสั่ง <strong>Migration</strong> เพื่อแปลง Schema นี้นำไปอัปเดตสร้างตารางบนฐานข้อมูลจริง:</p> <pre><code>npx prisma migrate dev --name init</code></pre> <h2>4. การเขียน CRUD Operations ด้วย Prisma Client</h2> <p>เมื่อสั่ง Migrate ระบบ Prisma จะสร้าง Prisma Client ให้อัตโนมัติในเบื้องหลัง ซึ่งเราสามารถนำมาเขียน CRUD (Create, Read, Update, Delete) ได้อย่างไหลลื่นและปลอดภัยจากการโดน SQL Injection:</p> <pre><code>const { PrismaClient } = require(\'@prisma/client\');\nconst prisma = new PrismaClient();\n\n// Create: เพิ่มผู้ใช้ใหม่\nconst newUser = await prisma.user.create({\n  data: { name: \'John Doe\', email: \'john@example.com\' }\n});\n\n// Read: ดึงข้อมูล (รวมข้อมูล Posts เข้ามาด้วย - เหมือนการสั่ง JOIN ใน SQL)\nconst users = await prisma.user.findMany({\n  include: { posts: true }\n});\n\n// Update: อัปเดตข้อมูลผู้ใช้\nconst updatedUser = await prisma.user.update({\n  where: { id: 1 },\n  data: { name: \'John Smith\' }\n});\n\n// Delete: ลบผู้ใช้\nconst deletedUser = await prisma.user.delete({\n  where: { id: 1 }\n});</code></pre> <div class="tip-box"><strong>💡 Prisma Studio เครื่องมือลับที่ทรงพลัง</strong>: Prisma มีหน้าต่าง UI สำหรับจัดการฐานข้อมูลที่สวยงามและใช้งานง่ายมาก สามารถเปิดดูผ่านคำสั่ง <code>npx prisma studio</code> ได้เลย ช่วยลดความจำเป็นในการลงโปรแกรมจัดการฐานข้อมูลภายนอก (อย่าง DBeaver) ได้เยอะเลยทีเดียว!</div>'
  },
  {
    number: 6,
    slug: 'auth',
    emoji: '🔒',
    title: 'ระบบ Authentication ด้วย JWT',
    content: '<h2>1. หลักการทำงานของ JWT (JSON Web Tokens)</h2> <p>พื้นฐานความปลอดภัย <strong>Authentication</strong> คือการยืนยันตัวตน (User เป็นใคร?) ส่วน <strong>Authorization</strong> คือการตรวจสอบสิทธิ์ (User ทำอะไรได้บ้าง?)</p> <p>ในยุคที่ Frontend กับ Backend แยกกันทำงาน JWT คือมาตรฐานยอดนิยมของการทำระบบยืนยันตัวตนแบบ <strong>Stateless</strong> หมายความว่า Server ไม่จำเป็นต้องกินหน่วยความจำเพื่อจำ Session ของทุกคน แต่จะอาศัยการเซ็นชื่อดิจิทัล (Signature) ออกมาเป็นรหัส Token และมอบให้ Client เก็บไว้ (มักจะเก็บใน LocalStorage หรือ HttpOnly Cookie) Client จะต้องแนบ Token นี้กลับมาทุกครั้งเมื่อร้องขอข้อมูลที่ถูกป้องกัน</p> <h2>2. การ Hash รหัสผ่านด้วย Bcrypt (เพื่อความปลอดภัยสูงสุด)</h2> <p><strong>กฎเหล็กความปลอดภัยของการทำแอปพลิเคชัน: ห้ามเก็บรหัสผ่านของ Users เป็นข้อความธรรมดา (Plain Text) ลงใน Database เด็ดขาด!</strong> เพราะหากข้อมูลหลุด รหัสผ่านทุกคนจะถูกเปิดเผยทันที เราต้องใช้กระบวนการเข้ารหัสทางเดียว (Hashing) ที่มีความปลอดภัยสูง เช่น <strong>bcrypt</strong> (<code>npm install bcrypt</code>)</p> <pre><code>const bcrypt = require(\'bcrypt\');\n\n// ในตอน Register (สมัครสมาชิก)\nconst saltRounds = 10;\nconst hashedPassword = await bcrypt.hash(req.body.password, saltRounds);\n// นำ hashedPassword ไปบันทึกลง Database แทนรหัสเดิม\n\n// ในตอน Login (เข้าสู่ระบบ)\nconst isMatch = await bcrypt.compare(req.body.password, dbUser.password);\n// จะได้ค่า boolean กลับมาว่าตรงกันหรือไม่</code></pre> <h2>3. การสร้าง Endpoint: Register และ Login</h2> <p>กระบวนการทำงานหลักๆ ของระบบมีดังนี้:</p> <p><strong>Register (สมัครสมาชิก):</strong> ตรวจสอบว่ามีอีเมลนี้ซ้ำในระบบไหม -> ทำการ Hash รหัสผ่าน -> บันทึก User ลงฐานข้อมูล<br> <strong>Login (เข้าสู่ระบบ):</strong> ค้นหา User ตามอีเมลจากฐานข้อมูล -> ตรวจสอบรหัสผ่านที่ส่งมาว่าตรงกันไหม (compare) -> สร้าง JWT Token ส่งคืนกลับไปให้ฝั่ง Client (<code>npm install jsonwebtoken</code>)</p> <pre><code>const jwt = require(\'jsonwebtoken\');\n\n// ภายใน Controller ฝั่ง Login, หากรหัสผ่านถูกต้องให้ทำตามนี้:\nconst payload = { id: dbUser.id, role: dbUser.role };\nconst secretKey = process.env.JWT_SECRET || \'fallback_super_secret_key\';\nconst token = jwt.sign(payload, secretKey, { expiresIn: \'1d\' }); // ให้ Token หมดอายุภายใน 1 วัน (24 ชั่วโมง)\n\nres.json({ success: true, token });</code></pre> <h2>4. การป้องกันเส้นทางด้วย Protected Middleware</h2> <p>เพื่อป้องกันไม่ให้ผู้ที่ไม่ได้ล็อกอินเข้าถึงข้อมูลส่วนตัว เราต้องสร้าง Custom Middleware เพื่อตรวจสอบ Header ว่ามี Token แนบมาหรือไม่ก่อนจะยอมให้เข้าสู่ Controller ภายใน</p> <pre><code>const jwt = require(\'jsonwebtoken\');\n\nexports.protect = (req, res, next) => {\n  // 1. ดึง Token จาก Header ในรูปแบบ \'Authorization: Bearer <token>\'\n  let token;\n  if (req.headers.authorization && req.headers.authorization.startsWith(\'Bearer\')) {\n    token = req.headers.authorization.split(\' \')[1];\n  }\n\n  // 2. ถ้าไม่มี Token ส่งมา ให้ปฏิเสธการเข้าถึง\n  if (!token) return res.status(401).json({ msg: \'Not authorized, no token provided\' });\n\n  // 3. ยืนยันความถูกต้องของ Token\n  try {\n    const decoded = jwt.verify(token, process.env.JWT_SECRET);\n    req.user = decoded; // ยัดข้อมูล User (เช่น id, role) เข้าไปใน Object req เพื่อให้ Controller ปลายทางนำไปใช้ต่อได้ทันที\n    next();\n  } catch (err) {\n    res.status(401).json({ msg: \'Not authorized, token failed or expired\' });\n  }\n};</code></pre> <div class="tip-box"><strong>💡 กฎการตั้งค่า JWT Secret</strong>: รหัสความลับ <code>JWT_SECRET</code> จะต้องยาวเพียงพอ, เป็นการสุ่มที่ยากต่อการคาดเดา, และต้องเก็บเป็นความลับในไฟล์ <code>.env</code> เสมอ ห้ามฮาร์ดโค้ด (Hardcode) หรือนำขึ้นฝากไว้บน Git Repository อย่างเด็ดขาด</div>'
  },
  {
    number: 7,
    slug: 'file-upload',
    emoji: '📁',
    title: 'การจัดการ File Upload ด้วย Multer',
    content: '<h2>1. ปัญหาของการจัดการ Multipart/Form-Data</h2> <p>โดยปกติแล้ว Express (แม้จะใช้ <code>express.json()</code> ก็ตาม) จะไม่สามารถอ่านข้อมูลไฟล์หรือรูปภาพที่ส่งมาจากฝั่งผู้ใช้ผ่านฟอร์มชนิด <code>multipart/form-data</code> (ฟอร์มมาตรฐานสำหรับการอัปโหลดไฟล์) ได้ด้วยตัวเอง เราจึงจำเป็นต้องพึ่งพา Middleware ยอดนิยมระดับตำนานที่ชื่อว่า <strong>Multer</strong> (<code>npm install multer</code>)</p> <h2>2. ตั้งค่า Multer พื้นฐาน (อัปโหลดเก็บใน Local Storage)</h2> <p>การอัปโหลดไฟล์เก็บไว้ในดิสก์ฮาร์ดไดรฟ์ของเซิร์ฟเวอร์โดยตรง สามารถทำได้ง่ายๆ โดยการกำหนดพารามิเตอร์ Destination (โฟลเดอร์ปลายทางที่เก็บไฟล์) และ Filename (การสร้างชื่อไฟล์ใหม่ให้ไม่ซ้ำกัน)</p> <pre><code>const multer = require(\'multer\');\nconst path = require(\'path\');\n\n// กำหนดกลยุทธ์การจัดเก็บ (Storage Engine)\nconst storage = multer.diskStorage({\n  destination: (req, file, cb) => {\n    // ต้องสร้างโฟลเดอร์ public/uploads รอไว้ล่วงหน้า\n    cb(null, \'public/uploads\'); \n  },\n  filename: (req, file, cb) => {\n    // กำหนดชื่อไฟล์เป็น วันที่ปัจจุบัน + ตัวเลขสุ่ม เพื่อป้องกันปัญหาการตั้งชื่อไฟล์ซ้ำกันและเขียนทับกันเอง\n    const uniqueSuffix = Date.now() + \'-\' + Math.round(Math.random() * 1E9);\n    cb(null, uniqueSuffix + path.extname(file.originalname));\n  }\n});\n\n// ฟิลเตอร์ (Filter) ความปลอดภัย: รับเฉพาะไฟล์ภาพเท่านั้น\nconst fileFilter = (req, file, cb) => {\n  if (file.mimetype.startsWith(\'image/\')) {\n    cb(null, true);\n  } else {\n    cb(new Error(\'ไม่อนุญาตให้อัปโหลด! รับเฉพาะไฟล์รูปภาพเท่านั้น\'), false);\n  }\n};\n\nconst upload = multer({ \n  storage, \n  fileFilter,\n  limits: { fileSize: 5 * 1024 * 1024 } // จำกัดขนาดไฟล์อัปโหลดไว้ที่ไม่เกิน 5MB\n});</code></pre> <h2>3. การนำ Multer ไปประยุกต์ใช้ใน Express Route</h2> <p>Multer สามารถถูกเรียกใช้เป็น Middleware คั่นระดับ Route เฉพาะจุดได้เลย ตัวอย่างเช่น การรับไฟล์เดี่ยวๆ ด้วยคำสั่ง <code>upload.single(\'field_name\')</code> หรือรับทีละหลายๆ ไฟล์พร้อมกันด้วย <code>upload.array()</code></p> <pre><code>app.post(\'/api/users/profile-pic\', upload.single(\'profilePic\'), (req, res) => {\n  if (!req.file) return res.status(400).send(\'No file uploaded or invalid file type\');\n  \n  // ไฟล์ถูกเซฟลงดิสก์เรียบร้อยแล้ว เราสามารถเอาเส้นทางของไฟล์ (Path) ไปบันทึกลง Database ได้เลย\n  res.json({ \n    message: \'อัปโหลดไฟล์สำเร็จ\', \n    url: \`/uploads/\${req.file.filename}\` \n  });\n});</code></pre> <h2>4. ก้าวไปอีกขั้น: แนะนำการใช้งานระบบ Cloud Storage (AWS S3)</h2> <p>การเก็บไฟล์เอาไว้บนดิสก์ของเซิร์ฟเวอร์โดยตรง มีข้อเสียร้ายแรงในระดับ Production คือ เมื่อแอปพลิเคชันของคุณถูก Scale ขยายระบบ (เพิ่มจำนวนเครื่องเซิร์ฟเวอร์เพื่อรับโหลด) หรือ นำไป Deploy บนระบบ PaaS อย่าง Heroku / Vercel / Render (ที่มีระบบไฟล์แบบชั่วคราว - Ephemeral Filesystem) ไฟล์อัปโหลดทั้งหมดของผู้ใช้จะหายวับไปกับตาทันทีที่เซิร์ฟเวอร์รีสตาร์ท!</p> <div class="tip-box"><strong>💡 Best Practice ระดับ Production สำหรับองค์กร</strong>: ในระบบที่มีผู้ใช้งานจริงและต้องการความเสถียร เรามักจะใช้ไลบรารี <code>multer-s3</code> ควบคู่กับ <strong>AWS SDK</strong> เพื่อทำการสตรีมไฟล์อัปโหลดจากแอป Node.js บินตรงไปเก็บไว้ที่ระบบคลัง Cloud Object Storage อย่าง <strong>AWS S3</strong> หรือ <strong>Google Cloud Storage</strong> แทน ทำให้ระบบ Backend ทำงานเบาลง แยกส่วนจากไฟล์รูปภาพได้อย่างสมบูรณ์ และรองรับโหลดจำนวนมหาศาลได้อย่างไม่มีปัญหา</div>'
  },
  {
    number: 8,
    slug: 'error-handling',
    emoji: '🚨',
    title: 'การจัดการข้อผิดพลาด (Error Handling)',
    content: '<h2>1. ความสำคัญของการจัดการข้อผิดพลาด</h2> <p>แอปพลิเคชันที่ดีไม่ควรพังหรือ Crash หยุดทำงานทันทีเมื่อเกิดข้อผิดพลาด และที่สำคัญที่สุด ไม่ควรส่ง Error Stack Trace (โค้ดแจ้งปัญหาที่ยาวเฟื้อย) กลับไปเปิดเผยโครงสร้างระบบให้ฝั่ง Client เห็นอย่างเด็ดขาดในระดับ Production การจัดการ Error ที่เป็นระบบจึงสำคัญอย่างยิ่งต่อทั้งเรื่องความเสถียร (Stability) และความปลอดภัย (Security)</p> <h2>2. Global Error Handling Middleware</h2> <p>ใน Express.js เราสามารถเขียน Middleware ตัวสุดท้ายที่ล่างสุดของแอป ซึ่งรับพารามิเตอร์ครบ 4 ตัว <code>(err, req, res, next)</code> ระบบ Express จะรับรู้ทันทีว่านี่คือ Error Middleware มันจะทำหน้าที่เป็นจุดรับ Error แบบรวมศูนย์จากทุกๆ ที่ในแอปพลิเคชัน</p> <pre><code>// วางโค้ดนี้ไว้ใต้ Routes ทั้งหมด ในไฟล์ index.js หรือ app.js\napp.use((err, req, res, next) => {\n  const statusCode = err.statusCode || 500;\n  const message = err.message || \'Internal Server Error (เกิดข้อผิดพลาดจากเซิร์ฟเวอร์)\';\n\n  // ส่ง JSON เป็นมาตรฐานเดียวกันทั้งหมด\n  res.status(statusCode).json({\n    success: false,\n    error: message,\n    stack: process.env.NODE_ENV === \'development\' ? err.stack : undefined // ซ่อน stack trace หากอยู่บน Production\n  });\n});</code></pre> <h2>3. การใช้ Custom App Error Class</h2> <p>การสร้าง Class เชิงวัตถุ (OOP) สำหรับจัดการ Error เฉพาะของแอป ช่วยให้เราส่งระบุ Status Code และแยกประเภท Error ออกจากบั๊กจริงๆ ได้ง่ายขึ้น</p> <pre><code>class AppError extends Error {\n  constructor(message, statusCode) {\n    super(message);\n    this.statusCode = statusCode;\n    this.isOperational = true; // ระบุว่าเป็นการแจ้งเตือนระบบ (Operational) ไม่ใช่บั๊กร้ายแรง\n    Error.captureStackTrace(this, this.constructor);\n  }\n}\n\n// วิธีเรียกใช้ใน Controller\nif (!userFound) return next(new AppError(\'ไม่พบผู้ใช้งานนี้ในระบบ\', 404));</code></pre> <h2>4. การจัดการ Async/Await Errors (โดยไม่ต้องเขียน Try/Catch จนล้นไฟล์)</h2> <p>โดยปกติฟังก์ชันของ Express ที่เป็น <code>async</code> หากมีข้อผิดพลาดเกิดขึ้น (เช่น ฐานข้อมูลตอบช้า) และไม่ได้มี <code>try/catch</code> คลุมไว้ Express จะจับ Error นั้นไม่ทันและทำให้เซิร์ฟเวอร์พังทันที วิธีแก้แบบดั้งเดิมคือเขียน <code>try/catch</code> ไว้ในทุกๆ Controller ซึ่งน่าเบื่อและซ้ำซ้อน</p> <div class="tip-box"><strong>💡 เคล็ดลับจากผู้เชี่ยวชาญ</strong>: ใช้แพ็กเกจ <code>express-async-errors</code> เพียงแค่สั่ง <code>require(\'express-async-errors\')</code> ไว้ที่บนสุดของไฟล์หลักเพียงบรรทัดเดียว มันจะทำการเข้าแก้ไข (Patch) ระบบ Express แบบอัตโนมัติ ทำให้เราเขียน Async Controller ต่อไปได้ยาวๆ โดยไม่ต้องเขียนบล็อก Try/Catch อีกเลย! หากเกิดอะไรขึ้น มันจะส่ง Error ทะลุลงไปหา Global Error Handler ให้ทันทีอย่างสวยงาม</div> <h2>5. Validation Errors (การคัดกรองข้อมูล)</h2> <p>การตรวจสอบข้อมูลขาเข้า (Input Validation) ก่อนบันทึกลง Database เป็นสิ่งที่ละเลยไม่ได้ แนะนำให้ใช้เครื่องมืออย่าง <strong>Zod</strong> หรือ <strong>Joi</strong> ในการสร้าง Schema ตรวจสอบ หากข้อมูลที่ Client ส่งมาไม่ตรงสเปค (เช่น รหัสผ่านสั้นเกินไป, รูปแบบอีเมลผิด) ระบบจะต้อง Throw Error 400 (Bad Request) เพื่อแจ้ง Client ให้แก้ไขข้อมูลทันทีโดยไม่ประมวลผลต่อ</p>'
  },
  {
    number: 9,
    slug: 'websocket',
    emoji: '🔌',
    title: 'สร้างแอป Real-time ด้วย Socket.io',
    content: '<h2>1. สถาปัตยกรรม HTTP vs WebSocket</h2> <p>ตามปกติ <strong>HTTP Protocol</strong> จะทำงานแบบ "Client ขอไป (Request) แล้ว Server จึงจะตอบกลับ (Response)" หาก Client ไม่ขอก่อน Server จะไม่สามารถยัดเยียดข้อมูล หรือแจ้งเตือนอะไรส่งกลับไปหา Client เองได้</p> <p>แต่ <strong>WebSocket Protocol</strong> แตกต่างออกไป มันเปิดการเชื่อมต่อแบบทางด่วนสองฝั่ง (Full-duplex) ผ่านท่อสัญญาณเดียวค้างเอาไว้ ทำให้ทั้งฝั่ง Client และ Server สามารถสาดข้อมูลตอบโต้กันไปมาได้ตลอดเวลาแบบ Real-time เหมาะสำหรับระบบ Chat, Notifications แจ้งเตือนแบบ Facebook, ระบบอัปเดตราคาคริปโต หรือเกมมัลติเพลเยอร์ออนไลน์</p> <h2>2. การติดตั้งและการตั้งค่า Socket.io พื้นฐาน</h2> <p><strong>Socket.io</strong> เป็นสุดยอดไลบรารียอดนิยมที่ครอบทับกลไก WebSocket ให้อีกที ช่วยให้การเรียกใช้งานง่ายขึ้นมหาศาล และมีระบบสำรอง (Fallback mechanism) ให้สามารถกลับไปใช้ HTTP Long-polling ได้หากตรวจพบว่าเบราว์เซอร์ของ Client เก่าเกินกว่าจะรองรับ WebSocket เริ่มต้นติดตั้งฝั่ง Backend เซิร์ฟเวอร์ด้วยคำสั่ง <code>npm install socket.io</code></p> <pre><code>const express = require(\'express\');\nconst http = require(\'http\');\nconst { Server } = require(\'socket.io\');\n\nconst app = express();\n// สร้าง HTTP Server ขึ้นมาครอบ Express App อีกชั้นหนึ่ง (เป็น Requirement ของ Socket.io)\nconst server = http.createServer(app); \n// นำ Socket.io มาผูกติดกับ HTTP Server พร้อมตั้งค่า CORS ให้ยอมรับ Connection จากภายนอก\nconst io = new Server(server, {\n  cors: { origin: \'*\' }\n});\n\n// ฟัง Event \'connection\' เมื่อมีผู้ใช้คนใหม่ต่อท่อเข้ามา\nio.on(\'connection\', (socket) => {\n  console.log(\`[🟢 Connected] ใช้งานได้แล้ว: \${socket.id}\`);\n\n  // รับฟังสัญญาณเมื่อผู้ใช้คนนี้ตัดการเชื่อมต่อ (ปิดหน้าเว็บ)\n  socket.on(\'disconnect\', () => {\n    console.log(\`[🔴 Disconnected] ขาดการเชื่อมต่อ: \${socket.id}\`);\n  });\n});\n\nserver.listen(3000, () => console.log(\'Server API & Socket.io is running on port 3000\'));</code></pre> <h2>3. ระบบส่งและรับข้อความ (Emitting and Listening Events)</h2> <p>Socket.io ใช้ระบบ Events ผ่านคำสั่งหลักๆ คือ <code>emit</code> (ส่งกระจายเสียงออกไป) และ <code>on</code> (เงี่ยหูรอฟังเสียง)</p> <pre><code>// สมมติ: รอฟังข้อความแชทจาก Client\nsocket.on(\'chat_message\', (data) => {\n  console.log(\'ได้รับข้อความจาก Client:\', data);\n  \n  // 1. Server ส่งข้อความนี้ (Broadcast) กระจายกลับไปให้ผู้ใช้ "ทุกคน" ในระบบได้เห็นพร้อมกัน\n  io.emit(\'receive_message\', data);\n  \n  // 2. หรือ ถ้าต้องการแจ้งผู้ใช้ทุกคน "ยกเว้น" คนส่ง (เช่น เสียงแจ้งเตือน Notification):\n  // socket.broadcast.emit(\'receive_message\', data);\n});</code></pre> <h2>4. ระบบห้องส่วนตัวด้วย Rooms และ Namespaces</h2> <div class="tip-box"><strong>💡 เคล็ดลับการแบ่งห้อง Chat</strong>: หากคุณต้องการสร้างระบบ "ห้องแชทส่วนตัว (Private Room)" ไม่จำเป็นต้องเขียนโค้ดจัดการรายชื่อผู้ใช้วุ่นวาย คุณสามารถใช้ฟีเจอร์ระดับเทพ <strong>Rooms</strong> ใน Socket.io ได้ง่ายๆ โดยใช้คำสั่งให้ผู้ใช้เข้าห้องด้วย <code>socket.join(\'room_id_123\')</code> จากนั้นก็สามารถยิงข้อความเฉพาะเจาะจงให้ดังขึ้นแค่ในห้องนั้นได้ด้วยคำสั่ง <code>io.to(\'room_id_123\').emit(...)</code> อย่างง่ายดาย</div>'
  },
  {
    number: 10,
    slug: 'testing',
    emoji: '🧪',
    title: 'การเขียน Automated Testing สำหรับ API',
    content: '<h2>1. ทำไมเราต้องเสียเวลาเขียน Test?</h2> <p>ความน่ากลัวของการเขียนระบบขนาดใหญ่ คือเวลาเพิ่มโค้ดใหม่มักจะไปทำให้ฟีเจอร์เก่าที่เคยใช้งานได้พังโดยไม่รู้ตัว (เราเรียกอาการนี้ว่า Regression bugs) การเขียน <strong>Automated Testing (ชุดทดสอบอัตโนมัติ)</strong> จะช่วยการันตีเสถียรภาพ สร้างความมั่นใจในการแก้ไขโครงสร้างโค้ด (Refactor) ให้กับนักพัฒนา และถือเป็นส่วนที่แบ่งแยกโปรแกรมเมอร์สมัครเล่น ออกจากวิศวกรซอฟต์แวร์ระดับมืออาชีพ</p> <p>เรามักจะทดสอบ Node.js API ผ่านเครื่องมือคู่บุญ 2 ตัวหลัก:</p> <ul> <li><strong>Jest:</strong> โครงสร้าง Testing Framework ระดับแนวหน้าจาก Facebook ให้คำสั่งพื้นฐานอย่าง <code>describe()</code>, <code>it()</code> และระบบปลอมแปลงข้อมูล (Mocking) ติดตั้งด้วย <code>npm install -D jest</code></li> <li><strong>Supertest:</strong> ไลบรารีสำหรับจำลองยิง HTTP Request (เหมือนเป็น Postman อัตโนมัติ) วิ่งเข้าไปที่ไฟล์ Express App ของเราโดยตรง เพื่อเช็คผลลัพธ์ ติดตั้งด้วย <code>npm install -D supertest</code></li> </ul> <h2>2. ตั้งค่าการรัน Test เบื้องต้น</h2> <p>ในไฟล์ <code>package.json</code> เพิ่มสคริปต์ <code>"test": "jest --watchAll"</code> เพื่อให้ Jest คอยสอดส่องและรันเทสต์อัตโนมัติเวลาเราแก้ไฟล์</p> <p>สิ่งสำคัญ: เราต้องแยกเซิร์ฟเวอร์ Express ออกเป็น 2 ไฟล์ คือ <code>app.js</code> (กำหนด Route ทั้งหมด แต่<strong>ห้าม</strong>สั่ง app.listen) และ <code>server.js</code> (เรียกใช้ <code>app.js</code> แล้วค่อยสั่ง app.listen) เพื่อให้ Supertest สามารถดึงเฉพาะ Logic ไปรันในสภาพแวดล้อมจำลองแบบเงียบๆ ได้ โดยไม่พอร์ตชนกัน</p> <h2>3. การเขียน Integration Test สำหรับตรวจสอบ API</h2> <p>ตัวอย่างการทดสอบ Route ที่ดึงข้อมูล User ว่าทำงานถูกต้องไหม:</p> <pre><code>// ชื่อไฟล์: users.test.js\nconst request = require(\'supertest\');\nconst app = require(\'../app\'); \n\ndescribe(\'ทดสอบระบบ Route ของส่วน User (GET /api/users)\', () => {\n\n  it(\'ควรส่งกลับ status 200 และข้อมูลใน body.data ต้องเป็น Array เสมอ\', async () => {\n    // ใช้ Supertest ยิงไปที่แอปของเรา\n    const res = await request(app).get(\'/api/users\');\n    \n    // ตั้งความคาดหวัง (Expectations)\n    expect(res.statusCode).toBe(200);\n    expect(Array.isArray(res.body.data)).toBeTruthy();\n  });\n\n  it(\'ควรคืนค่า 404 Not Found ถ้าเรียกเส้นทางมั่วๆ ที่ไม่มีอยู่จริง\', async () => {\n    const res = await request(app).get(\'/api/users/not-found-route-qwerty\');\n    expect(res.statusCode).toBe(404);\n  });\n\n});</code></pre> <h2>4. วงจรของ Test (Setup & Teardown) และการทำ Mocking</h2> <p>หากทดสอบการเขียนข้อมูลกับฐานข้อมูลจริง เราต้องมีกระบวนการล้างฐานข้อมูล (Clean up) ก่อนและหลัง รันเทสต์เสมอ (เรามักใช้ SQLite แบบ in-memory พิเศษสำหรับการรันเทสต์ เพื่อความรวดเร็ว)</p> <pre><code>beforeAll(async () => { /* เริ่มเชื่อม Database จำลอง, เตรียมตารางเปล่า */ });\nafterAll(async () => { /* เสร็จงาน, สั่งลบตาราง ตัดการเชื่อมต่อ Database ให้เกลี้ยง */ });\nbeforeEach(async () => { /* เคลียร์ข้อมูลทุกๆ รอบ เพื่อป้องกันไม่ให้เทสต์แต่ละเคสมีข้อมูลตกค้างตีกันเอง */ });</code></pre> <div class="tip-box"><strong>💡 ศิลปะแห่งการ Mocking</strong>: หาก API ยิงไปเรียกบริการ Third-party ภายนอกที่ต้องเสียเงิน (เช่น API ตัดบัตรเครดิตของ Stripe หรือ ส่ง SMS ของ Twilio) เวลาเรารันเทสต์ร้อยๆ รอบ เราไม่ควรให้มันเรียกไปตัดเงินจริงๆ เด็ดขาด! เราจะใช้ฟังก์ชัน <code>jest.mock(\'stripe\')</code> เพื่อสร้าง "สแตนด์อิน" (Mock) เข้ามาสวมรอยตอบกลับแบบหลอกๆ แทน ถือเป็นเทคนิคสำคัญที่ต้องเรียนรู้!</div>'
  },
  {
    number: 11,
    slug: 'project',
    emoji: '🛠️',
    title: 'โปรเจกต์สุดท้าย: สร้าง REST API ครบวงจร (E-Commerce)',
    content: '<h2>1. โครงสร้างโปรเจกต์สเกลใหญ่ที่แนะนำ (Enterprise Project Structure)</h2> <p>เมื่อแอปพลิเคชันของคุณขยายใหญ่ขึ้น การจัดเรียงโฟลเดอร์ให้เหมาะสมจะทำให้ทีมทำงานง่าย หาไฟล์เจอง่าย สถาปัตยกรรมที่ได้รับความนิยมสูงสุดในยุคปัจจุบันคือการแบ่งเลเยอร์ (<strong>Layered Architecture</strong>):</p> <pre><code>src/\n ├── config/       # รวมไฟล์ตั้งค่าเชื่อมต่อ Database, และกำหนดค่า Environment (.env config)\n ├── controllers/  # ตรรกะของแต่ละ Route (ทำหน้าที่แค่: รับ Request มา, โยนไปให้ Service ประมวลผล, ตอบ Response กลับไป)\n ├── middlewares/  # ไฟล์ Middleware พิเศษ (ตรวจ Auth token, จัดการ Global Error, ตัวอัปโหลด Multer)\n ├── routes/       # จุดศูนย์กลางกำหนด HTTP Methods & Endpoints แปะคู่กับ Controller\n ├── services/     # (ทางเลือก) จัดการ Business Logic ก้อนใหญ่ๆ ที่ซับซ้อนสุดๆ ไม่ผูกมัดตัวเองกับ Express HTTP\n ├── prisma/       # แหล่งรวม Schema ฐานข้อมูลและไฟล์ประวัติ Migrations ทั้งหมด\n └── app.js        # ตัวรวบรวม Express App (จุดเชื่อมต่อ Routes ทุกอย่าง)\nserver.js          # จุดเริ่มรันเซิร์ฟเวอร์ด้วย app.listen (ไฟล์ Entry point ของจริง)</code></pre> <h2>2. สิ่งที่ E-Commerce API ของคุณควรต้องมี (Checklist)</h2> <p>เพื่อนำความรู้ทั้งหมดมาประยุกต์ใช้ นี่คือฟีเจอร์พื้นฐานที่คุณควรสร้างขึ้น:</p> <ol style="padding-left:20px"> <li><strong>Authentication (ระบบจัดการ Users):</strong> ระบบสมัครสมาชิก, เข้ารหัสพาสเวิร์ดด้วย Bcrypt, เข้าสู่ระบบด้วย JWT, และแยกสิทธิ์ (Role Authorization) ระหว่างลูกค้ายูสเซอร์ปกติกับผู้ดูแลระบบ (Admin)</li> <li><strong>Products Management (ระบบจัดการสต็อกสินค้า):</strong> <ul> <li><strong>Admin:</strong> มีสิทธิ์ระดับสูงเพื่อสามารถสร้าง แก้ไข ลบ ข้อมูลสินค้า และอัปโหลดรูปภาพสินค้าขึ้น S3 ผ่าน Multer ได้</li> <li><strong>Customer:</strong> เรียกดูรายการสินค้า, ค้นหา, กรองหมวดหมู่ (ใช้ Query Strings เข้ามาช่วยดึง Data), และทำระบบแบ่งหน้าเว็บทีละ 20 ชิ้น (Pagination) เพื่อไม่ให้โหลดนานเกินไป</li> </ul> </li> <li><strong>Shopping Cart & Orders (ระบบตะกร้าและสั่งซื้อ):</strong> <ul> <li>จำลองตะกร้าสินค้า อาจเก็บไว้ใน Session ฐานข้อมูล หรือระบบที่เร็วกว่าอย่าง Redis</li> <li>การกดชำระเงิน (Checkout API) สร้าง Order ใหม่ (ซึ่งต้องใช้เทคนิค Database Transaction ของ Prisma: ตัดสต็อกสินค้าคงคลัง และ สร้างคำสั่งซื้อใหม่พร้อมกันในจังหวะเดียว หากจังหวะใดพัง ให้ระบบ Rollback ยกเลิกทุกอย่างทิ้งทันที เพื่อป้องกันปัญหาบัตรตัดเงินแต่ไม่ได้ของ)</li> </ul> </li> <li><strong>Error Handling & Validation:</strong> ป้องกันข้อมูลขยะหรือการแฮ็กแปลกๆ เข้ามาในระบบฐานข้อมูล และคืนค่าข้อความ Error ที่ชัดเจนอ่านรู้เรื่องเมื่อลูกค้ากรอกข้อมูลผิดพลาดหรือตกหล่น</li> </ol> <h2>3. การเตรียมความพร้อมสู่การขึ้น Production (Deployment)</h2> <p>ก่อนนำ API ของคุณขึ้นสู่ Server จริง (เช่น VPS, AWS EC2 หรือ DigitalOcean) ตรวจสอบ Checklists ข้อควรระวังขั้นสุดยอดเหล่านี้:</p> <ul> <li>✅ <strong>Environment Variables:</strong> เก็บความลับเช่น <code>DATABASE_URL</code>, <code>JWT_SECRET</code>, <code>AWS_ACCESS_KEY</code> ไว้ในไฟล์ <code>.env</code> เสมอ อย่าเผลอคอมมิตอัปโหลดขึ้น Github สาธารณะเด็ดขาด</li> <li>✅ <strong>CORS Strategy:</strong> ต้องเปิดรับ Request อนุญาตเฉพาะจากชื่อ Domain (URL) ฝั่งเว็บ Frontend ของคุณเองเท่านั้น เพื่อป้องกันคนอื่นลักลอบเอา API ของเราไปยิงใช้งานฟรีๆ</li> <li>✅ <strong>Security Headers & Rate Limiting:</strong> ใช้งาน <code>Helmet</code> ปิดช่องโหว่ความปลอดภัยมาตรฐาน และกำหนด Rate Limiting (ด้วยแพ็กเกจ <code>express-rate-limit</code>) เพื่อป้องกันการโจมตีแบบยิงรีเควสถล่มรัวๆ (DDoS Attack / Brute-force Login) เบื้องต้น</li> <li>✅ <strong>PM2 Process Manager:</strong> ในตอนรัน production ไม่ควรใช้ <code>nodemon</code> (มันไว้ใช้ตอนเขียนโค้ด) หรือ <code>node server.js</code> ธรรมดา (พอมัน Error แอปจะดับยาว) แต่เราต้องใช้เครื่องมืออย่าง <strong>PM2</strong> (<code>npm install -g pm2</code>) ที่ช่วยรัน Node.js เป็น Background Service แถมยังใช้ CPU ได้ครบทุก Cores พร้อมฟังก์ชันเทพที่จะ "รีสตาร์ทเซิร์ฟเวอร์ให้อัตโนมัติในเสี้ยววินาที" หากตัวโปรแกรมพังหรือเจอ Error</li> </ul> <div class="tip-box"><strong>💡 ยินดีด้วยครับ! คุณมาถึงเส้นชัยแล้ว</strong>: หากคุณเข้าใจคอนเซปต์ของคอร์สนี้ครบถ้วน คุณก็มีทักษะพร้อมสำหรับการเป็น Backend Developer สาย Node.js เต็มตัวแล้ว สิ่งสำคัญลำดับต่อไปคือ "การลงมือเขียนโค้ดจริงๆ" พยายามนำชิ้นส่วนต่างๆ เหล่านี้ไปประกอบร่างสร้างโปรเจกต์ในฝันของตัวเองขึ้นมา จะทำให้คุณเข้าใจภาพรวมเชิงลึกได้อย่างยอดเยี่ยมที่สุดครับ สู้ๆ!</div>'
  }
];
