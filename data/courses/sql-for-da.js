export const chapters = [
  {
    number: 1,
    slug: 'overview',
    emoji: '🗄️',
    title: 'SQL สำหรับ DA คืออะไร และต่างจาก DE อย่างไร',
    content: '<h2>SQL สำหรับ Data Analyst คืออะไร</h2><p>SQL (Structured Query Language) คือภาษาที่ใช้สื่อสารกับฐานข้อมูลเชิงสัมพันธ์ (Relational Database) และเป็นทักษะที่สำคัญที่สุดอันดับหนึ่งของ Data Analyst ทุกคน ไม่ว่าคุณจะทำงานในบริษัทสตาร์ทอัพเล็กๆ หรือองค์กรขนาดใหญ่ระดับโลก SQL คือเครื่องมือที่คุณจะต้องใช้ทุกวัน</p><h3>DA ใช้ SQL อย่างไร</h3><p>Data Analyst ใช้ SQL เพื่อดึงข้อมูล (Query) วิเคราะห์ข้อมูล สร้างรายงาน และตอบคำถามทางธุรกิจ เช่น ยอดขายเดือนนี้เท่าไหร่ ลูกค้ากลุ่มไหนซื้อสินค้ามากที่สุด หรือสินค้าใดมีอัตราการคืนสินค้าสูง งานของ DA มุ่งเน้นที่การ "อ่าน" และ "ตีความ" ข้อมูล มากกว่าการสร้างหรือดูแลระบบฐานข้อมูล</p><h3>DA vs DE ใช้ SQL ต่างกันอย่างไร</h3><p>Data Engineer (DE) ใช้ SQL เพื่อสร้าง Data Pipeline ออกแบบ Schema จัดการ ETL/ELT Process และดูแล Performance ของระบบฐานข้อมูลทั้งหมด ในขณะที่ DA มุ่งเน้นที่การ Query ข้อมูลที่มีอยู่แล้ว สร้าง Report และ Dashboard รวมถึงวิเคราะห์ Business Metrics ต่างๆ ความแตกต่างสำคัญคือ DE สร้างและดูแลระบบ ส่วน DA ใช้ระบบเพื่อหาคำตอบ</p><h3>ทำไม SQL ถึงสำคัญสำหรับ DA</h3><p>ในปัจจุบัน ข้อมูลส่วนใหญ่ขององค์กรถูกเก็บอยู่ในฐานข้อมูล ไม่ว่าจะเป็น MySQL, PostgreSQL, BigQuery, Snowflake หรือ Redshift การที่ DA สามารถดึงข้อมูลได้เองโดยไม่ต้องรอ DE ทำให้ทำงานได้เร็วขึ้นมาก และสามารถตอบคำถามธุรกิจได้อย่างรวดเร็ว นอกจากนี้ SQL ยังเป็นพื้นฐานที่ทำให้เข้าใจเครื่องมืออื่นๆ ได้ดีขึ้น เช่น dbt, Looker, หรือแม้แต่ pandas ใน Python</p><h3>ฐานข้อมูลที่ DA ควรรู้จัก</h3><p>มีหลายประเภทที่พบบ่อยในตลาดงาน ได้แก่ PostgreSQL ซึ่งเป็น Open Source และนิยมในบริษัทเทคโนโลยี, MySQL ที่ใช้กันอย่างแพร่หลายใน E-commerce, BigQuery ของ Google ที่เหมาะกับ Big Data, Snowflake ที่กำลังเป็นที่นิยมมากในองค์กรขนาดใหญ่ และ Microsoft SQL Server ที่พบในองค์กรไทยหลายแห่ง การเรียนรู้ SQL พื้นฐานจะใช้ได้กับทุกระบบ แม้ Syntax บางส่วนอาจแตกต่างกันเล็กน้อย</p><div class="tip-box"><strong>💡 Tip สำหรับมือใหม่:</strong> เริ่มต้นด้วย PostgreSQL หรือ SQLite ซึ่งเป็น Free และติดตั้งง่าย แนะนำให้ฝึกบน DB Fiddle หรือ Mode Analytics ที่สามารถ Run SQL ออนไลน์ได้โดยไม่ต้องติดตั้งอะไรเพิ่ม</div><h3>สิ่งที่จะได้เรียนในคอร์สนี้</h3><p>คอร์สนี้จะพาคุณผ่านทุก SQL Skills ที่ DA ต้องรู้ ตั้งแต่ SELECT พื้นฐาน ไปจนถึง Window Functions, CTEs และ Query Optimization เนื้อหาแต่ละบทถูกออกแบบมาให้ใช้งานได้จริงในชีวิตการทำงาน พร้อม Business Cases ที่คุณจะเจอบ่อย เมื่อจบคอร์สนี้ คุณจะสามารถตอบคำถามธุรกิจได้ด้วย SQL อย่างมั่นใจ</p>'
  },
  {
    number: 2,
    slug: 'select-basics',
    emoji: '🔍',
    title: 'SELECT, WHERE, ORDER BY — พื้นฐานที่ต้องแม่น',
    content: '<h2>SELECT: หัวใจของการดึงข้อมูล</h2><p>คำสั่ง SELECT คือจุดเริ่มต้นของทุก Query ใน SQL มันบอกฐานข้อมูลว่าคุณต้องการดูคอลัมน์อะไรบ้าง รูปแบบพื้นฐานคือ SELECT column1, column2 FROM table_name ซึ่งดูเรียบง่าย แต่มีรายละเอียดที่ต้องเข้าใจเพื่อใช้งานได้อย่างมีประสิทธิภาพ</p><h3>SELECT พื้นฐาน</h3><p>ตัวอย่างการ Query ข้อมูลลูกค้า: SELECT customer_id, customer_name, email FROM customers จะดึงเฉพาะ 3 คอลัมน์ที่ระบุ หากต้องการทุกคอลัมน์ใช้ SELECT * แต่ในทางปฏิบัติไม่แนะนำให้ใช้ SELECT * ใน Production Query เพราะทำให้ Query ช้าและดูแลยาก ควรระบุคอลัมน์ที่ต้องการเสมอ</p><h3>การใช้ Alias ด้วย AS</h3><p>การตั้งชื่อให้คอลัมน์ใหม่ด้วย AS ช่วยให้ผลลัพธ์อ่านง่ายขึ้น เช่น SELECT customer_name AS "ชื่อลูกค้า", total_purchase AS "ยอดซื้อรวม" FROM customers นอกจากนี้ยังสามารถทำ Calculation ใน SELECT ได้โดยตรง เช่น SELECT price * quantity AS total_amount FROM order_items</p><h3>WHERE: การกรองข้อมูล</h3><p>WHERE ช่วยกรองเฉพาะแถวที่ตรงตามเงื่อนไขที่ต้องการ ตัวอย่าง SELECT * FROM orders WHERE status = "completed" จะได้เฉพาะออเดอร์ที่เสร็จสมบูรณ์ Operators ที่ใช้ได้ใน WHERE ได้แก่ = (เท่ากับ), != หรือ <> (ไม่เท่ากับ), > (มากกว่า), < (น้อยกว่า), >= (มากกว่าหรือเท่ากับ), <= (น้อยกว่าหรือเท่ากับ)</p><h3>AND, OR, NOT</h3><p>สามารถรวมเงื่อนไขหลายๆ อย่างได้ด้วย AND และ OR เช่น SELECT * FROM customers WHERE country = "Thailand" AND age >= 25 AND is_active = true จะได้ลูกค้าไทยที่อายุ 25 ปีขึ้นไปและยังใช้งานอยู่ การใช้ OR เช่น WHERE city = "Bangkok" OR city = "Chiang Mai" จะได้ลูกค้าในกรุงเทพหรือเชียงใหม่</p><h3>ORDER BY: การเรียงลำดับ</h3><p>ORDER BY ใช้เรียงผลลัพธ์ตามคอลัมน์ที่ต้องการ ASC คือเรียงจากน้อยไปมาก (Default), DESC คือเรียงจากมากไปน้อย ตัวอย่าง SELECT customer_name, total_purchase FROM customers ORDER BY total_purchase DESC จะแสดงลูกค้าที่มียอดซื้อมากที่สุดก่อน สามารถ Order by หลายคอลัมน์ได้ เช่น ORDER BY country ASC, total_purchase DESC</p><h3>LIMIT และ OFFSET</h3><p>LIMIT ใช้จำกัดจำนวนแถวที่ต้องการ เช่น SELECT * FROM products ORDER BY price DESC LIMIT 10 จะได้สินค้า 10 ชิ้นที่แพงที่สุด OFFSET ใช้ข้ามแถว เช่น LIMIT 10 OFFSET 20 จะข้าม 20 แถวแรกแล้วดึงมา 10 แถว มักใช้ในการทำ Pagination</p><div class="tip-box"><strong>💡 Best Practice:</strong> ในการทำงานจริง ให้เพิ่ม LIMIT ทุกครั้งที่ Test Query ใหม่ เพื่อป้องกันการดึงข้อมูลหลักล้านแถวโดยไม่ตั้งใจ ซึ่งอาจทำให้ระบบช้าหรือเสียค่าใช้จ่ายสูงใน Cloud Database</div><h3>DISTINCT: กรองค่าซ้ำ</h3><p>SELECT DISTINCT ใช้เมื่อต้องการค่าที่ไม่ซ้ำกัน เช่น SELECT DISTINCT country FROM customers จะได้รายชื่อประเทศทั้งหมดที่มีลูกค้า โดยไม่ซ้ำ ประโยชน์มากเมื่อต้องการรู้ว่ามีค่าอะไรบ้างในคอลัมน์หนึ่งๆ</p>'
  },
  {
    number: 3,
    slug: 'filtering',
    emoji: '🎯',
    title: 'Filtering ชั้นสูง — IN, BETWEEN, LIKE, NULL',
    content: '<h2>Filtering ข้อมูลอย่างมีประสิทธิภาพ</h2><p>การกรองข้อมูลเป็นงานที่ DA ทำมากที่สุด การรู้จัก Operators ครบชุดจะช่วยให้เขียน Query ได้กระชับและมีประสิทธิภาพมากขึ้น บทนี้จะพาคุณรู้จัก IN, BETWEEN, LIKE, IS NULL และ Operators อื่นๆ ที่ใช้บ่อยในงานจริง</p><h3>IN Operator</h3><p>IN ใช้กรองข้อมูลที่ตรงกับค่าใดค่าหนึ่งในรายการ แทนที่จะเขียน WHERE country = "Thailand" OR country = "Vietnam" OR country = "Singapore" สามารถเขียนสั้นลงเป็น WHERE country IN ("Thailand", "Vietnam", "Singapore") ได้เลย ทำให้ Code อ่านง่ายขึ้นมาก และยังใช้กับ Subquery ได้ด้วย เช่น WHERE product_id IN (SELECT product_id FROM top_products)</p><h3>NOT IN Operator</h3><p>ตรงข้ามกับ IN คือ NOT IN ซึ่งกรองข้อมูลที่ไม่อยู่ในรายการ เช่น WHERE status NOT IN ("cancelled", "refunded") จะได้ทุก Order ยกเว้นที่ถูกยกเลิกหรือคืนเงิน ข้อควรระวังคือ NOT IN จะไม่ทำงานตามที่คาดถ้ามี NULL ในรายการ ควรระวังเรื่องนี้เสมอ</p><h3>BETWEEN Operator</h3><p>BETWEEN ใช้กรองค่าในช่วงที่กำหนด เช่น WHERE age BETWEEN 25 AND 35 หรือ WHERE order_date BETWEEN "2024-01-01" AND "2024-12-31" สำคัญมากคือ BETWEEN เป็น Inclusive ทั้งสองด้าน หมายความว่ารวมค่า 25 และ 35 ด้วย สามารถใช้กับตัวเลข วันที่ และข้อความได้</p><h3>LIKE Operator — Pattern Matching</h3><p>LIKE ใช้ค้นหาข้อมูลตามรูปแบบ Pattern โดยใช้ Wildcard สองตัว คือ % แทนอักขระใดๆ กี่ตัวก็ได้ และ _ แทนอักขระเดียว ตัวอย่าง WHERE email LIKE "%@gmail.com" จะได้ทุก Email ที่ลงท้ายด้วย @gmail.com และ WHERE name LIKE "ส%" จะได้ชื่อทุกชื่อที่ขึ้นต้นด้วย ส ส่วน WHERE code LIKE "TH__" จะได้ code ที่ขึ้นต้นด้วย TH ตามด้วยอักขระ 2 ตัวพอดี</p><h3>ILIKE — Case Insensitive</h3><p>ใน PostgreSQL มี ILIKE ซึ่งทำงานเหมือน LIKE แต่ไม่คำนึงถึงตัวพิมพ์ใหญ่-เล็ก เช่น WHERE email ILIKE "%@GMAIL.COM" จะได้ผลเหมือนกับ "%@gmail.com" มีประโยชน์มากเวลาข้อมูลในฐานข้อมูลไม่ได้ Standardize</p><h3>NULL: ค่าที่ไม่มีอยู่</h3><p>NULL เป็นแนวคิดพิเศษใน SQL หมายถึงค่าที่ไม่มีอยู่หรือไม่รู้ค่า NULL ไม่เท่ากับ 0 หรือ String ว่าง การเปรียบเทียบ NULL ต้องใช้ IS NULL หรือ IS NOT NULL เท่านั้น เช่น WHERE phone IS NULL จะได้ลูกค้าที่ไม่มีเบอร์โทร และ WHERE phone IS NOT NULL จะได้ลูกค้าที่มีเบอร์โทร การเขียน WHERE phone = NULL จะไม่ทำงานเลย</p><h3>COALESCE และ NULLIF</h3><p>COALESCE ใช้แทนค่า NULL ด้วยค่าอื่น เช่น SELECT COALESCE(phone, "ไม่มีเบอร์") AS phone FROM customers จะแสดง "ไม่มีเบอร์" แทน NULL NULLIF ทำตรงข้าม คือแปลงค่าที่กำหนดให้กลายเป็น NULL เช่น NULLIF(discount, 0) จะแปลง 0 เป็น NULL ประโยชน์มากในการป้องกัน Division by Zero</p><div class="tip-box"><strong>💡 Tip:</strong> เวลาทำงานกับ NULL ให้ระวังเสมอ การเปรียบเทียบใดๆ กับ NULL จะได้ผล Unknown (ไม่ใช่ True หรือ False) ซึ่งทำให้ WHERE Condition ไม่ทำงานตามที่คาดหวัง เป็นสาเหตุของ Bug ที่พบบ่อยมากใน SQL</div><h3>Regular Expressions</h3><p>ฐานข้อมูลหลายตัวรองรับ Regular Expression สำหรับ Pattern Matching ที่ซับซ้อน เช่น PostgreSQL ใช้ ~ สำหรับ Regex Match เช่น WHERE email ~ "^[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$" ใช้ตรวจสอบรูปแบบ Email แม้จะ Advanced กว่า LIKE แต่มีประสิทธิภาพสูงในงาน Data Cleaning</p>'
  },
  {
    number: 4,
    slug: 'joins',
    emoji: '🔗',
    title: 'JOINs — รวมตารางเพื่อวิเคราะห์ข้อมูล',
    content: '<h2>JOIN คืออะไร และทำไมต้องใช้</h2><p>ในชีวิตจริง ข้อมูลไม่ได้อยู่ในตารางเดียว ข้อมูลลูกค้าอยู่ตาราง customers ข้อมูลออเดอร์อยู่ตาราง orders ข้อมูลสินค้าอยู่ตาราง products การ JOIN คือการนำข้อมูลจากหลายตารางมารวมกันเพื่อวิเคราะห์ได้อย่างครบถ้วน เป็นทักษะที่สำคัญมากสำหรับ DA ที่ต้องทำงานกับ Relational Database</p><h3>INNER JOIN</h3><p>INNER JOIN คือ JOIN ที่ได้แถวที่มีคู่ตรงกันในทั้งสองตาราง เช่น SELECT o.order_id, c.customer_name, o.total_amount FROM orders o INNER JOIN customers c ON o.customer_id = c.customer_id จะได้เฉพาะ Order ที่มีข้อมูลลูกค้าครบถ้วน ถ้า Order ไหนไม่มีข้อมูลลูกค้าหรือลูกค้าไม่มี Order จะไม่ถูกรวมในผลลัพธ์</p><h3>LEFT JOIN (LEFT OUTER JOIN)</h3><p>LEFT JOIN ดึงทุกแถวจากตารางซ้าย (Left Table) และแถวที่ตรงกันจากตารางขวา ถ้าไม่มีคู่ในตารางขวา จะได้ NULL แทน ตัวอย่าง SELECT c.customer_name, o.order_id FROM customers c LEFT JOIN orders o ON c.customer_id = o.customer_id จะได้ลูกค้าทุกคน แม้ว่าลูกค้าบางคนจะไม่เคยสั่งซื้อ (order_id จะเป็น NULL) มีประโยชน์มากในการหาลูกค้าที่ยังไม่เคยซื้อสินค้า</p><h3>RIGHT JOIN</h3><p>RIGHT JOIN คือตรงข้ามกับ LEFT JOIN ดึงทุกแถวจากตารางขวาและแถวที่ตรงกันจากตารางซ้าย ในทางปฏิบัติ DA มักใช้ LEFT JOIN แทน RIGHT JOIN เพราะอ่านง่ายกว่า โดยการสลับลำดับตาราง</p><h3>FULL OUTER JOIN</h3><p>FULL OUTER JOIN ดึงทุกแถวจากทั้งสองตาราง ไม่ว่าจะมีคู่หรือไม่ก็ตาม ส่วนที่ไม่มีคู่จะได้ NULL ใช้เมื่อต้องการเห็น "ทุกอย่าง" จากทั้งสองตาราง เช่น เปรียบเทียบยอดขายเดือนนี้กับเดือนที่แล้ว แม้บางสินค้าจะมียอดขายเฉพาะเดือนใดเดือนหนึ่ง</p><h3>CROSS JOIN</h3><p>CROSS JOIN สร้าง Cartesian Product คือนำทุกแถวจากตารางแรกคูณกับทุกแถวจากตารางที่สอง ถ้าตารางแรกมี 100 แถว และตารางสองมี 50 แถว จะได้ 5,000 แถว ใช้เมื่อต้องการสร้างข้อมูลทุก Combination เช่น สร้างตาราง Date Dimension กับ Product Dimension</p><h3>Self JOIN</h3><p>Self JOIN คือการ JOIN ตารางกับตัวเอง ใช้เมื่อข้อมูลมีความสัมพันธ์แบบ Hierarchical เช่น ตาราง employees ที่มีคอลัมน์ manager_id ที่อ้างอิงกลับมาที่ employee_id ของตารางเดียวกัน SELECT e.name AS employee, m.name AS manager FROM employees e LEFT JOIN employees m ON e.manager_id = m.employee_id</p><div class="tip-box"><strong>💡 Tip สำคัญ:</strong> เมื่อ JOIN หลายตาราง ให้ตั้งชื่อย่อ (Alias) ให้ตาราง เช่น FROM orders o และอ้างอิงคอลัมน์ด้วย o.column_name เสมอ เพื่อป้องกัน Ambiguous Column Error และทำให้ Query อ่านง่ายขึ้น</div><h3>การ JOIN หลายตาราง</h3><p>ในงานจริง บ่อยครั้งต้อง JOIN มากกว่า 2 ตาราง เช่น ต้องการรายงานที่มีชื่อลูกค้า ชื่อสินค้า และชื่อหมวดหมู่ในแถวเดียว SELECT c.name, p.product_name, cat.category_name, oi.quantity, oi.price FROM order_items oi JOIN orders o ON oi.order_id = o.order_id JOIN customers c ON o.customer_id = c.customer_id JOIN products p ON oi.product_id = p.product_id JOIN categories cat ON p.category_id = cat.category_id การ JOIN หลายตารางต้องวางแผนให้ดีเพื่อหลีกเลี่ยงผลลัพธ์ที่ผิดพลาด</p>'
  },
  {
    number: 5,
    slug: 'aggregations',
    emoji: '📊',
    title: 'GROUP BY, HAVING, COUNT, SUM, AVG',
    content: '<h2>Aggregation: สรุปข้อมูลด้วย SQL</h2><p>หนึ่งในงานหลักของ DA คือการสรุปข้อมูลจำนวนมากให้เป็น Insight ที่เข้าใจง่าย SQL มี Aggregate Functions ที่ทรงพลังช่วยให้คุณคำนวณ ยอดรวม ค่าเฉลี่ย จำนวนนับ และอื่นๆ ได้อย่างรวดเร็ว</p><h3>Aggregate Functions พื้นฐาน</h3><p>COUNT(*) นับจำนวนแถวทั้งหมด, COUNT(column) นับแถวที่ไม่ใช่ NULL, SUM(column) หาผลรวม, AVG(column) หาค่าเฉลี่ย, MIN(column) หาค่าน้อยสุด, MAX(column) หาค่ามากสุด ตัวอย่าง SELECT COUNT(*) AS total_orders, SUM(amount) AS total_revenue, AVG(amount) AS avg_order_value FROM orders WHERE status = "completed"</p><h3>GROUP BY: จัดกลุ่มข้อมูล</h3><p>GROUP BY ใช้แบ่งข้อมูลออกเป็นกลุ่มแล้วคำนวณ Aggregate แต่ละกลุ่ม ตัวอย่าง SELECT country, COUNT(*) AS customer_count, SUM(lifetime_value) AS total_ltv FROM customers GROUP BY country ORDER BY total_ltv DESC จะได้ยอดรวม LTV แยกตามประเทศ ทุกคอลัมน์ที่อยู่ใน SELECT ที่ไม่ใช่ Aggregate Function ต้องอยู่ใน GROUP BY ด้วย</p><h3>GROUP BY หลายคอลัมน์</h3><p>สามารถ GROUP BY หลายคอลัมน์ได้ เช่น SELECT year, month, category, SUM(revenue) AS monthly_revenue FROM sales GROUP BY year, month, category ORDER BY year, month, category จะได้รายได้รายเดือนแยกตามหมวดหมู่สินค้า เหมาะสำหรับสร้าง Pivot-style Report</p><h3>HAVING: กรองผลหลัง GROUP BY</h3><p>HAVING ทำงานเหมือน WHERE แต่ใช้หลังจาก GROUP BY เพื่อกรองผลลัพธ์ที่ Aggregate แล้ว เช่น SELECT customer_id, COUNT(*) AS order_count, SUM(amount) AS total_spent FROM orders GROUP BY customer_id HAVING COUNT(*) >= 5 AND SUM(amount) >= 10000 จะได้ลูกค้าที่สั่งอย่างน้อย 5 ครั้งและใช้เงินรวมตั้งแต่ 10,000 บาทขึ้นไป</p><h3>WHERE vs HAVING</h3><p>ความแตกต่างสำคัญ: WHERE กรองแถวก่อน GROUP BY ส่วน HAVING กรองกลุ่มหลัง GROUP BY ตัวอย่าง SELECT category, AVG(price) AS avg_price FROM products WHERE is_active = true GROUP BY category HAVING AVG(price) > 500 คือกรองเฉพาะสินค้า Active ก่อน แล้วจัดกลุ่มตาม category และแสดงเฉพาะ category ที่มีราคาเฉลี่ยมากกว่า 500</p><h3>COUNT DISTINCT</h3><p>COUNT(DISTINCT column) ใช้นับจำนวนค่าที่ไม่ซ้ำกัน ใช้บ่อยมากในงาน DA เช่น SELECT DATE_TRUNC("month", order_date) AS month, COUNT(DISTINCT customer_id) AS unique_buyers, COUNT(*) AS total_orders FROM orders GROUP BY 1 ORDER BY 1 จะได้จำนวนลูกค้า Unique และจำนวน Order แยกตามเดือน</p><div class="tip-box"><strong>💡 Business Case:</strong> ตัวชี้วัดสำคัญที่ใช้ Aggregate มากคือ ARPU (Average Revenue Per User) = SUM(revenue) / COUNT(DISTINCT user_id), Conversion Rate = COUNT(DISTINCT buyer_id) / COUNT(DISTINCT visitor_id), และ Repeat Purchase Rate = จำนวนลูกค้าที่ซื้อมากกว่า 1 ครั้ง / จำนวนลูกค้าทั้งหมด</div><h3>ROLLUP และ CUBE</h3><p>ROLLUP สร้าง Subtotal อัตโนมัติ เช่น GROUP BY ROLLUP(year, month) จะได้ผลรวมรายเดือน รายปี และรวมทั้งหมดในครั้งเดียว CUBE ทำ Subtotal ทุก Combination เหมาะสำหรับรายงานแบบ Multi-dimensional ช่วยลดการเขียน UNION ALL หลายชั้น</p>'
  },
  {
    number: 6,
    slug: 'subqueries',
    emoji: '🔄',
    title: 'Subqueries และ Correlated Subqueries',
    content: '<h2>Subqueries: Query ซ้อน Query</h2><p>Subquery คือ SQL Query ที่ซ้อนอยู่ภายใน Query อื่น เปรียบเหมือนฟังก์ชันซ้อนฟังก์ชันในการเขียนโปรแกรม Subqueries ทำให้สามารถแก้ปัญหาที่ซับซ้อนได้ในคำสั่งเดียว แม้บางครั้ง CTEs จะอ่านง่ายกว่า แต่ Subqueries ก็ยังมีที่ใช้งานในสถานการณ์เฉพาะมากมาย</p><h3>Subquery ใน WHERE Clause</h3><p>รูปแบบที่พบบ่อยที่สุด เช่น SELECT customer_name FROM customers WHERE customer_id IN (SELECT customer_id FROM orders WHERE total_amount > 10000) จะได้ชื่อลูกค้าทุกคนที่เคยสั่งซื้อมากกว่า 10,000 บาทในออเดอร์เดียว Subquery จะ Run ก่อน แล้วส่งผลลัพธ์เป็น List ให้ Query หลักใช้</p><h3>Subquery ใน FROM Clause (Derived Table)</h3><p>ใส่ Subquery ใน FROM เพื่อสร้าง Virtual Table ชั่วคราว เช่น SELECT avg_data.category, avg_data.avg_price FROM (SELECT category, AVG(price) AS avg_price FROM products GROUP BY category) AS avg_data WHERE avg_data.avg_price > 500 เทคนิคนี้ช่วยให้ทำ Calculation หลายชั้นได้โดยไม่ต้องสร้าง View จริงๆ</p><h3>Subquery ใน SELECT Clause (Scalar Subquery)</h3><p>Scalar Subquery อยู่ใน SELECT และต้องคืนค่าเดียว เช่น SELECT product_name, price, (SELECT AVG(price) FROM products) AS avg_price, price - (SELECT AVG(price) FROM products) AS diff_from_avg FROM products จะได้ราคาสินค้าแต่ละชิ้นพร้อมเปรียบเทียบกับราคาเฉลี่ยทั้งหมด</p><h3>Correlated Subquery: ขึ้นกับ Query หลัก</h3><p>Correlated Subquery คือ Subquery ที่อ้างอิงคอลัมน์จาก Query หลัก ทำให้ต้อง Run ใหม่สำหรับทุกแถวของ Query หลัก ตัวอย่าง SELECT customer_id, customer_name, (SELECT COUNT(*) FROM orders o WHERE o.customer_id = c.customer_id) AS total_orders FROM customers c แต่ละแถวใน customers จะ Trigger Subquery ให้นับ Order ของลูกค้าคนนั้น</p><h3>EXISTS และ NOT EXISTS</h3><p>EXISTS ใช้ตรวจสอบว่า Subquery คืนผลลัพธ์หรือไม่ โดยไม่สนใจว่าค่าคืออะไร เร็วกว่า IN ในหลายกรณี SELECT customer_name FROM customers c WHERE EXISTS (SELECT 1 FROM orders o WHERE o.customer_id = c.customer_id AND o.status = "completed") จะได้ลูกค้าที่มี Completed Order อย่างน้อยหนึ่งออเดอร์</p><div class="tip-box"><strong>💡 Performance Tip:</strong> Correlated Subquery มักช้ากว่า Regular Subquery มาก เพราะต้อง Run ซ้ำสำหรับทุกแถว ในกรณีที่เป็นไปได้ ให้เปลี่ยนมาใช้ JOIN หรือ Window Function แทน ซึ่งมักเร็วกว่ามาก</div><h3>Subquery vs JOIN vs CTE</h3><p>โดยทั่วไป JOIN มักเร็วที่สุดสำหรับการรวมข้อมูล CTE อ่านง่ายที่สุดสำหรับ Logic ซับซ้อน และ Subquery เหมาะสำหรับการกรองหรือ Scalar Calculation เมื่อเขียน Query ซับซ้อน ให้ลองเขียนด้วย Subquery ก่อนเพื่อทำความเข้าใจ Logic แล้วค่อย Refactor เป็น CTE หรือ JOIN เพื่อ Performance</p>'
  },
  {
    number: 7,
    slug: 'window-functions',
    emoji: '🪟',
    title: 'Window Functions — ROW_NUMBER, RANK, LAG, LEAD',
    content: '<h2>Window Functions: SQL ขั้นสูงที่ DA ต้องรู้</h2><p>Window Functions เป็นฟีเจอร์ที่ทรงพลังที่สุดอย่างหนึ่งใน SQL ช่วยให้ทำการคำนวณที่ซับซ้อนได้โดยไม่ต้องลด (Collapse) ข้อมูล ต่างจาก GROUP BY ที่รวมหลายแถวเป็นแถวเดียว Window Functions คำนวณและยังคงให้ทุกแถวปรากฏในผลลัพธ์</p><h3>Syntax พื้นฐานของ Window Functions</h3><p>รูปแบบคือ function_name() OVER (PARTITION BY column ORDER BY column ROWS/RANGE BETWEEN ...) โดย PARTITION BY แบ่งข้อมูลออกเป็น "หน้าต่าง" แยกกัน ORDER BY เรียงลำดับภายในหน้าต่าง และ ROWS/RANGE กำหนดขอบเขตของหน้าต่าง</p><h3>ROW_NUMBER, RANK, DENSE_RANK</h3><p>ROW_NUMBER() ให้เลขลำดับที่ไม่ซ้ำกัน เช่น SELECT *, ROW_NUMBER() OVER (PARTITION BY category ORDER BY revenue DESC) AS rank_in_category FROM products RANK() ให้ลำดับที่อาจข้ามเลขถ้ามีค่าเท่ากัน เช่น 1,2,2,4 ส่วน DENSE_RANK() ไม่ข้ามเลข เช่น 1,2,2,3 ใช้เลือก Top N ต่อกลุ่มได้อย่างง่ายดาย</p><h3>LAG และ LEAD: เปรียบเทียบกับแถวก่อนหน้า/ถัดไป</h3><p>LAG(column, n) ดึงค่าจากแถวก่อนหน้า n แถว ส่วน LEAD(column, n) ดึงค่าจากแถวถัดไป ตัวอย่าง SELECT month, revenue, LAG(revenue, 1) OVER (ORDER BY month) AS prev_month_revenue, revenue - LAG(revenue, 1) OVER (ORDER BY month) AS mom_change FROM monthly_sales จะได้ยอดขายรายเดือนพร้อม Month-over-Month Change</p><h3>SUM OVER: Running Total</h3><p>SUM() OVER() ใช้คำนวณ Running Total หรือ Cumulative Sum SELECT order_date, daily_revenue, SUM(daily_revenue) OVER (ORDER BY order_date ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS cumulative_revenue FROM daily_sales จะได้ยอดสะสมตลอดช่วงเวลา มีประโยชน์มากในการสร้างกราฟ Cumulative</p><h3>Moving Average</h3><p>การคำนวณ Moving Average ทำได้ง่ายด้วย Window Function SELECT date, revenue, AVG(revenue) OVER (ORDER BY date ROWS BETWEEN 6 PRECEDING AND CURRENT ROW) AS moving_avg_7day FROM daily_sales จะได้ Moving Average 7 วัน ช่วยลด Noise และเห็น Trend ชัดขึ้น</p><h3>PERCENT_RANK และ NTILE</h3><p>PERCENT_RANK() คำนวณเปอร์เซนต์ Rank ของแต่ละแถว (0 ถึง 1) NTILE(n) แบ่งข้อมูลออกเป็น n กลุ่มเท่าๆ กัน เช่น NTILE(4) OVER (ORDER BY revenue) จะได้ Quartile 1-4 เหมาะสำหรับวิเคราะห์การกระจายของลูกค้า หรือแบ่ง Segment</p><div class="tip-box"><strong>💡 Use Case จริง:</strong> Window Functions ใช้มากในการคำนวณ MoM/YoY Growth, Running Totals, Top N per Group, Customer Cohort Analysis, และการหา First/Last Purchase ของลูกค้า เป็น Skill ที่ทำให้ DA มีคุณค่ามากขึ้นในทีม</div><h3>FIRST_VALUE และ LAST_VALUE</h3><p>FIRST_VALUE(column) OVER (...) ดึงค่าแรกของหน้าต่าง LAST_VALUE(column) OVER (...) ดึงค่าสุดท้าย ใช้หาราคาแรกและราคาล่าสุดที่ลูกค้าซื้อ หรือสถานะแรกและสุดท้ายของ Order เป็นเครื่องมือที่ทรงพลังมากในการวิเคราะห์ Customer Journey</p>'
  },
  {
    number: 8,
    slug: 'ctes',
    emoji: '🗂️',
    title: 'CTEs และ Recursive CTEs',
    content: '<h2>CTEs: เขียน SQL ให้อ่านง่ายขึ้น</h2><p>CTE (Common Table Expression) หรือที่เรียกว่า WITH Clause เป็นวิธีที่ช่วยให้ SQL อ่านง่ายขึ้นมาก โดยแบ่ง Query ซับซ้อนออกเป็นส่วนๆ ที่มีชื่อและเข้าใจได้ง่าย แทนที่จะซ้อน Subquery หลายชั้น</p><h3>Syntax ของ CTE พื้นฐาน</h3><p>รูปแบบคือ WITH cte_name AS (SELECT ...) SELECT * FROM cte_name ตัวอย่าง WITH high_value_customers AS (SELECT customer_id, SUM(amount) AS lifetime_value FROM orders GROUP BY customer_id HAVING SUM(amount) > 50000), customer_info AS (SELECT c.customer_id, c.name, h.lifetime_value FROM customers c JOIN high_value_customers h ON c.customer_id = h.customer_id) SELECT * FROM customer_info ORDER BY lifetime_value DESC</p><h3>ข้อดีของ CTE</h3><p>CTE ทำให้ Query อ่านง่ายขึ้น เหมือนการอธิบายแต่ละขั้นตอนในภาษาธรรมชาติ สามารถอ้างอิง CTE เดิมซ้ำหลายครั้งใน Query เดียวกัน ช่วย Debug ง่ายเพราะสามารถ SELECT จาก CTE แต่ละอันเพื่อดูผลกลางได้ และทำให้ทีมเข้าใจ Logic ได้ง่ายขึ้น</p><h3>Multiple CTEs</h3><p>สามารถสร้าง CTE หลายอันในคำสั่งเดียว โดย CTE ถัดไปสามารถอ้างอิง CTE ก่อนหน้าได้ WITH daily_stats AS (SELECT DATE(order_date) AS day, COUNT(*) AS orders, SUM(amount) AS revenue FROM orders GROUP BY 1), weekly_avg AS (SELECT AVG(orders) AS avg_daily_orders, AVG(revenue) AS avg_daily_revenue FROM daily_stats) SELECT d.*, w.avg_daily_orders, d.orders / w.avg_daily_orders AS orders_vs_avg FROM daily_stats d CROSS JOIN weekly_avg w</p><h3>Recursive CTEs: ข้อมูลแบบ Hierarchical</h3><p>Recursive CTE ใช้สำหรับข้อมูลที่มีโครงสร้างแบบ Tree หรือ Hierarchy เช่น โครงสร้างองค์กร (พนักงาน-ผู้จัดการ) หรือ Category ซ้อน Category รูปแบบคือ WITH RECURSIVE org_chart AS (SELECT id, name, manager_id, 1 AS level FROM employees WHERE manager_id IS NULL UNION ALL SELECT e.id, e.name, e.manager_id, oc.level + 1 FROM employees e JOIN org_chart oc ON e.manager_id = oc.id) SELECT * FROM org_chart ORDER BY level, name</p><h3>CTE vs Subquery vs View</h3><p>CTE เหมาะสำหรับ Query ครั้งเดียวที่ต้องการโครงสร้างชัดเจน Subquery เหมาะสำหรับ Filter หรือ Scalar เล็กๆ View เหมาะสำหรับ Query ที่ใช้ซ้ำบ่อยหลาย Query ข้อสังเกตสำคัญคือ CTE ส่วนใหญ่ไม่ถูก Materialize (ไม่ถูกเก็บใน Memory ก่อน) แต่ขึ้นอยู่กับ Query Optimizer ของแต่ละ Database</p><div class="tip-box"><strong>💡 Best Practice:</strong> ตั้งชื่อ CTE ให้สื่อความหมาย เช่น active_users, monthly_revenue, top_products แทนที่จะตั้งชื่อ cte1, temp, t1 การตั้งชื่อที่ดีทำให้คนอ่าน Code เข้าใจได้ทันทีว่า CTE นั้นทำอะไร</div><h3>Materialized CTEs ใน PostgreSQL</h3><p>PostgreSQL 12+ รองรับ WITH ... AS MATERIALIZED ซึ่งบังคับให้ CTE ถูก Execute และเก็บผลลัพธ์ก่อน ใช้เมื่อ CTE ถูกอ้างอิงหลายครั้งและมี Cost สูงในการ Execute ซ้ำ ส่วน WITH ... AS NOT MATERIALIZED บอกให้ Optimizer อิสระในการ Inline CTE</p>'
  },
  {
    number: 9,
    slug: 'date-functions',
    emoji: '📅',
    title: 'Date Functions — DATE_TRUNC, DATEDIFF, DATE_ADD',
    content: '<h2>Date Functions: วิเคราะห์ข้อมูลตามเวลา</h2><p>การวิเคราะห์ข้อมูลตาม Time Series เป็นงานที่ DA ทำบ่อยมาก ไม่ว่าจะเป็นยอดขายรายวัน รายสัปดาห์ รายเดือน หรือการเปรียบเทียบช่วงเวลา การเข้าใจ Date Functions เป็นสิ่งจำเป็นสำหรับงานวิเคราะห์ทุกประเภท</p><h3>DATE_TRUNC: ตัดวันที่ตามระดับ</h3><p>DATE_TRUNC("unit", timestamp) ตัดวันที่ให้เหลือเฉพาะส่วนที่ต้องการ เช่น DATE_TRUNC("month", order_date) จะได้วันที่ 1 ของเดือนนั้น ทำให้สามารถ GROUP BY เดือนได้ง่าย หน่วยที่ใช้ได้ ได้แก่ year, quarter, month, week, day, hour, minute ตัวอย่าง SELECT DATE_TRUNC("month", order_date) AS month, SUM(revenue) FROM sales GROUP BY 1 ORDER BY 1</p><h3>EXTRACT: ดึงส่วนของวันที่</h3><p>EXTRACT(part FROM date) ดึงเฉพาะส่วนที่ต้องการออกมาเป็นตัวเลข เช่น EXTRACT(YEAR FROM order_date) ได้ปี EXTRACT(MONTH FROM order_date) ได้เดือน EXTRACT(DOW FROM order_date) ได้วันในสัปดาห์ (0=อาทิตย์) เหมาะสำหรับวิเคราะห์ Seasonality หรือ Day-of-Week Pattern</p><h3>DATEDIFF: หาความต่างของวันที่</h3><p>ใน MySQL ใช้ DATEDIFF(end_date, start_date) ได้จำนวนวันระหว่างสองวัน ใน PostgreSQL ใช้ end_date - start_date ตัวอย่าง SELECT customer_id, MIN(order_date) AS first_purchase, MAX(order_date) AS last_purchase, MAX(order_date) - MIN(order_date) AS days_as_customer FROM orders GROUP BY customer_id จะได้ระยะเวลาที่ลูกค้าเป็น Active Customer</p><h3>DATE_ADD และ INTERVAL</h3><p>ใน PostgreSQL ใช้ date + INTERVAL "N days" เช่น order_date + INTERVAL "30 days" เพื่อได้วันที่ 30 วันหลัง ใน MySQL ใช้ DATE_ADD(date, INTERVAL 30 DAY) ใช้ในการคำนวณ Expiration Date, Due Date หรือ Cohort Window เช่น ลูกค้าที่ซื้ออีกครั้งภายใน 90 วัน</p><h3>CURRENT_DATE และ NOW()</h3><p>CURRENT_DATE คือวันที่ปัจจุบัน (ไม่มีเวลา) NOW() หรือ CURRENT_TIMESTAMP คือวันและเวลาปัจจุบัน ใช้ในการ Filter ข้อมูลย้อนหลัง เช่น WHERE order_date >= CURRENT_DATE - INTERVAL "30 days" จะได้ Order ในช่วง 30 วันที่ผ่านมา ทำให้ Query อัพเดทอัตโนมัติทุกวัน</p><div class="tip-box"><strong>💡 Tip สำคัญ:</strong> Timezone เป็นปัญหาใหญ่มากใน Date Analysis โดยเฉพาะบริษัทที่มีลูกค้าหลายประเทศ ให้ระวัง UTC vs Local Time เสมอ ใน PostgreSQL ใช้ AT TIME ZONE "Asia/Bangkok" เพื่อแปลง Timezone และ Timestamp ที่มี Timezone คือ TIMESTAMPTZ</div><h3>TO_CHAR: Format วันที่</h3><p>TO_CHAR(date, format) ใช้แปลงวันที่เป็น String ตาม Format ที่ต้องการ เช่น TO_CHAR(order_date, "YYYY-MM") ได้ปีเดือน หรือ TO_CHAR(order_date, "Day DD Month YYYY") ได้ชื่อวัน วันที่ ชื่อเดือน ปี มีประโยชน์มากในการสร้าง Report ที่อ่านง่าย</p><h3>Business Date Calculations</h3><p>ตัวอย่างสูตรที่ DA ใช้บ่อย เช่น YoY Growth: (revenue_this_year - revenue_last_year) / revenue_last_year * 100 Cohort Month: DATE_TRUNC("month", first_purchase_date) Days Since Last Purchase: CURRENT_DATE - MAX(order_date) Week Number: EXTRACT(WEEK FROM order_date) สูตรเหล่านี้ช่วยสร้าง Time-based Metrics ที่ Business Team ต้องการ</p>'
  },
  {
    number: 10,
    slug: 'business-queries',
    emoji: '💼',
    title: 'Business KPI Queries ที่ใช้งานจริง',
    content: '<h2>Business KPI Queries: SQL สำหรับ Decision Making</h2><p>ความสามารถในการแปลง Business Questions เป็น SQL Queries คือทักษะที่ทำให้ DA มีคุณค่าสูงสุด บทนี้จะพาคุณผ่าน KPI Queries ที่ใช้งานจริงในธุรกิจ E-commerce, SaaS และ Marketplace</p><h3>Revenue Metrics</h3><p>Total Revenue คือ SUM(amount) แต่สิ่งที่ Business ต้องการมากกว่าคือ Gross Revenue vs Net Revenue (หลังหัก Discount และ Refund), MoM Revenue Growth ซึ่งต้องใช้ LAG() หรือ Self JOIN, Revenue by Product Category ที่ต้องใช้ JOIN กับตาราง Categories และ Revenue per Customer Segment ที่ต้องมี Customer Segmentation ก่อน</p><h3>Customer Metrics — RFM Analysis</h3><p>RFM (Recency, Frequency, Monetary) เป็นกรอบการวิเคราะห์ลูกค้าที่ใช้กันอย่างแพร่หลาย SELECT customer_id, CURRENT_DATE - MAX(order_date) AS recency_days, COUNT(*) AS frequency, SUM(amount) AS monetary FROM orders WHERE status = "completed" GROUP BY customer_id จะได้ข้อมูลพื้นฐานสำหรับทำ RFM Scoring และแบ่ง Customer Segment</p><h3>Cohort Analysis</h3><p>Cohort Analysis ติดตามพฤติกรรมของลูกค้าตามช่วงเวลาที่เริ่มใช้งาน WITH first_purchase AS (SELECT customer_id, DATE_TRUNC("month", MIN(order_date)) AS cohort_month FROM orders GROUP BY 1), cohort_data AS (SELECT fp.cohort_month, DATE_TRUNC("month", o.order_date) AS order_month, COUNT(DISTINCT o.customer_id) AS customers FROM orders o JOIN first_purchase fp ON o.customer_id = fp.customer_id GROUP BY 1,2) SELECT cohort_month, order_month, customers FROM cohort_data ORDER BY 1,2</p><h3>Product Performance</h3><p>การวิเคราะห์สินค้าที่สำคัญได้แก่ Best Sellers (TOP 10 by Revenue), Slow-moving Products (สินค้าที่ขายน้อยหรือ Stock ค้าง), Product Affinity (สินค้าที่ซื้อร่วมกันบ่อย), และ Return Rate ต่อสินค้า ตัวอย่าง SELECT product_id, COUNT(DISTINCT order_id) AS orders, SUM(quantity) AS units_sold, SUM(revenue) AS total_revenue, AVG(rating) AS avg_rating FROM order_items GROUP BY 1 ORDER BY total_revenue DESC LIMIT 20</p><h3>Funnel Analysis</h3><p>วิเคราะห์ Conversion Funnel จาก Visitor ไปถึง Purchase SELECT DATE_TRUNC("week", event_date) AS week, COUNT(DISTINCT CASE WHEN event_type = "view" THEN user_id END) AS viewers, COUNT(DISTINCT CASE WHEN event_type = "add_to_cart" THEN user_id END) AS add_to_cart, COUNT(DISTINCT CASE WHEN event_type = "purchase" THEN user_id END) AS purchasers, ROUND(COUNT(DISTINCT CASE WHEN event_type = "purchase" THEN user_id END)::numeric / NULLIF(COUNT(DISTINCT CASE WHEN event_type = "view" THEN user_id END), 0) * 100, 2) AS conversion_rate FROM events GROUP BY 1</p><div class="tip-box"><strong>💡 สิ่งที่ Business ต้องการจริงๆ:</strong> ไม่ใช่แค่ตัวเลข แต่เป็น Insight และ Action Items Business ต้องการรู้ว่า "เพราะอะไร" และ "ควรทำอะไรต่อ" ทักษะการแปล Data เป็น Story คือสิ่งที่ทำให้ DA โดดเด่น</div><h3>Churn Analysis</h3><p>Churn คือลูกค้าที่หยุดใช้งาน WITH last_activity AS (SELECT customer_id, MAX(order_date) AS last_order_date FROM orders GROUP BY 1) SELECT COUNT(*) AS total_customers, COUNT(CASE WHEN last_order_date < CURRENT_DATE - INTERVAL "90 days" THEN 1 END) AS churned, ROUND(COUNT(CASE WHEN last_order_date < CURRENT_DATE - INTERVAL "90 days" THEN 1 END)::numeric / COUNT(*) * 100, 2) AS churn_rate FROM last_activity จะได้ Churn Rate ของลูกค้าที่ไม่ได้ซื้อในช่วง 90 วัน</p>'
  },
  {
    number: 11,
    slug: 'performance',
    emoji: '⚡',
    title: 'SQL Performance — EXPLAIN, Index, Query Optimization',
    content: '<h2>Query Performance: ทำให้ SQL เร็วขึ้น</h2><p>เมื่อ DA ทำงานกับข้อมูลขนาดใหญ่ Query ที่ช้าอาจใช้เวลาหลายนาทีหรือหลายชั่วโมง ความเข้าใจพื้นฐานเรื่อง Performance ช่วยให้ทำงานได้เร็วขึ้นมาก และลดค่าใช้จ่าย Cloud Computing ได้อย่างมีนัยสำคัญ</p><h3>EXPLAIN และ EXPLAIN ANALYZE</h3><p>EXPLAIN แสดง Query Execution Plan ที่ Database วางแผนจะใช้ EXPLAIN ANALYZE แสดง Plan พร้อม Actual Execution Time ตัวอย่าง EXPLAIN ANALYZE SELECT * FROM orders WHERE customer_id = 12345 ผลลัพธ์จะบอกว่าใช้ Index Scan หรือ Sequential Scan, จำนวนแถวที่ต้องอ่าน, และ Cost ประมาณ ให้มองหา "Seq Scan" บนตารางใหญ่ ซึ่งมักเป็นสัญญาณว่าต้องเพิ่ม Index</p><h3>Index คืออะไร</h3><p>Index เหมือนสารบัญในหนังสือ ทำให้ Database หาข้อมูลได้เร็วขึ้นมากโดยไม่ต้องอ่านทุกแถว ประเภท Index ที่ควรรู้ ได้แก่ B-tree Index (ใช้กับ =, <, >, BETWEEN, LIKE prefix), Hash Index (ใช้เฉพาะ = Comparison), Composite Index (Index บนหลายคอลัมน์), และ Partial Index (Index เฉพาะบางแถว) การสร้าง Index ใช้ CREATE INDEX idx_orders_customer ON orders(customer_id)</p><h3>เมื่อไหรควรสร้าง Index</h3><p>ควรสร้าง Index บนคอลัมน์ที่ใช้ใน WHERE Clause บ่อย เช่น customer_id, order_date, status คอลัมน์ที่ใช้ใน JOIN Condition, คอลัมน์ที่ใช้ใน ORDER BY บ่อยๆ แต่ไม่ควรสร้าง Index มากเกินไป เพราะแต่ละ Index ทำให้ INSERT/UPDATE/DELETE ช้าลง ควรหา Balance ที่เหมาะสม</p><h3>Query Optimization Techniques</h3><p>เทคนิคที่ DA ควรรู้ได้แก่ หลีกเลี่ยง SELECT * และระบุเฉพาะคอลัมน์ที่ต้องการ หลีกเลี่ยง Functions บน Indexed Column ใน WHERE เช่น WHERE YEAR(order_date) = 2024 ควรเขียนเป็น WHERE order_date BETWEEN "2024-01-01" AND "2024-12-31" แทน ใช้ LIMIT เสมอเวลา Test หลีกเลี่ยง DISTINCT และ ORDER BY ที่ไม่จำเป็น</p><h3>Partitioning: แบ่งตารางขนาดใหญ่</h3><p>สำหรับตารางที่มีข้อมูลหลายร้อยล้านแถว Partitioning ช่วยให้ Query เฉพาะ Partition ที่ต้องการแทนที่จะสแกนทั้งตาราง เช่น Partition ตาราง orders ตาม year-month เมื่อ Query WHERE order_date >= "2024-01-01" Database จะอ่านเฉพาะ Partition ปี 2024 เป็นเทคนิคที่ใช้กันแพร่หลายใน Data Warehouse</p><div class="tip-box"><strong>💡 Cloud Cost Tip:</strong> ใน BigQuery และ Snowflake ค่าใช้จ่ายคิดตามปริมาณข้อมูลที่ Query ดู การ Partition ตาราง Cluster ข้อมูล และระบุ Column ที่ต้องการแทน SELECT * สามารถลดค่าใช้จ่ายได้หลายสิบเปอร์เซนต์ DA ที่เข้าใจเรื่องนี้จะเป็นที่ชื่นชมของทีม</div><h3>Materialized Views</h3><p>Materialized View เป็น View ที่เก็บผลลัพธ์จริงๆ ไว้ (ต่างจาก Regular View ที่ Run Query ทุกครั้งที่เรียก) เหมาะสำหรับ Aggregation ที่คำนวณนานและใช้ซ้ำบ่อย เช่น Monthly Revenue Summary ที่ต้องการใช้ใน Dashboard หลายแห่ง ต้องมีการ REFRESH MATERIALIZED VIEW เป็นระยะ</p>'
  },
  {
    number: 12,
    slug: 'real-cases',
    emoji: '🛒',
    title: 'Real Cases — E-commerce และ Marketing Analytics',
    content: '<h2>Real Cases: SQL ในโลกการทำงานจริง</h2><p>บทสุดท้ายนี้จะรวบรวม Use Cases จริงจากธุรกิจ E-commerce และ Marketing ที่ DA มักพบในชีวิตการทำงาน พร้อม SQL ตัวอย่างที่สามารถนำไปปรับใช้ได้เลย</p><h3>E-commerce: Customer Lifetime Value (CLV)</h3><p>CLV คือมูลค่าที่ลูกค้าจะสร้างให้ธุรกิจตลอดช่วงเวลาที่เป็นลูกค้า การคำนวณ CLV อย่างง่าย SELECT customer_id, COUNT(DISTINCT order_id) AS total_orders, SUM(net_revenue) AS historical_clv, SUM(net_revenue) / COUNT(DISTINCT DATE_TRUNC("year", order_date)) AS annual_value, AVG(net_revenue) AS avg_order_value, EXTRACT(DAY FROM MAX(order_date) - MIN(order_date)) / 365.0 AS years_as_customer FROM orders WHERE status NOT IN ("cancelled", "refunded") GROUP BY customer_id</p><h3>E-commerce: Market Basket Analysis</h3><p>หาสินค้าที่ซื้อร่วมกันบ่อย (Product Affinity) SELECT a.product_id AS product_a, b.product_id AS product_b, COUNT(*) AS times_bought_together FROM order_items a JOIN order_items b ON a.order_id = b.order_id AND a.product_id < b.product_id GROUP BY 1, 2 HAVING COUNT(*) >= 10 ORDER BY times_bought_together DESC LIMIT 20 ข้อมูลนี้ใช้สร้าง "Frequently Bought Together" Recommendation</p><h3>Marketing: Campaign Attribution</h3><p>วิเคราะห์ว่า Marketing Channel ไหนทำให้ลูกค้า Convert มากที่สุด SELECT utm_source, utm_medium, COUNT(DISTINCT session_id) AS sessions, COUNT(DISTINCT CASE WHEN converted = true THEN user_id END) AS conversions, ROUND(COUNT(DISTINCT CASE WHEN converted = true THEN user_id END)::numeric / COUNT(DISTINCT session_id) * 100, 2) AS conversion_rate, SUM(CASE WHEN converted = true THEN order_value END) AS revenue FROM sessions_with_attribution GROUP BY 1, 2 ORDER BY revenue DESC</p><h3>Marketing: Email Campaign Performance</h3><p>วิเคราะห์ประสิทธิภาพของ Email Campaign SELECT campaign_name, COUNT(*) AS emails_sent, SUM(CASE WHEN opened_at IS NOT NULL THEN 1 ELSE 0 END) AS opens, ROUND(SUM(CASE WHEN opened_at IS NOT NULL THEN 1 ELSE 0 END)::numeric / COUNT(*) * 100, 2) AS open_rate, SUM(CASE WHEN clicked_at IS NOT NULL THEN 1 ELSE 0 END) AS clicks, ROUND(SUM(CASE WHEN clicked_at IS NOT NULL THEN 1 ELSE 0 END)::numeric / NULLIF(SUM(CASE WHEN opened_at IS NOT NULL THEN 1 ELSE 0 END), 0) * 100, 2) AS ctr FROM email_sends GROUP BY 1 ORDER BY open_rate DESC</p><h3>SaaS: MRR (Monthly Recurring Revenue)</h3><p>MRR คือรายได้ที่คาดว่าจะได้รับต่อเดือนจาก Subscription WITH active_subs AS (SELECT customer_id, plan_name, mrr FROM subscriptions WHERE status = "active" AND DATE_TRUNC("month", CURRENT_DATE) BETWEEN start_date AND COALESCE(end_date, "9999-12-31")) SELECT plan_name, COUNT(*) AS subscriber_count, SUM(mrr) AS total_mrr, AVG(mrr) AS avg_mrr FROM active_subs GROUP BY 1 ORDER BY total_mrr DESC</p><div class="tip-box"><strong>💡 สรุปสิ่งที่ DA ต้องจำ:</strong> SQL เป็นทักษะที่ต้องฝึกซ้ำๆ เพื่อให้ชำนาญ แนะนำให้ฝึกบน Dataset จริง เช่น ข้อมูลจาก Kaggle หรือ Google BigQuery Public Datasets ทุกวันสัก 30 นาที ภายใน 3 เดือนคุณจะสามารถตอบคำถาม Business ด้วย SQL ได้อย่างมั่นใจ</div><h3>Inventory Management: Stock Analysis</h3><p>วิเคราะห์สต็อกสินค้าเพื่อ Reorder Planning SELECT p.product_name, p.sku, i.current_stock, AVG(oi.quantity) AS avg_daily_sales, i.current_stock / NULLIF(AVG(oi.quantity), 0) AS days_of_stock_remaining, CASE WHEN i.current_stock / NULLIF(AVG(oi.quantity), 0) < 14 THEN "ต้องสั่งซื้อด่วน" WHEN i.current_stock / NULLIF(AVG(oi.quantity), 0) < 30 THEN "ใกล้หมด" ELSE "ปกติ" END AS stock_status FROM inventory i JOIN products p ON i.product_id = p.product_id LEFT JOIN order_items oi ON p.product_id = oi.product_id AND oi.created_at >= CURRENT_DATE - INTERVAL "30 days" GROUP BY 1,2,3 ORDER BY days_of_stock_remaining ASC</p>'
  }
];
