export const chapters = [
  {
    number: 1,
    slug: 'overview',
    emoji: '🐍',
    title: 'Python สำหรับ Data Analyst — ทำไมต้องเรียน',
    content: '<h2>Python สำหรับ Data Analyst</h2><p>Python กลายเป็นภาษาโปรแกรมมิ่งที่ Data Analyst ทั่วโลกใช้กันมากที่สุด เหตุผลหลักคือ Python มี Ecosystem ของ Library สำหรับ Data Analysis ที่สมบูรณ์แบบที่สุด ไม่ว่าจะเป็น pandas สำหรับจัดการข้อมูล matplotlib และ seaborn สำหรับสร้างกราฟ numpy สำหรับคำนวณทางคณิตศาสตร์ และ scikit-learn สำหรับ Machine Learning</p><h3>ทำไม Python ดีกว่า Excel สำหรับ DA</h3><p>Excel เป็นเครื่องมือที่ดีสำหรับข้อมูลขนาดเล็ก แต่มีข้อจำกัดมาก เมื่อข้อมูลมีมากกว่าล้านแถว Excel จะช้ามากหรือทำงานไม่ได้ ส่วน Python ไม่มีข้อจำกัดนี้ Python ยังสามารถ Automate งานซ้ำๆ ได้ เช่น ส่ง Report อัตโนมัติทุกวันจันทร์ หรือ Pull ข้อมูลจาก API แล้วอัพเดท Dashboard โดยไม่ต้องทำด้วยมือ นอกจากนี้ Python ยังสามารถทำ Advanced Analytics เช่น Statistical Testing, ML Models และ NLP ได้โดยใช้ Library เดียวกัน</p><h3>Python DA vs Python DE vs Python DS</h3><p>Data Analyst ใช้ Python เพื่อ Data Wrangling, EDA, Visualization และ Business Reporting ส่วน Data Engineer ใช้ Python เพื่อสร้าง Pipeline, Orchestration และ Infrastructure ส่วน Data Scientist ใช้ Python เพื่อสร้าง ML Models, A/B Testing และ Predictive Analytics DA ไม่จำเป็นต้องรู้ Machine Learning ลึก แต่ต้องรู้ pandas, matplotlib และ Statistics พื้นฐานเป็นอย่างดี</p><h3>Library ที่ DA ต้องรู้</h3><p>pandas เป็น Core Library ที่ DA ใช้บ่อยที่สุด ใช้อ่าน จัดการ และวิเคราะห์ข้อมูล numpy ใช้สำหรับ Numerical Computation matplotlib และ seaborn ใช้สร้าง Visualization plotly ใช้สร้าง Interactive Charts openpyxl และ xlsxwriter ใช้สร้างไฟล์ Excel requests ใช้เรียก API และ reportlab ใช้สร้าง PDF Report</p><h3>Career Advantage ของการรู้ Python</h3><p>DA ที่รู้ Python จะได้เงินเดือนสูงกว่า DA ที่รู้แค่ Excel และ SQL ประมาณ 20-40% ตามข้อมูลตลาดงาน 2024-2025 นอกจากนี้ยังมีโอกาสเติบโตไปเป็น Senior DA, Data Scientist หรือ Analytics Engineer ได้ง่ายขึ้น ทักษะ Python เป็น Differentiator สำคัญในตลาดงานที่แข่งขันสูง</p><div class="tip-box"><strong>💡 เริ่มต้นอย่างไร:</strong> ไม่จำเป็นต้องเรียน Python ทั้งหมดก่อนที่จะใช้ได้ เรียนพอใช้งาน pandas ได้ก็พอสำหรับงาน DA เบื้องต้น แล้วค่อยเรียนรู้เพิ่มเมื่อมีความต้องการจากงานจริง Learning by Doing คือวิธีที่ดีที่สุดสำหรับ Python</div><h3>สิ่งที่จะได้เรียนในคอร์สนี้</h3><p>คอร์สนี้จะพาคุณตั้งแต่ Setup Environment ไปจนถึง Capstone Project จริงๆ โดยเน้น Hands-on ทุกบท ไม่ใช่แค่ทฤษฎี คุณจะได้ทำ EDA จริง สร้าง Dashboard จริง Auto Report จริง และสุดท้ายทำ Sales Analysis Project ที่ใส่ใน Portfolio ได้เลย</p>'
  },
  {
    number: 2,
    slug: 'setup-jupyter',
    emoji: '⚙️',
    title: 'Setup Python และ Jupyter Notebook',
    content: '<h2>ติดตั้ง Python และ Jupyter Notebook</h2><p>ก่อนเริ่มเขียน Code ต้องติดตั้ง Environment ที่เหมาะสมก่อน สำหรับ DA มีตัวเลือกหลักอยู่สองทาง คือ Anaconda Distribution ที่รวมทุกอย่างมาให้พร้อม หรือ Python บวก Jupyter Notebook ติดตั้งเอง แนะนำสำหรับมือใหม่คือ Anaconda เพราะง่ายที่สุด</p><h3>ติดตั้ง Anaconda</h3><p>ดาวน์โหลด Anaconda จาก anaconda.com เลือกเวอร์ชันที่เหมาะกับ OS ของคุณ (Windows, Mac, Linux) ติดตั้งแบบ Default ทุกอย่าง Anaconda จะติดตั้ง Python, Jupyter Notebook, Spyder และ Library พื้นฐานหลายร้อยตัวให้อัตโนมัติ หลังติดตั้ง สามารถเปิด Anaconda Navigator เพื่อเปิด Jupyter Notebook ได้เลย</p><h3>Virtual Environment — ทำไมต้องใช้</h3><p>Virtual Environment เป็น Isolated Environment สำหรับแต่ละโปรเจค ทำให้ Library ของแต่ละโปรเจคไม่ชนกัน ใน Anaconda ใช้ conda create -n myenv python=3.11 เพื่อสร้าง Environment ใหม่ จากนั้น conda activate myenv เพื่อเปิดใช้ และ conda install pandas matplotlib seaborn เพื่อติดตั้ง Library ที่ต้องการ</p><h3>Jupyter Notebook Interface</h3><p>Jupyter Notebook ทำงานในเว็บบราวเซอร์ แบ่งเป็น Cell สองประเภทหลัก Code Cell สำหรับเขียน Python Code และ Markdown Cell สำหรับเขียนอธิบาย การกด Shift+Enter เพื่อ Run Cell การกด Ctrl+Enter เพื่อ Run Cell โดยไม่เลื่อนลง Kernel คือ Python Process ที่รัน Code ของคุณ ถ้า Kernel ค้างให้ Restart จาก Menu</p><h3>Jupyter Lab — อัพเกรดจาก Notebook</h3><p>Jupyter Lab คือ Interface ที่ Modern กว่า Jupyter Notebook มาก มี File Browser ด้านซ้าย สามารถเปิดหลาย Notebook พร้อมกัน รองรับ Terminal, Text Editor และ CSV Viewer ในหน้าเดียวกัน แนะนำให้ใช้ Jupyter Lab สำหรับงาน DA เพราะ Workflow สะดวกกว่ามาก</p><h3>VS Code กับ Jupyter</h3><p>VS Code ร่วมกับ Extension Jupyter ให้ประสบการณ์ที่ดีมากสำหรับ DA ที่ต้องการ Code Editor ที่ทรงพลัง มี Autocomplete, Linting, Git Integration และ Debugger ในตัว Install Extension Python และ Jupyter จาก VS Code Extension Marketplace แล้วสามารถเปิด .ipynb ไฟล์ได้เลย</p><div class="tip-box"><strong>💡 Google Colab — ทางเลือกฟรี:</strong> ถ้าไม่อยากติดตั้งอะไรบนเครื่อง ให้ลอง Google Colab (colab.research.google.com) ใช้ฟรี ไม่ต้องติดตั้ง มี GPU ฟรีสำหรับ Machine Learning และ Sync กับ Google Drive ได้ เหมาะมากสำหรับเริ่มต้นเรียนรู้</div><h3>Library ที่ต้องติดตั้งสำหรับคอร์สนี้</h3><p>รัน pip install pandas numpy matplotlib seaborn plotly openpyxl requests scipy statsmodels เพื่อติดตั้ง Library ทั้งหมดที่จะใช้ในคอร์สนี้ ถ้าใช้ conda ให้เปลี่ยน pip เป็น conda ตรวจสอบการติดตั้งด้วยการ import pandas as pd ใน Jupyter แล้วพิมพ์ pd.__version__ ถ้าเห็นเลขเวอร์ชันแสดงว่าติดตั้งสำเร็จ</p>'
  },
  {
    number: 3,
    slug: 'pandas-basics',
    emoji: '🐼',
    title: 'pandas DataFrame — อ่านและสรุปข้อมูล',
    content: '<h2>pandas: Library หลักของ DA</h2><p>pandas เป็น Library ที่สำคัญที่สุดสำหรับ Data Analyst ใน Python ชื่อมาจาก "Panel Data" ซึ่งเป็นคำในเศรษฐศาสตร์ pandas มี Structure หลักสองอย่าง คือ DataFrame (ตาราง 2 มิติ เหมือน Spreadsheet) และ Series (คอลัมน์เดียว)</p><h3>อ่านข้อมูลเข้า pandas</h3><p>pandas รองรับการอ่านข้อมูลหลายรูปแบบ อ่าน CSV ด้วย pd.read_csv("file.csv") อ่าน Excel ด้วย pd.read_excel("file.xlsx", sheet_name="Sheet1") อ่าน JSON ด้วย pd.read_json("file.json") อ่านจาก SQL Database ด้วย pd.read_sql("SELECT * FROM table", connection) และอ่านจาก Google Sheets โดยใช้ URL ที่ Export เป็น CSV</p><h3>สำรวจข้อมูลเบื้องต้น</h3><p>หลังอ่านข้อมูลเข้ามาแล้ว ให้เริ่มสำรวจด้วย df.head() แสดง 5 แถวแรก df.tail() แสดง 5 แถวสุดท้าย df.shape แสดงขนาด (rows, columns) df.columns แสดงชื่อคอลัมน์ df.dtypes แสดง Data Type ของแต่ละคอลัมน์ df.info() แสดงข้อมูลทั้งหมดรวมถึง Non-null Count และ df.describe() แสดงสถิติ Summary เช่น Mean, Std, Min, Max</p><h3>การเลือก Column และ Row</h3><p>เลือก Column เดียว df["column_name"] หรือ df.column_name (ถ้าชื่อไม่มีช่องว่าง) เลือกหลาย Column df[["col1", "col2", "col3"]] เลือก Row ด้วย iloc (Index-based): df.iloc[0] เลือก Row แรก df.iloc[0:5] เลือก 5 Row แรก เลือก Row ด้วย loc (Label-based): df.loc[df["status"] == "active"] กรอง Row ที่ status เป็น active</p><h3>การ Filter ข้อมูล</h3><p>กรองข้อมูลด้วย Boolean Mask เช่น df[df["age"] > 25] หรือรวมเงื่อนไข df[(df["age"] > 25) & (df["country"] == "Thailand")] ใช้ & สำหรับ AND และ | สำหรับ OR ต้องใส่วงเล็บรอบแต่ละเงื่อนไข นอกจากนี้ยังใช้ df.query("age > 25 and country == &#39;Thailand&#39;") ได้ซึ่งอ่านง่ายกว่า</p><h3>Groupby และ Aggregation</h3><p>df.groupby("category")["revenue"].sum() หาผลรวม Revenue ตาม Category df.groupby(["year", "month"])["orders"].agg(["sum", "mean", "count"]) ทำ Multiple Aggregation พร้อมกัน df.pivot_table(values="revenue", index="category", columns="month", aggfunc="sum") สร้าง Pivot Table แบบ Excel</p><div class="tip-box"><strong>💡 Tip ที่ช่วยประหยัดเวลา:</strong> ใช้ method chaining ใน pandas เพื่อเขียน Code กระชับ เช่น df.query("status == &#39;active&#39;").groupby("category")["revenue"].sum().sort_values(ascending=False).head(10) ทำ Filter, Group, Sum, Sort, และ Select Top 10 ในบรรทัดเดียว</div><h3>Merge และ Concat</h3><p>รวม DataFrame ด้วย pd.merge(df1, df2, on="customer_id", how="left") ทำงานเหมือน SQL JOIN รวม DataFrame แนวตั้งด้วย pd.concat([df1, df2], ignore_index=True) เหมาะสำหรับรวมหลายไฟล์ที่มี Schema เดียวกัน เช่น ข้อมูลรายเดือนหลายเดือน</p>'
  },
  {
    number: 4,
    slug: 'data-cleaning',
    emoji: '🧹',
    title: 'Data Cleaning — ทำความสะอาดข้อมูลด้วย pandas',
    content: '<h2>Data Cleaning: งานที่ใช้เวลา 70% ของ DA</h2><p>ข้อเท็จจริงที่น่าประหลาดใจคือ DA ใช้เวลาประมาณ 70-80% ของการทำงานทั้งหมดในการทำความสะอาดข้อมูล ไม่ใช่การวิเคราะห์หรือสร้าง Dashboard ข้อมูลในโลกจริงมักสกปรก ไม่ครบถ้วน ไม่สม่ำเสมอ และมีข้อผิดพลาดมากมาย การเข้าใจและแก้ไขปัญหาเหล่านี้คือทักษะที่มีค่ามากสำหรับ DA</p><h3>หา Missing Values</h3><p>df.isnull().sum() แสดงจำนวน Missing Values ต่อคอลัมน์ df.isnull().sum() / len(df) * 100 แสดงเปอร์เซนต์ Missing ใน pandas 2.0+ ยังใช้ df.isna() ได้เหมือนกัน หลังจากรู้ว่าคอลัมน์ไหนมี Missing เท่าไหร่ ต้องตัดสินใจว่าจะจัดการอย่างไร</p><h3>จัดการ Missing Values</h3><p>มีหลายวิธีในการจัดการ Missing Values ได้แก่ dropna() เพื่อลบแถวที่มี Missing fillna(value) เพื่อแทนด้วยค่าคงที่ เช่น 0 หรือ "unknown" fillna(df["col"].mean()) เพื่อแทนด้วยค่าเฉลี่ย fillna(method="ffill") เพื่อ Forward Fill (ใช้ค่าก่อนหน้า) และ Interpolation สำหรับ Time Series การเลือกวิธีขึ้นอยู่กับลักษณะข้อมูลและ Context ของธุรกิจ</p><h3>แก้ไข Data Types</h3><p>บ่อยครั้งที่คอลัมน์ตัวเลขถูกอ่านเป็น String หรือวันที่ถูกอ่านเป็น Object แปลง Data Type ด้วย df["price"] = pd.to_numeric(df["price"], errors="coerce") แปลงเป็นตัวเลข df["date"] = pd.to_datetime(df["date"], format="%Y-%m-%d") แปลงเป็น datetime df["category"] = df["category"].astype("category") ประหยัด Memory สำหรับคอลัมน์ที่มีค่าซ้ำ</p><h3>ทำความสะอาด String</h3><p>pandas มี str accessor สำหรับจัดการ String df["name"].str.strip() ลบช่องว่างหัวท้าย df["email"].str.lower() แปลงเป็นตัวพิมพ์เล็ก df["phone"].str.replace("-", "").str.replace(" ", "") ลบขีดและช่องว่าง df["city"].str.extract(r"(\w+)") ดึงส่วนที่ต้องการด้วย Regex การ Standardize String ช่วยให้ JOIN และ GROUP BY ถูกต้องมากขึ้น</p><h3>หาและจัดการ Duplicates</h3><p>df.duplicated().sum() นับจำนวนแถวซ้ำ df.duplicated(subset=["email"]).sum() นับแถวที่ Email ซ้ำ df.drop_duplicates() ลบแถวซ้ำ df.drop_duplicates(subset=["email"], keep="last") เก็บแถวล่าสุดที่มี Email ซ้ำ ควรระวังว่า "ซ้ำ" หมายความว่าอะไรในบริบทของธุรกิจ บางครั้งแถวที่ดูเหมือนซ้ำอาจเป็นข้อมูลที่ถูกต้อง</p><div class="tip-box"><strong>💡 Data Quality Rule of Thumb:</strong> ก่อน Clean ข้อมูล ให้ Document ไว้เสมอว่า "ข้อมูลต้นฉบับเป็นอย่างไร" และ "ทำอะไรไปบ้าง" เพราะถ้าผลวิเคราะห์ผิด จะได้ย้อนกลับมาดู Cleaning Steps ได้ ใช้ Jupyter Notebook เป็น Living Document บันทึกทุก Assumption ที่ทำ</div><h3>Outlier Detection</h3><p>Outlier คือค่าที่ผิดปกติ หาได้หลายวิธี IQR Method: Q1 - 1.5*IQR ถึง Q3 + 1.5*IQR Z-Score Method: |z| > 3 ถือว่า Outlier df[(df["price"] < df["price"].quantile(0.01)) | (df["price"] > df["price"].quantile(0.99))] หา Extreme Values การจัดการ Outlier ต้องพิจารณาว่าเป็น Error จริงๆ หรือเป็นข้อมูลที่ถูกต้องแต่ผิดปกติ</p>'
  },
  {
    number: 5,
    slug: 'exploratory-analysis',
    emoji: '🔭',
    title: 'EDA — Exploratory Data Analysis อย่างเป็นระบบ',
    content: '<h2>EDA: เข้าใจข้อมูลก่อนวิเคราะห์</h2><p>Exploratory Data Analysis (EDA) คือกระบวนการสำรวจข้อมูลเพื่อทำความเข้าใจโครงสร้าง Pattern และความสัมพันธ์ในข้อมูล ก่อนที่จะตั้งสมมติฐานหรือสร้าง Model ใดๆ EDA ที่ดีช่วยให้ DA ค้นพบ Insight ที่ไม่คาดคิด และหลีกเลี่ยงการสรุปผลที่ผิดพลาด</p><h3>Framework ของ EDA</h3><p>EDA มีขั้นตอนหลักดังนี้ ขั้นแรกคือ Univariate Analysis วิเคราะห์ทีละตัวแปร ขั้นที่สองคือ Bivariate Analysis วิเคราะห์ความสัมพันธ์ระหว่างสองตัวแปร ขั้นที่สามคือ Multivariate Analysis วิเคราะห์ตัวแปรหลายตัวพร้อมกัน และขั้นสุดท้ายคือการสรุป Findings และ Hypothesis เพื่อวิเคราะห์ขั้นต่อไป</p><h3>Univariate Analysis</h3><p>สำหรับ Numerical Variables ใช้ df["price"].describe() ดูสถิติพื้นฐาน df["price"].hist() สร้าง Histogram ดูการกระจาย df["price"].value_counts() ดูค่าที่พบบ่อย สำหรับ Categorical Variables ใช้ df["category"].value_counts() และ df["category"].value_counts(normalize=True) เพื่อดูสัดส่วนเปอร์เซนต์</p><h3>Bivariate Analysis</h3><p>วิเคราะห์ความสัมพันธ์ระหว่างตัวแปรด้วย df[["price", "rating"]].corr() หา Correlation df.groupby("category")["revenue"].mean().plot(kind="bar") เปรียบเทียบ Revenue ต่อ Category pd.crosstab(df["gender"], df["product_type"]) สร้าง Cross-tabulation และ df.pivot_table(values="sales", index="month", columns="category") สร้าง Pivot Table</p><h3>Time Series EDA</h3><p>สำหรับข้อมูล Time Series ต้องวิเคราะห์เพิ่มเติม df["date"] = pd.to_datetime(df["date"]) แปลงเป็น datetime df.set_index("date")["revenue"].resample("M").sum().plot() ดูยอดขายรายเดือน df["month"] = df["date"].dt.month และ df["dayofweek"] = df["date"].dt.dayofweek เพื่อวิเคราะห์ Seasonality และ Weekly Pattern</p><h3>การสรุป EDA Findings</h3><p>หลังทำ EDA ให้สรุปสิ่งที่พบเป็น Bullet Points ได้แก่ ข้อมูลมี Missing ในคอลัมน์ไหนบ้าง Data Quality Issues ที่พบ Distribution ของตัวแปรหลัก ความสัมพันธ์ระหว่างตัวแปร Outliers ที่น่าสนใจ และ Hypothesis ที่อยากทดสอบต่อ การ Document EDA ที่ดีช่วยให้ทีมเข้าใจข้อมูลได้รวดเร็ว</p><div class="tip-box"><strong>💡 EDA Best Practice:</strong> อย่าเพิ่งสรุปผลจาก EDA เพราะ Correlation ไม่ใช่ Causation เสมอไป EDA เป็นจุดเริ่มต้นของการสร้าง Hypothesis ไม่ใช่การพิสูจน์สมมติฐาน ต้องทำ Statistical Testing เพิ่มเติมเพื่อยืนยัน</div><h3>Pandas Profiling — EDA อัตโนมัติ</h3><p>ydata-profiling (เดิมชื่อ pandas-profiling) สร้าง EDA Report อัตโนมัติจาก DataFrame เพียงแค่ from ydata_profiling import ProfileReport และ ProfileReport(df, title="My EDA Report").to_notebook_iframe() จะได้ Report ครบถ้วนรวมถึง Correlation Matrix, Missing Values, Distribution, Duplicates ภายในไม่กี่วินาที</p>'
  },
  {
    number: 6,
    slug: 'visualization-matplotlib',
    emoji: '📈',
    title: 'Data Visualization ด้วย matplotlib',
    content: '<h2>matplotlib: สร้างกราฟด้วย Python</h2><p>matplotlib เป็น Library สร้าง Visualization ที่เก่าแก่และทรงพลังที่สุดใน Python เป็น Foundation ของ Library อื่นๆ อีกมาก เช่น seaborn และ pandas plotting แม้ Interface อาจดูซับซ้อนกว่า Library อื่น แต่ก็ให้การควบคุมที่ละเอียดที่สุด</p><h3>Figure และ Axes</h3><p>matplotlib ทำงานบน Concept ของ Figure (กระดาษ) และ Axes (พื้นที่วาดกราฟ) สร้าง Figure ด้วย fig, ax = plt.subplots(figsize=(10, 6)) แล้ววาดกราฟบน ax เช่น ax.plot(x, y) หรือ ax.bar(x, height) และตั้งค่าด้วย ax.set_title(), ax.set_xlabel(), ax.set_ylabel() และ ax.legend() วิธีนี้เรียกว่า Object-Oriented Interface ซึ่ง Professional กว่า pyplot Interface</p><h3>Line Chart — เส้นแนวโน้ม</h3><p>เหมาะสำหรับ Time Series Data ax.plot(df["date"], df["revenue"], color="#2563EB", linewidth=2, label="Revenue") ax.fill_between(df["date"], df["revenue"], alpha=0.1, color="#2563EB") เพิ่ม Area ใต้เส้น ax.set_title("Monthly Revenue Trend", fontsize=16, fontweight="bold") ควรใส่ Grid ด้วย ax.grid(True, alpha=0.3) เพื่อให้อ่านค่าง่ายขึ้น</p><h3>Bar Chart — เปรียบเทียบหมวดหมู่</h3><p>ax.bar(categories, values, color=["#2563EB", "#16A34A", "#DC2626", "#D97706"]) สร้าง Bar Chart หลากสีตาม Category สำหรับ Horizontal Bar ใช้ ax.barh() ซึ่งอ่านชื่อ Category ยาวๆ ได้ง่ายกว่า เพิ่ม Value Label บน Bar ด้วย for bar in bars: ax.text(bar.get_x() + bar.get_width()/2, bar.get_height(), f"{bar.get_height():,.0f}")</p><h3>Scatter Plot — ความสัมพันธ์</h3><p>ax.scatter(df["ad_spend"], df["revenue"], alpha=0.6, c=df["category"].cat.codes, cmap="tab10", s=df["order_count"]*10) สร้าง Scatter Plot ที่มี Color ตาม Category และ Size ตาม Order Count เพิ่ม Trend Line ด้วย numpy polyfit และ ax.plot(x, np.poly1d(z)(x), "r--") เพื่อเห็น Correlation ชัดขึ้น</p><h3>Subplot: หลายกราฟในหน้าเดียว</h3><p>fig, axes = plt.subplots(2, 2, figsize=(14, 10)) สร้าง Grid 2x2 จากนั้น axes[0,0].plot(...) วาดกราฟแรก axes[0,1].bar(...) วาดกราฟที่สอง ใช้ fig.suptitle("Sales Dashboard", fontsize=18) ตั้งชื่อ Figure หลัก และ plt.tight_layout() จัดระยะห่างให้ Subplot ไม่ทับกัน</p><div class="tip-box"><strong>💡 Design Tip:</strong> สีสวยทำให้ Visualization น่าเชื่อถือขึ้น หลีกเลี่ยงสีพื้นฐาน เช่น red, blue, green ที่ดูไม่ Professional ใช้ Palette จาก Colorbrewer หรือ Seaborn เช่น palette="husl" หรือ palette="Set2" และเลือก Color ให้สอดคล้องกับ Brand Color ของบริษัท</div><h3>Save และ Export</h3><p>plt.savefig("output.png", dpi=300, bbox_inches="tight") บันทึกกราฟความละเอียดสูง สำหรับ PDF ใช้ plt.savefig("output.pdf", format="pdf") สำหรับ SVG ที่ Scale ได้ใช้ plt.savefig("output.svg") dpi=300 เหมาะสำหรับ Print และ Presentation ส่วน dpi=72 เหมาะสำหรับแสดงบนเว็บ</p>'
  },
  {
    number: 7,
    slug: 'visualization-seaborn',
    emoji: '🎨',
    title: 'Advanced Visualization ด้วย seaborn',
    content: '<h2>seaborn: Statistical Visualization ที่สวยงาม</h2><p>seaborn เป็น Library ที่สร้างบน matplotlib แต่มี API ที่ง่ายกว่า และสร้าง Statistical Visualization ที่สวยงามได้โดยอัตโนมัติ seaborn เหมาะมากสำหรับการสำรวจความสัมพันธ์ระหว่างตัวแปรและดูการกระจายของข้อมูล</p><h3>Setup seaborn</h3><p>import seaborn as sns sns.set_theme(style="whitegrid", palette="husl") ตั้งค่า Theme ทั่วไป seaborn มี Theme ให้เลือกหลาย Style เช่น "white", "dark", "whitegrid", "darkgrid", "ticks" และ Palette หลายร้อยแบบ การตั้ง Theme ครั้งเดียวที่ต้น Notebook ทำให้ทุกกราฟมีสไตล์เดียวกัน</p><h3>Heatmap: วิเคราะห์ Correlation</h3><p>Heatmap ใช้ดู Correlation ระหว่างตัวแปรตัวเลขได้ชัดเจน corr_matrix = df.select_dtypes(include="number").corr() fig, ax = plt.subplots(figsize=(10, 8)) sns.heatmap(corr_matrix, annot=True, fmt=".2f", cmap="coolwarm", center=0, ax=ax) แสดง Correlation Matrix พร้อมค่าตัวเลข สีแดงคือ Positive Correlation สูง สีน้ำเงินคือ Negative Correlation สูง</p><h3>Pairplot: EDA อย่างรวดเร็ว</h3><p>sns.pairplot(df, hue="category", vars=["revenue", "quantity", "price", "rating"]) สร้าง Grid ของ Scatter Plot และ Distribution สำหรับทุก Combination ของตัวแปรที่เลือก โดย Color ตาม Category หนึ่งคำสั่งทำแทน 16 กราฟ เหมาะมากสำหรับ EDA เบื้องต้น</p><h3>Distribution Plots</h3><p>sns.histplot(df["revenue"], bins=30, kde=True) สร้าง Histogram พร้อม KDE Curve sns.boxplot(x="category", y="revenue", data=df) เปรียบเทียบ Distribution ของแต่ละ Category sns.violinplot(x="category", y="revenue", data=df) แสดง Distribution ในรูปแบบ Violin ที่สวยงามกว่า Boxplot และ sns.kdeplot(df["revenue"], fill=True) สร้าง Smooth Density Curve</p><h3>Categorical Plots</h3><p>sns.barplot(x="category", y="revenue", data=df, estimator="mean", errorbar="sd") สร้าง Bar Chart พร้อม Error Bar แสดงค่าเฉลี่ยและ Standard Deviation sns.countplot(x="status", data=df, order=df["status"].value_counts().index) นับและแสดง Count เรียงตามความถี่ sns.stripplot(x="category", y="rating", data=df, jitter=True, alpha=0.5) แสดง Individual Data Points</p><div class="tip-box"><strong>💡 seaborn vs matplotlib:</strong> ใช้ seaborn เมื่อต้องการ Statistical Plot อย่างรวดเร็ว และใช้ matplotlib เมื่อต้องการควบคุม Detail มากๆ ในทางปฏิบัติมักใช้ทั้งสองร่วมกัน สร้าง Plot ด้วย seaborn แล้ว Customize ด้วย matplotlib ax method</div><h3>FacetGrid: กราฟแยกตาม Category</h3><p>g = sns.FacetGrid(df, col="year", row="category", height=4) g.map(sns.histplot, "revenue") g.add_legend() สร้าง Grid ของกราฟแยกตาม Year และ Category โดยอัตโนมัติ ทรงพลังมากสำหรับ Exploratory Analysis เปรียบเทียบ Pattern ระหว่างกลุ่ม</p>'
  },
  {
    number: 8,
    slug: 'statistical-analysis',
    emoji: '📐',
    title: 'Statistical Analysis — Mean, Median, Std, Correlation',
    content: '<h2>Statistics ใน Python สำหรับ DA</h2><p>DA ไม่ต้องเป็นนักสถิติ แต่ต้องเข้าใจ Statistics พื้นฐานเพื่อตีความข้อมูลได้ถูกต้องและไม่สรุปผิด Python มี numpy, scipy และ statsmodels ที่ทำ Statistical Analysis ได้ครบถ้วน บทนี้จะเน้น Statistics ที่ DA ใช้จริงบ่อยที่สุด</p><h3>Descriptive Statistics ด้วย pandas</h3><p>df.describe() ให้ Count, Mean, Std, Min, Max, Q1, Q2, Q3 df["revenue"].mean() ค่าเฉลี่ย df["revenue"].median() ค่ากลาง df["revenue"].std() Standard Deviation df["revenue"].var() Variance df["revenue"].skew() ความเบ้ df["revenue"].kurtosis() ความโด่ง df["revenue"].quantile([0.25, 0.5, 0.75]) Quartiles</p><h3>ทำความเข้าใจ Distribution</h3><p>การเข้าใจว่าข้อมูลกระจายอย่างไรสำคัญมาก Skewed Distribution: ถ้า Mean > Median ข้อมูลเบ้ขวา (มี Outlier ค่าสูง) ถ้า Mean < Median ข้อมูลเบ้ซ้าย ยอดขายและรายได้มักเบ้ขวาเสมอ ควรใช้ Median แทน Mean ในการรายงาน เพราะ Median ทนต่อ Outlier มากกว่า</p><h3>Correlation Analysis</h3><p>df.corr() คำนวณ Pearson Correlation Matrix df.corr(method="spearman") ใช้ Spearman สำหรับข้อมูลที่ไม่ได้กระจายแบบ Normal หรือมี Outlier สูง ค่า Correlation ใกล้ 1 หรือ -1 หมายถึงสัมพันธ์กันสูง ใกล้ 0 หมายถึงไม่สัมพันธ์กัน ข้อควรระวัง Correlation ไม่ใช่ Causation</p><h3>Confidence Interval</h3><p>from scipy import stats ค่าเฉลี่ยตัวอย่าง sample_mean = df["revenue"].mean() ช่วงความเชื่อมั่น 95% ci = stats.t.interval(0.95, df=len(df)-1, loc=sample_mean, scale=stats.sem(df["revenue"])) ผลลัพธ์คือขอบล่างและขอบบนของ Confidence Interval บอกว่าค่าเฉลี่ยจริงน่าจะอยู่ในช่วงนี้ด้วยความน่าจะเป็น 95%</p><h3>t-test: เปรียบเทียบค่าเฉลี่ย</h3><p>ใช้ t-test เพื่อเปรียบเทียบว่าค่าเฉลี่ยของสองกลุ่มต่างกันอย่างมีนัยสำคัญหรือไม่ from scipy import stats group_a = df[df["group"] == "A"]["conversion_rate"] group_b = df[df["group"] == "B"]["conversion_rate"] t_stat, p_value = stats.ttest_ind(group_a, group_b) ถ้า p_value < 0.05 แสดงว่าต่างกันอย่างมีนัยสำคัญทางสถิติ</p><div class="tip-box"><strong>💡 สำคัญมาก:</strong> p-value < 0.05 ไม่ได้แปลว่าผลต่างนั้น "สำคัญทางธุรกิจ" มันแปลว่าผลต่างน่าจะไม่ได้เกิดจากโอกาสสุ่ม ต้องดู Effect Size ด้วยว่าความต่างนั้นมากพอที่จะ Take Action หรือไม่ เป็น Concept ที่ DA มักเข้าใจผิดบ่อย</div><h3>Regression Analysis เบื้องต้น</h3><p>from scipy.stats import linregress slope, intercept, r_value, p_value, std_err = linregress(df["ad_spend"], df["revenue"]) r_squared = r_value**2 บอกว่า ad_spend อธิบาย Variance ของ revenue ได้กี่เปอร์เซนต์ ยิ่ง R² ใกล้ 1 ยิ่งดี สำหรับ Multiple Regression ใช้ statsmodels.api.OLS() ซึ่งให้รายละเอียดครบกว่า</p>'
  },
  {
    number: 9,
    slug: 'business-insights',
    emoji: '💡',
    title: 'Business Insights จากข้อมูลจริง',
    content: '<h2>จาก Data สู่ Business Insight</h2><p>ทักษะที่ทำให้ DA มีคุณค่าสูงสุดคือการแปลงข้อมูลดิบให้กลายเป็น Actionable Insight ที่ Business สามารถนำไปใช้ได้จริง ไม่ใช่แค่รายงานตัวเลข แต่ต้องบอกได้ว่า "เพราะอะไร" และ "ควรทำอะไรต่อ"</p><h3>Framework: Data → Insight → Action</h3><p>กระบวนการสร้าง Business Insight เริ่มจาก Data Collection รวบรวมข้อมูลที่เกี่ยวข้อง Data Analysis วิเคราะห์ด้วย pandas หาสิ่งที่น่าสนใจ Pattern Recognition ค้นหา Pattern ที่ผิดปกติหรือน่าสนใจ Hypothesis Generation ตั้งสมมติฐานว่าเกิดอะไรขึ้น Validation ทดสอบสมมติฐานด้วย Statistical Testing และสุดท้าย Recommendation เสนอ Action ที่ชัดเจนพร้อมเหตุผล</p><h3>Customer Segmentation ด้วย Python</h3><p>แบ่ง Customer Segment ตาม RFM Score rfm = orders.groupby("customer_id").agg(recency=("order_date", lambda x: (pd.Timestamp.now() - x.max()).days), frequency=("order_id", "count"), monetary=("amount", "sum")) rfm["r_score"] = pd.qcut(rfm["recency"], 4, labels=[4,3,2,1]) rfm["f_score"] = pd.qcut(rfm["frequency"].rank(method="first"), 4, labels=[1,2,3,4]) rfm["m_score"] = pd.qcut(rfm["monetary"], 4, labels=[1,2,3,4]) rfm["segment"] = rfm["r_score"].astype(str) + rfm["f_score"].astype(str) + rfm["m_score"].astype(str)</p><h3>Price Elasticity Analysis</h3><p>ศึกษาว่าเมื่อราคาเพิ่มขึ้น Demand เปลี่ยนแปลงแค่ไหน price_demand = df.groupby("price")["quantity_sold"].mean().reset_index() from scipy.stats import linregress slope, intercept, r, p, se = linregress(price_demand["price"], price_demand["quantity_sold"]) elasticity = slope * (price_demand["price"].mean() / price_demand["quantity_sold"].mean()) ค่า Elasticity < -1 หมายถึง Demand ลดมากเมื่อราคาขึ้น</p><h3>Cohort Retention Analysis</h3><p>วิเคราะห์ว่าลูกค้าที่เริ่มซื้อในเดือนเดียวกันยังคงซื้ออยู่ในเดือนต่อๆ ไปหรือไม่ orders["cohort_month"] = orders.groupby("customer_id")["order_date"].transform("min").dt.to_period("M") orders["order_month"] = orders["order_date"].dt.to_period("M") orders["period_number"] = (orders["order_month"] - orders["cohort_month"]).apply(lambda x: x.n) cohort_table = orders.groupby(["cohort_month", "period_number"])["customer_id"].nunique().unstack() retention_table = cohort_table.divide(cohort_table[0], axis=0)</p><div class="tip-box"><strong>💡 Insight Presentation Tip:</strong> เวลานำเสนอ Insight ให้ Business ใช้โครงสร้าง BLUF (Bottom Line Up Front) คือบอกสรุปก่อน แล้วค่อยอธิบาย Detail เพราะ Executive มักไม่มีเวลาฟังรายละเอียดยาวๆ ตัวอย่าง "ยอดขายออนไลน์เติบโต 35% YoY โดยมีกลุ่มลูกค้า Premium เป็นตัวขับเคลื่อนหลัก แนะนำให้เพิ่ม Budget การตลาดในกลุ่มนี้"</div><h3>Anomaly Detection เบื้องต้น</h3><p>ตรวจจับความผิดปกติในข้อมูล daily_revenue = df.groupby("date")["revenue"].sum() rolling_mean = daily_revenue.rolling(7).mean() rolling_std = daily_revenue.rolling(7).std() anomalies = daily_revenue[(daily_revenue > rolling_mean + 2*rolling_std) | (daily_revenue < rolling_mean - 2*rolling_std)] Anomaly อาจเป็นสัญญาณของ Fraud, System Error, หรือ Marketing Event ที่น่าสนใจ</p>'
  },
  {
    number: 10,
    slug: 'automation-reports',
    emoji: '🤖',
    title: 'Auto Report — สร้าง PDF และ Excel อัตโนมัติ',
    content: '<h2>Report Automation: ทำครั้งเดียว ใช้ทุกวัน</h2><p>หนึ่งใน Value Proposition ที่ดีที่สุดของ DA ที่รู้ Python คือการ Automate งาน Report ที่เคยทำด้วยมือทุกสัปดาห์ การสร้าง Script ที่ Pull ข้อมูล วิเคราะห์ และส่ง Report ให้ทีมโดยอัตโนมัติสามารถประหยัดเวลาได้หลายชั่วโมงต่อสัปดาห์</p><h3>สร้าง Excel Report ด้วย openpyxl</h3><p>import openpyxl from openpyxl.chart import BarChart, Reference wb = openpyxl.Workbook() ws = wb.active ws.title = "Monthly Report" ws["A1"] = "Monthly Sales Report" สร้าง Header, ใส่ข้อมูลจาก DataFrame, สร้าง Chart, ตกแต่ง Style และ wb.save("monthly_report.xlsx") สร้างไฟล์ Excel ที่สวยงามและมีกราฟโดยอัตโนมัติ</p><h3>สร้าง Excel ที่ซับซ้อนด้วย xlsxwriter</h3><p>xlsxwriter ให้ Control มากกว่า openpyxl import xlsxwriter workbook = xlsxwriter.Workbook("report.xlsx") worksheet = workbook.add_worksheet("Summary") header_format = workbook.add_format({"bold": True, "bg_color": "#2563EB", "font_color": "white"}) สร้าง Format ที่ต้องการ ใส่ข้อมูล สร้าง Chart และ workbook.close() xlsxwriter รองรับ Conditional Formatting, Data Validation, Charts หลากหลายประเภท</p><h3>สร้าง PDF Report ด้วย reportlab</h3><p>from reportlab.lib.pagesizes import A4 from reportlab.platypus import SimpleDocTemplate, Table, TableStyle, Paragraph, Image doc = SimpleDocTemplate("report.pdf", pagesize=A4) elements = [] elements.append(Paragraph("Monthly Sales Report", title_style)) บันทึก matplotlib Figure เป็น Image ก่อน แล้วใส่ใน PDF elements.append(Image("chart.png", width=400, height=250)) elements.append(Table(data, style=table_style)) doc.build(elements)</p><h3>ส่ง Email อัตโนมัติ</h3><p>import smtplib from email.mime.multipart import MIMEMultipart from email.mime.base import MIMEBase from email import encoders msg = MIMEMultipart() msg["Subject"] = "Weekly Report - " + datetime.now().strftime("%Y-%m-%d") msg["From"] = "da@company.com" msg["To"] = "manager@company.com" แนบไฟล์ Excel หรือ PDF และส่งผ่าน SMTP Server สำหรับ Gmail ต้องใช้ App Password แทน Password จริง</p><h3>Schedule ด้วย Schedule Library</h3><p>import schedule import time def generate_and_send_report(): สร้างและส่ง Report schedule.every().monday.at("08:00").do(generate_and_send_report) while True: schedule.run_pending() time.sleep(60) Script นี้จะส่ง Report ทุกวันจันทร์เวลา 08:00 โดยอัตโนมัติ สำหรับ Production แนะนำให้ใช้ Cron Job บน Server หรือ Apache Airflow</p><div class="tip-box"><strong>💡 Pro Tip:</strong> ก่อน Automate ให้แน่ใจว่า Report Manual ทำงานถูกต้องก่อน แล้วค่อย Automate ขั้นตอนทีละขั้น อย่า Automate ทุกอย่างพร้อมกัน เพราะถ้ามี Bug จะหาสาเหตุยาก และเพิ่ม Error Handling สำหรับทุก Step เพื่อให้ Script แข็งแกร่งพอสำหรับการใช้งาน Production</div><h3>Streamlit: Dashboard ออนไลน์แบบง่าย</h3><p>Streamlit ช่วยให้ DA สร้าง Interactive Dashboard ได้โดยไม่ต้องรู้ Web Development import streamlit as st st.title("Sales Dashboard") date_range = st.date_input("เลือกช่วงวันที่", value=[start_date, end_date]) filtered_df = df[(df["date"] >= date_range[0]) & (df["date"] <= date_range[1])] st.metric("Total Revenue", f"{filtered_df.revenue.sum():,.0f}") st.plotly_chart(create_chart(filtered_df)) จากนั้น streamlit run app.py เพื่อเปิด Dashboard ในเบราวเซอร์</p>'
  },
  {
    number: 11,
    slug: 'api-data',
    emoji: '🌐',
    title: 'Pull ข้อมูลจาก API ด้วย Python',
    content: '<h2>API Data: ดึงข้อมูล Real-time</h2><p>หลายธุรกิจมีข้อมูลที่เข้าถึงได้ผ่าน API เช่น ข้อมูลการขายจาก Shopify, Engagement จาก Facebook, Performance จาก Google Analytics หรือข้อมูลตลาดหุ้น การรู้วิธี Pull ข้อมูลจาก API ช่วยให้ DA สามารถวิเคราะห์ข้อมูล Real-time ได้โดยไม่ต้องรอใคร</p><h3>REST API คืออะไร</h3><p>API (Application Programming Interface) คือช่องทางที่โปรแกรมสื่อสารกัน REST API ใช้ HTTP Protocol และรับ-ส่งข้อมูลในรูป JSON โดยทั่วไป Endpoint คือ URL ที่ใช้เรียก API HTTP Methods ที่ใช้บ่อยคือ GET (ดึงข้อมูล), POST (ส่งข้อมูล), PUT (อัพเดท), DELETE (ลบ) Authentication มักใช้ API Key หรือ OAuth Token</p><h3>requests Library พื้นฐาน</h3><p>import requests response = requests.get("https://api.example.com/sales", headers={"Authorization": "Bearer YOUR_API_KEY"}, params={"start_date": "2024-01-01", "end_date": "2024-12-31"}) ตรวจสอบ Status Code response.status_code ดึงข้อมูล JSON data = response.json() แปลงเป็น DataFrame df = pd.json_normalize(data["results"]) และวิเคราะห์ต่อได้เลย</p><h3>Handle Pagination</h3><p>API ส่วนใหญ่ไม่ส่งข้อมูลทีเดียวทั้งหมด มักแบ่งเป็น Pages all_data = [] page = 1 while True: response = requests.get(url, params={"page": page, "limit": 100, "api_key": KEY}) data = response.json() if not data["results"]: break all_data.extend(data["results"]) page += 1 df = pd.DataFrame(all_data) วนจนกว่าจะได้ข้อมูลครบทุก Page</p><h3>Rate Limiting และ Error Handling</h3><p>API มักมี Rate Limit เช่น 100 requests ต่อนาที ต้องใส่ Delay import time time.sleep(0.5) ระหว่าง Request และใส่ Error Handling try: response = requests.get(url, timeout=30) response.raise_for_status() except requests.exceptions.RequestException as e: print(f"Error: {e}") การใส่ Retry Logic ก็สำคัญเพราะ Network อาจไม่เสถียร</p><h3>Google Sheets API</h3><p>import gspread from google.oauth2.service_account import Credentials creds = Credentials.from_service_account_file("credentials.json", scopes=["https://www.googleapis.com/auth/spreadsheets"]) client = gspread.authorize(creds) sheet = client.open_by_key("SPREADSHEET_ID").worksheet("Sheet1") data = sheet.get_all_records() df = pd.DataFrame(data) สามารถ Write กลับไปด้วย sheet.update("A1", df.values.tolist())</p><div class="tip-box"><strong>💡 Security Tip:</strong> ห้าม Hardcode API Key ใน Code เด็ดขาด ให้เก็บใน Environment Variable แทน import os api_key = os.environ.get("MY_API_KEY") หรือใช้ python-dotenv และไฟล์ .env ที่ไม่ได้ Commit ขึ้น Git อย่าลืมเพิ่ม .env ใน .gitignore</div><h3>เก็บข้อมูล API ลง Database</h3><p>หลัง Pull ข้อมูลจาก API แล้ว สามารถเก็บลงฐานข้อมูลเพื่อใช้ซ้ำได้ from sqlalchemy import create_engine engine = create_engine("postgresql://user:password@localhost/mydb") df.to_sql("api_data", engine, if_exists="append", index=False) ใช้ if_exists="append" เพื่อต่อท้ายข้อมูลเดิม หรือ "replace" เพื่อแทนที่ทั้งตาราง</p>'
  },
  {
    number: 12,
    slug: 'capstone',
    emoji: '🏆',
    title: 'Capstone Project — Sales Analysis ครบวงจร',
    content: '<h2>Capstone Project: Sales Analysis</h2><p>ถึงเวลารวบรวมทุก Skill ที่เรียนมาในคอร์สนี้เพื่อทำ End-to-End Sales Analysis Project ที่สามารถใส่ใน Portfolio ได้จริง Project นี้จะครอบคลุมตั้งแต่ Data Loading, Cleaning, EDA, Visualization, Statistical Analysis ไปจนถึงการสรุป Business Insights และสร้าง Report</p><h3>Dataset ที่จะใช้</h3><p>ในโปรเจคนี้จะใช้ Dataset E-commerce Sales ที่มีตารางหลัก 4 ตาราง ได้แก่ orders (order_id, customer_id, order_date, total_amount, status), customers (customer_id, name, email, country, signup_date), order_items (item_id, order_id, product_id, quantity, price), และ products (product_id, name, category, cost) สามารถดาวน์โหลดจาก Kaggle หรือสร้าง Synthetic Data ด้วย Faker Library</p><h3>Step 1: Data Loading และ Merging</h3><p>orders = pd.read_csv("orders.csv", parse_dates=["order_date"]) customers = pd.read_csv("customers.csv", parse_dates=["signup_date"]) items = pd.read_csv("order_items.csv") products = pd.read_csv("products.csv") full_df = orders.merge(customers, on="customer_id").merge(items, on="order_id").merge(products, on="product_id") full_df["profit"] = (full_df["price"] - full_df["cost"]) * full_df["quantity"] full_df["revenue"] = full_df["price"] * full_df["quantity"]</p><h3>Step 2: Data Cleaning</h3><p>ตรวจหา Missing Values และ Duplicates แก้ไข Data Types ให้ถูกต้อง ลบ Cancelled Orders ออกจากการวิเคราะห์ยอดขาย (แต่เก็บไว้วิเคราะห์ Cancellation Rate แยก) และ Standardize ชื่อสินค้าและหมวดหมู่ให้สม่ำเสมอ Document ทุกขั้นตอน Cleaning ใน Markdown Cell</p><h3>Step 3: Exploratory Analysis</h3><p>สร้าง Monthly Revenue Trend Chart ดู Seasonal Pattern วิเคราะห์ Top 10 Products by Revenue และ Profit Margin เปรียบเทียบยอดขายตาม Country วิเคราะห์ Customer Acquisition ต่อเดือน ดู Repeat Purchase Rate และทำ RFM Analysis เพื่อแบ่ง Customer Segment</p><h3>Step 4: Business Questions ที่ต้องตอบ</h3><p>โปรเจคต้องตอบ 5 Business Questions หลัก ได้แก่ หนึ่ง สินค้าหมวดหมู่ไหนทำกำไรมากที่สุด สอง ลูกค้ากลุ่มไหน (RFM Segment) มีมูลค่ามากที่สุด สาม ช่วงเวลาใดในปีที่ยอดขายดีที่สุด สี่ อัตราการ Churn เป็นเท่าไหร่และมีสัญญาณอะไรก่อนที่ลูกค้าจะ Churn และห้า ประเทศไหนมีโอกาสเติบโตมากที่สุด</p><div class="tip-box"><strong>💡 Portfolio Tip:</strong> เมื่อทำ Project เสร็จ ให้เขียน Executive Summary ความยาว 1 หน้า สรุป Key Findings และ Recommendations อัพโหลดขึ้น GitHub พร้อม README ที่อธิบายโปรเจค และแชร์ใน LinkedIn หรือ Kaggle เพื่อให้ HR และ Hiring Manager เห็น นี่คือ Portfolio ที่แสดงให้เห็น End-to-End Skills จริงๆ</div><h3>Step 5: Final Report</h3><p>สร้าง Executive Report ใน PDF ประกอบด้วย Cover Page, Executive Summary, Key Metrics Dashboard, Detailed Analysis ต่อ Business Question, Recommendations พร้อมลำดับความสำคัญ และ Appendix ที่มี Data Sources และ Methodology ใช้ reportlab หรือ Export จาก Jupyter เป็น HTML แล้ว Print เป็น PDF เพื่อ Presentation ที่ดูเป็น Professional</p>'
  }
];
