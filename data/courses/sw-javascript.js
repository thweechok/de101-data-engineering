export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '📚',
    title: 'บทนำ: ทำความรู้จักกับ JavaScript (Overview)',
    content: '<h2>ทำไมต้อง JavaScript? (Why JavaScript?)</h2><p>ในโลกของการพัฒนาเว็บไซต์ หากเปรียบเทียบ HTML เป็นโครงสร้างกระดูกที่ทำให้เว็บไซต์มีรูปร่าง และ CSS เป็นผิวหนังหรือเสื้อผ้าที่ทำให้เว็บไซต์ดูสวยงามน่าดึงดูด <strong>JavaScript</strong> ก็คือสมองและระบบประสาทที่ทำให้เว็บไซต์สามารถมีชีวิต โต้ตอบกับผู้ใช้งานได้จริง (Interactive) อย่างชาญฉลาด</p><p>JavaScript เป็นภาษาโปรแกรมที่ทำงานบนเว็บเบราว์เซอร์ (Client-side) ซึ่งช่วยให้คุณสามารถอัปเดตเนื้อหา เปลี่ยนแปลงสไตล์ โต้ตอบกับผู้ใช้ ตรวจสอบฟอร์ม สร้างแอนิเมชัน และดึงข้อมูลจากเซิร์ฟเวอร์ได้โดยไม่ต้องรีเฟรชหน้าเว็บ สิ่งเหล่านี้ทำให้ประสบการณ์ผู้ใช้ (UX) ลื่นไหลและรวดเร็วเหมือนกำลังใช้งานแอปพลิเคชันบนมือถือหรือเดสก์ท็อป</p><h2>ประวัติความเป็นมาของ JavaScript (History of JavaScript)</h2><p>JavaScript ถูกสร้างขึ้นในปี 1995 โดย Brendan Eich ขณะที่ทำงานอยู่ที่บริษัท Netscape Communications สิ่งที่น่าทึ่งคือเขาใช้เวลาสร้างภาษานี้เวอร์ชันแรกเพียง 10 วันเท่านั้น! เริ่มแรกมันถูกเรียกว่า Mocha ต่อมาเปลี่ยนชื่อเป็น LiveScript และสุดท้ายเปลี่ยนเป็น JavaScript เพื่ออาศัยความนิยมของภาษา Java ที่กำลังโด่งดังสุดขีดในยุคนั้นเป็นกลยุทธ์ทางการตลาด (แม้ว่าทั้งสองภาษาจะมีโครงสร้างและแนวคิดพื้นฐานที่ไม่เกี่ยวข้องกันเลยก็ตาม)</p><p>ในช่วงแรก JavaScript มักถูกมองว่าเป็นเพียง "ภาษาของเล่น" หรือสคริปต์ง่ายๆ สำหรับทำลูกเล่นเล็กๆ น้อยๆ แต่เมื่อเวลาผ่านไป อินเทอร์เน็ตพัฒนาขึ้น ความสามารถของเบราว์เซอร์ก็ก้าวกระโดด ทำให้ JavaScript กลายเป็นเครื่องมือทรงพลังที่ไม่สามารถขาดได้เลยในปัจจุบัน</p><h2>ยุคใหม่ของ JS: ES6 และหลังจากนั้น (ES6+)</h2><p>จุดเปลี่ยนที่สำคัญที่สุดของ JavaScript คือการเปิดตัวมาตรฐาน <strong>ECMAScript 2015 (หรือที่รู้จักกันอย่างกว้างขวางในชื่อ ES6)</strong> ซึ่งนำเสนอฟีเจอร์ใหม่ๆ มากมายมหาศาลที่ทำให้การเขียนโค้ดสะอาดขึ้น เป็นระเบียบมากขึ้น และมีประสิทธิภาพสูงขึ้น ฟีเจอร์เด่นๆ เช่น การประกาศตัวแปรด้วย `let` และ `const`, ฟังก์ชันแบบลูกศร (Arrow Functions), คลาส (Classes), โครงสร้างข้อมูลแบบ Promise และอื่นๆ</p><p>ในปัจจุบัน JavaScript มีการอัปเดตฟีเจอร์เวอร์ชันใหม่ทุกๆ ปี (ES7, ES8, ES9, ...) ทำให้มีเครื่องมือที่ทันสมัยอยู่เสมอ การเรียนรู้มาตรฐานใหม่เหล่านี้เป็นสิ่งสำคัญมากสำหรับนักพัฒนาเว็บยุคใหม่</p><div class="tip-box"><strong>💡 คำศัพท์ที่ควรรู้</strong>: ECMAScript (ES) คือ "เอกสารมาตรฐาน" ว่าภาษาควรทำงานอย่างไร ส่วน JavaScript คือ "การสร้างภาษาจริง" ที่ทำตามมาตรฐานนั้น</div>'
  },
  {
    number: 1,
    slug: 'variables',
    emoji: '📦',
    title: 'ตัวแปรและประเภทข้อมูล (Variables and Data Types)',
    content: '<h2>การประกาศตัวแปร (Declaring Variables)</h2><p>ตัวแปร (Variables) เปรียบเสมือนกล่องหรือภาชนะสำหรับเก็บข้อมูลในหน่วยความจำของคอมพิวเตอร์ ใน JavaScript เรามีวิธีประกาศตัวแปร 3 วิธีหลักๆ ซึ่งแต่ละวิธีมีพฤติกรรมแตกต่างกัน คือ <code>let</code>, <code>const</code>, และ <code>var</code></p><ul><li><strong>let</strong>: เป็นวิธีการประกาศตัวแปรสมัยใหม่ที่ใช้สำหรับข้อมูลที่ "ค่าสามารถเปลี่ยนแปลงได้ในอนาคต" (Reassignable) โดยมีขอบเขตการใช้งานจำกัดอยู่แค่ภายในบล็อก (Block Scope) ที่มันถูกประกาศเท่านั้น</li><li><strong>const</strong>: ย่อมาจาก Constant ใช้สำหรับประกาศ "ค่าคงที่" ที่ไม่ต้องการให้เปลี่ยนแปลงค่าหลังจากกำหนดค่าเริ่มต้นไปแล้ว หากพยายามเปลี่ยนค่า (Reassign) จะทำให้โปรแกรมเกิดข้อผิดพลาด (Error) ทันที การใช้ <code>const</code> เป็นทางเลือกที่ดีที่สุดที่ควรใช้เป็นค่าเริ่มต้น</li><li><strong>var</strong>: เป็นวิธีดั้งเดิมตั้งแต่เริ่มมี JS มีขอบเขตการใช้งานแบบฟังก์ชัน (Function Scope) <strong>ในปัจจุบันควรหลีกเลี่ยงการใช้ <code>var</code> อย่างเด็ดขาดในโปรเจกต์ใหม่</strong> เนื่องจากอาจทำให้เกิดข้อผิดพลาดที่ค้นหาแก้ไขได้ยากจากปัญหา Hoisting และ Scope ที่ไม่คาดคิด</li></ul><pre><code>let currentAge = 25;\ncurrentAge = 26; // สามารถเปลี่ยนค่าได้\n\nconst firstName = "John";\n// firstName = "Jane"; // จะเกิด Error: Assignment to constant variable.\n\nvar oldWay = "Not recommended";</code></pre><h2>ประเภทข้อมูลพื้นฐาน (Primitive Data Types)</h2><p>JavaScript มีประเภทข้อมูลพื้นฐานที่เรียกว่า Primitives ทั้งหมด 7 ชนิด ซึ่งเป็นหน่วยย่อยที่สุดของข้อมูล:</p><ol style="padding-left:20px"><li><strong>String (ข้อความ)</strong>: ชุดตัวอักษร ถูกล้อมรอบด้วย Single quote (\'), Double quote (") หรือ Backtick (`) ซึ่ง Backtick อนุญาตให้เราแทรกตัวแปรลงในข้อความได้ง่ายๆ ผ่าน ${} (Template Literals)</li><li><strong>Number (ตัวเลข)</strong>: ข้อมูลประเภทตัวเลข ทั้งจำนวนเต็มและทศนิยม ใน JS มีแค่ Number ประเภทเดียว ไม่มีแยก Int, Float, หรือ Double</li><li><strong>Boolean (ค่าความจริง)</strong>: ข้อมูลทางตรรกะ มีเพียง 2 ค่าคือ <code>true</code> (จริง) และ <code>false</code> (เท็จ)</li><li><strong>Null (ค่าว่างเปล่า)</strong>: ค่าที่ผู้เขียนโปรแกรมตั้งใจกำหนดให้เป็น "ไม่มีค่า" หรือ "ว่างเปล่า" เพื่อบ่งบอกว่าตอนนี้ไม่มีข้อมูล</li><li><strong>Undefined (ยังไม่กำหนดค่า)</strong>: ตัวแปรที่ถูกประกาศขึ้นมาแต่ยังไม่ได้ระบุค่า JS จะกำหนดให้เป็น <code>undefined</code> โดยอัตโนมัติ</li><li><strong>Symbol</strong>: ข้อมูลที่มีค่าไม่ซ้ำกันเลย นิยมใช้เป็น Key ลับใน Object (เริ่มมีใน ES6)</li><li><strong>BigInt</strong>: ใช้สำหรับเก็บตัวเลขจำนวนเต็มที่มีขนาดมหาศาล เกินกว่าขีดจำกัดที่ Number ปกติจะสามารถรองรับได้อย่างแม่นยำ</li></ol><div class="tip-box"><strong>💡 Pro Tip</strong>: คุณสามารถใช้คำสั่ง <code>typeof</code> ตรวจสอบประเภทของข้อมูลได้ เช่น <code>typeof 42</code> จะได้ "number" หรือ <code>typeof true</code> จะได้ "boolean" แต่แปลกตรงที่ <code>typeof null</code> จะคืนค่า "object" ซึ่งเป็นบั๊กของ JS ตั้งแต่ยุคแรกและไม่เคยถูกแก้ไขเพื่อความเข้ากันได้ของระบบเก่า!</div>'
  },
  {
    number: 2,
    slug: 'operators',
    emoji: '➕',
    title: 'ตัวดำเนินการและคณิตศาสตร์ (Operators and Math)',
    content: '<h2>ตัวดำเนินการทางคณิตศาสตร์ (Arithmetic Operators)</h2><p>JavaScript มีเครื่องหมายสำหรับคำนวณคณิตศาสตร์ครบถ้วน เช่น การบวก (<code>+</code>), ลบ (<code>-</code>), คูณ (<code>*</code>), หาร (<code>/</code>), การหารเอาเศษหรือโมดูโล (<code>%</code>) ซึ่งมีประโยชน์มากในการหาเลขคู่เลขคี่ หรือการคำนวณที่วนลูป และการยกกำลัง (<code>**</code>)</p><pre><code>const total = 10 + 5; // 15\nconst isEven = 10 % 2 === 0; // หาเศษเป็น 0 แสดงว่าเป็นเลขคู่\nconst power = 2 ** 3; // 2 ยกกำลัง 3 ได้ 8</code></pre><h2>ตัวดำเนินการเปรียบเทียบ (Comparison Operators)</h2><p>การเปรียบเทียบค่าเป็นหัวใจสำคัญของการเขียนโปรแกรม สิ่งที่ต้องระวังที่สุดใน JS คือความแตกต่างระหว่าง <code>==</code> และ <code>===</code></p><ul><li><strong>== (Loose Equality)</strong>: เปรียบเทียบเฉพาะ "ค่า" โดย JS จะพยายามแปลงประเภทข้อมูล (Type Coercion) ให้ตรงกันก่อนเปรียบเทียบ เช่น <code>5 == "5"</code> จะได้ <code>true</code> ซึ่งบ่อยครั้งทำให้เกิดพฤติกรรมแปลกๆ ที่เราไม่ได้คาดคิด</li><li><strong>=== (Strict Equality)</strong>: เปรียบเทียบทั้ง "ค่า" และ "ประเภทข้อมูล" ควบคู่กัน เช่น <code>5 === "5"</code> จะได้ <code>false</code> เพราะฝั่งซ้ายเป็น Number ฝั่งขวาเป็น String <strong>แนะนำให้ใช้ <code>===</code> เสมอเป็นนิสัยเพื่อความปลอดภัยและป้องกันบั๊ก</strong></li><li>นอกจากนี้ยังมี <code>!=</code>, <code>!==</code>, <code>&gt;</code>, <code>&lt;</code>, <code>&gt;=</code>, <code>&lt;=</code></li></ul><h2>ตัวดำเนินการทางตรรกะ (Logical Operators)</h2><p>ใช้สำหรับเชื่อมเงื่อนไขหลายๆ อันเข้าด้วยกัน:</p><ul><li><strong>&& (AND)</strong>: จะได้ผลเป็น <code>true</code> ก็ต่อเมื่อเงื่อนไข "ทั้งหมด" เป็นจริง</li><li><strong>|| (OR)</strong>: จะได้ผลเป็น <code>true</code> หากมีเงื่อนไขใดเงื่อนไขหนึ่งเป็นจริงเพียงข้อเดียว</li><li><strong>! (NOT)</strong>: เป็นการกลับด้านความจริง (Invert) จากจริงเป็นเท็จ และจากเท็จเป็นจริง</li></ul><h2>ฟีเจอร์โมเดิร์น: Nullish Coalescing และ Optional Chaining</h2><p>สองฟีเจอร์นี้ช่วยชีวิตนักพัฒนาได้มากเมื่อต้องจัดการกับข้อมูลที่อาจจะไม่มีอยู่จริง:</p><p><strong>Nullish Coalescing (<code>??</code>)</strong>: คืนค่าทางด้านขวา หากค่าทางด้านซ้ายมีค่าเป็น <code>null</code> หรือ <code>undefined</code> เหมาะกับการตั้งค่า Default fallback</p><p><strong>Optional Chaining (<code>?.</code>)</strong>: ช่วยป้องกันแอปพัง (Error) เมื่อเราพยายามเจาะลึกเข้าไปอ่าน Properties ของ Object ที่เป็น null หรือ undefined มันจะหยุดรันและคืนค่า undefined แทนที่จะแจ้ง Error ร้ายแรง</p><pre><code>const user = { name: "Alice" };\n// ถ้าไม่มี Optional Chaining และพยายามอ่าน user.address.city โปรแกรมจะพัง\nconsole.log(user.address?.city); // จะได้ undefined ออกมาอย่างปลอดภัย\n\nconst port = process.env.PORT ?? 3000; // ถ้าหา PORT ไม่เจอ ให้ใช้ 3000 แทน</code></pre><div class="tip-box"><strong>💡 ข้อควรระวัง</strong>: ความต่างระหว่าง <code>||</code> และ <code>??</code> คือ <code>||</code> จะทำงานเมื่อค่าซ้ายเป็น Falsy ทั้งหมด (เช่น 0, "") แต่ <code>??</code> จะโฟกัสแค่ null หรือ undefined เท่านั้น ทำให้ <code>??</code> ปลอดภัยกว่าหากต้องการอนุญาตให้ส่งค่า 0 เข้ามาได้</div>'
  },
  {
    number: 3,
    slug: 'control-flow',
    emoji: '🔀',
    title: 'การควบคุมเส้นทางโปรแกรม (Control Flow)',
    content: '<h2>เงื่อนไข if / else (If/Else Statements)</h2><p>Control flow คือการบอกให้โปรแกรมตัดสินใจเลือกทำงานบางส่วน และข้ามบางส่วนตามเงื่อนไขที่กำหนด <code>if / else</code> เป็นโครงสร้างที่เข้าใจง่ายที่สุด เราใช้มันเพื่อรันบล็อกของโค้ดเมื่อเงื่อนไข (Condition) เป็นความจริง และสามารถใช้ <code>else if</code> ในการทดสอบเงื่อนไขถัดไปได้เรื่อยๆ ก่อนจะปิดท้ายด้วย <code>else</code> หากไม่มีเงื่อนไขใดเป็นจริงเลย</p><pre><code>let score = 85;\nif (score >= 80) {\n  console.log("คุณได้เกรด A");\n} else if (score >= 70) {\n  console.log("คุณได้เกรด B");\n} else {\n  console.log("พยายามอีกนิดนะ!");\n}</code></pre><h2>Switch Statement</h2><p>เมื่อโปรแกรมมีความซับซ้อนและต้องนำตัวแปรตัวเดียวไปเทียบกับค่าคงที่ต่างๆ หลายกรณี (Cases) การใช้ <code>switch</code> จะช่วยให้โครงสร้างโค้ดอ่านง่ายและเป็นระเบียบกว่า <code>if / else</code> ที่ต่อกันยาวๆ สิ่งสำคัญคืออย่าลืมใส่ <code>break;</code> เพื่อหยุดการทำงานเมื่อเจอเคสที่ตรงกัน ไม่เช่นนั้นโค้ดจะรันทะลุเคสอื่นๆ ลงไป (Fall-through)</p><pre><code>const role = "admin";\nswitch (role) {\n  case "admin":\n    console.log("คุณมีสิทธิ์เข้าถึงทุกส่วน");\n    break;\n  case "editor":\n    console.log("คุณสามารถแก้ไขบทความได้");\n    break;\n  default:\n    console.log("คุณเป็นผู้ใช้ทั่วไป");\n}</code></pre><h2>Ternary Operator (if/else แบบย่อ)</h2><p>บ่อยครั้งที่เราแค่ต้องการเช็คเงื่อนไขง่ายๆ เพื่อกำหนดค่าให้ตัวแปร การใช้ Ternary Operator จะช่วยให้โค้ดสั้นลงเหลือบรรทัดเดียว รูปแบบการเขียนคือ <code>เงื่อนไข ? ถ้าจริงทำสิ่งนี้ : ถ้าเท็จทำสิ่งนี้</code></p><pre><code>const age = 20;\nconst canVote = (age >= 18) ? "มีสิทธิ์เลือกตั้ง" : "ยังไม่มีสิทธิ์เลือกตั้ง";</code></pre><h2>Truthy และ Falsy Values</h2><p>ความสามารถพิเศษอย่างหนึ่งของ JavaScript คือคุณสามารถเอาอะไรก็ตามไปใส่ในเงื่อนไข <code>if</code> ได้ ไม่จำเป็นต้องเป็น Boolean (true/false) โดยตรง JS จะทำการแปลงค่านั้นให้เป็น Boolean แบบชั่วคราว (Type Coercion) โดยมีกติกาดังนี้:</p><p><strong>Falsy values</strong> (ค่าที่จะถูกมองว่าเป็นเท็จ) มีอยู่เพียง 6 ตัวเท่านั้นในภาษา JS: <br>1. <code>false</code><br>2. <code>0</code> (เลขศูนย์)<br>3. <code>""</code> (สตริงว่าง)<br>4. <code>null</code><br>5. <code>undefined</code><br>6. <code>NaN</code> (Not a Number)</p><p><strong>Truthy values</strong>: ค่าใดๆ ก็ตามที่ไม่อยู่ใน 6 ข้อด้านบน จะถือว่าเป็นความจริง (true) ทั้งหมด แม้กระทั่ง สตริง <code>"false"</code>, สตริง <code>"0"</code>, อาเรย์ว่างเปล่า <code>[]</code>, หรืออ็อบเจกต์ว่างเปล่า <code>{}</code> ก็ตาม!</p><div class="tip-box"><strong>💡 เทคนิคการเขียนโค้ด</strong>: การเข้าใจ Truthy/Falsy ช่วยลดความยาวโค้ดได้มหาศาล แทนที่จะเขียน <code>if (username !== "")</code> เราสามารถเขียนแค่ <code>if (username) { ... }</code> ได้เลย โค้ดจะสะอาดตาขึ้นมาก</div>'
  },
  {
    number: 4,
    slug: 'functions',
    emoji: '⚙️',
    title: 'ฟังก์ชัน (Functions)',
    content: '<h2>ฟังก์ชันคืออะไร?</h2><p>ฟังก์ชันเปรียบเสมือน "โรงงานขนาดเล็ก" ภายในโค้ดของเรา เป็นชุดของคำสั่งที่ถูกจัดกลุ่มรวมกันเพื่อทำงานใดงานหนึ่งโดยเฉพาะ เมื่อเราเขียนฟังก์ชันเสร็จแล้ว เราสามารถเรียกใช้งานมัน (Call/Invoke) กี่ครั้งก็ได้ที่ไหนก็ได้ในโปรแกรม (Reusable) ช่วยแก้ปัญหาการก็อปปี้โค้ดมาวางซ้ำๆ (DRY - Don\'t Repeat Yourself)</p><h2>การประกาศฟังก์ชัน (Declarations vs Expressions)</h2><p><strong>1. Function Declaration</strong>: วิธีดั้งเดิมที่ประกาศด้วยคีย์เวิร์ด <code>function</code> แบบนี้จะมีความสามารถพิเศษคือโดน Hoisting ทำให้เราเรียกใช้งานบรรทัดไหนก็ได้ (แม้บรรทัดเรียกจะอยู่ก่อนบรรทัดประกาศก็ตาม)</p><pre><code>function greet(name) {\n  return "สวัสดีคุณ " + name;\n}</code></pre><p><strong>2. Function Expression</strong>: สร้างฟังก์ชันโดยไม่มีชื่อ (Anonymous function) แล้วจับใส่ตัวแปร แบบนี้จะไม่โดน Hoisting ต้องประกาศก่อนเรียกใช้เสมอ</p><pre><code>const greet = function(name) {\n  return "สวัสดีคุณ " + name;\n};</code></pre><h2>ฟังก์ชันลูกศร (Arrow Functions)</h2><p>เปิดตัวใน ES6 เป็นฟีเจอร์ที่ได้รับความนิยมสูงมากในปัจจุบันเพราะไวยากรณ์ที่สั้นกระชับ และไม่มีการสร้าง <code>this</code> Context ของตัวเอง ซึ่งแก้ปัญหาปวดหัวเรื่อง <code>this</code> ในหลายสถานการณ์ได้</p><pre><code>// รูปแบบมาตรฐานของ Arrow Function\nconst calculateTax = (price) =&gt; {\n  return price * 0.07;\n};\n\n// ถ้ามีลอจิกบรรทัดเดียว สามารถละปีกกา {} และคำว่า return ออกได้ (Implicit Return)\nconst multiply = (a, b) =&gt; a * b;\n\n// ถ้ามีพารามิเตอร์เดียว สามารถละวงเล็บ () ได้ด้วย\nconst square = x =&gt; x * x;</code></pre><h2>Parameters, Default Parameters และ Arguments</h2><p>เราส่งข้อมูลเข้าสู่ฟังก์ชันผ่าน <strong>Parameters</strong> (ตัวแปรตอนประกาศ) และเรียกสิ่งที่เราส่งจริงๆ ว่า <strong>Arguments</strong> (ข้อมูลตอนเรียกใช้)</p><p>บางครั้งผู้ใช้อาจลืมส่งข้อมูลมา เราสามารถตั้งค่าเริ่มต้น (Default Parameters) ไว้เผื่อได้ ป้องกันการเกิด <code>undefined</code> แล้วนำไปสู่ Error</p><pre><code>function greetUser(username = "Guest") {\n  console.log(`Welcome, ${username}!`);\n}\n\ngreetUser("John"); // Welcome, John!\ngreetUser(); // Welcome, Guest! (ใช้ค่า default)</code></pre><h2>การส่งค่ากลับ (Return Values)</h2><p>คำสั่ง <code>return</code> คือการพ่นผลลัพธ์ออกจากโรงงาน เมื่อ JS รันมาเจอคำว่า return ฟังก์ชันนั้นจะหยุดการทำงานทันที โค้ดใดๆ ที่อยู่ใต้บรรทัด return จะไม่มีวันถูกรัน (Unreachable code) หากฟังก์ชันไม่มี <code>return</code> มันจะคืนค่าเป็น <code>undefined</code> โดยอัตโนมัติ</p>'
  },
  {
    number: 5,
    slug: 'scope',
    emoji: '🔭',
    title: 'ขอบเขตตัวแปรและ Hoisting (Scope and Hoisting)',
    content: '<h2>Scope (ขอบเขต) คืออะไร?</h2><p>Scope เปรียบเสมือน "อาณาเขต" หรือ "กำแพง" ที่กำหนดว่าตัวแปรและฟังก์ชันของเรานั้นสามารถถูกมองเห็นและเรียกใช้งานจากส่วนไหนของโปรแกรมได้บ้าง การเข้าใจ Scope จะช่วยป้องกันไม่ให้ตัวแปรไปทับซ้อนกับตัวแปรที่ชื่อเหมือนกันในที่อื่น JavaScript มี Scope หลักๆ 3 ประเภท:</p><ul><li><strong>Global Scope</strong>: ตัวแปรที่ประกาศอยู่นอกสุดของไฟล์ ไม่ถูกครอบด้วยบล็อกหรือฟังก์ชันใดๆ สามารถถูกเรียกใช้ ดัดแปลง และเข้าถึงได้จากทุกๆ บรรทัดในไฟล์นั้น (การสร้าง Global variable มากเกินไปเป็นนิสัยที่ไม่ดี เพราะอาจเกิดการตั้งชื่อชนกันและทำให้เกิดบั๊ก)</li><li><strong>Function Scope (Local Scope)</strong>: ตัวแปรที่ประกาศด้วย <code>var</code> ภายในฟังก์ชัน หรือพารามิเตอร์ของฟังก์ชัน จะมีชีวิตอยู่แค่ในขอบเขตของฟังก์ชันนั้น ไม่สามารถเรียกจากภายนอกได้</li><li><strong>Block Scope</strong>: ฟีเจอร์ใหม่ตั้งแต่ ES6 หากเราประกาศด้วย <code>let</code> หรือ <code>const</code> ภายในบล็อก <code>{ ... }</code> (เช่น ในคำสั่ง if, for, while) ตัวแปรนั้นจะอยู่รอดแค่ในปีกกานั้น ทันทีที่หลุดวงปีกกา ตัวแปรนั้นจะถูกลบออกจากระบบ</li></ul><pre><code>let globalMsg = "Global";\n\nfunction testScope() {\n  var funcMsg = "Function scoped";\n  if (true) {\n    let blockMsg = "Block scoped";\n    console.log(blockMsg); // เรียกใช้งานได้\n  }\n  // console.log(blockMsg); // พัง! Error: blockMsg is not defined\n}\n// console.log(funcMsg); // พัง! Error: funcMsg is not defined</code></pre><h2>Lexical Scope และ Closures</h2><p>ฟังก์ชันใน JavaScript จะจดจำสภาพแวดล้อม ณ ตำแหน่งที่มันถูก "สร้าง" ขึ้นมา ไม่ใช่ตอนที่มันถูก "เรียกใช้" หมายความว่าฟังก์ชันลูก (Inner) สามารถเข้าถึงตัวแปรของฟังก์ชันแม่ (Outer) ที่ครอบมันอยู่ได้เสมอ พฤติกรรมนี้เรียกว่า <strong>Lexical Scoping</strong> และเป็นพื้นฐานของการสร้างระบบปิดที่เรียกว่า <strong>Closure</strong> ที่นิยมใช้ทำ Data Privacy (ซ่อนตัวแปรไม่ให้ใครแก้ได้โดยตรง)</p><h2>ไขปริศนา Hoisting</h2><p>เวลาเราเขียนโค้ด JS เบื้องหลังตัว Engine (เช่น V8) จะทำการอ่านโค้ดเราสองรอบ รอบแรกคือ Compilation (อ่านหาตัวแปรและฟังก์ชันทั้งหมด) รอบสองคือ Execution (รันทีละบรรทัด) ขั้นตอนแรกนี้เองก่อให้เกิดพฤติกรรมที่เรียกว่า <strong>Hoisting</strong> ซึ่งดูเหมือนว่า JS แอบ "ยก" บรรทัดประกาศขึ้นไปไว้บนสุด</p><ul><li><strong>var</strong>: จะถูก Hoist ยกขึ้นไป และกำหนดค่าเริ่มต้นเป็น <code>undefined</code> ให้ด้วย ทำให้เรียกใช้ก่อนประกาศไม่ Error แต่ได้ค่า undefined</li><li><strong>let / const</strong>: จะถูก Hoist เช่นกัน แต่ถูกคุมขังในสถานะ <strong>Temporal Dead Zone (TDZ)</strong> ไม่ให้ค่าเริ่มต้นใดๆ หากพยายามเรียกใช้ก่อนบรรทัดประกาศ จะเจอตอกกลับด้วย Error อย่างรุนแรง ซึ่งถือเป็นเรื่องดีเพราะมันบังคับให้เราประกาศก่อนใช้เสมอ!</li><li><strong>Function Declaration</strong>: ถูก Hoist ขึ้นไปแบบสมบูรณ์พร้อมตัวบอดี้โค้ด จึงเรียกใช้ฟังก์ชันก่อนประกาศได้</li></ul><div class="tip-box"><strong>💡 กฎทองคำ</strong>: เพื่อหลีกเลี่ยงความปวดหัวกับ Hoisting ให้ยึดหลัก "ประกาศตัวแปร (let/const) ที่จุดเริ่มต้นของบล็อกเสมอ" และ "หลีกเลี่ยงการใช้ var ในทุกกรณี"</div>'
  },
  {
    number: 6,
    slug: 'arrays',
    emoji: '📊',
    title: 'โครงสร้างข้อมูลแบบอาร์เรย์ (Arrays)',
    content: '<h2>ทำความรู้จักกับ Array</h2><p>ในการเขียนโปรแกรม เรามักต้องจัดการกับกลุ่มของข้อมูลที่เหมือนๆ กัน หากต้องประกาศตัวแปร user1, user2, user3 ไปถึง user100 คงเหนื่อยแย่ Array จึงเข้ามาแก้ปัญหานี้ Array คือโครงสร้างข้อมูลที่ใช้เก็บข้อมูลหลายๆ ค่า (Elements) เรียงต่อกันเป็นคิวไว้ในตัวแปรเดียว การเข้าถึงแต่ละค่าจะใช้ตัวเลขชี้ตำแหน่งที่เรียกว่า Index โดยในโลกโปรแกรมมิ่ง Index ของ Array <strong>มักจะเริ่มต้นที่ 0 เสมอ</strong></p><pre><code>const techStack = ["JavaScript", "React", "Node.js", "MongoDB"];\nconsole.log(techStack[0]); // ผลลัพธ์: JavaScript (ตัวแรกสุด)\nconsole.log(techStack.length); // ผลลัพธ์: 4 (จำนวนสมาชิกทั้งหมด)\n// เข้าถึงตัวสุดท้ายเสมอ\nconsole.log(techStack[techStack.length - 1]); // ผลลัพธ์: MongoDB</code></pre><h2>Method พื้นฐานของการปรับแต่ง Array (Mutating Methods)</h2><p>JavaScript มาพร้อมกับฟังก์ชันอำนวยความสะดวกมากมายที่ติดมากับ Array เรียกว่า Methods กลุ่มนี้เป็น Method ที่เมื่อรันแล้ว จะเปลี่ยนข้อมูลใน Array ต้นฉบับ (Mutate) ทันที:</p><ul><li><strong>push(item)</strong>: เพิ่มสมาชิกใหม่เข้าไป <strong>ต่อท้าย</strong> (นิยมใช้บ่อยที่สุด)</li><li><strong>pop()</strong>: ดึงสมาชิก <strong>ตัวสุดท้าย</strong> ออกไปและลบทิ้ง</li><li><strong>unshift(item)</strong>: เพิ่มสมาชิกใหม่เข้าไป <strong>แทรกที่ตำแหน่งแรกสุด</strong> (index 0) ขยับตัวอื่นถอยหลัง</li><li><strong>shift()</strong>: ดึงสมาชิก <strong>ตัวแรกสุด</strong> ออกไปและลบทิ้ง</li></ul><h2>slice() ปะทะ splice() (การหั่นและการผ่าตัด)</h2><p>สองคำนี้สะกดคล้ายกัน ทำงานคล้ายกัน แต่วิธีปฏิบัติต่อข้อมูลต่างกันโดยสิ้นเชิง เป็นจุดที่หลายคนสับสน:</p><p><strong>1. slice(start, end) (คัดลอก)</strong>: แปลว่า "ฝาน" มันจะทำการคัดลอก (Copy) ส่วนหนึ่งของ Array ออกมาเป็น <strong>Array ก้อนใหม่เอี่ยม</strong> โดยไม่แตะต้องทำร้าย Array ต้นฉบับเลย เหมาะสำหรับการทำ Immutable Data <em>(หมายเหตุ: end คือตำแหน่งที่ตัด จะไม่ถูกรวมในผลลัพธ์)</em></p><p><strong>2. splice(start, deleteCount, items...) (ผ่าตัดแก้ไข)</strong>: ใช้สำหรับล้วงเข้าไปลบ แทรก หรือแทนที่ข้อมูลกลาง Array ได้อย่างอิสระ ข้อควรระวังสูงสุดคือ Method นี้ <strong>เปลี่ยนแปลง Array ต้นฉบับ</strong></p><pre><code>const fruits = ["Apple", "Banana", "Cherry", "Durian"];\n\n// slice คัดลอกออกมา\nconst myFav = fruits.slice(1, 3); // ได้ ["Banana", "Cherry"] (fruits ไม่เปลี่ยน)\n\n// splice ลบแล้วแทนที่\nfruits.splice(1, 1, "Blueberry"); \n// เริ่มตำแหน่ง 1, ลบทิ้ง 1 ตัว (Banana), แล้วยัด Blueberry ใส่แทน\n// ตอนนี้ fruits กลายเป็น ["Apple", "Blueberry", "Cherry", "Durian"]</code></pre><h2>Spread Operator (...) เพื่อความสะอาดของโค้ด</h2><p>สัญลักษณ์จุดสามจุด <code>...</code> นำมาใช้ "แตก" หรือ "กระจาย" สมาชิกที่ถูกอัดอยู่ใน Array ออกมาเป็นชิ้นๆ มันสร้างปาฏิหาริย์ในการเขียนโค้ดได้มากมาย เช่น การก็อปปี้ Array ทั้งยวงอย่างปลอดภัย หรือการนำ Array หลายอันมาต่อกันโดยไม่ต้องใช้ <code>concat()</code></p><pre><code>const frontend = ["HTML", "CSS", "JS"];\nconst backend = ["Node", "SQL"];\n\n// การรวม Array แบบสมัยใหม่\nconst fullstack = [...frontend, ...backend]; // ["HTML", "CSS", "JS", "Node", "SQL"]\n\n// การสร้างแฝด (Clone) ที่แก้ไขแล้วไม่กระทบต้นฉบับ\nconst frontendCopy = [...frontend];</code></pre>'
  },
  {
    number: 7,
    slug: 'array-iteration',
    emoji: '🔄',
    title: 'การวนลูปและจัดการอาร์เรย์ (Array Iteration)',
    content: '<h2>ทำไมลูปแบบเก่าถึงเชยไปแล้ว?</h2><p>การใช้ <code>for (let i = 0; i &lt; array.length; i++)</code> ไม่ได้ผิด และบางกรณียังทำงานได้เร็วที่สุดด้วยซ้ำ แต่มันมีโครงสร้างที่ยาวเยิ่นเย้อ (Boilerplate) และต้องมาจัดการตัวแปรเคาน์เตอร์ (i) เอง ในปัจจุบัน JavaScript สาย Declarative Programming มี Method ขั้นสูง (Higher-Order Functions) สำหรับจัดการ Array แบบทรงพลังและสื่อความหมายชัดเจนกว่ามาก</p><h2>แก๊ง 3 ทหารเสือ: map(), filter(), reduce()</h2><p>3 ฟังก์ชันนี้นิยมใช้มากที่สุดในสายงาน Web Development ทั้ง React, Vue หรือทำ Backend</p><p><strong>1. map() (ตัวแปลงร่าง)</strong><br>ทำหน้าที่วนลูปสมาชิก <strong>ทุกตัว</strong> จับโยนเข้าฟังก์ชัน และเอาผลลัพธ์ใหม่มาประกอบร่างเป็น <strong>Array ใหม่ที่มีขนาดเท่าเดิมเป๊ะ</strong> (ไม่กระทบต้นฉบับ) ใช้บ่อยมากเวลาดึงข้อมูล API มาแล้วต้องการดึงมาเฉพาะบางฟิลด์</p><pre><code>const prices = [100, 200, 300];\nconst withTax = prices.map(price =&gt; price * 1.07); // [107, 214, 321]</code></pre><p><strong>2. filter() (ตะแกรงร่อน)</strong><br>ทำหน้าที่คัดกรองข้อมูล เงื่อนไขในฟังก์ชันต้อง <code>return</code> ค่าความจริง (true/false) เท่านั้น สมาชิกตัวไหนที่ได้ <code>true</code> จะได้ไปต่อใน <strong>Array ใหม่</strong> ส่วนใครได้ <code>false</code> จะถูกทิ้งไป</p><pre><code>const scores = [45, 80, 22, 95, 60];\nconst passes = scores.filter(score =&gt; score &gt;= 50); // [80, 95, 60]</code></pre><p><strong>3. reduce() (เครื่องบดอัด)</strong><br>ทรงพลังแต่เข้าใจยากที่สุด มันทำหน้าที่ <strong>ยุบ</strong> Array ที่มีหลายสิบหลายร้อยค่า ให้รวบยอดเหลือ <strong>เพียงค่าเดียว (Single value)</strong> มันต้องการพารามิเตอร์ 2 ตัวคือ Accumulator (ตัวสะสม) และ Current Value (ค่าปัจจุบันในลูป) เหมาะสำหรับหาผลรวมราคาสินค้าในตะกร้า หรือจัดกลุ่มข้อมูล</p><pre><code>const cart = [150, 200, 50];\n// 0 ด้านหลังคือค่าเริ่มต้นของตัวสะสม (total)\nconst sum = cart.reduce((total, item) =&gt; total + item, 0); // 400</code></pre><h2>ตัวช่วยอื่นๆ ที่มีประโยชน์ไม่แพ้กัน</h2><ul><li><strong>forEach()</strong>: วนลูปเพื่อ "ทำ" อะไรบางอย่างเท่านั้น ไม่ได้สร้าง Array ใหม่กลับมา (ไม่มีค่า return) เหมาะกับการสั่งปริ้นท์ข้อความ หรือสั่งอัปเดตหน้าจอ DOM</li><li><strong>find()</strong>: เหมือน filter แต่แทนที่จะหาทั้งหมด มันจะคืนค่า "สมาชิกตัวแรกสุด" ตัวเดียวที่ตรงเงื่อนไข แล้วหยุดลูปทันที</li><li><strong>findIndex()</strong>: เหมือน find แต่คืนค่าเป็นตำแหน่ง (Index) แทนตัวข้อมูล</li><li><strong>some()</strong>: ทดสอบว่ามีใครในกลุ่ม <strong>อย่างน้อยหนึ่งคน</strong> สอบผ่านเงื่อนไขหรือไม่? (คืนค่า Boolean)</li><li><strong>every()</strong>: ทดสอบว่า <strong>ทุกคน</strong> ในกลุ่ม สอบผ่านเงื่อนไขหรือไม่? (คืนค่า Boolean)</li></ul><div class="tip-box"><strong>💡 Method Chaining</strong>: พลังที่แท้จริงคือการนำมันมาร้อยเรียงกัน! เช่น <code>users.filter(u =&gt; u.age &gt; 18).map(u =&gt; u.name).forEach(name =&gt; console.log(name))</code> เรียงยาวได้ในสเตปเดียว!</div>'
  },
  {
    number: 8,
    slug: 'objects',
    emoji: '📦',
    title: 'ออบเจกต์ (Objects)',
    content: '<h2>ไขความลับของ Object</h2><p>ถ้า Array คือการเรียงแถวข้อมูลเป็นคิว Object ก็คือตู้ลิ้นชักสารพัดนึกที่มีป้ายแปะหน้าลิ้นชักบอกว่าข้างในคืออะไร เราเรียกโครงสร้างนี้ว่า <strong>Key-Value Pair (คู่ของกุญแจและข้อมูล)</strong> Object เป็นหัวใจของ JavaScript แทบทุกอย่างใน JS จะถูกมองเป็น Object ได้ทั้งหมด การเก็บข้อมูลเป็น Object มีข้อดีคือการเข้าถึงข้อมูลรวดเร็ว และเข้าใจความหมายได้ทันทีผ่านชื่อ Key</p><h2>การสร้าง Object แบบ Literal และการเข้าถึง</h2><p>วิธีสร้าง Object ที่ง่ายที่สุดคือใช้ปีกกา <code>{}</code> ส่วนการอ่านและการเขียนข้อมูลเราทำได้ 2 วิธียอดฮิต:</p><ol style="padding-left:20px"><li><strong>Dot Notation (สัญกรณ์จุด) <code>.</code></strong>: นิยมใช้มากที่สุด โค้ดดูสะอาดตา เช่น <code>person.name</code> หรือ <code>person.age = 30</code> ข้อจำกัดคือชื่อ Key ต้องไม่มีเว้นวรรค และห้ามขึ้นต้นด้วยตัวเลข</li><li><strong>Bracket Notation (สัญกรณ์วงเล็บเหลี่ยม) <code>[]</code></strong>: ใช้ในกรณีที่ชื่อ Key เป็นคำที่มีเว้นวรรค อักขระพิเศษ หรือกรณีที่ล้ำหน้าที่สุดคือ <strong>Dynamic Key</strong> (การนำค่าจากตัวแปรมาใช้แทนชื่อ Key)</li></ol><pre><code>const laptop = {\n  brand: "Apple",\n  model: "MacBook Pro",\n  "screen size": 14\n};\n\nconsole.log(laptop.brand); // Dot notation: "Apple"\nconsole.log(laptop["screen size"]); // Bracket จำเป็นเมื่อมีช่องว่าง\n\nconst keyName = "model";\nconsole.log(laptop[keyName]); // Dynamic: ไปดึง "MacBook Pro" มา\n\n// เพิ่ม Property ใหม่ได้ดื้อๆ เลย\nlaptop.color = "Space Gray";</code></pre><h2>Methods และปีศาจที่ชื่อว่า <code>this</code></h2><p>Object ไม่ได้เก็บแค่ข้อความหรือตัวเลข แต่ยังเก็บ "ฟังก์ชัน" ไว้ข้างในได้ด้วย เราเรียกฟังก์ชันที่อยู่ใน Object ว่า <strong>Method</strong> (เปรียบเหมือนพฤติกรรมที่ Object นั้นๆ สามารถทำได้) เมื่อ Method ต้องอ้างอิงถึงข้อมูลอื่นๆ ใน Object เดียวกัน เราจะใช้คีย์เวิร์ดศักดิ์สิทธิ์ที่ชื่อว่า <code>this</code> (ตัวฉันเอง)</p><pre><code>const player = {\n  username: "Zelda",\n  score: 100,\n  playGame: function() {\n    // this อ้างอิงถึง Object "player"\n    this.score += 10;\n    console.log(`${this.username} now has ${this.score} points.`);\n  }\n};\nplayer.playGame(); // "Zelda now has 110 points."</code></pre><h2>Object Destructuring (การชำแหละออบเจกต์)</h2><p>เป็นฟีเจอร์เด็ดจากยุค ES6 ช่วยให้คุณประหยัดเวลาพิมพ์โค้ดได้เป็นชั่วโมงๆ แทนที่จะต้อง <code>const x = obj.x; const y = obj.y;</code> คุณสามารถแกะข้อมูลที่ต้องการออกมาประกาศเป็นตัวแปรใหม่ได้ในบรรทัดเดียว นิยมใช้มากตอนดึงข้อมูลจาก API</p><pre><code>const weather = { city: "Bangkok", temp: 35, condition: "Sunny" };\n// ระบุชื่อ Key ที่ต้องการในปีกกาด้านซ้าย\nconst { city, temp } = weather;\nconsole.log(`Today in ${city} is ${temp} degrees.`);</code></pre><h2>Rest Operator ใน Object</h2><p>บ่อยครั้งที่เราต้องการดึงแค่บาง Property ออกมา แล้วจับเอาส่วนที่เหลือ (Rest) มัดรวมกันเป็น Object ก้อนใหม่ เราสามารถใช้จุดสามจุด <code>...</code> ที่ฝั่งซ้ายของสมการได้</p><pre><code>const config = { host: "localhost", port: 8080, debug: true, ssl: false };\nconst { host, port, ...otherConfigs } = config;\n// host ได้ "localhost"\n// otherConfigs ได้ออบเจกต์ { debug: true, ssl: false } ไปใช้งานต่อ</code></pre>'
  },
  {
    number: 9,
    slug: 'dom',
    emoji: '🌲',
    title: 'การจัดการ DOM (DOM Manipulation)',
    content: '<h2>DOM ต้นไม้เวทมนตร์ (Document Object Model)</h2><p>เมื่อเบราว์เซอร์ดาวน์โหลดไฟล์ HTML ของคุณมาเปิด มันไม่ได้มองโค้ดเหล่านั้นเป็นเพียงตัวหนังสือ แต่มันแปลงโครงสร้างแท็กทั้งหมด (html, head, body, div, p) ให้กลายเป็น <strong>Object ต้นไม้</strong> ที่มีความสัมพันธ์แบบ พ่อ-แม่-ลูก (Parent-Child) เราเรียกสถาปัตยกรรมนี้ว่า <strong>DOM</strong> ซึ่ง JavaScript ได้รับพลังพิเศษจากเบราว์เซอร์ให้สามารถปีนป่ายต้นไม้นี้เพื่อเข้าถึง แก้ไข เพิ่ม หรือลบกิ่งก้าน (Elements) ได้ตามใจชอบแบบ Real-time</p><h2>การตามล่าหา Elements (Selectors)</h2><p>ก่อนจะลงมือเสกเวทมนตร์ เราต้องหาไม้กายสิทธิ์ของเราให้เจอก่อน (หา Element ที่ต้องการ) ในอดีตเราใช้ <code>getElementById</code> หรือ <code>getElementsByClassName</code> แต่ยุคปัจจุบันทุกคนหันมาใช้คำสั่งที่ทรงพลังครอบจักรวาลคือ <code>querySelector</code></p><pre><code>// ใช้ CSS Selectors ได้ครบถ้วน (., #, [], :hover ฯลฯ)\nconst header = document.querySelector("#main-title");\nconst navItem = document.querySelector(".nav-list &gt; li.active");\n\n// หากต้องการเลือกทั้งหมดที่ตรงเงื่อนไข จะได้กลับมาเป็น NodeList (คล้าย Array)\nconst allButtons = document.querySelectorAll("button");\nallButtons.forEach(btn =&gt; btn.style.borderRadius = "8px");</code></pre><h2>การแปลงโฉม: ข้อความและโครงสร้าง HTML</h2><p>เมื่อจับตัว Element ได้แล้ว เราสามารถแก้ไขเนื้อหาข้างในได้ 2 ท่าหลักๆ:</p><ul><li><strong>textContent</strong>: ใช้อ่านหรือเขียนเฉพาะ "ข้อความล้วน" ไม่มี HTML ผสม วิธีนี้ประสิทธิภาพสูงและปลอดภัยจากการถูกโจมตีแบบ XSS (Cross-Site Scripting)</li><li><strong>innerHTML</strong>: ใช้เมื่อต้องการฝังโครงสร้าง HTML เข้าไป เช่น สร้างตัวหนา ยัดลิงก์ หรือสร้างตาราง โปรดระวังอย่านำข้อมูลที่ผู้ใช้พิมพ์กรอกมาเข้าสู่ innerHTML ตรงๆ เพราะอาจเป็นโค้ดแฮกเกอร์ฝังมา</li></ul><pre><code>const box = document.querySelector(".box");\nbox.textContent = "สวัสดีคุณผู้ชม";\nbox.innerHTML = "มาดู &lt;b&gt;ความสามารถ&lt;/b&gt; นี้สิ!";</code></pre><h2>การควบคุมความงาม (Styles และ Classes)</h2><p>แม้คุณจะสามารถอัด CSS ตรงๆ ผ่าน <code>element.style.color = "red"</code> ได้ แต่นั่นถือเป็น Bad Practice ในโปรเจกต์ขนาดใหญ่ วิธีที่เหล่ามือโปรใช้คือการเตรียม CSS Class ไว้ล่วงหน้าในไฟล์ .css แล้วใช้ JavaScript ควบคุมการเปิด/ปิด Class ผ่าน API ที่ชื่อว่า <code>classList</code></p><pre><code>const card = document.querySelector(".card");\ncard.classList.add("fade-in");    // เพิ่มคลาส\ncard.classList.remove("hidden");  // ลบคลาส\ncard.classList.toggle("active");  // สวิตช์: ถ้ามีให้เอาออก ถ้าไม่มีให้เติม</code></pre><h2>การเสกสร้างและการทำลาย (Create and Remove Elements)</h2><p>เราสามารถสร้างแท็กใหม่เอี่ยมจากความว่างเปล่า ปั้นแต่งมัน แล้วค่อยยัดมันเข้าไปในหน้าเว็บได้ และแน่นอน สามารถเตะมันทิ้งได้เช่นกัน</p><pre><code>// 1. สร้างแท็กใหม่ (ยังลอยเคว้งอยู่ในอากาศ ไม่แสดงบนจอ)\nconst newBanner = document.createElement("div");\nnewBanner.classList.add("promo-banner");\nnewBanner.textContent = "ลดราคา 50% วันนี้เท่านั้น!";\n\n// 2. ระบุตำแหน่งและต่อเชื่อมเข้าไป (Append)\ndocument.body.appendChild(newBanner); // นำไปต่อท้ายสุดของ body\n\n// 3. การลบออก\n// newBanner.remove(); // ลบตัวเองทิ้ง</code></pre><div class="tip-box"><strong>💡 ประสิทธิภาพ (Performance Tip)</strong>: การไปยุ่งกับ DOM คือสิ่งที่ช้าที่สุดที่ JS ทำ (แพงมากทางหน่วยความจำ) หากคุณต้องเพิ่ม <code>&lt;li&gt;</code> 1,000 อัน อย่า <code>appendChild</code> 1,000 รอบ ให้ต่อ HTML ให้เสร็จใน JS ก่อนเป็น String แล้วค่อย <code>innerHTML</code> รวดเดียว หรือใช้สิ่งที่เรียกว่า DocumentFragment</div>'
  },
  {
    number: 10,
    slug: 'events',
    emoji: '🖱️',
    title: 'การรับฟังอีเวนต์ (Event Listeners)',
    content: '<h2>Event คือหัวใจของความ Interactive</h2><p>ถ้าไม่มี Event หน้าเว็บก็เป็นเพียงแค่โปสเตอร์ที่นิ่งงัน Event (เหตุการณ์) คือระบบที่เบราว์เซอร์ร้องตะโกนบอกแอปพลิเคชันว่า "เฮ้! มีบางอย่างเกิดขึ้น" เช่น ผู้ใช้คลิกเมาส์ พิมพ์คีย์บอร์ด เลื่อนสกอร์ลบาร์ ย่อหน้าต่าง โหลดภาพเสร็จ หรือเน็ตหลุด หน้าที่ของ JavaScript คือการฝังสายลับที่เรียกว่า <strong>Event Listener</strong> ไปยืนเกาะติด Element เหล่านั้น เมื่อเกิดเหตุการณ์ปุ๊บ สายลับจะส่งสัญญาณให้ฟังก์ชัน (Callback) ทำงานทันที</p><h2>addEventListener() ศิลปะแห่งการดักจับ</h2><p>นี่คือคำสั่งพระเอกสำหรับการจัดการเหตุการณ์ โครงสร้างของมันรับ 2 พารามิเตอร์คือ <code>(ชื่ออีเวนต์, ฟังก์ชันที่จะให้รัน)</code></p><pre><code>const loginBtn = document.querySelector("#login");\n\n// ดักจับการคลิก\nloginBtn.addEventListener("click", function() {\n  console.log("พยายามเข้าสู่ระบบ...");\n  // เปลี่ยนปุ่มเป็นสถานะโหลด\n  loginBtn.textContent = "กำลังโหลด...";\n  loginBtn.disabled = true;\n});</code></pre><h2>Event Object (อ็อบเจกต์รายงานข่าว)</h2><p>เมื่อเหตุการณ์เกิดขึ้น เบราว์เซอร์ไม่ได้แค่เรียกฟังก์ชันเฉยๆ แต่มันจะแถม <strong>Event Object</strong> โยนใส่มาในพารามิเตอร์ตัวแรกของฟังก์ชันเราด้วย (คนมักตั้งชื่อย่อว่า <code>e</code> หรือ <code>event</code> หรือ <code>evt</code>) อ็อบเจกต์นี้อัดแน่นไปด้วยข้อมูลสำคัญ เช่น เมาส์คลิกที่พิกัด x, y เท่าไหร่? พิมพ์ปุ่มไหนบนคีย์บอร์ด? หรือใครคือคนที่ถูกคลิกจริงๆ (target)</p><pre><code>const searchBox = document.querySelector("#search");\n// รับฟังตอนปุ่มคีย์บอร์ดเด้งขึ้นมา\nsearchBox.addEventListener("keyup", (e) =&gt; {\n  if (e.key === "Enter") {\n    console.log("ค้นหาคำว่า:", e.target.value);\n  }\n});</code></pre><h2>preventDefault(): หยุดพฤติกรรมดั้งเดิม</h2><p>HTML มีพฤติกรรมดั้งเดิม (Default Action) ติดตัวมา เช่น แท็ก <code>&lt;a&gt;</code> จะวิ่งเปลี่ยนหน้า หรือแท็ก <code>&lt;form&gt;</code> เมื่อกด Submit มันจะพยายามโหลดหน้ารีเฟรชตัวเอง ในยุคของเว็บแอปพลิเคชันสมัยใหม่ (SPA) เราต้องสกัดกั้นพฤติกรรมเหล่านี้เสียก่อน ด้วยคำสั่ง <code>e.preventDefault()</code> แล้วค่อยให้ JS ไปยิง API ส่งข้อมูลแทนหน้าจอจะได้ไม่กระพริบ</p><pre><code>const form = document.querySelector(".contact-form");\nform.addEventListener("submit", (e) =&gt; {\n  e.preventDefault(); // เบรกเอี๊ยด! ห้ามรีเฟรชหน้านะ\n  console.log("กำลังส่งข้อมูลด้วย JS (AJAX/Fetch)...");\n});</code></pre><h2>Event Bubbling (ปรากฏการณ์ฟองอากาศ)</h2><p>เมื่อคุณคลิกแท็ก <code>&lt;span&gt;</code> ที่อยู่ข้างใน <code>&lt;button&gt;</code> ที่อยู่ใน <code>&lt;div&gt;</code> อีกที เบราว์เซอร์จะประมวลผลว่าคุณคลิกทั้ง 3 อย่าง! โดยเหตุการณ์จะจุดประกายจากจุดในสุด (span) แล้วลอยบุ๋งๆ ทะลุชั้นพ่อแม่ขึ้นไปเรื่อยๆ จนถึงระดับบนสุด (document) เราเรียกสิ่งนี้ว่า <strong>Bubbling</strong> ซึ่งหากต้องการหยุดฟองอากาศนี้ไม่ให้ทะลุไปโดนคนอื่น สามารถสั่ง <code>e.stopPropagation()</code> ได้</p><div class="tip-box"><strong>💡 Event Delegation (เทคนิคฝากเลี้ยง)</strong>: เราใช้ประโยชน์จาก Bubbling ในการลดปริมาณโค้ด สมมติเรามีรายการสินค้า &lt;li&gt; 50 ชิ้น แทนที่เราจะไปแปะ Event ให้ปุ่มใน &lt;li&gt; ทั้ง 50 อัน (เปลืองเมมโมรี) เราแปะ Event ที่ &lt;ul&gt; (แม่) ตัวเดียวพอ เมื่อลูกถูกคลิก ฟองอากาศจะลอยมาหาแม่ แล้วแม่ค่อยเช็คว่า <code>e.target</code> คือใคร เทคนิคนี้โปรแกรมเมอร์ซีเนียร์ใช้กันทุกคน!</div>'
  },
  {
    number: 11,
    slug: 'errors',
    emoji: '🚨',
    title: 'การจัดการข้อผิดพลาด (Error Handling)',
    content: '<h2>ทำไมโปรแกรมเมอร์ที่เก่งถึงหลงรัก Error?</h2><p>สำหรับมือใหม่ สีแดงเต็มคอนโซลคือฝันร้าย แต่ความจริงแล้ว Error คือ "ระบบรักษาความปลอดภัย" ที่คอยเตือนว่ามีบางอย่างผิดปกติ ลองนึกภาพถ้าโปรแกรมทำงานผิดพลาดแต่ดันรันต่อไปเงียบๆ (Silent Failure) จะเกิดความพินาศของข้อมูลมากแค่ไหน อย่างไรก็ตาม การปล่อยให้ Error ระเบิดกลางแอปจะทำให้แอปหยุดทำงาน (Crash) ผู้ใช้จะทำอะไรต่อไม่ได้ เราจึงต้องมีศิลปะในการ <strong>"ดักจับและจัดการ" (Catching)</strong> เพื่อให้โปรแกรมฟื้นตัวและแจ้งเตือนผู้ใช้อย่างสุภาพ</p><h2>โครงสร้างปราการป้องกัน: try...catch...finally</h2><p>นี่คือโครงสร้างมาตรฐานในการจัดการโค้ดที่มีความเสี่ยงสูง (เช่น การแปลง JSON, การดึงข้อมูลอินเทอร์เน็ต)</p><ol style="padding-left:20px"><li><strong>try</strong>: นำโค้ดที่ "สุ่มเสี่ยง" ไปใส่ในบล็อกนี้ JS จะพยายามรันโค้ด หากรอดตลอดรอดฝั่ง ก็จะทำงานตามปกติและข้าม catch ไป</li><li><strong>catch (error)</strong>: หากเกิด Error ที่บรรทัดใดบรรทัดหนึ่งใน try การทำงานจะหยุดรันบรรทัดถัดไปทันที และกระโดดทะลุมิติมาที่ catch โดยจะพกอ็อบเจกต์ error มาให้เราวิเคราะห์ด้วย เราใช้จุดนี้ในการโชว์ Popup เตือนผู้ใช้</li><li><strong>finally</strong>: (มีหรือไม่มีก็ได้) โค้ดในบล็อกนี้จะ <strong>ถูกรันเสมอ 100%</strong> ไม่ว่าจะสำเร็จ (try) หรือพังพินาศ (catch) มักใช้ทำความสะอาดระบบ เช่น ปิดวงล้อหมุนโหลดติ้ง (Loading Spinner) หรือปิดการเชื่อมต่อฐานข้อมูล</li></ol><pre><code>function processUserData(jsonString) {\n  try {\n    console.log("กำลังประมวลผล...");\n    // หาก jsonString ผิดรูปแบบ บรรทัดนี้จะระเบิดตูม!\n    const user = JSON.parse(jsonString);\n    \n    // ถ้าระเบิด บรรทัดนี้จะไม่มีวันได้รัน\n    console.log("ชื่อผู้ใช้คือ:", user.name);\n  } catch (error) {\n    // ควบคุมสติและแจ้งเตือน\n    console.error("เกิดปัญหาในการอ่านข้อมูล: ", error.message);\n    alert("ขออภัย ข้อมูลเซิร์ฟเวอร์ผิดปกติ กรุณาลองใหม่");\n  } finally {\n    console.log("เสร็จสิ้นกระบวนการทั้งหมด ปิดระบบโหลด");\n  }\n}</code></pre><h2>การโยนระเบิดด้วยตัวเอง (Throwing Errors)</h2><p>ไม่ใช่ทุกความผิดพลาดจะเกิดจากไวยากรณ์ภาษา บางครั้งมันผิดพลาดทางลอจิกธุรกิจ (Business Logic Error) เช่น ถอนเงินเกินยอดที่มี รหัสผ่านสั้นเกินไป ซึ่ง JS ไม่มองว่าเป็น Error เราต้องเป็นคนสร้าง Error และขว้าง (throw) มันออกไปเอง เพื่อให้ระบบ <code>try...catch</code> เข้ามาจัดการ</p><pre><code>function loginUser(username, password) {\n  if (!username || !password) {\n    // สร้างข้อผิดพลาดขึ้นมาเอง และโยนออกไป\n    throw new Error("กรุณากรอกข้อมูลให้ครบถ้วน!");\n  }\n  if (password.length &lt; 6) {\n    throw new Error("รหัสผ่านต้องยาวกว่า 6 ตัวอักษร");\n  }\n  return "เข้าสู่ระบบสำเร็จ!";\n}\n\ntry {\n  loginUser("admin", "123");\n} catch (err) {\n  // ดักจับ Error ที่เราขว้างมาเอง\n  document.querySelector(".error-msg").textContent = err.message;\n}</code></pre><div class="tip-box"><strong>💡 Custom Error Classes</strong>: ในสเกลระดับ Enterprise นักพัฒนามักจะขยายคลาสพื้นฐานเพื่อแยกประเภท Error เช่น สร้าง <code>class DatabaseError extends Error {}</code> เพื่อให้แยกแยะใน catch ได้ว่าพังเพราะเน็ต หรือพังเพราะกรอกข้อมูลผิด ผ่านเช็คแบบ <code>if (err instanceof DatabaseError)</code></div>'
  },
  {
    number: 12,
    slug: 'async',
    emoji: '⏳',
    title: 'JavaScript แบบอะซิงโครนัส (Asynchronous JavaScript)',
    content: '<h2>ธรรมชาติของ JS (Single-Threaded)</h2><p>จินตนาการว่า JavaScript เป็นร้านอาหารที่มี "พนักงานเสิร์ฟเพียงคนเดียว" (Single Thread) พนักงานคนนี้ต้องรับออเดอร์ เสิร์ฟน้ำ คิดเงิน ทุกอย่างทำแบบ <strong>Synchronous</strong> คือทำตามลำดับคิวเป๊ะๆ ปัญหาจะเกิดเมื่อมีลูกค้าสั่ง "สเต็กเนื้ออบ 1 ชั่วโมง" ถ้าพนักงานคนนี้ไปยืนเฝ้าเตาอบ (Blocking Task) ลูกค้าโต๊ะอื่นก็จะโวยวายเพราะไม่มีใครดูแล (หน้าเว็บค้าง Freeze กดอะไรไม่ได้)</p><p>ทางออกคือ <strong>Asynchronous (อะซิงโครนัส)</strong> พนักงานเสิร์ฟรับออเดอร์สเต็ก ส่งเข้าครัว (ให้เบราว์เซอร์จัดการอยู่เบื้องหลัง) แล้วหมุนตัวไปรับออเดอร์โต๊ะอื่นต่อทันที เมื่อสเต็กสุก พ่อครัวจะสั่นกระดิ่งเรียก (Event Loop) ให้พนักงานกลับมาเสิร์ฟ โค้ดสามารถวิ่งต่อไปโดยไม่ต้องหยุดรอ!</p><h2>ยุคมืดของ Callback (Callback Hell)</h2><p>ในช่วงแรกเริ่ม เราจัดการ Asynchronous ด้วย Callback (การส่งฟังก์ชันไปให้เรียกกลับเมื่อเสร็จงาน) สมมติเราต้องทำงานต่อเนื่องกัน 1.ดึงข้อมูลผู้ใช้ 2.ดึงรายการสินค้า 3.สั่งจ่ายเงิน โค้ดเราจะบุ๋มลึกลงไปเรื่อยๆ เป็นรูปทรงพีระมิดนรก (Pyramid of Doom)</p><pre><code>// โค้ดที่ดูแลรักษายากมาก\ngetUser(userId, function(user) {\n  getCart(user.cartId, function(cart) {\n    processPayment(cart.total, function(receipt) {\n      sendEmail(receipt, function(success) {\n         console.log("เสร็จสิ้นซักที!");\n      });\n    });\n  });\n});</code></pre><h2>วีรบุรุษขี่ม้าขาว: Promises (คำสัญญา)</h2><p>เพื่อกอบกู้โครงสร้างโค้ด ES6 ได้เปิดตัวโครงสร้าง <strong>Promise</strong> มันคืออ็อบเจกต์ที่เป็นตัวแทนของ "ผลลัพธ์ในอนาคต" ที่ตอนนี้ยังไม่มี แต่ให้สัญญาว่าจะเอามาให้ (คล้ายกับใบเสร็จคิวรับกาแฟ) Promise มี 3 สถานะที่ชัดเจน:</p><ul><li><strong>Pending (รอคอย)</strong>: เพิ่งส่งเข้าครัว ยังทำอาหารอยู่</li><li><strong>Fulfilled / Resolved (สำเร็จ)</strong>: ทำอาหารเสร็จ พร้อมเสิร์ฟ</li><li><strong>Rejected (ล้มเหลว)</strong>: วัตถุดิบหมด หรือทำไฟไหม้กระทะ (เกิด Error)</li></ul><h2>การแกะกล่องคำสัญญา (.then และ .catch)</h2><p>เมื่อฟังก์ชันคืนค่ามาเป็น Promise เราจะไม่สามารถนำไปใส่ตัวแปรตรงๆ ได้ (เพราะข้อมูลยังไม่มา) เราต้องต่อแถวด้วย Method <code>.then()</code> เพื่อรอรับผลสำเร็จ และร้อย <code>.catch()</code> เพื่อดักข้อผิดพลาดที่อาจเกิดตรงจุดไหนก็ได้ในสายโซ่</p><pre><code>// โค้ดดูเป็นระเบียบขึ้นแบนราบลง\nfetchUserData(userId)\n  .then(user =&gt; fetchCartData(user.cartId)) // ส่งต่อผลลัพธ์ลงไปเรื่อยๆ (Chaining)\n  .then(cart =&gt; processPayment(cart.total))\n  .then(receipt =&gt; console.log("ใบเสร็จ: ", receipt))\n  .catch(error =&gt; console.error("เกิดปัญหาในกระบวนการ: ", error));</code></pre>'
  },
  {
    number: 13,
    slug: 'async-await',
    emoji: '🚀',
    title: 'การใช้ Async/Await (Async/Await)',
    content: '<h2>Async/Await: เมื่อโค้ดแห่งอนาคตมาถึง</h2><p>แม้ว่า Promise และ <code>.then()</code> จะช่วยแก้ปัญหา Callback Hell ให้โค้ดแบนราบลงได้ แต่ก็ปฏิเสธไม่ได้ว่าการต้องเขียน Callback function ใส่เข้าไปใน <code>.then()</code> ตลอดเวลายังคงดูรกสายตาและขัดกับสัญชาตญาณการเขียนโค้ดของมนุษย์ที่ชอบอ่านจากบนลงล่างทีละบรรทัด</p><p>ในปี 2017 (ES8) โลกของ JavaScript ได้รู้จักกับ <strong>Async/Await</strong> ซึ่งเป็นเพียง Syntactic Sugar (น้ำตาลเคลือบยา) ที่ไปครอบการทำงานของ Promise อีกชั้นหนึ่ง เพื่อปลอมแปลงและทำให้โค้ดแบบ Asynchronous ดูเรียบง่ายเหมือนเขียนโค้ดแบบ Synchronous ธรรมดาเป๊ะ!</p><h2>กฎสองข้อของการใช้ Async / Await</h2><ol style="padding-left:20px"><li><strong>กฎข้อที่ 1 (async)</strong>: คุณไม่สามารถใช้คำสั่ง <code>await</code> ลอยๆ ได้ คุณต้องติดป้าย <code>async</code> ไว้ที่หน้าฟังก์ชันที่ครอบมันอยู่เสียก่อน การเติมคำว่า async จะทำให้ฟังก์ชันนั้น <strong>คืนค่ากลับมาเป็น Promise เสมอโดยอัตโนมัติ</strong></li><li><strong>กฎข้อที่ 2 (await)</strong>: เมื่อนำคำว่า <code>await</code> ไปวางหน้าฟังก์ชันที่เป็น Promise (เช่น fetch) มันจะสั่งให้ JavaScript "แช่แข็ง / หยุดรอชั่วคราว" ที่บรรทัดนั้น จนกว่า Promise จะทำงานเสร็จเรียบร้อย แล้วมันค่อยคายผลลัพธ์ใส่ตัวแปร แล้วจึงค่อยขยับไปทำบรรทัดล่างต่อไป (หมายเหตุ: หยุดรอเฉพาะในบริบทของฟังก์ชันนี้เท่านั้น ตัว Main Thread ของแอปไม่ได้หยุดค้าง ผู้ใช้ยังกดปุ่มอื่นๆ ได้ปกติ)</li></ol><pre><code>// เปรียบเทียบความสวยงาม\n// แบบ Promise Chaining ดั้งเดิม\nfunction displayProfile() {\n  fetchUser().then(user =&gt; {\n    fetchPosts(user.id).then(posts =&gt; {\n      console.log("ชื่อผู้ใช้: " + user.name, "มีโพสต์: " + posts.length);\n    });\n  });\n}\n\n// แบบ Async / Await สมัยใหม่ (สะอาดและอ่านง่ายมาก)\nasync function displayProfileAsync() {\n  // พักรอกระทั่งได้ user\n  const user = await fetchUser();\n  // นำ user.id มาพักรอกระทั่งได้ posts\n  const posts = await fetchPosts(user.id);\n  console.log("ชื่อผู้ใช้: " + user.name, "มีโพสต์: " + posts.length);\n}</code></pre><h2>การจับผู้ร้ายด้วย Try/Catch</h2><p>เนื่องจากโค้ดดูเป็นเส้นตรงแล้ว เราจึงไม่สามารถใช้ <code>.catch()</code> ต่อท้ายได้อีกต่อไป วิธีการรองรับ Error แบบฉบับของ <code>await</code> คือการห่อหุ้มโซนอันตรายด้วยบล็อก <code>try...catch</code> ตามมาตรฐานภาษา</p><pre><code>async function safeFetch() {\n  try {\n    const response = await fetch("https://api.fake.com/broken-link");\n    const data = await response.json();\n    console.log("ได้ข้อมูลแล้ว", data);\n  } catch (error) {\n    // หากเน็ตหลุด จะถูกเด้งมาที่นี่ทันที\n    console.error("โอ๊ะโอ! เกิดข้อผิดพลาดด้านเครือข่าย", error);\n  }\n}</code></pre><h2>วิ่งแบบคู่ขนานขีดสุดด้วย Promise.all()</h2><p>จุดอ่อนร้ายแรงที่มือใหม่มักพลาดคือการใส่ <code>await</code> ซ้อนกันหลายๆ บรรทัดกับของที่ไม่เกี่ยวข้องกันเลย สมมติโหลดโฆษณา 2 วินาที, โหลดสินค้า 2 วินาที, โหลดพยากรณ์อากาศ 2 วินาที หากคุณเขียน <code>await</code> ทีละบรรทัด แอปคุณจะกินเวลาโหลดนานถึง 2+2+2 = 6 วินาที! (Sequential Execution)</p><p>ทางแก้คือส่งออกไปวิ่งพร้อมๆ กัน (Parallel) แล้วรอให้ทั้ง 3 ตัวเข้าเส้นชัยค่อยประมวลผล ใช้เวลาแค่ 2 วินาที! โดยใช้ฟังก์ชันระดับเทพอย่าง <strong>Promise.all()</strong></p><pre><code>async function parallelLoad() {\n  // เริ่มยิงจรวดพร้อมกัน 3 ลูก จับใส่ Array แล้วรอรวดเดียว\n  const [ads, products, weather] = await Promise.all([\n    fetchAds(),\n    fetchProducts(),\n    fetchWeather()\n  ]);\n  console.log("ทุกอย่างพร้อมใช้งาน!");\n}</code></pre>'
  },
  {
    number: 14,
    slug: 'fetch',
    emoji: '🌐',
    title: 'การเชื่อมต่อ API ด้วย Fetch API (Fetch API)',
    content: '<h2>หมดยุค AJAX แล้ว ยินดีต้อนรับ Fetch API</h2><p>ในอดีต หากเราต้องการดึงข้อมูลจากอินเทอร์เน็ตโดยไม่เปลี่ยนหน้าเว็บ เราต้องเขียนโค้ดที่ชื่อว่า <code>XMLHttpRequest (XHR)</code> ซึ่งมีไวยากรณ์ที่ยาวเหยียด ยุ่งยาก และปวดหัว เบราว์เซอร์ยุคใหม่จึงได้ผนวกฟังก์ชันทรงพลังที่ชื่อ <strong>Fetch API</strong> มาให้ใช้งานฟรีๆ โดยไม่ต้องลงไลบรารีเพิ่มเลย ความเจ๋งคือมันถูกออกแบบมาบนพื้นฐานของ Promise 100% ทำให้มันทำงานเข้าขากับ Async/Await ได้ราวกับกิ่งทองใบหยก</p><h2>พื้นฐานการดึงข้อมูล: GET Request</h2><p>การขอดึงข้อมูล (GET) เป็นค่าเริ่มต้นของ Fetch เสมอ สิ่งที่ควรรู้คือการดึงข้อมูล 1 ครั้งจะประกอบด้วย 2 ขั้นตอน (2 Promises)</p><ol style="padding-left:20px"><li>ดึง <strong>Response Object</strong>: เป็นการรับข้อมูลส่วนหัว (Headers) เช่น สถานะ HTTP ว่าสำเร็จ 200 ไหม?</li><li>ดึง <strong>Body Content</strong>: ต้องสั่งแยกชิ้นส่วนเพื่อแปลงเนื้อหาที่เซิร์ฟเวอร์ส่งมา ให้อยู่ในรูป Object ของ JS (ผ่านคำสั่ง <code>.json()</code>)</li></ol><pre><code>async function getEmployees() {\n  try {\n    // ขั้นที่ 1: ติดต่อเซิร์ฟเวอร์\n    const response = await fetch("https://dummyapi.com/employees");\n    \n    // ** จุดดักจับ Error ระดับ HTTP **\n    // Fetch จะไม่โยน Error ออกมาแม้เซิร์ฟเวอร์จะตอบ 404 (ไม่พบหน้า) หรือ 500 (เซิร์ฟเวอร์พัง)\n    // เราจึงต้องเช็คคุณสมบัติ .ok ว่าเป็น true ไหม ก่อนจะไปต่อ\n    if (!response.ok) {\n      throw new Error(`คำขอถูกปฏิเสธ สถานะ: ${response.status}`);\n    }\n\n    // ขั้นที่ 2: แกะกล่องข้อมูล JSON\n    const data = await response.json();\n    console.log("รายชื่อพนักงาน:", data);\n  } catch (err) {\n    // Error ตรงนี้มักเกิดจากเน็ตหลุด สายแลนขาด (Network Error) หรือเกิดจากการ Throw ด้านบน\n    console.error("เกิดข้อผิดพลาดในการดึงข้อมูล:", err.message);\n  }\n}</code></pre><h2>ส่งข้อมูลกลับไปบันทึก: POST Request</h2><p>เมื่อต้องการเพิ่มข้อมูลใหม่ เช่น ผู้ใช้กรอกแบบฟอร์มสมัครสมาชิก เราต้องส่งข้อมูลกลับไปหาเซิร์ฟเวอร์ เราต้องยัดกล่องตั้งค่า (Options Object) เข้าไปเป็นพารามิเตอร์ตัวที่ 2 ของ fetch เพื่อเปลี่ยนกระบวนท่าจากการขอ (GET) เป็นการส่ง (POST)</p><pre><code>async function registerUser(userInfo) {\n  try {\n    const response = await fetch("https://dummyapi.com/users", {\n      method: "POST", // เปลี่ยนท่าทางการยิง API\n      headers: {\n        // บอกรปภ.หน้าเซิร์ฟเวอร์ว่า "ฉันกำลังส่งข้อมูลเป็นรูปแบบ JSON ไปนะ"\n        "Content-Type": "application/json", \n        // บางครั้งอาจต้องส่ง Token ยืนยันตัวตนไปพร้อมด้วย\n        // "Authorization": "Bearer token-xxxx"\n      },\n      // แปลงข้อมูล JS Object ให้กลายเป็น String (ตัวอักษร) ก่อนส่งผ่านสายแลน\n      body: JSON.stringify(userInfo) \n    });\n    \n    if (!response.ok) throw new Error("บันทึกข้อมูลไม่สำเร็จ");\n    \n    const responseData = await response.json();\n    console.log("บันทึกสำเร็จ ได้รหัสไอดีใหม่มาคือ:", responseData.id);\n  } catch (err) {\n    alert(err.message);\n  }\n}</code></pre><div class="tip-box"><strong>💡 CORS (Cross-Origin Resource Sharing)</strong>: ปัญหาคลาสสิกของมือใหม่คือยิง API แล้วติด CORS แดงเถือกในคอนโซล โปรดจำไว้ว่านี่ไม่ใช่บั๊กของโค้ดคุณ แต่มันคือระบบรักษาความปลอดภัยของเบราว์เซอร์ที่ไม่ยอมให้เว็บ Domain A แอบดึงข้อมูลจาก Domain B ยกเว้นแต่ว่าเซิร์ฟเวอร์ B จะตั้งค่าอนุญาต (เปิดประตู) ให้เข้าถึงได้</div>'
  },
  {
    number: 15,
    slug: 'project',
    emoji: '✅',
    title: 'โปรเจกต์: สร้าง Todo App (Project: Todo App)',
    content: '<h2>นำทุกความรู้มาร้อยเรียง (The Capstone Project)</h2><p>ถึงเวลาเปลี่ยนทฤษฎีให้เป็นผลิตภัณฑ์จริง! เราจะสร้างแอปพลิเคชัน <strong>"Todo List บันทึกสิ่งที่ต้องทำ"</strong> ที่รวบรวมแก่นของ JavaScript ทั้งหมด: การวางโครงสร้างข้อมูล (Arrays & Objects), การจัดการหน้าจอ (DOM Manipulation), การโต้ตอบกับผู้ใช้ (Event Listeners), ลอจิกการทำงาน (Functions & Control Flow), และการบันทึกข้อมูลถาวรบนเบราว์เซอร์ (LocalStorage)</p><h2>1. โครงร่างเหล็ก (HTML Structure)</h2><p>เราเตรียมกล่องรับข้อมูล (Input) และกล่องแสดงผล (UL List)</p><pre><code>&lt;div class="todo-app"&gt;\n  &lt;h1&gt;เป้าหมายวันนี้ 🚀&lt;/h1&gt;\n  &lt;form id="todo-form"&gt;\n    &lt;input type="text" id="task-input" placeholder="พิมพ์สิ่งที่ต้องทำ..." required&gt;\n    &lt;button type="submit"&gt;เพิ่ม&lt;/button&gt;\n  &lt;/form&gt;\n  &lt;ul id="task-list"&gt;\n    &lt;!-- รายการจะถูกยัดเข้ามาที่นี่ด้วย JS --&gt;\n  &lt;/ul&gt;\n&lt;/div&gt;</code></pre><h2>2. สมองส่วนความจำ (Data State & LocalStorage)</h2><p>หัวใจของแอประดับโปรคือ <strong>"การแยกข้อมูลออกจากหน้าจอ"</strong> (State Management) เราจะเก็บรายการทั้งหมดไว้ใน Array <code>todos</code> และเราจะเขียนตัวช่วยในการเซฟ/โหลด ข้อมูลลงเครื่อง</p><pre><code>// โครงสร้างของข้อมูลจะเป็น: [{ id: 1234, text: "ซื้อนม", completed: false }]\nlet todos = [];\n\n// ฟังก์ชันบันทึกข้อมูล (เซฟลงเครื่องผู้ใช้)\nfunction saveToStorage() {\n  // LocalStorage รับแต่ตัวหนังสือ จึงต้องใช้ JSON.stringify แปลงร่าง Array\n  localStorage.setItem("myTodos", JSON.stringify(todos));\n}\n\n// ฟังก์ชันดึงข้อมูลกลับมาตอนเปิดเว็บ\nfunction loadFromStorage() {\n  const savedData = localStorage.getItem("myTodos");\n  if (savedData) {\n    todos = JSON.parse(savedData); // คืนร่างกลับเป็น Array\n  }\n}</code></pre><h2>3. ฟังก์ชันวาดภาพหน้าจอ (Render Function)</h2><p>ทุกครั้งที่ข้อมูลเปลี่ยน เราจะเรียกฟังก์ชัน <code>render()</code> เพื่อล้างจอเก่าและวาดรายการใหม่ขึ้นมาให้ตรงกับข้อมูล <code>todos</code> ล่าสุดเสมอ</p><pre><code>function render() {\n  const listElement = document.getElementById("task-list");\n  listElement.innerHTML = ""; // ล้างกระดาน\n\n  todos.forEach((todo) =&gt; {\n    // สร้างแท็ก &lt;li&gt;\n    const li = document.createElement("li");\n    // หากงานไหนเสร็จแล้ว ให้ใส่ class "done" (เอาไว้ขีดฆ่าใน CSS)\n    if (todo.completed) li.classList.add("done");\n\n    // ใส่เนื้อหาและปุ่มลบลงไป\n    li.innerHTML = `\n      &lt;span class="task-text"&gt;${todo.text}&lt;/span&gt;\n      &lt;button class="delete-btn" onclick="deleteTask(${todo.id})"&gt;❌&lt;/button&gt;\n    `;\n\n    // ดักจับการคลิกที่ข้อความเพื่อสลับสถานะ เสร็จ/ไม่เสร็จ\n    const spanElement = li.querySelector(".task-text");\n    spanElement.addEventListener("click", () =&gt; toggleTask(todo.id));\n\n    listElement.appendChild(li);\n  });\n}</code></pre><h2>4. ระบบประสาทสั่งการ (Actions / Events)</h2><p>เชื่อมต่อปุ่มกดเข้ากับการปรับแก้ Array</p><pre><code>// เพิ่มงานใหม่\ndocument.getElementById("todo-form").addEventListener("submit", function(e) {\n  e.preventDefault();\n  const input = document.getElementById("task-input");\n  \n  const newTask = {\n    id: Date.now(), // ใช้เวลาปัจจุบันสร้าง ID ไม่ซ้ำกัน\n    text: input.value,\n    completed: false\n  };\n  \n  todos.push(newTask);\n  input.value = ""; // เคลียร์ช่องพิมพ์\n  \n  saveToStorage();\n  render();\n});\n\n// ลบงานทิ้ง\n// กำหนดให้เป็น Global (window.deleteTask) เพื่อให้ปุ่มแบบ onclick เรียกได้\nwindow.deleteTask = function(id) {\n  // ใช้ filter คัดกรองตัวที่ ID ตรงกันทิ้งไป\n  todos = todos.filter(t =&gt; t.id !== id);\n  saveToStorage();\n  render();\n};\n\n// สลับสถานะ\nfunction toggleTask(id) {\n  todos = todos.map(t =&gt; {\n    if (t.id === id) {\n      return { ...t, completed: !t.completed }; // สลับ Boolean\n    }\n    return t;\n  });\n  saveToStorage();\n  render();\n}\n\n// จุดเริ่มต้นโปรแกรม\nloadFromStorage();\nrender();</code></pre><div class="tip-box"><strong>💡 ขอแสดงความยินดี!</strong>: คุณได้เดินผ่านมาถึงจุดสูงสุดของพื้นฐาน JavaScript แล้ว! การสร้าง Todo App คือพิธีกรรมการเริ่มต้น (Rite of Passage) ของนักพัฒนาทุกคน แนะนำให้ลองตกแต่ง CSS เพิ่มเติม หรือท้าทายตัวเองด้วยการเพิ่มฟีเจอร์ "แก้ไขข้อความได้" หรือ "กรองดูเฉพาะงานที่เสร็จแล้ว" แล้วคุณจะเก่งขึ้นอย่างก้าวกระโดด!</div>'
  }
];
