export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '📊',
    title: 'ภาพรวมของ State Management (Overview)',
    content: '<h2>State ใน Flutter คืออะไร?</h2><p>ในการพัฒนาแอปพลิเคชันด้วย <strong>Flutter</strong> สิ่งที่สำคัญที่สุดอย่างหนึ่งคือความเข้าใจในเรื่องของ <strong>State</strong> หรือสถานะของแอปพลิเคชัน ในความหมายที่ง่ายที่สุด State คือข้อมูลทุกอย่างที่ UI ของแอปต้องการเพื่อแสดงผลบนหน้าจอ ณ ช่วงเวลาใดเวลาหนึ่ง ไม่ว่าจะเป็นค่าตัวแปร สถานะการโหลดข้อมูล ข้อมูลที่ดึงมาจาก API หรือแม้กระทั่งสถานะของ Checkbox ว่าถูกติ๊กอยู่หรือไม่</p><p>Flutter ถูกออกแบบมาบนแนวคิดของ <strong>Declarative UI</strong> ซึ่งหมายความว่า UI ของคุณจะถูกสร้างขึ้นมาใหม่ตาม State ปัจจุบันเสมอ (UI = f(State)) ดังนั้นเมื่อ State เปลี่ยนแปลง Flutter จะทำการวาด UI ใหม่ (Rebuild) ให้สอดคล้องกับข้อมูลใหม่โดยอัตโนมัติ</p><h2>Ephemeral State vs App State</h2><p>ในการจัดการ State เราสามารถแบ่งประเภทของ State ออกเป็น 2 ประเภทหลักๆ เพื่อให้ง่ายต่อการเลือกเครื่องมือมาจัดการ ได้แก่ <strong>Ephemeral State</strong> และ <strong>App State</strong></p><h3>1. Ephemeral State (Local State)</h3><p>Ephemeral State หรือ Local State คือสถานะที่มีความเกี่ยวข้องกับ Widget เพียงตัวเดียว และไม่จำเป็นต้องแชร์ข้อมูลนี้ให้กับ Widget อื่นๆ ในแอปพลิเคชัน ตัวอย่างที่เห็นได้ชัดเช่น สถานะการทำงานของ Animation, ตำแหน่งปัจจุบันของ PageView หรือสถานะของ BottomNavigationBar ว่าผู้ใช้กำลังเลือก Tab ใดอยู่</p><p>สำหรับการจัดการ Ephemeral State เราสามารถใช้ <strong>StatefulWidget</strong> และ <code>setState()</code> ได้อย่างเพียงพอ โดยไม่ต้องพึ่งพา State Management Library ใดๆ</p><h3>2. App State (Global/Shared State)</h3><p>App State คือสถานะที่ต้องถูกแชร์และใช้งานร่วมกันในหลายๆ ส่วนของแอปพลิเคชัน ตัวอย่างเช่น ข้อมูลผู้ใช้งานที่เข้าสู่ระบบ (User Session), ข้อมูลในตะกร้าสินค้า (Shopping Cart), การตั้งค่า Theme ของแอป หรือสถานะการแจ้งเตือน (Notifications)</p><p>การจัดการ App State ด้วย <code>setState()</code> เพียงอย่างเดียวจะทำให้โค้ดมีความซับซ้อนมาก เพราะคุณจะต้องส่งผ่านข้อมูลผ่าน Constructor ของ Widget ซ้อนทับกันหลายชั้น (Prop Drilling) นี่คือจุดที่เราจำเป็นต้องนำ <strong>State Management</strong> มาใช้งาน</p><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ไม่มีกฎตายตัวว่าต้องใช้ App State กับทุกอย่าง บางครั้งการใช้แค่ Ephemeral State ผ่าน <code>setState()</code> ก็เพียงพอและทำให้โค้ดอ่านง่ายกว่า จงเลือกใช้ให้เหมาะสมกับขนาดและความซับซ้อนของข้อมูล</div><h2>ทำไมเราถึงต้องมี State Management?</h2><p>เมื่อแอปพลิเคชันเริ่มมีขนาดใหญ่ขึ้นเรื่อยๆ Widget Tree ของคุณจะเริ่มลึกและซับซ้อนขึ้น หากไม่มี State Management ที่ดี คุณจะพบกับปัญหาต่างๆ ดังนี้:</p><ol style="padding-left:20px"><li><strong>Prop Drilling:</strong> การต้องส่งผ่านข้อมูลผ่าน Constructor ของ Widget ที่ไม่จำเป็นต้องใช้ข้อมูลนั้น เพียงเพื่อส่งต่อไปยัง Widget ลูกที่ต้องการใช้ ทำให้โค้ดรกรุงรังและแก้ไขยาก</li><li><strong>Performance Issues:</strong> หากจัดการ State ไม่ดี การเรียก <code>setState()</code> ที่ต้นทางของ Widget Tree อาจส่งผลให้แอปพลิเคชันต้องวาด UI ใหม่ (Rebuild) ทั้งหน้า ซึ่งส่งผลเสียต่อประสิทธิภาพ (Performance) อย่างมาก</li><li><strong>Unmaintainable Code:</strong> การเขียน Business Logic ปะปนกับ UI (Presentation Logic) จะทำให้โค้ดอ่านยาก ทดสอบยาก (Unit Testing) และยากต่อการทำงานร่วมกันเป็นทีม</li></ol><p>ในบทถัดๆ ไป เราจะมาเจาะลึกเครื่องมือและสถาปัตยกรรมการจัดการ State ในรูปแบบต่างๆ ไม่ว่าจะเป็น Provider, Riverpod และ BLoC เพื่อให้คุณสามารถเลือกใช้เครื่องมือที่เหมาะสมกับโปรเจกต์ของคุณได้อย่างมืออาชีพ!</p>'
  },
  {
    number: 1,
    slug: 'setstate',
    emoji: '🧱',
    title: 'setState และ InheritedWidget',
    content: '<h2>พื้นฐานของการจัดการ State ด้วย setState</h2><p>การจัดการ State ที่พื้นฐานที่สุดใน Flutter คือการใช้ <strong>StatefulWidget</strong> ร่วมกับเมธอด <code>setState()</code> เมื่อใดก็ตามที่คุณเรียก <code>setState()</code> Flutter จะรับรู้ว่าข้อมูลมีการเปลี่ยนแปลง และจะทำการเรียกเมธอด <code>build()</code> ของ Widget นั้นๆ ใหม่เพื่อให้ UI สะท้อนข้อมูลล่าสุด</p><pre><code>class CounterWidget extends StatefulWidget {\n  @override\n  _CounterWidgetState createState() => _CounterWidgetState();\n}\n\nclass _CounterWidgetState extends State&lt;CounterWidget&gt; {\n  int _counter = 0;\n\n  void _increment() {\n    setState(() {\n      _counter++;\n    });\n  }\n\n  @override\n  Widget build(BuildContext context) {\n    return Column(\n      children: [\n        Text(\'Count: $_counter\'),\n        ElevatedButton(onPressed: _increment, child: Text(\'Add\')),\n      ],\n    );\n  }\n}</code></pre><h2>Lifting State Up (การยก State ขึ้นไปด้านบน)</h2><p>เมื่อคุณมี Widget สองตัวในระดับเดียวกัน (Siblings) ที่ต้องการใช้งาน State ร่วมกัน วิธีแก้ปัญหาด้วย <code>setState()</code> คือการ <strong>Lifting State Up</strong> หรือการย้าย State ขึ้นไปยัง Widget พ่อ (Parent Widget) ที่ครอบ Widget ทั้งสองตัวนั้นไว้</p><p>ตัวอย่างเช่น หากคุณมี Widget แสดงผลคะแนน และ Widget ปุ่มกดเพิ่มคะแนน คุณต้องเอา <code>_score</code> ไปไว้ที่ Parent Widget แล้วส่งค่า <code>_score</code> เป็นพารามิเตอร์ให้ Widget แสดงผล และส่ง Function callback ไปให้ Widget ปุ่มกดเพื่อใช้เรียก <code>setState()</code> จากตัว Parent</p><p>การทำแบบนี้ใช้งานได้ดีในระดับหนึ่ง แต่เมื่อ Parent ต้องส่งค่าผ่าน Widget ลูกหลายๆ ชั้น (Prop Drilling) จะทำให้เกิดปัญหาโค้ดซับซ้อนและการ Rebuild ที่ไม่จำเป็นในวงกว้าง</p><h2>เมื่อไหร่ที่ setState ก็เพียงพอ?</h2><p>แม้ว่าจะมี State Management Library มากมาย แต่ <code>setState()</code> ก็ยังเป็นเครื่องมือที่ทรงพลังและควรใช้ในสถานการณ์เหล่านี้:</p><ul style="padding-left:20px"><li><strong>Local Form State:</strong> การจัดการค่าใน TextField หรือการสลับสถานะ Checkbox ภายในหน้าเดียวกัน</li><li><strong>UI Animations:</strong> การจัดการค่าสำหรับการทำ Animation พื้นฐาน</li><li><strong>Toggle States:</strong> สถานะง่ายๆ เช่น การเปิด/ปิด Panel หรือ Accordion</li></ul><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> อย่ากลัวที่จะใช้ <code>setState()</code> การใช้ State Management ที่ซับซ้อนกับปัญหาง่ายๆ ถือเป็น Overengineering ที่ทำให้โปรเจกต์ดูแลยากขึ้นโดยใช่เหตุ</div><h2>เบื้องหลังการทำงานของ InheritedWidget</h2><p>เพื่อแก้ปัญหา Prop Drilling ของ <code>setState()</code> Flutter จึงมีกลไกที่เรียกว่า <strong>InheritedWidget</strong> ซึ่งเป็น Widget พิเศษที่อนุญาตให้ Widget ลูกๆ ที่อยู่ลึกลงไปใน Tree สามารถเข้าถึงข้อมูลของมันได้โดยตรงผ่าน <code>BuildContext</code> (เช่น <code>Theme.of(context)</code> หรือ <code>MediaQuery.of(context)</code>)</p><p>เมื่อข้อมูลใน InheritedWidget เปลี่ยนแปลง มันจะแจ้งเตือน (Notify) ให้เฉพาะ Widget ลูกที่กำลังใช้ข้อมูลนั้นๆ ทำการ Rebuild เท่านั้น ทำให้มีประสิทธิภาพ (Performance) ดีกว่าการส่งข้อมูลลงมาเป็นทอดๆ</p><pre><code>class MyColorWidget extends InheritedWidget {\n  final Color color;\n\n  MyColorWidget({required this.color, required Widget child}) : super(child: child);\n\n  static MyColorWidget? of(BuildContext context) {\n    return context.dependOnInheritedWidgetOfExactType&lt;MyColorWidget&gt;();\n  }\n\n  @override\n  bool updateShouldNotify(MyColorWidget oldWidget) {\n    return color != oldWidget.color;\n  }\n}</code></pre><p>แม้ว่า InheritedWidget จะทรงพลัง แต่มันก็เขียนค่อนข้างยากและมี Boilerplate โค้ดเยอะ นี่จึงเป็นที่มาของ Library อย่าง <strong>Provider</strong> ที่ถูกสร้างขึ้นมาเป็น Wrapper ครอบการทำงานของ InheritedWidget ให้ใช้งานได้ง่ายและปลอดภัยมากยิ่งขึ้นนั่นเอง</p>'
  },
  {
    number: 2,
    slug: 'provider',
    emoji: '📦',
    title: 'Provider Fundamentals',
    content: '<h2>ทำความรู้จักกับ Provider</h2><p><strong>Provider</strong> คือ State Management Library ที่ได้รับความนิยมอย่างมากในชุมชน Flutter (และเคยได้รับการแนะนำอย่างเป็นทางการจากทีม Google) หลักการทำงานของ Provider คือการสร้าง Wrapper (ตัวครอบ) การทำงานของ <strong>InheritedWidget</strong> ให้มี Boilerplate โค้ดน้อยลง ใช้งานง่ายขึ้น และเพิ่มฟีเจอร์ในการจัดการ Dependency Injection และ Lifecycle ควบคู่ไปด้วย</p><p>หัวใจสำคัญของ Provider คือการแยก Business Logic ออกจาก UI อย่างชัดเจน ช่วยให้โค้ดมีความเป็นระเบียบ (Separation of Concerns) และสามารถทำ Unit Testing ได้ง่าย</p><h2>ChangeNotifier และ ChangeNotifierProvider</h2><p>วิธีที่ได้รับความนิยมที่สุดในการใช้ Provider คือการทำงานร่วมกับคลาส <strong>ChangeNotifier</strong> ซึ่งเป็นคลาสที่มีอยู่ใน Flutter SDK โดยตรง</p><p>คุณสามารถสร้างคลาส State ของคุณโดยการ <code>extend ChangeNotifier</code> เมื่อมีข้อมูลเปลี่ยนแปลงและคุณต้องการให้ UI อัปเดต คุณเพียงแค่เรียกเมธอด <code>notifyListeners()</code></p><pre><code>class CartProvider extends ChangeNotifier {\n  List&lt;String&gt; _items = [];\n  List&lt;String&gt; get items =&gt; _items;\n\n  void addItem(String item) {\n    _items.add(item);\n    notifyListeners(); // สั่งให้ UI ที่กำลังฟังอยู่ทำการ Rebuild\n  }\n}</code></pre><p>หลังจากนั้น คุณต้องใช้ <strong>ChangeNotifierProvider</strong> ครอบ Widget Tree ในจุดที่ต้องการให้ State นี้เข้าถึงได้ เช่น:</p><pre><code>void main() {\n  runApp(\n    ChangeNotifierProvider(\n      create: (context) =&gt; CartProvider(),\n      child: MyApp(),\n    ),\n  );\n}</code></pre><h2>Consumer vs context.watch / context.read</h2><p>เมื่อ Provider ถูกประกาศไว้ใน Widget Tree แล้ว Widget ลูกๆ สามารถเข้าถึง State ได้หลายวิธี:</p><h3>1. การใช้ context.watch&lt;T&gt;()</h3><p>ใช้เมื่อคุณต้องการให้ Widget ทำการ <strong>Rebuild ทุกครั้ง</strong> ที่ State มีการเปลี่ยนแปลง (เรียกภายใน <code>build()</code> method เท่านั้น)</p><pre><code>Widget build(BuildContext context) {\n  final cart = context.watch&lt;CartProvider&gt;();\n  return Text(\'Items in cart: ${cart.items.length}\');\n}</code></pre><h3>2. การใช้ context.read&lt;T&gt;()</h3><p>ใช้เมื่อคุณต้องการเข้าถึง State หรือเรียกฟังก์ชัน <strong>เพียงครั้งเดียวโดยไม่ต้องการ Rebuild</strong> (มักใช้ในปุ่มกด หรือ initState)</p><pre><code>ElevatedButton(\n  onPressed: () {\n    context.read&lt;CartProvider&gt;().addItem(\'Apple\');\n  },\n  child: Text(\'Add Apple\'),\n)</code></pre><h3>3. การใช้ Consumer&lt;T&gt;</h3><p>เป็น Widget ที่ทำหน้าที่เหมือน <code>context.watch</code> แต่ช่วยลดขอบเขตการ Rebuild ให้แคบลงเฉพาะในส่วนของ <code>builder</code> แทนที่จะ Rebuild ทั้งหน้าจอ</p><pre><code>Consumer&lt;CartProvider&gt;(\n  builder: (context, cart, child) {\n    return Text(\'Items: ${cart.items.length}\');\n  },\n)</code></pre><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> เพื่อ Performance ที่ดีที่สุด ควรพยายามใช้ <code>Consumer</code> หุ้มเฉพาะ Widget ส่วนเล็กๆ ที่จำเป็นต้องเปลี่ยนค่าจริงๆ การใช้ <code>context.watch</code> ที่ด้านบนสุดของ <code>build()</code> จะทำให้วาดใหม่ทั้งหน้าซึ่งอาจทำให้แอปกะตุกได้</div><h2>การใช้งาน MultiProvider</h2><p>ในแอปพลิเคชันจริง คุณมักจะมี Provider หลายตัว (เช่น AuthProvider, CartProvider, ThemeProvider) การเอา Provider มาซ้อนกันหลายๆ ชั้นในรูปแบบ Tree จะทำให้โค้ดดูลึกและอ่านยาก (Callback Hell) Provider จึงมี <strong>MultiProvider</strong> มาให้ใช้เพื่อรวม Provider ทั้งหมดไว้ในที่เดียว</p><pre><code>MultiProvider(\n  providers: [\n    ChangeNotifierProvider(create: (_) =&gt; AuthProvider()),\n    ChangeNotifierProvider(create: (_) =&gt; CartProvider()),\n  ],\n  child: MyApp(),\n)</code></pre><p>การเข้าใจพื้นฐานเหล่านี้ถือเป็นก้าวแรกที่สำคัญ ในบทต่อไปเราจะมาดูการใช้ Provider ขั้นสูง และการเชื่อมโยง State เข้าด้วยกัน</p>'
  },
  {
    number: 3,
    slug: 'advanced-provider',
    emoji: '🚀',
    title: 'Advanced Provider',
    content: '<h2>การใช้งาน Provider ขั้นสูง</h2><p>เมื่อคุณคุ้นเคยกับ <code>ChangeNotifierProvider</code> แล้ว คุณจะพบว่าในสถาปัตยกรรมแอปพลิเคชันที่ซับซ้อน State มักจะต้องทำงานร่วมกันหรือพึ่งพากัน (Dependency) ตัวอย่างเช่น <code>CartProvider</code> อาจจำเป็นต้องใช้ข้อมูลจาก <code>AuthProvider</code> เพื่อรู้ว่าใครคือเจ้าของตะกร้าสินค้านี้ ในสถานการณ์เช่นนี้ <strong>ProxyProvider</strong> คือพระเอกของเรา</p><h2>ProxyProvider (Provider ที่ขึ้นอยู่กับ Provider อื่น)</h2><p><strong>ProxyProvider</strong> เป็นเครื่องมือที่ช่วยให้เราสร้างออบเจกต์ใหม่ (หรืออัปเดตข้อมูล) โดยอาศัยค่าจาก Provider ตัวอื่นๆ ที่อยู่ใน Widget Tree เหนือขึ้นไป เมื่อค่าของ Provider ต้นทางเปลี่ยน ProxyProvider จะทำการคำนวณและสร้างค่าใหม่โดยอัตโนมัติ</p><pre><code>MultiProvider(\n  providers: [\n    ChangeNotifierProvider(create: (_) =&gt; AuthProvider()),\n    ProxyProvider&lt;AuthProvider, ApiService&gt;(\n      update: (context, auth, previous) =&gt; ApiService(auth.token),\n    ),\n  ],\n  child: MyApp(),\n)</code></pre><p>ในตัวอย่างด้านบน เมื่อผู้ใช้ Login และ <code>AuthProvider</code> ได้รับ <code>token</code> ใหม่ ProxyProvider จะทำการสร้าง <code>ApiService</code> ตัวใหม่ที่ถือ token ล่าสุดทันที ทำให้ Component อื่นๆ สามารถเข้าถึง <code>ApiService</code> ที่มี token พร้อมใช้งานได้เสมอ</p><h2>FutureProvider และ StreamProvider</h2><p>นอกจากการจัดการ State ด้วยคลาสธรรมดาแล้ว Provider ยังมีฟีเจอร์สำหรับการจัดการข้อมูลแบบ Asynchronous อย่างเต็มรูปแบบ</p><h3>FutureProvider</h3><p>ใช้สำหรับข้อมูลที่มีการโหลดครั้งเดียวผ่าน <code>Future</code> เช่น การโหลดข้อมูล Configuration จาก API หรือการอ่านข้อมูลจาก SharedPreferences ระบบจะแสดงค่า <code>initialData</code> ก่อน และเมื่อ <code>Future</code> ทำงานเสร็จก็จะอัปเดต UI ให้อัตโนมัติ</p><pre><code>FutureProvider&lt;String&gt;(\n  create: (context) =&gt; fetchConfig(),\n  initialData: \'Loading...\',\n  child: MyApp(),\n)</code></pre><h3>StreamProvider</h3><p>เหมาะสำหรับการรับข้อมูลที่มีการอัปเดตอย่างต่อเนื่องแบบ Real-time (Stream) เช่น ข้อมูลจาก Firebase Cloud Firestore, ตำแหน่ง GPS ปัจจุบัน, หรือ WebSocket</p><pre><code>StreamProvider&lt;int&gt;(\n  create: (context) =&gt; streamOfCounters(),\n  initialData: 0,\n  child: MyApp(),\n)</code></pre><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> แม้ว่า FutureProvider และ StreamProvider จะใช้งานง่าย แต่ถ้าข้อมูลนั้นต้องการการจัดการ Business Logic หรือการจัดเก็บสถานะการโหลด/ความผิดพลาด (Loading/Error States) ที่ซับซ้อน แนะนำให้ใช้ <code>ChangeNotifierProvider</code> ร่วมกับ State Enum แทนจะควบคุมได้ดีกว่า</div><h2>Best Practices สำหรับการใช้งาน Provider</h2><ol style="padding-left:20px"><li><strong>อย่าวาง Provider สูงเกินความจำเป็น:</strong> หาก Provider ตัวนั้นใช้แค่ในหน้าจอ <code>ProfilePage</code> ก็ให้ครอบ Provider ไว้ที่ ProfilePage แทนที่จะวางไว้ที่ <code>MaterialApp</code> เพื่อประหยัดหน่วยความจำ</li><li><strong>ใช้ Selector แทน Consumer:</strong> หาก Provider ของคุณมีข้อมูลเยอะมาก แต่ Widget ของคุณต้องการใช้แค่ค่า <code>isDarkMode</code> ให้ใช้ <code>Selector</code> เพื่อป้องกันการ Rebuild เมื่อข้อมูลส่วนอื่นเปลี่ยน<br><pre><code>Selector&lt;ThemeModel, bool&gt;(\n  selector: (context, model) =&gt; model.isDarkMode,\n  builder: (context, isDark, child) {\n    return Switch(value: isDark, onChanged: ...);\n  },\n)</code></pre></li><li><strong>หลีกเลี่ยงการเรียก context.read นอกวงจรชีวิตที่เหมาะสม:</strong> พยายามหลีกเลี่ยงการใช้ <code>context.read()</code> ใน <code>build()</code> method ให้ใช้สำหรับ Event handlers (เช่น <code>onPressed</code>) เสมอ</li></ol><p>Provider เป็นรากฐานที่ดีมาก แต่ยังมีข้อจำกัดเรื่องการที่ขึ้นอยู่กับ Widget Tree สิ่งนี้จะถูกแก้ไขและพัฒนาให้สมบูรณ์ขึ้นใน Riverpod ซึ่งเราจะพูดถึงในบทถัดไป</p>'
  },
  {
    number: 4,
    slug: 'riverpod-intro',
    emoji: '🌊',
    title: 'Riverpod Intro',
    content: '<h2>ทำไมถึงต้องมี Riverpod? (What is Riverpod?)</h2><p><strong>Riverpod</strong> เป็น State Management Library ที่ถูกสร้างขึ้นโดย Remi Rousselet ซึ่งเป็นผู้พัฒนาคนเดียวกันกับที่สร้าง Provider ในความเป็นจริง ชื่อ "Riverpod" เป็นการสลับตัวอักษร (Anagram) ของคำว่า "Provider" นั่นเอง</p><p>แม้ Provider จะยอดเยี่ยม แต่ก็มีข้อจำกัดบางประการที่แก้ได้ยากเนื่องจากมันพึ่งพา <strong>InheritedWidget</strong> อย่างหนัก ข้อจำกัดเหล่านั้นได้แก่:</p><ul style="padding-left:20px"><li><strong>ProviderNotFoundException:</strong> เกิดเมื่อคุณพยายามเรียกใช้ Provider ที่ยังไม่ได้ถูกประกาศไว้ใน Widget Tree เหนือตัวมัน ซึ่งมักเจอเมื่อมีการทำ Navigation หรือ Dialog</li><li><strong>ปัญหา Provider ชนิดเดียวกันซ้ำกัน:</strong> Provider ไม่สามารถใช้ <code>Provider&lt;String&gt;</code> สองตัวในระดับเดียวกันได้ เพราะมันอ้างอิงข้อมูลด้วย <strong>Type</strong> ทำให้ระบบแยกไม่ออกว่ากำลังเรียกตัวไหน</li><li><strong>ขาดความสามารถเรื่อง Safety ตอน Compile:</strong> บางครั้งคุณอาจเผลอใช้งานผิดวิธีโดยที่ระบบไม่เตือนและไปแครชตอนรันแอป (Runtime Exception)</li></ul><p>Riverpod ถูกสร้างขึ้นมาเพื่อ <strong>ข้ามข้อจำกัดของ Widget Tree</strong> โดยสิ้นเชิง Provider ของ Riverpod นั้นถูกประกาศเป็น Global Variable ที่ปลอดภัย และคอมไพเลอร์สามารถตรวจสอบข้อผิดพลาดให้คุณได้ 100% (Compile-Safe)</p><h2>ProviderScope ศูนย์กลางของ State</h2><p>เพื่อให้แอปพลิเคชันใช้งาน Riverpod ได้ คุณต้องครอบ Widget ที่ระดับบนสุด (Root) ของแอปด้วย <strong>ProviderScope</strong> นี่คือจุดที่ Riverpod ใช้เก็บ State ทั้งหมดของแอป (เปรียบเสมือนโกดังเก็บข้อมูลส่วนกลาง)</p><pre><code>void main() {\n  runApp(\n    ProviderScope(\n      child: MyApp(),\n    ),\n  );\n}</code></pre><h2>ConsumerWidget และ ref.watch vs ref.read</h2><p>ในการเข้าถึง State จาก Riverpod คุณไม่สามารถใช้ <code>BuildContext</code> แบบ Provider ได้อีกต่อไป แต่คุณจะต้องใช้ <strong>WidgetRef</strong> (มักเรียกสั้นๆ ว่า <code>ref</code>)</p><p>วิธีที่ง่ายที่สุดคือการเปลี่ยนจาก <code>StatelessWidget</code> เป็น <strong>ConsumerWidget</strong> ซึ่งจะให้ตัวแปร <code>ref</code> มาในเมธอด <code>build()</code></p><pre><code>// ประกาศ Provider (ทำเป็น Global Variable ได้อย่างปลอดภัย)\nfinal greetingProvider = Provider&lt;String&gt;((ref) =&gt; \'Hello, Riverpod!\');\n\nclass GreetingWidget extends ConsumerWidget {\n  @override\n  Widget build(BuildContext context, WidgetRef ref) {\n    // ใช้ ref.watch เพื่อติดตามและรับค่า\n    final greeting = ref.watch(greetingProvider);\n    return Text(greeting);\n  }\n}</code></pre><h3>ความแตกต่างระหว่าง ref.watch และ ref.read</h3><ol style="padding-left:20px"><li><strong>ref.watch:</strong> ใช้ <em>ภายในเมธอด build() หรือภายใน Provider ตัวอื่น</em> เพื่อเฝ้าดูการเปลี่ยนแปลง หากข้อมูลเปลี่ยน Widget นี้จะถูก Rebuild อัตโนมัติ (เป็น Best Practice ที่ควรใช้เกือบตลอดเวลา)</li><li><strong>ref.read:</strong> ใช้สำหรับอ่านค่า <em>เพียงครั้งเดียว</em> โดยไม่มีผลต่อการ Rebuild มักใช้ในปุ่มกด (onClick) หรืองานแบบ Fire-and-forget <strong>ข้อควรระวัง: ห้ามใช้ ref.read ภายใน build() เด็ดขาด</strong></li></ol><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> กฎเหล็กของ Riverpod คือจงใช้ <code>ref.watch</code> ให้มากที่สุดเท่าที่จะทำได้ แม้จะเป็นฟังก์ชันธรรมดาก็ตาม เพื่อให้แน่ใจว่า UI จะสอดคล้องกับ State เสมอ ยกเว้นใน Event Callback (เช่น onPressed) ค่อยใช้ <code>ref.read</code></div><h2>ข้อดีของ Riverpod</h2><p>นอกจากความปลอดภัยในระดับ Compile-Time แล้ว Riverpod ยังช่วยให้เราทดสอบโค้ด (Unit Testing) ได้ง่ายดายมาก เพราะเราไม่ต้องจำลอง (Mock) Widget Tree อันสลับซับซ้อนอีกต่อไป แค่สร้าง <code>ProviderContainer</code> ก็สามารถดึง State ไปทดสอบได้ทันที การย้ายจาก Provider มาใช้ Riverpod ถือเป็นการยกระดับสถาปัตยกรรมของแอปพลิเคชันให้พร้อมรองรับการสเกลอย่างแท้จริง</p>'
  },
  {
    number: 5,
    slug: 'riverpod-providers',
    emoji: '🧩',
    title: 'Riverpod Providers',
    content: '<h2>ประเภทของ Providers ใน Riverpod</h2><p>Riverpod มี Provider ให้เลือกใช้หลายประเภท เพื่อให้เหมาะสมกับความซับซ้อนของข้อมูลและสถานการณ์ต่างๆ การเลือกใช้ Provider ที่ถูกต้องจะทำให้การจัดการ State มีประสิทธิภาพและโค้ดอ่านง่ายขึ้น ใน Riverpod เวอร์ชัน 1.x และ 2.x (แบบ Legacy) เรามักจะเจอกับ Provider พื้นฐานดังนี้:</p><h3>1. Provider</h3><p>นี่คือ Provider ที่เรียบง่ายที่สุด ใช้สำหรับคืนค่าออบเจกต์ที่ไม่สามารถเปลี่ยนแปลงได้ (Immutable) หรือไม่ต้องการการทำ UI Rebuild จากภายในตัวมันเอง มักใช้สำหรับการจัดการ Dependency Injection (DI) เช่น การสร้าง Instance ของ Repository หรือ API Client</p><pre><code>final apiRepositoryProvider = Provider&lt;ApiRepository&gt;((ref) {\n  return ApiRepository();\n});</code></pre><h3>2. StateProvider</h3><p>เหมาะสำหรับการจัดการ State พื้นฐานง่ายๆ ที่เป็นตัวแปรเดี่ยว เช่น `int`, `String`, `bool` ตัวอย่างคลาสสิกคือตัวนับ (Counter) หรือสถานะการเลือก Tab ใน UI</p><pre><code>final counterProvider = StateProvider&lt;int&gt;((ref) =&gt; 0);\n\n// วิธีใช้งาน\nref.read(counterProvider.notifier).state++; // เพิ่มค่า</code></pre><h3>3. StateNotifierProvider</h3><p>เมื่อ State เริ่มมีความซับซ้อน หรือต้องจัดการ Business Logic ก่อนอัปเดตข้อมูล (เช่น ตรวจสอบความถูกต้องก่อนเพิ่มลง List) เราจะใช้ <code>StateNotifier</code> นี่คือหัวใจสำคัญในการแยก Logic ออกจาก UI อย่างแท้จริงใน Riverpod (แม้ในปัจจุบันจะถูกแทนที่ด้วย <code>Notifier</code> ก็ตาม แต่มันยังคงถูกใช้อย่างแพร่หลาย)</p><pre><code>class CartNotifier extends StateNotifier&lt;List&lt;String&gt;&gt; {\n  CartNotifier() : super([]); // ข้อมูลเริ่มต้น\n\n  void addItem(String item) {\n    state = [...state, item]; // สร้าง List ใหม่เพื่อกระตุ้นการอัปเดต\n  }\n}\n\nfinal cartProvider = StateNotifierProvider&lt;CartNotifier, List&lt;String&gt;&gt;((ref) {\n  return CartNotifier();\n});</code></pre><h3>4. FutureProvider</h3><p>เป็นเครื่องมือทรงพลังสำหรับการจัดการ Asynchronous Operations โดย <code>FutureProvider</code> จะจัดการสถานะให้เรา 3 แบบอัตโนมัติ คือ: โหลด (Loading), สำเร็จ (Data), และ ล้มเหลว (Error) นิยมใช้ดึงข้อมูลจาก API</p><pre><code>final userProvider = FutureProvider.autoDispose&lt;User&gt;((ref) async {\n  final api = ref.watch(apiRepositoryProvider);\n  return await api.fetchUserData();\n});</code></pre><h2>ความมหัศจรรย์ของ Modifiers (autoDispose และ family)</h2><p>ความสามารถพิเศษของ Riverpod ที่ทำให้เหนือกว่าตัวอื่นๆ คือการมี <strong>Modifiers</strong> นำมาต่อท้าย Provider เพื่อปรับพฤติกรรมได้อย่างอิสระ</p><h3>.autoDispose</h3><p>โดยปกติ Provider ใน Riverpod จะเก็บ State ไว้ในหน่วยความจำ (Memory) ตลอดไปจนกว่าแอปจะปิด แต่ถ้าเราต่อท้ายด้วย <code>.autoDispose</code> ระบบจะทำลาย (Dispose) State นั้นทิ้งทันทีเมื่อไม่มี Widget ตัวไหนกำลัง <code>watch</code> มันอยู่! สิ่งนี้ช่วยลดปัญหา Memory Leak ได้อย่างมหาศาล โดยเฉพาะในหน้าจอชั่วคราว (เช่น หน้า Detail ข้อมูล)</p><pre><code>final tempCounterProvider = StateProvider.autoDispose&lt;int&gt;((ref) =&gt; 0);</code></pre><h3>.family</h3><p>ใช้ในกรณีที่คุณต้องการส่งพารามิเตอร์หรือ Argument เข้าไปใน Provider ตอนที่เรียกสร้าง (ตัวอย่างเช่น ส่ง <code>userId</code> เข้าไปเพื่อดึงข้อมูลผู้ใช้นั้นๆ จาก API)</p><pre><code>final userDetailProvider = FutureProvider.family&lt;User, String&gt;((ref, userId) async {\n  return await fetchUserFromApi(userId);\n});\n\n// เวลาใช้งานใน Widget\nfinal userData = ref.watch(userDetailProvider(\'user_123\'));</code></pre><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> คุณสามารถใช้ Modifier ร่วมกันได้ เช่น <code>FutureProvider.autoDispose.family</code> ซึ่งจะทำให้เราได้ Provider ที่ดึงข้อมูลตาม ID และล้างหน่วยความจำทิ้งทันทีเมื่อผู้ใช้ออกจากหน้านั้น นี่คือท่ามาตรฐานที่มีประสิทธิภาพสูงมากในการดึงข้อมูลจาก API</div>'
  },
  {
    number: 6,
    slug: 'riverpod-2',
    emoji: '⚡',
    title: 'Riverpod 2.0 (Notifier)',
    content: '<h2>การปฏิวัติสถาปัตยกรรมใน Riverpod 2.0</h2><p>เมื่อ Riverpod พัฒนามาถึงเวอร์ชัน 2.0 ได้มีการอัปเดต API ขนานใหญ่เพื่อลดความซับซ้อน ลดความซ้ำซ้อนของโค้ด (Boilerplate) และทำให้รองรับความสามารถใหม่ๆ ได้ดีขึ้น การเปลี่ยนแปลงที่สำคัญที่สุดคือการยกเลิกแนะนำให้ใช้ <code>StateNotifierProvider</code> และหันมาใช้ <strong>Notifier</strong> และ <strong>AsyncNotifier</strong> แทน</p><h2>จาก StateNotifier สู่ Notifier</h2><p>ในเวอร์ชันเก่า <code>StateNotifier</code> มีข้อจำกัดที่มันไม่สามารถเข้าถึง <code>ref</code> ได้อย่างสมบูรณ์ตั้งแต่ตอนถูกสร้าง และมี Syntax ที่ค่อนข้างเยิ่นเย้อ แต่ <code>Notifier</code> ตัวใหม่นั้นเข้าถึง <code>ref</code> ได้โดยตรงผ่าน <code>build()</code> method ทำให้การนำ Provider มาประกอบเข้าด้วยกัน (Composition) เป็นเรื่องธรรมชาติมากขึ้น</p><pre><code>// รูปแบบใหม่ใน Riverpod 2.0\nclass CounterNotifier extends Notifier&lt;int&gt; {\n  @override\n  int build() {\n    // คุณสามารถเข้าถึง ref.watch() อื่นๆ ได้ที่นี่\n    return 0; // คืนค่าเริ่มต้น\n  }\n\n  void increment() {\n    state++; // อัปเดตค่า state\n  }\n}\n\nfinal counterProvider = NotifierProvider&lt;CounterNotifier, int&gt;(() {\n  return CounterNotifier();\n});</code></pre><h2>จัดการข้อมูลแบบ Asynchronous ด้วย AsyncNotifier</h2><p>ก่อนหน้านี้ หากเราต้องการแก้ไขข้อมูลที่มาจาก <code>FutureProvider</code> (เช่น โหลดข้อมูลเสร็จแล้ว อยากแก้ไข แล้วอัปเดตกลับไปที่ State) จะทำได้ยากมาก Riverpod จึงออก <strong>AsyncNotifier</strong> มาเพื่อรวมร่างระหว่าง `FutureProvider` และ `Notifier` เข้าด้วยกัน</p><pre><code>class TodosNotifier extends AsyncNotifier&lt;List&lt;Todo&gt;&gt; {\n  @override\n  Future&lt;List&lt;Todo&gt;&gt; build() async {\n    // โหลดข้อมูลเริ่มต้น (ทำหน้าที่เหมือน FutureProvider)\n    return await fetchTodos();\n  }\n\n  Future&lt;void&gt; addTodo(Todo newTodo) async {\n    // ตั้งสถานะเป็น Loading\n    state = const AsyncValue.loading();\n    try {\n      await uploadTodoToApi(newTodo);\n      // ดึงข้อมูลใหม่หรืออัปเดต state ปัจจุบัน\n      final currentTodos = await fetchTodos();\n      state = AsyncValue.data(currentTodos);\n    } catch (e, stack) {\n      // จัดการ Error อัตโนมัติ\n      state = AsyncValue.error(e, stack);\n    }\n  }\n}\n\nfinal todosProvider = AsyncNotifierProvider&lt;TodosNotifier, List&lt;Todo&gt;&gt;(() =&gt; TodosNotifier());</code></pre><h2>ก้าวสู่อนาคตด้วย Code Generation (@riverpod)</h2><p>ฟีเจอร์เด็ดที่สุดที่ Riverpod 2.0 ผลักดันคือการใช้ <strong>Code Generation</strong> (ผ่าน package <code>riverpod_generator</code> และ <code>build_runner</code>) ซึ่งช่วยให้คุณเขียนโค้ดสั้นลงอย่างมหาศาล ระบบจะจัดการประเภท (Types) และ Modifier (เช่น autoDispose) ให้อัตโนมัติ</p><p>แทนที่คุณจะต้องประกาศ <code>NotifierProvider</code> ยืดยาว คุณเพียงแค่เขียน Annotation <code>@riverpod</code> กำกับไว้เท่านั้น</p><pre><code>import \'package:riverpod_annotation/riverpod_annotation.dart\';\npart \'counter.g.dart\';\n\n@riverpod\nclass Counter extends _$Counter {\n  @override\n  int build() =&gt; 0;\n\n  void increment() =&gt; state++;\n}</code></pre><p>หรือถ้าเป็น Provider แบบ Provider ธรรมดา ก็เขียนแค่ฟังก์ชัน:</p><pre><code>@riverpod\nString greeting(GreetingRef ref) {\n  return \'Hello from Generator!\';\n}</code></pre><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> ผู้สร้าง Riverpod แนะนำให้ใช้ Code Generation สำหรับโปรเจกต์ใหม่ๆ ทุกโปรเจกต์ แม้จะดูยุ่งยากที่ต้องรัน <code>build_runner</code> แต่ประโยชน์เรื่อง Type-safety, การลดโค้ดซ้ำซาก, และการรองรับพารามิเตอร์แบบ <code>family</code> หลายๆ ตัว ถือว่าคุ้มค่าเกินราคา</div>'
  },
  {
    number: 7,
    slug: 'bloc-basics',
    emoji: '🧱',
    title: 'BLoC Pattern Basics',
    content: '<h2>แนวคิดพื้นฐานของ BLoC Pattern</h2><p><strong>BLoC (Business Logic Component)</strong> เป็นสถาปัตยกรรมการจัดการ State ที่ทรงอิทธิพลมากที่สุดตัวหนึ่งในโลกของ Flutter มันถูกนำเสนอครั้งแรกโดยวิศวกรของ Google แนวคิดหลักของ BLoC คือการแยก <strong>Business Logic</strong> ออกจาก <strong>UI (Presentation Layer)</strong> อย่างเด็ดขาด โดยบังคับให้มีการสื่อสารระหว่าง UI และ Logic ผ่าน "เหตุการณ์" (Events) และ "สถานะ" (States) เพียงเท่านั้น</p><p>จินตนาการว่า BLoC เป็นกล่องดำ (Black Box) กล่องหนึ่ง: UI จะไม่รู้เลยว่าการคำนวณข้างในทำงานอย่างไร UI มีหน้าที่เพียงสองอย่างคือ <strong>"ส่ง Event เข้าไป"</strong> (เช่น ผู้ใช้กดปุ่ม Login) และ <strong>"รอรับ State ที่พ่นออกมา"</strong> (เช่น กำลังโหลด, สำเร็จ, ล้มเหลว) เพื่อนำมาวาดหน้าจอใหม่</p><h2>Streams และ Sinks พื้นฐานที่ควรรู้</h2><p>ก่อนที่แพ็กเกจช่วยอำนวยความสะดวกจะถือกำเนิดขึ้น BLoC แทร้ๆ จะพึ่งพาเทคโนโลยี <strong>Stream</strong> ของภาษา Dart แบบ 100%</p><ul style="padding-left:20px"><li><strong>Sink (ทางเข้า):</strong> เปรียบเสมือนท่อน้ำที่คุณสามารถหยอดข้อมูลหรือ Event เข้าไปได้ (เช่น <code>streamController.sink.add(event)</code>)</li><li><strong>Stream (ทางออก):</strong> เปรียบเสมือนปลายท่อน้ำที่ UI สามารถ <code>listen</code> รอรับข้อมูลที่ไหลออกมา เพื่ออัปเดตหน้าจอ (ผ่าน <code>StreamBuilder</code>)</li></ul><pre><code>// ตัวอย่างการเขียน BLoC แบบดั้งเดิม (โดยไม่ใช้ Package)\nclass CounterBloc {\n  int _counter = 0;\n\n  // Stream/Sink สำหรับ State\n  final _stateController = StreamController&lt;int&gt;();\n  Stream&lt;int&gt; get stateStream =&gt; _stateController.stream;\n\n  // Stream/Sink สำหรับ Event\n  final _eventController = StreamController&lt;CounterEvent&gt;();\n  Sink&lt;CounterEvent&gt; get eventSink =&gt; _eventController.sink;\n\n  CounterBloc() {\n    // ฟัง Event ที่เข้ามา แล้วอัปเดต State\n    _eventController.stream.listen((event) {\n      if (event is IncrementEvent) {\n        _counter++;\n        _stateController.sink.add(_counter);\n      }\n    });\n  }\n\n  void dispose() {\n    _stateController.close();\n    _eventController.close();\n  }\n}</code></pre><h2>Separation of Concerns (การแยกสัดส่วนความรับผิดชอบ)</h2><p>ข้อดีที่สุดของ BLoC คือโครงสร้างที่บังคับให้โค้ดของคุณมีความเป็นระเบียบสูงมาก การทำงานของ BLoC มักจะแบ่งเลเยอร์ (Layer) ชัดเจนดังนี้:</p><ol style="padding-left:20px"><li><strong>Data Layer:</strong> จัดการเรื่อง API, Database สร้าง <code>Repository</code> และ <code>Models</code></li><li><strong>Business Logic Layer (BLoC):</strong> นำข้อมูลจาก Repository มาประมวลผลตามกฎทางธุรกิจ จัดการ Error แล้วปล่อย State ที่ UI เข้าใจได้ออกมา</li><li><strong>Presentation Layer (UI):</strong> ไม่มีการคำนวณใดๆ ทั้งสิ้น มีเพียงแค่ <code>if (state is Loading) showIndicator()</code> หน้าที่เดียวคือสะท้อน State ให้ออกมาเป็นภาพ</li></ol><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> สถาปัตยกรรมแบบ BLoC ทำให้การเขียน Unit Test เป็นเรื่องกล้วยๆ มาก เพราะคุณสามารถส่งจำลอง Event เข้าไป แล้วทดสอบ (Expect) ว่า State ที่พ่นออกมาเรียงลำดับถูกต้องหรือไม่ โดยไม่ต้องมี UI หรือ Widget เข้ามาเกี่ยวข้องเลย!</div><p>แม้คอนเซปต์จะดูดี แต่คุณจะเห็นว่าการเขียน BLoC ด้วย Stream เองนั้นต้องสร้าง Boilerplate Code (โค้ดซ้ำซาก) มหาศาล นี่จึงเป็นสาเหตุที่คอมมูนิตี้สร้างแพ็กเกจ <strong>flutter_bloc</strong> ขึ้นมา ซึ่งเราจะไปเจาะลึกกันในบทถัดไป</p>'
  },
  {
    number: 8,
    slug: 'flutter-bloc',
    emoji: '🛠️',
    title: 'flutter_bloc Package',
    content: '<h2>การใช้งานแพ็กเกจ flutter_bloc</h2><p>แพ็กเกจ <strong>flutter_bloc</strong> สร้างโดย Felix Angelov กลายเป็นมาตรฐานอุตสาหกรรม (Industry Standard) สำหรับการทำ BLoC ใน Flutter แพ็กเกจนี้ซ่อนความยุ่งยากของการจัดการ Stream/Sink และจัดเตรียม Widget สำเร็จรูปสำหรับการเชื่อมโยง BLoC เข้ากับ Widget Tree ไว้อย่างครบถ้วน</p><h2>Cubit vs BLoC (เครื่องมือ 2 รูปแบบ)</h2><p>แพ็กเกจ flutter_bloc มอบเครื่องมือให้คุณ 2 ทางเลือกในการจัดการ State ซึ่งทั้งสองตัวใช้กลไกการส่ง State ออกมาแบบเดียวกัน แต่มีวิธีรับคำสั่งที่ต่างกัน:</p><h3>1. Cubit (เรียบง่าย รวดเร็ว)</h3><p>Cubit เป็นรูปแบบที่ถูกลดทอนความซับซ้อนของ BLoC ลง แทนที่จะรับคำสั่งผ่าน Event Cubit อนุญาตให้ UI "เรียกฟังก์ชัน (Method)" ได้โดยตรง แล้ว Cubit ค่อย <code>emit()</code> สถานะใหม่ออกมา Cubit เหมาะกับงานที่ไม่ซับซ้อนมากนัก</p><pre><code>class CounterCubit extends Cubit&lt;int&gt; {\n  CounterCubit() : super(0); // State เริ่มต้น\n\n  void increment() =&gt; emit(state + 1);\n  void decrement() =&gt; emit(state - 1);\n}</code></pre><h3>2. BLoC (ซับซ้อน ทรงพลัง)</h3><p>BLoC แบบเต็มรูปแบบ จะต้องมีการสร้างคลาส Event ขึ้นมา BLoC จะรับ Event เหล่านี้ผ่าน <code>on&lt;Event&gt;()</code> ข้อดีคือมันสามารถจัดการ Concurrency (เช่น การกดปุ่มรัวๆ) การแปลง Stream (เช่น Debounce หรือ Throttle เวลาพิมพ์ค้นหา) ได้อย่างทรงพลัง</p><pre><code>// Events\nabstract class CounterEvent {}\nclass IncrementPressed extends CounterEvent {}\n\n// BLoC\nclass CounterBloc extends Bloc&lt;CounterEvent, int&gt; {\n  CounterBloc() : super(0) {\n    // Map Event สู่ State\n    on&lt;IncrementPressed&gt;((event, emit) {\n      emit(state + 1);\n    });\n  }\n}</code></pre><h2>Widgets สำคัญใน flutter_bloc</h2><p>เพื่อเชื่อม UI กับ State แพ็กเกจนี้ได้เตรียม Widget หลักๆ ไว้ 3 ตัว (อารมณ์คล้าย Provider เลยครับ เพราะใต้ฝากระโปรงมันก็ใช้ Provider นั่นแหละ):</p><ol style="padding-left:20px"><li><strong>BlocProvider:</strong> ทำหน้าที่สร้าง Instance ของ Bloc/Cubit และจัดเก็บไว้ใน Widget Tree คอยดูแล Lifecycle ให้ (Dispose อัตโนมัติเมื่อเลิกใช้) <br><pre><code>BlocProvider(\n  create: (context) =&gt; CounterBloc(),\n  child: MyApp(),\n)</code></pre></li><li><strong>BlocBuilder:</strong> ทำหน้าที่ฟังการเปลี่ยนแปลง State และ Rebuild UI ตาม State ล่าสุด (คล้าย Consumer) <br><pre><code>BlocBuilder&lt;CounterBloc, int&gt;(\n  builder: (context, count) {\n    return Text(\'Count: $count\');\n  },\n)</code></pre></li><li><strong>BlocListener:</strong> คล้าย Builder แต่จะไม่ Rebuild UI มันใช้สำหรับ "ฟัง State เพื่อสั่งทำ Action เพียงครั้งเดียว" (Side-effects) เช่น การโชว์ Snackbar, แสดง Dialog หรือการ Navigation ไปหน้าใหม่ <br><pre><code>BlocListener&lt;AuthBloc, AuthState&gt;(\n  listener: (context, state) {\n    if (state is AuthError) {\n      ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(state.message)));\n    }\n  },\n  child: Container(),\n)</code></pre></li></ol><div class=\'tip-box\'><strong>💡 Pro Tip:</strong> หากคุณต้องใช้ทั้ง BlocBuilder และ BlocListener ในระดับเดียวกัน คุณสามารถใช้ <strong>BlocConsumer</strong> ซึ่งเป็นการรวมร่างสองตัวนี้เข้าด้วยกันใน Widget เดียว ทำให้โค้ดสะอาดขึ้นอย่างมาก</div>'
  },
  {
    number: 9,
    slug: 'comparison',
    emoji: '⚖️',
    title: 'Comparison and Architecture',
    content: '<h2>เปรียบเทียบ Provider vs Riverpod vs BLoC</h2><p>หลังจากเรียนรู้เครื่องมือหลักครบทั้ง 3 ตัวแล้ว คำถามที่พบได้บ่อยที่สุดในหมู่นักพัฒนา Flutter คือ <em>"แล้วโปรเจกต์ของฉันควรเลือกใช้อะไรดี?"</em> ไม่มีเครื่องมือไหนที่ดีที่สุดในทุกสถานการณ์ แต่ละตัวมีจุดแข็งและจุดประสงค์ที่แตกต่างกัน เรามาดูเปรียบเทียบแบบเจาะลึกกันครับ:</p><h3>1. Provider</h3><ul style="padding-left:20px"><li><strong>จุดเด่น:</strong> เรียบง่าย เรียนรู้ได้ไวที่สุด โค้ดน้อย เหมาะสำหรับการเริ่มต้น และแอปพลิเคชันขนาดเล็กถึงขนาดกลาง</li><li><strong>จุดด้อย:</strong> ขึ้นอยู่กับ Widget Tree อย่างหนัก อาจเจอปัญหา <code>ProviderNotFoundException</code> หากวางตำแหน่งผิด ซ้อนกันหลายชั้นโค้ดจะรก ขาด Compile-safe แบบ 100%</li><li><strong>เหมาะกับใคร:</strong> มือใหม่ที่เพิ่งย้ายมาทำ Flutter, โปรเจกต์ที่ทำคนเดียว, แอปพลิเคชันทั่วไปที่ State ไม่ซับซ้อนมาก</li></ul><h3>2. Riverpod</h3><ul style="padding-left:20px"><li><strong>จุดเด่น:</strong> คือร่างพัฒนาของ Provider ปลอดภัยระดับ Compile-time 100% ไม่ต้องพึ่งพา BuildContext หรือ Widget Tree รองรับการจัดการ Asynchronous State (Future/Stream) ได้สุดยอดมาก มี Modifier ช่วยลบ Memory อัตโนมัติ (autoDispose) และการใช้ Code Generation ทำให้โค้ดสะอาดมาก</li><li><strong>จุดด้อย:</strong> ช่วงการเรียนรู้ (Learning Curve) ค่อนข้างชันในช่วงแรก API มีหลายแบบอาจทำให้สับสน ต้องทำความคุ้นเคยกับ Annotation และ <code>build_runner</code></li><li><strong>เหมาะกับใคร:</strong> โปรเจกต์สมัยใหม่ แอปพลิเคชันขนาดกลางถึงใหญ่ ทีมที่ต้องการความปลอดภัยของ Type-safety และการทำ Unit Testing ที่ง่ายดาย (ถือเป็นตัวเลือกอันดับ 1 ในยุคปัจจุบัน)</li></ul><h3>3. BLoC / flutter_bloc</h3><ul style="padding-left:20px"><li><strong>จุดเด่น:</strong> เป็นสถาปัตยกรรมระดับองค์กร (Enterprise Grade) บังคับให้เกิด Separation of Concerns สูงสุด การใช้ Event &rarr; State ชัดเจนมาก ทำให้ระบบสามารถติดตาม (Trace) เหตุการณ์ทั้งหมดได้ง่าย (Predictable) จัดการเหตุการณ์พร้อมกัน (Concurrency) ได้ทรงพลัง</li><li><strong>จุดด้อย:</strong> Boilerplate (โค้ดที่ต้องเขียนซ้ำๆ) เยอะมากที่สุด การเพิ่มแค่ฟีเจอร์เล็กๆ อาจต้องไปแก้ไฟล์ Event, State และ Bloc ทำให้ทำงานได้ช้าลง (แม้จะมี Cubit มาช่วยแล้วก็ตาม)</li><li><strong>เหมาะกับใคร:</strong> ทีมขนาดใหญ่ที่ทำงานร่วมกันหลายคน โปรเจกต์ระดับ Enterprise แอปพลิเคชันที่มี Business Logic ซับซ้อนมากระดับแอปธนาคาร หรือต้องการโครงสร้างที่ทุกคนต้องเขียนในมาตรฐานเดียวกันเป๊ะๆ</li></ul><h2>การออกแบบ Repository Pattern ร่วมกับ State Management</h2><p>ไม่ว่าคุณจะเลือกใช้ State Management ตัวใด สิ่งหนึ่งที่เป็น <strong>Best Practice</strong> เสมอคือการนำ <strong>Repository Pattern</strong> มาใช้ร่วมด้วย</p><p>กฎเหล็กคือ State Management (ไม่ว่าจะเป็น Provider, Notifier หรือ Bloc) <strong>ห้ามคุยกับฐานข้อมูลหรือเรียก API โดยตรงเด็ดขาด!</strong></p><p>คุณควรสร้างคลาส <code>Repository</code> ขึ้นมาทำหน้าที่ติดต่อกับโลกภายนอก (เช่น <code>UserRepository</code> คุยกับ Firebase หรือ REST API) จากนั้นให้ State Management ทำหน้าที่ถือ Dependency ของ Repository นั้นๆ และเรียกใช้งาน</p><pre><code>// ตัวอย่างโครงสร้างที่ถูกต้อง\nAPI (Network) / Database (Local)\n      &darr;\nRepository (จัดรูปแบบข้อมูล, แปลง JSON เป็น Object)\n      &darr;\nState Management (Riverpod / BLoC - สั่งงาน Repository และรับข้อมูลมาอัปเดต State)\n      &darr;\nUI (วาดหน้าจอตาม State ปัจจุบัน)</code></pre><div class=\'tip-box\'><strong>💡 บทสรุปส่งท้าย:</strong> การเป็นนักพัฒนา Flutter ที่ดี ไม่ใช่คนที่รู้ว่าแพ็กเกจไหนคือ "สิ่งที่ดีที่สุด" แต่คือคนที่เข้าใจ "ข้อจำกัด" ของแต่ละเครื่องมือ และสามารถ "เลือก" ใช้เครื่องมือที่เหมาะสมกับ <strong>สเกลของโปรเจกต์ และ ทักษะของทีม</strong> ได้อย่างชาญฉลาดที่สุดครับ ขอให้สนุกกับการพัฒนาแอปพลิเคชัน!</div>'
  }
];
