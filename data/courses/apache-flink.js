export const chapters = [
  { number: 0, slug: 'overview', emoji: '⚡', title: 'Apache Flink vs Kafka Streams', content: '<h2>Apache Flink คืออะไร?</h2><p><strong>Apache Flink</strong> คือ Framework สำหรับ <em>Distributed Stream Processing</em> ที่ถูกพัฒนาขึ้นโดย Apache Software Foundation โดยมีจุดเด่นที่การประมวลผลข้อมูลแบบ Real-time ด้วย Latency ต่ำมาก รองรับ Stateful Processing และมีกลไก Fault Tolerance ที่แข็งแกร่ง Flink ถูกใช้งานอย่างแพร่หลายใน Production ของบริษัทใหญ่ ๆ เช่น Alibaba, Netflix, Uber และ LinkedIn</p><h2>Use Cases หลักของ Apache Flink</h2><ul><li><strong>Fraud Detection</strong> — ตรวจจับการทุจริตในธุรกรรมการเงินแบบ Real-time โดยวิเคราะห์ Pattern ของข้อมูลที่ไหลเข้ามาอย่างต่อเนื่อง</li><li><strong>Real-time Analytics</strong> — สร้าง Dashboard และ Report แบบ Real-time สำหรับธุรกิจ E-commerce และ Fintech</li><li><strong>Event-driven Applications</strong> — ระบบที่ตอบสนองต่อ Event ทันที เช่น การแจ้งเตือนราคาหุ้น</li><li><strong>ETL Pipelines</strong> — แปลงและส่งข้อมูลระหว่าง Systems แบบ Streaming</li><li><strong>Machine Learning Inference</strong> — ประยุกต์ใช้ ML Model กับ Data Stream แบบ Real-time</li></ul><h2>เปรียบเทียบ Flink กับ Kafka Streams</h2><p><strong>Kafka Streams</strong> เป็น Client Library ที่รันอยู่ใน Application ของคุณโดยตรง ไม่ต้องการ Cluster แยกต่างหาก เหมาะสำหรับ Microservice ขนาดเล็กถึงกลางที่ต้องการ Simplicity ในการ Deploy ในขณะที่ <strong>Apache Flink</strong> เป็น Distributed Processing Engine ที่มี Cluster แยกต่างหาก รองรับ Workload ขนาดใหญ่ได้ดีกว่า มี Operator ที่หลากหลายกว่า และรองรับทั้ง Batch และ Stream Processing ใน Framework เดียวกัน</p><h3>ความแตกต่างด้านการจัดการ State</h3><p>Kafka Streams ใช้ <code>RocksDB</code> เป็น State Store โดย Default และ State จะถูกผูกอยู่กับ Application Instance แต่ละตัว ส่วน Flink มี State Backend หลายประเภทได้แก่ <code>HashMapStateBackend</code>, <code>EmbeddedRocksDBStateBackend</code> และมีกลไก Checkpoint ที่ทำ Snapshot ของ State ทั้งหมดไปยัง Remote Storage เช่น HDFS หรือ S3</p><h2>Flink vs Spark Streaming</h2><p><strong>Spark Streaming</strong> ใช้แนวคิด <em>Micro-batch</em> คือแบ่งข้อมูลออกเป็น Batch เล็ก ๆ แล้วประมวลผลทีละ Batch ทำให้ Latency อยู่ในระดับ วินาที ส่วน <strong>Flink</strong> ใช้ <em>True Streaming</em> คือประมวลผล Event ทีละ Record ทำให้ Latency ได้ถึงระดับ Millisecond และยังรองรับ Stateful Operations ได้ดีกว่าอย่างมีนัยสำคัญ</p><h2>Event Time vs Processing Time</h2><p>ความแตกต่างระหว่าง <strong>Event Time</strong> และ <strong>Processing Time</strong> เป็นแนวคิดพื้นฐานที่สำคัญมากใน Flink</p><ul><li><strong>Event Time</strong> — เวลาที่ Event เกิดขึ้นจริงในโลก ณ แหล่งกำเนิด เช่น Timestamp ของ Transaction ที่เกิดขึ้นที่ร้านค้า ใช้ข้อมูลนี้เมื่อต้องการผลลัพธ์ที่ถูกต้องแม้ข้อมูลมาถึงล่าช้าหรือไม่เรียงลำดับ</li><li><strong>Processing Time</strong> — เวลาที่ Flink ประมวลผล Event นั้นจริง ๆ เป็น Approach ที่ง่ายที่สุดแต่อาจให้ผลลัพธ์ไม่ถูกต้องเมื่อมี Network Delay หรือข้อมูลมาช้า</li><li><strong>Ingestion Time</strong> — เวลาที่ Source Operator รับ Event เข้าสู่ Flink มีความแม่นยำระหว่างทั้งสองแบบข้างต้น</li></ul><h3>Watermark คืออะไร?</h3><p>Watermark เป็นกลไกที่ Flink ใช้ติดตาม Progress ของ Event Time บอกระบบว่า "ข้อมูลที่มี Timestamp ก่อนหน้า Watermark นี้ควรมาถึงครบแล้ว" ทำให้สามารถ Trigger Window Computation ได้อย่างถูกต้องแม้ข้อมูลมาไม่เรียงลำดับ</p><h2>Latency ของ Flink</h2><p>Flink สามารถทำ End-to-end Latency ได้ในระดับ <strong>ต่ำกว่า 10 Millisecond</strong> สำหรับ Workload ที่ไม่ซับซ้อน และในระดับ 100ms สำหรับ Pipeline ที่ซับซ้อนกว่า ทำให้เหมาะอย่างยิ่งสำหรับ Application ที่ต้องการ Low-latency Response เช่น Trading Systems และ Fraud Detection</p>' },
  { number: 1, slug: 'architecture', emoji: '🏗️', title: 'Flink Architecture: JobManager, TaskManager, Checkpoints', content: '<h2>ภาพรวม Architecture ของ Apache Flink</h2><p><strong>Apache Flink</strong> ทำงานในรูปแบบ Master-Worker Architecture โดยมี Component หลักสองส่วนคือ <em>JobManager</em> ซึ่งทำหน้าที่เป็น Master และ <em>TaskManager</em> ซึ่งทำหน้าที่เป็น Worker ทั้งสองส่วนนี้ทำงานร่วมกันเพื่อ Execute Flink Job อย่างมีประสิทธิภาพและทนทานต่อความล้มเหลว</p><h2>JobManager</h2><p>JobManager คือ Central Coordinator ของ Flink Cluster ทำหน้าที่หลักดังนี้</p><ul><li><strong>Resource Management</strong> — จัดสรร Slot จาก TaskManager ให้กับ Job Tasks</li><li><strong>Job Scheduling</strong> — แปลง Logical Execution Graph ให้เป็น Physical Execution Graph และ Schedule การทำงาน</li><li><strong>Checkpoint Coordination</strong> — ประสานงาน Checkpoint Process โดยส่ง Barrier ไปยัง Task ทุกตัว</li><li><strong>Failure Recovery</strong> — เมื่อ Task ล้มเหลว JobManager จะ Restart Task จาก Checkpoint ล่าสุด</li></ul><h3>JobManager High Availability</h3><p>ใน Production Environment ควรตั้งค่า <strong>JobManager HA</strong> เพื่อป้องกัน Single Point of Failure โดย Flink รองรับ HA ผ่าน <code>ZooKeeper</code> หรือ <code>Kubernetes</code> เมื่อ Active JobManager ล้มเหลว Standby JobManager จะเข้ามาทำหน้าที่แทนโดยอ่าน Job Metadata จาก Shared Storage เช่น HDFS หรือ S3 Configuration หลักคือ <code>high-availability: zookeeper</code> และ <code>high-availability.storageDir</code></p><h2>TaskManager</h2><p>TaskManager คือ Worker Node ที่ Execute Task จริง ๆ แต่ละ TaskManager มี <strong>Task Slot</strong> หลายตัว แต่ละ Slot คือ Thread ที่แยกออกมาสำหรับรัน Sub-task โดย Slot ใช้ Memory แยกกัน แต่ใช้ CPU ร่วมกัน</p><h3>Task Slots และ Parallelism</h3><p><strong>Parallelism</strong> ของ Flink Job กำหนดว่าแต่ละ Operator จะมีกี่ Parallel Instance ค่า Parallelism สูงสุดที่ทำได้ขึ้นอยู่กับ Total Slots ที่มีในทุก TaskManager รวมกัน เช่น ถ้ามี 4 TaskManager แต่ละตัวมี 4 Slot จะรองรับ Parallelism สูงสุด 16 การตั้งค่า Slot เหมาะสมจะช่วยให้ใช้ทรัพยากรได้อย่างมีประสิทธิภาพ</p><h2>Execution Graph</h2><p>เมื่อ Submit Job ไปยัง Flink Cluster กระบวนการแปลง Job จะผ่าน 4 ขั้นตอน</p><ul><li><strong>StreamGraph</strong> — Logical Graph ที่สร้างจาก DataStream API Code ของผู้ใช้</li><li><strong>JobGraph</strong> — Optimized Graph หลังจากทำ Chaining Operators เข้าด้วยกัน</li><li><strong>ExecutionGraph</strong> — Parallel version ของ JobGraph ที่ขยายตาม Parallelism ที่กำหนด</li><li><strong>Physical Execution</strong> — Task ที่รันจริงบน TaskManager</li></ul><h2>Checkpoint Mechanism</h2><p>Checkpoint คือกลไก Fault Tolerance หลักของ Flink โดยใช้อัลกอริทึม <strong>Asynchronous Barrier Snapshotting</strong> (ABS) ซึ่งอิงจาก Chandy-Lamport Algorithm ขั้นตอนการทำงานมีดังนี้</p><ul><li>JobManager ส่ง <em>Checkpoint Barrier</em> เข้าสู่ Data Stream</li><li>เมื่อ Operator ได้รับ Barrier จาก Input ทุกตัวแล้ว จะทำ Snapshot ของ State ตัวเองลงไปยัง Storage</li><li>หลังจาก Snapshot เสร็จ Operator ส่ง Barrier ต่อไปยัง Downstream</li><li>เมื่อ Sink รับ Barrier ครบ Checkpoint นั้นจึงถือว่า Complete</li></ul><h3>Savepoints</h3><p><strong>Savepoints</strong> คล้ายกับ Checkpoint แต่ถูก Trigger ด้วยตนเอง (Manually) และไม่ถูกลบโดยอัตโนมัติ ใช้สำหรับการ Upgrade Application, A/B Testing หรือย้าย Job ไปยัง Cluster ใหม่ Savepoint จะเก็บ State ของทุก Operator พร้อม Metadata ที่สามารถ Resume Job ได้ในภายหลัง</p><h2>Operator Chaining</h2><p>Flink จะทำ <strong>Operator Chaining</strong> โดยอัตโนมัติเมื่อเป็นไปได้ คือรวม Operator หลาย ๆ ตัวให้รันใน Thread เดียวกัน เพื่อลด Overhead ของการส่งข้อมูลระหว่าง Operator ทำให้ Performance ดีขึ้นอย่างมาก สามารถปิด Chaining ได้ด้วย <code>disableChaining()</code> หรือ <code>startNewChain()</code></p>' },
  { number: 2, slug: 'datastream', emoji: '🌊', title: 'DataStream API: อ่านและ Process ข้อมูล', content: '<h2>DataStream API คืออะไร?</h2><p><strong>DataStream API</strong> คือ Core API ของ Flink สำหรับการ Process ข้อมูลแบบ Stream เป็น Low-level API ที่ให้ความยืดหยุ่นสูงสุด เหมาะสำหรับ Use Case ที่ซับซ้อนที่ Table API หรือ SQL ไม่สามารถรองรับได้ DataStream API ทำงานกับ Type-safe Java/Scala/Python Objects</p><h2>Sources: การอ่านข้อมูลเข้า Flink</h2><p>Flink รองรับ Source หลากหลายประเภท</p><h3>Kafka Source</h3><p>Kafka เป็น Source ที่นิยมใช้มากที่สุดกับ Flink การตั้งค่า Kafka Source ใช้ <code>KafkaSource</code> Builder</p><ul><li>กำหนด Bootstrap Servers และ Topic ที่ต้องการอ่าน</li><li>ตั้งค่า Consumer Group ID เพื่อ Track Offset</li><li>เลือก <code>OffsetsInitializer</code> เช่น earliest, latest หรือ Committed Offset</li><li>กำหนด Deserializer สำหรับแปลง Bytes เป็น Object</li></ul><h3>File Source</h3><p>ใช้ <code>FileSource</code> สำหรับอ่านข้อมูลจาก File System เช่น Local FS, HDFS หรือ S3 รองรับ Format หลากหลายเช่น Text, CSV, JSON, Parquet, Avro และยังมี Continuous Monitoring Mode ที่คอย Monitor Directory เพื่ออ่าน File ใหม่ที่เพิ่มเข้ามาแบบ Automatic</p><h3>Socket Source</h3><p><code>socketTextStream</code> ใช้สำหรับ Testing และ Development โดยรับข้อมูลจาก TCP Socket ไม่แนะนำสำหรับ Production เนื่องจากไม่มี Fault Tolerance</p><h2>Transformations: การแปลงข้อมูล</h2><h3>map และ flatMap</h3><p><strong>map</strong> แปลง Element แต่ละตัวเป็น Element ใหม่แบบ 1-to-1 เช่น แปลง String เป็น Integer หรือ Parse JSON String เป็น Object ส่วน <strong>flatMap</strong> แปลง Element หนึ่งตัวเป็น Zero หรือหลาย Element ใช้เมื่อต้องการ Split หรือ Expand ข้อมูล</p><h3>filter</h3><p><strong>filter</strong> กรองเฉพาะ Element ที่ผ่าน Condition ที่กำหนด ส่งคืน DataStream ที่มีเฉพาะ Element ที่ Function คืนค่า <code>true</code></p><h3>keyBy</h3><p><strong>keyBy</strong> เป็นหนึ่งใน Transformation ที่สำคัญที่สุด ทำการ Partition Stream ตาม Key ที่กำหนด Element ที่มี Key เดียวกันจะถูกส่งไปยัง Parallel Instance เดียวกัน ทำให้สามารถทำ Stateful Operation ต่อ Key ได้ เช่น นับจำนวน Transaction ต่อ UserID</p><h3>reduce และ aggregate</h3><p><strong>reduce</strong> รวม Element ใน Keyed Stream โดยการ Apply Function แบบ Pairwise เช่น หาผลรวม หา Maximum หรือนับจำนวน ส่วน <strong>aggregate</strong> ใช้กับ Window เพื่อ Accumulate ข้อมูลในรูปแบบที่กำหนดเองได้</p><h2>Sinks: การส่งข้อมูลออก</h2><p>Flink รองรับ Sink หลากหลายประเภท</p><ul><li><strong>Kafka Sink</strong> — ส่งผลลัพธ์ไปยัง Kafka Topic โดยรองรับ Exactly-once Semantics ผ่าน Transactional Producer</li><li><strong>JDBC Sink</strong> — เขียนข้อมูลลง Relational Database เช่น PostgreSQL, MySQL</li><li><strong>Elasticsearch Sink</strong> — Index ข้อมูลเข้า Elasticsearch สำหรับ Search และ Analytics</li><li><strong>File Sink</strong> — เขียน File ลง HDFS หรือ S3 รองรับ Format Parquet, Avro, CSV</li><li><strong>Print Sink</strong> — print ออกทาง stdout ใช้สำหรับ Debug และ Testing</li></ul><h2>Watermarks และ Event Time Processing</h2><p>การใช้ Event Time ใน Flink ต้องกำหนด <strong>WatermarkStrategy</strong> เพื่อบอก Flink ว่าจะ Extract Timestamp จาก Event อย่างไร และจะ Generate Watermark อย่างไร กลยุทธ์ที่นิยมคือ <code>WatermarkStrategy.forBoundedOutOfOrderness(Duration.ofSeconds(5))</code> ซึ่งบอกว่า Event อาจมาล่าช้าสูงสุด 5 วินาที</p><h2>Async I/O</h2><p><strong>Async I/O</strong> ใน Flink ช่วยให้สามารถทำ External Lookup เช่น Query Database หรือเรียก REST API ระหว่างการ Process Stream โดยไม่ Blocking Thread ทำให้ Throughput ดีขึ้นอย่างมีนัยสำคัญ ใช้ <code>AsyncDataStream.unorderedWait()</code> หรือ <code>orderedWait()</code> ขึ้นอยู่กับว่าต้องการ Preserve Order หรือไม่</p>' },
  { number: 3, slug: 'table-sql', emoji: '📊', title: 'Table API และ Flink SQL', content: '<h2>Table API และ Flink SQL คืออะไร?</h2><p><strong>Table API</strong> และ <strong>Flink SQL</strong> คือ Higher-level API ของ Flink ที่ให้นักพัฒนาสามารถ Query ข้อมูล Stream ด้วย SQL-like Syntax ซึ่งคุ้นเคยกับผู้ใช้ Database และ Data Warehouse ทำให้ลด Learning Curve ได้มาก และเหมาะสำหรับ Analytics Use Case ที่ไม่ต้องการ Custom Logic ที่ซับซ้อน</p><h2>Table API vs DataStream API</h2><p>การเลือกใช้ API ที่เหมาะสมขึ้นอยู่กับ Use Case</p><ul><li><strong>Table API / SQL</strong> — เหมาะสำหรับ Analytics, Aggregations, Joins ที่ Standard และทีมที่มีพื้นฐาน SQL ดี Code สั้นกว่าและ Optimizer จะช่วย Optimize Query ให้อัตโนมัติ</li><li><strong>DataStream API</strong> — เหมาะสำหรับ Complex Event Processing, Custom State Management, Low-latency Applications และกรณีที่ต้องการควบคุม Detail ของการประมวลผล</li><li><strong>ผสมผสาน</strong> — Flink รองรับการแปลงระหว่าง DataStream และ Table ได้ด้วย <code>toDataStream()</code> และ <code>fromDataStream()</code></li></ul><h2>Flink SQL Syntax</h2><p>Flink SQL ใช้ Syntax ที่คล้าย ANSI SQL มาก รองรับ Statement หลักดังนี้</p><h3>DDL Statements</h3><p>ใช้สำหรับสร้างและจัดการ Schema ของ Table</p><ul><li><code>CREATE TABLE</code> — สร้าง Table พร้อมกำหนด Schema, Connector และ Format</li><li><code>CREATE VIEW</code> — สร้าง Virtual Table จาก Query</li><li><code>CREATE CATALOG</code> — เชื่อมต่อ External Catalog เช่น Hive Metastore</li><li><code>DROP TABLE</code>, <code>ALTER TABLE</code> — จัดการ Table ที่มีอยู่แล้ว</li></ul><h3>DML Statements</h3><p>ใช้สำหรับ Query และ Manipulate ข้อมูล</p><ul><li><code>SELECT</code> — Query ข้อมูลจาก Table หรือ Stream</li><li><code>INSERT INTO</code> — เขียนผลลัพธ์ของ Query ลงไปยัง Sink Table</li><li><code>GROUP BY</code> พร้อม Window Functions สำหรับ Aggregation</li></ul><h2>Temporal Tables</h2><p><strong>Temporal Tables</strong> คือแนวคิดใน Flink SQL ที่ใช้แทน Dimension Table ที่เปลี่ยนแปลงได้ตามเวลา เช่น ตาราง Exchange Rate ที่อัปเดตทุกวัน ช่วยให้ทำ Temporal Join ได้อย่างถูกต้อง โดย Flink จะใช้ Version ของ Dimension Table ที่ตรงกับ Event Time ของ Fact Record</p><h2>Streaming SQL Concepts</h2><p>การใช้ SQL กับ Stream มีแนวคิดพิเศษที่ต่างจาก Batch SQL</p><ul><li><strong>Continuous Queries</strong> — Query ที่รันตลอดเวลาและ Update ผลลัพธ์เมื่อมีข้อมูลใหม่เข้ามา</li><li><strong>Append-only Streams</strong> — Stream ที่มีแต่การเพิ่มข้อมูลไม่มีการ Update หรือ Delete</li><li><strong>Retract Streams</strong> — Stream ที่มี Message สำหรับ Retract (ยกเลิก) ข้อมูลก่อนหน้า ใช้กับ Aggregation ที่ Update ได้</li><li><strong>Upsert Streams</strong> — Stream ที่ทำ Insert หรือ Update ตาม Primary Key</li></ul><h2>Catalog Integration</h2><p>Flink รองรับ <strong>Catalog</strong> หลายประเภทสำหรับจัดการ Metadata ของ Table</p><ul><li><strong>In-Memory Catalog</strong> — Default Catalog ที่ไม่ Persist ข้าม Session</li><li><strong>Hive Catalog</strong> — เชื่อมต่อกับ Hive Metastore ทำให้สามารถ Share Table Definition กับ Spark และ Hive ได้</li><li><strong>JDBC Catalog</strong> — อ่าน Table Schema จาก Relational Database โดยตรง</li></ul><h2>SQL Client</h2><p><strong>Flink SQL Client</strong> เป็น Interactive Shell สำหรับรัน Flink SQL Query โดยไม่ต้องเขียน Java หรือ Python Code เหมาะสำหรับ Ad-hoc Analysis และ Testing เปิดใช้งานด้วยคำสั่ง <code>./bin/sql-client.sh</code> และรองรับการ Set Session Properties, Submit Job และดู Query Results แบบ Table หรือ Tableau</p>' },
  { number: 4, slug: 'windows', emoji: '🪟', title: 'Windows: Tumbling, Sliding, Session', content: '<h2>Window Processing ใน Flink</h2><p><strong>Window</strong> คือกลไกสำคัญที่ทำให้ Flink สามารถทำ Aggregation และ Computation บน Bounded Set ของข้อมูลจาก Unbounded Stream ได้ Window จะแบ่ง Stream ออกเป็น "ถัง" (Bucket) ของ Element ตาม Time หรือ Count แล้วประมวลผลแต่ละ Bucket อิสระจากกัน</p><h2>Tumbling Windows</h2><p><strong>Tumbling Window</strong> คือ Window ที่มีขนาดคงที่และไม่ Overlap กัน Element แต่ละตัวจะอยู่ใน Window เดียวเท่านั้น ตัวอย่างเช่น Tumbling Window ขนาด 5 นาที จะสร้าง Window สำหรับ 00:00-05:00, 05:00-10:00, 10:00-15:00 เป็นต้น เหมาะสำหรับ Use Case ที่ต้องการ Report ทุก ๆ ช่วงเวลาที่แน่นอน เช่น ยอดขายทุก 1 ชั่วโมง</p><ul><li><code>TumblingEventTimeWindows.of(Time.minutes(5))</code> — Tumbling Window ตาม Event Time</li><li><code>TumblingProcessingTimeWindows.of(Time.hours(1))</code> — Tumbling Window ตาม Processing Time</li></ul><h2>Sliding Windows</h2><p><strong>Sliding Window</strong> มีขนาดคงที่เช่นกัน แต่ Slide ไปทีละช่วงที่เล็กกว่า Window Size ทำให้ Window Overlap กันได้ Element เดียวอาจอยู่ใน Window หลายอันพร้อมกัน เช่น Window ขนาด 10 นาที ที่ Slide ทุก 5 นาที จะให้ Moving Average ที่ Smooth กว่า Tumbling Window เหมาะสำหรับ Trend Analysis และ Anomaly Detection</p><h2>Session Windows</h2><p><strong>Session Window</strong> มีขนาดที่ Dynamic ขึ้นอยู่กับ Activity ของผู้ใช้ Window จะเปิดเมื่อมี Event เข้ามาและปิดเมื่อไม่มี Event มานานเกิน Gap Duration ที่กำหนด เหมาะมากสำหรับ User Behavior Analysis เช่น นับจำนวน Page View ต่อ Session โดย Session จบเมื่อผู้ใช้ Inactive นาน 30 นาที</p><h2>Global Windows</h2><p><strong>Global Window</strong> รวม Element ทั้งหมดที่มี Key เดียวกันเข้าไปใน Window เดียว ไม่มี Default Trigger ดังนั้นต้องกำหนด Custom Trigger เอง เช่น Count Trigger ที่ Fire เมื่อมีครบ 1,000 Element ใช้สำหรับ Use Case ที่ไม่เหมาะกับ Time-based Window</p><h2>Window Functions</h2><p>เมื่อ Window ถูก Trigger แล้ว Flink จะเรียก Window Function เพื่อประมวลผล Element ใน Window นั้น</p><h3>ReduceFunction</h3><p>Incrementally Aggregate Element เข้าด้วยกันทีละตัวเมื่อเข้ามา ประหยัด Memory มากที่สุด เพราะ State จะมีแค่ Accumulated Value ตัวเดียว ไม่ได้เก็บ Element ทั้งหมด</p><h3>AggregateFunction</h3><p>คล้าย ReduceFunction แต่ยืดหยุ่นกว่า สามารถกำหนด Input, Accumulator และ Output Type แยกกันได้ เหมาะสำหรับ Aggregation ที่ซับซ้อนเช่น คำนวณ Average หรือ Standard Deviation</p><h3>ProcessWindowFunction</h3><p>ให้ Access ทุก Element ใน Window พร้อมกันทั้งหมด รวมถึง Context ของ Window เช่น Window Start/End Time ยืดหยุ่นที่สุดแต่ใช้ Memory มากที่สุดเพราะต้องเก็บ Element ทั้งหมดไว้จนกว่า Window จะปิด</p><h2>Allowed Lateness</h2><p><strong>Allowed Lateness</strong> คือการบอก Flink ว่ายอมรับ Late Data ได้อีกนานเท่าไรหลังจาก Window ปิดแล้ว เช่น <code>allowedLateness(Time.minutes(1))</code> หมายความว่า Window จะถูก Trigger ใหม่ทุกครั้งที่มี Late Element เข้ามาในช่วง 1 นาทีนั้น Late Element ที่มาหลังจากนั้นจะถูก Drop หรือส่งไปยัง Side Output</p><h3>Side Output สำหรับ Late Data</h3><p>การใช้ Side Output ช่วยให้ไม่สูญเสีย Late Data โดย Route ไปประมวลผลต่างหากแทนการ Drop ทิ้ง ใช้ <code>OutputTag</code> เพื่อ Define Side Output และดึงมาจาก Main Stream ด้วย <code>getSideOutput()</code></p>' },
  { number: 5, slug: 'state-management', emoji: '💾', title: 'Keyed State และ Checkpointing', content: '<h2>State Management ใน Flink</h2><p><strong>State</strong> คือข้อมูลที่ Operator เก็บไว้ระหว่างการประมวลผล Event ต่าง ๆ เป็นพื้นฐานของ Stateful Stream Processing ที่ทำให้ Flink ทรงพลัง เช่น การนับจำนวนสะสม การ Detect Pattern ข้ามหลาย Event หรือการ Join Stream ที่มาจากแหล่งต่างกัน</p><h2>ประเภทของ State</h2><h3>Keyed State</h3><p><strong>Keyed State</strong> ผูกกับ Key ของ Stream ที่ผ่าน <code>keyBy()</code> แต่ละ Key จะมี State แยกกันอิสระ เหมาะสำหรับการ Track ข้อมูลต่อ User, ต่อ Device หรือต่อ Transaction ID</p><ul><li><strong>ValueState</strong> — เก็บค่าเดี่ยว ๆ เช่น Counter หรือ Last Event Timestamp อ่านด้วย <code>value()</code> อัปเดตด้วย <code>update()</code></li><li><strong>ListState</strong> — เก็บ List ของ Element เพิ่ม Element ด้วย <code>add()</code> หรือ <code>addAll()</code> และอ่านด้วย <code>get()</code></li><li><strong>MapState</strong> — เก็บ Key-Value Map ภายใน State Access ด้วย <code>get(key)</code>, <code>put(key, value)</code> เหมาะสำหรับ Feature Lookup</li><li><strong>ReducingState</strong> — เก็บ Aggregated Value เดียวที่ถูก Reduce ทุกครั้งที่เพิ่ม Element</li><li><strong>AggregatingState</strong> — คล้าย ReducingState แต่ Input และ Output Type ต่างกันได้</li></ul><h3>Operator State</h3><p><strong>Operator State</strong> ไม่ผูกกับ Key แต่ผูกกับ Operator Instance แต่ละตัว ใช้สำหรับ Source Connector เพื่อ Track Offset ของ Kafka Partition หรือ File Position ที่อ่านค้างไว้</p><h2>State TTL Configuration</h2><p>การตั้งค่า <strong>Time-to-Live (TTL)</strong> ให้กับ State ช่วยป้องกัน State โตไม่หยุด สามารถกำหนดได้ว่า State ของ Key ที่ไม่มีการ Access นานเท่าไรจะถูกลบออก</p><ul><li>ตั้งค่าด้วย <code>StateTtlConfig</code> พร้อมกำหนด TTL Duration</li><li>เลือก <code>UpdateType</code> ว่า TTL จะ Reset เมื่อ Read, Write หรือทั้งสองอย่าง</li><li>กำหนด <code>StateVisibility</code> ว่าจะ Return Expired Value หรือไม่</li></ul><h2>RocksDB State Backend</h2><p><strong>EmbeddedRocksDBStateBackend</strong> เป็น State Backend ที่แนะนำสำหรับ Production เพราะเก็บ State ลงดิสก์ทำให้รองรับ State ขนาดใหญ่ได้มากกว่า Memory-based Backend (HashMapStateBackend) แต่ Performance จะช้ากว่าเล็กน้อยเนื่องจาก Disk I/O การตั้งค่าใช้ <code>env.setStateBackend(new EmbeddedRocksDBStateBackend())</code></p><h3>Incremental Checkpoints</h3><p>RocksDB รองรับ <strong>Incremental Checkpoints</strong> ที่บันทึกเฉพาะส่วนที่เปลี่ยนแปลงจาก Checkpoint ล่าสุด ทำให้ Checkpoint ใช้เวลาน้อยลงมากสำหรับ State ขนาดใหญ่ เปิดใช้งานด้วย <code>new EmbeddedRocksDBStateBackend(true)</code></p><h2>Checkpoint Configuration</h2><p>การตั้งค่า Checkpoint ที่ดีมีผลต่อทั้ง Performance และ Recovery Time</p><ul><li><strong>Checkpoint Interval</strong> — ความถี่ในการทำ Checkpoint เช่น ทุก 1 นาที ค่าน้อยหมายถึง Recovery เร็วขึ้นแต่ Overhead มากขึ้น</li><li><strong>Checkpoint Timeout</strong> — ถ้า Checkpoint ใช้เวลานานกว่านี้จะถูกยกเลิก</li><li><strong>Max Concurrent Checkpoints</strong> — จำนวน Checkpoint ที่รันพร้อมกันได้</li><li><strong>Minimum Pause Between Checkpoints</strong> — ช่วงเวลาพักระหว่าง Checkpoint เพื่อให้ Job มีเวลาทำงานปกติ</li></ul><h2>QueryableState</h2><p><strong>Queryable State</strong> เป็น Feature ที่ช่วยให้ External Application สามารถ Query ค่า State ของ Flink Job แบบ Real-time ได้โดยไม่ต้องส่ง Event เข้ามา เหมาะสำหรับ Use Case ที่ต้องการ Read State บ่อย ๆ เช่น Dashboard ที่แสดงสถานะปัจจุบันของ User แต่ละคน</p>' },
  { number: 6, slug: 'kafka-flink', emoji: '🔗', title: 'Kafka + Flink Pipeline', content: '<h2>ทำไมต้องใช้ Kafka กับ Flink?</h2><p>การใช้ <strong>Apache Kafka</strong> ร่วมกับ <strong>Apache Flink</strong> คือ Architecture ที่ได้รับความนิยมสูงสุดสำหรับ Real-time Data Pipeline ในองค์กร Kafka ทำหน้าที่เป็น Durable Message Buffer ที่รองรับ High Throughput และ Replayable ส่วน Flink ทำหน้าที่ประมวลผลข้อมูลจาก Kafka อย่างรวดเร็วและ Stateful</p><h2>Flink Kafka Connector</h2><p>Flink มี Official Connector สำหรับ Kafka ทั้ง Source และ Sink</p><h3>Kafka Source Configuration</h3><p>การตั้งค่า <code>KafkaSource</code> ประกอบด้วย Component หลักดังนี้</p><ul><li><strong>Bootstrap Servers</strong> — ที่อยู่ของ Kafka Broker เช่น <code>localhost:9092</code></li><li><strong>Topics</strong> — รายชื่อ Topic ที่ต้องการ Subscribe อาจระบุหลาย Topic หรือใช้ Pattern</li><li><strong>Consumer Group ID</strong> — ใช้สำหรับ Offset Management และการกำหนดกลุ่ม Consumer</li><li><strong>Deserialization Schema</strong> — วิธีแปลง Message Bytes เป็น Java Object เช่น <code>JsonDeserializationSchema</code></li><li><strong>Starting Offsets</strong> — จุดเริ่มต้นการอ่าน เช่น Earliest, Latest หรือ Specific Timestamp</li></ul><h3>Kafka Sink Configuration</h3><p>การตั้งค่า <code>KafkaSink</code> สำหรับส่งผลลัพธ์กลับไปยัง Kafka</p><ul><li><strong>Serialization Schema</strong> — แปลง Object กลับเป็น Bytes ก่อนส่ง</li><li><strong>Delivery Guarantee</strong> — เลือกระหว่าง AT_LEAST_ONCE หรือ EXACTLY_ONCE</li><li><strong>Transactional ID Prefix</strong> — จำเป็นสำหรับ Exactly-once เพื่อ Identify Kafka Transactions</li></ul><h2>Consumer Group Management</h2><p>Flink จัดการ Kafka Consumer Group ต่างจาก Consumer ทั่วไป เพราะ Offset ถูก Managed โดย Flink เอง ไม่ใช่ Kafka Consumer Group Protocol Offset จะถูก Commit ไปยัง Kafka เมื่อ Checkpoint สำเร็จเท่านั้น ทำให้ External Monitoring Tool อาจแสดง Consumer Lag ที่ไม่ตรงกับความเป็นจริงในระหว่าง Checkpoint Interval</p><h2>Kafka Topic Partitioning กับ Flink Parallelism</h2><p>การออกแบบ Kafka Topic Partition กับ Flink Parallelism มีผลต่อ Performance อย่างมาก</p><ul><li><strong>Parallelism = Partition Count</strong> — กรณีที่เหมาะสมที่สุดคือให้ Flink Source Parallelism เท่ากับจำนวน Partition ของ Topic ทำให้แต่ละ Parallel Instance อ่านคนละ Partition พอดี</li><li><strong>Parallelism น้อยกว่า Partition</strong> — Instance หนึ่งอาจอ่านหลาย Partition ได้ แต่ Load อาจไม่สมดุล</li><li><strong>Parallelism มากกว่า Partition</strong> — บาง Instance จะว่างงาน ไม่ได้ประโยชน์และเปลืองทรัพยากร</li></ul><h2>Exactly-once กับ Kafka</h2><p>การทำ Exactly-once End-to-end ระหว่าง Kafka และ Flink ต้องการ</p><ul><li>Kafka Broker Version 0.11 ขึ้นไปที่รองรับ Transactional API</li><li>ตั้งค่า <code>DeliveryGuarantee.EXACTLY_ONCE</code> บน Kafka Sink</li><li>เปิดใช้ Flink Checkpoint</li><li>ตั้งค่า <code>isolation.level=read_committed</code> บน Consumer ที่อ่าน Output Topic</li></ul><h2>Schema Registry Integration</h2><p>ใน Production มักใช้ <strong>Confluent Schema Registry</strong> หรือ <strong>AWS Glue Schema Registry</strong> ร่วมกับ Flink-Kafka Connector เพื่อ Enforce Schema ของ Message และรองรับ Schema Evolution การใช้ Avro หรือ Protobuf Format ร่วมกับ Schema Registry ช่วยให้ระบบ Robust ยิ่งขึ้น</p>' },
  { number: 7, slug: 'exactly-once', emoji: '✅', title: 'Exactly-once Semantics', content: '<h2>Delivery Semantics ใน Stream Processing</h2><p>ใน Stream Processing System มีระดับการรับประกันการส่งข้อมูล 3 ระดับที่ต้องทำความเข้าใจ ระดับที่เลือกจะส่งผลต่อทั้ง Correctness ของผลลัพธ์และ Performance ของระบบ</p><h2>At-most-once</h2><p><strong>At-most-once</strong> คือการรับประกันว่า Message จะถูกประมวลผลไม่เกิน 1 ครั้ง อาจ 0 ครั้งก็ได้ถ้าระบบล้มเหลว เป็น Level ที่ให้ Performance ดีที่สุดเพราะไม่ต้องทำ Retry หรือ State Tracking แต่ยอมรับการสูญหายของข้อมูลได้ เหมาะสำหรับ Log Monitoring ที่ Data Loss เล็กน้อยไม่กระทบผลลัพธ์</p><h2>At-least-once</h2><p><strong>At-least-once</strong> รับประกันว่า Message จะถูกประมวลผลอย่างน้อย 1 ครั้ง เมื่อระบบล้มเหลวจะ Replay Message จาก Checkpoint ทำให้บาง Message ถูกประมวลผลซ้ำ Application ต้องรองรับ Idempotency เพื่อให้ผลลัพธ์ถูกต้อง ใช้กับ Flink เมื่อเปิด Checkpoint แต่ไม่ใช้ Transactional Sink</p><h2>Exactly-once</h2><p><strong>Exactly-once</strong> รับประกันว่าแต่ละ Message จะมีผลต่อ State และ Output เพียง 1 ครั้งเท่านั้น แม้ระบบล้มเหลวและ Restart ก็ตาม นี่คือ Level ที่ Flink โดดเด่นที่สุด และเป็นหัวใจของการใช้ Flink ในระบบ Financial Transaction</p><h2>Two-phase Commit ใน Flink</h2><p>Flink ใช้โปรโตคอล <strong>Two-phase Commit (2PC)</strong> เพื่อรับประกัน Exactly-once สำหรับ Transactional Sink ขั้นตอนการทำงาน</p><ul><li><strong>Phase 1 — Pre-commit</strong> เมื่อ Checkpoint Barrier ผ่าน Sink จะเปิด Transaction และเขียนข้อมูลในลักษณะ Pre-commit (ยังไม่ Visible กับ Consumer)</li><li><strong>Phase 2 — Commit</strong> เมื่อ Checkpoint สำเร็จ JobManager จะแจ้ง Sink ให้ Commit Transaction ทำให้ข้อมูล Visible</li><li>ถ้าระบบล้มเหลวก่อน Commit Flink จะ Rollback Transaction และ Replay จาก Checkpoint</li></ul><h2>Checkpoint Barriers</h2><p><strong>Checkpoint Barrier</strong> คือ Special Record ที่ JobManager Inject เข้าไปใน Data Stream เพื่อแบ่ง Stream ออกเป็นส่วน ๆ ตาม Checkpoint</p><ul><li>Barrier ไหลไปพร้อมกับ Data ตามลำดับ</li><li>เมื่อ Operator ได้รับ Barrier จากทุก Input Channel จะ Snapshot State และส่ง Barrier ต่อไป</li><li>ใช้ Barrier Alignment เพื่อ Exactly-once หรือ Unaligned Checkpoint สำหรับ Low Latency</li></ul><h2>Transactional Sinks</h2><p>Flink มี Sink หลายประเภทที่รองรับ Exactly-once ผ่าน 2PC</p><ul><li><strong>Kafka Sink</strong> ใช้ Kafka Transactional Producer API สำหรับ Exactly-once</li><li><strong>JDBC Sink</strong> รองรับ Exactly-once ผ่าน Database Transaction</li><li><strong>File Sink</strong> ใช้การ Rename File แบบ Atomic เพื่อ Exactly-once บน HDFS และ S3</li></ul><h2>End-to-end Exactly-once กับ Kafka</h2><p>การทำ End-to-end Exactly-once ในระบบ Kafka-Flink-Kafka ต้องการการตั้งค่าทั้งสองฝั่ง ฝั่ง Flink ต้องเปิด Checkpoint และใช้ Exactly-once Kafka Sink ส่วนฝั่ง Consumer ที่อ่าน Output Topic ต้องตั้งค่า <code>isolation.level=read_committed</code> เพื่อไม่อ่าน Message ที่ยังไม่ Commit Kafka Broker ต้องมี <code>transaction.max.timeout.ms</code> ที่มากกว่า Checkpoint Interval</p>' },
  { number: 8, slug: 'deployment', emoji: '🚀', title: 'Deploy Flink บน Kubernetes และ YARN', content: '<h2>Deployment Modes ของ Flink</h2><p>Flink รองรับ Deployment Mode หลัก 3 รูปแบบที่มีลักษณะการจัดการ Resource และ Lifecycle ที่แตกต่างกัน การเลือก Mode ที่เหมาะสมขึ้นอยู่กับ Infrastructure, Isolation Requirement และ Operational Complexity ที่ยอมรับได้</p><h2>Session Mode</h2><p><strong>Session Mode</strong> คือการรัน Flink Cluster ที่ทำงานตลอดเวลา และ Submit หลาย Job ให้ทำงานบน Cluster เดียวกัน</p><ul><li>เหมาะสำหรับ Job ขนาดเล็กจำนวนมากที่ Startup Time สำคัญ</li><li>Job ทั้งหมดใช้ Resource ร่วมกัน Job หนึ่ง Crash อาจกระทบ Job อื่น</li><li>Cluster ต้องถูกสร้างล่วงหน้าก่อน Submit Job</li></ul><h2>Per-job Mode</h2><p><strong>Per-job Mode</strong> สร้าง Cluster ใหม่สำหรับแต่ละ Job โดยเฉพาะ เมื่อ Job เสร็จ Cluster จะถูก Terminate อัตโนมัติ</p><ul><li>Isolation ดีเยี่ยม Job แต่ละตัวไม่กระทบกัน</li><li>Startup Time ช้ากว่า Session Mode เพราะต้องสร้าง Cluster ใหม่</li><li>รองรับโดย YARN และ Kubernetes (ผ่าน Operator)</li></ul><h2>Application Mode</h2><p><strong>Application Mode</strong> เป็น Mode ใหม่ที่แนะนำสำหรับ Production บน Kubernetes เพราะรัน <code>main()</code> ของ Application บน Cluster โดยตรง ไม่ต้องรันบน Client ทำให้ลด Network Transfer ของ JAR และลด Memory Footprint บน Client</p><h2>Flink บน Kubernetes Native</h2><p>Flink รองรับ <strong>Native Kubernetes</strong> ตั้งแต่ Version 1.10 โดย Flink จะ Communicate กับ Kubernetes API Server โดยตรงเพื่อ Request และ Release Pod</p><ul><li>สร้าง Kubernetes Cluster แล้วใช้คำสั่ง <code>flink run-application</code> พร้อม <code>-t kubernetes-application</code></li><li>Flink จะสร้าง JobManager Pod และ TaskManager Pod ตามที่ต้องการ</li><li>กำหนด Docker Image ที่รวม Flink Binary และ Application JAR ไว้ด้วยกัน</li></ul><h2>Flink Kubernetes Operator</h2><p><strong>Flink Kubernetes Operator</strong> เป็น Official Operator ที่ช่วยจัดการ Flink Job บน Kubernetes ด้วย Custom Resource Definition (CRD) ทำให้ Deployment เป็น GitOps-friendly สามารถ Declare สถานะที่ต้องการใน YAML และ Operator จะจัดการให้ รองรับ Lifecycle Management, Savepoint Triggers และ Auto-scaling</p><h2>YARN Deployment</h2><p>สำหรับ Infrastructure ที่มี Hadoop YARN อยู่แล้ว การ Deploy Flink บน YARN เป็นตัวเลือกที่ดี</p><ul><li><code>flink run -t yarn-per-job</code> สำหรับ Per-job Mode บน YARN</li><li><code>flink run -t yarn-session</code> สำหรับ Session Mode บน YARN</li><li>กำหนด <code>-yjm</code> สำหรับ JobManager Memory และ <code>-ytm</code> สำหรับ TaskManager Memory</li><li>กำหนด <code>-ys</code> สำหรับจำนวน Slot ต่อ TaskManager</li></ul><h2>Docker Image Configuration</h2><p>Flink มี Official Docker Image บน Docker Hub สามารถ Extend เพื่อเพิ่ม JAR และ Configuration</p><ul><li>Base Image: <code>apache/flink:latest</code></li><li>เพิ่ม Application JAR ไปใน <code>/opt/flink/lib/</code> หรือ <code>/opt/flink/usrlib/</code></li><li>Custom Configuration ใน <code>/opt/flink/conf/flink-conf.yaml</code></li></ul><h2>Prometheus Monitoring</h2><p>Flink มี Metrics System ที่รองรับ <strong>Prometheus</strong> Reporter โดยตรง ตั้งค่าด้วย <code>metrics.reporter.prom.class</code> และ <code>metrics.reporter.prom.port</code> แล้ว Prometheus จะ Scrape Metrics ของ Flink ได้ Metrics สำคัญที่ควร Monitor ได้แก่ Checkpoint Duration, Checkpoint Size, Kafka Consumer Lag, Task Backpressure และ Memory Usage ของ TaskManager</p>' },
  { number: 9, slug: 'vs-spark', emoji: '⚖️', title: 'Flink vs Spark Streaming: เปรียบเทียบ', content: '<h2>Flink vs Spark Streaming</h2><p>การเปรียบเทียบระหว่าง <strong>Apache Flink</strong> และ <strong>Apache Spark Streaming</strong> (Structured Streaming) เป็นคำถามยอดนิยมในวงการ Data Engineering การเลือกใช้งานที่เหมาะสมต้องพิจารณาหลายปัจจัยตามลักษณะของ Use Case และ Requirement ขององค์กร</p><h2>Architecture ที่แตกต่างกัน</h2><h3>Spark Structured Streaming: Micro-batch</h3><p><strong>Spark Structured Streaming</strong> ใช้แนวคิด <em>Micro-batch Processing</em> โดยแบ่ง Stream ออกเป็น Batch เล็ก ๆ ทุก ๆ ช่วงเวลา (Trigger Interval) แล้วประมวลผลแต่ละ Batch ด้วย Spark Engine ปกติ ทำให้สามารถใช้ Spark SQL Optimizer และ DataFrame API ได้เต็มรูปแบบ แต่ Latency ขั้นต่ำอยู่ที่หลัก 100ms ถึง 1 วินาที</p><h3>Flink: True Streaming</h3><p><strong>Flink</strong> ใช้ <em>True Streaming Processing</em> โดยประมวลผล Event ทีละ Record ทันทีที่มาถึง ไม่มีการรอสะสม Batch ทำให้ Latency ได้ถึงระดับ Millisecond และ State จะถูก Update ทีละ Event ทำให้ผลลัพธ์ Real-time มากกว่า</p><h2>Latency เปรียบเทียบ</h2><ul><li><strong>Flink</strong> — End-to-end Latency ต่ำกว่า 10ms สำหรับ Simple Pipeline และ 100-500ms สำหรับ Complex Stateful Pipeline</li><li><strong>Spark Streaming</strong> — Minimum Latency ประมาณ 100ms-1s ขึ้นอยู่กับ Trigger Interval ที่กำหนด</li><li>สำหรับ Application ที่ Latency ไม่ใช่ Priority สูง ความแตกต่างนี้อาจไม่มีนัยสำคัญ</li></ul><h2>State Management เปรียบเทียบ</h2><p><strong>Flink</strong> มี State Management ที่ครบครันและยืดหยุ่นกว่ามาก ด้วย Keyed State ประเภทต่าง ๆ RocksDB Backend ที่รองรับ State ขนาดใหญ่ Incremental Checkpoint และ TTL ส่วน <strong>Spark Streaming</strong> มี StatefulDataset และ mapGroupsWithState แต่ยังมีข้อจำกัดในด้านความซับซ้อนของ State Logic และ State Size ที่จัดการได้</p><h2>Exactly-once Guarantees</h2><p>ทั้งสองรองรับ Exactly-once แต่ด้วย Approach ต่างกัน</p><ul><li><strong>Flink</strong> ใช้ Distributed Snapshot ร่วมกับ 2-Phase Commit สำหรับ Sink ทำให้ Exactly-once เป็นส่วนหนึ่งของ Core Architecture</li><li><strong>Spark Streaming</strong> ใช้ Idempotent Writes และ WAL (Write-Ahead Log) ซึ่งง่ายกว่าในการ Implement แต่ต้องการ Sink ที่รองรับ Idempotency</li></ul><h2>SQL Support เปรียบเทียบ</h2><p><strong>Spark</strong> มี SQL Support ที่แข็งแกร่งกว่าและ Mature กว่าผ่าน Spark SQL และ DataFrame API ซึ่ง Optimize ด้วย Catalyst Optimizer ส่วน <strong>Flink SQL</strong> พัฒนาขึ้นเรื่อย ๆ และรองรับ Streaming SQL ได้ดีกว่า Spark แต่ยังไม่มี Ecosystem ที่กว้างเท่า</p><h2>Ecosystem และ Community</h2><ul><li><strong>Spark</strong> — Ecosystem ใหญ่กว่ามาก Community ใหญ่กว่า Integration กับ Tools ในวงการ Data Science และ ML เช่น MLlib, PySpark ดีกว่า</li><li><strong>Flink</strong> — Community เล็กกว่าแต่เติบโตเร็ว Integration กับ Kafka ดีเยี่ยม ใช้ใน Production ของบริษัทใหญ่ระดับโลก</li></ul><h2>เมื่อไรควรเลือก Flink?</h2><ul><li>ต้องการ <strong>Low Latency</strong> ระดับ Millisecond</li><li>มี <strong>Complex Stateful Logic</strong> เช่น CEP หรือ Session Window</li><li>ต้องการ <strong>Exactly-once End-to-end</strong> กับ Kafka</li><li>ข้อมูลมี <strong>Out-of-order Event</strong> ที่ต้องจัดการด้วย Event Time</li></ul><h2>เมื่อไรควรเลือก Spark Streaming?</h2><ul><li>ทีมมีพื้นฐาน <strong>Spark</strong> อยู่แล้วและต้องการ Reuse Knowledge</li><li>ต้องการ <strong>ผสมผสาน Batch และ Streaming</strong> ใน Codebase เดียวกัน</li><li>ใช้ <strong>ML Model</strong> จาก MLlib หรือต้องการ Integration กับ Spark Ecosystem</li><li>Latency ระดับวินาทียอมรับได้</li></ul>' }
];
