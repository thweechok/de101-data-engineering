export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '📘',
    title: 'Overview - ทำไมต้อง TypeScript?',
    content: '<h2>บทนำ: ทำไมต้อง TypeScript? (Why TypeScript?)</h2> <p>ในยุคปัจจุบันของการพัฒนา Web Application ที่มีความซับซ้อนเพิ่มขึ้นอย่างก้าวกระโดด โค้ดเบสของโปรเจคสามารถขยายตัวจากหลักพันบรรทัดเป็นหลักแสนหรือหลักล้านบรรทัดได้อย่างรวดเร็ว ภาษา JavaScript (JS) ซึ่งเป็นภาษาหลักของการพัฒนาเว็บไซต์ มีความยืดหยุ่นสูงด้วยธรรมชาติของความเป็น <strong>Dynamic Typing</strong> แต่นั่นกลับกลายเป็นดาบสองคม เมื่อตัวแปรสามารถเปลี่ยนประเภทข้อมูลได้ตลอดเวลา ส่งผลให้เกิดบั๊ก (Bugs) ที่ซ่อนเร้นและมักจะแสดงอาการก็ต่อเมื่อโปรแกรมกำลังทำงาน (Runtime) ซึ่งบางครั้งอาจทำให้ระบบพังบน Production ได้เลย</p> <p>นี่คือจุดที่ <strong>TypeScript (TS)</strong> ซึ่งถูกพัฒนาโดย Microsoft ก้าวเข้ามาเป็นฮีโร่ TypeScript ถูกออกแบบมาให้เป็น <strong>Superset ของ JavaScript</strong> หมายความว่าโค้ด JS เดิมของคุณสามารถทำงานบน TS ได้ 100% แต่สิ่งที่ TS เพิ่มเข้ามาคือระบบ <strong>Static Typing</strong> ที่อนุญาตให้คุณระบุชนิดข้อมูล (Type) อย่างชัดเจนตั้งแต่ขั้นตอนการเขียนโค้ด ทำให้เครื่องมือและ Compiler สามารถตรวจสอบความถูกต้องและแจ้งเตือนข้อผิดพลาดให้คุณทราบก่อนที่คุณจะรันโปรแกรมเสียอีก</p>  <h2>ความแตกต่างที่สำคัญระหว่าง JavaScript และ TypeScript</h2> <p>เพื่อทำความเข้าใจให้เห็นภาพชัดเจนที่สุด ลองพิจารณาตัวอย่างโค้ดด้านล่างนี้:</p> <pre><code>// JavaScript ปกติ function calculateTotal(price, taxRate) {   return price + (price * taxRate); }  const total = calculateTotal(100, "0.07"); // เผลอส่ง String เข้าไป console.log(total); // ผลลัพธ์: "1000.07" (กลายเป็นการต่อ String แทนการบวกเลข) </code></pre> <p>ในตัวอย่างข้างต้น JavaScript จะพยายามทำการแปลงประเภทข้อมูลให้โดยอัตโนมัติ (Type Coercion) ทำให้ผลลัพธ์ผิดเพี้ยน แต่ถ้าเป็น TypeScript เราสามารถป้องกันปัญหานี้ได้ตั้งแต่เนิ่นๆ:</p> <pre><code>// TypeScript function calculateTotal(price: number, taxRate: number): number {   return price + (price * taxRate); }  // คอมไพเลอร์จะแจ้งเตือนทันที: Argument of type \'string\' is not assignable to parameter of type \'number\'. const total = calculateTotal(100, "0.07");  </code></pre>  <h2>เหตุผลหลักที่คุณต้องเริ่มใช้ TypeScript ในปี 2025</h2> <ol style="padding-left:20px">   <li><strong>ดักจับข้อผิดพลาดก่อนเกิดความเสียหายจริง (Catching Errors at Compile Time):</strong> จากสถิติพบว่า TypeScript สามารถดักจับบั๊กได้ถึง 15% ตั้งแต่ตอนคอมไพล์ ซึ่งช่วยลดเวลาที่ต้องเสียไปกับการ Debug ในช่วง Runtime ได้อย่างมหาศาล</li>   <li><strong>ประสบการณ์การพัฒนาที่เหนือกว่า (Better Developer Experience & IDE Support):</strong> เมื่อคุณกำหนด Types อย่างชัดเจน Editor อย่าง VS Code จะสามารถทำ Autocomplete (IntelliSense) ได้อย่างแม่นยำ คุณจะรู้ได้ทันทีว่า Object หนึ่งๆ มี Properties หรือ Methods อะไรให้เรียกใช้บ้าง โดยไม่ต้องเดาหรือเปิดดูคู่มือตลอดเวลา</li>   <li><strong>โค้ดที่อธิบายตัวเองได้ (Self-documenting Code):</strong> การกำหนด Type ถือเป็นการเขียน Documentation ไปในตัว นักพัฒนาคนอื่น (หรือแม้แต่ตัวคุณเองในอีก 6 เดือนข้างหน้า) สามารถเข้าใจโครงสร้างข้อมูลและจุดประสงค์ของฟังก์ชันได้ง่ายขึ้น</li>   <li><strong>การ Refactor โค้ดที่ปลอดภัย (Safe Refactoring):</strong> เมื่อโปรเจคมีขนาดใหญ่ การเปลี่ยนชื่อตัวแปร หรือการเปลี่ยนโครงสร้าง API มักเป็นเรื่องน่าปวดหัว แต่ด้วย TypeScript เมื่อคุณเปลี่ยน Interface จุดอื่นๆ ในโปรเจคที่ใช้ Interface นั้นจะเกิด Error ทันที ช่วยชี้เป้าให้คุณตามไปแก้ไขได้อย่างครบถ้วนและปลอดภัย</li> </ol>  <div class="tip-box">   <strong>💡 Pro Tip</strong>: นักพัฒนาหลายคนอาจรู้สึกต้านทานในช่วงแรกเพราะต้องเขียนโค้ดเพิ่มขึ้น (Boilerplate) แต่เมื่อโปรเจคเริ่มซับซ้อนขึ้น คุณจะพบว่าเวลาที่เสียไปกับการเขียน Types นั้นน้อยกว่าเวลาที่ต้องเสียไปกับการงมหาบั๊กที่เกิดจาก Type Mismatch อย่างเทียบกันไม่ได้เลย </div>  <h2>TypeScript ในบริบทของปี 2025</h2> <p>ในปัจจุบัน TypeScript ไม่ได้เป็นเพียงแค่ "ทางเลือก" อีกต่อไป แต่กลายเป็น <strong>Industry Standard</strong> ของการพัฒนาเว็บ Frontend Frameworks ยอดนิยมไม่ว่าจะเป็น React (ผ่าน Next.js), Vue (ผ่าน Nuxt), หรือ Angular รวมถึง Backend Frameworks อย่าง NestJS ต่างก็สร้างขึ้นมาพร้อมกับการรองรับ TypeScript เป็น First-class citizen แทบทุกโปรเจคใหม่ในองค์กรขนาดกลางถึงขนาดใหญ่มักจะเริ่มต้นด้วย TypeScript ทันที การมีความรู้และทักษะด้าน TS จึงเป็นสิ่งที่ขาดไม่ได้สำหรับนักพัฒนาที่ต้องการความก้าวหน้าในสายอาชีพนี้</p> <p>แม้ว่าการเรียนรู้ TypeScript จะมี Learning Curve และอาจเพิ่มเวลาในกระบวนการ Build เล็กน้อย แต่มูลค่าที่มันมอบให้กับโปรเจคในระยะยาว ไม่ว่าจะเป็นความเสถียร ความเร็วในการทำความเข้าใจโค้ด และความมั่นใจในการ Deploy ล้วนทำให้การลงทุนเวลากับ TypeScript เป็นสิ่งคุ้มค่าที่สุดในฐานะนักพัฒนา</p>'
  },
  {
    number: 1,
    slug: 'basic-types',
    emoji: '📊',
    title: 'Types เบื้องต้น',
    content: '<h2>Primitive Types ใน TypeScript</h2> <p>Primitive Types คือประเภทข้อมูลพื้นฐานที่สุดใน JavaScript ซึ่ง TypeScript ได้เพิ่มความสามารถในการตรวจสอบ Type เหล่านี้ให้เข้มงวดขึ้น ข้อมูลพื้นฐานเหล่านี้ประกอบไปด้วย:</p> <ul>   <li><strong>string:</strong> สำหรับข้อความ (Text) เช่น <code>let name: string = \'John\';</code></li>   <li><strong>number:</strong> สำหรับตัวเลขทั้งหมด ทั้งจำนวนเต็มและทศนิยม เช่น <code>let age: number = 30;</code> หรือ <code>let pi: number = 3.14;</code></li>   <li><strong>boolean:</strong> สำหรับค่าความจริง <code>true</code> หรือ <code>false</code> เช่น <code>let isActive: boolean = true;</code></li>   <li><strong>null และ undefined:</strong> ใช้สำหรับระบุว่าไม่มีค่าใดๆ ซึ่งความเข้มงวดจะขึ้นอยู่กับการตั้งค่า <code>strictNullChecks</code> ใน <code>tsconfig.json</code></li>   <li><strong>symbol และ bigint:</strong> สำหรับค่าที่ไม่ซ้ำกัน และตัวเลขขนาดใหญ่ตามลำดับ</li> </ul> <p>ข้อดีอย่างหนึ่งของ TypeScript คือ <strong>Type Inference</strong> หมายความว่าหากเรากำหนดค่าให้ตัวแปรทันที TS จะเดา Type ให้เราโดยอัตโนมัติ เช่น <code>let score = 100;</code> TS จะรู้ทันทีว่า <code>score</code> เป็น <code>number</code> โดยที่เราไม่ต้องระบุ</p>  <h2>Arrays และ Tuples</h2> <p>เมื่อต้องทำงานกับกลุ่มข้อมูล เราสามารถระบุ Type ให้กับ Array ได้สองรูปแบบหลักๆ คือ การเติม <code>[]</code> ไว้หลัง Type หรือใช้ Generic <code>Array&lt;Type&gt;</code></p> <pre><code>// ทั้งสองแบบนี้มีความหมายเหมือนกัน let scores: number[] = [85, 92, 78]; let names: Array&lt;string&gt; = [\'Alice\', \'Bob\', \'Charlie\']; </code></pre> <p>นอกเหนือจาก Array ทั่วไป TypeScript ยังนำเสนอโครงสร้างแบบ <strong>Tuple</strong> ซึ่งก็คือ Array ที่ระบุจำนวนสมาชิกและ Type ของแต่ละตำแหน่งไว้อย่างชัดเจน มักใช้สำหรับจัดกลุ่มข้อมูลที่มีความเกี่ยวข้องกันในรูปแบบสั้นๆ</p> <pre><code>// Tuple ที่ตำแหน่งแรกต้องเป็น string และตำแหน่งสองต้องเป็น number let userRecord: [string, number] = [\'Admin\', 101];  // หากพยายามสลับตำแหน่งหรือเพิ่มสมาชิก จะเกิด Error ทันที // userRecord = [101, \'Admin\']; // Error! </code></pre>  <h2>Enums (Enumerations)</h2> <p>Enum เป็นฟีเจอร์ที่ไม่มีใน JS แต่ TS สร้างขึ้นมาเพื่อช่วยจัดการกับกลุ่มของค่าคงที่ (Constants) ที่มีความเกี่ยวข้องกัน ทำให้โค้ดอ่านง่ายและสื่อความหมายได้ดีขึ้น</p> <pre><code>enum OrderStatus {   Pending,    // 0   Processing, // 1   Shipped,    // 2   Delivered   // 3 }  let myOrder: OrderStatus = OrderStatus.Shipped; console.log(myOrder); // จะได้ค่า 2 </code></pre> <p>นอกจาก Numeric Enums แล้ว เรายังสามารถสร้าง String Enums ซึ่งมีประโยชน์มากเมื่อต้องการเก็บข้อมูลลงฐานข้อมูลหรือสื่อสารผ่าน API เป็นข้อความที่มนุษย์อ่านเข้าใจ</p> <pre><code>enum Role {   Admin = \'ADMIN\',   User = \'USER\',   Guest = \'GUEST\' } </code></pre>  <h2>Any vs Unknown</h2> <p>Type <code>any</code> ถือเป็น "ทางออกฉุกเฉิน" ที่จะบอกให้ TS หยุดตรวจสอบ Type ของตัวแปรนี้ ซึ่งการใช้ <code>any</code> บ่อยๆ จะทำให้สูญเสียประโยชน์ของการใช้ TypeScript ไปโดยสิ้นเชิง</p> <p>ในทางกลับกัน Type <code>unknown</code> เป็นทางเลือกที่ปลอดภัยกว่า เมื่อเราไม่ทราบ Type ของข้อมูลที่รับมาล่วงหน้า (เช่น ข้อมูลจาก API ของบุคคลที่สาม) ตัวแปรแบบ <code>unknown</code> จะบังคับให้เราต้องทำการตรวจสอบ Type (Type Checking หรือ Type Narrowing) ก่อนจึงจะสามารถนำไปใช้งานต่อได้</p> <pre><code>let data: unknown = fetchData();  // data.toUpperCase(); // Error! เพราะ TS ยังไม่มั่นใจว่า data เป็น string หรือไม่  if (typeof data === \'string\') {   console.log(data.toUpperCase()); // ทำงานได้ปกติ เพราะเรายืนยัน Type แล้ว } </code></pre>  <h2>Never และ Void</h2> <p>สอง Type นี้มักใช้เป็น Return Type ของฟังก์ชัน:</p> <ul>   <li><strong>Void:</strong> หมายถึงฟังก์ชันนั้นไม่คืนค่าใดๆ กลับมา (ไม่ใช้คำสั่ง <code>return</code>) เช่น ฟังก์ชันที่ทำหน้าที่พิมพ์ข้อความลง Console อย่างเดียว</li>   <li><strong>Never:</strong> หมายถึงฟังก์ชันนั้นจะไม่มีวันทำงานจนจบ (Never returns) มักใช้กับฟังก์ชันที่ทำการ <code>throw Error</code> เสมอ หรือเป็น <code>while (true)</code> แบบไม่มีที่สิ้นสุด</li> </ul>  <h2>Type Assertions และ as const</h2> <p>บางครั้งนักพัฒนาอาจรู้ข้อมูลเกี่ยวกับ Type ดีกว่าที่ Compiler รู้ เราสามารถใช้ Type Assertions ผ่านคีย์เวิร์ด <code>as</code> เพื่อบังคับ (Cast) ให้ TS มองตัวแปรนั้นเป็น Type ที่เราต้องการ</p> <pre><code>let someValue: unknown = "this is a string"; // เรามั่นใจว่าเป็น string แน่นอน จึงใช้ as string เพื่อเรียกใช้ property .length ได้ let strLength: number = (someValue as string).length; </code></pre> <div class="tip-box">   <strong>💡 Tip - "as const"</strong>: เป็นฟีเจอร์ที่เจ๋งมากเมื่อคุณต้องการสร้าง Object หรือ Array ที่เป็น Immutable (แก้ไขไม่ได้) และต้องการให้ TS ล็อก Type ให้แคบที่สุดเท่าที่จะเป็นไปได้   <br><code>const config = { method: \'GET\', url: \'/api\' } as const;</code>   <br>วิธีนี้จะทำให้ TS มองว่า method คือ \'GET\' (Literal type) ไม่ใช่แค่ string ธรรมดา </div>'
  },
  {
    number: 2,
    slug: 'interfaces',
    emoji: '📋',
    title: 'Interfaces และ Type Aliases',
    content: '<h2>ทำความรู้จัก Interface และ Type Aliases</h2> <p>ในการเขียน TypeScript การกำหนดโครงสร้างของ Object ถือเป็นงานที่ทำบ่อยที่สุด ซึ่ง TS มีสองเครื่องมือหลักให้เราใช้งานคือ <strong>Interface</strong> และ <strong>Type Aliases</strong> ทั้งสองอย่างมีหน้าที่คล้ายกันมาก แต่ก็มีข้อแตกต่างที่สำคัญในรายละเอียดของการนำไปใช้งาน</p>  <h3>Interface: พิมพ์เขียวสำหรับ Object</h3> <p><code>interface</code> มักถูกนำมาใช้เพื่อประกาศโครงสร้าง (Shape) ของ Object อย่างชัดเจน นิยมใช้อย่างแพร่หลายเมื่อต้องทำงานกับ Classes หรือเมื่อต้องกำหนดรูปแบบของ Props/State ในโปรเจค</p> <pre><code>interface User {   id: number;   username: string;   email: string;   isActive: boolean; }  const currentUser: User = {   id: 1,   username: \'john_doe\',   email: \'john@example.com\',   isActive: true }; </code></pre>  <h3>Type Aliases: ความยืดหยุ่นที่ครอบคลุม</h3> <p>ในขณะที่ <code>interface</code> จำกัดเฉพาะรูปร่างของ Object <code>type</code> นั้นมีความสามารถครอบคลุมกว่า เพราะสามารถใช้ตั้งชื่อใหม่ (Alias) ให้กับ Primitive Types, Union, หรือ Tuple ได้ด้วย</p> <pre><code>// สร้าง Type ใหม่ที่รวมค่าที่เป็นไปได้หลายแบบ type ID = number | string; type UserStatus = \'active\' | \'suspended\' | \'deleted\'; // Literal Types </code></pre>  <h2>Extending Interfaces vs Intersection Types</h2> <p>เมื่อโปรเจคขยายใหญ่ขึ้น เรามักต้องการสร้าง Type ใหม่ที่มีพื้นฐานมาจาก Type เดิม</p> <p><strong>การขยาย Interface (Extending Interfaces)</strong> ใช้คีย์เวิร์ด <code>extends</code> ซึ่งทำงานคล้ายกับการสืบทอด (Inheritance) ใน Object-Oriented Programming โค้ดจะอ่านง่ายและคอมไพเลอร์สามารถแสดง Error Message ที่ชัดเจนกว่า</p> <pre><code>interface BaseEntity {   createdAt: Date;   updatedAt: Date; }  interface Product extends BaseEntity {   sku: string;   price: number; } </code></pre> <p><strong>การรวม Type (Intersection Types)</strong> ใช้สัญลักษณ์ <code>&amp;</code> เพื่อนำหลายๆ Types มารวมเป็น Type เดียว ซึ่งสามารถใช้รวม <code>type</code> หรือ <code>interface</code> เข้าด้วยกันได้อย่างอิสระ</p> <pre><code>type Address = { street: string; city: string }; type Contact = { phone: string; email: string }; type EmployeeInfo = Address &amp; Contact; // ต้องมี Properties จากทั้งสอง Type </code></pre>  <h2>Interface vs Type: เลือกใช้อย่างไรดี?</h2> <div class="tip-box">   <strong>💡 ข้อเสนอแนะในการเลือกใช้:</strong>   <ul>     <li>เริ่มใช้ <code>interface</code> เป็นค่าเริ่มต้นเสมอเมื่อต้องกำหนดโครงสร้างของ Object เพราะประสิทธิภาพในการประมวลผลของ Compiler (Type checking performance) ในโปรเจคขนาดใหญ่มักจะดีกว่า</li>     <li>ใช้ <code>interface</code> เมื่อคุณสร้าง Library สาธารณะ เนื่องจากผู้ใช้งานสามารถทำ <strong>Declaration Merging</strong> (เพิ่ม Properties ใหม่เข้าไปใน Interface เดิมโดยไม่ต้องแก้ไขโค้ดต้นฉบับ) ได้</li>     <li>เปลี่ยนไปใช้ <code>type</code> เมื่อคุณต้องการใช้ฟีเจอร์อย่าง Union (<code>|</code>), Intersection (<code>&amp;</code>), Mapped Types หรือ Conditional Types ที่ <code>interface</code> ไม่สามารถทำได้</li>   </ul> </div>  <h2>Union Types และ Discriminated Unions</h2> <p><strong>Union Types</strong> (ใช้สัญลักษณ์ <code>|</code>) อนุญาตให้ตัวแปรสามารถเก็บข้อมูลได้มากกว่าหนึ่ง Type ถือเป็นเครื่องมือที่ทรงพลังมากในการจัดการความไม่แน่นอนของข้อมูล</p> <pre><code>function printId(id: number | string) {   if (typeof id === \'string\') {     console.log(id.toUpperCase());   } else {     console.log(id);   } } </code></pre> <p>ก้าวไปอีกขั้นกับ <strong>Discriminated Unions</strong> รูปแบบที่นิยมมากในการจัดการ State หรือ Events ที่ซับซ้อน โดยอาศัยการใส่ Property พิเศษ (มักเรียกว่า Tag หรือ Discriminator) ไว้ในทุกๆ Type เพื่อให้ TS แยกแยะได้ว่ากำลังทำงานกับ Object รูปแบบใด</p> <pre><code>interface Square {   kind: \'square\'; // Tag   size: number; } interface Circle {   kind: \'circle\'; // Tag   radius: number; } type Shape = Square | Circle;  function getArea(shape: Shape) {   switch (shape.kind) { // TS จะทำการ Type Narrowing ให้อัตโนมัติ     case \'square\':       return shape.size * shape.size;     case \'circle\':       return Math.PI * shape.radius ** 2;   } } </code></pre> <p>ด้วยวิธีนี้ หากเราเพิ่ม <code>Triangle</code> เข้าไปใน <code>Shape</code> แต่ลืมเขียนกรณี <code>case \'triangle\'</code> ในฟังก์ชัน TS จะสามารถแจ้งเตือน (Exhaustiveness checking) ให้เราทราบได้ทันที ซึ่งช่วยป้องกันบั๊กได้อย่างดีเยี่ยม</p>  <h2>Optional Properties (Property ที่มีหรือไม่มีก็ได้)</h2> <p>ในหลายครั้ง ข้อมูลใน Object ไม่จำเป็นต้องมีครบทุกฟิลด์ เราสามารถใส่เครื่องหมายคำถาม <code>?</code> ต่อท้ายชื่อ Property เพื่อระบุว่าข้อมูลนี้เป็น <strong>Optional</strong> ได้</p> <pre><code>interface UserProfile {   id: number;   name: string;   bio?: string; // อาจจะ undefined ได้   avatarUrl?: string; }  const user: UserProfile = { id: 1, name: \'Alice\' }; // ไม่ต้องส่ง bio หรือ avatarUrl ก็ผ่าน </code></pre> <p>การใช้ Optional properties ช่วยให้ฟังก์ชันหรือ Components ของเรามีความยืดหยุ่นสูงขึ้น แต่ก็ต้องระวังเรื่องการจัดการกรณีที่ค่าเป็น <code>undefined</code> ในภายหลังด้วยการใช้ Optional Chaining (<code>?.</code>) หรือ Nullish Coalescing (<code>??</code>) ใน JavaScript</p>'
  },
  {
    number: 3,
    slug: 'generics',
    emoji: '🧲',
    title: 'Generics: พลังแห่งความยืดหยุ่น',
    content: '<h2>Generics คืออะไร?</h2> <p>ถ้าให้เปรียบเทียบ <strong>Generics</strong> ก็เหมือนกับการสร้าง "ตัวแปรสำหรับ Type" แทนที่เราจะล็อคตายตัวว่าฟังก์ชันนี้ต้องรับค่าเป็น <code>number</code> หรือ <code>string</code> เท่านั้น เราสามารถเขียนฟังก์ชันที่สามารถทำงานได้กับ <strong>ทุก Type</strong> โดยที่ยังคงความปลอดภัยแบบ Type-safe เอาไว้ได้ นี่คือหนึ่งในฟีเจอร์ที่ทรงพลังที่สุดใน TypeScript</p>  <h2>Generic Functions</h2> <p>มาดูตัวอย่างฟังก์ชันแบบธรรมดากันก่อน:</p> <pre><code>function identity(arg: any): any {   return arg; } </code></pre> <p>การใช้ <code>any</code> จะทำให้เราสูญเสียข้อมูล Type ขาออกไป (เราไม่รู้ว่าค่าที่ return ออกมาเป็น Type อะไร) แต่ถ้าเราใช้ Generics <code>&lt;T&gt;</code> (Type Variable):</p> <pre><code>function identity&lt;T&gt;(arg: T): T {   return arg; }  let output1 = identity&lt;string&gt;("myString"); // output1 เป็น string let output2 = identity(100); // TS ฉลาดพอที่จะอนุมาน (Infer) ว่า output2 เป็น number </code></pre>  <h2>Generic Interfaces และ Classes</h2> <p>เราสามารถนำ Generics ไปใช้กับ Interface เพื่อสร้างโครงสร้างข้อมูลที่นำไปใช้ซ้ำได้ (Reusable Data Structures) เช่น โครงสร้าง Response จาก API</p> <pre><code>interface ApiResponse&lt;T&gt; {   status: number;   message: string;   data: T; }  const userResponse: ApiResponse&lt;{ name: string }&gt; = {   status: 200,   message: "Success",   data: { name: "Alice" } }; </code></pre> <p>สำหรับ <strong>Generic Classes</strong> ก็มีหลักการเดียวกัน ซึ่งเป็นประโยชน์มากในการสร้างคลาสจำพวก Data Collections (เช่น Stack, Queue, หรือ List)</p> <pre><code>class GenericNumber&lt;T&gt; {   zeroValue: T;   add: (x: T, y: T) =&gt; T; } </code></pre>  <h2>Constraints (การตีกรอบ Generics)</h2> <p>บางครั้งการเปิดกว้างให้ <code>T</code> เป็นอะไรก็ได้อาจจะกว้างเกินไป เราสามารถใช้ <code>extends</code> เพื่อจำกัดว่า <code>T</code> ต้องมีหน้าตาพื้นฐานอย่างไร (Constraints)</p> <pre><code>interface Lengthwise {   length: number; }  // T ต้องเป็นข้อมูลที่มี property .length เท่านั้น (เช่น string หรือ array) function loggingIdentity&lt;T extends Lengthwise&gt;(arg: T): T {   console.log(arg.length);    return arg; }  loggingIdentity("hello"); // ผ่าน เพราะ string มี .length // loggingIdentity(10); // Error: Argument of type \'number\' is not assignable to parameter of type \'Lengthwise\'. </code></pre>  <h2>Conditional Types และ infer keyword</h2> <p><strong>Conditional Types</strong> ทำงานคล้ายกับ Ternary Operator (<code>condition ? true : false</code>) แต่ทำในระดับของ Types:</p> <pre><code>// ถ้า T เป็น string ให้ประเมินเป็น string, ถ้าไม่ ให้ประเมินเป็น number type IsString&lt;T&gt; = T extends string ? string : number; type Result1 = IsString&lt;"Hello"&gt;; // string type Result2 = IsString&lt;100&gt;; // number </code></pre> <p>คีย์เวิร์ด <code>infer</code> มักใช้คู่กับ Conditional Types เพื่อ "สกัด (Extract)" Type ย่อยออกมาจากโครงสร้างที่ซับซ้อน เช่น การดึง Return Type ออกมาจากฟังก์ชัน</p> <pre><code>type GetReturnType&lt;Type&gt; = Type extends (...args: never[]) =&gt; infer Return   ? Return   : never;  type MyFunc = () =&gt; string; type ExtractedType = GetReturnType&lt;MyFunc&gt;; // ได้ผลลัพธ์เป็น string </code></pre>  <h2>Mapped Types</h2> <p><strong>Mapped Types</strong> เป็นวิธียอดฮิตในการสร้าง Type ใหม่โดยการลูป (Loop) ทะลุ Properties ของ Type เดิม เหมาะมากสำหรับทำ Utility Types</p> <pre><code>type OptionsFlags&lt;Type&gt; = {   [Property in keyof Type]: boolean; // เปลี่ยน Type ของทุกๆ Properties ให้เป็น boolean };  interface FeatureFlags {   darkMode: () =&gt; void;   newUserProfile: () =&gt; void; }  type FeatureOptions = OptionsFlags&lt;FeatureFlags&gt;; // ผลลัพธ์: { darkMode: boolean; newUserProfile: boolean; } </code></pre>'
  },
  {
    number: 4,
    slug: 'advanced-types',
    emoji: '⚡',
    title: 'Advanced Types และ Utility Types',
    content: '<h2>Utility Types: ตัวช่วยลดเวลาในการเขียน Types</h2> <p>TypeScript มาพร้อมกับ <strong>Utility Types</strong> ในตัว (Built-in) ที่ถูกสร้างขึ้นมาเพื่อให้เราสามารถแปลง (Transform) Type เดิมให้เป็นรูปร่างใหม่ได้อย่างรวดเร็ว โดยที่เราไม่ต้องไปเขียน Type ใหม่ตั้งแต่ต้นทุกครั้ง</p>  <h3>1. Partial และ Required</h3> <ul>   <li><code>Partial&lt;T&gt;</code>: จะแปลงทุกๆ Properties ใน Type T ให้กลายเป็น Optional (มีหรือไม่มีก็ได้) เหมาะกับการใช้สร้าง Type สำหรับฟังก์ชัน Update/Edit</li>   <li><code>Required&lt;T&gt;</code>: ตรงข้ามกับ Partial มันจะบังคับให้ทุกๆ Properties ใน Type T ต้องมีค่าเสมอ (ลบเครื่องหมาย <code>?</code> ออกทั้งหมด)</li> </ul> <pre><code>interface Todo {   title: string;   description: string; }  function updateTodo(todo: Todo, fieldsToUpdate: Partial&lt;Todo&gt;) {   return { ...todo, ...fieldsToUpdate }; } </code></pre>  <h3>2. Pick และ Omit</h3> <ul>   <li><code>Pick&lt;T, K&gt;</code>: เป็นการ "หยิบ" เฉพาะ Properties ที่ต้องการออกมาจาก Type T</li>   <li><code>Omit&lt;T, K&gt;</code>: เป็นการ "คัดออก" Properties ที่ไม่ต้องการออกจาก Type T</li> </ul> <pre><code>interface User {   id: number;   name: string;   email: string;   passwordHash: string; }  // เอาแค่ id และ name ไปแสดงบนหน้าจอ type UserPreview = Pick&lt;User, "id" | "name"&gt;;  // ส่งข้อมูล User กลับไปให้ Client โดยต้องไม่หลุดรหัสผ่าน type PublicUser = Omit&lt;User, "passwordHash"&gt;; </code></pre>  <h3>3. Record</h3> <p><code>Record&lt;Keys, Type&gt;</code> ใช้สำหรับการสร้าง Object Type ที่ต้องการกำหนดโครงสร้างของ Key และ Value ให้ชัดเจน เป็นวิธีที่กระชับและอ่านง่ายกว่าการเขียนแบบ Index Signature (<code>[key: string]: Type</code>)</p> <pre><code>type PageInfo = {   title: string; } type Page = "home" | "about" | "contact";  const nav: Record&lt;Page, PageInfo&gt; = {   home: { title: "Home Page" },   about: { title: "About Us" },   contact: { title: "Contact Us" } }; </code></pre>  <h3>4. Readonly</h3> <p><code>Readonly&lt;T&gt;</code> จะป้องกันไม่ให้เราสามารถแก้ไข (Re-assign) ค่าของ Properties ใน Object ได้อีกหลังจากที่ถูกกำหนดค่าครั้งแรก ช่วยลดปัญหา Side-effects จากการเปลี่ยนแปลงข้อมูลโดยไม่ตั้งใจ</p>  <h3>5. ReturnType และ Parameters</h3> <ul>   <li><code>ReturnType&lt;T&gt;</code>: สกัดเอา Type ที่ถูก return กลับมาจากฟังก์ชัน</li>   <li><code>Parameters&lt;T&gt;</code>: สกัดเอา Array ของ Types ที่ฟังก์ชันนั้นรับเป็น Parameters</li> </ul> <pre><code>function createPerson(name: string, age: number) {   return { id: 1, name, age }; }  // ไม่ต้องไปนั่งเดาว่าฟังก์ชันนี้ return อะไร type Person = ReturnType&lt;typeof createPerson&gt;;  </code></pre>  <h2>Template Literal Types</h2> <p>ใน ES6 เรามี Template Literals (<code>``</code>) ที่ใช้สร้าง String แบบ Dynamic สำหรับ TypeScript แล้ว เราก็มี <strong>Template Literal Types</strong> ที่ทรงพลังไม่แพ้กัน มันสามารถประกอบร่าง String Types ขึ้นมาใหม่ได้</p> <pre><code>type World = "world"; type Greeting = `hello ${World}`; // type Greeting = "hello world"  type CSSUnit = "px" | "em" | "rem"; type Size = `10${CSSUnit}`; // "10px" | "10em" | "10rem" </code></pre> <p>นอกจากนี้ยังมี Utilities สำหรับ String โดยเฉพาะ เช่น <code>Uppercase&lt;StringType&gt;</code>, <code>Lowercase&lt;StringType&gt;</code>, <code>Capitalize&lt;StringType&gt;</code> ซึ่งช่วยแปลงรูปร่างของข้อความได้ในระดับของ Type เลย!</p> <div class="tip-box">   <strong>💡 Pro Tip</strong>: การประยุกต์ใช้ Advanced Types และ Utility Types ร่วมกัน สามารถสร้าง Type System ที่ฉลาดมากๆ ได้ เช่น การเขียน Type ผูกกับระบบ Routing หรือ การจัดการ State ใน Redux ทำให้มี Autocomplete แบบ Magic อย่างไม่น่าเชื่อ </div>'
  },
  {
    number: 5,
    slug: 'classes',
    emoji: '🏗️',
    title: 'Classes ใน TypeScript',
    content: '<h2>การเขียน Classes ขั้นกว่าด้วย TypeScript</h2> <p>แม้ว่า JavaScript ในมาตรฐาน ES6 จะรองรับคีย์เวิร์ด <code>class</code> อย่างเป็นทางการแล้ว แต่โครงสร้างพื้นฐานยังมีข้อจำกัดเรื่องความเป็นส่วนตัวของข้อมูล (Data Privacy) TypeScript จึงได้เสริมพลังให้กับ Classes ด้วย <strong>Access Modifiers</strong>, ฟีเจอร์ขั้นสูงอย่าง <strong>Abstract Classes</strong> และอื่นๆ ที่ทำให้นักพัฒนาที่คุ้นเคยกับภาษาอย่าง Java หรือ C# รู้สึกเหมือนอยู่บ้าน</p>  <h2>Access Modifiers (public, private, protected)</h2> <p>Modifiers เหล่านี้ช่วยกำหนดขอบเขตว่าใครบ้างที่จะสามารถเข้าถึง Properties และ Methods ในคลาสได้:</p> <ul>   <li><strong>public:</strong> (ค่าเริ่มต้น) สามารถถูกเข้าถึงได้จากทุกที่อย่างอิสระ</li>   <li><strong>private:</strong> สามารถเข้าถึงได้เฉพาะ "ภายในคลาสที่มันถูกประกาศ" เท่านั้น ไม่สามารถเรียกจากภายนอกคลาส หรือแม้แต่จากคลาสลูกที่สืบทอดไป (Subclass)</li>   <li><strong>protected:</strong> คล้ายกับ private คือไม่ให้คนภายนอกเข้าถึง แต่ "อนุญาตให้คลาสลูก (Subclass) ที่ <code>extends</code> ไป สามารถเรียกใช้ได้"</li> </ul> <pre><code>class Employee {   public name: string;   private salary: number;   protected department: string;    constructor(name: string, salary: number, department: string) {     this.name = name;     this.salary = salary;     this.department = department;   }    public getSalaryInfo(): string {     return `${this.name} earns ${this.salary}`; // เข้าถึง private ได้เพราะอยู่ในคลาส   } }  const emp = new Employee("John", 50000, "IT"); console.log(emp.name); // ผ่าน (public) // console.log(emp.salary); // Error! เข้าถึงไม่ได้เพราะเป็น private // console.log(emp.department); // Error! เข้าถึงไม่ได้จากข้างนอกเพราะเป็น protected </code></pre>  <h2>Constructor Shorthand (Parameter Properties)</h2> <p>จากตัวอย่าง <code>Employee</code> ด้านบน คุณจะเห็นว่าเราต้องประกาศ Property แจ้ง Type จากนั้นก็ไปเขียนรับค่าใน <code>constructor</code> แล้วค่อยมาทำ <code>this.name = name;</code> ซึ่งมันซ้ำซ้อน (Boilerplate) มาก TypeScript จึงมี <strong>Parameter Properties</strong> ซึ่งช่วยให้คุณเขียนแบบนี้ได้ในบรรทัดเดียว:</p> <pre><code>class ModernEmployee {   // การใส่ public/private/protected หน้าตัวแปรใน constructor    // TS จะสร้างและกำหนดค่าเข้า Property ให้อัตโนมัติ!   constructor(     public name: string,     private salary: number,     protected department: string   ) {} } </code></pre> <div class="tip-box">   <strong>💡 Tip - Readonly Modifier</strong>: หากคุณมีค่าที่ไม่ควรถูกแก้ไขเลยหลังจากสร้าง Instance ขึ้นมา คุณสามารถใส่ <code>readonly</code> คู่กับ Modifier อื่นๆ ได้ เช่น <code>constructor(public readonly id: number) {}</code> </div>  <h2>Abstract Classes</h2> <p><strong>Abstract Class</strong> ทำหน้าที่เป็น "พิมพ์เขียวหลัก (Base Class)" ที่ไม่สามารถสร้างเป็น Object ได้โดยตรง (ห้ามใช้ <code>new</code>) จุดประสงค์หลักคือบังคับให้คลาสลูกที่สืบทอดไป ต้องนำ <strong>Abstract Methods</strong> ไปเขียนการทำงานจริง (Implementation) ให้ครบถ้วน</p> <pre><code>abstract class Animal {   constructor(public name: string) {}    // Abstract Method ไม่มีเนื้อหาข้างใน บังคับให้คลาสลูกต้องมีฟังก์ชันนี้   abstract makeSound(): void;    // เมธอดปกติ สามารถเขียนลอจิกให้คลาสลูกเอาไปใช้ได้เลย   move(): void {     console.log("Moving along...");   } }  class Dog extends Animal {   makeSound(): void {     console.log("Woof! Woof!"); // ถ้าลืมสร้างฟังก์ชันนี้ TS จะแจ้ง Error    } }  const myDog = new Dog("Buddy"); // const myAnimal = new Animal("Unkown"); // Error! ไม่สามารถ instantiate abstract class ได้ </code></pre>  <h2>Decorators (ภาพรวม)</h2> <p><strong>Decorators</strong> (สัญลักษณ์ <code>@</code>) เป็นฟีเจอร์ขั้นสูงที่อนุญาตให้เราแนบ Logic เสริมเข้าไปที่ Classes, Methods หรือ Properties ได้โดยตรง (ใช้กันอย่างหนักหน่วงใน Framework อย่าง NestJS หรือ Angular) การใช้งานต้องเปิดตั้งค่า <code>"experimentalDecorators": true</code> ใน <code>tsconfig.json</code> ก่อน</p> <pre><code>// ตัวอย่างการสร้าง Class Decorator ง่ายๆ function Logger(constructor: Function) {   console.log("Logging class creation:", constructor.name); }  @Logger class Person {   constructor(public name: string) {} } // เมื่อโค้ดรัน จะแสดง "Logging class creation: Person" ในคอนโซล </code></pre>'
  },
  {
    number: 6,
    slug: 'modules',
    emoji: '📦',
    title: 'Modules และ Declaration Files',
    content: '<h2>การจัดการ Modules ใน TypeScript</h2> <p>TypeScript ใช้มาตรฐาน ES Modules (<code>import</code> / <code>export</code>) เช่นเดียวกับ JavaScript ยุคใหม่ แต่สิ่งที่เพิ่มเข้ามาคือความสามารถในการ <strong>Import/Export Types</strong> โดยเฉพาะ ซึ่งมีประโยชน์มากในการจัดการโค้ดเบสขนาดใหญ่</p>  <h3>Import และ Export Types</h3> <p>บ่อยครั้งที่เราต้องการแค่โครงสร้าง Type จากไฟล์อื่น โดยไม่ต้องการให้โค้ดนั้นถูกแปลงไปรวมในไฟล์ JavaScript สุดท้าย (เพื่อลดขนาด Bundle) เราสามารถใช้คีย์เวิร์ด <code>type</code> ในการ import/export ได้</p> <pre><code>// types.ts export interface User { id: number; name: string; } export type Status = "active" | "inactive";  // app.ts // ใช้ \'import type\' เพื่อบอก Compiler ว่านี่เป็นแค่ Type นะ  // เมื่อแปลงเป็น JS บรรทัดนี้จะถูกลบทิ้ง (Erased at runtime) import type { User, Status } from \'./types\';  const user: User = { id: 1, name: "Alice" }; </code></pre>  <h2>Declaration Merging</h2> <p>หนึ่งในฟีเจอร์ที่ทำให้ TypeScript ทำงานกับ Ecosystem ของ JS ได้อย่างแนบเนียนคือ <strong>Declaration Merging</strong> เมื่อเราประกาศ Interface ชื่อเดียวกันหลายๆ ครั้งใน Scope เดียวกัน TypeScript จะนำ Properties ทั้งหมดมา "รวมร่าง" กันโดยอัตโนมัติ</p> <pre><code>interface Window {   myCustomGlobal: string; }  // ในอีกไฟล์หนึ่ง หรือบรรทัดถัดมา interface Window {   initApp(): void; }  // ตอนนี้ \'Window\' จะมีทั้ง myCustomGlobal และ initApp window.myCustomGlobal = "Hello"; window.initApp = () =&gt; console.log("App Started"); </code></pre> <p>ฟีเจอร์นี้ถูกใช้งานอย่างหนักเบื้องหลังเมื่อเราทำการเพิ่มฟังก์ชันเข้าไปใน Library เดิม หรือการกำหนด Global Variables (เช่น <code>process.env</code> ใน Node.js)</p>  <h2>ไฟล์ .d.ts (Declaration Files)</h2> <p>ไฟล์ <strong>.d.ts (d stands for declaration)</strong> เป็นไฟล์พิเศษใน TypeScript ที่มีไว้สำหรับประกาศ "โครงสร้าง Types ล้วนๆ" โดยห้ามมี Logic การทำงานอยู่ข้างใน (ห้ามเขียน Implementation) ไฟล์เหล่านี้เปรียบเสมือนคู่มือ (Manual) ที่บอกให้ TypeScript Compiler รู้จักรูปร่างหน้าตาของโค้ด JavaScript</p> <p>เรามักจะใช้ <code>.d.ts</code> เมื่อ:</p> <ol style="padding-left:20px">   <li>ต้องการประกาศ Global Types ที่ใช้ร่วมกันทั้งโปรเจคโดยไม่ต้อง import</li>   <li>ต้องการเขียน Types คลุมให้กับ Library JavaScript เก่าๆ ที่ไม่มี Types มาให้</li> </ol>  <h2>DefinitelyTyped (@types)</h2> <p>คุณอาจจะเคยสงสัยว่า ทำไมตอนติดตั้ง Library อย่าง React หรือ Lodash เราต้องติดตั้ง <code>@types/react</code> หรือ <code>@types/lodash</code> ตามมาด้วย? นั่นเป็นเพราะ Library เหล่านั้นถูกเขียนด้วย JavaScript ล้วนๆ</p> <p><strong>DefinitelyTyped</strong> เป็นโปรเจค Open Source ขนาดยักษ์บน GitHub ที่คอมมูนิตี้ช่วยกันเขียนไฟล์ <code>.d.ts</code> ให้กับ Library เกือบทุกตัวบนโลก npm เมื่อคุณรันคำสั่ง <code>npm install @types/jquery</code> คุณกำลังดาวน์โหลดคู่มือ Types เพื่อมาใช้ร่วมกับโค้ดจริงของ jQuery นั่นเอง</p>  <h2>การเขียน Declarations ให้กับ JS Libraries ที่ไม่มี Types</h2> <p>ถ้าคุณต้องใช้ Library ภายในบริษัท หรือ Library เก่ามากๆ ที่ไม่มีแม้กระทั่งใน <code>@types</code> คุณก็สามารถสร้างไฟล์ <code>.d.ts</code> ขึ้นมาคลุมมันได้เอง!</p> <pre><code>// สมมติว่ามีไลบรารี่ชื่อ \'super-legacy-lib\' // ให้สร้างไฟล์ types/super-legacy-lib.d.ts  declare module \'super-legacy-lib\' {   export function doMagic(input: string): number;   export const version: string; }  // จากนั้นในโค้ดของคุณก็จะสามารถ import มาใช้งานพร้อม Autocomplete ได้ import { doMagic, version } from \'super-legacy-lib\'; </code></pre> <div class="tip-box">   <strong>💡 Tip</strong>: การตั้งค่า <code>"typeRoots"</code> ใน <code>tsconfig.json</code> จะช่วยบอก TS ว่าให้ไปค้นหาไฟล์ .d.ts ของเราจากโฟลเดอร์ไหนบ้าง </div>'
  },
  {
    number: 7,
    slug: 'ts-react',
    emoji: '⚛️',
    title: 'TypeScript + React คู่หูไร้พ่าย',
    content: '<h2>ยกระดับ React ด้วย TypeScript</h2> <p>การเขียน React คู่กับ TypeScript คือหนึ่งใน Combination ที่ทรงพลังที่สุดในวงการ Frontend ปัจจุบัน มันช่วยขจัดปัญหาการส่ง Props ผิดประเภท การลืมเช็ค Null และช่วยให้ Components กลายเป็นบล็อกโค้ดที่เรียกใช้งานได้อย่างมั่นใจ</p>  <h2>Typing Components และ Props</h2> <p>วิธีการมาตรฐานในการกำหนด Type ให้กับ Props ของ Component คือการใช้ <code>interface</code> หรือ <code>type</code> แล้วจับคู่เข้ากับ Function Component ปกติ</p> <pre><code>interface ButtonProps {   label: string;   onClick: () =&gt; void;   variant?: \'primary\' | \'secondary\' | \'danger\'; // Optional   disabled?: boolean; }  // แนะนำให้เขียนแบบ Function รับ Props ตรงๆ  export function Button({ label, onClick, variant = \'primary\', disabled }: ButtonProps) {   return (     &lt;button        className={`btn-${variant}`}        onClick={onClick}        disabled={disabled}&gt;       {label}     &lt;/button&gt;   ); } </code></pre> <p><em>หมายเหตุ: ในอดีตมักนิยมใช้ <code>React.FC&lt;ButtonProps&gt;</code> แต่ปัจจุบัน React Community แนะนำให้เขียนแบบรับ Props ตรงๆ มากกว่า เพราะโค้ดคลีนกว่าและรองรับ Generic Components ได้ดีกว่า</em></p>  <h2>Typing Hooks อย่างถูกต้อง</h2> <p>React Hooks ทำงานร่วมกับ TS ได้อย่างยอดเยี่ยม โดยมากแล้ว TS สามารถ <strong>Infer</strong> Type ให้เราได้อัตโนมัติ แต่ในบางกรณีเราก็ต้องระบุ Type ด้วยตัวเองผ่าน Generic</p> <h3>useState</h3> <pre><code>// TS รู้ว่าเป็น boolean const [isLoading, setIsLoading] = useState(false);   // เมื่อเริ่มต้นเป็น null แต่ข้อมูลจริงจะเป็น Object User เราต้องกำหนด &lt;User | null&gt; const [user, setUser] = useState&lt;User | null&gt;(null); </code></pre>  <h3>useRef</h3> <p>เวลาเราอ้างอิงถึง DOM Elements เราต้องระบุ Type ของ Element นั้นๆ เพื่อให้ได้ Autocomplete ครบถ้วน (เช่น <code>.focus()</code> หรือ <code>.value</code>)</p> <pre><code>const inputRef = useRef&lt;HTMLInputElement&gt;(null);  const focusInput = () =&gt; {   // ต้องใส่ ? เพราะ current อาจเป็น null   inputRef.current?.focus();  };  return &lt;input ref={inputRef} /&gt;; </code></pre>  <h2>การจัดการ Event Types</h2> <p>เวลาเราส่งฟังก์ชันไปผูกกับเหตุการณ์ (Events) อย่าง <code>onClick</code> หรือ <code>onChange</code> เรามักต้องรับค่า <code>event</code> เข้ามา ซึ่ง Type ของ <code>event</code> มักจะยาวและจำยาก</p> <pre><code>// การจัดการ Form Input const handleChange = (e: React.ChangeEvent&lt;HTMLInputElement&gt;) =&gt; {   console.log(e.target.value); };  // การจัดการ Form Submit const handleSubmit = (e: React.FormEvent&lt;HTMLFormElement&gt;) =&gt; {   e.preventDefault(); };  return (   &lt;form onSubmit={handleSubmit}&gt;     &lt;input type="text" onChange={handleChange} /&gt;     &lt;button type="submit"&gt;Submit&lt;/button&gt;   &lt;/form&gt; ); </code></pre>  <h2>Generic Components ใน React</h2> <p>ถ้าเราต้องสร้าง Component อย่าง Table หรือ List ที่สามารถรับข้อมูลแบบไหนก็ได้ (Dynamic) เราสามารถประยุกต์ใช้ Generics เข้ากับ Component ได้!</p> <pre><code>// Component ที่รับ Data แบบ Generic interface ListProps&lt;T&gt; {   items: T[];   renderItem: (item: T, index: number) =&gt; React.ReactNode; }  // สังเกตการวาง &lt;T,&gt; มีคอมม่าเพื่อให้ TS รู้ว่าเป็น Generic ไม่ใช่ JSX tag function List&lt;T,&gt;({ items, renderItem }: ListProps&lt;T&gt;) {   return &lt;ul&gt;{items.map(renderItem)}&lt;/ul&gt;; }  // เวลาเรียกใช้งาน const users = [{ name: \'Alice\' }, { name: \'Bob\' }]; &lt;List    items={users}    renderItem={(user) =&gt; &lt;li key={user.name}&gt;{user.name}&lt;/li&gt;}  /&gt; </code></pre>  <h2>การทำ Context Typing</h2> <p>การใช้ Context API ค่อนข้างท้าทายเล็กน้อย เพราะตอนเรียก <code>createContext</code> เรายังไม่มีค่าเริ่มต้นที่ถูกต้อง วิธีแก้คือโยน <code>null</code> เข้าไปและใช้ Union Type</p> <pre><code>interface ThemeContextType {   theme: \'light\' | \'dark\';   toggleTheme: () =&gt; void; }  // กำหนดให้เป็น ThemeContextType หรือ null const ThemeContext = createContext&lt;ThemeContextType | null&gt;(null);  export const useTheme = () =&gt; {   const context = useContext(ThemeContext);   // ถ้าเรียกใช้นอก Provider ให้โยน Error ไปเลย   if (!context) throw new Error("useTheme must be used within ThemeProvider");   return context; }; </code></pre> <div class="tip-box">   <strong>💡 Tip</strong>: การทำ Custom Hook อย่าง <code>useTheme</code> ด้านบน ช่วยการันตีว่าเราจะได้ <code>ThemeContextType</code> กลับมาเสมอโดยไม่ต้องมานั่งเช็ค <code>null</code> ใน Component ทุกครั้งที่เรียกใช้ </div>'
  },
  {
    number: 8,
    slug: 'config',
    emoji: '⚙️',
    title: 'TypeScript Config และ Tooling',
    content: '<h2>เจาะลึก tsconfig.json</h2> <p>ไฟล์ <code>tsconfig.json</code> เปรียบเสมือนหัวใจของการทำงานในโปรเจค TypeScript มันคือตัวควบคุมว่า Compiler (tsc) จะต้องปฏิบัติตัวอย่างไร จะยอมรับโค้ดแบบไหน และจะแปลงโค้ด (Transpile) ออกมาในรูปแบบใด</p>  <h3>ตั้งค่า Strict Mode ให้สุดทาง</h3> <p>เพื่อดึงศักยภาพของ TypeScript ออกมาให้มากที่สุด คุณควรเปิด <code>"strict": true</code> เสมอ ซึ่งมันจะไปเปิดฟีเจอร์ย่อยๆ เหล่านี้ด้วย:</p> <ul>   <li><code>strictNullChecks</code>: บังคับให้คุณต้องจัดการกับ <code>null</code> และ <code>undefined</code> ป้องกันบั๊กสุดฮิตอย่าง <em>"Cannot read properties of undefined"</em></li>   <li><code>noImplicitAny</code>: ห้ามไม่ให้ปล่อยตัวแปรเป็น Type <code>any</code> โดยปริยาย คุณต้องระบุ Type เสมอหาก TS เดาไม่ได้</li>   <li><code>strictBindCallApply</code>: เช็ค Type ของฟังก์ชันเมื่อมีการเรียกใช้ <code>.bind()</code>, <code>.call()</code>, หรือ <code>.apply()</code></li> </ul>  <h3>Target, Lib และ Module</h3> <ul>   <li><code>target</code>: ระบุเวอร์ชันของ JavaScript ขาออก เช่น <code>"ES2015"</code> หรือ <code>"ESNext"</code> ถ้าแอปต้องรันบนเบราว์เซอร์เก่าๆ ก็อาจจะตั้งเป็น <code>"ES5"</code></li>   <li><code>lib</code>: บอกให้ TS รู้จัก Environment ที่เรากำลังทำงานอยู่ เช่น <code>["DOM", "ES2015"]</code> เพื่อให้มี Autocomplete สำหรับ <code>window</code> หรือ <code>document</code></li>   <li><code>module</code> และ <code>moduleResolution</code>: ตั้งค่าวิธีที่ไฟล์หากันเจอ ปกติในโปรเจคสมัยใหม่มักจะตั้งเป็น <code>"ESNext"</code> หรือ <code>"NodeNext"</code></li> </ul>  <h3>Path Aliases บอกลา \'../../../\'</h3> <p>เมื่อโปรเจคใหญ่ขึ้น การ import ไฟล์ข้ามโฟลเดอร์จะเริ่มเป็นฝันร้าย เราสามารถตั้งค่า <code>paths</code> เพื่อสร้าง Alias ให้กับโฟลเดอร์ได้</p> <pre><code>// ใน tsconfig.json {   "compilerOptions": {     "baseUrl": ".",     "paths": {       "@components/*": ["src/components/*"],       "@utils/*": ["src/utils/*"]     }   } } </code></pre> <p>ทำให้สามารถเปลี่ยนการ import จาก <code>import Button from \'../../../components/Button\'</code> กลายเป็น <code>import Button from \'@components/Button\'</code> ได้เลย</p>  <h2>ESLint ร่วมกับ TypeScript</h2> <p>TypeScript เช็ค Types แต่ไม่ได้เช็คสไตล์การเขียนโค้ด ดังนั้นเราจึงต้องใช้ <strong>ESLint</strong> ควบคู่กันไป โดยใช้ Plugin <code>@typescript-eslint/eslint-plugin</code> และ <code>@typescript-eslint/parser</code> เพื่อให้ ESLint เข้าใจไวยากรณ์ของ TS และสามารถบังคับกฎเกณฑ์ของทีมได้ เช่น "ห้ามใช้ interface ให้ใช้ type เท่านั้น" หรือ "ต้องตั้งชื่อ Interface ด้วยตัว I นำหน้า"</p>  <h2>Type Checking ใน CI Pipeline</h2> <p>ในโปรเจคที่ใช้ Tool อย่าง Vite หรือ Next.js การคอมไพล์โค้ดอาจจะข้ามการเช็ค Type ไปเพื่อความรวดเร็ว (ใช้ esbuild หรือ swc) ดังนั้นก่อนที่จะ Deploy หรือ Merge โค้ด เราควรมีขั้นตอนใน CI/CD Pipeline (เช่น GitHub Actions) เพื่อรันคำสั่ง <code>tsc --noEmit</code> คำสั่งนี้จะบอกให้ TS ตรวจสอบ Type ทั้งโปรเจคอย่างละเอียดโดยไม่ต้องสร้างไฟล์ JS ออกมา หากมี Error ระบบ CI จะได้แจ้งเตือนให้ไปแก้ไขก่อนโค้ดพังบน Production</p>'
  },
  {
    number: 9,
    slug: 'project',
    emoji: '🛠️',
    title: 'ตัวอย่างโปรเจค TypeScript จริงในระดับ Production',
    content: '<h2>กลยุทธ์การ Migrate โปรเจค JavaScript ไปสู่ TypeScript</h2> <p>ถ้าคุณมีโปรเจค JS เดิมอยู่แล้ว ไม่ต้องกลัวที่จะย้ายมาใช้ TS เพราะคุณไม่ต้องเขียนใหม่ทั้งหมดในวันเดียว (Incremental Migration) เริ่มต้นด้วยการเปิดตั้งค่า <code>"allowJs": true</code> ใน <code>tsconfig.json</code> ซึ่งจะทำให้โปรเจคของคุณมีทั้งไฟล์ <code>.js</code> และ <code>.ts</code> อยู่ร่วมกันได้ จากนั้นค่อยๆ เปลี่ยนนามสกุลไฟล์ทีละไฟล์ เริ่มจากไฟล์ Utils หรือ Types พื้นฐาน แล้วค่อยขยับไปที่ Components ที่ซับซ้อนขึ้น</p>  <h2>การสร้าง Type-safe API Client</h2> <p>หนึ่งในจุดคุ้มทุนที่สุดของการใช้ TS คือการคุยกับ Backend เมื่อเรากำหนดรูปร่างของ Response ล่วงหน้า เราจะไม่มีทางดึงข้อมูลผิดฟิลด์</p> <pre><code>// types/api.ts export interface Post {   id: number;   title: string;   body: string; }  // api/client.ts export async function getPosts(): Promise&lt;Post[]&gt; {   const response = await fetch(\'https://jsonplaceholder.typicode.com/posts\');   if (!response.ok) throw new Error(\'Failed to fetch\');   // การใช้ "as" ตรงนี้เป็นการยืนยันชั่วคราว ควรใช้ Zod เพื่อความปลอดภัยขั้นสุด (อธิบายด้านล่าง)   return response.json() as Promise&lt;Post[]&gt;; } </code></pre>  <h2>Typed State Management ด้วย Zustand / Redux Toolkit</h2> <p>ไลบรารี่จัดการ State ยุคใหม่ถูกเขียนด้วย TypeScript เกือบทั้งหมด ตัวอย่างเช่น <strong>Zustand</strong> ที่ทำได้ง่ายและคลีนมากๆ</p> <pre><code>import { create } from \'zustand\';  interface AuthState {   user: { name: string } | null;   isLoggedIn: boolean;   login: (name: string) =&gt; void;   logout: () =&gt; void; }  export const useAuthStore = create&lt;AuthState&gt;((set) =&gt; ({   user: null,   isLoggedIn: false,   login: (name) =&gt; set({ user: { name }, isLoggedIn: true }),   logout: () =&gt; set({ user: null, isLoggedIn: false }), })); </code></pre>  <h2>การทำ Type-safe อย่างสมบูรณ์แบบด้วย Zod</h2> <p>TypeScript เป็นระบบเช็ค Types <strong>ณ เวลา Compile</strong> เท่านั้น เมื่อโค้ดรันไปแล้ว (Runtime) Type เหล่านั้นจะหายไป หาก API ส่งข้อมูลกลับมาไม่ตรงกับ <code>interface</code> ที่เรากำหนด แอปก็ยังพังอยู่ดี!</p> <p>ทางแก้คือการใช้ Data Validation Library อย่าง <strong>Zod</strong> เพื่อตรวจสอบข้อมูลที่ Runtime พร้อมกับสกัด Type ออกมาใช้ตอน Compile-time (Single Source of Truth)</p> <pre><code>import { z } from \'zod\';  // 1. สร้าง Schema ของข้อมูลที่จะตรวจสอบตอน Runtime const userSchema = z.object({   id: z.number(),   email: z.string().email(),   role: z.enum(["ADMIN", "USER"]) });  // 2. สกัด (Infer) Type ออกมาใช้กับ TypeScript (ไม่ต้องเขียน Interface แยก) type User = z.infer&lt;typeof userSchema&gt;;  // 3. นำไปตรวจสอบข้อมูลที่รับมาจากภายนอก async function fetchUser(id: string) {   const res = await fetch(`/api/users/${id}`);   const data = await res.json();      // parse() จะโยน Error ถ้ารูปแบบไม่ตรงตาม Schema   const validUser: User = userSchema.parse(data);    return validUser; } </code></pre>  <h2>บทสรุปส่งท้าย</h2> <p>จากที่เราได้เรียนรู้มาตลอดทั้งคอร์ส คุณจะเห็นว่า TypeScript ไม่ได้เป็นเพียงแค่การใส่ Types เข้าไปใน JavaScript แต่มันคือ <strong>"วิธีคิดและการออกแบบสถาปัตยกรรม"</strong> เมื่อคุณใช้ Generics, Utility Types, และ Tools อย่าง Zod หรือ Zustand อย่างถูกต้อง คุณจะสามารถสร้างแอปพลิเคชันที่แข็งแกร่ง (Robust) บำรุงรักษาง่าย (Maintainable) และสเกลโปรเจคไปสู่ระดับองค์กรได้อย่างมั่นใจในสไตล์นักพัฒนาปี 2025!</p>'
  }
];
