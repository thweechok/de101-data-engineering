export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '🧪',
    title: 'Overview',
    content: '<h2>ทำไมต้องมี Testing? (Why Testing?)</h2><p>การเขียนโปรแกรมในปัจจุบันไม่ได้มีเป้าหมายแค่ทำให้ซอฟต์แวร์ทำงานได้ (It Works!) แต่ยังต้องคำนึงถึงความเสถียร (Stability) การบำรุงรักษา (Maintainability) และความมั่นใจในการแก้ไขโค้ด (Confidence in Refactoring) ซอฟต์แวร์ที่ไม่มีการทำ Automated Testing เลย เปรียบเสมือนการสร้างตึกสูงที่ไม่มีวิศวกรตรวจสอบโครงสร้าง เมื่อมีการต่อเติมในอนาคต โอกาสที่จะพังทลายลงมาย่อมสูงมาก</p><p><strong>Software Testing</strong> คือกระบวนการที่เราใช้เพื่อตรวจสอบว่าโค้ดที่เราเขียนทำงานถูกต้องตาม Business Requirements และไม่เกิดพฤติกรรมที่ไม่คาดคิด (Unexpected Behaviors) การเทสต์ช่วยลดโอกาสที่จะปล่อยบั๊กขึ้นสู่ระบบ Production ซึ่งจะส่งผลเสียหายต่อธุรกิจและประสบการณ์ผู้ใช้งาน</p><h2>Testing Pyramid</h2><p>หนึ่งในแนวคิดที่สำคัญที่สุดของการทำ Testing คือ <strong>Testing Pyramid</strong> ซึ่งถูกนำเสนอโดย Mike Cohn แนวคิดนี้แนะนำสัดส่วนของการเขียน Test ในระดับต่างๆ เพื่อให้เกิดประสิทธิภาพและคุ้มค่าที่สุด โดยแบ่งเป็น 3 ระดับหลัก (จากฐานขึ้นสู่ยอด):</p><ol style="padding-left:20px"><li><strong>Unit Tests (ฐานปิระมิด)</strong>: เป็นการทดสอบในระดับที่เล็กที่สุด เช่น ฟังก์ชัน (Functions) หรือ เมธอด (Methods) เดี่ยวๆ ควรมีจำนวนมากที่สุดเพราะทำงานได้เร็วที่สุดและระบุจุดที่พังได้แม่นยำ</li><li><strong>Integration Tests (ส่วนกลาง)</strong>: เป็นการทดสอบการทำงานร่วมกันของหลายๆ ส่วน (Modules/Components) เช่น การเชื่อมต่อ API หรือการเขียนอ่านฐานข้อมูล (Database) ทำงานช้ากว่า Unit Test และใช้เวลาเตรียม Setup มากกว่า</li><li><strong>End-to-End (E2E) Tests (ยอดปิระมิด)</strong>: การทดสอบตั้งแต่ต้นจนจบเหมือนเป็น User คนหนึ่งใช้งานระบบจริง ตั้งแต่เปิดบราวเซอร์ คลิกปุ่ม และดูผลลัพธ์ ควรมีจำนวนน้อยที่สุดเนื่องจากทำงานช้าที่สุดและเกิดความไม่แน่นอน (Flaky) ได้ง่าย</li></ol><div class="tip-box"><strong>💡 Pro Tip</strong>: ในระบบที่มี UI ซับซ้อน บางครั้งเราอาจจะเห็นการเพิ่มระดับ UI Component Tests (เช่น การใช้ React Testing Library) เข้ามาอยู่ระหว่าง Unit และ Integration Tests ด้วย</div><h2>ต้นทุนของข้อผิดพลาด (Cost of Bugs)</h2><p>กฎของการพัฒนาซอฟต์แวร์ที่ควรรู้คือ <strong>"ยิ่งเจอบั๊กช้า ยิ่งมีราคาแพง (The later a bug is found, the more it costs to fix)"</strong></p><ul><li><strong>เจอตอนเขียนโค้ด (Development)</strong>: แก้ไขได้ทันที ใช้เวลาไม่กี่นาที ต้นทุนต่ำมาก</li><li><strong>เจอตอนรัน Automated Tests (CI/CD)</strong>: Developer ได้รับ Feedback ทันที ใช้เวลาแก้หลักนาทีถึงชั่วโมง</li><li><strong>เจอตอนทำ QA/UAT (Testing Phase)</strong>: ต้องเปิด Ticket, หาคนรับผิดชอบ, 재현 (Reproduce) ปัญหา ใช้เวลาเป็นวันหรือสัปดาห์</li><li><strong>เจอตอนอยู่บน Production</strong>: กระทบผู้ใช้งานจริง อาจทำให้เสียลูกค้า เสียชื่อเสียง หรือถูกฟ้องร้อง ต้นทุนประเมินค่าไม่ได้</li></ul><p>ดังนั้นการลงทุนทำ Automated Testing ตั้งแต่แรก แม้จะดูเหมือนทำให้ใช้เวลาเขียนโค้ดนานขึ้น แต่ในระยะยาวจะประหยัดเวลาและค่าใช้จ่ายขององค์กรได้อย่างมหาศาล</p><h2>วัฒนธรรมการทดสอบ (Testing Culture)</h2><p>เครื่องมือเป็นเพียงส่วนหนึ่งของความสำเร็จ สิ่งที่ยากกว่าคือการสร้าง <strong>Testing Culture</strong> ในทีม</p><p>ทีมที่มี Testing Culture ที่ดีจะไม่มองว่าการเขียน Test เป็นภาระ หรือเป็นสิ่งที่ทำแค่เพื่อให้ได้ Coverage 100% (ซึ่งมักจะกลายเป็น Vanity Metric) แต่ทีมจะมองว่า Test เป็น "เอกสารที่มีชีวิต (Living Documentation)" และเป็นตัวช่วยป้องกัน Regression Bugs (บั๊กเก่าที่โผล่กลับมาใหม่หลังจากแก้โค้ด)</p><p>หากคุณเคยรู้สึกกลัวที่จะกดปุ่ม Deploy ในวันศุกร์เย็น นั่นแปลว่าระบบของคุณขาด Automated Tests ที่มากพอ เมื่อทีมมีความมั่นใจใน Test Suite การ Deploy จะกลายเป็นเรื่องน่าเบื่อ (Boring) ซึ่งในทาง Software Engineering ถือเป็นเรื่องดีที่สุด!</p><h2>สรุป</h2><p>การทำ Software Testing ถือเป็นทักษะที่แยก Developer ทั่วไปออกจาก Senior Developer ได้อย่างชัดเจน ในบทต่อๆ ไปของคอร์สนี้ เราจะเจาะลึกไปทีละระดับของ Testing Pyramid เริ่มตั้งแต่การเขียน Unit Test ด้วย Jest ไปจนถึงการทำ End-to-End Test และการใช้แนวคิด TDD เพื่อขับเคลื่อนการเขียนโค้ด</p>'
  },
  {
    number: 1,
    slug: 'unit-testing',
    emoji: '🟢',
    title: 'Unit Testing ด้วย Jest',
    content: '<h2>ทำความรู้จักกับ Jest</h2><p><strong>Jest</strong> คือ JavaScript Testing Framework ที่ได้รับความนิยมสูงสุดในปัจจุบัน พัฒนาโดย Meta (Facebook) จุดเด่นของ Jest คือความครบเครื่องแบบ "Zero-configuration" มาพร้อมกับฟีเจอร์ต่างๆ ครบครันในตัวเดียว ทั้ง Test Runner, Assertion Library, Mocking Library และ Code Coverage Reports โดยไม่ต้องติดตั้งเครื่องมืออื่นๆ เพิ่มเติม (Batteries Included)</p><h2>การตั้งค่าเริ่มต้น (Jest Setup)</h2><p>การติดตั้ง Jest เริ่มต้นด้วยการรันคำสั่ง npm หรือ yarn ในโปรเจกต์ของคุณ:</p><pre>npm install --save-dev jest</pre><p>จากนั้นในไฟล์ <code>package.json</code> ให้เพิ่มคำสั่งสำหรับการรัน test ในส่วนของ scripts:</p><pre>"scripts": {\n  "test": "jest"\n}</pre><p>หากคุณใช้ TypeScript ต้องติดตั้ง <code>ts-jest</code> และ <code>@types/jest</code> เพิ่มเติม เพื่อให้ Jest เข้าใจและรันไฟล์ TypeScript ได้โดยตรง</p><h2>โครงสร้างของ Unit Test: describe, it, test</h2><p>ในการเขียน Test เราจะจัดกลุ่มและอธิบายสิ่งที่กำลังจะทดสอบผ่านฟังก์ชันที่ Jest เตรียมไว้ให้:</p><ul><li><code>describe(name, fn)</code>: ใช้จัดกลุ่ม Test Suite ที่เกี่ยวข้องกัน เพื่อให้ Report อ่านง่ายขึ้น</li><li><code>test(name, fn)</code> หรือ <code>it(name, fn)</code>: เป็นบล็อกที่ใช้เขียน Assertion จริงๆ ทั้งสองคำนี้ทำงานเหมือนกันเป๊ะ <code>it</code> มักถูกใช้เพื่ออ่านเป็นประโยคภาษาอังกฤษที่ลื่นไหล (เช่น it should return true)</li></ul><pre>describe(\'Calculator Module\', () => {\n  it(\'should add two numbers correctly\', () => {\n    // test logic\n  });\n});</pre><h2>Expect Matchers (การตรวจสอบผลลัพธ์)</h2><p>Jest มี <strong>Matchers</strong> สำหรับตรวจสอบ (Assert) ผลลัพธ์ในรูปแบบต่างๆ มากมาย การใช้ Matcher ที่ถูกต้องช่วยให้ข้อความ Error สื่อความหมายชัดเจนขึ้น:</p><ul style="padding-left:20px"><li><code>expect(a).toBe(b)</code>: ตรวจสอบความเท่ากันแบบ Primitive (เหมือน ===)</li><li><code>expect(a).toEqual(b)</code>: ตรวจสอบค่าภายในของ Object หรือ Array (Deep Equality)</li><li><code>expect(a).toBeNull()</code>: เช็คว่าเป็น null</li><li><code>expect(a).toBeDefined()</code>: เช็คว่ามีค่า (ไม่ใช่ undefined)</li><li><code>expect(a).toBeTruthy()</code> / <code>expect(a).toBeFalsy()</code>: เช็คค่าเมื่อแปลงเป็น Boolean</li><li><code>expect(a).toContain(b)</code>: เช็คว่าใน Array หรือ String มีค่านั้นอยู่</li><li><code>expect(() => fn()).toThrow()</code>: เช็คว่าฟังก์ชันโยน Exception (Error) ออกมา</li></ul><div class="tip-box"><strong>💡 Pro Tip</strong>: เวลาตรวจสอบ Object เสมอๆ ให้ใช้ <code>toEqual()</code> ไม่ใช่ <code>toBe()</code> เพราะ Object สองตัวที่หน้าตาเหมือนกันจะมี Reference ใน Memory ต่างกัน การใช้ toBe จะทำให้ Test Fail เสมอ</div><h2>วงจรชีวิตของ Test (Setup and Teardown)</h2><p>บ่อยครั้งที่เราต้องเตรียมข้อมูล (Setup) ก่อนเริ่มรัน Test และล้างข้อมูล (Teardown) หลัง Test จบ Jest มี Hooks เหล่านี้ให้ใช้งาน:</p><ul><li><code>beforeAll(fn)</code>: รันเพียง 1 ครั้ง ก่อนเริ่มรันทุกๆ test ในไฟล์นั้น</li><li><code>beforeEach(fn)</code>: รันทุกครั้ง "ก่อน" จะเริ่มแต่ละ <code>test()</code> (เหมาะสำหรับ reset state หรือ mock)</li><li><code>afterEach(fn)</code>: รันทุกครั้ง "หลัง" จบแต่ละ <code>test()</code> (เหมาะสำหรับ clean up)</li><li><code>afterAll(fn)</code>: รัน 1 ครั้ง หลังรัน test ทั้งหมดเสร็จสิ้น</li></ul><h2>ศิลปะของการ Mocking</h2><p>หนึ่งในความท้าทายที่สุดของ Unit Test คือ "การแยก" สิ่งที่เราต้องการเทสต์ออกจาก Dependency รอบข้าง (เช่น Database, API, หรือ 3rd Party Library) การ Mock จึงถูกนำมาใช้เพื่อจำลองพฤติกรรมเหล่านั้น</p><h3>1. jest.fn() (Mock Functions / Spies)</h3><p>ใช้สร้าง "ฟังก์ชันปลอม" เพื่อดูว่าถูกเรียกใช้งานหรือไม่ ถูกเรียกกี่ครั้ง และถูกเรียกด้วยอาร์กิวเมนต์อะไร</p><pre>const mockCallback = jest.fn();\nfunction map(arr, callback) {\n  for(let i=0; i<arr.length; i++) callback(arr[i]);\n}\n\nmap([1, 2], mockCallback);\nexpect(mockCallback).toHaveBeenCalledTimes(2);\nexpect(mockCallback).toHaveBeenCalledWith(1);</pre><h3>2. jest.mock() (Mock Modules)</h3><p>ใช้จำลองการทำงานของ Module ทั้งไฟล์ (เช่น จำลอง axios เพื่อไม่ให้ยิง Network จริงๆ)</p><pre>import axios from \'axios\';\njest.mock(\'axios\');\n\ntest(\'should fetch users\', async () => {\n  const users = [{name: \'John\'}];\n  const resp = {data: users};\n  axios.get.mockResolvedValue(resp);\n\n  const result = await fetchUsers();\n  expect(result).toEqual(users);\n});</pre><h3>3. jest.spyOn() (Spies)</h3><p>คล้ายกับ <code>jest.fn()</code> แต่ใช้กับฟังก์ชันที่มีอยู่แล้วใน Object เพื่อดักดูการเรียกใช้งาน โดยยังสามารถยอมให้รันโค้ดจริง (Original Implementation) หรือจะ Mock ทับก็ได้</p><pre>const video = {\n  play() {\n    return true;\n  },\n};\nconst spy = jest.spyOn(video, \'play\');\nvideo.play();\nexpect(spy).toHaveBeenCalled();\nspy.mockRestore(); // คืนค่ากลับเป็นปกติ</pre><h2>สรุป</h2><p>Unit Testing ด้วย Jest เป็นทักษะพื้นฐานที่สุดที่นักพัฒนา JavaScript ทุกคนควรมี การเข้าใจวิธีใช้ Matchers ต่างๆ อย่างเหมาะสม และเทคนิคการ Mock dependency อย่างถูกวิธี จะช่วยให้คุณสามารถเขียน Test ที่ทำงานรวดเร็ว เสถียร และให้ Feedback ได้อย่างแม่นยำเมื่อมีข้อผิดพลาดเกิดขึ้นในโค้ดของคุณ</p>'
  },
  {
    number: 2,
    slug: 'async-testing',
    emoji: '⏱️',
    title: 'Testing Async Code',
    content: '<h2>ความท้าทายของการ Testing Asynchronous Code</h2><p>ในโลกของ JavaScript การทำงานแบบ Asynchronous (อซิงโครนัส) เป็นเรื่องปกติที่เราต้องพบเจอ ไม่ว่าจะเป็นการดึงข้อมูลจาก API (Network Requests), การอ่านเขียนไฟล์, หรือแม้แต่การตั้งเวลาด้วย <code>setTimeout</code> แต่ในการเขียน Test การจัดการกับ Async Code ถือเป็นเรื่องท้าทาย เพราะถ้าเราไม่รอให้กระบวนการ Async เสร็จสิ้นก่อนที่จะตรวจสอบ (Assert) ผลลัพธ์ Test Runner ก็จะจบการทำงานและแจ้งว่า Test ผ่าน (False Positive) ทั้งๆ ที่ยังไม่ได้ตรวจสอบผลที่แท้จริงเลย</p><p>บทนี้เราจะเรียนรู้วิธีการเขียน Test สำหรับ Async Code ด้วยเครื่องมือและเทคนิคต่างๆ ให้มีความเสถียรและแม่นยำ</p><h2>1. การทดสอบ Promises และ Async/Await</h2><p>วิธีที่ง่ายและเป็นที่นิยมที่สุดใน Jest ยุคปัจจุบัน คือการใช้ <code>async / await</code> แบบเดียวกับที่เราใช้ในโค้ดปกติ เมื่อเราใส่ keyword <code>async</code> ไว้หน้า callback ของ <code>test()</code> หรือ <code>it()</code> Jest จะรอจนกว่า Promise จะ Resolve หรือ Reject</p><pre>test(\'the data is peanut butter\', async () => {\n  const data = await fetchData();\n  expect(data).toBe(\'peanut butter\');\n});</pre><p>ถ้าฟังก์ชันที่คุณต้องการทดสอบโยน Error (Reject) การตรวจสอบสามารถทำได้โดยใช้ <code>.rejects</code> คู่กับ <code>toThrow()</code></p><pre>test(\'the fetch fails with an error\', async () => {\n  await expect(fetchDataThatFails()).rejects.toThrow(\'error\');\n});</pre><p><strong>ข้อควรระวัง (Gotcha)</strong>: หากคุณเทสต์ฟังก์ชันที่คืนค่าเป็น Promise เสมอ ห้ามลืมเขียน <code>await</code> หรือ <code>return</code> หน้าการเรียกฟังก์ชันเด็ดขาด มิฉะนั้น Test จะผ่านทันทีโดยไมรอผลลัพธ์</p><h2>2. การจำลอง Network Requests ด้วย MSW (Mock Service Worker)</h2><p>ในการทำ Integration Test หรือ Component Test เราไม่ควรที่จะยิง Network Request ไปยัง API ของจริง เนื่องจากจะทำให้ Test ทำงานช้า ต้องพึ่งพาอินเทอร์เน็ต และเกิด Flaky Test ได้ง่าย (เทสต์ผ่านบ้างไม่ผ่านบ้างจากปัจจัยภายนอก)</p><p>ในอดีตเรามักจะใช้ <code>jest.mock()</code> เพื่อดักฟังก์ชัน <code>fetch</code> หรือ <code>axios</code> แต่ปัจจุบัน <strong>MSW (Mock Service Worker)</strong> ได้กลายมาเป็นมาตรฐานใหม่ (Standard Tool) ในการ Mock API</p><p>MSW ทำงานด้วยการดักจับ (Intercept) Network Requests ในระดับของ Network Layer จริงๆ ทำให้โค้ดของเรายังคงใช้ <code>fetch()</code> เหมือนปกติ โดยที่มันไม่รู้เลยว่าถูก Mock อยู่</p><pre>// setup handlers\nimport { rest } from \'msw\';\nexport const handlers = [\n  rest.get(\'/api/user\', (req, res, ctx) => {\n    return res(\n      ctx.status(200),\n      ctx.json({ username: \'john_doe\' })\n    )\n  }),\n];</pre><p>ข้อดีของ MSW คือเราสามารถนำ Handlers เดียวกันนี้ไปใช้ได้ทั้งใน Jest (Node.js environment) รวมไปถึงใช้ Mock API ใน Storybook หรือขณะรันเซิร์ฟเวอร์สำหรับ Development ในฝั่งบราวเซอร์ได้ด้วย (Write once, reuse everywhere)</p><h2>3. การจัดการเรื่องเวลาด้วย Fake Timers</h2><p>บ่อยครั้งโค้ดของเราต้องใช้ฟังก์ชันจำพวก <code>setTimeout</code>, <code>setInterval</code> หรือรอให้เกิด Delay หากเราปล่อยให้ Test รอตามเวลาจริง (เช่น รอ 10 วินาที) จะทำให้ Test Suite ของเราทำงานช้ามากๆ</p><p>Jest มีฟีเจอร์ <strong>Fake Timers</strong> ที่ทำหน้าที่ "ปลอมเข็มนาฬิกา" ของ Environment ทำให้เราสามารถควบคุมเวลา (Time Travel) ได้อย่างอิสระ</p><pre>jest.useFakeTimers();\n\ntest(\'waits 1 second before calling callback\', () => {\n  const timerCallback = jest.fn();\n  setTimeout(timerCallback, 1000);\n\n  // ณ จุดนี้ callback ยังไม่ถูกเรียก\n  expect(timerCallback).not.toBeCalled();\n\n  // เลื่อนเวลาไปข้างหน้า 1 วินาทีแบบทันที!\n  jest.advanceTimersByTime(1000);\n\n  // callback จะถูกเรียกแล้ว\n  expect(timerCallback).toBeCalled();\n  expect(timerCallback).toHaveBeenCalledTimes(1);\n});</pre><div class="tip-box"><strong>💡 Pro Tip</strong>: อย่าลืมเรียกใช้ <code>jest.useRealTimers()</code> ใน <code>afterEach</code> เพื่อคืนค่ากลับเป็นนาฬิกาจริง ป้องกันไม่ให้การ Mock เวลานี้ไปส่งผลกระทบแปลกๆ กับ Test ตัวถัดไปในไฟล์เดียวกัน</div><h2>สรุป</h2><p>การเข้าใจกลไก Asynchronous ของ JavaScript และรู้วิธีใช้เครื่องมือสำหรับ Test อย่าง <code>async/await</code>, <code>MSW</code>, และ <code>Fake Timers</code> เป็นทักษะที่ขาดไม่ได้ เพราะแอปพลิเคชันยุคใหม่ล้วนถูกขับเคลื่อนด้วย Async Operations แทบทั้งสิ้น การจัดการอย่างถูกวิธีจะทำให้ Test ของคุณทำงานได้รวดเร็วปานสายฟ้า และไม่ต้องพึ่งพา Backend API ตัวจริงเลย</p>'
  },
  {
    number: 3,
    slug: 'react-testing',
    emoji: '⚛️',
    title: 'React Testing Library',
    content: '<h2>ทำไมต้อง React Testing Library (RTL)?</h2><p>ในอดีต นักพัฒนา React นิยมใช้ไลบรารีอย่าง <strong>Enzyme</strong> เพื่อทำ Unit Test แต่ Enzyme มักจะผูกติดกับ Implementation Details ภายในของ Component (เช่น การดึงข้อมูล State ภายในตรงๆ) ทำให้เมื่อเรา Refactor โค้ดโดยที่พฤติกรรมผู้ใช้ไม่เปลี่ยน Test ก็ดันพังซะอย่างนั้น</p><p>ปรัชญาของ <strong>React Testing Library (RTL)</strong> (สร้างโดย Kent C. Dodds) ถูกสรุปไว้ด้วยประโยคสั้นๆ ว่า: <em>"The more your tests resemble the way your software is used, the more confidence they can give you."</em> (ยิ่ง Test ของคุณเหมือนวิธีการที่ผู้ใช้ใช้งานซอฟต์แวร์จริงๆ มากเท่าไหร่ มันยิ่งให้ความมั่นใจกับคุณได้มากเท่านั้น)</p><p>RTL จึงบังคับให้เรา Test จากมุมมองของผู้ใช้งานจริงๆ ไม่สนใจว่า State ข้างในเก็บยังไง สนใจแค่ว่าหน้าจอ (DOM) แสดงผลอะไรออกมา</p><h2>Core API: render() และ screen queries</h2><p>การทำงานพื้นฐานของ RTL ประกอบด้วย 2 ขั้นตอน คือ เรนเดอร์คอมโพเนนต์ และ ค้นหา/ตรวจสอบสิ่งที่ปรากฏ</p><pre>import { render, screen } from \'@testing-library/react\';\nimport Greeting from \'./Greeting\';\n\ntest(\'shows greeting message\', () => {\n  render(<Greeting name="John" />);\n  const heading = screen.getByText(/Hello, John/i);\n  expect(heading).toBeInTheDocument();\n});</pre><p>RTL มี <strong>Queries</strong> ให้เลือกใช้มากมาย โดยเรียงลำดับความสำคัญตาม "Accessibility (a11y)" เพื่อให้เราเขียนโค้ดที่รองรับคนพิการ (Screen Readers) ไปในตัวด้วย:</p><ol style="padding-left:20px"><li><strong>getByRole</strong>: ดีที่สุด! ค้นหาตามบทบาทของ Element เช่น button, heading, textbox (ควรใช้ตัวนี้เป็นตัวเลือกแรกเสมอ)</li><li><strong>getByLabelText</strong>: สำหรับ Form input ที่ผูกกับ Label (สำคัญมากสำหรับ User Experience)</li><li><strong>getByPlaceholderText</strong> / <strong>getByText</strong>: ค้นหาด้วยข้อความธรรมดา</li><li><strong>getByTestId</strong>: ตัวเลือกสุดท้ายจริงๆ เมื่อหาด้วยวิธีอื่นไม่ได้ โดยการเติม <code>data-testid="..."</code> ลงใน HTML</li></ol><div class="tip-box"><strong>💡 Pro Tip</strong>: Query มี 3 รูปแบบคือ <code>getBy...</code> (พังทันทีถ้าหาไม่เจอ), <code>queryBy...</code> (คืนค่า null ถ้าหาไม่เจอ เหมาะสำหรับเทสต์ว่า "ไม่มี" สิ่งนี้อยู่บนหน้าจอ) และ <code>findBy...</code> (ทำงานแบบ Async ใช้คู่กับ await เพื่อรอจนกว่าของจะปรากฏบนจอ)</div><h2>การทดสอบ User Interactions ด้วย @testing-library/user-event</h2><p>การจำลองพฤติกรรมผู้ใช้ เช่น การคลิก หรือการพิมพ์ข้อความ แม้ RTL จะมี <code>fireEvent</code> ให้ใช้ แต่ <strong>userEvent</strong> คือสิ่งที่เราแนะนำให้ใช้เสมอ</p><p><code>userEvent</code> ทำการจำลอง Event ต่างๆ ที่เกิดขึ้นบน Browser จริงๆ (เช่น เวลาพิมพ์ 1 ตัวอักษร มันจะเกิด keyDown, keyPress, keyUp, input, change ต่อกัน) ทำให้ได้ผลลัพธ์ที่ใกล้เคียงกับ User ของจริงมากกว่ามาก</p><pre>import userEvent from \'@testing-library/user-event\';\n\ntest(\'allows users to add items to their list\', async () => {\n  const user = userEvent.setup(); // สร้าง instance ของ user\n  render(<TodoList />);\n  \n  const input = screen.getByRole(\'textbox\', { name: /new item/i });\n  const button = screen.getByRole(\'button\', { name: /add/i });\n\n  await user.type(input, \'Learn React\');\n  await user.click(button);\n\n  expect(screen.getByText(\'Learn React\')).toBeInTheDocument();\n});</pre><h2>การทดสอบ Custom Hooks</h2><p>บางครั้งตรรกะอันซับซ้อนถูกแยกออกไปเขียนเป็น Custom Hooks (เช่น <code>useAuth</code> หรือ <code>useFetch</code>) การเทสต์ Hooks เหล่านี้โดยตรงโดยไม่ต้องไปผูกกับ Component ทำได้ง่ายๆ ผ่าน <code>@testing-library/react-hooks</code> หรือถ้าใช้ RTL v13+ จะมี <code>renderHook</code> ให้ใช้เลย</p><pre>import { renderHook, act } from \'@testing-library/react\';\nimport useCounter from \'./useCounter\';\n\ntest(\'should increment counter\', () => {\n  const { result } = renderHook(() => useCounter());\n  \n  // การอัพเดท State ต้องครอบด้วย act()\n  act(() => {\n    result.current.increment();\n  });\n\n  expect(result.current.count).toBe(1);\n});</pre><h2>สรุป</h2><p>React Testing Library ได้เปลี่ยน Paradigm ของการเทสต์ฝั่ง Frontend จากการตรวจสอบ Component Internals ไปสู่การตรวจสอบ User Experience อย่างแท้จริง การหมั่นใช้งาน <code>getByRole</code> จะช่วยให้แอปพลิเคชันของคุณรองรับ Accessibility (a11y) ได้ดีขึ้นโดยอัตโนมัติ และทำให้ Test มีความทนทานต่อการ Refactor สูง (Resilient to Refactoring)</p>'
  },
  {
    number: 4,
    slug: 'integration',
    emoji: '🔗',
    title: 'Integration Testing',
    content: '<h2>ทำไม Unit Test อย่างเดียวถึงไม่พอ?</h2><p>มีมุขตลกคลาสสิกในหมู่ Developer ที่เปรียบเทียบ Unit Test กับหน้าต่างและประตู: หน้าต่างเปิดปิดได้ปกติ (Unit Test ผ่าน), ประตูล็อคได้ปกติ (Unit Test ผ่าน) แต่เมื่อนำมาประกอบเข้ากับบ้านจริง ปรากฏว่าประตูไปขัดหน้าต่างทำให้เปิดไม่ได้! นี่คือปัญหาที่ <strong>Integration Testing</strong> เข้ามาแก้ไข</p><p>แม้แต่ละ Component หรือ Function จะทำงานถูกต้อง 100% แต่การทดสอบ <strong>"รอยต่อ (Integration points)"</strong> ที่ให้พวกมันทำงานร่วมกันต่างหาก คือสิ่งที่จะพิสูจน์ได้ว่าระบบโดยรวมสามารถทำงานสอดประสานกันได้อย่างสมบูรณ์หรือไม่</p><h2>ขอบเขตของ Integration Testing</h2><p>Integration Test สามารถตีความได้ 2 รูปแบบหลักๆ ตามลักษณะสถาปัตยกรรม (Architecture):</p><ul><li><strong>ในระดับ Frontend (Component Integration)</strong>: การนำ Child Components หลายๆ ตัวมาประกอบรวมกันใน Page Component แล้วทดสอบว่าเมื่อ State เกิดการเปลี่ยนแปลง ข้อมูลไหลจากพ่อไปหาลูกได้ถูกต้องหรือไม่</li><li><strong>ในระดับ Backend (System Integration)</strong>: การทดสอบ Endpoint ของ API (เช่น Express หรือ NestJS) ควบคู่ไปกับการอ่านเขียน <strong>ฐานข้อมูล (Database)</strong> จริงๆ หรือการคุยกับ External Services</li></ul><h2>การทำ API Integration Testing ด้วย Supertest</h2><p>ในฝั่ง Node.js การเขียนเทสต์ API ส่วนใหญ่มักจะใช้ <strong>Supertest</strong> ร่วมกับ Jest เครื่องมือนี้ช่วยให้เราสามารถส่ง HTTP Request จำลองเข้าไปยัง Express Application (หรือ framework อื่นๆ) ได้โดยไม่ต้องสั่ง <code>app.listen()</code> เปิดพอร์ตให้ยุ่งยาก</p><pre>import request from \'supertest\';\nimport app from \'../src/app\';\n\ndescribe(\'POST /users\', () => {\n  it(\'should create a new user and return 201 status\', async () => {\n    const payload = { email: \'test@example.com\', password: \'secret123\' };\n    \n    const response = await request(app)\n      .post(\'/users\')\n      .send(payload);\n      \n    expect(response.status).toBe(201);\n    expect(response.body).toHaveProperty(\'id\');\n    expect(response.body.email).toBe(payload.email);\n  });\n});</pre><h2>การทดสอบฐานข้อมูล (Database Integration Tests)</h2><p>การ Mock Database ในระดับ Integration Test เป็นสิ่งที่ไม่ควรทำ! (Anti-pattern) เพราะจุดประสงค์ของเทสต์นี้คือการตรวจสอบว่าคำสั่ง SQL (หรือ Prisma, TypeORM, Mongoose) สามารถคุยกับ Database Engine ของจริงได้อย่างถูกต้องหรือไม่</p><p>แนวทางปฏิบัติที่ดีที่สุด (Best Practice) ในปัจจุบันคือ:</p><ol style="padding-left:20px"><li><strong>ใช้ Test Database แยกต่างหาก</strong>: อย่าใช้ Database ก้อนเดียวกับ Development หรือ (ห้ามเด็ดขาด!) Production เด็ดขาด</li><li><strong>ใช้ Docker / Testcontainers</strong>: การปั่น (Spin up) Database Container ใหม่สดๆ ขึ้นมาเฉพาะช่วงที่รัน Test เป็นวิธีที่ได้รับความนิยมอย่างมาก เพราะจะได้ Database ที่สะอาด (Clean state) เสมอ</li><li><strong>Clean up (Teardown)</strong>: หลังรัน <code>beforeEach</code> หรือ <code>afterEach</code> ควรมีการสั่งรันคำสั่งเคลียร์ตารางหรือลบข้อมูลเสมอ เพื่อให้ Test แต่ละตัวเป็นอิสระต่อกัน (Isolated)</li></ol><div class="tip-box"><strong>💡 Pro Tip</strong>: การรัน Integration Test ร่วมกับฐานข้อมูลจริงจะใช้เวลาค่อนข้างนาน ดังนั้นในหลายๆ โปรเจกต์ ทีมมักจะแยกสคริปต์ <code>npm run test:unit</code> และ <code>npm run test:integration</code> ออกจากกัน เพื่อให้ตอนเขียนโค้ด Developer สามารถรัน Unit Test ได้อย่างรวดเร็ว ส่วน Integration จะรันแค่ตอนก่อน Push หรือบน CI/CD Pipeline เท่านั้น</div><h2>สรุป</h2><p>Integration Test ให้ความมั่นใจในระดับสูงว่าชิ้นส่วนต่างๆ ของซอฟต์แวร์สามารถทำงานร่วมกันได้อย่างราบรื่น แม้จะมีต้นทุนเรื่องเวลาในการ Setup สภาพแวดล้อมและรัน Test ที่นานกว่า Unit Test แต่สำหรับเส้นทางหลักของธุรกิจ (Critical Paths) การมี Integration Test ถือเป็นเสาหลักที่ขาดไม่ได้</p>'
  },
  {
    number: 5,
    slug: 'e2e',
    emoji: '🌍',
    title: 'E2E Testing ด้วย Playwright',
    content: '<h2>เข้าใจ End-to-End (E2E) Testing</h2><p>End-to-End Test อยู่บนยอดสุดของ Testing Pyramid เป็นการทดสอบที่จำลองสถานการณ์จริงที่สุด โดยเปิดเบราว์เซอร์จริง (เช่น Chrome, Firefox, Safari) คอยคลิก กรอกข้อมูล และนำทางไปตามหน้าต่างๆ ราวกับเป็นผู้ใช้งาน (Real User) เพื่อยืนยันว่าระบบตั้งแต่ หน้าบ้าน (Frontend) -> หลังบ้าน (Backend) -> ฐานข้อมูล (Database) ทำงานร่วมกันได้ 100%</p><h2>ทำไมถึงเลือก Playwright?</h2><p>ในช่วงหลายปีที่ผ่านมา Cypress เคยเป็นราชันย์แห่ง E2E ฝั่ง JavaScript แต่ในปัจจุบัน <strong>Playwright</strong> ที่พัฒนาโดยทีมงานจาก Microsoft ได้ผงาดขึ้นมาเป็นตัวเลือกอันดับ 1 แทน ด้วยข้อได้เปรียบที่สำคัญดังนี้:</p><ul><li>รองรับ <strong>Multiple Browsers</strong> อย่างแท้จริง: Chromium (Chrome/Edge), WebKit (Safari), และ Firefox</li><li>ความเร็วสูงส่ง และรองรับ <strong>Parallel Execution</strong> ได้ตั้งแต่แกะกล่อง</li><li>มีเครื่องมือ <strong>Codegen (Trace Viewer)</strong> ที่เจ๋งมาก สามารถบันทึกการคลิกของเราและแปลงเป็นโค้ด Test ให้อัตโนมัติ</li><li>รองรับทั้งหน้าต่างแบบเปิด (Headed) และแบบซ่อน (Headless)</li></ul><h2>การเขียน Test Scenario พื้นฐาน</h2><p>โค้ดของ Playwright จะมีหน้าตาคล้ายกับการใช้ Jest หรือ Mocha ผสมกับ API ควบคุมเบราว์เซอร์</p><pre>import { test, expect } from \'@playwright/test\';\n\ntest(\'user can login successfully\', async ({ page }) => {\n  // 1. นำทางไปยัง URL\n  await page.goto(\'https://my-app.com/login\');\n\n  // 2. หา Elements และกระทำ (Interact)\n  await page.getByLabel(\'Email\').fill(\'user@example.com\');\n  await page.getByLabel(\'Password\').fill(\'supersecret\');\n  await page.getByRole(\'button\', { name: \'Log In\' }).click();\n\n  // 3. ตรวจสอบผลลัพธ์ (Assertion)\n  await expect(page).toHaveURL(\'https://my-app.com/dashboard\');\n  await expect(page.getByText(\'Welcome, User!\')).toBeVisible();\n});</pre><h2>โครงสร้าง Page Object Model (POM)</h2><p>เมื่อโปรเจกต์ใหญ่ขึ้น ถ้าเราเขียนโค้ด E2E ให้ค้นหา Element กระจัดกระจายไปทั่วไฟล์ Test โค้ดจะอ่านยากและดูแลยากมาก (เช่น ถ้า ID ของปุ่มถูกเปลี่ยน ต้องตามแก้หลายสิบไฟล์)</p><p><strong>Page Object Model (POM)</strong> เป็น Design Pattern อมตะสำหรับการทำ E2E โดยเราจะสร้าง Class มาเป็นตัวแทนของ "หน้าเว็บ" (Page) และรวบรวม Locators รวมถึง Action ไว้ในนั้นที่เดียว</p><pre>// LoginPage.ts\nexport class LoginPage {\n  constructor(private page: Page) {}\n\n  async navigate() { await this.page.goto(\'/login\'); }\n  async fillCredentials(u, p) { ... }\n  async submit() { ... }\n}\n\n// login.spec.ts\ntest(\'login flow using POM\', async ({ page }) => {\n  const loginPage = new LoginPage(page);\n  await loginPage.navigate();\n  await loginPage.fillCredentials(\'user\', \'pass\');\n  await loginPage.submit();\n});</pre><div class="tip-box"><strong>💡 Pro Tip</strong>: Playwright มาพร้อมฟีเจอร์ <strong>Auto-waiting</strong> คือมันจะรอ (Wait) ให้ element สามารถคลิกได้หรือมองเห็นได้เองโดยอัตโนมัติก่อนทำ Action คุณแทบไม่จำเป็นต้องเขียนโค้ดจำพวก <code>page.waitForTimeout(5000)</code> อีกต่อไป (ซึ่งเป็นตัวการทำให้เทสต์ช้าและ Flaky)</div><h2>Visual Regression Testing</h2><p>นอกจากการเทสต์ Functionality แล้ว Playwright ยังสามารถเทสต์เรื่องหน้าตาที่ผิดเพี้ยน (UI/CSS) ได้ด้วยฟีเจอร์ <strong>Screenshots</strong> สิ่งนี้เรียกว่า Visual Regression</p><pre>test(\'homepage visual check\', async ({ page }) => {\n  await page.goto(\'/\');\n  // ถ่ายภาพหน้าจอและเปรียบเทียบกับภาพต้นฉบับ (Baseline)\n  await expect(page).toHaveScreenshot(\'home-page.png\');\n});</pre><p>หากมีคนไปแก้ CSS จนปุ่มเบี้ยวหรือสีเพี้ยน Test ตัวนี้ก็จะแจ้งเตือนให้เรารู้ทันที</p><h2>สรุป</h2><p>E2E Testing เป็นด่านสุดท้ายของการป้องกันบั๊กหลุดขึ้น Production เครื่องมืออย่าง Playwright ทำให้การเขียนและการรักษาสคริปต์ E2E เป็นเรื่องง่ายขึ้นมาก อย่างไรก็ตาม ไม่ควรเขียน E2E เยอะจนเกินความจำเป็น (จงจำ Testing Pyramid ไว้) ควรโฟกัสเฉพาะ <strong>Happy Paths และ Business Critical Flows</strong> เช่น การสมัครสมาชิก, การชำระเงิน หรือการค้นหาสินค้า เป็นต้น</p>'
  },
  {
    number: 6,
    slug: 'tdd',
    emoji: '🔴',
    title: 'TDD ในปฏิบัติ',
    content: '<h2>Test-Driven Development (TDD) คืออะไร?</h2><p>คนส่วนใหญ่มักจะเขียนโค้ดหลัก (Production Code) ให้เสร็จก่อน แล้วค่อยกลับมาเขียน Test เพื่อให้ได้ Coverage ตามเป้าหมาย แต่ <strong>TDD (Test-Driven Development)</strong> จะกลับตาลปัตรกระบวนการนี้โดยสิ้นเชิง โดยบังคับให้เรา <strong>"เขียน Test ก่อนเขียนโค้ด"</strong> เสมอ</p><p>TDD ไม่ใช่แค่เรื่องของการ Testing แต่เป็นเรื่องของการออกแบบซอฟต์แวร์ (Design) การเขียน Test ก่อนบังคับให้เราคิดถึง API/Interface จากมุมมองของผู้ใช้งาน (Consumer) มากกว่าจะสนใจแค่ Implementation Details</p><h2>วงจรชีวิตของ TDD: Red - Green - Refactor</h2><p>หัวใจสำคัญของ TDD คือรอบวัฏจักร (Cycle) สั้นๆ 3 ขั้นตอน ซึ่งเรียกกันคุ้นปากว่า <strong>Red-Green-Refactor</strong>:</p><ol style="padding-left:20px"><li><strong>🔴 RED (Write a failing test)</strong>: เริ่มต้นด้วยการเขียน Test ตัวเล็กๆ สำหรับฟีเจอร์ถัดไป เนื่องจากยังไม่มีโค้ดมารองรับ เมื่อรัน Test มันจะต้องพัง (สีแดง) เสมอ (นี่คือการพิสูจน์ว่า Test ทำงานจริงๆ)</li><li><strong>🟢 GREEN (Make it pass)</strong>: เขียนโค้ด Production "ให้น้อยที่สุดและเรียบง่ายที่สุด" เท่าที่จะทำให้ Test ตัวนั้นเปลี่ยนเป็นสีเขียว (ผ่าน) ได้ ในขั้นตอนนี้เรายังไม่สนใจความสวยงามของโค้ด โฟกัสแค่ให้มันทำงานได้ก่อน</li><li><strong>🔵 REFACTOR (Make it better)</strong>: กลับมาปรับปรุงโค้ดให้สะอาดขึ้น (Clean Code) ลดความซ้ำซ้อน (DRY) จัดโครงสร้างให้ดีขึ้น โดยเรามีความมั่นใจเต็มเปี่ยม เพราะมี Test คอยคุ้มกันอยู่ว่าเราไม่ได้ทำของเดิมพัง</li></ol><h2>TDD Kata: ตัวอย่างการปฏิบัติจริง</h2><p>ลองจินตนาการว่าเรากำลังเขียนฟังก์ชันแปลงเลขอารบิกเป็นเลขโรมัน (Roman Numerals Kata)</p><p><strong>รอบที่ 1:</strong></p><ul><li><strong>Red</strong>: เขียนเทสต์ <code>expect(toRoman(1)).toBe(\'I\')</code> (รันแล้วพัง)</li><li><strong>Green</strong>: ประกาศฟังก์ชัน <code>function toRoman() { return \'I\'; }</code> (เทสต์ผ่าน! ใช่ครับ เราฮาร์ดโค้ดมันโต้งๆ แบบนี้แหละ)</li><li><strong>Refactor</strong>: ยังไม่มีอะไรต้องแก้</li></ul><p><strong>รอบที่ 2:</strong></p><ul><li><strong>Red</strong>: เขียนเทสต์ <code>expect(toRoman(2)).toBe(\'II\')</code> (พัง)</li><li><strong>Green</strong>: แก้โค้ด <code>if (num===2) return \'II\'; return \'I\';</code></li><li><strong>Refactor</strong>: ยังพอรับได้ ไปต่อ</li></ul><p><strong>รอบที่ 3:</strong></p><ul><li><strong>Red</strong>: เทสต์กรณีเลข 3 (พัง)</li><li><strong>Green</strong>: <code>if (num===3) return \'III\';</code></li><li><strong>Refactor</strong>: โค้ดชักจะน่าเกลียดแล้ว (Code Smell) ได้เวลา refactor โดยอาจใช้ loop หรือ lookup table ซึ่งเรากล้าแก้เพราะ Test คลุมไว้หมดแล้ว</li></ul><h2>เมื่อไหร่ควรใช้ TDD และเมื่อไหร่ไม่ควรใช้</h2><p>TDD ไม่ใช่กระสุนเงิน (Silver Bullet) มีเวลาที่เหมาะสมและไม่เหมาะสม</p><p><strong>✅ สถานการณ์ที่ TDD เปล่งประกาย:</strong></p><ul><li>Business Logic ที่มีความซับซ้อน เช่น ระบบคำนวณภาษี, ตะกร้าสินค้า, หรือ State Machines</li><li>การสร้าง Public API / Libraries ที่ต้องการให้ Interface ใช้งานง่าย</li><li>การแก้บั๊ก (Reproduce บั๊กใน Test ก่อน แล้วค่อยไปแก้โค้ด เพื่อรับประกันว่าบั๊กนี้จะไม่กลับมาอีก)</li></ul><p><strong>❌ สถานการณ์ที่ TDD อาจเป็นภาระ:</strong></p><ul><li>ช่วงทำ Proof of Concept (PoC) หรือ Prototyping ที่ต้องการความเร็วและ Requirements เปลี่ยนแปลงแทบทุกชั่วโมง</li><li>การจัดวางหน้าตา UI (CSS/Styling) ซึ่งการมองด้วยตาเปล่าเร็วกว่าการเขียนเทสต์มาก</li><li>โค้ดที่ผูกติดกับ 3rd Party มากๆ จนต้องใช้เวลาเขียน Mock นานกว่าเขียน Business Logic เสียอีก</li></ul><h2>สรุป</h2><p>TDD คือปรัชญาที่ต้องใช้เวลาฝึกฝน ในช่วงแรกคุณจะรู้สึกว่ามันช้าและขัดกับสัญชาตญาณเอามากๆ แต่เมื่อคุณทำจนเป็นนิสัย คุณจะพบว่ามันทำให้คุณเขียนโค้ดได้เร็วขึ้นในช่วงท้าย (ลดเวลา Debug อย่างมหาศาล) ได้สถาปัตยกรรมที่สะอาด และได้เอกสารที่มีชีวิต (Test Suite) ไปพร้อมๆ กัน</p>'
  },
  {
    number: 7,
    slug: 'coverage',
    emoji: '📊',
    title: 'Test Coverage',
    content: '<h2>ทำความรู้จักกับ Test Coverage</h2><p><strong>Test Coverage (หรือ Code Coverage)</strong> คือเครื่องมือที่ใช้วัดว่า Automated Tests ที่เราเขียนขึ้นมานั้น ได้เข้าไปสัมผัส (Execute) โค้ดหลัก (Production Code) มากน้อยเพียงใด มักแสดงผลออกมาเป็นเปอร์เซ็นต์ (%)</p><p>ในโลกของ JavaScript มักจะใช้ไลบรารีอย่าง <strong>Istanbul</strong> หรือตัวใหม่กว่าอย่าง <strong>c8</strong> (มักมาพร้อมกับ Vitest) ในการสร้างรายงาน (Coverage Report) ซึ่งเครื่องมือเหล่านี้จะเข้ามาแทรกแซงโค้ดของเรา (Instrumentation) เพื่อจับตาดูว่าตอนรัน Test มีบรรทัดไหนถูกเรียกใช้งานบ้าง</p><h2>มาตรวัดทั้ง 4 ของ Code Coverage</h2><p>Coverage ไม่ได้วัดแค่บรรทัดโค้ดเท่านั้น แต่มักจะแบ่งรายงานออกเป็น 4 ระดับ (Metrics) หลักๆ:</p><ol style="padding-left:20px"><li><strong>Statement Coverage</strong>: มี Statement (คำสั่ง) กี่เปอร์เซ็นต์ที่ถูกรัน</li><li><strong>Branch Coverage (หรือ Logic Coverage)</strong>: สำคัญที่สุด! วัดว่าทางแยกของโค้ด (เช่น <code>if/else</code>, <code>switch</code>, <code>ternary operator</code>) ถูกทดสอบครอบคลุมทั้งกรณีจริง (True) และเท็จ (False) แล้วหรือยัง</li><li><strong>Function Coverage</strong>: วัดว่าแต่ละฟังก์ชัน (หรือเมธอด) ถูกเรียกใช้งานอย่างน้อย 1 ครั้งหรือไม่</li><li><strong>Line Coverage</strong>: วัดจำนวนบรรทัดของโค้ดที่ถูกเรนเดอร์ (คล้าย Statement แต่อิงตามการเว้นบรรทัด)</li></ol><pre>// ตัวอย่าง Branch Coverage\nfunction getDiscount(isMember) {\n  if (isMember) {        // Branch A\n    return 0.1;\n  } else {               // Branch B\n    return 0;\n  }\n}</pre><p>ถ้าเราเขียนเทสต์แค่กรณี <code>isMember = true</code> เราอาจจะได้ Line Coverage 80% แต่ Branch Coverage จะได้แค่ 50% เท่านั้น! ซึ่งหมายความว่ากรณี <code>false</code> ยังเสี่ยงที่จะมีบั๊กซ่อนอยู่</p><h2>การผูก Coverage เข้ากับ CI/CD Pipeline</h2><p>การดู Coverage ในเครื่องตัวเองนั้นดี แต่จะเกิดประโยชน์สูงสุดเมื่อนำไปเชื่อมโยงกับ Continuous Integration (CI) เช่น GitHub Actions หรือ GitLab CI</p><p>เราสามารถตั้งค่า <strong>Coverage Threshold (เกณฑ์ขั้นต่ำ)</strong> ได้ใน Jest:</p><pre>// jest.config.js\nmodule.exports = {\n  coverageThreshold: {\n    global: {\n      branches: 80,\n      functions: 80,\n      lines: 80,\n      statements: 80\n    }\n  }\n};</pre><p>หากมีคนในทีมส่ง Pull Request ที่มีโค้ดห่วยๆ ไม่มีเทสต์กำกับ จนทำให้ Coverage รวมตกหล่นต่ำกว่า 80% ระบบ CI จะ <strong>Fail Pipeline</strong> ทันทีและไม่อนุญาตให้ Merge โค้ดชุดนั้น (Quality Gate) นี่คือวิธีบังคับใช้มาตรฐานโค้ดในทีมอัตโนมัติ</p><h2>ความเชื่อผิดๆ ของตัวเลข "Coverage 100%" (The 100% Myth)</h2><p>ผู้บริหารหรือบางทีมมักจะตั้งเป้าหมายตั้งต้นว่าต้องมี <strong>Code Coverage 100%</strong> ซึ่งในความเป็นจริงกลับเป็นผลเสียมากกว่าผลดี</p><div class="tip-box"><strong>💡 Pro Tip</strong>: กฎของ Goodhart (Goodhart\'s Law) กล่าวว่า <em>"เมื่อการวัดผลกลายเป็นเป้าหมาย มันจะหยุดเป็นมาตรวัดที่ดี (When a measure becomes a target, it ceases to be a good measure)"</em></div><p>สิ่งที่มักจะเกิดขึ้นเมื่อบังคับ 100% Coverage คือ:</p><ul><li>Developer จะพยายาม "แฮ็ก" ระบบด้วยการเขียน Test เปล่าๆ ไม่มีการ <code>expect()</code> (Assertion-free tests) เพียงเพื่อให้โค้ดมันทำงานผ่านไปเท่านั้น</li><li>เกิดการเสียเวลาอันมีค่าไปกับการนั่งเทสต์ Getter/Setter ทั่วไป หรือโค้ด Boilerplate ที่ไม่น่าจะพังได้</li><li>ทำให้ทีมไม่อยากเขียนโค้ดใหม่หรือ Refactor เพราะจะทำให้ Coverage ตก</li></ul><p><strong>ตัวเลขที่เหมาะสม:</strong> บริษัทยักษ์ใหญ่ส่วนมากมองว่า <strong>70% - 80% เป็นจุดที่คุ้มค่า (Sweet spot)</strong> ระหว่างคุณภาพและเวลาที่เสียไป ส่วนอีก 20% ที่เหลือคือโค้ดที่ไม่สำคัญหรือเทสต์ได้ยากจนไม่คุ้มค่าเหนื่อย</p><h2>สรุป</h2><p>Test Coverage เป็นเครื่องมือที่ดีในการ <strong>"หาจุดบอด (Blind Spots)"</strong> ของโค้ดที่ไม่เคยถูกทดสอบเลย แต่มันไม่สามารถบอกได้ว่า Test ที่เราเขียนนั้น "มีคุณภาพ" หรือไม่ ใช้ Coverage เป็นแผนที่นำทาง แต่อย่าใช้มันเป็นพระเจ้าที่ต้องบูชาจนหน้ามืดตามัว</p>'
  },
  {
    number: 8,
    slug: 'snapshot',
    emoji: '📸',
    title: 'Snapshot และ Visual Testing',
    content: '<h2>Snapshot Testing คืออะไร?</h2><p><strong>Snapshot Testing</strong> คือเทคนิคการทดสอบที่ทำงานคล้ายกับการ "ถ่ายรูป" สถานะของโค้ด (หรือ UI) ในตอนที่มันทำงานถูกต้องเก็บเอาไว้ แล้วเมื่อมีการรันเทสต์ในครั้งถัดไป ระบบจะทำการเปรียบเทียบผลลัพธ์ใหม่ที่ได้ กับรูปถ่ายเดิมที่เคยถ่ายไว้ (Baseline) หากมีความแตกต่างกันแม้แต่ตัวอักษรเดียว เทสต์นั้นจะล้มเหลว (Fail) ทันที</p><p>ในโลกของ Jest, Snapshot Testing ได้รับความนิยมมากกับการเทสต์ React Components แต่ก็สามารถนำไปใช้กับโครงสร้างข้อมูล (Data Structures), ผลลัพธ์ของ API (JSON), หรือแม้กระทั่ง SQL Queries ได้เช่นกัน</p><pre>import renderer from \'react-test-renderer\';\nimport Link from \'../Link\';\n\nit(\'renders correctly\', () => {\n  const tree = renderer\n    .create(<Link page="http://www.facebook.com">Facebook</Link>)\n    .toJSON();\n    \n  // สิ่งมหัศจรรย์เกิดขึ้นบรรทัดนี้\n  expect(tree).toMatchSnapshot();\n});</pre><p>เมื่อรันเทสต์นี้ครั้งแรก Jest จะสร้างโฟลเดอร์ <code>__snapshots__</code> และเก็บไฟล์ข้อมูลที่มีหน้าตาเหมือน HTML/DOM ลงไป ในการรันครั้งต่อๆ ไป มันจะนำผลลัพธ์มาเปรียบเทียบกับไฟล์นี้</p><h2>ปัญหาของ Snapshot (Snapshot Fatigue)</h2><p>แม้ Snapshot จะเขียนง่ายและได้ Coverage เร็ว แต่มันก็ถูกนักพัฒนาหลายคนมองว่าเป็น <strong>Anti-pattern</strong> หากใช้ผิดวิธี</p><p>ปัญหาที่พบบ่อยคือ <strong>Snapshot Fatigue</strong> (อาการเหนื่อยล้าจาก Snapshot): เมื่อมีการแก้โค้ดเพียงเล็กน้อย (เช่น เปลี่ยน margin ใน CSS หรือแก้คำผิด) Snapshot จำนวนมากจะพังพร้อมกัน Developer มักจะแก้ปัญหาด้วยการกด <code>jest -u</code> (Update Snapshot) เพื่อทับของเดิมทันทีโดยไม่อ่านด้วยซ้ำว่ามันพังเพราะอะไร ทำให้การเทสต์หมดความหมายไปโดยปริยาย</p><p><strong>Best Practices สำหรับ Snapshot:</strong></p><ul><li>ใช้กับ Component ที่หน้าตา "นิ่งแล้ว" (Stable) ไม่ค่อยเปลี่ยนแปลง</li><li>พยายามให้ Snapshot มีขนาดเล็ก (Small) และอ่านง่าย ถ้า Snapshot หนึ่งตัวยาวเป็นร้อยบรรทัด แสดงว่ามีปัญหาแล้ว</li><li>อย่าใช้ Snapshot แทน Explicit Assertions (ถ้าต้องการตรวจว่ามีปุ่ม Submit หรือไม่ ให้ใช้ React Testing Library ค้นหาปุ่มนั้นตรงๆ ดีกว่าการเอาทั้ง DOM ไปทำ Snapshot)</li></ul><h2>ก้าวล้ำไปอีกขั้นด้วย Visual Regression Testing</h2><p>Snapshot แบบโค้ดนั้นยังจับภาพได้แค่ "โครงสร้าง (Structure)" แต่ไม่สามารถบอกได้ว่าหน้าตา (Visual) สวยงามหรือพังเบี้ยวไปหรือไม่ เช่น ถ้า CSS ไม่โหลด โครงสร้าง HTML อาจจะเหมือนเดิมเป๊ะ และ Snapshot ก็จะผ่าน (False Positive)</p><p><strong>Visual Regression Testing</strong> จึงถือกำเนิดขึ้น เพื่อทำการถ่าย "ภาพสกรีนช็อตจริงๆ (Pixel-by-pixel)" ของเบราว์เซอร์ แล้วนำมาเปรียบเทียบกัน</p><p>เครื่องมือระดับองค์กร (Enterprise Tools) ที่เป็นที่นิยมได้แก่:</p><ol style="padding-left:20px"><li><strong>Chromatic</strong>: สร้างโดยทีม Storybook สามารถทำงานร่วมกับ Storybook ได้อย่างไร้รอยต่อ อัพโหลด Component ขึ้นไปถ่ายรูปบน Cloud ได้เลย</li><li><strong>Percy (by BrowserStack)</strong>: เก็บ Snapshot อัตโนมัติใน CI/CD มี UI ให้ผู้รีวิว (Reviewer) สามารถ Approve ความเปลี่ยนแปลงของดีไซน์ได้</li><li><strong>Playwright</strong>: มีระบบ <code>toHaveScreenshot()</code> มาให้ในตัว เหมาะสำหรับคนที่ทำ E2E อยู่แล้ว</li></ol><div class="tip-box"><strong>💡 Pro Tip</strong>: ความท้าทายหลักของ Visual Testing คือ <strong>ความไม่แน่นอน (Flakiness)</strong> จาก Animation (ควรปิดทิ้งตอนเทสต์), ข้อมูลจำลองที่สุ่มค่ามา (Dynamic Data), หรือแม้แต่ความต่างของระบบปฏิบัติการ (Font บน Mac กับ Windows หนาไม่เท่ากัน) ดังนั้นการใช้เครื่องมือเฉพาะทางแบบ Cloud มักจะขจัดปัญหาสภาพแวดล้อมเหล่านี้ได้ดีกว่าการเซฟรูปลงเครื่องตัวเอง</div><h2>สรุป</h2><p>Snapshot และ Visual Testing เป็นเสมือน "สายตรวจ" ที่คอยแจ้งเตือนเมื่อมีการเปลี่ยนแปลงที่คุณไม่ได้ตั้งใจ (Unintended Changes) เกิดขึ้น มันทรงพลังมากในการป้องกันบั๊กเรื่องหน้าตา UI แต่จงใช้อย่างระมัดระวัง เพื่อไม่ให้ทีมของคุณมีนิสัยเสพติดการกด "Update All" โดยไม่ตรวจสอบ</p>'
  },
  {
    number: 9,
    slug: 'best-practices',
    emoji: '⭐',
    title: 'Testing Best Practices',
    content: '<h2>ศิลปะแห่งการเขียนโค้ดทดสอบ (The Art of Testing)</h2><p>ในบทสุดท้ายของคอร์สนี้ เราจะมารวบรวม <strong>แนวทางปฏิบัติที่เป็นเลิศ (Best Practices)</strong> และสิ่งที่ควรหลีกเลี่ยง (Anti-patterns) เพื่อยกระดับให้ Test Suite ของคุณมีความเสถียร (Reliable) อ่านง่าย (Readable) และรักษาง่าย (Maintainable) ในระยะยาว</p><h2>1. กฎ 3 ส่วน (The AAA Pattern)</h2><p>โครงสร้างของ Test ที่ดีควรแบ่งออกเป็น 3 ส่วนชัดเจน (มักจะคั่นด้วยบรรทัดว่าง) เพื่อให้อ่านเข้าใจง่าย เรียกว่า AAA Pattern:</p><ol style="padding-left:20px"><li><strong>Arrange (เตรียมการ)</strong>: Setup ข้อมูล, Mock ค่า, เตรียม Component ให้พร้อม</li><li><strong>Act (กระทำ)</strong>: เรียกฟังก์ชัน, จำลองการคลิก, หรือส่ง Request (ควรมีแค่ 1 Action หลักเท่านั้น)</li><li><strong>Assert (ตรวจสอบ)</strong>: ใช้ <code>expect()</code> เพื่อยืนยันผลลัพธ์</li></ol><pre>test(\'should calculate total price with tax\', () => {\n  // 1. Arrange\n  const cart = [{ price: 100 }, { price: 200 }];\n  const taxRate = 0.07;\n\n  // 2. Act\n  const total = calculateTotal(cart, taxRate);\n\n  // 3. Assert\n  expect(total).toBe(321);\n});</pre><h2>2. การตั้งชื่อ Test (Test Naming)</h2><p>ชื่อของ Test เป็นสิ่งแรกที่คุณจะเห็นเวลาที่ Test พังใน CI มันควรจะทำหน้าที่เหมือนเป็น "เอกสาร (Documentation)" แจ้งให้ทราบว่าอะไรที่ผิดปกติ</p><p>หลีกเลี่ยงชื่อกว้างๆ เช่น <code>it(\'works\', ...)</code> หรือ <code>it(\'test function\', ...)</code></p><p>แนะนำให้ตั้งชื่อตามโครงสร้าง: <strong>"ควรทำอะไร (Should...)" "ภายใต้เงื่อนไขใด (When...)"</strong></p><ul><li>✅ <code>it(\'should return 400 when email is invalid\')</code></li><li>✅ <code>it(\'should display error message if API fails\')</code></li></ul><h2>3. DRY ใน Test (จงระวังให้ดี)</h2><p>ใน Production Code กฎ "Don\'t Repeat Yourself (DRY)" คือเรื่องดี แต่สำหรับ Testing Code การใช้ DRY มากเกินไปจนต้องสร้าง Helper functions ซ้อนกันหลายชั้น จะทำให้เสีย <strong>DAMP (Descriptive And Meaningful Phrases)</strong></p><p>Test ที่ดีควรอ่านเข้าใจได้ตั้งแต่ต้นจนจบภายใน Block ของตัวเอง การมีโค้ดซ้ำกันนิดหน่อย (Duplication) ใน Test ถือเป็นเรื่องที่ยอมรับได้ แลกกับการที่ไม่ต้องกระโดดไปดูไฟล์อื่นเพื่อทำความเข้าใจ</p><h2>4. ปีศาจที่เรียกว่า Flaky Tests</h2><p><strong>Flaky Test</strong> คือ Test ที่บางครั้งก็ผ่าน บางครั้งก็พัง ทั้งๆ ที่ไม่มีการแก้โค้ดเลย (Non-deterministic) นี่คือสิ่งที่บั่นทอนความเชื่อมั่นของทีมต่อ Automated Testing มากที่สุด เมื่อเกิด Flaky test นักพัฒนามักจะแก้ปัญหาชั่วคราวด้วยการกด Re-run Pipeline ซึ่งเป็นสัญญาณอันตราย</p><p><strong>สาเหตุหลักของ Flaky Tests:</strong></p><ul><li>พึ่งพาข้อมูลจาก Database หรือ API จริง ที่อาจจะช้าหรือล่ม (แก้ด้วยการ Mock หรือใช้ Test DB สดใหม่)</li><li>พึ่งพาเวลา (Time-based) หรือ Date.now() ตรงๆ (แก้ด้วย Fake Timers)</li><li>การเรียงลำดับการรันเทสต์ (Test Order Dependency) เทสต์ A ต้องรันก่อนเทสต์ B (แก้ด้วยการ Clean state ใน beforeEach เสมอ)</li><li>ใน E2E เกิดจากจังหวะ Animation หรือ Loading นานเกินไป (แก้ด้วย Auto-waiting ของ Playwright ไม่ใช่ hard sleep)</li></ul><div class="tip-box"><strong>💡 Pro Tip</strong>: กฎเหล็กคือ หากมี Flaky Test เกิดขึ้น คุณต้องเลือก <strong>"แก้ให้หายขาด" หรือ "ลบ/ข้าม (skip) มันทิ้งซะ"</strong> อย่าปล่อยให้มันอยู่ใน CI Pipeline เด็ดขาด เพราะมันจะทำให้ทีมเพิกเฉยต่อ Test สีแดง (Broken Window Theory)</div><h2>5. Test คือ "เอกสารที่มีชีวิต (Living Documentation)"</h2><p>ในโปรเจกต์ขนาดใหญ่ เอกสาร (Documentation) มักจะล้าสมัยเสมอเมื่อโค้ดเปลี่ยนไป แต่ Test Suite จะไม่มีวันโกหกคุณ ถ้าคุณอยากรู้ว่า API เส้นนี้รับ Payload หน้าตาแบบไหน หรือ Component นี้รับ Props อะไรได้บ้าง การเข้าไปดูที่ไฟล์ Test ควรมองเห็นภาพรวมได้ชัดเจนที่สุด</p><h2>ส่งท้ายคอร์ส</h2><p>การลงทุนเขียน Automated Tests เปรียบเสมือนการทำประกันชีวิตให้กับซอฟต์แวร์ของคุณ แม้จะต้องจ่ายเบี้ยประกัน (เวลาในการเขียนเทสต์) ทุกวัน แต่วันที่เกิดวิกฤต (Refactoring หรือเจอ Edge case ร้ายแรง) คุณจะขอบคุณตัวเองในอดีตที่ตัดสินใจวางระบบ Testing เอาไว้ ขอให้สนุกกับการเขียนโค้ดที่เชื่อถือได้และนอนหลับสบายในทุกๆ คืนวันศุกร์ครับ!</p>'
  }
];
