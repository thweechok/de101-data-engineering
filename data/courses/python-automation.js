export const chapters = [
  {
    number: 0,
    slug: 'overview',
    emoji: '🐍',
    title: 'Python สำหรับ Automation: ไม่ใช่ DE แต่เป็นเครื่องมือทำงาน',
    content: '<div class="chapter-content"><h2>ทำไมคนทำงานทั่วไปถึงควรเรียน Python?</h2><p>คำถามที่ได้ยินบ่อยคือ "Python ไม่ใช่สำหรับ Programmer หรือ Data Engineer เหรอ?" คำตอบคือ <strong>Python ในปี 2025 คือเครื่องมือ Productivity</strong> เหมือนที่ Excel เคยเป็นในยุค 2000 ถ้าคุณทำงานที่มีการดึงข้อมูล, จัดการไฟล์, ส่ง Report หรือเก็บข้อมูลจากเว็บ Python จะช่วยให้คุณทำสิ่งเหล่านั้นได้เร็วขึ้น 10-100 เท่า</p><h3>Python ทำอะไรได้บ้างในงานจริง?</h3><p>นี่คือตัวอย่างงานจริงที่คนไม่ใช่ Developer ใช้ Python แก้ปัญหาได้ทุกวัน: <strong>ดึงราคาสินค้าคู่แข่ง</strong>จากเว็บทุกชั่วโมงโดยอัตโนมัติ, <strong>ส่ง Report ทางอีเมล</strong>ทุกเช้าโดยไม่ต้องเปิดคอม, <strong>จัดเรียงและ Rename ไฟล์</strong>หลักพันไฟล์ภายในไม่กี่วินาที, <strong>อ่านข้อมูลจาก PDF</strong>หลายร้อยหน้าและรวมเป็น Excel, <strong>อัพเดต Google Sheets</strong>จากหลายแหล่งข้อมูลอัตโนมัติ และ <strong>สร้าง LINE Bot</strong>ที่แจ้งเตือนเมื่อ Stock ต่ำ</p><h3>Python vs Excel: เมื่อไหรควรใช้อะไร?</h3><p>ใช้ <strong>Excel/Sheets</strong> เมื่อ: ต้องการ Visualize และ Explore ข้อมูลเร็วๆ, ทำรายงานที่ต้องส่งให้ผู้บริหารดู, หรือทำงานในทีมที่ทุกคนเปิด Excel ได้ ใช้ <strong>Python</strong> เมื่อ: ข้อมูลมีมากกว่า 100,000 แถว, ต้องทำซ้ำทุกวัน/สัปดาห์, ต้องดึงข้อมูลจากเว็บหรือ API, หรือต้องการความยืดหยุ่นในการประมวลผลที่ Excel ทำไม่ได้ คอร์สนี้จะสอนให้คุณรู้ว่าควรใช้เครื่องมือไหนกับงานไหน</p><h3>คอร์สนี้ต่างจากคอร์ส Python ทั่วไปอย่างไร?</h3><p>คอร์ส Python ส่วนใหญ่เน้นสอน Concept ของ Programming (OOP, Algorithms, Data Structures) ซึ่งดีสำหรับคนที่จะเป็น Developer แต่ <em>คอร์สนี้เน้นที่ "ทำงานได้จริงเร็วที่สุด"</em> คุณจะเรียนรู้เฉพาะส่วนของ Python ที่ใช้สำหรับ Automation และจะเห็นผลลัพธ์จริงตั้งแต่วันแรก</p><h3>สิ่งที่คุณจะสร้างได้เมื่อจบคอร์ส</h3><ul><li>🗂️ ระบบจัดการไฟล์อัตโนมัติ</li><li>📊 ระบบดึงข้อมูลจากเว็บและบันทึกลง Excel/Sheets</li><li>📧 Robot ส่ง Email Report รายวัน/รายสัปดาห์</li><li>📄 ระบบสร้าง PDF Report อัตโนมัติ</li><li>🤖 LINE Bot แจ้งเตือนจาก Python</li><li>🌐 Script รันบน Cloud 24/7 ไม่ต้องเปิดคอม</li></ul><h3>AI ช่วยให้เรียน Python ง่ายขึ้นมาก</h3><p>ด้วย ChatGPT หรือ Claude คุณไม่จำเป็นต้องจำ Syntax ทุกอย่าง แค่รู้ว่าต้องการทำอะไร แล้วให้ AI เขียน Code ให้ จากนั้นคุณแค่ต้องอ่านทำความเข้าใจ, ปรับแต่งตามความต้องการ, และรัน ทักษะที่สำคัญที่สุดในการใช้ Python ยุค AI ไม่ใช่การจำ Syntax แต่คือ <em>การอธิบายปัญหาให้ AI เข้าใจและตรวจสอบ Code ที่ AI เขียนให้ได้</em></p><blockquote><p>🎯 <strong>เป้าหมาย:</strong> หลังจบคอร์ส คุณจะประหยัดเวลาทำงานได้อย่างน้อย 5-10 ชั่วโมงต่อสัปดาห์ด้วย Python Automation และมีทักษะที่หายากในตลาดงาน/ธุรกิจไทย</p></blockquote></div>'
  },
  {
    number: 1,
    slug: 'setup',
    emoji: '⚙️',
    title: 'ติดตั้ง Python, VS Code, และ Libraries หลัก',
    content: '<div class="chapter-content"><h2>Setup Python Environment: เตรียมพื้นที่ทำงานให้พร้อม</h2><p>การ Setup ที่ถูกต้องตั้งแต่แรกจะช่วยป้องกันปัญหาหัวปวดในภายหลังได้มาก บทนี้จะ Setup Python Environment ที่เป็น Professional Grade แต่ทำได้ภายใน 30 นาที</p><h3>Step 1: ติดตั้ง Python</h3><p>ไปที่ <strong>python.org/downloads</strong> ดาวน์โหลด Python 3.12 หรือใหม่กว่า (เลือก Windows Installer 64-bit) <em>สำคัญมาก:</em> ตอนติดตั้งให้ Checkbox "Add Python to PATH" ก่อน Install เสมอ ถ้าลืมจะต้องตั้งค่า Environment Variable ด้วยตัวเอง ทดสอบโดยเปิด Command Prompt แล้วพิมพ์ <code>python --version</code> ควรแสดง Version Number</p><h3>Step 2: ติดตั้ง VS Code</h3><p>VS Code (Visual Studio Code) เป็น Code Editor ฟรีที่ดีที่สุดสำหรับ Python ดาวน์โหลดจาก <strong>code.visualstudio.com</strong> หลังติดตั้งให้เปิด VS Code และติดตั้ง Extensions: <strong>Python</strong> (by Microsoft) — จำเป็น, <strong>Pylance</strong> — Auto-complete ที่ดี, <strong>Python Indent</strong> — จัดการ Indentation อัตโนมัติ, <strong>GitLens</strong> — Version Control</p><h3>Step 3: สร้าง Virtual Environment</h3><p>Virtual Environment เป็นพื้นที่แยกสำหรับแต่ละ Project เพื่อป้องกัน Library ของ Project หนึ่งรบกวนอีก Project เปิด Terminal ใน VS Code (Ctrl+`) แล้วรัน: <code>python -m venv venv</code> จากนั้น Activate: Windows: <code>venv\Scripts\activate</code> Mac/Linux: <code>source venv/bin/activate</code> คุณจะเห็น <code>(venv)</code> ปรากฏที่ต้น Command Line แสดงว่า Active แล้ว</p><h3>Step 4: ติดตั้ง Libraries หลัก</h3><p>รัน Command นี้เพื่อติดตั้งทุก Library ที่ต้องใช้ในคอร์ส: <code>pip install pandas openpyxl requests beautifulsoup4 python-dotenv schedule reportlab gspread google-auth openai anthropic</code> แต่ละ Library มีไว้สำหรับ: <strong>pandas</strong> วิเคราะห์ข้อมูล Excel/CSV, <strong>openpyxl</strong> อ่าน/เขียน Excel, <strong>requests + beautifulsoup4</strong> Web Scraping, <strong>python-dotenv</strong> จัดการ API Keys อย่างปลอดภัย, <strong>schedule</strong> ตั้งเวลารัน Script, <strong>reportlab</strong> สร้าง PDF, <strong>gspread</strong> Google Sheets API, <strong>openai + anthropic</strong> เชื่อม AI API</p><h3>Step 5: สร้างไฟล์ Python แรก</h3><p>สร้างไฟล์ <code>hello.py</code> แล้วเขียน: <code>print("สวัสดี Python!")</code> รันด้วย <code>python hello.py</code> ถ้าเห็น "สวัสดี Python!" แสดงว่าพร้อมแล้ว!</p><h3>Python Basics ที่ต้องรู้ก่อน</h3><p>ก่อนเข้าบทต่อไป มาทบทวน Python พื้นฐานที่สำคัญ: <strong>Variables:</strong> <code>name = "สมชาย"</code>, <code>price = 150.5</code>, <code>count = 10</code> <strong>Lists:</strong> <code>fruits = ["apple", "banana", "mango"]</code> เข้าถึงด้วย <code>fruits[0]</code> <strong>Dictionaries:</strong> <code>product = {"name": "กาแฟ", "price": 45}</code> เข้าถึงด้วย <code>product["name"]</code> <strong>For Loops:</strong> <code>for fruit in fruits: print(fruit)</code> <strong>Functions:</strong> <code>def calculate_total(price, qty): return price * qty</code> <strong>If/Else:</strong> <code>if price > 100: print("แพง") else: print("ราคาเหมาะสม")</code></p><h3>การใช้ .env ไฟล์สำหรับ API Keys</h3><p>อย่า Hardcode API Keys ใน Code เด็ดขาด สร้างไฟล์ <code>.env</code> ในโฟลเดอร์ Project: <code>OPENAI_API_KEY=sk-xxxxx</code>, <code>LINE_TOKEN=xxxxx</code> แล้วเรียกใช้ใน Python: <code>from dotenv import load_dotenv; import os; load_dotenv(); key = os.getenv("OPENAI_API_KEY")</code> และเพิ่ม <code>.env</code> ใน <code>.gitignore</code> เพื่อไม่ให้ Push ขึ้น GitHub</p><blockquote><p>💡 <strong>Tip:</strong> ติดตั้ง Extension "Thunder Client" ใน VS Code เพื่อทดสอบ API ได้โดยตรงโดยไม่ต้องเปิด Postman แยก ช่วยในการทดสอบ API Endpoints ก่อนเขียน Code</p></blockquote></div>'
  },
  {
    number: 2,
    slug: 'file-automation',
    emoji: '🗂️',
    title: 'File Automation: เคลื่อนย้าย, Rename, จัดโฟลเดอร์อัตโนมัติ',
    content: '<div class="chapter-content"><h2>File Automation: จัดการไฟล์ด้วย Python</h2><p>มีไฟล์หลายร้อยไฟล์ที่ต้อง Rename, จัดเข้าโฟลเดอร์, หรือย้ายตามวันที่? งานที่ใช้เวลาหลายชั่วโมง Python ทำให้เสร็จในไม่กี่วินาที บทนี้จะสอน <strong>os</strong> และ <strong>pathlib</strong> สองเครื่องมือหลักสำหรับ File Automation</p><h3>ทำความรู้จัก os และ pathlib</h3><p><strong>os module</strong> เป็น Module พื้นฐานของ Python สำหรับจัดการ Files และ Directories: <code>import os</code> <strong>pathlib</strong> เป็น Modern Way ที่ใช้งานง่ายกว่า: <code>from pathlib import Path</code> แนะนำให้ใช้ pathlib เป็นหลักเพราะ Code อ่านง่ายกว่ามาก</p><h3>การทำงานกับ Path</h3><p><code>p = Path("D:/Documents/Reports")</code> สร้าง Path Object, <code>p.exists()</code> เช็คว่ามีอยู่จริงไหม, <code>p.is_file()</code> เป็นไฟล์ไหม, <code>p.is_dir()</code> เป็นโฟลเดอร์ไหม, <code>p.suffix</code> ดู Extension (.xlsx, .pdf ฯลฯ), <code>p.stem</code> ชื่อไฟล์ไม่รวม Extension, <code>p.parent</code> โฟลเดอร์ที่อยู่, <code>list(p.glob("*.xlsx"))</code> หาไฟล์ Excel ทั้งหมดในโฟลเดอร์</p><h3>Project 1: Auto Rename ไฟล์ตาม Pattern</h3><p>สมมติมีไฟล์ชื่อ "report 2025-01-15.xlsx", "report 2025-01-16.xlsx" ต้องการ Rename เป็น "2025-01-15_report.xlsx": <code>from pathlib import Path; folder = Path("D:/Reports"); for f in folder.glob("report *.xlsx"): date_part = f.stem.split(" ")[1]; new_name = f.parent / f"{date_part}_report.xlsx"; f.rename(new_name); print(f"Renamed: {f.name} -> {new_name.name}")</code></p><h3>Project 2: จัดไฟล์เข้าโฟลเดอร์ตาม Extension</h3><p>รับไฟล์ทุกประเภทในโฟลเดอร์ Downloads แล้วจัดเข้าโฟลเดอร์ตาม Type: <code>import shutil; from pathlib import Path; downloads = Path("C:/Users/User/Downloads"); categories = {".pdf": "PDFs", ".xlsx": "Excel", ".jpg": "Images", ".png": "Images", ".mp4": "Videos"}; for f in downloads.iterdir(): if f.is_file() and f.suffix in categories: dest = downloads / categories[f.suffix]; dest.mkdir(exist_ok=True); shutil.move(str(f), str(dest / f.name))</code></p><h3>Project 3: สำรองไฟล์รายวัน</h3><p>Script นี้ Copy ไฟล์สำคัญไปยังโฟลเดอร์ Backup พร้อมวันที่: <code>import shutil, datetime; from pathlib import Path; source = Path("D:/ImportantProject"); today = datetime.date.today().strftime("%Y-%m-%d"); backup = Path(f"D:/Backups/{today}"); shutil.copytree(str(source), str(backup), dirs_exist_ok=True); print(f"Backed up to {backup}")</code></p><h3>Project 4: หาไฟล์ Duplicate</h3><p>ใช้ Hash เพื่อหาไฟล์ที่เนื้อหาเหมือนกันแต่ชื่อต่างกัน: <code>import hashlib; from pathlib import Path; from collections import defaultdict; def get_hash(filepath): h = hashlib.md5(); h.update(Path(filepath).read_bytes()); return h.hexdigest(); files = defaultdict(list); folder = Path("D:/Downloads"); for f in folder.rglob("*"): if f.is_file(): files[get_hash(f)].append(f); duplicates = {k: v for k, v in files.items() if len(v) > 1}; print(f"Found {len(duplicates)} groups of duplicates")</code></p><h3>Watchdog — Monitor โฟลเดอร์ Real-time</h3><p>Library <code>watchdog</code> ทำให้ Python ตรวจสอบโฟลเดอร์ตลอดเวลาและ Trigger Action เมื่อมีไฟล์ใหม่: <code>pip install watchdog</code> เหมาะสำหรับ: Auto Process ไฟล์ที่ Client ส่งมา, แจ้งเตือนเมื่อมีไฟล์เข้า Folder, หรือ Auto Upload ไฟล์ขึ้น Cloud</p><blockquote><p>⚡ <strong>Real Case:</strong> ร้านค้าออนไลน์รับสลิปโอนเงินจากลูกค้าผ่าน LINE แล้ว Save ลงโฟลเดอร์ Watchdog Script ตรวจจับไฟล์ใหม่ ส่งผ่าน OCR API อ่านตัวเลขยอดโอน บันทึกลง Google Sheets และส่ง Confirmation กลับลูกค้าอัตโนมัติ ทั้งหมดภายใน 30 วินาทีโดยไม่มีคนทำ</p></blockquote></div>'
  },
  {
    number: 3,
    slug: 'excel-pandas',
    emoji: '🐼',
    title: 'Excel/CSV ด้วย Pandas: อ่าน, แก้ไข, สรุปข้อมูล',
    content: '<div class="chapter-content"><h2>Pandas: Python สำหรับข้อมูล Excel ที่ Excel เองทำไม่ได้</h2><p>Pandas เป็น Library ที่ทรงพลังที่สุดสำหรับวิเคราะห์ข้อมูลใน Python ลองนึกภาพ Excel ที่สามารถจัดการข้อมูลหลายล้านแถว, Merge ตารางหลายอันพร้อมกัน, และทำ Automation ได้ทั้งหมดนี้ด้วย Code ไม่กี่บรรทัด นั่นคือ Pandas</p><h3>อ่านไฟล์ Excel และ CSV</h3><p><code>import pandas as pd</code> อ่าน Excel: <code>df = pd.read_excel("sales.xlsx", sheet_name="January")</code> อ่าน CSV: <code>df = pd.read_csv("data.csv", encoding="utf-8-sig")</code> (ใช้ utf-8-sig สำหรับไฟล์ภาษาไทย) ดู 5 แถวแรก: <code>df.head()</code> ดูข้อมูล Overview: <code>df.info()</code> สถิติเบื้องต้น: <code>df.describe()</code></p><h3>Data Selection — เลือกข้อมูลที่ต้องการ</h3><p>เลือกคอลัมน์: <code>df["ยอดขาย"]</code> หรือหลายคอลัมน์: <code>df[["ชื่อ", "ยอดขาย", "เดือน"]]</code> กรองแถวตามเงื่อนไข: <code>df[df["ยอดขาย"] > 10000]</code> กรองหลายเงื่อนไข: <code>df[(df["เดือน"] == "มกราคม") & (df["สาขา"] == "สุขุมวิท")]</code> ใช้ <code>.loc</code> สำหรับ Label-based และ <code>.iloc</code> สำหรับ Index-based</p><h3>Data Cleaning ด้วย Pandas</h3><p>ลบแถวที่มีค่า Null: <code>df.dropna()</code> เติมค่า Null: <code>df["ยอดขาย"].fillna(0)</code> ลบ Duplicate: <code>df.drop_duplicates()</code> Rename Column: <code>df.rename(columns={"old": "new"})</code> เปลี่ยน Type: <code>df["วันที่"] = pd.to_datetime(df["วันที่"])</code> ตัด Space: <code>df["ชื่อ"] = df["ชื่อ"].str.strip()</code> แทนที่ค่า: <code>df["สถานะ"].replace({"Y": "ใช่", "N": "ไม่ใช่"})</code></p><h3>GroupBy — สรุปข้อมูลเหมือน Pivot Table</h3><p><code>df.groupby("สาขา")["ยอดขาย"].sum()</code> รวมยอดขายตามสาขา <code>df.groupby(["สาขา","เดือน"]).agg({"ยอดขาย": "sum", "จำนวน_Order": "count"})</code> สรุปหลายคอลัมน์พร้อมกัน <code>df.groupby("สาขา")["ยอดขาย"].agg(["sum","mean","max","min"])</code> สถิติครบในคำสั่งเดียว</p><h3>Merge DataFrames — Join ตาราง</h3><p>เหมือน VLOOKUP/JOIN ใน SQL: <code>result = pd.merge(orders_df, products_df, on="product_id", how="left")</code> how options: "left" (เอาทุก Row จาก Left), "inner" (เฉพาะที่ Match ทั้งคู่), "outer" (ทุก Row จากทั้งคู่) สำหรับการรวมตารางหลายอัน (เหมือน Append): <code>combined = pd.concat([df_jan, df_feb, df_mar], ignore_index=True)</code></p><h3>สร้างไฟล์ Excel ที่สวยงาม</h3><p>Pandas + openpyxl ทำ Excel ที่ Format สวยได้: <code>with pd.ExcelWriter("report.xlsx", engine="openpyxl") as writer: df.to_excel(writer, sheet_name="Data", index=False); summary.to_excel(writer, sheet_name="Summary", index=False)</code> ใช้ <code>xlsxwriter</code> สำหรับ Format ขั้นสูง: สี, Bold, Chart ใน Excel จาก Python</p><h3>การทำงานกับวันที่</h3><p><code>df["วันที่"] = pd.to_datetime(df["วันที่"])</code> แปลงเป็น datetime ก่อนเสมอ จากนั้น: <code>df["เดือน"] = df["วันที่"].dt.month</code>, <code>df["ปี"] = df["วันที่"].dt.year</code>, <code>df["วันในสัปดาห์"] = df["วันที่"].dt.day_name()</code>, กรองตาม Date Range: <code>df[df["วันที่"].between("2025-01-01", "2025-03-31")]</code></p><blockquote><p>🚀 <strong>Performance Tip:</strong> สำหรับ CSV ขนาดใหญ่ (มากกว่า 1GB) ใช้ <code>chunksize</code>: <code>for chunk in pd.read_csv("big.csv", chunksize=10000): process(chunk)</code> หรือใช้ <code>polars</code> ซึ่งเร็วกว่า pandas 10-50 เท่าสำหรับข้อมูลขนาดใหญ่</p></blockquote></div>'
  },
  {
    number: 4,
    slug: 'web-scraping',
    emoji: '🕷️',
    title: 'Web Scraping: ดึงข้อมูลจากเว็บด้วย requests + BeautifulSoup',
    content: '<div class="chapter-content"><h2>Web Scraping: ดึงข้อมูลจากเว็บอัตโนมัติ</h2><p>ข้อมูลที่ต้องการมักอยู่บนเว็บไซต์ต่างๆ การ Copy ด้วยมือไม่ใช่คำตอบสำหรับข้อมูลจำนวนมากหรือที่ต้อง Update บ่อย Python + <strong>requests</strong> + <strong>BeautifulSoup</strong> ให้คุณดึงข้อมูลจากเว็บได้อัตโนมัติ</p><h3>ทำความเข้าใจ HTML ก่อน</h3><p>เว็บไซต์คือไฟล์ HTML ที่ Browser แปลงเป็น Visual ที่เห็น เมื่อ Click ขวาที่ Element บนเว็บ → "Inspect" จะเห็นโครงสร้าง HTML ข้อมูลที่ต้องการมักอยู่ใน Tag เช่น <code>&lt;p&gt;</code>, <code>&lt;span&gt;</code>, <code>&lt;div class="price"&gt;</code>, <code>&lt;table&gt;</code> การรู้ว่า Element อยู่ใน Tag และ Class ไหนคือกุญแจสำคัญของ Web Scraping</p><h3>ดึงหน้าเว็บด้วย requests</h3><p><code>import requests; response = requests.get("https://example.com"); print(response.status_code)</code> Status 200 = สำเร็จ, 404 = ไม่พบหน้า, 403 = ถูก Block เพิ่ม Headers เพื่อให้ดูเหมือน Browser: <code>headers = {"User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"}; response = requests.get(url, headers=headers)</code></p><h3>Parse HTML ด้วย BeautifulSoup</h3><p><code>from bs4 import BeautifulSoup; soup = BeautifulSoup(response.text, "html.parser")</code> หา Element: <code>soup.find("h1")</code> ค้นหาแรกที่เจอ, <code>soup.find_all("div", class_="product")</code> ค้นหาทั้งหมด, <code>soup.select(".product-price")</code> ใช้ CSS Selector, <code>soup.select_one("#main-title")</code> หา Element ด้วย ID ดึง Text: <code>element.text.strip()</code> ดึง Attribute: <code>element.get("href")</code> หรือ <code>element["src"]</code></p><h3>Project: Price Monitor</h3><p>ติดตามราคาสินค้าจากเว็บ e-commerce ทุกวัน: <code>import requests, pandas as pd; from bs4 import BeautifulSoup; from datetime import date; urls = {"กาแฟ A": "https://...", "กาแฟ B": "https://..."}; results = []; for name, url in urls.items(): r = requests.get(url, headers=headers); soup = BeautifulSoup(r.text, "html.parser"); price = soup.select_one(".price").text.strip(); results.append({"สินค้า": name, "ราคา": price, "วันที่": date.today()}); df = pd.DataFrame(results); df.to_csv("price_history.csv", mode="a", header=False, index=False)</code></p><h3>Selenium สำหรับเว็บที่ใช้ JavaScript</h3><p>บางเว็บโหลดข้อมูลด้วย JavaScript (เช่น Lazy Loading, AJAX) requests ดึงได้แค่ HTML เริ่มต้น ต้องใช้ <strong>Selenium</strong>: <code>pip install selenium webdriver-manager</code> <code>from selenium import webdriver; from selenium.webdriver.chrome.service import Service; from webdriver_manager.chrome import ChromeDriverManager; driver = webdriver.Chrome(service=Service(ChromeDriverManager().install())); driver.get("https://..."); element = driver.find_element(By.CSS_SELECTOR, ".price"); print(element.text); driver.quit()</code></p><h3>Ethics & Rate Limiting</h3><p>การ Scrape ควรทำอย่างมีจริยธรรม: ตรวจสอบ <code>robots.txt</code> ก่อน (เช่น example.com/robots.txt), เพิ่ม Delay ระหว่าง Request: <code>import time; time.sleep(2)</code> เพื่อไม่ให้ Server ถูก Overload, ใช้ Session: <code>session = requests.Session()</code> เพื่อ Reuse Connection, และอย่า Scrape ข้อมูลส่วนตัวหรือข้อมูลที่มี Copyright</p><blockquote><p>⚠️ <strong>Legal Note:</strong> บางเว็บห้าม Scraping ในข้อตกลงการใช้งาน ตรวจสอบ Terms of Service ก่อนเสมอ ทางเลือกที่ดีกว่าคือดูว่าเว็บนั้นมี Official API ให้ใช้ไหม เพราะปลอดภัย เสถียร และถูกกฎหมายกว่า</p></blockquote></div>'
  },
  {
    number: 5,
    slug: 'email-automation',
    emoji: '📧',
    title: 'ส่ง Email อัตโนมัติด้วย Python: Gmail API, SMTP',
    content: '<div class="chapter-content"><h2>Email Automation: ส่งรายงานอัตโนมัติด้วย Python</h2><p>Email ยังคงเป็นช่องทางหลักในการสื่อสารธุรกิจ การส่ง Email Report อัตโนมัติทุกเช้าโดยไม่ต้องเปิดคอมเป็นงานที่ Python ทำได้ง่ายมาก บทนี้จะครอบคลุมทั้ง SMTP (วิธีง่าย) และ Gmail API (วิธีที่ปลอดภัยและ Professional)</p><h3>วิธีที่ 1: ส่ง Email ด้วย SMTP</h3><p>SMTP เป็น Protocol มาตรฐานในการส่ง Email สำหรับ Gmail ต้องเปิด "App Password" ก่อน (Account Settings → Security → 2-Step Verification → App Passwords): <code>import smtplib; from email.mime.multipart import MIMEMultipart; from email.mime.text import MIMEText; from email.mime.base import MIMEBase; from email import encoders</code></p><p><code>def send_email(to, subject, body, attachment_path=None): msg = MIMEMultipart(); msg["From"] = "your@gmail.com"; msg["To"] = to; msg["Subject"] = subject; msg.attach(MIMEText(body, "html")); if attachment_path: with open(attachment_path, "rb") as f: part = MIMEBase("application", "octet-stream"); part.set_payload(f.read()); encoders.encode_base64(part); part.add_header("Content-Disposition", f"attachment; filename={attachment_path}"); msg.attach(part); with smtplib.SMTP_SSL("smtp.gmail.com", 465) as server: server.login("your@gmail.com", "app_password"); server.send_message(msg)</code></p><h3>สร้าง HTML Email Template</h3><p>Email แบบ HTML ดูดีกว่า Plain Text มาก: <code>html_body = f"""&lt;html&gt;&lt;body&gt;&lt;h2 style="color:#2c3e50"&gt;รายงานยอดขายประจำวัน&lt;/h2&gt;&lt;p&gt;วันที่: {today}&lt;/p&gt;&lt;table border="1" cellpadding="8" style="border-collapse:collapse"&gt;&lt;tr style="background:#3498db;color:white"&gt;&lt;th&gt;สาขา&lt;/th&gt;&lt;th&gt;ยอดขาย&lt;/th&gt;&lt;/tr&gt;{rows}&lt;/table&gt;&lt;/body&gt;&lt;/html&gt;"""</code></p><h3>วิธีที่ 2: Gmail API (แนะนำ)</h3><p>Gmail API ปลอดภัยกว่าและไม่ต้องใช้ Password: 1) เปิด Google Cloud Console → สร้าง Project → Enable Gmail API 2) สร้าง OAuth 2.0 Credentials → Download JSON 3) <code>pip install google-api-python-client google-auth-httplib2 google-auth-oauthlib</code> Gmail API ให้คุณส่ง Email ในชื่อ Gmail Account จริง, อ่าน Inbox, Search Email, และ Label Email ได้</p><h3>ส่ง Email หลายคนพร้อมกัน (Mail Merge)</h3><p>สร้าง Personalized Email สำหรับลูกค้าแต่ละคน: <code>import pandas as pd; customers = pd.read_excel("customers.xlsx"); for _, row in customers.iterrows(): body = f"สวัสดีคุณ {row[\'ชื่อ\']}, ยอดใช้จ่ายของท่านในเดือนนี้คือ {row[\'ยอดรวม\']:,.0f} บาท"; send_email(row["Email"], "สรุปยอดประจำเดือน", body); print(f"Sent to {row[\'ชื่อ\']}")</code></p><h3>Sendgrid และ Mailchimp API</h3><p>สำหรับการส่ง Email Marketing จำนวนมาก (หลักพัน-แสนฉบับ) ควรใช้ Service จากผู้ให้บริการ: <strong>SendGrid</strong> ฟรี 100 Email/วัน, <strong>Mailchimp Transactional</strong>, <strong>AWS SES</strong> ถูกมาก ($0.10 ต่อ 1,000 ฉบับ) ใช้ Python SDK ของแต่ละ Service ซึ่งจัดการ Delivery, Bounce, และ Open Rate ให้</p><blockquote><p>📬 <strong>Best Practice:</strong> ทดสอบ Email Automation กับ Account ทดสอบก่อนเสมอ ตรวจสอบว่า Email ไม่ไปตก Spam (ใช้ Mail Tester), มี Unsubscribe Link สำหรับ Marketing Email, และ Log ทุก Email ที่ส่งเพื่อ Debug ได้</p></blockquote></div>'
  },
  {
    number: 6,
    slug: 'pdf-reports',
    emoji: '📄',
    title: 'สร้าง PDF Report อัตโนมัติด้วย Python',
    content: '<div class="chapter-content"><h2>สร้าง PDF Report ด้วย Python: จากข้อมูลถึงเอกสารสวยงาม</h2><p>PDF Report เป็นรูปแบบการนำเสนอข้อมูลที่เป็นมาตรฐานธุรกิจ Python มีเครื่องมือหลายอย่างในการสร้าง PDF คุณภาพสูงอัตโนมัติ ตั้งแต่ Report ง่ายๆ ไปจนถึงเอกสารที่มี Charts, Tables, และ Logo บริษัท</p><h3>Library ที่ใช้</h3><p><strong>ReportLab</strong>: ควบคุม Layout ได้ทุกอย่าง เหมาะสำหรับ Report ที่ซับซ้อน <strong>WeasyPrint</strong>: แปลง HTML เป็น PDF ง่ายและสวยมาก เหมาะสำหรับคนที่รู้ HTML/CSS แล้ว <strong>fpdf2</strong>: เรียบง่าย เหมาะสำหรับ Report พื้นฐาน <strong>matplotlib + savefig</strong>: บันทึก Chart เป็น PDF ได้โดยตรง คอร์สนี้จะใช้ WeasyPrint เพราะง่ายที่สุดและให้ผลลัพธ์สวยที่สุด</p><h3>WeasyPrint: แปลง HTML เป็น PDF</h3><p><code>pip install weasyprint</code> แนวคิดคือสร้าง HTML Template ก่อน แล้วแปลงเป็น PDF: <code>from weasyprint import HTML; html_content = """&lt;!DOCTYPE html&gt;&lt;html&gt;&lt;head&gt;&lt;style&gt;body{font-family:sans-serif;} table{width:100%;border-collapse:collapse;} th{background:#2c3e50;color:white;padding:8px;} td{padding:8px;border:1px solid #ddd;}&lt;/style&gt;&lt;/head&gt;&lt;body&gt;&lt;h1&gt;รายงานยอดขาย&lt;/h1&gt;{table_html}&lt;/body&gt;&lt;/html&gt;"""; HTML(string=html_content).write_pdf("report.pdf")</code></p><h3>ใส่ข้อมูลจาก Pandas</h3><p>แปลง DataFrame เป็น HTML Table: <code>import pandas as pd; df = pd.read_excel("sales.xlsx"); table_html = df.to_html(index=False, classes="data-table"); full_html = template.replace("{table_html}", table_html); HTML(string=full_html).write_pdf("report.pdf")</code> สามารถ Style ตารางด้วย CSS ได้เต็มที่เลย</p><h3>ใส่ Chart ใน PDF</h3><p>สร้าง Chart ด้วย matplotlib แล้วแปลงเป็น Base64 เพื่อ Embed ใน HTML: <code>import matplotlib.pyplot as plt; import base64; from io import BytesIO; fig, ax = plt.subplots(); ax.bar(df["สาขา"], df["ยอดขาย"]); buf = BytesIO(); fig.savefig(buf, format="png", dpi=150); buf.seek(0); img_base64 = base64.b64encode(buf.read()).decode(); chart_html = f\'&lt;img src="data:image/png;base64,{img_base64}" width="100%"&gt;\'</code></p><h3>สร้าง Report หลายหน้าพร้อมกัน</h3><p>ถ้ามีข้อมูล 12 สาขา สร้าง PDF แยก 12 ไฟล์อัตโนมัติ: <code>for branch in df["สาขา"].unique(): branch_df = df[df["สาขา"] == branch]; html = generate_html(branch_df, branch); HTML(string=html).write_pdf(f"report_{branch}.pdf"); print(f"Created report for {branch}")</code></p><h3>ใช้ Jinja2 สำหรับ HTML Template</h3><p>Jinja2 ทำให้จัดการ HTML Template ได้สวยงามกว่าการ String Format: <code>pip install jinja2; from jinja2 import Template; template = Template(open("template.html").read()); html = template.render(date=today, data=records, company="บริษัทตัวอย่าง"); HTML(string=html).write_pdf("output.pdf")</code> แยก Logic และ Template ออกจากกัน Code สะอาดกว่ามาก</p><h3>Merge PDF หลายไฟล์</h3><p><code>pip install pypdf; from pypdf import PdfMerger; merger = PdfMerger(); for pdf in Path("reports").glob("*.pdf"): merger.append(str(pdf)); merger.write("combined_report.pdf"); merger.close()</code> เหมาะสำหรับรวม Report รายสาขาเป็น Report รวม</p><blockquote><p>💡 <strong>Design Tip:</strong> สร้าง CSS ใน Template ให้รองรับ @page rule: <code>@page { size: A4; margin: 2cm; } @page:first { margin-top: 3cm; }</code> เพื่อจัดการ Margin และ Header/Footer แต่ละหน้าได้</p></blockquote></div>'
  },
  {
    number: 7,
    slug: 'google-sheets-py',
    emoji: '📋',
    title: 'Google Sheets API: อ่าน/เขียนด้วย Python',
    content: '<div class="chapter-content"><h2>Google Sheets API: ใช้ Python ควบคุม Sheets</h2><p>Google Sheets API เปิดให้ Python อ่าน เขียน และอัพเดต Spreadsheet ได้โดยตรง ทำให้คุณสร้าง <strong>ระบบ Dashboard ที่ Update อัตโนมัติ</strong> ดึงข้อมูลจาก Sheets มาประมวลผล หรือส่งผลลัพธ์กลับเข้า Sheets ได้โดยไม่ต้องเปิด Browser</p><h3>Setup Google Sheets API</h3><p>ขั้นตอน: 1) ไปที่ console.cloud.google.com → สร้าง Project ใหม่ 2) Enable Google Sheets API และ Google Drive API 3) สร้าง Service Account → Download JSON Key (credentials.json) 4) Share Spreadsheet กับ Email ของ Service Account (ที่มีลักษณะ xxx@project.iam.gserviceaccount.com) 5) <code>pip install gspread google-auth</code></p><h3>เชื่อมต่อและเปิด Spreadsheet</h3><p><code>import gspread; from google.oauth2.service_account import Credentials; scope = ["https://spreadsheets.google.com/feeds", "https://www.googleapis.com/auth/drive"]; creds = Credentials.from_service_account_file("credentials.json", scopes=scope); gc = gspread.authorize(creds); sh = gc.open("ชื่อ Spreadsheet"); worksheet = sh.sheet1</code> หรือเปิดด้วย URL: <code>sh = gc.open_by_url("https://docs.google.com/spreadsheets/d/...")</code></p><h3>อ่านข้อมูลจาก Sheets</h3><p>อ่านทั้งหมด: <code>data = worksheet.get_all_values()</code> คืนค่าเป็น List of Lists, อ่านเป็น Dict: <code>data = worksheet.get_all_records()</code> ใช้ Row แรกเป็น Key, อ่านเฉพาะ Range: <code>data = worksheet.get("A1:D100")</code>, อ่านเซลล์เดียว: <code>value = worksheet.acell("B2").value</code> แปลงเป็น DataFrame: <code>import pandas as pd; df = pd.DataFrame(data[1:], columns=data[0])</code></p><h3>เขียนข้อมูลลง Sheets</h3><p>เขียนทั้งหมด (Clear แล้วเขียนใหม่): <code>worksheet.clear(); worksheet.update([df.columns.values.tolist()] + df.values.tolist())</code> อัพเดตเซลล์เดียว: <code>worksheet.update_acell("B2", "ค่าใหม่")</code>, Append Row ใหม่: <code>worksheet.append_row(["2025-01-15", "สุขุมวิท", 15000])</code>, อัพเดต Range: <code>worksheet.update("A2:C10", values)</code></p><h3>สร้าง Auto-Update Dashboard</h3><p>Script ที่รันทุกชั่วโมงและ Update Sheets: <code>import schedule, time; def update_dashboard(): data = fetch_data_from_source(); df = process_data(data); worksheet.clear(); worksheet.update([df.columns.tolist()] + df.values.tolist()); print(f"Updated at {datetime.now()}"); schedule.every().hour.do(update_dashboard); while True: schedule.run_pending(); time.sleep(60)</code></p><h3>Batch Update เพื่อความเร็ว</h3><p>แทนที่จะเรียก API ทีละ Cell ควรใช้ Batch Update: <code>worksheet.batch_update([{"range": "A1:A10", "values": [[v] for v in values]}, {"range": "B1:B10", "values": [[v] for v in values2]}])</code> ลดจำนวน API Call ลงมาก ป้องกัน Rate Limit (Google Sheets API มี Limit 300 requests/minute)</p><blockquote><p>🔄 <strong>Use Case:</strong> Scrape ราคาสินค้า 100 รายการจากหลายเว็บทุก 30 นาที บันทึกลง Google Sheets Sheets นั้นเปิดใน Browser ของทีมซื้อตลอดเวลา ทีมเห็นราคาล่าสุดเสมอโดยไม่ต้องทำอะไร</p></blockquote></div>'
  },
  {
    number: 8,
    slug: 'line-bot-py',
    emoji: '💚',
    title: 'สร้าง LINE Bot ด้วย Python: เบื้องต้น',
    content: '<div class="chapter-content"><h2>LINE Bot ด้วย Python: สร้าง Chatbot สำหรับธุรกิจไทย</h2><p>LINE เป็น Platform ที่คนไทยใช้มากที่สุด การสร้าง LINE Bot ด้วย Python ทำให้คุณส่งแจ้งเตือน, ตอบ FAQ อัตโนมัติ, รับ Order, หรือส่ง Report ผ่าน LINE ได้ บทนี้จะสร้าง LINE Bot ตั้งแต่ต้นจนใช้งานได้จริง</p><h3>LINE Messaging API Setup</h3><p>ขั้นตอน: 1) ไปที่ developers.line.biz → สร้าง Provider และ Channel (Messaging API) 2) ในหน้า Channel Settings: Copy "Channel Access Token" และ "Channel Secret" 3) <code>pip install line-bot-sdk flask</code> 4) สร้าง Webhook URL (ต้องเป็น HTTPS) — ใช้ ngrok สำหรับ Testing: <code>ngrok http 5000</code></p><h3>Bot พื้นฐาน: Echo Bot</h3><p><code>from flask import Flask, request, abort; from linebot import LineBotApi, WebhookHandler; from linebot.models import MessageEvent, TextMessage, TextSendMessage; app = Flask(__name__); line_bot_api = LineBotApi("CHANNEL_ACCESS_TOKEN"); handler = WebhookHandler("CHANNEL_SECRET"); @app.route("/webhook", methods=["POST"]) def webhook(): signature = request.headers["X-Line-Signature"]; body = request.get_data(as_text=True); handler.handle(body, signature); return "OK"; @handler.add(MessageEvent, message=TextMessage) def handle_message(event): line_bot_api.reply_message(event.reply_token, TextSendMessage(text=event.message.text))</code></p><h3>ส่ง Rich Messages</h3><p><strong>Flex Message</strong> ส่ง Card สวยงาม: <code>from linebot.models import FlexSendMessage; flex = FlexSendMessage(alt_text="รายงาน", contents={"type":"bubble","body":{"type":"box","layout":"vertical","contents":[{"type":"text","text":"ยอดขายวันนี้","weight":"bold","size":"xl"},{"type":"text","text":"฿125,000","size":"xxl","color":"#1DB954"}]}}); line_bot_api.push_message("USER_ID", flex)</code> <strong>Quick Reply</strong> ปุ่มตัวเลือก: <code>from linebot.models import QuickReply, QuickReplyButton, MessageAction; quick_reply = QuickReply(items=[QuickReplyButton(action=MessageAction(label="ยอดขาย", text="ยอดขาย")), QuickReplyButton(action=MessageAction(label="สต็อก", text="สต็อก"))])</code></p><h3>Push Notification อัตโนมัติ</h3><p>ส่งแจ้งเตือนโดยไม่ต้องรอให้ User พิมพ์ก่อน: <code>line_bot_api.push_message("USER_ID_หรือ_GROUP_ID", TextSendMessage(text=f"🚨 สต็อก {product_name} เหลือ {qty} ชิ้น ต้องสั่งซื้อด่วน!"))</code> สามารถส่งไปยัง User ID, Group ID, หรือ Room ID ได้ ส่งหลายคนพร้อมกัน: <code>line_bot_api.multicast(["USER_ID_1", "USER_ID_2"], messages)</code></p><h3>Bot ที่ตอบตาม Keyword</h3><p><code>@handler.add(MessageEvent, message=TextMessage) def handle_message(event): text = event.message.text.lower(); if "ยอดขาย" in text: reply = get_sales_report(); elif "สต็อก" in text: reply = get_stock_report(); elif "ราคา" in text: reply = get_price_list(); else: reply = "พิมพ์: ยอดขาย, สต็อก, หรือ ราคา"; line_bot_api.reply_message(event.reply_token, TextSendMessage(text=reply))</code></p><h3>เชื่อม Bot กับ Google Sheets</h3><p>Bot ดึงข้อมูลจาก Sheets แบบ Real-time: <code>def get_sales_report(): sh = gc.open("Sales Data"); ws = sh.sheet1; data = ws.get_all_records(); today_sales = sum(row["ยอดขาย"] for row in data if row["วันที่"] == str(date.today())); return f"ยอดขายวันนี้: ฿{today_sales:,.0f}"</code></p><blockquote><p>🚀 <strong>Idea:</strong> สร้าง LINE Group สำหรับทีม แล้วเพิ่ม Bot เข้าไป เมื่อมียอดขายใหม่เข้ามา Bot จะแจ้งใน Group ทันที ทีมทุกคนเห็นข้อมูล Real-time โดยไม่ต้องเปิด Dashboard</p></blockquote></div>'
  },
  {
    number: 9,
    slug: 'schedule-tasks',
    emoji: '⏰',
    title: 'Schedule Tasks: รัน Script อัตโนมัติตามเวลา',
    content: '<div class="chapter-content"><h2>Task Scheduling: ให้ Python ทำงานแทนคุณตามเวลา</h2><p>Script ที่รันแค่ครั้งเดียวมีประโยชน์ แต่ Script ที่รันอัตโนมัติตามเวลาที่กำหนด นั่นคือระบบ Automation ที่แท้จริง บทนี้จะครอบคลุมทั้งการ Schedule บนเครื่องของคุณและบน Cloud</p><h3>Library: schedule</h3><p>schedule ทำให้ตั้งเวลา Task ได้ง่ายมาก: <code>import schedule, time; schedule.every().day.at("08:00").do(send_daily_report); schedule.every().monday.at("09:00").do(send_weekly_summary); schedule.every(30).minutes.do(check_stock_levels); schedule.every().hour.do(update_dashboard); while True: schedule.run_pending(); time.sleep(60)</code> แต่ข้อเสียคือต้องเปิดคอมและ Script รันอยู่ตลอด</p><h3>Windows Task Scheduler</h3><p>สำหรับ Windows: ใช้ Task Scheduler ที่มีอยู่ในระบบ สร้าง Batch File: <code>@echo off cd D:\MyScripts call venv\Scripts\activate python daily_report.py</code> เปิด Task Scheduler → Create Basic Task → กำหนดเวลา → Action: Start a program → เลือก .bat file วิธีนี้ Script รันได้แม้ไม่มีหน้าต่างเปิดอยู่ ตราบใดที่เปิดเครื่องอยู่</p><h3>Cron Jobs (Mac/Linux)</h3><p>เปิด Terminal แล้วพิมพ์ <code>crontab -e</code> เพิ่ม Cron Job: <code>0 8 * * * /path/to/venv/bin/python /path/to/script.py</code> รูปแบบ: <code>นาที ชั่วโมง วัน เดือน วันในสัปดาห์ คำสั่ง</code> ตัวอย่าง: <code>0 8 * * 1-5</code> = 8:00 น. วันจันทร์-ศุกร์, <code>*/30 * * * *</code> = ทุก 30 นาที, <code>0 9 1 * *</code> = วันที่ 1 ของทุกเดือน เวลา 9:00</p><h3>APScheduler — Advanced Scheduling</h3><p>APScheduler ทรงพลังกว่า schedule library: <code>pip install apscheduler; from apscheduler.schedulers.blocking import BlockingScheduler; scheduler = BlockingScheduler(); @scheduler.scheduled_job("cron", hour=8, minute=0) def morning_report(): send_email_report(); @scheduler.scheduled_job("interval", minutes=30) def check_prices(): monitor_prices(); scheduler.start()</code> รองรับ Cron Expression, Interval, และ One-shot Date</p><h3>Logging — ติดตาม Script ที่รัน Background</h3><p>Script ที่รัน Background ต้องมี Log เพื่อ Debug: <code>import logging; logging.basicConfig(filename="automation.log", level=logging.INFO, format="%(asctime)s - %(levelname)s - %(message)s"); logging.info("Daily report sent successfully"); logging.error(f"Failed: {str(e)}")</code> ตรวจสอบ Log ทุกวันเพื่อดูว่า Script ทำงานปกติ</p><h3>Retry Logic สำหรับ Error</h3><p>Script Automation ต้องรับมือกับ Network Error: <code>import time; def retry(func, max_attempts=3, delay=60): for attempt in range(max_attempts): try: return func(); except Exception as e: logging.error(f"Attempt {attempt+1} failed: {e}"); if attempt < max_attempts - 1: time.sleep(delay); raise Exception("Max retries exceeded")</code></p><blockquote><p>🏗️ <strong>Architecture Tip:</strong> แยก Script ออกเป็น 3 ส่วน: 1) Fetch Data (ดึงข้อมูล) 2) Process Data (ประมวลผล) 3) Output (ส่ง Email/อัพเดต Sheets) ทำให้ Debug ง่ายกว่ามาก เพราะรู้ว่า Error เกิดขึ้นที่ขั้นตอนไหน</p></blockquote></div>'
  },
  {
    number: 10,
    slug: 'ai-api-python',
    emoji: '🧠',
    title: 'เชื่อม OpenAI/Claude API ด้วย Python',
    content: '<div class="chapter-content"><h2>AI API + Python: สร้าง Automation ที่ "คิดได้"</h2><p>การเชื่อม Python กับ AI API เปิดโลกใหม่ให้กับ Automation ไม่ใช่แค่ Script ที่ทำตาม Rule แต่ System ที่สามารถ <em>วิเคราะห์ข้อความ, สรุปเอกสาร, จัดหมวดหมู่ข้อมูล</em> และทำงานอื่นๆ ที่ก่อนหน้านี้ต้องใช้คนทำ</p><h3>OpenAI API</h3><p><code>pip install openai</code> เริ่มต้น: <code>from openai import OpenAI; client = OpenAI(api_key="sk-xxx"); response = client.chat.completions.create(model="gpt-4o-mini", messages=[{"role":"user","content":"สรุปรีวิวนี้เป็น 3 ประโยค: " + review_text}]); print(response.choices[0].message.content)</code> gpt-4o-mini ราคาถูกและเร็ว เหมาะสำหรับ Automation ที่ต้องเรียก API หลายพัน Request</p><h3>Claude API (Anthropic)</h3><p><code>pip install anthropic</code> <code>import anthropic; client = anthropic.Anthropic(api_key="sk-ant-xxx"); message = client.messages.create(model="claude-3-haiku-20240307", max_tokens=1024, messages=[{"role":"user","content":"จัดหมวดหมู่ Feedback นี้ว่าเป็น Positive/Negative/Neutral: " + text}]); print(message.content[0].text)</code> claude-3-haiku เร็วและถูกมาก เหมาะสำหรับ Classification Tasks</p><h3>Use Case 1: วิเคราะห์ Review อัตโนมัติ</h3><p><code>import pandas as pd; from openai import OpenAI; client = OpenAI(); df = pd.read_excel("reviews.xlsx"); def analyze_review(text): r = client.chat.completions.create(model="gpt-4o-mini", messages=[{"role":"user","content":f"วิเคราะห์ Review นี้และตอบเป็น JSON: sentiment (positive/negative/neutral), main_issue (ถ้ามี), rating (1-5)\n\n{text}"}]); return r.choices[0].message.content; df["analysis"] = df["review"].apply(analyze_review); df.to_excel("reviews_analyzed.xlsx", index=False)</code></p><h3>Use Case 2: สรุปรายงานจาก PDF</h3><p><code>import fitz; from openai import OpenAI; def extract_pdf_text(path): doc = fitz.open(path); return "\n".join(page.get_text() for page in doc); def summarize_report(pdf_path): text = extract_pdf_text(pdf_path); client = OpenAI(); r = client.chat.completions.create(model="gpt-4o", messages=[{"role":"user","content":f"สรุปรายงานนี้เป็นภาษาไทย โดยระบุ: ประเด็นหลัก 3 ข้อ, ตัวเลขสำคัญ, และข้อสังเกต:\n\n{text[:8000]}"}]); return r.choices[0].message.content</code></p><h3>Use Case 3: Categorize และ Route Email</h3><p>ดึง Email จาก Gmail → ให้ AI จัดหมวดหมู่ (Complaint, Inquiry, Order, Spam) → Forward ไปยังทีมที่รับผิดชอบอัตโนมัติ ลด Load ของทีม Customer Service และเพิ่มความเร็วในการตอบสนอง</p><h3>Cost Optimization</h3><p>AI API มีค่าใช้จ่ายตาม Token ที่ใช้ วิธีลดค่าใช้จ่าย: ใช้ Model เล็กกว่าสำหรับงานง่าย (gpt-4o-mini, claude-haiku), จำกัด max_tokens ใน Response, Cache ผลลัพธ์ที่ Query ซ้ำ, Batch Process แทนการเรียก API ทีละรายการ, และทดสอบด้วย Input น้อยก่อน Scale</p><blockquote><p>💰 <strong>ราคาโดยประมาณ (ม.ค. 2025):</strong> GPT-4o-mini: ~$0.15/1M input tokens, Claude Haiku: ~$0.25/1M input tokens สำหรับการวิเคราะห์ 1,000 Review ยาว 200 คำ ค่าใช้จ่ายประมาณ $1-3 ซึ่งคุ้มมากเมื่อเทียบกับค่าแรงคน</p></blockquote></div>'
  },
  {
    number: 11,
    slug: 'real-projects',
    emoji: '🛠️',
    title: 'Projects จริง: Price Monitor, Report Bot, Data Cleaner',
    content: '<div class="chapter-content"><h2>Real Projects: รวมทุกทักษะสู่ระบบ Automation จริง</h2><p>ถึงเวลาสร้าง Projects จริงที่รวมทุกสิ่งที่เรียนมา แต่ละ Project แก้ปัญหาธุรกิจจริงที่ SME ไทยเผชิญ คุณสามารถนำ Code เหล่านี้ไปปรับใช้กับงานตัวเองได้เลย</p><h3>Project 1: Price Monitor System</h3><p><strong>โจทย์:</strong> ติดตามราคาสินค้า 50 รายการจาก Shopee, Lazada ทุกชั่วโมง ส่งแจ้งเตือนทาง LINE เมื่อราคาเปลี่ยนมากกว่า 5% บันทึกประวัติราคาลง Google Sheets</p><p><strong>Architecture:</strong> Scraper Module (requests + BeautifulSoup + Selenium) → Data Module (pandas + gspread) → Alert Module (LINE API) → Scheduler (APScheduler ทุก 60 นาที)</p><p><strong>Features:</strong> Dashboard ใน Google Sheets แสดงราคาล่าสุดและ % เปลี่ยนแปลง, กราฟ Price History ต่อสินค้า, Alert เมื่อราคาต่ำกว่า Target Price, Export รายงานรายสัปดาห์เป็น PDF</p><h3>Project 2: Automated Report Bot</h3><p><strong>โจทย์:</strong> ทุกวันจันทร์เวลา 9:00 น. ส่งสรุปยอดขายสัปดาห์ที่ผ่านมาทาง Email ให้ผู้บริหาร พร้อม PDF Report และ Excel ข้อมูลดิบเป็น Attachment</p><p><strong>Pipeline:</strong> 1) ดึงข้อมูลจาก Google Sheets (Sales Data ที่ทีมกรอกทุกวัน) 2) Aggregate ด้วย Pandas: ยอดรวม, Top 10 สินค้า, YoY Comparison 3) สร้าง Chart ด้วย matplotlib 4) สร้าง PDF ด้วย WeasyPrint 5) ส่ง Email พร้อม Attachment ด้วย Gmail API 6) Log ผลลัพธ์และ Retry ถ้า Fail</p><p><strong>ตัวอย่าง Email Output:</strong> Subject: "📊 Weekly Sales Report 6-12 Jan 2025", Body: HTML Email สวยงาม KPI Cards, Attachment: weekly_report_2025-01-12.pdf และ raw_data.xlsx</p><h3>Project 3: Data Cleaner Tool</h3><p><strong>โจทย์:</strong> ทีม Sales กรอกข้อมูลลูกค้าลง Excel ทุกวัน แต่มี Inconsistency เยอะมาก: ชื่อสะกดผิด, เบอร์โทรรูปแบบต่างกัน, Email ไม่ถูก Format, วันที่รูปแบบต่างกัน ต้องการ Script ที่ Clean Data อัตโนมัติก่อนนำเข้า CRM</p><p><strong>Cleaning Rules:</strong> ชื่อ: <code>.str.title().str.strip()</code> และตัดอักขระพิเศษ, เบอร์โทร: Standardize เป็น 0XX-XXX-XXXX ด้วย Regex: <code>re.sub(r"[^0-9]","",phone)</code> แล้ว Format, Email: Validate ด้วย Regex และ Lowercase, วันที่: <code>pd.to_datetime(df["วันที่"], dayfirst=True, errors="coerce")</code></p><p><strong>Output:</strong> ไฟล์ Cleaned, Log file บอกว่าแถวไหนมีปัญหาและถูกแก้ยังไง, Summary Report จำนวน Records ทั้งหมด, Cleaned, Flagged สำหรับ Manual Review</p><h3>Tips การ Deploy Project</h3><p>1) เก็บ Config ทั้งหมดใน .env file อย่า Hardcode 2) เขียน README.md อธิบายวิธีติดตั้งและใช้งาน 3) ทดสอบกับข้อมูลตัวอย่างที่ Cover Edge Cases 4) ตั้ง Alert เมื่อ Script Fail (ส่ง Email หรือ LINE) 5) Monitor ด้วย Log file และตรวจสอบทุกสัปดาห์</p><blockquote><p>🎓 <strong>Next Step:</strong> หลังจากสร้าง Projects เหล่านี้ได้ คุณมีทักษะที่สามารถ Freelance ได้ในราคา 5,000-30,000 บาท/Project ขึ้นอยู่กับความซับซ้อน Python Automation เป็น Niche ที่ยังขาดคนในตลาดไทยมาก</p></blockquote></div>'
  },
  {
    number: 12,
    slug: 'deployment',
    emoji: '☁️',
    title: 'Deploy Script ขึ้น Cloud: Railway, Render, ตั้ง Cron',
    content: '<div class="chapter-content"><h2>Cloud Deployment: ให้ Python ทำงาน 24/7 โดยไม่ต้องเปิดคอม</h2><p>Script ที่รันแค่บนเครื่องตัวเองมีข้อจำกัดชัดเจน: ต้องเปิดคอมอยู่, เสี่ยงพังถ้าไฟดับหรือ Internet หลุด การ Deploy ขึ้น Cloud ทำให้ Script รัน 24/7 อย่างน่าเชื่อถือ และ Platform ยุคใหม่ทำให้ทำได้ง่ายมาก</p><h3>เตรียม Project สำหรับ Deploy</h3><p>สิ่งที่ต้องมีก่อน Deploy: 1) <strong>requirements.txt</strong>: <code>pip freeze > requirements.txt</code> บันทึก Library ทั้งหมด 2) <strong>Procfile</strong> (สำหรับบาง Platform): <code>worker: python main.py</code> 3) ย้าย Config ทั้งหมดออกจาก Code ไปเป็น Environment Variables 4) ทดสอบบนเครื่องตัวเองอีกครั้งก่อน Deploy</p><h3>Railway — Platform ที่ใช้งานง่ายที่สุด</h3><p>Railway เป็น Platform ที่แนะนำสำหรับ Beginner: 1) สร้างบัญชีที่ railway.app (ฟรี $5 credit ต่อเดือน เพียงพอสำหรับ Light Scripts) 2) Connect GitHub Repository 3) ตั้ง Environment Variables ใน Settings 4) Deploy อัตโนมัติเมื่อ Push Code ขึ้น GitHub Railway จัดการ Server, SSL, Monitoring ให้ทั้งหมด</p><h3>Render — Free Tier สำหรับ Web Service</h3><p>Render เหมาะสำหรับ Script ที่มี Webhook (เช่น LINE Bot): 1) ไปที่ render.com → สร้าง Web Service 2) Connect GitHub Repo 3) Build Command: <code>pip install -r requirements.txt</code> 4) Start Command: <code>python main.py</code> 5) ตั้ง Environment Variables Free Tier ใน Render มีข้อจำกัดคือ Sleep หลังไม่ใช้งาน 15 นาที แต่ใช้ UptimeRobot Ping ทุก 10 นาทีเพื่อ Keep Alive</p><h3>GitHub Actions — Cron Jobs ฟรี</h3><p>สำหรับ Script ที่รันตามเวลา GitHub Actions เป็นตัวเลือกที่ดีมาก: สร้างไฟล์ <code>.github/workflows/daily.yml</code>: <code>name: Daily Report; on: schedule: - cron: "0 1 * * 1-5"; jobs: run: runs-on: ubuntu-latest; steps: - uses: actions/checkout@v3; - uses: actions/setup-python@v4; with: python-version: "3.12"; - run: pip install -r requirements.txt; - run: python daily_report.py; env: OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}</code> ฟรี 2,000 นาที/เดือน เกินพอสำหรับ Daily Scripts</p><h3>Google Cloud Run — Serverless</h3><p>สำหรับ Script ที่รันเป็นครั้งคราว (ไม่ต้องรันตลอดเวลา) Cloud Run เหมาะมาก: จ่ายเฉพาะเวลาที่รัน, Scale อัตโนมัติ, Free Tier 2 ล้าน Requests/เดือน ใช้ร่วมกับ Cloud Scheduler เพื่อตั้ง Cron Job บน Google Cloud</p><h3>Secrets Management</h3><p>อย่าเก็บ API Keys ใน Code เด็ดขาด ใช้ Environment Variables ของแต่ละ Platform: Railway → Variables Section, Render → Environment Section, GitHub Actions → Repository Secrets สำหรับ Production ขั้นสูง ใช้ Google Secret Manager หรือ AWS Secrets Manager</p><h3>Monitoring และ Alerting</h3><p>เมื่อ Script รันบน Cloud ต้องมี Monitoring: <strong>UptimeRobot</strong> ฟรี Monitor URL ทุก 5 นาที แจ้งเตือน Email/LINE เมื่อล่ม, <strong>Sentry</strong> Error Tracking ที่ Capture Exception พร้อม Context, <strong>Healthcheck.io</strong> รับ Ping จาก Script เมื่อทำงานสำเร็จ แจ้งเตือนถ้าไม่ได้รับ Ping ตามกำหนด</p><blockquote><p>🎉 <strong>ยินดีด้วย!</strong> คุณเรียนจบคอร์ส Python Automation แล้ว ตอนนี้คุณมีทักษะที่สร้าง Automation Systems ทำงานบน Cloud 24/7 ได้ ขั้นตอนต่อไป: ลงมือสร้าง Project จริงกับงานของตัวเอง และแชร์ใน Community เพื่อรับ Feedback และพัฒนาต่อ</p></blockquote></div>'
  }
];
